# 給組員：色系詞表與妝容對照表整理規格書

文件日期：2026-07-20
負責範圍：**只做資料整理，不寫演算法、不碰資料庫**
交付物：兩份 Python dictionary（`COLOR_FAMILY`、`STYLE_COLOR_FAMILY`）

---

## 0. 三十秒版本

你要做的事：

1. 拿到商品清單匯出檔（JSON）
2. 把裡面所有商品名稱掃過一遍，整理出「同一個色系有哪些不同講法」→ `COLOR_FAMILY`
3. 對照 8 個妝容，決定每個妝容的每個部位適合哪些色系 → `STYLE_COLOR_FAMILY`

你**不需要**寫比對程式、不需要連資料庫、不需要算顏色。那些後面我接。

---

## 1. 為什麼要做這個

目前的商品推薦是拿妝容關鍵字去比對商品名稱。問題是：

> 珊瑚橘適合日系，但不是每個珊瑚橘色的商品都會在名字裡寫「珊瑚橘」。

實際商品可能叫「蜜桃奶茶」「杏桃拿鐵」「元氣橘」，它們都是同一個色系，但因為字面不同，比對時全部漏掉。

**你的工作就是把這些「講法不同但其實同色」的詞收在一起。** 收得越齊，漏算越少。

### 為什麼不用電腦自動算顏色

你可能會想：直接分析商品圖片的顏色不就好了？我們評估過，不行：

- 商品圖多半是**外盒包裝**，管身的金色跟膏體的珊瑚橘沒關係
- **眼影盤是一盤 N 個色**，取平均會得到一個沒意義的灰褐色
- 爬蟲抓回來的欄位裡**根本沒有色號或色系**，只有 `name`、`brand`、`price`、`imageUrl`、`category`

所以只能從**商品名稱的文字**下手，這也是為什麼詞表整理得夠不夠齊，直接決定推薦準不準。這份工作的價值就在這裡。

---

## 2. 開工前你需要拿到的東西

跟我拿（我還在跟資料庫組要）：

- `products_export.json` — 商品清單匯出檔

拿到後先確認兩件事，回報給我：

1. `category` 欄位裡實際出現哪些值？是 `lipstick` 這種英文，還是「唇彩」中文？**照它實際長的樣子用，不要自己改成別的寫法。**
2. 商品總共幾筆？

**在拿到匯出檔之前不要開始編詞表。** 憑印象想像商品名稱會編出一堆資料庫裡不存在的詞，那是白工。

---

## 3. 交付物一：`COLOR_FAMILY`（色系詞表）

### 格式

```python
COLOR_FAMILY = {
    "色系代號": {
        "label": "中文說明，給人看的",
        "keywords": ["實際在商品名稱裡出現過的詞", "..."],
    },
}
```

### 範例（**這些只是格式示範，不是答案**）

```python
COLOR_FAMILY = {
    "coral_orange": {
        "label": "珊瑚橘／蜜桃橘調",
        "keywords": ["珊瑚", "蜜桃", "杏桃", "橘調", "蜜柑", "coral", "peach"],
    },
    "rose_brown": {
        "label": "玫瑰棕／豆沙調",
        "keywords": ["玫瑰棕", "乾燥玫瑰", "豆沙", "楓葉", "rosy"],
    },
}
```

### 規則

**必須遵守：**

1. `keywords` 裡的每一個詞，**都要真的在某個商品名稱裡出現過**。編一個「橙緋色」但沒有商品叫這個名字，是無效資料。
2. 每收一個詞，在工作表裡記下你是從哪個商品名稱看到的（見第 5 節）。之後我要驗證。
3. 色系代號用英文小寫加底線，例如 `coral_orange`。名字自己取沒關係，但整份文件要一致。
4. 同一個詞可以出現在多個色系嗎？**盡量避免。** 如果真的很模糊（例如「蜜桃粉」到底算橘還是粉），先放到你覺得最接近的那個，然後**記在第 6 節的疑問清單**，不要自己糾結太久。

**不要做的事：**

- 不要為了湊數量把不確定的詞塞進去。**漏收比錯收好修** — 漏收我看得出來，錯收會讓推薦推出奇怪的東西。
- 不要收「持妝」「霧面」「水光」這種**質地**詞。那是另一個維度，不是色系。
- 不要收品牌名、系列名。

### 大概會有幾個色系

彩妝常見色系大約 8~15 個。如果你整理出 40 個，代表切太細了；如果只有 3 個，代表切太粗。中間值比較合理，但**以商品實際情況為準**，不用硬湊。

---

## 4. 交付物二：`STYLE_COLOR_FAMILY`（妝容對照表）

### 妝容清單（固定 8 個，不可增減）

這 8 個妝容名稱來自 `Ollama_suggestion.py` 的 `MAKEUP_DATABASE`，**必須一字不差照抄**，多一個空格或改成「日雜清透妝」都會對不上：

| 妝容名稱 | 系統既有的色調描述（`tone` 欄位） |
|---|---|
| `日常自然妝` | 暖杏蜜桃色系 |
| `Soft Baddie` | 肉桂奶油與夢幻藍色系 |
| `韓系亞裔` | 低飽和粉棕系 |
| `日雜清透` | 暖杏蜜桃色系 |
| `千金` | 香檳粉金系 |
| `港風` | 復古紅棕色與冷灰色 |
| `病嬌` | 煙燻玫瑰色與淡粉紫色 |
| `男士白開水` | 裸色系、低彩度杏棕 |

**右邊那欄是你最重要的依據。** 這是系統裡已經寫好的妝容色調設定，你的對照表要跟它一致。例如「港風」寫的是復古紅棕與冷灰，那它的唇彩就不該對到蜜桃粉。

### 格式

```python
STYLE_COLOR_FAMILY = {
    "妝容名稱": {
        "lip":   ["色系代號", ...],
        "blush": [...],
        "eye":   [...],
        "brow":  [...],
    },
}
```

### 範例（**格式示範，不是答案**）

```python
STYLE_COLOR_FAMILY = {
    "港風": {
        "lip":   ["classic_red", "brick_red"],
        "blush": ["cool_brown"],
        "eye":   ["cool_grey", "charcoal_brown"],
        "brow":  ["dark_brown"],
    },
}
```

### 規則

1. 每個色系代號**必須在 `COLOR_FAMILY` 裡存在**。寫了 `classic_red` 但詞表裡沒有這個色系，程式會直接報錯。
2. 一個部位可以對到多個色系，這很正常（大部分妝容的唇彩都有 2~3 個可選色系）。
3. 某個部位如果該妝容沒有明確主張，**留空陣列 `[]`**，不要硬填。例如「男士白開水」可能就沒有腮紅。
4. 8 個妝容全部都要有，不能只做 5 個。

### 眼影跟眉筆的特別說明

- **`eye`（眼影）**：眼影盤是多色組合，很難只用一個色系描述。這裡填**這盤眼影的整體色調傾向**（暖棕調、冷灰調），不是列出每一格的顏色。抓大方向就好。
- **`brow`（眉筆）**：眉筆的顏色選擇很少，實務上大概就黑、灰、深棕、淺棕、卡其這幾種。這格反而最好填，通常 1~2 個色系就夠。

---

## 5. 你的工作表（跟交付物一起給我）

編詞表的時候同步記錄，用 CSV 或 Excel 都可以：

| 商品名稱 | category | 你判定的色系 | 抓到的關鍵詞 | 備註 |
|---|---|---|---|---|
| 蜜桃奶茶唇釉 | lipstick | coral_orange | 蜜桃 | |
| 楓葉紅唇膏 | lipstick | rose_brown | 楓葉 | 也有點偏紅，不太確定 |

**這份工作表跟 dictionary 一樣重要。** 之後如果推薦結果怪怪的，我要靠它回推是哪個詞判錯了。沒有工作表我沒辦法 debug。

---

## 6. 交付時請一併回報

1. **完全歸不到任何色系的商品** — 列出來。可能是命名太抽象（「夢遊仙境 #03」），這種我另外處理，不是你的錯。
2. **你猶豫過的判斷** — 例如「蜜桃粉到底算 coral 還是 pink」。列出來我們一起決定，不要自己硬決定然後不講。
3. **某個妝容找不到適合商品的部位** — 例如整個資料庫沒有適合病嬌的唇色。這是資料缺口，要回報給採購/爬蟲組，不是你去編一個。

---

## 7. 絕對不要做的事

- ❌ **不要新增資料庫裡不存在的商品或妝容。** 妝容就是那 8 個。
- ❌ **不要改資料庫。** 這次全程唯讀，你只會拿到一份匯出檔。
- ❌ **不要猜商品的顏色。** 只從名稱文字判斷。看到「YSL 12號」不知道是什麼色，就放進第 6 節的清單，不要去 Google 查了填進來 — 那個資料不在我們系統裡，填了會造成日後對不上。
- ❌ **不要改妝容名稱的寫法。**

---

## 8. 驗收標準

我收到之後會跑這幾項檢查，你可以先自己確認：

1. `STYLE_COLOR_FAMILY` 用到的每個色系代號，都存在於 `COLOR_FAMILY`
2. 8 個妝容全部都在，名稱與 `MAKEUP_DATABASE` 完全一致
3. `COLOR_FAMILY` 的每個 keyword，至少能在商品清單裡命中 1 筆商品
4. 沒有商品同時被兩個互斥色系命中（例如同時是 `classic_red` 和 `peach_pink`）
5. 工作表的商品數 = 匯出檔的商品數

第 3 項最容易掛。**每收一個詞就順手在匯出檔搜一下**，比最後一次全部檢查省事很多。

---

## 9. 有問題直接問

特別是這兩種情況，不要自己想太久：

- 不確定某個商品算哪個色系 → 丟進疑問清單，繼續往下做
- 覺得色系該切更細或更粗 → 直接跟我討論，這個沒有標準答案

這份工作的難點不在技術，在**判斷的一致性**。同一種色，你在第 10 筆跟第 200 筆要判成一樣的結果。所以**工作表要邊做邊填**，隨時可以回頭看自己前面是怎麼判的。
