# 給資料庫端 — 商品清單 API 與會員點數／打卡問題回報

**日期：** 2026-08-02
**提出方：** DECORATE ME 前端 + AI Gateway
**對象：** 會員／商品資料庫後端維運者
**實測對象：**
`PRODUCT_DATABASE_URL` = `MEMBER_DATABASE_URL` = `https://regional-components-lowest-ridge.trycloudflare.com`
（兩個環境變數目前指向同一個 host，見 Cloud Run `ai-gateway` 的設定）
**實測時間：** 2026-08-02 04:3x～04:5x（台北時間）
**實測管道：** 經 `https://decorate-me.web.app/product-api/...` 與直接打通道，兩條都試過，結果一致

---

## 0. 這份文件怎麼讀

每一項都附**可以自己重跑的指令與我們拿到的實際回應**。我們不憑印象回報，也不希望你們憑印象修。

| 標記 | 意思 |
| --- | --- |
| 🔴 | 使用者現在就踩得到，或會靜默產生錯誤資料 |
| 🟡 | 目前有繞道，但繞道很脆弱 |
| 🟢 | 契約確認，暫時不影響功能 |

分成兩塊：

- **第 1～3 節：商品清單 API**（`/api/products`）
- **第 4 節：會員點數與打卡**（`/api/members/{email}/points`、`/check-in`）

---

## 1. 🔴 `limit` 被壓在 100，後台只看得到 1041 筆裡的 100 筆

### 現象

管理員在後台「商品管理」列表只看得到 100 筆，但頁面上的總數顯示 1041。

### 實測

```
GET /api/products?limit=200
→ items 100 筆，total 1041，nextCursor "100"

GET /api/products?limit=100
→ items 100 筆，total 1041，nextCursor "100"
```

送 `limit=200` 跟送 `limit=100` 回一樣多。`limit` 只有「不超過 100」這半邊生效。

### 但翻頁是好的

```
GET /api/products?limit=100&cursor=100   → items 100 筆，第一筆 id=101，nextCursor "200"
GET /api/products?limit=100&cursor=1000  → items  41 筆，第一筆 id=1001，nextCursor 空
```

`cursor` 完全正常，也會在最後一頁正確地停下來。

### 順帶一提：`offset` 沒有作用

```
GET /api/products?limit=100&offset=100   → 第一筆仍是 id=1
```

如果這是有意的（只支援 `cursor`），請在規格書上寫明，我們就不會再試。

### 希望你們怎麼修

擇一即可：

- **A**：讓 `limit` 真的吃到（至少放寬到 500 或 1000），或
- **B**：維持上限 100，但在〈商品搜尋管理與推薦演算法整合規格書 2026-07-17〉§6.4 把「`limit` 上限為 100」寫清楚。

我們這邊已經改成照著 `nextCursor` 翻頁（見第 5 節），所以 **B 也可以接受**——只要別讓下一個接的人再踩一次。

---

## 2. 🔴 `type` 參數完全不生效，後台的商品類型下拉選什麼都一樣

### 現象

後台「商品類型」下拉選「唇彩」、選「腮紅」、選「眼影」，列表內容**完全一樣**。

### 實測

```
GET /api/products?type=lipsticks&status=active&limit=200
→ 100 筆，全部 type=lipsticks

GET /api/products?type=blushes&status=active&limit=200
→ 100 筆，全部 type=lipsticks        ← 跟上面一模一樣
```

第二個請求要的是腮紅，回來的是唇彩。因為它根本不是在過濾，只是把資料庫的前 100 筆原樣回來（前 378 筆剛好都是唇彩）。

我們也試過其他可能的參數名，全部無效——回應退回「忽略所有參數」的那個分支，一次回 1041 筆：

```
GET /api/products?type=blushes        → 1041 筆（未過濾）
GET /api/products?category=腮紅        → 1041 筆（未過濾）
GET /api/products?item_type=blushes   → 1041 筆（未過濾）
GET /api/products?cat=blushes         → 1041 筆（未過濾）
```

### 附帶發現：回應形狀會跟著參數變

- **帶** `limit` / `q` / `status` → `{ ok, items, products, total, nextCursor, query }`
- **完全不帶**或只帶不認得的參數 → `{ products: [...1041 筆] }`，**沒有** `total`、沒有 `nextCursor`

同一支端點兩種形狀，我們前端得同時認 `items` 和 `products`。建議統一，但這件事本身不急。

### 對照：`q` 是好的

```
GET /api/products?q=MAC&limit=100  → total 274（不是 1041）
```

關鍵字搜尋有實作、也有正確縮小結果集。所以缺的就只有 `type` 這一個。

### 希望你們怎麼修

實作 `type` 的伺服器端過濾，值就用你們自己 `type` 欄位的那 8 個 slug：

```
foundations  eyeshadows  eyeliner_mascara  lipsticks
blushes      eyebrows    contouring        highlighters
```

過濾後 `total` 請回**過濾後的筆數**（像 `q=MAC` 那樣回 274），不要回 1041。

---

## 3. 🟢 `category` 回中文、`type` 回英文 slug —— 請確認這是定案

### 現況

```json
{ "id": 1, "category": "唇彩", "type": "lipsticks", "name": "Za 午後花園柔霧唇膏 - 櫻桃陷阱" }
```

`category` 是**中文顯示名**，`type` 是**英文 slug**。

### 這件事害我們壞了一段時間

我們前端原本把 `category` 當成 slug 拿去查表，中文查不到就掉進預設值「底妝」，於是 **1041 筆商品全部被標成底妝**：前台除了底妝以外每個分類都是空的，後台分類欄也全是底妝。這是我們自己的 bug，**已經修好並上線**（見第 5 節），不需要你們做什麼。

### 但有一件事要請你們確認

請確認 **`type` 是穩定的機器可讀鍵、`category` 只是顯示字串**，而且未來只會動 `category` 的中文措辭、不會動 `type` 的值。

我們現在用 `type` 來組：

- 單品 API 路徑 `/api/product/{type}/{id}`
- 收藏同步的 `item_type`
- 前端商品 id `api-{type}-{id}`

`type` 的值一改，這三件事會同時斷掉，而且**跟上次 id 重編號一樣不會有任何錯誤訊息**。

如果 8 個 slug 的清單有異動計畫，請先告知。

---

## 4. 🔴 會員打卡與點數：資料庫裡查不到紀錄

### 現象

會員註冊、登入、按打卡，畫面顯示「打卡成功，獲得 10 點」，點數也確實增加。但你們在資料庫裡看，**沒有任何點數或打卡紀錄**。

### 我們這邊先排除掉的

**排除一：不是連線沒通。** 登入本身就要打會員資料庫，過往的串接經驗也是「資料庫沒連上就登不進去」。現在會員登得進去，所以這條線是通的，`members` 那張表也確實收到了註冊的帳號。**「整個資料庫沒接上」可以直接排除。**

**排除二：不是 AI Gateway 攔住或偽造。** Gateway 對 `member-database` 是純代理（`ai_gateway.py:119-147`），只有一份路徑白名單，沒有任何本地儲存或 mock。`points`、`check-in` 兩條都在白名單裡：

```python
r"api/members/[^/]+/points",
r"api/members/[^/]+/check-in",
```

而且通道是活的——商品 API 走同一個 host，剛剛才抓回 1041 筆商品。

### 我們這邊確認有問題的部分（已修，見第 5 節）

前端有一條**靜默退回瀏覽器 localStorage** 的路：遠端打卡不管回 401、404、500 還是斷線，都會掉進本機的 demo 模組，然後跳出跟成功**一模一樣**的「打卡成功，獲得 10 點」。點數顯示同理——先畫 localStorage 的數字，API 成功才蓋掉，失敗就一直留著本機那個數字。

所以**至少有一部分**的「看得到點數」是假的，那是我們的錯。

### 但有一段只有你們查得到

從外面沒辦法確認 `/points` 和 `/check-in` 到底有沒有實作，因為你們對 `/api/members/*` 有一層**全域 401 守衛**，它比路由更早執行：

```
GET /api/members/xxx/definitely-not-a-route   → 401
GET /api/members/xxx/zzz-bogus                → 401
GET /api/members/xxx/points                   → 401     ← 跟亂打的路徑完全一樣
GET /api/bogus-top-level                      → 404     ← 對照組，這裡才會 404
```

亂打的子路徑跟真端點回一樣的 401，所以**401 完全蓋掉了「有沒有這個路由」的差別**，我們無法再往下判斷。

### 所以問題範圍縮到這裡

連線是通的、`members` 表有資料、Gateway 沒攔。那剩下的可能只有兩個：

- **(a) 點數／打卡寫到了另一張表**，你們查的不是那張；或
- **(b) 這兩支端點根本沒實作**，前端拿到 401/404 之後靜默退回 localStorage（我們那條 bug），所以從頭到尾沒有任何寫入發生。

從外面沒辦法分辨 (a) 和 (b)，只有你們查得到。

### 請你們確認四件事

1. **`POST /api/members/{email}/check-in` 有實作嗎？** 如果沒有，請補；如果有，請告訴我們回傳格式（我們前端讀 `awarded` 或 `points`）。
2. **`GET /api/members/{email}/points` 有實作嗎？** 我們預期 `{ balance, lifetime, transactions[] }`。
3. **點數與打卡各自寫在哪張表？** 表名給我們一下，好對照第 (a) 種可能。如果有 `points` / `point_ledger` / `check_ins` 這類表，請確認註冊時會不會一併建立對應的初始列——如果註冊只寫 `members`、點數表要等第一次打卡才建列，而打卡又沒實作，那就會剛好長成現在這樣。
4. **帶著有效的會員 session 打一次 check-in，資料庫裡會不會多一列？** 這是唯一能一次分辨「前端沒送到」還是「後端沒寫入」的測試。

### 補充：後台名單沒有假資料

後台的會員名單**沒有任何 demo fallback**（`js/router.js:4226`，連不上就直接顯示「會員資料庫目前無法讀取」）。所以後台列出來的每一個會員，都是 `GET /api/members` 從你們資料庫真的回來的——這一點跟「登入得進去」互相印證。

後台顯示的**點數數字**也同理：它只有兩個來源，會員清單 API 的 `points` 欄位，或 `GET /api/members/{email}/points`，兩條都是打資料庫，不可能是 localStorage。**只有會員自己頁面上看到的那個數字才可能是本機的。**

這代表如果後台跟會員端顯示的點數**不一樣**，差額就是累積在 localStorage 裡、從來沒送出去的那些。這也是你們可以拿來驗證的一個切入點。

---

## 5. 我們這邊已經做完的（2026-08-02 已上線）

| 項目 | 處置 |
| --- | --- |
| 商品分類全變底妝 | 已修：改成 `type` slug 優先，中文 `category` 只要是已知的 8 個分類就直接採用。8 個分類的筆數已與你們 `type` 欄位的分佈完全吻合 |
| `apiType` 變成中文 | 已修：一律輸出英文 slug，連帶修好收藏跨裝置同步的 `api-{type}-{id}` 對應與唇彩的以色找色 |
| 後台編輯表單預帶「底妝」 | 已修（同上）。先前從編輯表單存檔會把商品 type 改寫成 `foundations`，這是靜默的資料破壞 |
| `limit` 上限 100 | 已繞道：後台自己照 `nextCursor` 翻頁把 1041 筆全撈完 |
| `type` 不生效 | 已繞道：類型過濾改在前端做。`type` 仍照送，你們補上伺服器端過濾後這段會自動變成沒作用的複驗，不需要我們再改一次 |
| 打卡靜默退回 localStorage | **已修並上線**：遠端失敗就明講失敗（401／404／其他分別給不同訊息），不再自己在瀏覽器裡發點數。按鈕**維持可按**，你們補好端點後不必重整就能直接重試 |
| 點數顯示是本機數字 | 已標示：讀不到資料庫餘額時，標籤改成「會員點數（本機）」，不再讓瀏覽器裡的數字冒充已同步的餘額 |

> **這代表什麼**：從 2026-08-02 起，會員端不會再產生「假的成功打卡」。在你們補好 `/check-in` 之前，會員按打卡會看到明確的錯誤訊息——這是刻意的，我們寧可讓它顯示失敗，也不要再讓點數只累積在瀏覽器裡。

驗證方式：把修好的正規化函式抽出來、餵**真實的 1041 筆線上資料**跑過一遍：

```
cat 分佈:     唇彩 378 / 腮紅 223 / 眼線·睫毛 120 / 眉毛彩妝 120 /
              底妝 68 / 眼影 60 / 打亮 44 / 修容 28
apiType 分佈: lipsticks 378 / blushes 223 / eyeliner_mascara 120 / eyebrows 120 /
              foundations 68 / eyeshadows 60 / highlighters 44 / contouring 28
apiType 為 null: 0
```

與你們 `type` 欄位的實際分佈逐項相符。

---

## 6. 優先順序建議

1. **第 4 節**（點數／打卡）——使用者現在會看到假的成功訊息，而且資料真的在流失
2. **第 2 節**（`type` 不生效）——後台管理員完全沒辦法按類型找商品
3. **第 3 節**（確認 `type` 值穩定）——回一句話就好，但避免下一次靜默斷線
4. **第 1 節**（`limit` 上限）——我們已經繞過去了，補文件即可
