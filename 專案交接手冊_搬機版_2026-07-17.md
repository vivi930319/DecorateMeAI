# Decorate Me 專案交接手冊（搬機版）

文件日期：2026-07-17  
目前工作區：`C:\Users\isach\PycharmProjects\PythonProject12`  
目前 Git 分支：`Isa`  
GitHub：`https://github.com/vivi930319/DecorateMeAI.git`

這份文件是把專案搬到另一台電腦時的單一入口。請先讀完「重要警告」與「搬機流程」，再啟動服務。

---

## 1. 專案一句話說明

Decorate Me 是一套 AI 美妝系統：使用者上傳正面／側面照片，系統分析臉型、五官、膚色與四季型，再提供 Ollama 妝容建議、AI 妝容渲染、商品推薦、妝容收藏、會員管理與後台資料庫操作。

系統由以下部分組成：

```text
使用者瀏覽器／未來 iOS App
        │
        ├── Firebase Hosting 前端
        │       ├── 臉部分析 API
        │       ├── Ollama 文字建議 API
        │       ├── Render API
        │       └── 會員／商品資料庫 API
        │
        ├── AI Gateway（會員 Bearer Token、API 代理、Cloud Run IAM）
        ├── Face BASIC／PRO（FastAPI、InsightFace、MediaPipe）
        ├── Ollama Suggestion（妝容文字建議）
        ├── Replicate Render（AI 試妝）
        ├── Firestore（非同步 Job）
        ├── GCS（渲染圖永久網址）
        └── 會員／商品資料庫（外部服務）
```

---

## 2. 搬機前最重要的警告

### 2.1 不要只 clone GitHub 就以為資料完整

目前根目錄 `.gitignore` 是「全部忽略、少數白名單放行」的設定。現在 Git 追蹤的前端檔案只有少數幾個，以下類型目前可能沒有進 GitHub：

- `firebase-hosting-full/public/pages/` 的多數頁面
- `firebase-hosting-full/public/js/image-worker.js`
- 前端圖片、字型素材與本機資料
- `firebase-hosting-full/public/config.local.js`
- 本文件建立前的其他被忽略檔案

所以搬機要採用以下其中一種方式：

1. **最安全：**把整個 `PythonProject12` 資料夾用外接硬碟或加密壓縮檔搬到新電腦。
2. **GitHub + 完整資料夾：**先 clone GitHub，再從舊電腦補回被忽略的前端、資料集、模型與設定檔。
3. **長期改善：**逐一確認應該進版控的前端程式與文件，取消不必要的忽略規則，再 commit／push。

不要把整個 `.venv` 當成可攜式環境；新電腦應重新建立虛擬環境。

### 2.2 `.env`、API Key、Token 不要放進 GitHub 或公開壓縮檔

目前專案曾經在前端公開設定中放過服務金鑰。這些金鑰應視為已曝光，搬機時不要直接複製到公開 GitHub；正式處理順序是：

1. 重新產生／輪替 FACE、RENDER、SUGGESTION、Replicate、Gateway 金鑰。
2. 新電腦用私密方式建立 `.env`。
3. 前端的 `config.local.js` 只放本機必要設定；不要把真正的秘密提交到 Git。
4. 長期應改成前端只呼叫 AI Gateway，AI 核心服務改 Cloud Run 私有。

本文件只列環境變數名稱與 placeholder，不列出目前實際金鑰。

### 2.3 臉部照片與資料集屬於敏感資料

`data/`、`celeba_raw/`、`facing_ba/`、`My_project_data/` 等資料可能包含人臉照片或標註。搬移時應使用加密硬碟／加密壓縮檔，並確認維修人員不會取得不必要的個資資料。

---

## 3. 目前版本與 Git 狀態

盤點日期：2026-07-17。

```text
分支：Isa
遠端：origin = https://github.com/vivi930319/DecorateMeAI.git
最新 commit：6001c73 docs: define member deletion database contract
工作區未追蹤檔：tools/__init__.py
```

最近重要修改：

- `b578bad`：前台 saved look 與會員資料庫刪除同步
- `55736b7`：會員刪除資料庫契約
- `a9412e4`：管理員刪除會員收藏妝容規格
- `5b88f97`：後台收藏卡片放大
- `6001c73`：會員刪除與資料庫端契約文件

搬機前可在舊電腦執行：

```powershell
cd "C:\Users\isach\PycharmProjects\PythonProject12"
git status --short
git log -8 --oneline --decorate
git bundle create ..\\DecorateMeAI-Isa-20260717.bundle --all
```

`git bundle` 只包含已追蹤的 Git 歷史，不包含 `.env`、被忽略的前端素材、資料集與模型；完整資料仍要另外複製。

---

## 4. 根目錄結構與用途

### 4.1 正式服務程式

| 檔案 | 用途 |
|---|---|
| `Face_analyzer_BASIC.py` | BASIC 單張正面照分析、姿態偵測、非同步分析 Job |
| `Face_analyzer_PRO.py` | PRO 正面＋側面分析與非同步 Job |
| `Ollama_suggestion.py` | 依 `analysisPackage` 產生文字妝容建議 |
| `replicate_render.py` | 呼叫 Replicate／OpenAI provider、上傳 GCS、刪除渲染圖 |
| `replicate_render_api.py` | Render API、配額、去重、Job、GCS 刪除端點 |
| `ai_gateway.py` | 會員登入、短期 Bearer Token、API 代理、Cloud Run IAM 上游呼叫 |
| `analysis_package.py` | 前後端共用的分析結果資料結構與轉換 |
| `job_store.py` | Firestore Job 儲存與本機 fallback |
| `api_errors.py` | 統一錯誤格式與 request ID |
| `dev_server_utils.py` | 本機啟動、CORS、服務設定 |
| `cloud_start.py` | Cloud Run 依 `SERVICE_NAME` 啟動 BASIC／PRO／suggestion |
| `face_roi.py`、`basic_roi_shadow.py` | BASIC ROI 裁切與 shadow 模型推論 |

### 4.2 容器與部署

| 檔案 | 用途 |
|---|---|
| `Dockerfile` | BASIC／PRO／suggestion 共用映像基礎 |
| `Dockerfile.render` | Render Cloud Run 映像 |
| `Dockerfile.gateway` | AI Gateway Cloud Run 映像 |
| `docker-compose.yml` | 本機 BASIC、PRO、suggestion、可選 Render |
| `cloudbuild.gateway.yaml` | 建立 Gateway 映像 |
| `cloudbuild.render.yaml` | 建立 Render 映像 |
| `deploy_render_cloudrun.ps1` | 建置、部署 Render、健康檢查、可選套用 GCS lifecycle |
| `gcs-lifecycle.json` | `rendered/` 前綴圖片 30 天後刪除 |
| `.dockerignore`、`.gcloudignore` | 控制 Docker／Cloud Build 上傳內容 |

### 4.3 測試

| 檔案 | 用途 |
|---|---|
| `ai_gateway_test.py` | Gateway Bearer、API Key、路徑白名單、Header 清理測試 |
| `render_api_test.py` | Render 輸入、Job、去重、GCS URL、錯誤格式測試 |
| `backend_smoke_test.py` | 連線實際 BASIC／PRO 服務的整合檢查 |
| `ollama_suggestion_smoke_test.py` | 連線實際 Ollama suggestion 服務 |
| `firebase-hosting-full/public/frontend_smoke_check.js` | 前端 API 設定、方法、資料包結構檢查 |

### 4.4 資料、模型與訓練工具

| 目錄 | 用途 | 搬機必要性 |
|---|---|---|
| `models/basic_features_roi/` | BASIC runtime 需要的 `.onnx` 與 class JSON | 執行 BASIC 建議保留 |
| `data/` | 分析資料、ROI cache、訓練／驗證資料 | 要繼續訓練才需要 |
| `celeba_raw/` | 原始資料集 | 要重新訓練才需要 |
| `1008/`、`facing_ba/`、`json_yolo_project/`、`nose_shape_side/` | 臉部／鼻型／資料處理資料 | 研究用途 |
| `render_model_ab_test/` | 渲染模型測試資料 | 渲染研究用途 |
| `tools/` | 標註、分類、資料整理、模型實驗腳本 | 要繼續研究才需要 |

---

## 5. 服務、Port 與 API

### 5.1 本機服務

| 服務 | 本機 Port | 啟動方式 | 主要用途 |
|---|---:|---|---|
| 前端 | 5500 | `firebase-hosting-full/public/dev_server.py` | 靜態前端與 no-cache |
| Face BASIC | 8001 | `python Face_analyzer_BASIC.py` | 正面臉部分析、pose |
| Face PRO | 8002 | `python Face_analyzer_PRO.py` | 正面＋側面分析 |
| Ollama Suggestion | 8010 | `python Ollama_suggestion.py` | 妝容文字建議 |
| Render | 8020 | Docker Compose 或手動 Uvicorn | AI 試妝 |
| AI Gateway | 8015 | `python ai_gateway.py` | 本機 Gateway 測試 |
| 測試用 product | 8030 | 外部資料庫或 mock | 商品清單／推薦 |
| 測試用 member DB | 8040 | 外部資料庫或 mock | 會員 API |

Cloud Run 容器通常使用 `PORT=8080`，不要把本機 Port 與 Cloud Run Port 混用。

### 5.1.1 目前設定檔最後記錄的外部 URL

以下是 2026-07-17 盤點 `public/config.local.js` 時看到的 URL；Cloudflare Tunnel 可能因重啟失效或改名，搬機後要先以 `/health` 驗證，不要把它們視為永久網址：

```text
Face BASIC       https://face-basic-258021445391.asia-east1.run.app
Face PRO         https://face-pro-258021445391.asia-east1.run.app
Render           https://replicate-render-eu5pq7c53a-de.a.run.app
Suggestion      https://morning-deeper-kick-medium.trycloudflare.com
Member/Product  https://debut-exemption-lounge-depending.trycloudflare.com
```

### 5.2 Face API

```text
GET  /health
POST /v1/face/analyze/basic
POST /v1/face/pose
POST /v1/face/analyze/pro
POST /v1/face/jobs/basic
POST /v1/face/jobs/pro
GET  /v1/face/jobs/{jobId}
GET  /v1/face/jobs/{jobId}/result
```

分析上傳使用 multipart file。非同步 Job 建立後回傳 `jobId` 與 `resultToken`；查詢時要帶 `X-Job-Token: <resultToken>` 或 `result_token` query parameter。

### 5.3 Ollama API

```text
GET  /health
POST /suggest
POST /suggest/stream
```

主要輸入包含 `analysisPackage`、`faceAnalysis`、`style`、`userNote`。目前 Ollama 服務通常透過 Cloudflare Tunnel 指向另一台電腦上的 Ollama；Tunnel URL 可能隨重啟改變，必須更新設定。

### 5.4 Render API

```text
GET    /health
POST   /render
POST   /render/jobs
GET    /render/jobs/{jobId}
DELETE /render/jobs/{jobId}
```

渲染流程：前端傳原圖與白名單 `styleId`，後端建立 prompt，呼叫 Replicate 的 `openai/gpt-image-2`，再上傳 GCS `rendered/` 前綴。資料庫只應保存永久圖片 URL，不應保存 `replicate.delivery` 臨時 URL。

### 5.5 AI Gateway

```text
GET  /health
POST /auth/login
GET|POST|DELETE /{service}/{path}
```

正式流程應為：

```text
Frontend → AI Gateway → Cloud Run IAM private Face／Render
```

目前程式仍有部分前端直接呼叫 Face／Render／Ollama 並帶公開設定中的 API Key，這是搬機後第一個應優先修正的架構問題。

---

## 6. 前端與會員資料庫契約

完整資料庫規格請看：

- `MEMBER_DATABASE_INTEGRATION_SPEC.md`
- `給資料庫端_會員刪除與妝容收藏刪除更新規格_2026-07-16.md`
- `firebase-hosting-full/會員資料庫接入規格書.md`

前端主要呼叫：

```text
POST   /api/login
POST   /api/register
POST   /api/send-otp
POST   /api/verify-otp
GET    /api/members
PATCH  /api/members/{email}
DELETE /api/members/{email}
GET    /api/members/{email}/points
GET    /api/members/{email}/check-in
POST   /api/members/{email}/check-in
GET    /api/members/{email}/tasks
POST   /api/members/{email}/tasks/{taskId}/claim
POST   /api/members/{email}/theme-shop/{themeId}/redeem
GET    /api/members/{email}/saved-looks
POST   /api/members/{email}/saved-looks
DELETE /api/members/{email}/saved-looks/{id}
GET    /api/products
POST   /api/products
PATCH  /api/products/{id}
DELETE /api/products/{id}
```

會員回應至少要有 `name`、`email`、`level`、`role`、`status`、`allowedPages`；若有配額，使用 `renderQuota.dailyLimit`、`remaining`、`resetAt`。前端後台權限只能相信資料庫回傳欄位，不要用 Email 名稱推論 admin。

saved look 建議 payload：

```json
{
  "style": "daily",
  "beforeImageUrl": "https://storage.googleapis.com/...",
  "afterImageUrl": "https://storage.googleapis.com/...",
  "analysisSummary": {}
}
```

資料庫刪除規則：使用者只能刪自己的 saved look；管理員可刪指定會員資料但後端必須驗證 admin；前端只有收到成功回應後才移除本機快取；GCS 檔案由後端或非同步清理工作處理。

`DELETE /api/members/{email}` 必須只允許 admin，並可靠處理 `saved_looks`、`tryon_records`、`favorites`、`cart`、`points`、`check-in`、`tasks` 等關聯資料。前端不能假刪除。

---

## 7. 環境變數與秘密設定

根目錄目前 `.env` 出現過的名稱如下，搬機要自己重新建立，不要從本文件複製真正值：

```text
IMAGE_PROVIDER
REPLICATE_API_TOKEN
REPLICATE_MODEL
OPENAI_API_KEY
OPENAI_IMAGE_MODEL
OLLAMA_BASE_URL
OLLAMA_MODEL
MEMBER_DATABASE_URL
FACE_API_KEY
RENDER_API_KEY
SUGGESTION_API_KEY
GATEWAY_FACE_API_KEY
GATEWAY_RENDER_API_KEY
UPSTREAM_FACE_API_KEY
UPSTREAM_RENDER_API_KEY
FACE_BASIC_URL
FACE_PRO_URL
RENDER_URL
GATEWAY_SESSION_SECRET
CORS_ORIGINS
APP_ENV
REQUIRE_EXPLICIT_CORS
GCS_RENDER_BUCKET
GCS_RENDER_RETENTION_DAYS
RENDER_RATE_LIMIT_MAX_REQUESTS
RENDER_RATE_LIMIT_WINDOW_SECONDS
RENDER_QUOTA_MAX_REQUESTS
RENDER_QUOTA_WINDOW_SECONDS
RENDER_JOB_TIMEOUT_SECONDS
RENDER_JOB_RETENTION_SECONDS
RENDER_JOB_MAX_COUNT
RENDER_DEDUP_TTL_SECONDS
RENDER_DURABLE_DEDUP_ENABLED
```

本機 `.env` 只放在安全位置：

```dotenv
APP_ENV=development
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=gemma3
MEMBER_DATABASE_URL=https://<member-db-url>
FACE_API_KEY=
SUGGESTION_API_KEY=
RENDER_API_KEY=
REPLICATE_API_TOKEN=<在安全位置填入>
```

---

## 8. 新電腦安裝與啟動流程

### 8.1 建議安裝

- Git
- Python 3.10.x
- Node.js LTS
- Docker Desktop
- Google Cloud CLI
- Firebase CLI
- Ollama（若新電腦要本機跑文字建議）

舊電腦盤點版本：Python 3.10.11、Node v24.16.0、npm 11.13.0、Google Cloud SDK 574.0.0、Firebase CLI 15.22.2。新電腦不必完全相同，但 Python 建議維持 3.10。

### 8.2 Clone 與補回完整資料

```powershell
git clone https://github.com/vivi930319/DecorateMeAI.git
cd DecorateMeAI
git checkout Isa
```

接著從完整備份補回被忽略的：

```text
firebase-hosting-full/public/pages/
firebase-hosting-full/public/js/image-worker.js
firebase-hosting-full/public/*.png、*.jpg
firebase-hosting-full/public/config.local.js（私密傳送）
models/basic_features_roi/
.env（私密傳送或重建）
```

### 8.3 建立 Python 虛擬環境

```powershell
cd "C:\Users\isach\PycharmProjects\PythonProject12"
py -3.10 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
pip install -r requirements.render.txt
pip install -r requirements.gateway.txt
```

若 PowerShell 禁止啟用腳本，可直接使用 `\.venv\Scripts\python.exe -m pip install ...`。InsightFace 第一次初始化可能需要下載 `buffalo_l` 模型；Dockerfile 會在 build 時下載。

### 8.4 建立前端本機設定

```powershell
Copy-Item .\firebase-hosting-full\public\config.local.example.js .\firebase-hosting-full\public\config.local.js
```

把 URL 改成本機或正式服務位置。`config.local.js` 會被瀏覽器下載，不能放真正秘密；長期應改成前端只呼叫 Gateway。

### 8.5 本機啟動

```powershell
Start-Process powershell -ArgumentList '-NoExit','-Command','cd "C:\Users\isach\PycharmProjects\PythonProject12\firebase-hosting-full\public"; python dev_server.py'
Start-Process powershell -ArgumentList '-NoExit','-Command','cd "C:\Users\isach\PycharmProjects\PythonProject12"; .\.venv\Scripts\python.exe Face_analyzer_BASIC.py'
Start-Process powershell -ArgumentList '-NoExit','-Command','cd "C:\Users\isach\PycharmProjects\PythonProject12"; .\.venv\Scripts\python.exe Face_analyzer_PRO.py'
Start-Process powershell -ArgumentList '-NoExit','-Command','cd "C:\Users\isach\PycharmProjects\PythonProject12"; .\.venv\Scripts\python.exe Ollama_suggestion.py'
```

前端網址：`http://127.0.0.1:5500/?v=local#dashboard`。

### 8.6 Docker Compose

```powershell
docker compose up --build face_analysis_basic face_analysis_pro ollama_suggestion
docker compose --profile optional up --build replicate_render
```

正式服務不能使用空 API key 啟動；Ollama 若跑在主機，確認 Docker 可以連到 `OLLAMA_BASE_URL`。

---

## 9. 測試與部署

### 測試

```powershell
python -m unittest discover -p '*test.py' -v
python -m unittest discover -p '*_test.py' -v
cd firebase-hosting-full\\public
node --check js/api.js
node --check js/data.js
node --check js/router.js
node frontend_smoke_check.js
```

2026-07-17 盤點結果：後端兩次 unittest discovery 都是 13 tests passed、OK。前端 smoke test 需要 `config.local.example.js`，本次已補上無秘密範本。

需要外部服務時才執行：

```powershell
python backend_smoke_test.py
python ollama_suggestion_smoke_test.py
```

### Firebase Hosting

```powershell
cd firebase-hosting-full
firebase login
firebase use decorate-me
firebase deploy --only hosting
```

### Render Cloud Run

```powershell
./deploy_render_cloudrun.ps1 -ProjectId decorate-me -Region asia-east1 -ServiceName replicate-render -GcsBucketName <bucket-name>
gcloud storage buckets update gs://<bucket-name> --lifecycle-file=gcs-lifecycle.json
```

舊部署腳本仍有 `--allow-unauthenticated`，且沒有完整設定 Cloud Run IAM、Secret Manager、max instances 與 CPU throttling；正式搬機後不要不檢查就直接公開部署。

---

## 10. 搬機後驗收清單

- [ ] `firebase-hosting-full/public/pages/` 完整存在
- [ ] `public/js/image-worker.js` 存在
- [ ] 前端圖片素材完整
- [ ] `models/basic_features_roi/` 的 `.onnx` 與 class JSON 存在
- [ ] `.env` 已安全重建，沒有進 Git
- [ ] 5500 前端可開啟
- [ ] 8001 BASIC、8002 PRO、8010 suggestion `/health` 回 200
- [ ] BASIC／PRO 分析可完成
- [ ] saved look 寫入／刪除可完成
- [ ] 後台會員讀取／刪除失敗時不會假刪
- [ ] FACE／RENDER／SUGGESTION 金鑰已輪替
- [ ] 正式 CORS 不是 `*`
- [ ] AI 核心 Cloud Run 不允許匿名呼叫
- [ ] GCS lifecycle 已套用

---

## 11. 已知問題與處理順序

### 立即處理

1. 前端目前仍可取得 AI 服務 API key，應全面改走 Gateway。
2. Render 部署腳本仍公開 `allow-unauthenticated`。
3. 正式服務金鑰缺少時部分 API 會 fail-open。
4. GCS 上傳失敗時不能把 Replicate 臨時 URL 存進 saved_looks。

### 高優先

1. Render 配額與去重應從已驗證 Token 取得會員身份，不要相信 `X-User-Email`。
2. `threading.Thread`／FastAPI `BackgroundTasks` 不能當正式可靠佇列，應改 Cloud Tasks 或 worker。
3. 點數目前仍有 localStorage demo fallback，正式版應由資料庫作唯一來源。
4. 購物車目前只有 localStorage，尚未跨裝置或跨會員同步。
5. 會員刪除需資料庫端交易、權限與 GCS 清理契約。

### 中期

1. Ollama 加輸入長度、model 白名單、並發限制與輸出 Schema。
2. requirements 版本鎖定並加入 CI、secret scan、dependency audit。
3. 補上 CSP、`nosniff`、`frame-ancestors`、Referrer Policy 等安全標頭。
4. 將 Cloudflare Tunnel URL 改成穩定正式服務網址。
5. 重新整理 `.gitignore`，讓必要前端程式、文件與測試可被 GitHub 完整保存。

---

## 12. 搬移順序

```text
1. 舊電腦建立 Git bundle
2. 另外建立完整資料夾／加密壓縮備份
3. 新電腦 clone GitHub 的 Isa 分支
4. 補回被忽略的完整前端、模型、資料與文件
5. 重新建立 Python .venv
6. 重新建立 .env 與前端 config.local.js
7. 先跑前端語法與 smoke test
8. 再跑 BASIC／PRO／Ollama health
9. 再測登入、saved look、後台與資料庫
10. 最後才部署
```

搬機完成前不要刪除舊電腦資料；至少等新電腦完成前端、後端、資料庫與部署驗收後，再清除舊備份。
