# 給商品後端 · 演算法端 — 商品 API 與推薦結果問題回報

> ## 🔴 2026-07-29 05:5x 追記：資料庫換網址，而且 **id 全部被重編號了**
>
> 新網址 `https://regional-components-lowest-ridge.trycloudflare.com`。
> 同一批 1041 筆商品，但 `id` 從「1～3131 有跳號」變成「1～1041 連號」：
>
> ```
> 舊：id=2656  Za 午後花園柔霧唇膏 - 櫻桃陷阱   (sale_page_id: za-za-lipstick-櫻桃陷阱)
> 新：id=1     Za 午後花園柔霧唇膏 - 櫻桃陷阱   (sale_page_id 不變)
> ```
>
> **這件事本身比下面任何一項都嚴重，請先讀第 4.5 節。**
>
> 好消息：`GET /api/products/{id}` 在新網址回 **200**（舊網址是 405），§1 那一項已修。
> `sale_page_id` 在重編號前後保持不變 —— 目前唯一穩定的商品識別。

**日期：** 2026-07-29
**提出方：** DECORATE ME 前端 + AI Gateway
**對象：** 商品資料庫後端維運者、推薦演算法維運者
**實測對象：** `PRODUCT_DATABASE_URL = https://tube-series-nor-punch.trycloudflare.com`
**實測時間：** 2026-07-29 05:4x（台北時間）

---

## 0. 這份文件怎麼讀

每一項都附**可以自己重跑的指令與實際回應**。我們不是憑印象回報，也不希望你們憑印象修。

分成兩塊：

- **第 1～3 節：商品後端**（`/api/products` 系列）
- **第 4～6 節：演算法端**（`/recommend-products`）
- **第 7 節：好消息**（已經修好、我們可以撤掉繞道的部分）

嚴重度標記：

| 標記 | 意思 |
| --- | --- |
| 🔴 | 使用者現在就踩得到，或會靜默產生錯誤資料 |
| 🟡 | 目前有繞道，但繞道很脆弱 |
| 🟢 | 規格與實作不一致，暫時不影響功能 |

---

## 1. 🔴 後台改不了商品：PATCH 回 404，但那件商品確實存在

### 現象

管理員在後台編輯商品按儲存，畫面顯示「資料庫寫入失敗：找不到商品」。

```
PATCH https://decorate-me.web.app/admin-api/products/2656
If-Match: 1
→ 404
```

### 我們這一段已經排除

Gateway 的 `proxy_admin_request` 做的事：驗 CSRF → 驗 session 是 admin → 帶
`Authorization: Bearer {PRODUCT_ADMIN_API_KEY}` 轉到 `{PRODUCT_DATABASE_URL}/api/products/2656`。
**路徑正確、金鑰有帶、身分驗證通過**，之後才拿到你們回的 404。

### 證據：你們的清單吐得出 2656，你們的 PATCH 說找不到它

```bash
U=https://tube-series-nor-punch.trycloudflare.com

# ① 清單裡有 2656
curl -s "$U/api/products" | python -c "import json,sys;d=json.load(sys.stdin)['products'];print([p for p in d if p['id']==2656])"
```
```json
{
  "id": 2656,
  "name": "Za 午後花園柔霧唇膏 - 櫻桃陷阱",
  "brand": "Za",
  "type": "lipsticks",
  "sale_page_id": "za-za-lipstick-櫻桃陷阱",
  "price": "NT$380",
  "hex_primary": "#b40000"
}
```

```bash
# ② 這條路由存在，而且會先驗身分（所以不是路由不存在）
curl -s -o /dev/null -w "%{http_code}\n" -X PATCH "$U/api/products/2656" \
     -H "Content-Type: application/json" -d '{}'
# → 401  {"error":{"code":"UNAUTHENTICATED","message":"請先登入"}}

# ③ 但同一條路由不收 GET
curl -s -o /dev/null -w "%{http_code}\n" "$U/api/products/2656"
# → 405  METHOD_NOT_ALLOWED
```

### 我們的推測（請你們確認，不要照著改）

`type: "lipsticks"` 這個欄位很可疑。加上早先實測 `/api/product/{type}/{id}` 是 404，
我們猜 **PATCH 的查詢走的是另一張表，或需要 `type` 才定位得到**，而 `/api/products`
的清單是另一個來源（或是合併視圖）。

### 我們需要的

**`PATCH /api/products/{id}` 的 `{id}` 必須就是 `GET /api/products` 回應裡那個 `id`。**

如果做不到，請告訴我們正確的定位方式（例如需要 `?type=lipsticks`，或改用 `sale_page_id`），
我們改 Gateway 配合。**但請不要讓兩支端點對同一個號碼有不同解釋** —— 那是第 4 節同一個病。

### 順帶：`GET /api/products/{id}` 回 405 —— **已於新網址修好**

舊網址回 405，新網址 `GET /api/products/1` 回 **200**。感謝。

### PATCH 404 需要在新網址重測

上面的 404 是在舊網址觀察到的。新網址不帶金鑰時 `PATCH /api/products/1` 回 401
（路由存在、先驗身分），與舊網址行為一致，但**帶管理員金鑰之後會不會仍然 404，
我們無法自行驗證**（我們不持有那把金鑰）。請後台管理員再試一次編輯商品；
若仍失敗，§1 的分析與請求維持不變。

另外請注意：重編號之後，後台畫面上任何**重新整理前就握著的 id 都已經失效** ——
這本身就足以造成 404。這一點請併第 4.5 節一起看。

---

## 2. 🟢 `/api/products` 沒有實作規格書講好的分頁 envelope

〈商品搜尋管理與推薦演算法整合規格書 2026-07-17〉§6.4 寫的是：

```json
{ "ok": true, "items": [], "total": 1250, "nextCursor": null, "query": "YSL 唇膏" }
```

實測回的是：

```bash
curl -s "$U/api/products?limit=3" | python -c "import json,sys;d=json.load(sys.stdin);print(list(d.keys()), len(d['products']))"
# → ['products'] 1041
```

- 只有 `products`，沒有 `items` / `total` / `nextCursor`
- **`limit=3` 被忽略**，回了全部 1041 筆

### 目前不影響功能，但有一個地雷

前端已經寫好「跟著 `nextCursor` 翻頁」的邏輯，但**第一次請求刻意不帶 `limit`**。
原因就是上面這個狀態：

> 如果你們哪天先實作了 `limit`、卻還沒實作 `nextCursor`，
> 我們就會安靜地只拿到前 100 筆，把它當成全部商品。

**所以請務必成對實作**：要嘛兩個都做，要嘛兩個都別做。
單獨上線 `limit` 會讓前端商品頁少掉九成商品，而且沒有任何錯誤訊息。

---

## 3. 🟢 欄位命名不一致（已在前端吸收，記錄備查）

| 你們回的 | 前端原本預期 | 現況 |
| --- | --- | --- |
| `hex_primary` | `hex` | 前端已改讀 `hex_primary` |
| `image_src` | `imageUrl` | 前端兩個都讀 |
| `products` | `items` | 前端兩個都讀 |

不需要為了我們改，但如果之後要統一，請先講一聲。

---

## 4. 🔴 演算法端與商品清單的 `id` 不是同一個號碼系統

**這是這份文件最重要的一項。**

### 現象

```bash
# 推薦回來的第一筆
curl -s -X POST "$U/recommend-products" -H "Content-Type: application/json" \
  -d '{"analysisPackage":{"style":"hongKong","faceAnalysis":{"faceShape":"鵝蛋臉","eyeShape":"鳳眼"}},"limit":12}'
```
```json
{
  "id": 93,
  "candidateKey": "blushes:93",
  "name": "柔礦迷光金屬光炫彩餅 - Soft & Gentle",
  "brand": "MAC",
  "type": "blushes",
  "productUrl": "mac-blush-MT1329"
}
```

```bash
# 商品清單裡的 id=93
curl -s "$U/api/products" | python -c "import json,sys;print([p['name'] for p in json.load(sys.stdin)['products'] if p['id']==93])"
# → ['INTEGRATE櫻特芮 自由繪型柔色眉彩膏6g 【BR371】']   type=eyebrows
```

**同一個 `id: 93`：推薦端說它是 MAC 腮紅，清單端說它是 INTEGRATE 眉彩膏。**

`candidateKey: "blushes:93"` 透露了原因 —— 推薦端的 `id` 是**類別內的編號**，
清單端的 `id` 是**全域編號**（實測範圍 1～3131）。

### 為什麼這件事很嚴重

前端本來用 `rawId` 把兩邊的同一件商品對起來（推薦回應早期缺圖，要靠清單補）。
在這個號碼系統下，那等於**把兩個不相干的商品合併成一件，而且合出來的東西看起來完全正常** ——
名稱、價格、圖片都在，只是它們來自不同的商品。沒有任何錯誤訊息。

我們已經在 2026-07-29 把 `rawId` 從比對條件裡拿掉，改用 `sale_page_id`。但這是繞道，不是修好。

### 我們需要的

**推薦回應的 `id` 請改成商品清單的全域 `id`。**

如果類別內編號在你們內部有用途，請保留在 `candidateKey` 就好（它本來就已經帶了），
`id` 這個欄位請讓它跟 `/api/products` 一致。**一個系統裡不該有兩種 `id` 的意思。**

### 目前唯一可靠的對應

`sale_page_id`。實測對得上：

```bash
curl -s "$U/api/products" | python -c "import json,sys;print([p['name'] for p in json.load(sys.stdin)['products'] if p.get('sale_page_id')=='mac-blush-MT1329'])"
# → ['柔礦迷光金屬光炫彩餅 - Soft & Gentle']   ✓ 對得上
```

在 `id` 統一之前，**請確保每一筆推薦都帶得出 `productUrl`（= `sale_page_id`）**，
那是我們現在唯一能安全對應的鍵。

---

## 4.5 🔴 商品 `id` 不是穩定識別碼 —— 換一次資料庫，全站的收藏就指向別的商品

### 發生了什麼

2026-07-29 換資料庫網址時比對前後兩份清單：**同一批商品被重新編號了**。

```bash
# 舊網址
curl -s "$OLD/api/products" | ... id 範圍 1~3131，1041 筆（有跳號）
# 新網址
curl -s "$NEW/api/products" | ... id 範圍 1~1041，1041 筆（連號）

# 用 sale_page_id 追同一件商品
za-za-lipstick-櫻桃陷阱   舊 id=2656  →  新 id=1
mac-blush-MT1329          舊 id=93    →  新 id=406
```

### 為什麼這是嚴重問題

前端的收藏功能**以 `id` 當永久鍵**存在兩個地方：

1. 瀏覽器 `localStorage`：`api-{type}-{id}`
2. 會員資料庫的收藏表：`{item_id, item_type}`

所以這次重編號之後，**每一筆既有收藏都指向了另一件商品** —— 使用者收藏的是唇膏，
重新整理後變成腮紅。沒有錯誤訊息，沒有任何跡象，資料就這樣錯位了。

推薦快取、後台編輯時握著的 id、任何我們存下來的 id，全部同理。

### 我們需要的（這一項請優先於其他所有項目）

**`id` 一旦發給前端，就不能再變。** 具體要求：

- 重建資料表、重新匯入、換資料來源時，**`id` 必須跟著商品走**，不能重新編號
- 如果做不到，請**明確告訴我們 `id` 是不穩定的**，我們改用 `sale_page_id` 當永久鍵
  （它在這次重編號前後保持不變，看起來才是真正的商品身分）

現在最怕的是「有時候穩定、有時候不穩定」—— 那種東西沒辦法寫程式對付。

### 給我們自己的提醒

在收到你們的答覆之前，前端會把 `sale_page_id` 當成商品身分，`id` 只當成「這一次請求裡的
臨時編號」。已收藏的資料無法回溯修正 —— 那批已經錯位了。

---

## 5. 🟡 `productUrl` / `sourceUrl` 不是網址

```json
"productUrl": "mac-blush-MT1329",
"sourceUrl":  "mac-blush-MT1329"
```

欄位名叫 `...Url`，值卻是 slug。前端後台的「原始商品頁」欄位因此被填進一段不能點的字串
（已於 2026-07-29 加上 `http(s)` 檢查擋掉）。

### 我們需要的（二選一，你們決定）

1. **改名**：既然是 sale page id，就叫 `salePageId`，別叫 `...Url`；或
2. **補真網址**：另外給一個 `sourceUrl` 是完整的 `https://...` 商品頁連結

我們比較希望是 (2) —— 使用者點推薦商品時，我們想帶他去真正的商品頁。
但 (1) 至少讓欄位名誠實，也可以接受。

---

## 6. 🔴 推薦結果的類別覆蓋率太低，湊不出一套完整的妝

### 現象

要 12 筆推薦，實際回來的類別分布：

```
eyeshadows  8
blushes     2
lipsticks   2
```

**12 筆只涵蓋 3 個類別**，而你們資料庫裡是有八大類的：

```
lipsticks 378   blushes 223   eyeliner_mascara 120   eyebrows 120
foundations 68  eyeshadows 60 highlighters 44        contouring 28
```

沒有底妝、沒有眉、沒有眼線睫毛、沒有修容、沒有打亮。

### 為什麼這對我們是問題

前端的「本次個人化推薦」產品需求是 **「每個美妝大類至少一件」**（2026-07-15 定案）——
使用者做完臉部分析、選了風格，期待看到的是**一整套可以照著買的妝**，
不是八個眼影盤。現在的結果湊不出一套。

而且 `eyeshadows` 只有 60 筆庫存卻拿到 8 個名額，`lipsticks` 有 378 筆卻只拿到 2 個，
看起來不像是庫存造成的，而是評分或關鍵字比對的偏好。

### 我們需要的

推薦結果請做**類別配額**：先確保每個大類至少一件（該類真的沒有合適商品才略過），
剩下的名額再依分數分配。

如果你們認為某些類別不該出現在某些風格（例如「男士白開水」不推腮紅），
那也可以 —— 但請在回應裡講出來，例如：

```json
"coverage": { "requested": 8, "returned": 3, "skipped": { "contouring": "風格不適用" } }
```

我們才知道是「演算法決定不推」還是「漏了」。

### 順帶：`fallbackUsed` / `fallbackReason` 是什麼？

回應裡有這兩個欄位，但規格書沒寫。請補說明 —— 我們想在推薦品質不好時給使用者提示，
但現在不知道它們代表什麼。

---

## 7. 🟢 好消息：`imageUrl` 已經修好了

先前回報過「推薦端點的 `imageUrl` 一律是空字串」，前端因此寫了一段用商品清單補圖的繞道。

2026-07-29 實測：**12 筆推薦，12 筆都有 `imageUrl`，0 筆是空的。** 謝謝。

等第 4 節的 `id` 統一之後，我們就可以把補圖那段繞道整個拿掉。

---

## 8. 優先順序建議

| 順序 | 項目 | 節 | 誰 |
| --- | --- | --- | --- |
| 1 | PATCH 404 —— 後台現在完全改不了商品 | §1 | 商品後端 |
| 2 | 推薦的 `id` 改成全域 id | §4 | 演算法端 |
| 3 | 推薦的類別覆蓋率 | §6 | 演算法端 |
| 4 | `productUrl` 改名或補真網址 | §5 | 演算法端 |
| 5 | 分頁 envelope（**要做就 `limit` 和 `nextCursor` 一起做**） | §2 | 商品後端 |
| 6 | `GET /api/products/{id}` 補上 | §1 | 商品後端 |

---

## 9. 我們這邊已經做掉的（不用你們處理）

| 日期 | 內容 |
| --- | --- |
| 2026-07-29 | `fillRecommendedImages` 拿掉 `rawId` 比對，改用 `sale_page_id` —— 避免合併到不相干的商品（§4 的繞道） |
| 2026-07-29 | `sourceUrl` 加上 `http(s)` 檢查，slug 不再被當成網址（§5 的繞道） |
| 2026-07-29 | 商品清單改成「只有真的收到 `nextCursor` 才翻頁」，第一次請求不帶 `limit`（§2 的地雷） |
| 2026-07-29 | 「本次個人化推薦」改成每大類至少一件、剩餘依分數排 —— 但**類別不夠是 §6，我們排不出沒有的東西** |

---

## 10. 聯絡

問題或需要我們配合改 Gateway，直接回這份文件。
所有指令都可以自己重跑，`$U` 換成當時的 `PRODUCT_DATABASE_URL` 即可。

註：目前 `PRODUCT_DATABASE_URL` 是 Cloudflare Quick Tunnel，重啟就換網址。
正式上線前需要一個固定網址（issue #23）。
