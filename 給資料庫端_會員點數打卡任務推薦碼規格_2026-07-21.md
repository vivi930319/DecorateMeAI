# 給資料庫端：會員點數／打卡／任務／推薦碼規格

> 文件日期：2026-07-21
> 範圍：**只談會員端。** 商品端卡在爬蟲那邊，等他們有進度再另外談，這份不涉及商品。
> 前置：Firebase Hosting 剝 cookie 的問題（S56）已修好並上線，登入後的請求現在真的到得了你們那邊。

---

## 0. 忙碌版結論

**先說好消息：會員端主線是通的，我們實測過了**（證據見第 1 節）。
登入、會員資料、點數、打卡、任務五條全部 `200`，打卡的連續天數也真的在累積。
先前擔心的「前端只是拿本機資料裝樣子」**不成立**。

**現在要的（照急迫度）**

1. 🔴 **`/tasks` 沒有回 `done` 欄位** —— 前端因此把每一個任務都當成「已完成」，
   使用者可以領取根本沒完成的任務。這是目前最實際的 bug，見 2.3。
2. 🟠 **`/api/members/{email}/referral` 回 `403`**，而不是像其他不存在的端點那樣回 `404`。
   這條路徑在你們那邊有東西擋著，請說明那是什麼，見第 3 節。
3. 🟠 **`first_referral` 任務已被領走 20 點，但推薦功能根本還不存在**，見 2.3。
4. 🟠 **推薦碼要全新做**（第 3 節），前端現有那套是 demo，設計上就不能用，會整個丟掉重寫。
5. 🟡 **`/api/recommend/personal` 與 `/api/admin/product-audit-logs` 都回 `404`** ——
   Gateway 已放行但你們沒有這兩條，請確認是要做還是我們拿掉。

**已經回報過、還沒動的**

- issue **#27 登入端點可列舉已註冊帳號**（第 4 節），這個要修。

---

## 1. 實測結果（2026-07-21）

用真實會員帳號直接對你們的資料庫做過驗證，不經過 Gateway，結果如下：

| 端點 | 狀態 | 回傳重點 |
|---|---|---|
| `POST /api/login` | `200` | 有 `Set-Cookie: member_session`，**帶 `Max-Age`**（不是純 session cookie，正確）；同時回 `accessToken` JWT，`role=member`，有效期 1 小時 |
| `GET /api/members/{email}` | `200` | `level=VIP會員`、`emailVerified=true`、`allowedPages` 齊全 |
| `GET /api/members/{email}/points` | `200` | `balance=40`、`lifetime=120`、交易明細完整 |
| `GET /api/members/{email}/check-in` | `200` | `checkedToday=true`、`streak=5`、`awarded=15` |
| `GET /api/members/{email}/tasks` | `200` | 四筆任務，但**格式有缺**，見 2.3 |
| `GET /api/members/{email}/referral` | `403` | 這條要說明，見第 3 節 |
| `GET /api/recommend/personal` | `404` | 不存在 |
| `GET /api/admin/product-audit-logs` | `404` | 不存在 |

**打卡是真的在運作的**，交易明細裡看得到 `check_in_streak_bonus_4d`（7/20）、
`check_in_streak_bonus_5d`（7/21），連續天數確實在累積。這點先前一直沒被證實過，現在確認了。

### 為什麼要特地做這個驗證

前端在每一條會員資料上都有一層 fallback：先畫瀏覽器 localStorage 的舊資料，
再非同步打你們的 API，**成功才覆蓋**。失敗的話 `.catch(() => {})` 把錯誤吃掉，畫面停在本機資料。

打卡最明顯，`router.js` 的按鈕邏輯是：

```js
const remoteResult = await Api.checkInMember(email).catch(() => null);
if (remoteResult?.ok) { ...; return; }          // 走資料庫
const result = MemberRewards.checkin(email);    // 打不通 → 寫瀏覽器本機
showToast(`打卡成功，獲得 ${result.points} 點`); // 兩種情況都顯示「打卡成功」
```

**你們的端點壞掉時，使用者看到的畫面跟正常完全一樣**，我們這邊也沒有錯誤日誌可看。
所以「畫面正常」不能當作後端正常的證據 —— 這也是為什麼上面那張表是直接打你們的 API 測的。

這層 fallback 是資料庫還沒接上時留的過渡機制，我們這邊會排時間拿掉。
在那之前，前端唯一的線索是卡片上多一行「（資料庫同步）」／任務標題旁的 `DB` 標記。

---

## 2. 已接上的端點：請確認契約

以下五條 Gateway 已放行（`ai_gateway.py` 的 `member-database` allowlist）。
所有請求都由 Gateway 代打，帶著登入時取得的上游 cookie，前端碰不到你們的網址。

### 2.1 會員點數 ✅ 已符合，不用改

```
GET /api/members/{email}/points
```

你們目前回的格式（實測）：

```json
{
  "balance": 40,
  "lifetime": 120,
  "transactions": [
    { "delta": 15, "reason": "check_in_streak_bonus_5d",
      "balance_after": 40, "created_at": "2026-07-21T02:18:49.418990" }
  ]
}
```

這個格式**前端已經正確吃得到**，`delta` / `balance_after` / `created_at` 都對得上，不需要改。

語意也確認過了：

- `balance` = 目前可用餘額，兌換會扣。
- `lifetime` = **累計獲得過**的點數，只增不減。會員等級門檻用這個算，
  這樣花點數兌換不會把自己降級。實測 `lifetime=120`、`balance=40`，
  代表累計拿過 120 點、已兌換掉 80 點，兩者分開記錄是對的。

### 2.2 每日打卡 ✅ 已在運作

```
GET  /api/members/{email}/check-in      查詢今天的狀態
POST /api/members/{email}/check-in      執行打卡
```

`GET` 實測回應：

```json
{ "checkedToday": true, "streak": 5, "awarded": 15 }
```

格式沒問題，前端 `{ ok: true, ...data }` 直接展開就吃得到。
`POST` 期望回 `{ "ok": true, "awarded": 10, "streak": 5, "balance": 120 }`。

連續天數確實在累積（交易明細有 `check_in_streak_bonus_4d`、`_5d`），這條可以視為已完成。

只有三件事想確認：

- 同一天重複打卡是不是**由你們擋**？請回明確錯誤，不要靜默成功。
- **「今天」是用哪個時區判斷的？** 請用固定時區（建議 UTC+8），
  不要用請求端送來的時間，否則使用者改系統時鐘就能重複領。
- 連續獎勵的天數門檻與點數，跟前端暫定的 3 / 7 / 14 / 30 天一致嗎？
  實測第 5 天發了 15 點，跟前端的表對不太起來，以你們的為準，但請告訴我們正確的表。

### 2.3 任務中心 🔴 這裡有 bug，要改

```
GET  /api/members/{email}/tasks
POST /api/members/{email}/tasks/{taskId}/claim
```

你們目前回的格式（實測）：

```json
{ "tasks": [
  { "taskId": "first_analysis", "reward": 20, "claimed": true },
  { "taskId": "first_favorite",  "reward": 10, "claimed": true },
  { "taskId": "first_referral",  "reward": 20, "claimed": true },
  { "taskId": "daily_checkin",   "reward": 5,  "claimed": true }
] }
```

#### 問題一：少了 `done`，前端把所有任務都當成已完成 🔴

前端的判斷是（`router.js`）：

```js
done: t.done !== false
```

你們沒有回 `done`，`undefined !== false` 的結果是 **`true`**，
所以**每一個任務都會被畫成「已完成、可領取」**。

現在剛好看不出來，因為那四筆 `claimed` 都是 `true`，按鈕本來就是 disabled 的。
但只要出現一筆還沒領的任務，使用者就能直接領走一個**根本沒完成**的任務。

請在每一筆補上 `done`（布林值，代表這個任務的達成條件有沒有滿足）。
我們這邊也會把 `t.done !== false` 改成 `t.done === true`，兩邊一起補，才不會再有一次靠預設值矇混過去。

#### 問題二：`first_referral` 已被領走，但推薦功能不存在 🟠

`first_referral`（成功推薦一位好友，20 點）的 `claimed` 是 `true`。
但推薦碼功能**在你們那邊根本還沒有**（`/referral` 回 403），前端那套是純瀏覽器 demo。

所以這 20 點是在「不可能真的完成」的情況下發出去的。請確認是哪一種：

- 從舊的 localStorage demo 資料同步過去的？
- 還是 `claim` 沒有檢查達成條件，呼叫就給？

如果是後者，那所有任務都可以直接呼叫 API 領獎勵，跟問題一是同一個根因。

#### 問題三：`daily_checkin` 可能重複發點 🟡

任務清單裡有 `daily_checkin`（5 點），但每日打卡本身已經給 10 點＋連續獎勵。
請確認這兩個是不是同一件事發了兩次點。

#### 其他

- `taskId` 這個欄位名沒問題，前端有 `t.id || t.taskId` 的相容處理。
- 沒有 `title` / `group` 也可以，前端有本機中文對照表會補，
  不會讓 `first_analysis` 這種代碼露在畫面上。但**新增任務時請通知我們**，
  否則新任務會顯示成英文代碼。
- `claim` 期望回 `{ "ok": true, "awarded": 20, "balance": 140 }`。
- **重複領取與未完成領取都請由你們擋**，不要相信前端送來的請求。

### 2.4 主題商店兌換

```
POST /api/members/{email}/theme-shop/{themeId}/redeem
```

點數不足要回明確失敗。這裡有一個前端踩過的坑供參考：**扣點成功但主題沒解鎖**會讓使用者白花點數，所以請確保扣點與解鎖是同一個交易，要嘛都成功要嘛都失敗。

### 2.5 個人化推薦

```
GET /api/recommend/personal
```

**這條 Gateway 已放行，但你們那邊不存在。** 請回覆：要做，還是我們從 allowlist 拿掉？

### 2.6 妝容收藏：`before_image_url` 請允許 null 🟠

```
GET    /api/members/{email}/saved-looks
POST   /api/members/{email}/saved-looks
DELETE /api/members/{email}/saved-looks/{id}
```

實測 `GET` 回 `{"looks": []}` —— 目前一筆都沒有。原因在我們這邊，已經修好了，但**需要你們配合一個欄位調整**。

#### 背景

`saved_looks` 的 `before_image_url` / `after_image_url` 是 `String(500)`，這個限制是對的。
但兩張圖的來源完全不同：

| | 來源 | 有沒有網址 |
|---|---|---|
| **妝後圖** | 渲染服務產生，存在 GCS | ✅ 有，目前形式是 `/media/render/<32碼hex>` |
| **妝前圖** | 使用者自己上傳的照片，在瀏覽器端壓縮 | ❌ **沒有，而且系統裡沒有任何管道能讓它變成網址** |

妝前圖永遠是 `data:image/jpeg;base64,...`，最長可到 90 萬字元。
我們先前為了不讓它汙染 `String(500)` 欄位，在前端擋掉了整筆送出 ——
結果變成**每一筆收藏都沒進資料庫，而且畫面顯示成功**，所以拖到現在才發現。

#### 要你們做的

**請確認 `before_image_url` 允許 `null` 或空字串。**

我們改成：妝後圖有網址就送，妝前圖沒有網址就留空。這樣：

- 後台看得到會員的收藏，也看得到妝後圖
- 但**沒有妝前對比圖**

#### 順帶說明：使用者的原始照片不會進你們的資料庫

這是刻意的決定，不是偷懶。臉部原始照片屬於敏感個資，讓它留在使用者自己的瀏覽器，
比上傳到伺服器再想辦法保護簡單也安全得多。

如果後台**真的需要**妝前對比圖，那要另外做一套私有媒體上傳（Gateway 新增上傳端點 + GCS +
存取控制），而且要重新評估隱私影響。請先回覆你們需不需要，我們再決定要不要開這個工。

#### 妝後圖網址的形式

存進去的會長這樣：

```
/media/render/a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6
```

**這是相對路徑，不是完整網址**，長度固定 38 字元。它要透過 Gateway 才讀得到，
Gateway 會驗證讀取者是圖片擁有者或 admin，然後換發一個十分鐘有效的 GCS 簽名網址。
後台用 admin 身分是讀得到的。

請**不要**對這個欄位做「必須以 http 開頭」之類的格式驗證。

---

## 3. 推薦碼：要全新做

### 3.0 先問一件事：`/referral` 為什麼回 403？

實測 `GET /api/members/{email}/referral` 回 **`403`**，
而 `/api/recommend/personal`、`/api/admin/product-audit-logs` 這兩條真的不存在的端點回的是 **`404`**。

403 跟 404 不一樣 —— 代表這條路徑在你們那邊**有東西擋著**，不是單純沒實作。請說明是哪一種：

- 已經做了一半，只是還沒開放？
- 路由匹配到某個需要 admin 權限的 handler？
- 還是有個 catch-all 對未知的 `/api/members/{email}/*` 一律回 403？

第三種的話要注意：**它會讓「端點不存在」跟「權限不足」變得無法區分**，
我們排查時只能靠猜。建議未實作的路徑就老實回 404。

### 3.1 為什麼現有的不能沿用

前端現在那套（`api.js` 的 `Referral`）是純瀏覽器 demo，有三個**設計層級**的問題，不是補一補就能用：

1. **推薦碼是 email 的雜湊值**（`hash * 31 + charCodeAt`，取 6 碼）。知道別人 email 就能算出他的推薦碼，反過來也能爆破。32-bit 壓成 6 碼還會撞碼。
2. **反查推薦人只在本機 localStorage 裡找**。新會員 B 在自己的瀏覽器填 A 的推薦碼，除非 A 曾經在 B 那台電腦登入過，否則一律回「推薦碼不存在」。**正常使用情境下必定失敗。**
3. **就算成功，點數是加在 B 的瀏覽器裡**，A 換裝置永遠看不到。

所以這一塊會整個丟掉重寫，前端也要改。原本 `MEMBERSHIP_POINTS_SYSTEM_PLAN.md` 寫的是「管理員人工核發」，那是因為當時還沒有共用資料庫 —— 現在有了，應該直接自動入帳。

### 3.2 建議的做法

**推薦碼由你們產生並保存**，不要用任何可以從 email 推算出來的值：

- 註冊時產生一組隨機碼，存在會員資料表（例如 `referral_code` 欄位，唯一索引）。
- 建議 8 碼、排除易混淆字元（`0/O`、`1/I/l`），例如 `K7M9XQ4T`。
- 一個會員一組，永久不變。

需要三條端點：

```
GET  /api/members/{email}/referral
POST /api/members/{email}/referral/apply
```

**查自己的推薦碼與戰績**

```
GET /api/members/{email}/referral
→ { "code": "K7M9XQ4T", "referredCount": 3, "rewardPoints": 50 }
```

**新會員套用推薦碼**（註冊 / OTP 驗證通過當下呼叫一次）

```
POST /api/members/{email}/referral/apply
{ "code": "K7M9XQ4T" }
→ { "ok": true, "referrerEmail": "a@example.com", "awarded": 50 }
```

必須由你們擋掉的情況（前端擋不住，也不該由前端擋）：

| 情況 | 期望行為 |
|---|---|
| 碼不存在 | `ok:false`，但**註冊本身仍要成功**，只是不發獎勵 |
| 用自己的碼 | `ok:false` |
| 同一個帳號套用第二次 | `ok:false` |
| 推薦人與新會員是同一個 IP／裝置 | 建議記錄下來供事後稽核，先不用擋 |

**防刷**：這是最容易被薅的功能。搭配 issue #7（OTP 沒有真正強制，可以用任意 email 註冊）看，現在等於「隨便填 email 就能自己推薦自己賺 50 點」。**建議 OTP 真正強制之後再開放推薦碼發點**，否則上線就是送分。

### 3.3 也可以先不做

如果你們現在人手不夠，推薦碼可以整個延後 —— 前端會把那張卡片收起來，不會出現壞掉的功能。**但請不要維持現狀**：現在畫面上有一組看起來能用、實際上永遠失敗的推薦碼，比沒有更糟。

---

## 4. issue #27：登入端點可列舉已註冊帳號

`POST /api/login` 目前對兩種失敗回不同的錯誤碼：

| 情境 | 回應 |
|---|---|
| 帳號存在、密碼錯 | `401` `{"error":{"code":"WRONG_PASSWORD","message":"密碼錯誤"}}` |
| 帳號不存在 | `401` `{"error":{"code":"USER_NOT_FOUND","message":"此帳號尚未註冊"}}` |

狀態碼一樣，但錯誤碼不同 —— 拿一組假密碼逐一測 email，就能掃出哪些信箱是已註冊會員。

Gateway 這層做的是對的（把上游的 401/403/404 一律轉成 `INVALID_CREDENTIALS`），但**你們的服務掛在公開 Quick Tunnel 上，不經過 Gateway 就能直接呼叫**，所以那層遮蔽形同虛設。

請統一：**狀態碼、錯誤碼、訊息、回應時間四項都要一致**。回應時間也算 —— 如果「帳號不存在」跳過了密碼雜湊比對而明顯比較快，一樣測得出來。

根本緩解仍然是讓資料庫退出公開 Tunnel（見第 6 節）。

---

## 5. 驗收方式

第 1 節的驗證就是用這套流程跑的，修好之後可以照同一份指令自己複驗。

> **注意**：這台開發機的 Avast 會攔截 TLS，`curl` 走 Schannel 連不出去（回 `000`／SSL error）。
> 在 Windows 上請改用 PowerShell 的 `Invoke-WebRequest`（走 .NET 與 Windows 憑證存放區，可以通）。
> 下面保留 curl 版本給非 Windows 環境。

```bash
# 1. 登入拿 cookie
curl -i -c jar.txt -X POST https://<你們的網址>/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"demo@example.com","password":"..."}'

# 2. 帶同一個 cookie 立刻讀回自己（這步過不了，後面都不用測）
curl -i -b jar.txt https://<你們的網址>/api/members/demo@example.com

# 3. 點數 / 打卡 / 任務
curl -i -b jar.txt https://<你們的網址>/api/members/demo@example.com/points
curl -i -b jar.txt https://<你們的網址>/api/members/demo@example.com/check-in
curl -i -b jar.txt -X POST https://<你們的網址>/api/members/demo@example.com/check-in
curl -i -b jar.txt https://<你們的網址>/api/members/demo@example.com/tasks
```

第 2 步是關鍵：**登入後用同一個 cookie 立刻讀回同一位會員**，這是 Gateway 建立自己 session 的前提，做不到的話前端根本進不了會員頁。

端到端驗收從 S51 延宕到現在的原因是沒有測試帳密，這次改用真實會員帳號跑完了。
但長期還是麻煩給一組**專用 demo 帳號**，真實會員的密碼不該拿來做例行驗收。

---

## 6. 長期：請退出公開 Quick Tunnel

你們現在掛在 `*.trycloudflare.com` 的 quick tunnel 上，這一件事同時造成三個問題：

1. **網址會換**，換了我們就連不上，歷史上已經換過至少五次，每次都表現成「商品載入中卡住」或「登入打不通」，我們要花時間才確認不是程式壞掉。
2. **公開可直連**，任何人都能繞過 Gateway 直接打你們的 API，第 4 節的帳號列舉遮蔽因此失效。
3. **換址時沒有通知**，我們都是壞了才發現。

建議改成固定網址（Cloud Run 或付費 ngrok 都可以）。這一件事解掉三個問題。

在那之前，**換網址時麻煩通知我們一聲**，我們要同步更新 Cloud Run 上 `ai-gateway` 的 `MEMBER_DATABASE_URL` 環境變數。

---

## 7. 需要你們回覆的

端點都通了，所以問題比原本少。麻煩就這七點：

1. 🔴 `/tasks` 什麼時候可以補上 `done` 欄位？（2.3 問題一）
   以及 `saved_looks.before_image_url` 可以允許 null 嗎？（2.6）
2. 🟠 `first_referral` 那 20 點是怎麼發出去的？`claim` 有檢查達成條件嗎？（2.3 問題二）
3. 🟠 `daily_checkin` 任務跟每日打卡是不是重複發點？（2.3 問題三）
4. 🟠 `/api/members/{email}/referral` 回 403 是什麼擋的？（3.0）
5. 🟡 打卡的「今天」用哪個時區？連續獎勵的正確天數／點數表是什麼？（2.2）
6. 🟡 `/api/recommend/personal`、`/api/admin/product-audit-logs` 要做還是我們拿掉？
7. 推薦碼（第 3 節）要做嗎？大概什麼時候？

另外 issue **#27**（登入端點可列舉帳號，第 4 節）還是要修，那個不急但不能不修。

**測試帳密就不用給了** —— 我們已經用真實會員帳號驗證過。
之後如果需要重複驗收，再麻煩給一組專用的 demo 帳號，不要用真實會員資料。

有問題直接找我們討論，不用等文件往返。
