# Decorate Me 後端完整技術文件

> 版本：2026-07-25　｜　對應分支：後端 `Isa`、前端 `dev_makeup`
> 這份文件把後端「所有細節」寫在一起：架構、每個服務的端點與行為、共用模組、資安機制、
> 資料儲存、環境變數、Job 生命週期、部署與測試。**不含任何實際金鑰／Email／照片／權杖。**
> 讀法：先看「一、系統總覽」建立地圖，再依需要跳到對應服務或機制章節。

---

## 一、系統總覽

### 1.1 服務清單

| 服務 | 檔案 | 預設本機 port | 對外？ | 說明 |
|------|------|---------------|--------|------|
| **AI Gateway** | `ai_gateway.py` | 8015 | ✅ 前端唯一入口 | 反向代理＋會員 session＋所有安全防線 |
| **Face BASIC** | `Face_analyzer_BASIC.py` | 8001 | ❌ 只給 Gateway | 正面照五官分類、膚色、對稱性、拍攝角度 |
| **Face PRO** | `Face_analyzer_PRO.py` | 8002 | ❌ 只給 Gateway | 正面＋側面照；膚色只採正面，側面用於側臉鼻型 |
| **Render** | `replicate_render_api.py`（＋`replicate_render.py`） | 8080（uvicorn/Docker） | ❌ 只給 Gateway | 妝容渲染（Replicate／OpenAI），GCS 儲存；不在一鍵本機 stack |
| **Ollama Suggestion** | `Ollama_suggestion.py` | 8010 | ❌（預設關） | 文字妝容建議＋英文渲染 prompt |

> 前端（另一個 repo，`web_frontend`／分支 `dev_makeup`）只認得 Gateway 的同源路徑；
> 上游服務的真實網址只存在於 Cloud Run 環境，瀏覽器拿不到。

### 1.2 請求資料流

```
瀏覽器 ──(HttpOnly __session cookie)──▶ Firebase Hosting rewrite ──▶ AI Gateway
                                                                        │
        ┌───────────────────────────────────────────────────────────┼──────────────┐
        ▼(Cloud Run IAM identity token)      ▼(IAM)         ▼(IAM)     ▼(x-api-key，預設關)
   Face BASIC / PRO                      Render          會員資料庫    Ollama Suggestion
                                            │
                                            ▼
                                     Replicate / OpenAI（第三方渲染）＋ GCS bucket
```

- **對外只有一個入口**：Gateway。Face／Render／會員 DB 都移除了 `allUsers`，匿名直連回 403。
- Gateway 呼叫 Face／Render 用 **Cloud Run IAM identity token**（`requires_cloud_run_iam=True`）；
  呼叫會員 DB／文字建議用 **`x-api-key`**（`requires_cloud_run_iam=False`）。
- 前端與 Gateway 之間走**同源**（Firebase Hosting rewrite），所以 `SameSite=Lax` cookie 送得出去、不必處理 CORS。

### 1.3 共用模組

| 模組 | 職責 |
|------|------|
| `api_errors.py` | 統一錯誤 envelope、request logging、log 遮罩、服務金鑰 fail-closed、固定時間比較、429 形狀 |
| `image_safety.py` | 上傳圖片：分段讀取、magic bytes 白名單、偽 MIME、解壓縮炸彈、EXIF/GPS/XMP 清除 |
| `admin_audit.py` | 高風險 Admin 操作稽核（只存雜湊，不含 PII） |
| `job_store.py` | Firestore-backed job 儲存＋固定視窗限流計數（含唯讀 peek） |
| `dev_server_utils.py` | CORS origins 解析、本機 dev server 啟動（含 port 佔用提示） |
| `analysis_package.py` | 臉部分析結果正規化為前後端共用欄位（純資料整理，不呼叫服務） |

---

## 二、AI Gateway（`ai_gateway.py`）

前端唯一入口，也是所有安全防線集中的地方。

### 2.1 端點

**公開／認證**
| 方法 路徑 | 說明 |
|-----------|------|
| `GET /health` | 健康檢查 |
| `GET /public-config` | 只回同源路徑（`/member-database`、`/product-api` 等），**不交出上游真實網址** |
| `POST /auth/login` | 帳密登入；成功後種 `__session`＋`dm_csrf` cookie |
| `POST /auth/logout` | 登出（可帶 `?actor=` 只登出一個帳號，多帳號模式） |
| `GET /auth/session` | 進私有頁前確認兩層 session；回 `sub`／`actorId`／`role`／`status` |
| `POST /auth/register` `POST /auth/send-otp` `POST /auth/verify-otp` | 轉發會員 DB（公開，不需 session） |

**媒體（登入後）**
| `GET /media/render/{job_id}` `/before` | 渲染妝後／妝前圖，經 owner 驗證串流代理 |
| `GET /media/legacy/{media_token}` | legacy 簽章媒體，非 owner 回 404 |

**Admin（需 `role=admin`＋CSRF）**
| `GET/POST /admin-api/products`、`GET/PATCH/DELETE /admin-api/products/{id}` | 商品管理 |
| `POST /admin-api/crawler/product-preview`、`/search-preview` | 爬蟲預覽 |
| `GET /admin-api/product-audit-logs` | 商品稽核 |

**通用代理**
| `GET/POST /product-api/{path}` | 公開商品查詢／推薦（白名單路徑） |
| `{GET,POST,PATCH,DELETE} /{service}/{path}` | 通用代理到 `face-basic`／`face-pro`／`render-service`／`text-suggestion`／`member-database` |

### 2.2 UPSTREAMS 設定（`ai_gateway.py:59`）

每個上游是一個 `Upstream(base_url, allowed_paths, requires_cloud_run_iam)`：
- `allowed_paths`：正則白名單，`is_path_allowed()` 用 `fullmatch` 檢查——**只有明列的路徑能被代理**，多一條路徑就是多一個要記得擋的地方。
- `requires_cloud_run_iam`：`face-basic`／`face-pro`／`render-service` 為 True（帶 IAM identity token）；`text-suggestion`／`member-database` 為 False（帶 `x-api-key`）。

### 2.3 會員 Session（`__session` cookie）

- 單一 cookie `__session` 內含兩半：`gateway_access_token`（Gateway 簽的 JWT）＋ `sealed_member_cookie`（封裝過的上游會員 cookie）。分隔字元 `SESSION_COOKIE_SEPARATOR`。
- 屬性：`HttpOnly + Secure(production) + SameSite=Lax + Path=/`。
- JWT：HS256，`aud=decorate-me-ai`、`iss=decorate-me-ai-gateway`，claims 含 `sub`（email）／`role`／`status`／`exp`／`iat`。TTL 由 `GATEWAY_SESSION_TTL_SECONDS`（預設 2 小時）。
- 上游會員 cookie 用 `cryptography` Fernet 對稱加密封裝（`seal_member_cookie`／`unseal_member_cookie`），TTL 與 session 一致。
- **多帳號模式**（旗標 `GATEWAY_MULTI_SESSION`，預設關）：`__session` 改成 `v2.` + base64(JSON 陣列)，每個帳號一個槽位；`set_session_slots` 從最舊淘汰直到塞得下 cookie 大小上限。

### 2.4 安全防線一覽

| 機制 | 函式 | 說明 |
|------|------|------|
| **跨分頁帳號隔離** | `enforce_expected_actor` | 狀態變更請求必帶 `X-Expected-Actor`（分頁登入時 pin 的 opaque actor），不符或缺少在到達上游前回 **409**。擋「別的分頁登入把這個分頁的寫入導到別人帳號」。 |
| **CSRF double-submit** | `enforce_csrf` | 管理端 Proxy 與 admin 角色的會員寫入，要求 `X-CSRF-Token` 標頭 == `dm_csrf` cookie（`secret_equals` 固定時間比較）。登入發、`/auth/session` 對已登入無 token 者補發。 |
| **會員路徑授權** | `_authorize_member_path` | `/api/members/{email}/…` 的 email 必須等於 session 的 `sub`；`api/members` 名冊只給 admin（`MEMBER_LIST_PATH_RE`）。 |
| **登入限流** | `enforce_login_rate_limit`／`record_failed_login` | 見 §2.5。 |
| **body 大小** | `MAX_BODY_BYTES`／`ADMIN_PROXY_MAX_BODY_BYTES` | 先看 `Content-Length`、再看實際 body，超限 413。 |
| **回應標頭** | — | 一律加 `X-Content-Type-Options: nosniff`；只透傳 `content-type`／`cache-control`／`retry-after`／`etag`／`x-request-id`。CORS `expose_headers` 放行 `Retry-After`／`X-Request-ID`。 |
| **Render 回應清洗** | `_sanitize_render_payload` | 移除 provider 暫存網址，只留穩定的 Gateway 媒體路徑。 |
| **會員刪除連動** | 通用代理 DELETE 分支 | 刪 saved-look／會員時先刪 GCS 媒體與 Render job，失敗即 503 中止、不回假成功。 |

### 2.5 登入限流（2026-07-25 修正）

- 兩個維度：`ip:<client_ip>` 與 `account:<sha256(email)>`（**不存 email 本身**）。
- **只計失敗登入**：`enforce_login_rate_limit` 進來只「唯讀檢查」（`_login_quota_check` → `job_store.peek_window_quota`）；真正記一次是在帳密錯誤時呼叫 `record_failed_login`（`_login_quota_record` → `consume_window_quota`）。→ 管理員用正確密碼反覆測試不會把自己鎖住。
- **IP 辨識不出就不設 IP 維度**：`client_ip` 在 Firebase→Cloud Run 下可能回 `unknown`，`_login_rate_keys` 遇到 `unknown` 就跳過 IP 維度，只靠可靠的帳號維度——避免全站塞進同一桶一起 429。
- 跨 instance 一致優先走 Firestore 固定視窗（`GATEWAY_LOGIN_LIMIT_COLLECTION`），Firestore 不可用才退回程序內記憶體。
- 上游 DB 自己回 429 時，Gateway 改回 `MEMBER_SERVICE_RATE_LIMITED`（區分是哪一層在限流）。
- 預設：視窗 600 秒、上限 10 次（`GATEWAY_LOGIN_RATE_LIMIT_*` 可覆寫）。

### 2.6 admin 稽核串接

通用代理與 admin proxy 在狀態變更成功／失敗後呼叫 `record_admin_action`（見 §六）：
- 商品：`_audit_action("product", method)` → `product.create/update/delete`，`target_ref=upstream_path`。
- 會員（admin 對別人帳號）：`_audit_action("member", method)`，`target_ref=target_owner_id`（雜湊 actor）。

---

## 三、Face BASIC（`Face_analyzer_BASIC.py`）

### 3.1 端點

| 方法 路徑 | 說明 |
|-----------|------|
| `GET /health` | 健康檢查（附 job 統計與限制） |
| `POST /analyze`、`POST /v1/face/analyze/basic` | 同步分析，回完整結果 |
| `POST /v1/face/pose` | 只回拍攝角度（yaw/pitch/roll/captureRole/side/confidence） |
| `POST /v1/face/jobs/basic` | 建非同步 job，回 `jobId`＋`resultToken` |
| `GET /v1/face/jobs/{job_id}` | 查 job 狀態（需 `X-Job-Token` 或 `result_token`） |
| `GET /v1/face/jobs/{job_id}/result` | 取結果（需 token，且 status=completed） |

### 3.2 認證與上傳

- **API key 守衛**：非 `/health`、非根路徑、非 OPTIONS 一律要 `x-api-key == FACE_API_KEY`（`secret_equals`）。缺金鑰**拒絕啟動**（`enforce_service_api_key`，見 §六）。
- **上傳安全**：`_read_clean_image` → `image_safety.sanitize_upload`（分段讀取／magic bytes／偽 MIME／像素上限／EXIF-GPS-XMP 清除／解碼逾時）。三個入口（analyze、pose、jobs/basic）都走同一條。

### 3.3 分析內容

- **模型**：InsightFace `buffalo_l`（detection＋landmark_3d_68）＋ MediaPipe FaceMesh，只載一次並在 lifespan 預熱。
- **輸出**（`export_json`）：臉型、眉型、眼型、鼻型、嘴型、膚色（四季型／膚色分級／LAB）、嘴唇 LAB、臉部對稱性、拍攝角度。
- **分類來源**：ROI CNN（shadow，`ROI_MODEL_FIRST`）＋幾何決策樹（眼型／臉型）＋規則式 fallback。詳見 `模型訓練記錄_2026-07-24.md`。
- **拍攝角度門檻**：`FACE_YAW_LIMIT`／`FACE_PITCH_LIMIT` 等，角度抑制預設關閉（CNN 對角度不敏感）。

### 3.4 Job 狀態機（2026-07-25 拆分，2026-08-03 再分出照片問題）

`_run_basic_job` 分兩段 try，**分析段有三種結果**：

| 情況 | `stage` | `error.code` | `error.message` |
| --- | --- | --- | --- |
| 照片本身的問題 | `unusable_image` | `FACE_IMAGE_UNUSABLE` | 可行動的中文指引，逐字來自例外 |
| 分析出錯（內部） | `failed` | `FACE_ANALYSIS_ERROR` | 固定句「臉部分析失敗，請稍後再試」 |
| 封裝／寫回失敗 | `analysis_completed` | `PACKAGE_BUILD_FAILED` | 前端明說「不需重拍」 |

三者 `status` 都是 `failed`、`retryable` 都是 `true`。

前兩者的分岔在 `fail_job()`（`Face_analyzer_BASIC.py`，BASIC 與 PRO 共用）：`FaceAnalyzer` 對照片問題一律 raise 中文 ValueError（`pose_guidance` 的「請把頭轉向你的右邊」、「沒偵測到人臉」、「膚色區域不足…」），那些句子是寫給會員看的；但 numpy／cv2 也會 raise ValueError，訊息是英文內部文字。`unusable_image_message()` 用「訊息裡有沒有中文」分開兩者，內部錯誤走 `FACE_ANALYSIS_ERROR` 並保留 traceback，不會把英文內部字串當成重拍建議送到前端。

> **`FACE_IMAGE_UNUSABLE` 刻意不要收進前端的 `USER_ERROR_ZH` 對照表。** 那張表一旦命中就會用固定字串取代訊息，收錄了反而會把這裡的具體指引蓋掉。目前前端 `localizeUserError()` 查表未命中、訊息含中文時會原樣顯示，正是這個碼要的行為。

狀態轉換用 `job_store.patch_if_status`（只在預期狀態下更新，擋「完成的 job 被延遲 worker 覆寫回 running」）。

---

## 四、Face PRO（`Face_analyzer_PRO.py`）

- 端點同 BASIC，前綴改 `/v1/face/analyze/pro`、`/v1/face/jobs/pro`。
- 收最多四張照片（front 必填、left45／right45／side 選填），每張都走 `_read_image` → `sanitize_upload`。
- `_merge_basic_and_pro`：正面照沿用 BASIC 分析；側面照（約 10° yaw）用於**側臉鼻型輔助分類**與對稱性輔助。**膚色不做雙角度平均**（2026-08-05 起）——側面照的頰部 ROI 無法做遮擋檢查，混進來等於有一半的膚色訊號沒把關，改為一律只採正面照。**精細分析狀態誠實標註**：側臉鼻型驗證樣本仍有限，回應會附 caveat。
- Job 狀態機同 BASIC（analysis vs package 拆分）。

---

## 五、Render（`replicate_render_api.py` ＋ `replicate_render.py`）

### 5.1 端點

| 方法 路徑 | 說明 |
|-----------|------|
| `GET /health` | 健康檢查（附 rate limit 設定） |
| `POST /render` | 同步渲染 |
| `POST /render/jobs` | 建渲染 job（需 `X-User-Id`＝會員身分，否則 401） |
| `GET /render/jobs/{job_id}` | 查 job（需 `X-Job-Token`＋**owner**，2026-07-25 加 owner 驗證） |
| `GET /render/jobs/{job_id}/signed-url` | 簽 10 分鐘短效網址（owner 驗證；`variant=after/before`） |
| `GET /render/jobs/{job_id}/content` | 串流圖片內容（owner 驗證） |
| `POST /render/jobs/{job_id}/retain` | 收藏時把妝前／妝後圖從 `temporary/` 移到 `retained/` |
| `DELETE /render/jobs/{job_id}/artifact`、`/render/media`、`/render/users/{owner_id}`、`/render/jobs/{job_id}` | 刪除連動 |

### 5.2 認證與限流

- **API key 守衛**：`require_api_key`（`RENDER_API_KEY`，`secret_equals`）。缺金鑰拒絕啟動。
- **Job token**：`_verify_job_token` 用 `secret_equals`（2026-07-25 修好先前的 `!=`）；`get_render_job` 另加 `_require_job_owner`（token＋owner 都要對）。
- **限流／配額**：`enforce_render_rate_limit`（每視窗次數）＋`enforce_render_quota`（每日／全站），都經 `rate_limited_error`（`Retry-After`＋`retryAfterSeconds`）。key = IP｜email。跨 instance 走 Firestore。

### 5.3 圖片與 SSRF

- 前端送的 data URL、或 `imageUrl` 抓下來的圖，一律經 `data_url_to_bytes`／`sanitize_data_url`（`image_safety` 清洗，去 EXIF/GPS/XMP）後才送第三方。
- `fetch_remote_image_bytes`（SSRF 防護）：只允許 https＋白名單 host（`replicate.delivery`／`storage.googleapis.com`，可用 `RENDER_FETCH_ALLOWED_HOSTS` 覆寫）＋443 埠＋**不跟隨轉址**＋串流逐塊卡 `MAX_RENDER_IMAGE_BYTES`。
- 像素上限 `MAX_RENDER_IMAGE_PIXELS`（預設 16M）；重編碼驗證是真圖。

### 5.4 渲染 prompt（2026-07-25 結構化）

- `RENDER_STYLE_SECTIONS`：8 種風格各拆四段（**Base 底妝／Brows and eyes 眉眼／Cheeks and contour 腮紅修容／Lips 唇妝**）。
- `build_server_render_prompt` → `format_makeup_sections`：輸出帶標籤的分段指令，模型分區套色（不會把腮紅色套到唇上）。
- **identity lock** 一律由 Render 自己疊在最後（不可改臉型／五官／膚色／姿勢／背景）——不讓外部模型決定「可不可以改長相」。
- 攤平字串 `RENDER_STYLE_PROMPTS` 由四段推導，dedup key／health 廣告／測試型別不變。
- 個人化：`build_personalized_render_prompt` 由 Render 服務**自己**去跟建議服務（`SUGGESTION_SERVICE_URL/suggest`）要 `renderPromptEn`；拿不到就退回 styleId 白名單固定 prompt（渲染絕不因建議服務掛掉而失敗）。前端**不**經手 prompt（`renderApiKey` 是明文公開，開放自由 prompt 等於讓任何人燒 Replicate 額度）。

### 5.5 儲存與生命週期（GCS）

- bucket `decorate-me-renders`（私有，Public Access Prevention＋Uniform BLA）。
- `temporary/`：新渲染先存這，2 天 lifecycle 自動刪未收藏工作。
- `retained/{opaqueOwnerId}/{jobId}`：收藏後移入，不受 lifecycle 影響。
- 刪 saved-look 時 Gateway 同步刪 Render job 與 GCS 物件；刪物件失敗即保留 job、標 failed、可重試（不回假成功）。

---

## 六、共用模組細節

### 6.1 `api_errors.py`

- **錯誤 envelope**：`error_payload(code, message, retryable, **extra)` → `{"error": {...}}`；HTTP／validation／未處理例外都轉成同一形狀，附 `X-Request-ID`。
- **429 統一形狀**：`rate_limited_error(code, message, retry_after_seconds, **extra)` → `Retry-After` 標頭 ＋ 回應內 `retryAfterSeconds`（跨來源 fetch 讀不到自訂標頭，前端顯示秒數只能靠 body）。
- **log 遮罩**：`RedactingFormatter` 掛 root logger，洗每筆 log 成品（含 traceback）的 email、URL query、`Authorization`／`Cookie`／`x-api-key`／`x-job-token`／Bearer。`redact_log_path` 把路徑裡的 email（含 `%40`）遮成 `<member>`。
- **服務金鑰 fail-closed**：`enforce_service_api_key(app, env_name)` 掛在**啟動流程**（不是 import 時，離線訓練工具照常 import）。缺金鑰即拒絕啟動；`ALLOW_INSECURE_LOCAL_DEV=1` 才免驗證，**且該旗標在 `APP_ENV=production` 下一律失效**。
- **固定時間比較**：`secret_equals(provided, expected)` 以 bytes 比較（`hmac.compare_digest`），非 ASCII 金鑰回 401 而非 500。

### 6.2 `image_safety.py`

- `read_upload_within_limit`：分段讀取（256KB/塊），累計超限即停（413 `PAYLOAD_TOO_LARGE`），空檔 400 `EMPTY_UPLOAD`。
- `sniff_image_format`：只認 JPEG（`FFD8FF`）／PNG（`89504E47…`）／WebP（RIFF…WEBP）的實際檔頭。
- `sanitize_image_bytes`：
  - 偽 MIME → 400 `IMAGE_TYPE_MISMATCH`／`INVALID_IMAGE`（檔頭 vs Pillow 解出格式比對）。
  - 解碼**前**卡像素數（`Image.MAX_IMAGE_PIXELS`）→ 413 `IMAGE_DIMENSIONS_TOO_LARGE`（`_too_many_pixels` 統一）。
  - 中繼資料鍵 `_METADATA_INFO_KEYS = exif, xmp, comment, Comment, XML:com.adobe.xmp, icc_profile`（2026-07-25 補 **xmp**，堵住「GPS 只藏在 JPEG XMP、沒有 EXIF」的漏洞）。有中繼資料就重編碼（重編前先 `exif_transpose` 保方向），沒有就走快速路徑原封回傳。
- `sanitize_upload`（async）：分段讀取 → `asyncio.to_thread(sanitize_image_bytes)` 套 `wait_for` 逾時（504 `IMAGE_DECODE_TIMEOUT`）。
- 拒絕例外 `ImageRejected(HTTPException)`，帶標準 envelope。

### 6.3 `admin_audit.py`

- `record_admin_action(action, *, actor_id, target_ref, status_code, request_id, **extra)`：寫一筆事件，**永不丟例外**（稽核失敗不影響使用者操作）。
- 只收純量 extra（dict/list 很可能是 body，不落地）；`request_id` 內部截 128。
- 欄位：`eventId`／`action`／`actorId`（雜湊）／`targetRef`（雜湊或商品 ID）／`statusCode`／`outcome`（success/failure）／`requestId`／`at`／`expiresAt`（Firestore TTL）。
- `recent_admin_actions(limit)`：新到舊，給稽核頁。

### 6.4 `job_store.py`

- Firestore-backed（`GOOGLE_CLOUD_PROJECT`），無 `google.cloud.firestore` 時退回程序內記憶體（本機開發）。
- `create`／`get`／`patch`／`unset`／`delete`／`all_jobs`／`find_by_field`。
- `patch_if_status`：transaction guard，只在預期狀態下更新。
- `consume_window_quota`：原子固定視窗計數（含 `expiresAt` 給 TTL）。
- `peek_window_quota`（2026-07-25）：唯讀檢查是否超量，**不消耗**額度——登入限流「只計失敗」靠它。

### 6.5 `dev_server_utils.py`

- `get_cors_origins`：讀 `CORS_ORIGINS`；`REQUIRE_EXPLICIT_CORS=1` 或 `APP_ENV=production` 時禁止 `*`。
- `run_dev_server`：本機啟動，port 被佔用時給清楚提示（含健康檢查建議）。

---

## 七、資料儲存

### 7.1 Firestore collections

| collection | 用途 |
|-----------|------|
| `face_jobs_basic` / `face_jobs_pro` | Face 非同步 job |
| `render_jobs`（`RENDER_JOBS_COLLECTION`） | 渲染 job |
| `render_quota_counters` | 渲染配額固定視窗計數 |
| `gateway_login_limits`（`GATEWAY_LOGIN_LIMIT_COLLECTION`） | 登入限流固定視窗計數 |
| `admin_audit_events`（`ADMIN_AUDIT_COLLECTION`） | Admin 稽核事件 |

### 7.2 GCS

- bucket `decorate-me-renders`（私有）；prefix `temporary/`（2 天）、`retained/{opaqueOwnerId}/{jobId}`、`rendered/`（legacy）。
- 擁有者目錄用不可逆雜湊 `opaqueOwnerId`，不用完整 email。

---

## 八、環境變數完整表

### Gateway
`GATEWAY_SESSION_SECRET`（必填，簽 JWT）、`GATEWAY_FACE_API_KEY`／`GATEWAY_RENDER_API_KEY`／`GATEWAY_MEMBER_API_KEY`／`GATEWAY_TEXT_SUGGESTION_API_KEY`（呼叫上游用）、`PRODUCT_ADMIN_API_KEY`、`GATEWAY_SESSION_TTL_SECONDS`(7200)、`GATEWAY_SESSION_ONLY`、`GATEWAY_MULTI_SESSION`(0)、`GATEWAY_MAX_SESSION_SLOTS`／`GATEWAY_MAX_SESSION_COOKIE_BYTES`、`GATEWAY_LOGIN_RATE_LIMIT_WINDOW_SECONDS`(600)／`_MAX_REQUESTS`(10)／`GATEWAY_LOGIN_LIMIT_COLLECTION`、`GATEWAY_ALLOW_EXTERNAL_TEXT_UPSTREAM`(0)、`AI_GATEWAY_MAX_BODY_BYTES`／`ADMIN_PROXY_MAX_BODY_BYTES`／`ADMIN_PROXY_TIMEOUT_SECONDS`／`AI_GATEWAY_UPSTREAM_TIMEOUT_SECONDS`、`UPSTREAM_*_API_KEY`、`APP_ENV`。

### Face BASIC／PRO
`FACE_API_KEY`（必填）、`MAX_UPLOAD_BYTES`(8MB)／`MAX_IMAGE_PIXELS`(16M)／`MAX_IMAGE_SIZE`(1024)、`INSIGHTFACE_ROOT`／`INSIGHT_DET_SIZE`(384)／`INSIGHT_ALLOWED_MODULES`、`FACE_JOB_TIMEOUT_SECONDS`(180)／`_RETENTION_SECONDS`(3600)／`_MAX_COUNT`(200)、`FACE_YAW_LIMIT`／`FACE_PITCH_LIMIT` 等、`BRIGHTNESS_*`、`FRONTEND_URL`、`LOG_LEVEL`。

### Render
`RENDER_API_KEY`（必填）、`REPLICATE_API_TOKEN`／`OPENAI_API_KEY`、`IMAGE_PROVIDER`(replicate)／`REPLICATE_MODEL`／`OPENAI_IMAGE_MODEL`、`RENDER_RATE_LIMIT_*`／`RENDER_QUOTA_*`／`RENDER_JOB_*`／`RENDER_DEDUP_TTL_SECONDS`／`RENDER_DURABLE_DEDUP_ENABLED`、`MAX_RENDER_IMAGE_BYTES`(8MB)／`MAX_RENDER_IMAGE_PIXELS`(16M)／`MAX_RENDER_IMAGE_CHARS`／`MAX_ANALYSIS_PACKAGE_CHARS`、`GCS_RENDER_BUCKET`／`GCS_RENDER_RETENTION_DAYS`／`GCS_SIGNED_URL_SECONDS`／`GCS_SIGNING_SERVICE_ACCOUNT`、`RENDER_FETCH_ALLOWED_HOSTS`／`RENDER_GUIDANCE`／`RENDER_FALLBACK_ASPECT_RATIO`、`SUGGESTION_SERVICE_URL`／`SUGGESTION_SERVICE_API_KEY`／`SUGGESTION_SERVICE_TIMEOUT`、`TRUST_FORWARDED_FOR`。

### Ollama Suggestion
`SUGGESTION_API_KEY`、`OLLAMA_BASE_URL`／`OLLAMA_MODEL`／`OLLAMA_NUM_PREDICT`／`OLLAMA_TIMEOUT`。

### 共用
`ALLOW_INSECURE_LOCAL_DEV`（本機免驗證，production 下失效）、`APP_ENV`、`CORS_ORIGINS`／`REQUIRE_EXPLICIT_CORS`、`GOOGLE_CLOUD_PROJECT`、`ADMIN_AUDIT_COLLECTION`／`ADMIN_AUDIT_RETENTION_DAYS`(180)、`IMAGE_DECODE_TIMEOUT_SECONDS`(20)、`LOG_LEVEL`。

---

## 九、部署

### 9.1 Docker（2026-07-25 加固）

- 三份 Dockerfile（`Dockerfile` BASIC＋PRO＋Ollama、`Dockerfile.gateway`、`Dockerfile.render`）base image 都**釘 digest**（`python:3.x-slim@sha256:…`）。
- 各加 `appuser`（uid 10001）並 `USER 10001`（非 root）。
- BASIC 的 `build-essential` 在同一 RUN 內裝→`pip install`→`purge`，不留在映像。
- `.dockerignore` 是 deny-all＋允許清單；新模組（`image_safety.py`／`admin_audit.py`）與 BASIC 相依（`mediapipe_ascii.py`／`basic_rule_trees.py`／`rule_features.py`）都已放行。
- **正式部署走 `.gcloudignore`（Cloud Build），不受 `.dockerignore` 影響**；`.dockerignore` 只管本機／CI 的 `docker build`。

### 9.2 CI（`.github/workflows/ci.yml`）

| job | 內容 | 阻擋部署？ |
|-----|------|-----------|
| `backend-tests` | `ai_gateway_test.py`＋`render_api_test.py`＋`image_safety_test.py` | ✅ |
| `lint` | ruff 只選會壞掉的規則（E9,F63,F7,F82,F401,F841） | ✅ |
| `secret-scan` | 擋可部署碼裡的 trycloudflare 網址／上游金鑰欄位 | ✅ |
| `dependency-audit` | pip-audit（諮詢性） | ❌ continue-on-error |
| `docker-image-scan` | build gateway 映像＋Trivy（官方腳本安裝，非 marketplace action） | ❌ 諮詢性 |
| `lint` 內 `mypy` | 型別檢查（諮詢性） | ❌ |

### 9.3 部署腳本

`deploy_render_cloudrun.ps1`：部署前跑測試、**紅燈中止**（`-SkipTests` 才略過）；用 `--no-allow-unauthenticated`（先前每跑一次就把已移除的 allUsers 綁回去）；部署後帶 identity token 打 `/health`。

### 9.4 本機一鍵啟動

`start_full_stack_local.bat`：啟動前端 dev server(5500)＋Gateway(8015)＋BASIC(8001)＋PRO(8002)＋Ollama(8010)；就緒判斷用各服務 `/health` 回 200（不只是 port 在聽）。前端未設 `aiGatewayUrl` 且從 localhost 開時自動指向本機 Gateway。

---

## 十、測試

- **後端單元測試（91 項全過）**：`ai_gateway_test.py`（session／CSRF／稽核／限流／路徑授權／路由白名單）、`render_api_test.py`（job owner／dedup／SSRF／偽 MIME／刪除連動）、`image_safety_test.py`（分段讀取／magic bytes／偽 MIME／解壓縮炸彈／EXIF＋XMP 清除）。
- **實機 smoke**：`backend_smoke_test.py`（BASIC/PRO sync＋job＋result，加圖片安全拒絕案例）；`ollama_suggestion_smoke_test.py`。
- **前端 smoke**：`frontend_smoke_check.js`（錯誤中文化／限流等待／頭像不落地／admin 只認後端角色／session pin）。

---

## 十一、已知限制與待外部組（⛔）

我方後端已完成 P0 全部 🔧 項目。以下需別組配合（詳見各 `給*端_待辦彙總規格_2026-07-25.md`）：

- **資料庫端**：OTP 假信箱（S7）／登入枚舉（S55）、會員硬刪除 deletion job、`sessionVersion` 撤銷、memberId/`/me`、Job token 只存雜湊、點數／歷史／收藏／購物車端點、renderQuota 來源、註冊冪等（EMAIL_EXISTS 區分已/未驗證）、以真實 `X-Forwarded-For` 計登入限流、schema migration、縮圖／索引。
- **Ollama 端**：金鑰輪替（S5）、固定端點＋服務身分限制、Prompt 展示 Demo 後移除。
- **爬蟲端**：商品資料 schema 驗證、後端 SSRF／host 驗證、縮圖、搜尋預覽端點、搬離 tunnel。
- **演算法端**：recommend-products 端點＋style、配額依可信 userId、資料最小化＋TTL、統一 Job 狀態＋持久佇列。
- **我方待決策／待補**：Signed URL V4 自簽、BASIC/PRO 部署前 smoke gate、頭像上傳私有物件、Render 解碼逾時（目前靠像素上限）。

---

## 十二、相關文件

- `系統資安效率流程改善總表.md`（單一權威待辦表）
- `歷史流程更改追蹤.md`（完整變更歷程）
- `模型訓練記錄_2026-07-24.md`（五官分類模型）
- `給{資料庫,Ollama,爬蟲,演算法}端_待辦彙總規格_2026-07-25.md`（各組規格）
- GitHub issue #28（部署前 Demo 驗證清單）
