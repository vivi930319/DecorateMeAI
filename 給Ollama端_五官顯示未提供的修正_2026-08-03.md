# 給 Ollama 端 — 妝容建議的五官欄位顯示「未提供」

**日期：** 2026-08-03
**提出方：** DECORATE ME 前端 + 臉部分析端
**對象：** 文字建議服務（Ollama）維運者
**影響範圍：** `POST /suggest` 產生的建議內文
**要你們做的事：** 同步 `Ollama_suggestion.py` 的 `_pick()`（一個函式，約 10 行）

---

## 0. 一句話

`_pick()` 判斷的是「這個鍵在不在」而不是「這個鍵有沒有值」，於是前端送來的 `null` 讓它直接回「未提供」，底下那條 `raw` 的退路永遠走不到——**而答案其實就在 `raw` 裡**。

---

## 1. 現象

使用者產生妝容建議，內文的五官摘要變成：

```
臉型：未提供、眉型：未提供、眼型：未提供、鼻型：未提供、唇型：未提供、膚色：秋季
```

建議照樣生得出來，**沒有任何地方報錯**，所以我們是靠使用者回報才發現的。

---

## 2. 原因

`Ollama_suggestion.py` 的 `_pick()`：

```python
def _pick(face_analysis, english_key, chinese_key, default="未提供"):
    if english_key in face_analysis:
        return face_analysis.get(english_key) or default   # ← 鍵在、值是 None 就結束
    if chinese_key in face_analysis:
        return face_analysis.get(chinese_key) or default
    raw = face_analysis.get("raw") or {}
    return raw.get(chinese_key) or default                 # ← 永遠到不了
```

而前端建 `faceAnalysis` 時是**無條件**建出這五個鍵的：

```js
faceShape: raw?.['臉型'] || null,     // 中文值缺 → 寫 null，不是「不寫這個鍵」
```

所以 `english_key in face_analysis` 恆為真。只要上游任何一步讓中文欄位變空，第一個分支就回 `未提供`，**`raw` 裡明明還留著完整答案卻不會被讀到**。

---

## 3. 修正

我們 repo 裡已經改好（commit 見下），請照抄：

```python
def _pick(face_analysis: dict[str, Any], english_key: str, chinese_key: str, default: str = "未提供") -> Any:
    """依序找英文鍵、中文鍵、raw 裡的中文鍵，全都沒有才回 default。"""
    for source, key in (
        (face_analysis, english_key),
        (face_analysis, chinese_key),
        (face_analysis.get("raw") or {}, chinese_key),
    ):
        value = source.get(key)
        if value:
            return value
    return default
```

差別只有一點：**改成「找到有值的才用」**，而不是「鍵存在就用」。其餘行為完全不變——正常情況下第一個分支就命中，跟原本一樣。

### 驗證

六個案例，涵蓋各種缺值組合：

| 輸入 | 期望 |
| --- | --- |
| `{"faceShape": "鵝蛋臉", "raw": {"臉型": "鵝蛋臉"}}` | `鵝蛋臉` |
| `{"faceShape": None, "raw": {"臉型": "圓形臉"}}` | `圓形臉` ← 修正的重點 |
| `{"faceShape": "", "raw": {"臉型": "方形臉"}}` | `方形臉` |
| `{"臉型": "長形臉"}` | `長形臉` |
| `{"faceShape": None, "raw": {}}` | `未提供` |
| `{}` | `未提供` |

---

## 4. 為什麼要你們動手

`TEXT_SUGGESTION_URL` 指向 trycloudflare 通道，也就是這個服務跑在**你們的機器上**。我們 repo 裡這份 `Ollama_suggestion.py` 改了不會影響線上行為，**必須你們那邊同步才會生效**。

如果你們的部署來源就是我們這個 repo，`git pull` 即可；如果是各自維護的副本，請把 §3 那段抄過去。

---

## 5. 我們還沒能確認的部分

**這是把退路接回去，不是確定找到了根因。**

我們拿真實分析輸出走完整條路徑（前端 `fromRawFaceAnalysis` → `_pick`），五個欄位都正常解析，**重現不出「未提供」**。所以有可能上游還有一步讓中文欄位變空，那一步我們還沒找到。

修好 `_pick` 之後如果還會出現「未提供」，代表 `raw` 裡也是空的，問題在更前面——請告訴我們，我們往臉部分析與修正流程那邊追。

---

## 6. 順帶一提：一個對你們有用的欄位

同一版起 `faceAnalysis.skinTone` 多了 `labReliable`（boolean）。`false` 代表這次的膚色取樣被頭髮或陰影污染了。

建議：`labReliable === false` 時，**建議內文不要寫死膚色相關的具體色號建議**，或加一句「膚色判斷可能受遮擋影響」。細節見〈演算法端接口規格書_analysis_package商品推薦_2026-07-13.md〉的 2026-08-03 追記。

這一項不緊急，`_pick` 那個才是現在就會被使用者看到的。
