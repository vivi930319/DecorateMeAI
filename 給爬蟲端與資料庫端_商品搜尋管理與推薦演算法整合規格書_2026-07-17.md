# 給爬蟲端與資料庫端：商品搜尋、後台管理與推薦演算法整合規格書

文件日期：2026-07-17  
適用系統：Decorate Me Web Admin、商品爬蟲、會員／商品資料庫、商品推薦演算法  
文件目的：讓管理員「找得到、爬得到、改得動、刪得掉」，並確保新爬商品可以真正進入推薦演算法計算，而不是只有圖片與名稱能展示。

---

## 0. 忙碌版結論

目前缺口不是只有前端按鈕，而是商品資料流沒有完整閉環：

```text
品牌／商品關鍵字
  → 搜尋資料庫既有商品
  → 若不存在，Google 搜尋來源頁或貼商品網址
  → 爬蟲擷取原始資料
  → 欄位標準化與演算法標籤補齊
  → 管理員預覽、修正、確認
  → 商品資料庫正式上架
  → 推薦演算法讀取可計算商品
  → 前端顯示推薦結果
```

三端必須共同完成：

1. **爬蟲端**不能只回名稱、價格與圖片，必須回傳或協助產生分類、色號、風格標籤與來源追蹤資料。
2. **資料庫端**必須保存演算法所需欄位，支援搜尋、CRUD、審核狀態、去重與 audit log。
3. **推薦端**必須使用資料庫商品的 `styleTags`、`lab`、`seasonTags` 等欄位計分，並回傳 `imageUrl` 與分數拆解。
4. **前端 Admin**所有新增、編輯、刪除與爬蟲匯入都必須寫入真實商品資料庫，不得只改 localStorage 或假資料。

---

## 1. 系統責任邊界

| 端 | 必須負責 | 不應負責 |
|---|---|---|
| Admin 前端 | 搜尋、預覽、人工修正、確認新增／編輯／刪除、顯示錯誤 | 直接寫資料庫、私自產生正式商品 ID、前端自行算推薦分數 |
| 爬蟲端 | 取回來源頁、解析商品、清理文字、抽取圖片／價格／色號／規格、產生候選標籤 | 未經管理員確認直接正式上架、把網站 HTML 原文直接當商品資料 |
| 商品資料庫端 | 欄位驗證、去重、CRUD、搜尋、狀態、版本、audit log、提供推薦候選商品 | 信任瀏覽器傳來的管理員角色、用商品名稱當唯一鍵 |
| 推薦演算法端 | 依臉部分析、風格、色彩與商品標籤計分排序 | 從商品名稱猜全部欄位、推薦未上架或缺庫存商品 |

---

## 2. 管理員商品管理需求

### 2.1 搜尋既有商品

管理員可輸入：

- 品牌：`YSL`、`Za`、`rom&nd`
- 商品名稱：`情挑誘光水唇膏`
- 分類：`唇彩`、`粉底`
- 關鍵字：`霧面`、`冷粉色`、`持妝`
- 商品 ID 或來源網站

前端目前先對已載入的 `/api/products` 即時篩選。資料量增加後，資料庫端必須支援伺服器搜尋：

```http
GET /api/products?q=YSL%20唇膏&status=active&limit=50&cursor=...
```

搜尋欄位至少涵蓋：

```text
name, brand, category, type, description,
style_tags, finish_tags, season_tags,
shade_name, source_site, source_url, id
```

### 2.2 Google 搜尋引導

若資料庫查無商品，Admin 可開啟：

```text
https://www.google.com/search?q=<管理員關鍵字>+彩妝+商品
```

注意：

- Google 只用來協助管理員找到官方／電商商品頁。
- 不可將 Google 搜尋結果摘要直接當成正式商品資料。
- 管理員選定來源頁後，仍須交給爬蟲預覽並人工確認。

### 2.3 新增、編輯、刪除

Admin 左側固定顯示：

- `新增商品`
- `儲存編輯`
- `刪除商品`
- `取消選取`

規則：

1. 未選商品時，只允許新增。
2. 點商品列後，將資料庫商品帶入表單，才啟用編輯與刪除。
3. 新增成功後重新呼叫 `GET /api/products`，不得只在畫面自行插入。
4. 編輯成功後重新讀取資料庫，以資料庫回傳內容為準。
5. 刪除成功後才從畫面移除；失敗時保留原資料。
6. 正式商用建議採 soft delete；畢業專題若採 hard delete，仍需保留 audit log。

---

## 3. 必要 API 一覽

| Method | Path | 使用者 | 用途 |
|---|---|---|---|
| GET | `/health` | 全端 | 健康檢查 |
| GET | `/api/products` | 前端／推薦端 | 商品清單與搜尋 |
| GET | `/api/products/{id}` | 前端／推薦端 | 單一商品完整資料 |
| POST | `/api/products` | Admin | 新增正式商品 |
| PATCH | `/api/products/{id}` | Admin | 編輯商品 |
| DELETE | `/api/products/{id}` | Admin | 下架／刪除商品 |
| POST | `/api/crawler/product-preview` | Admin | URL 爬取預覽，不直接上架 |
| POST | `/api/crawler/search-preview` | Admin | 選配：品牌／關鍵字取得候選來源頁 |
| POST | `/recommend-products` | 前端／整合服務 | 依 analysisPackage 推薦 |
| GET | `/api/admin/product-audit-logs` | Admin | 查看商品操作紀錄 |

所有 Admin 寫入端點必須驗證 Bearer Token 或安全 session，且由後端判斷 `role=admin`。

---

## 4. 統一商品資料 Schema

### 4.1 核心識別與管理欄位

| 欄位 | 型別 | 必填 | 說明 |
|---|---|---:|---|
| `id` | string/int | 回應必填 | 資料庫唯一 ID |
| `sku` | string/null | 建議 | 品牌 SKU；有資料時用於去重 |
| `name` | string | 是 | 商品正式名稱 |
| `brand` | string | 是 | 標準化品牌名稱 |
| `category` | enum | 是 | 前端中文分類 |
| `type` | enum | 是 | 演算法英文分類 |
| `description` | string | 建議 | 清理後商品描述 |
| `status` | enum | 是 | `draft` / `active` / `inactive` / `deleted` |
| `reviewStatus` | enum | 是 | `pending` / `approved` / `rejected` |
| `inStock` | boolean | 是 | 是否可推薦，預設不可假設 true |
| `createdAt` | ISO datetime | 回應必填 | 建立時間 |
| `updatedAt` | ISO datetime | 回應必填 | 最後修改時間 |
| `version` | integer | 建議 | 樂觀鎖版本，避免兩個管理員互相覆蓋 |

### 4.2 價格、圖片與來源欄位

| 欄位 | 型別 | 必填 | 說明 |
|---|---|---:|---|
| `price` | number | 是 | 純數字，例如 `1650`，不可存 `NT$1,650` |
| `currency` | string | 是 | ISO 4217，預設 `TWD` |
| `imageUrl` | URL | 是 | 前端與推薦結果使用的主圖 |
| `imageUrls` | URL[] | 否 | 多圖 |
| `sourceUrl` | URL | 是 | 原始商品頁 |
| `sourceSite` | string | 是 | 網域或網站代碼 |
| `sourceProductId` | string/null | 建議 | 來源站商品 ID |
| `lastCrawledAt` | ISO datetime/null | 建議 | 最後爬取時間 |
| `crawlFingerprint` | string/null | 建議 | URL＋SKU＋規格雜湊，用於去重 |

### 4.3 推薦演算法必要欄位

| 欄位 | 型別 | 必填規則 | 用途 |
|---|---|---|---|
| `styleTags` | string[] | 非底妝也建議必填 | 風格相似度 |
| `finishTags` | string[] | 建議必填 | 霧面、水光、珠光、自然等 |
| `seasonTags` | enum[] | 有色彩商品建議必填 | `spring/summer/autumn/winter/neutral` |
| `occasionTags` | string[] | 選填 | daily、office、party、date 等 |
| `featureTags` | string[] | 選填 | 適合眼型、臉型、唇型等 |
| `avoidTags` | string[] | 選填 | 不適合條件 |
| `shadeName` | string/null | 有色號時必填 | 色號名稱 |
| `hex` | string/null | 有色號時強烈建議 | `#RRGGBB` |
| `lab` | number[3]/null | 粉底／唇彩強烈必填 | ΔE 色差計算 |
| `coverage` | enum/null | 底妝建議 | `light/medium/full` |
| `undertone` | enum/null | 底妝建議 | `cool/warm/neutral/olive` |
| `texture` | string/null | 建議 | liquid、cream、powder、stick 等 |
| `recommendationReady` | boolean | 是 | 是否具備推薦計算最低欄位 |
| `dataQualityScore` | number 0..1 | 建議 | 欄位完整度與可信度 |

### 4.4 分類 enum 對照

| `category` | `type` | 推薦群組 |
|---|---|---|
| `底妝` | `foundations` | `base` |
| `眼影` | `eyeshadows` | `eye` |
| `眼線/睫毛` | `eyeliner_mascara` | `eye` |
| `唇彩` | `lipsticks` | `lip` |
| `腮紅` | `blushes` | `blush` |
| `眉毛彩妝` | `eyebrows` | `brow` |
| `修容` | `contouring` | `contour` |
| `打亮` | `highlighters` | `highlight` |

`type` 必須使用固定 enum，不可把來源網站的自由文字直接存入。

---

## 5. 爬蟲端規格

### 5.1 URL 預覽 Request

```http
POST /api/crawler/product-preview
Content-Type: application/json
Authorization: Bearer <admin-token>
```

```json
{
  "url": "https://brand.example/product/123",
  "source": "manual_admin_import",
  "adminId": "admin@example.com",
  "locale": "zh-TW"
}
```

### 5.2 預覽 Response

```json
{
  "ok": true,
  "partial": false,
  "product": {
    "name": "情挑誘光水唇膏 205",
    "brand": "YSL",
    "category": "唇彩",
    "type": "lipsticks",
    "price": 1650,
    "currency": "TWD",
    "imageUrl": "https://.../main.jpg",
    "imageUrls": ["https://.../main.jpg"],
    "description": "...",
    "sourceUrl": "https://brand.example/product/123",
    "sourceSite": "brand.example",
    "sourceProductId": "123",
    "shadeName": "205",
    "hex": "#C94F62",
    "lab": [48.1, 47.3, 18.2],
    "styleTags": ["glossy", "romantic", "elegant"],
    "finishTags": ["glossy", "sheer"],
    "seasonTags": ["summer", "winter"],
    "recommendationReady": true,
    "dataQualityScore": 0.88
  },
  "missingFields": [],
  "warnings": [],
  "crawlMeta": {
    "parser": "json-ld",
    "crawledAt": "2026-07-17T03:00:00+08:00"
  }
}
```

### 5.3 擷取優先順序

1. JSON-LD `Product`。
2. Open Graph / Twitter Card。
3. 頁面內結構化商品 JSON。
4. 靜態 DOM 選擇器。
5. 必要時才使用 Playwright 等動態瀏覽器。

### 5.4 標籤產生規則

- 來源頁明確寫出妝效時，可加入 `finishTags`。
- `styleTags` 必須經白名單 mapping，不可保存任意廣告文案。
- 無色碼時可回 `hex=null`，不得捏造；交由管理員補齊。
- 可由圖片估色產生 `hexCandidate`，但需標示 `confidence`，管理員確認後才寫正式 `hex`。
- `lab` 必須由正式 Hex／RGB 使用固定色彩轉換產生，避免各端算法不同。
- 推論欄位應附 `fieldSources`，區分 `source-page`、`image-estimate`、`rule-map`、`admin`。

### 5.5 去重鍵

資料庫端依序判斷：

1. `sourceSite + sourceProductId + shadeName`
2. `brand + sku + shadeName`
3. 正規化後 `sourceUrl`
4. `brand + normalizedName + shadeName`

重複時回 `409 PRODUCT_ALREADY_EXISTS`，並提供既有 `productId`，讓 Admin 直接切換編輯。

---

## 6. 商品資料庫 CRUD 契約

### 6.1 新增

```http
POST /api/products
Authorization: Bearer <admin-token>
```

資料庫必須：驗證 enum、正規化價格、檢查 URL、去重、計算 `recommendationReady`、寫 audit log。

### 6.2 編輯

```http
PATCH /api/products/{id}
Authorization: Bearer <admin-token>
If-Match: <version>
```

只更新 request 提供欄位；更新會使 `version + 1`，重新計算資料品質與推薦可用狀態。

### 6.3 刪除／下架

```http
DELETE /api/products/{id}
Authorization: Bearer <admin-token>
```

建議行為：

```json
{
  "ok": true,
  "id": 2499,
  "status": "inactive",
  "deletedAt": "2026-07-17T03:10:00+08:00"
}
```

推薦 API 必須立即排除 `inactive/deleted` 商品。

### 6.4 搜尋回應

```json
{
  "ok": true,
  "items": [],
  "total": 1250,
  "nextCursor": null,
  "query": "YSL 唇膏"
}
```

為相容現有前端，可暫時同時支援直接 array；正式版建議統一使用上述 envelope。

---

## 7. 推薦演算法整合

### 7.1 為什麼新爬商品目前沒有進入有效計算

若商品只有以下欄位：

```text
name, price, imageUrl, description
```

推薦端只能展示，無法可靠計算：

- 色差 ΔE
- 妝容風格相似度
- 四季型匹配
- 膚色 undertone
- 臉型／眼型適配
- 妝效與場合匹配

所以商品雖「進資料庫」，實際推薦可能永遠拿不到分數，或只能靠名稱猜測。

### 7.2 候選商品條件

推薦端只能讀取：

```text
status = active
reviewStatus = approved
inStock = true
recommendationReady = true
imageUrl != empty
```

### 7.3 計分輸入

商品端欄位需對應 `analysisPackage`：

| analysisPackage | 商品欄位 | 計算 |
|---|---|---|
| `faceAnalysis.skinTone.lab` | `lab` | 粉底 ΔE |
| `faceAnalysis.lipLab` | `lab` | 唇彩 ΔE |
| `faceAnalysis.skinTone.season` | `seasonTags` | 季型分數 |
| `generativeText.styleTags` | `styleTags` | tag 相似度 |
| `generativeText.preferredColors` | `hex/lab` | 色彩偏好 |
| `generativeText.avoidTags` | `styleTags/finishTags` | 扣分 |
| `faceAnalysis.faceShape` | `featureTags` | 修容／腮紅適配 |
| `faceAnalysis.eyeShape` | `featureTags` | 眼妝適配 |

### 7.4 建議計分權重

| 類別 | color | style | feature | availability |
|---|---:|---:|---:|---:|
| base | 0.75 | 0.05 | 0.10 | 0.10 |
| lip | 0.55 | 0.30 | 0.05 | 0.10 |
| eye | 0.25 | 0.55 | 0.10 | 0.10 |
| blush | 0.35 | 0.45 | 0.10 | 0.10 |
| contour | 0.20 | 0.25 | 0.45 | 0.10 |
| highlight | 0.30 | 0.45 | 0.15 | 0.10 |
| brow | 0.25 | 0.35 | 0.30 | 0.10 |

### 7.5 推薦 Response 必要欄位

```json
{
  "id": 2499,
  "name": "情挑誘光水唇膏 45",
  "brand": "YSL",
  "category": "唇彩",
  "price": 1650,
  "currency": "TWD",
  "imageUrl": "https://...",
  "sourceUrl": "https://...",
  "styleTags": ["glossy", "elegant"],
  "score": 0.89,
  "scoreBreakdown": {
    "colorScore": 0.92,
    "styleScore": 0.86,
    "featureScore": 0.70,
    "availabilityScore": 1.0
  },
  "reason": "與夏季型及水光優雅妝感相符"
}
```

`imageUrl` 不得為空；前端不應再自行用 demo 圖補正式推薦結果。

---

## 8. 統一錯誤格式

```json
{
  "ok": false,
  "error": {
    "code": "PRODUCT_ALREADY_EXISTS",
    "message": "商品已存在",
    "retryable": false,
    "details": { "productId": 2499 }
  }
}
```

| code | HTTP | 說明 |
|---|---:|---|
| `INVALID_URL` | 400 | 商品網址格式錯誤 |
| `UNSUPPORTED_SITE` | 422 | 暫不支援來源網站 |
| `SCRAPE_BLOCKED` | 502 | 來源阻擋爬蟲 |
| `FETCH_TIMEOUT` | 504 | 來源逾時 |
| `PARSE_FAILED` | 422 | 無法解析商品 |
| `MISSING_REQUIRED_FIELDS` | 422 | 缺正式上架欄位 |
| `INVALID_PRODUCT_ENUM` | 422 | category/type/tag 非白名單 |
| `PRODUCT_ALREADY_EXISTS` | 409 | 商品重複 |
| `PRODUCT_NOT_FOUND` | 404 | 商品不存在 |
| `VERSION_CONFLICT` | 409 | 編輯版本衝突 |
| `UNAUTHORIZED` | 401 | 未登入／Token 無效 |
| `FORBIDDEN` | 403 | 非管理員 |
| `PRODUCT_DB_UNAVAILABLE` | 503 | 商品資料庫不可用 |

---

## 9. 安全、CORS 與資料品質

- 正式 Origin 至少允許 `https://decorate-me.web.app`。
- 若使用 cookie/session，必須 `Access-Control-Allow-Credentials: true`，不得搭配 `Access-Control-Allow-Origin: *`。
- Admin 寫入要求後端驗證角色；不可相信前端送的 `role=admin`。
- 爬蟲只允許 `http/https`，禁止 localhost、內網 IP、metadata IP，避免 SSRF。
- 限制頁面大小、redirect 次數、下載時間與圖片大小。
- 描述與規格必須移除 script、事件屬性與不安全 HTML。
- 每次新增／編輯／刪除記錄：admin、時間、商品 ID、before、after、來源 IP/request ID。
- `sourceUrl`、圖片授權與網站使用條款需由團隊確認；爬取成功不代表可商業使用。

---

## 10. 驗收案例

### A. 搜尋既有商品

1. 輸入品牌，清單只顯示符合商品。
2. 輸入商品名稱片段、分類、ID 都可搜尋。
3. 清除後恢復完整清單。

### B. Google 引導＋爬蟲

1. 資料庫查無商品。
2. Google 搜尋連結帶入管理員關鍵字。
3. 管理員選擇來源頁，貼入爬蟲匯入。
4. 爬蟲回預覽，不直接正式上架。

### C. 新增／編輯／刪除

1. 新增成功後資料庫可讀到新商品。
2. 點商品後左側欄位完整帶入。
3. 儲存編輯後重新讀取仍為新值。
4. 刪除失敗時畫面不得假裝成功。

### D. 推薦演算法

1. 新商品補齊 `type/styleTags/lab` 且 approved 後，能出現在推薦候選。
2. 相同分析資料下，修改 styleTags 會改變 styleScore。
3. 修改 Lab 會改變 colorScore。
4. `imageUrl` 在推薦回應與前端都能顯示。
5. inactive、pending、out-of-stock 商品不會被推薦。

### E. 去重與安全

1. 同一來源商品重複匯入回 409 並提供既有 ID。
2. 非 Admin 呼叫 POST/PATCH/DELETE 回 403。
3. localhost／內網網址爬取被拒絕。

---

## 11. 各端交付清單

### 爬蟲端回覆

```text
crawlerUrl:
支援網站清單:
POST /api/crawler/product-preview 成功／部分成功／失敗範例:
分類與 styleTags mapping:
hex/Lab 產生方式:
去重欄位:
SSRF、timeout、大小限制:
```

### 資料庫端回覆

```text
productUrl:
products 資料表 migration:
GET 搜尋與分頁範例:
POST/PATCH/DELETE 成功與失敗範例:
Admin Token 驗證方式:
audit log schema:
recommendationReady 計算規則:
CORS 驗證結果:
```

### 推薦端回覆

```text
實際讀取商品欄位:
各類別計分權重:
缺欄位 fallback:
scoreBreakdown 範例:
新爬商品進入候選的驗收證據:
imageUrl 非空驗收:
```

---

## 12. 完成定義

只有同時符合以下條件才算完成：

- Admin 可用品牌／商品／關鍵字搜尋資料庫。
- 無結果時能引導 Google 找來源頁。
- 爬蟲預覽欄位可帶入 Admin 表單。
- 新增、編輯、刪除全部真正寫入商品資料庫。
- 商品資料庫保存演算法必要欄位。
- 新爬商品補齊欄位並審核後，實際參與推薦計分。
- 推薦回傳正確圖片、分數拆解與推薦原因。
- 全流程有權限、錯誤、去重、CORS 與 audit log 驗收。

一句話總結：**爬到商品不等於完成；商品必須經過標準化、人工確認、資料庫保存與推薦欄位驗證，最後真的進入演算法候選並能解釋分數，才算完整串接。**
