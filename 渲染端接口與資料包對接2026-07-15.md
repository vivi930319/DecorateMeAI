# 渲染端接口與資料包對接：新手版完整流程

> 日期：2026-07-15
> 對象：第一次接觸渲染端的組員（前端或後端都適用）
> 依據：線上 `replicate-render` 服務實際程式碼（`replicate_render_api.py`）與 `/health` 實測，不是憑印象寫的。

---

## 0. 先搞懂五個名詞（30 秒）

| 名詞 | 白話解釋 |
| --- | --- |
| **data URL** | 把圖片檔轉成一長串文字（`data:image/jpeg;base64,/9j/4AAQ...`），可以直接塞進 JSON 送給 API，不用另外上傳檔案。 |
| **jobId** | 渲染要跑 50~150 秒，API 不會讓你掛著等。送出後先拿一張「號碼牌」（jobId），之後憑它查進度。 |
| **resultToken** | 號碼牌的「防偽碼」。查進度時要一起出示，不然任何撿到 jobId 的人都能看你的渲染結果。 |
| **X-API-Key** | 服務大門的鑰匙。每個請求的 header 都要帶，擋掉掃到網址就亂打的人。 |
| **analysisPackage（資料包）** | 全站統一的資料單位：臉部分析結果、選的風格、Ollama 建議、渲染結果全部裝在同一包。渲染端也吃這一包。 |

---

## 1. 服務基本資料

| 項目 | 值 |
| --- | --- |
| 服務名稱 | `replicate-render`（Cloud Run，asia-east1） |
| 底層模型 | Replicate 上的 `openai/gpt-image-2`（`provider: replicate`） |
| 服務網址 | 由前端 `config.local.js` 的 `renderUrl` 注入 |
| 金鑰 | `config.local.js` 的 `renderApiKey`，放請求 header `X-API-Key` |
| 健康檢查 | `GET /health`（不用金鑰） |
| 渲染結果儲存 | 自動上傳 GCS bucket `decorate-me-renders`，回傳**永久網址** |
| 速率限制 | 每小時 10 次／每個「IP＋X-User-Email」組合（輪詢不計） |
| 圖片上限 | data URL 長度 ≤ 12,582,912 字元（約 12MB） |

先打一次健康檢查熟悉一下（PowerShell 或 Git Bash 都可以）：

```bash
curl -s https://<renderUrl>/health
```

回應裡最重要的三個欄位：

- `"model": "openai/gpt-image-2"` → 現在跑哪個模型。
- `"render_prompt_policy": { "client_prompt_accepted": false, "allowed_style_ids": [...] }` → **前端送 prompt 沒有用**（原因見第 3 節），只認這 8 個 styleId。
- `"async_jobs": { "submit": "POST /render/jobs", "poll": "GET /render/jobs/{job_id}" }` → 非同步的兩支端點。

---

## 2. 輸入接口（你要送什麼進去）

### 2.1 端點

| 端點 | 用途 | 什麼時候用 |
| --- | --- | --- |
| `POST /render/jobs` | **非同步渲染（正式流程用這支）**：立刻回 jobId，背景慢慢跑 | 一律用這支。gpt-image-2 要 50~150 秒，同步等會撞 Cloud Run 逾時 |
| `GET /render/jobs/{jobId}` | 查進度／取結果 | 送出後每 2 秒打一次 |
| `POST /render` | 同步渲染（舊接口，會掛著等到跑完） | 只留給舊呼叫端相容，新程式不要用 |

### 2.2 請求格式（`POST /render/jobs`）

Header：

```
Content-Type: application/json
X-API-Key: <renderApiKey>
X-User-Email: <登入者 email，可選，用於速率限制分桶>
```

Body（JSON）：

```json
{
  "image": "data:image/jpeg;base64,/9j/4AAQ...",
  "styleId": "koreanClean",
  "strength": 0.35,
  "analysisPackage": {
    "faceAnalysis": {
      "faceShape": "鵝蛋臉",
      "eyeShape": "杏眼",
      "skinTone": { "season": "暖秋", "level": "自然", "lab": {"L": 62.1, "a": 11.3, "b": 15.8} },
      "lipLab": {"L": 48.2, "a": 25.6, "b": 12.4}
    },
    "render": { "styleId": "koreanClean" }
  }
}
```

一個欄位一個欄位講：

| 欄位 | 必填 | 說明 |
| --- | --- | --- |
| `image` | ✅ | 妝前照的 base64 data URL。必須以 `data:image/` 開頭且含 `;base64,`，否則回 `400 INVALID_IMAGE`；超過 12MB 字元回 `413 IMAGE_TOO_LARGE`。 |
| `styleId` | 建議填 | 8 選 1：`natural`／`koreanClean`／`japaneseClear`／`hongKong`／`richGirl`／`softBaddie`／`yandere`／`mensPlain`。不填預設 `natural`；填了白名單以外的值回 `422 INVALID_RENDER_STYLE`。 |
| `strength` | 否 | 歷史遺留欄位，現行模型不使用，送不送都行（預設 0.35）。 |
| `analysisPackage` | 建議帶 | 資料包。渲染端只讀兩個地方：`faceAnalysis`（拿去組個人化 prompt）和 `render.styleId`（頂層 `styleId` 沒填時的備援）。 |
| `faceAnalysis` | 否 | 舊相容欄位：沒帶整包資料包時，也可以單獨給臉部分析。 |

### 2.3 重要：後端「刻意忽略」什麼

- **`prompt` 欄位送了也沒用**，會被直接丟掉（Pydantic `extra="ignore"`）。
- **資料包裡的 `generativeText.renderPromptEn` 也一律忽略**。

原因（資安）：renderApiKey 是明文寫在網頁裡的，誰都拿得到。如果渲染端照著前端送來的 prompt 跑，任何人都能拿這把 key 用我們的 Replicate 額度生成任意圖片。所以**前端只能送「結構化資料」（styleId＋faceAnalysis），真正下給模型的指令一律由後端自己組**。

### 2.4 後端怎麼組 prompt（你不用做，但要知道）

1. 後端拿你送的 `styleId`＋`faceAnalysis`，去跟**建議服務**要一份個人化英文渲染指令（`promptSource: "ollama"`）。
2. 建議服務掛了或逾時 → 退回該 styleId 的**固定白名單 prompt**（`promptSource: "style_allowlist"`）。
3. 兩條路都會把實際用的指令放進回應的 `renderPrompt` 欄位，渲染結果不如預期時可以看它 debug。

---

## 3. 輸出接口（你會拿到什麼）

### 3.1 `POST /render/jobs` 的立即回應

```json
{
  "jobId": "RJOB-1a2b3c4d5e6f",
  "resultToken": "807d190f15994cdd86270c2f2ca9ea0b",
  "status": "queued",
  "progress": 1,
  "afterImageUrl": null,
  "renderPrompt": "Apply a clean Korean-style makeup ...",
  "promptSource": "ollama",
  "createdAt": 1752555555.1,
  "estimatedSeconds": 90
}
```

- **馬上把 `jobId` 和 `resultToken` 存起來**，等一下輪詢要用。`resultToken` 只活在這次渲染流程，不要寫進 localStorage、不要放進網址。
- 特例：**10 分鐘內用同一張圖＋同一個 prompt 重複送**，會直接回 `status: "completed"`＋`afterImageUrl`＋`deduped: true`（去重快取命中），不用輪詢、也不重扣 Replicate 額度。

### 3.2 輪詢 `GET /render/jobs/{jobId}`

Header 一定要帶兩個：

```
X-API-Key: <renderApiKey>
X-Job-Token: <剛剛存的 resultToken>
```

沒帶或帶錯 `X-Job-Token` → `403 FORBIDDEN "Invalid or missing job token."`（**不是暫時性錯誤，重試一百次也一樣**）。

回應依狀態分三種：

| status | 代表 | 你該做什麼 |
| --- | --- | --- |
| `queued` / `running` | 還在跑 | `progress` 是 1~95 的**估算值**（照經過時間用指數曲線算的，Replicate 不回報真實進度），拿去畫進度條，2 秒後再查 |
| `completed` | 完成 | `afterImageUrl` 是 GCS 永久網址，直接给 `<img>` 用；`progress` 為 100 |
| `failed` | 失敗 | 看 `error` 欄位（對外只回通用訊息，細節在 Cloud Run log） |

其他狀況：`404 JOB_NOT_FOUND` = jobId 打錯，或 job 已過期被清（completed/failed 保留 1 小時；卡超過 10 分鐘會被標 failed）。

### 3.3 錯誤碼總表

| HTTP | code | 意思 | 排錯方向 |
| --- | --- | --- | --- |
| 400 | `INVALID_IMAGE` | image 不是合法 data URL | 檢查 `data:image/...;base64,` 前綴 |
| 401 | — | X-API-Key 沒帶或不對 | 對 `config.local.js` 的 `renderApiKey` |
| 403 | `FORBIDDEN` | X-Job-Token 沒帶或不對 | 用建立 job 時回的 `resultToken` |
| 404 | `JOB_NOT_FOUND` | job 不存在或已過期 | 檢查 jobId；超過 1 小時要重新渲染 |
| 413 | `IMAGE_TOO_LARGE` | 圖太大 | 前端先壓縮再送 |
| 422 | `INVALID_RENDER_STYLE` | styleId 不在白名單 | 回應會附 `allowedStyleIds` |
| 429 | — | 一小時超過 10 次 | 看 `retryAfter`，稍後再試 |

---

## 4. 跟資料包（analysisPackage）的對接關係

渲染只是資料包旅程的最後一站。整包的流向：

```
臉部分析 ──→ analysisPackage.faceAnalysis（五官、膚色/唇色 LAB）
選風格   ──→ analysisPackage.render.styleId
Ollama   ──→ analysisPackage.generativeText.suggestion（給人看的中文建議）
商品推薦 ──→ analysisPackage.recommendations.products
渲染     ──→ analysisPackage.render.afterImageUrl（本文件的主角）
```

**渲染端「讀」資料包的哪裡：**

| 讀什麼 | 拿去做什麼 |
| --- | --- |
| `faceAnalysis` | 交給建議服務組「個人化渲染指令」（例：圓形臉→修容指令不同） |
| `render.styleId` | 頂層 `styleId` 沒填時的備援 |
| ~~`generativeText.renderPromptEn`~~ | **刻意不讀**（防竄改，見 2.3） |

**渲染完成後，前端要把結果「寫」回資料包：**

| 寫什麼 | 值 |
| --- | --- |
| `render.status` | `completed` 或 `failed` |
| `render.afterImageUrl` | 回應的 GCS 永久網址 |
| `render.beforeImageDataUrl` | 妝前壓縮圖（分析完成時就先存好了） |
| `render.styleId` | 這次用的風格 |

寫回之後 compare 頁（妝前妝後對比）、收藏功能才有資料可用。

---

## 5. 一步一步完整流程（新手照抄版）

前置條件：使用者已完成臉部分析（資料包裡有 `faceAnalysis`）、已在風格頁選了風格。

**第 1 步：準備妝前圖。**
從資料包拿 `render.beforeImageDataUrl`（分析完成時已壓縮存好的正面照 data URL）。沒有的話，把原圖用 canvas 壓到長邊 1024 左右再轉 data URL——不要拿手機原圖直接送，會撞 413。

**第 2 步：組請求 body。**
`image` = 第 1 步的 data URL；`styleId` = 使用者選的風格；`analysisPackage` = 至少帶 `faceAnalysis` 和 `render.styleId` 兩塊（見 2.2 範例）。

**第 3 步：送出 job。**
`POST {renderUrl}/render/jobs`，header 帶 `Content-Type: application/json`、`X-API-Key`、（有登入時）`X-User-Email`。

**第 4 步：處理立即回應。**
- 拿到 `jobId`＋`resultToken` → 存進這次流程的變數（例如資料包的 `async` 區）。
- 如果直接回 `status:"completed"`（去重命中）→ 跳到第 7 步。
- 401 → 金鑰問題；422 → styleId 打錯；429 → 額度用完，引導使用者稍後再試。

**第 5 步：開始輪詢。**
每 2 秒 `GET {renderUrl}/render/jobs/{jobId}`，header 帶 `X-API-Key` **和 `X-Job-Token`（值＝resultToken）**。把回應的 `progress` 餵給進度條。設一個 5 分鐘的保險絲，超過就放棄並顯示逾時。

**第 6 步：判斷狀態。**
`queued`/`running` → 繼續等；`failed` → 顯示 `error` 並讓使用者重試；`completed` → 下一步。

**第 7 步：把結果寫回資料包。**
`render.afterImageUrl` = 回應的網址、`render.status` = `completed`，存檔（AnalysisDraft）。

**第 8 步：顯示。**
compare 頁左右放 `beforeImageDataUrl` 和 `afterImageUrl`。`afterImageUrl` 是 GCS 永久網址，收藏、歷史紀錄可以直接存這個網址。

### 用 curl 手動走一遍（測通接口用）

⚠️ 注意：這會真的呼叫 Replicate、真的花錢＋佔每小時 10 次額度，測接口格式就好，不要寫進迴圈。

```bash
# 1. 準備一張小圖的 data URL（Git Bash）
IMG="data:image/jpeg;base64,$(base64 -w0 test_face.jpg)"

# 2. 送出 job（把回應裡的 jobId 和 resultToken 抄下來）
curl -s -X POST "https://<renderUrl>/render/jobs" \
  -H "Content-Type: application/json" -H "X-API-Key: <renderApiKey>" \
  -d "{\"image\":\"$IMG\",\"styleId\":\"natural\"}"

# 3. 每隔幾秒查一次（jobId、resultToken 換成第 2 步拿到的）
curl -s "https://<renderUrl>/render/jobs/<jobId>" \
  -H "X-API-Key: <renderApiKey>" -H "X-Job-Token: <resultToken>"

# 4. 看到 "status":"completed" 後，開 afterImageUrl 確認圖片
```

---

## 6. 前端曾有的兩個落差（2026-07-15 已修復並上線）

寫這份文件時實際比對線上前端程式碼，發現 `js/api.js` 的 `renderMakeupAsync()` 是舊版流程，跟現行後端對不上，**當時正式站的渲染功能實質是壞的**：

1. **送的是 `prompt`，沒送 `styleId` 也沒送 `analysisPackage`**。後端忽略 `prompt`，styleId 落到預設 `natural` → 不管使用者選什麼風格，實際都渲染 natural，而且吃不到 faceAnalysis 個人化。
2. **輪詢沒帶 `X-Job-Token`**。新 job 一律有 resultToken 檢查 → 每次輪詢都 403，前端把它當暫時性錯誤繼續等 → 等滿 5 分鐘跳「渲染逾時」，即使背景其實已經渲染完成。

修復內容（同日已部署，並以真實渲染 curl 走完第 5 步全流程驗證：job 44 秒完成、`promptSource: ollama`、不帶 token 403／帶 token 200、GCS 妝後圖可開）：

- `renderMakeupAsync` 的 body 改送 `{ image, styleId, analysisPackage }`；建立 job 後保存 `submitted.resultToken`，輪詢 header 加 `X-Job-Token`。
- router 呼叫端改組「只含 `faceAnalysis` 與 `render.styleId` 的精簡資料包」（不整包送——資料包裡有 base64 圖，整包送 payload 會爆）。
- 依 S21 定案移除 compare 頁的「可編輯英文渲染指令」輸入框；預覽區改成渲染完成後顯示**後端實際使用**的 `renderPrompt`。

---

## 7. 常見問題（FAQ）

**Q：為什麼我送了自訂 prompt 沒效果？**
A：設計如此（2.3 節）。要調整妝感請改用 styleId，或透過建議服務端調整個人化 prompt 模板。

**Q：進度條為什麼會卡在 90 幾很久？**
A：`progress` 是估算值，越接近 95 越慢是刻意的（實際耗時 50~150 秒浮動大，瓶頸在 Replicate 排隊，這樣設計才不會提前跑滿）。

**Q：渲染完成的圖会過期嗎？**
A：不會，`afterImageUrl` 是 GCS 公開 bucket 的永久網址（檔名是亂數 UUID，無法枚舉，但拿到連結的人都能開——這是已記錄的取捨，見歷史追蹤 S6）。

**Q：同一張圖按兩次渲染會扣兩次額度嗎？**
A：10 分鐘內同圖同風格不會（去重快取直接回上次結果），超過 10 分鐘或換風格會。

**Q：job 建立成功但一直 `queued` 不動？**
A：先看 `/health` 是否正常；若 Cloud Run 沒開 `--no-cpu-throttling`，背景執行緒會被凍住，job 會卡到 10 分鐘後被清成 failed——部署時必須帶這個參數。
