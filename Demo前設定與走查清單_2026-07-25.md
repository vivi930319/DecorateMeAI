# Demo 前設定與走查清單

> 用途：九月專題 Demo 前一天 + 當天照著跑，確保「登入→上傳→分析→推薦→渲染→保存→刪除」全程順、
> 成本可控、出事有備援。**指令裡的專案 ID／服務名／地區請先用 `gcloud run services list` 對過再貼。**
> 不含任何實際金鑰／Email／密碼／照片。

專案預設值（依實際情況調整）：
- `PROJECT=decorate-me`　`REGION=asia-east1`
- Cloud Run 服務（用 `gcloud run services list --project=$PROJECT` 確認實際名稱）：
  `ai-gateway`、`face-basic`、`face-pro`、`replicate-render`（會員 DB／Ollama 為外部組）。

---

## 〇、前置：程式碼與 CI（Demo 前 2~3 天）

- [ ] 後端 `Isa`、前端 `dev_makeup` 都已 push，CI 綠燈（`gh run list --branch Isa --limit 1`）。
- [ ] 確認要 Demo 的版本**已部署到線上**——本次 2026-07-25 的資安批次（登入限流修正、admin 只認後端角色、圖片安全、CSRF、稽核）**只有部署後才生效**。線上跑舊版的話，Demo 會重現先前的 bug。
- [ ] 用 `curl` 驗線上而不是只看部署有沒有跑完（專案在 OneDrive 底下，部署可能傳到未同步的舊檔）：
  ```bash
  curl -s https://<gateway-url>/health
  curl -s https://<gateway-url>/public-config    # 只該回同源路徑，不該有 trycloudflare 或上游真實網址
  ```

---

## 一、Cloud Run 設定（Demo 前一天）

### 1.1 開 min-instances=1（消除冷啟動）

冷啟動會讓 Demo 第一個請求卡十幾秒（Face 服務還要載 InsightFace 模型）。Demo 期間每個核心服務設 `min-instances=1`：

```bash
for SVC in ai-gateway face-basic face-pro replicate-render; do
  gcloud run services update $SVC --project=$PROJECT --region=$REGION --min-instances=1
done
```

> Render 若用 `--no-cpu-throttling`（背景 thread 需要），確認沒被關掉。

### 1.2 登入限流放寬（避免 Demo 現場被自己鎖住）

預設登入限流是 **10 次 / 10 分鐘**（只計失敗登入，但評審多人試錯還是可能踩到）。Demo 期間暫時放寬 Gateway：

```bash
gcloud run services update ai-gateway --project=$PROJECT --region=$REGION \
  --update-env-vars GATEWAY_LOGIN_RATE_LIMIT_MAX_REQUESTS=100,GATEWAY_LOGIN_RATE_LIMIT_WINDOW_SECONDS=60
```

> Demo 後記得調回（見第五節）。**注意**：若現場出現「會員服務目前限制登入頻率（`MEMBER_SERVICE_RATE_LIMITED`）」，那是**資料庫端**的限流，Gateway 放寬沒用，要請資料庫端放寬或改以真實使用者位址計算。

### 1.3 確認匿名直連仍被擋

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://<face-basic-url>/v1/face/analyze/basic   # 應為 403
```

---

## 二、Demo 帳號與素材（Demo 前一天）

- [ ] **固定假帳號**：準備 1 個一般會員 + 1 個 admin 帳號，**都要真的通過 OTP 驗證**（未驗證帳號登入會被擋、也進不了分析——這是正確行為，別現場才發現）。
      - 記錄帳密在**離線**便條（不要進 Git、不要進聊天）。
- [ ] **固定照片**：準備 2~3 張授權可公開的正臉照（清晰、正面、光線正常），放在隨手可取的位置。避免現場臨時找圖踩到「偵測不到人臉」。
      - 若要展示 PRO：另備一張側臉照。
- [ ] **離線備援結果**：先把每個風格各跑一次渲染，把**妝前／妝後對比圖存下來**，並在投影片或資料夾**明確標示「離線備援，非現場即時生成」**。現場若渲染服務或 Replicate 出問題，直接改放備援圖，不要卡在轉圈。
- [ ] **拒絕案例素材**（可選，展示資安）：一張改過副檔名的假圖（例如 `.jpg` 其實是文字），現場示範被擋下、回明確錯誤。

---

## 三、逐項走查（Demo 前一天，用線上環境跑一遍）

> 對應 GitHub issue #28。**用實際帳號、實際照片跑，不是只看程式碼。**

### 3.1 登入 / Session
- [ ] 一般會員帳號登入成功、進到 dashboard。
- [ ] admin 帳號登入 → **看得到管理後台入口**（若看不到：檢查資料庫端登入回應是否帶 `role=admin`，2026-07-25 起 admin UI 只認後端驗過的角色）。
- [ ] 一般會員 / 訪客 → **看不到**管理後台。
- [ ] 登出後再操作 → 正確要求重新登入。

### 3.2 上傳 / 分析
- [ ] 上傳正臉照 → BASIC 分析完成，五官／膚色／對稱性都有值。
- [ ] （若展示 PRO）正面＋側面 → 完成，精細分析狀態文案正確。
- [ ] 上傳假圖（改副檔名）→ 被擋，回明確錯誤（不是 500）。
- [ ] 直式手機照上傳 → 方向正確（沒有躺平）。

### 3.3 推薦 / 渲染 / 保存 / 刪除
- [ ] 商品推薦有結果（若 `/recommend-products` 仍 404，改用備援畫面——這條屬演算法端）。
- [ ] 選一個風格渲染 → 出妝後圖；妝前妝後對比正常。
- [ ] 收藏 → 重新整理後仍在（保存成功）。
- [ ] 刪除收藏 → 圖確實消失、可再操作。

### 3.4 記錄
- [ ] 把走查中**任何異常**記到 issue #28，當天決定「現場避開」或「用備援」。

---

## 四、Demo 當天

- [ ] 開場前 10 分鐘，各服務打一次 `/health`（暖機、確認都活著）。
- [ ] 用**固定假帳號 + 固定照片**走主線，不要現場即興換帳號／換圖。
- [ ] 渲染這一步最容易慢或失敗：**準備好隨時切離線備援圖**。
- [ ] 資安亮點可講（都已上線後）：私有 GCS＋擁有者驗證＋Signed URL、圖片上傳防護（偽 MIME／解壓縮炸彈／去 GPS）、跨分頁帳號隔離、admin CSRF、登入限流、log 去識別化。

---

## 五、Demo 後收尾（控成本）

```bash
# min-instances 調回 0（沒有流量不燒錢）
for SVC in ai-gateway face-basic face-pro replicate-render; do
  gcloud run services update $SVC --project=$PROJECT --region=$REGION --min-instances=0
done

# 登入限流調回安全值
gcloud run services update ai-gateway --project=$PROJECT --region=$REGION \
  --update-env-vars GATEWAY_LOGIN_RATE_LIMIT_MAX_REQUESTS=10,GATEWAY_LOGIN_RATE_LIMIT_WINDOW_SECONDS=600
```

- [ ] 確認 `min-instances` 都回 0（`gcloud run services describe <svc> --format='value(spec.template.metadata.annotations)'`）。
- [ ] 若 Demo 用了臨時放寬的設定或假資料，記得清理／還原。

---

## 附：現場故障快速對照

| 現象 | 最可能原因 | 現場對策 |
|------|-----------|---------|
| 第一個請求卡十幾秒 | 冷啟動 | 事前 `min-instances=1`＋開場前打 `/health` 暖機 |
| 登入「嘗試次數過多」 | Gateway 登入限流 | 已放寬則等 60 秒；或換另一個假帳號 |
| 登入「會員服務限制頻率」 | 資料庫端限流（`MEMBER_SERVICE_RATE_LIMITED`） | 換帳號；事後請資料庫端調整 |
| 分析「沒偵測到人臉」 | 照片角度／光線 | 換固定備援照片 |
| 渲染很慢或失敗 | Replicate／第三方 | 切離線備援對比圖 |
| admin 看不到後台 | 登入回應沒帶 `role=admin` | 用確認過的 admin 帳號；事後查資料庫端 role 欄位 |
| 「無法確認分頁登入身分」 | session 未建立（訪客／過期） | 重新用真帳號登入，不要用訪客做需要身分的操作 |
