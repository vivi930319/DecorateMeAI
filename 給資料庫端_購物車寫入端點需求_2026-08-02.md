# 給資料庫端 · 購物車寫入端點需求

**日期：** 2026-08-02
**提出者：** 前端／Gateway 端
**優先度：** 高（目前使用者的購物車**一登出就消失**）

---

## 一、現象

使用者加入購物車 → 登出 → 再登入 → **購物車是空的**。

## 二、已確認的事實

實際請求（帶合法 session、CSRF、actor）：

```
POST https://decorate-me.web.app/member-database/api/members/{email}/cart
Content-Type: application/json

→ 405 Method Not Allowed
```

**405 是上游回的，不是 Gateway。** 依據：

| 若是這個原因 | 會回什麼 | 實際 |
| --- | --- | --- |
| 未登入 | 401 | — |
| Gateway 白名單沒放行 | 404 | — |
| **上游未實作該 method** | **405** | ✅ |

Gateway 的 `UPSTREAMS["member-database"].allowed_paths` 已放行
`api/members/[^/]+/cart`，路由是通的。

`GET /api/members/{email}/cart` 目前可用（前端讀得到），**只缺寫入**。

---

## 三、需要實作的端點

### `POST /api/members/{email}/cart`

**語意：整台購物車覆蓋寫入（last-write-wins）**，不是增量。
前端每次變更就送完整清單，不需要 PATCH／DELETE 單項。

**Request**

```json
{
  "items": [
    { "id": "商品ID字串", "qty": 2 },
    { "id": "另一個商品ID", "qty": 1 }
  ]
}
```

- `id`：字串
- `qty`：正整數（前端已保證 `>= 1`）
- `items` 可以是空陣列（代表清空購物車）

**Response**

成功回 200 或 204 皆可。若回 JSON，前端不依賴其內容。

**錯誤**

| 情況 | 建議狀態碼 |
| --- | --- |
| 未登入／session 無效 | 401 |
| `{email}` 不是本人（且非管理員）| 403 |
| body 格式不符 | 400 |

---

## 四、授權要求

**必須驗證 `{email}` 是登入者本人。** Gateway 已經做了 session 驗證與
`X-Expected-Actor` 的跨帳號隔離，但**上游仍須自行檢查擁有者**——
不能假設「能打到我就是本人」。

---

## 五、前端目前的行為（實作後會自動生效）

```javascript
// 上游還沒實作這個寫入端點（回 405/501）。第一次撞到就記下來，之後整個
// 分頁生命週期都不再送——否則每次加減數量、每次登入都會再打一次。
if (this._cartPushUnsupported) return null;
```

前端**已經寫好了**，只是撞到 405 之後就停止再送。
端點實作完成後，重新整理頁面即可生效，前端不需要改動。

---

## 六、範圍界定：其他端點的狀況

**已確認正常的（不用動）：**

| 功能 | 端點 | 依據 |
| --- | --- | --- |
| 會員點數 | `POST .../check-in`、`.../tasks/{id}/claim`、`.../theme-shop/{id}/redeem` | **管理中台讀資料庫，顯示的點數與前台一致** —— 代表這三條寫入都有實作且正常 |
| 各項讀取 | `GET .../points`、`.../favorites`、`.../cart` 等 | 前端讀得回資料 |

**尚未驗證的（麻煩一併確認）：**

| 功能 | 端點 | 為什麼要確認 |
| --- | --- | --- |
| 商品收藏 | `POST /api/favorites/toggle` | 不帶 session 打會回 401 而非 404，代表路徑存在；但前端是背景同步、**不看回傳值**，即使一直失敗也不會被發現 |
| 收藏妝容 | `POST .../saved-looks` | 同上 |

驗證方式與購物車相同：收藏一項 → 登出 → 重新登入 → 看還在不在。

> **這一節的重點是把範圍縮小。** 點數已經證實是好的，所以**唯一確定要補的是購物車寫入**；
> 收藏那兩條只是還沒人實際驗過，不一定有問題。

前端診斷腳本：`tools/probe_member_endpoints.js`（貼進 DevTools Console 可一次列出
所有端點的回應碼，405 代表未實作）。

---

## 七、驗收方式

實作完成後，用同一條路徑驗證（需要登入的瀏覽器）：

```
1. 加入兩件商品到購物車
2. 登出
3. 重新登入
4. 購物車應該還有那兩件
```

並在 DevTools 的 Network 確認 `POST .../cart` 回 200/204 而非 405。
