# 購物車跨裝置同步：資料庫端規格書

> 文件版本：`2026-07-24-v1`
> 適用服務：前端（web_frontend）↔ AI Gateway ↔ 會員資料庫 API（合作方維運，透過 Cloudflare Tunnel 交付）
> 目前狀態：**前端與 Gateway 這一半已經做好並測過**（見第 5 節）。只等會員資料庫端補上本文件第 2 節的兩個端點，跨裝置購物車就會通。

## 0. 一句話需求

讓使用者「換一台裝置／換一個瀏覽器登入，看到同一個購物車」。做法跟現有的**收藏（favorites）**完全一樣：購物車存到會員資料庫，綁在會員身上；前端登入時讀回、變更時寫回。

現在購物車只存在瀏覽器本機（localStorage），所以換裝置就是空的——這正是收藏在補上 `GET /api/members/{email}/favorites` 之前遇過的同一個坑。

## 1. 資料模型

每個會員一台購物車，內容是一個品項陣列，**每個品項只有兩個欄位**：

| 欄位 | 型別 | 說明 |
| --- | --- | --- |
| `id` | string | 商品識別碼。對資料庫端是**不透明字串**，原樣存、原樣回即可，不需要解析或對照商品表。前端自己負責用它對回商品目錄。長度上限建議 200 字元。 |
| `qty` | integer | 數量，`>= 1` 的整數。 |

購物車本身建議附一個 `updatedAt`（ISO 8601 字串）方便除錯，非必要。

## 2. 需要新增的兩個端點

兩個端點都掛在既有的會員資源路徑 `/{email}` 底下，跟 favorites / saved-looks / points 同一個模式。`{email}` 是路徑參數，需經 URL 編碼。

### 2.1 讀取購物車

```
GET /api/members/{email}/cart
```

**回應 200**：

```json
{
  "items": [
    { "id": "api-product-123", "qty": 2 },
    { "id": "api-product-88", "qty": 1 }
  ],
  "updatedAt": "2026-07-24T09:00:00Z"
}
```

- 購物車為空時回 `{ "items": [] }`（**不要回 404**）。
- `items` 一律是陣列；沒有就給空陣列。

### 2.2 覆蓋寫入購物車（整台替換）

```
POST /api/members/{email}/cart
Content-Type: application/json
```

**請求 body**：

```json
{ "items": [ { "id": "api-product-123", "qty": 3 } ] }
```

- 語意是**整台覆蓋**（last-write-wins），不是「加一件」。前端每次變更都會送出當下完整的購物車，伺服器直接用它取代舊的。
- 回應回存好後的購物車，形狀同 2.1 的 200。

> 為什麼用 POST 而不是 PUT：Gateway 的通用會員代理只放行 `GET/POST/PATCH/DELETE`，不收 PUT。所以「整台覆蓋」走 POST。

### 2.3 驗證與上限（請務必做，避免被灌爆）

- `qty` 非正整數時，夾到 `1`；建議上限每項 `99`。
- `items` 筆數建議上限 `200`；超過就截斷或回 `400`。
- `id` 空字串或超長就丟掉該筆。
- body 過大（例如 > 256KB）回 `413`。

## 3. 認證與授權（沿用現有機制，不需要另做）

- 前端**不會**直接打會員資料庫，一律經 AI Gateway。Gateway 會帶上該會員的登入 session cookie（跟 favorites / points 一模一樣）。
- Gateway 已經強制「**只能存取自己 email 底下的資料**」：`api/members/{別人的email}/cart` 會在 Gateway 就被擋成 `403 MEMBER_SCOPE_FORBIDDEN`，打不到你們。所以資料庫端只要照 session 對應的會員存取即可，跨會員的防護 Gateway 已經擋掉。
- 認證方式與現有會員端點相同（session cookie）。不需要額外 API Key。

## 4. 帳號刪除時的連帶處理

會員刪除時，購物車比照 `MEMBER_DATABASE_INTEGRATION_SPEC.md` 對關聯資料的要求一起處理（cascade delete 或匿名化），**不可留下無主購物車**。購物車不含個資、不含照片，cascade delete 即可。

## 5. 前端 / Gateway 這一半已完成（給你們對照）

這邊已經做好、測過，等你們端點上線就會自動通：

- **Gateway**（`ai_gateway.py`）：`member-database` 白名單已加入 `api/members/[^/]+/cart`；授權沿用 `_authorize_member_path`。已驗證：購物車路徑放行、跨會員擋 403、既有 38 項測試全過。
- **前端**（`web_frontend/js/api.js`、`js/router.js`）：
  - `Api.getRemoteCart(email)` → `GET .../cart`
  - `Api.saveRemoteCart(items)` → `POST .../cart`（背景、debounce 500ms、走換帳號防線 `_protectedWrite`）
  - `Cart` 模組登入後每次加減都會背景寫回；`syncRemoteCart()` 在登入與啟動時讀回。
  - **合併規則**：登入前的訪客購物車，登入時與伺服器車**同商品數量相加**合併一次，再寫回伺服器；之後的重載／換裝置一律以伺服器為準直接取代（避免每次載入都相加灌水）。合併全在前端做，**你們端點不需要處理合併邏輯**，只要老實地讀回 / 覆蓋寫入即可。

## 6. 驗收檢查點

1. 登入 A 裝置加 2 件 → B 裝置登入同帳號 → 看得到那 2 件。
2. B 裝置改成 5 件 → A 裝置重新整理 → 變 5 件（last-write-wins）。
3. 未登入先加 1 件 → 登入（伺服器原本有 2 件同商品）→ 變 3 件（訪客車相加合併一次）。
4. 打 `api/members/{別人的email}/cart` → Gateway 回 403（這條你們端不會收到）。
5. 刪除帳號 → 購物車一併清除，無殘留。
</content>
