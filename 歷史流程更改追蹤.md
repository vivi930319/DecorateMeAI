# 資安跟串接歷史流程

用途：記錄各服務串接相關的資安問題與處理歷程。

狀態標示：(已解決)／(緩解中)／(待處理)／(取捨)
涉及端：前端(FE)、臉部分析(FACE)、AI 渲染(RENDER)、會員/商品資料庫(DB)、Ollama(OLL)、儲存(GCS)

---

## 總覽

| # | 問題 | 端 | 嚴重度 | 狀態 |
|---|------|----|--------|------|
| S1 | 會員個資可被任何人讀取 | DB | 高 | 已解決 |
| S2 | AI 渲染服務無保護，可被無限呼叫燒錢 | RENDER | 高 | 已解決 |
| S3 | 臉部分析服務無 API key | FACE | 中 | 已解決 |
| S4 | 前端 config.local.js（含金鑰）進版控 | FE | 中 | 已解決 |
| S5 | git 歷史殘留 Ollama 金鑰 | OLL | 中 | 待處理 |
| S6 | 渲染人臉圖存公開 GCS bucket | GCS | 中 | 取捨 |
| S7 | OTP 未真正防禦，可用假／不存在 email 建帳號並登入 | DB/FE | 高 | 待處理 |
| S8 | session cookie 無效期＋跨站，頻繁掉線 | DB | 中 | 已解決 |
| S9 | CORS 缺 Allow-Credentials | DB | 中 | 已解決 |
| S10 | 前端有 admin@ email 管理員後門 | FE | 中 | 已解決 |
| S11 | 前端把明碼密碼存 localStorage | FE | 中 | 已解決 |
| S12 | 自動重登把帳密暫存 sessionStorage | FE | 低 | 已解決 |
| S13 | 臉部分析上傳無大小上限（OOM/DoS）| FACE | 中 | 已解決 |
| S14 | 服務例外把內部錯誤回給前端 | FACE/RENDER | 低 | 已解決 |
| S15 | 內部檔（原始碼備份/文件）被部署到公開網站 | FE | 低 | 已解決 |
| S16 | 妝容收藏 localStorage 未分帳號，跨帳號互看 | FE | 中 | 已解決 |
| S17 | 登入無法區分未註冊 vs 密碼錯 | DB/FE | 低 | 已解決 |
| S18 | Cloud Run 服務 allow-unauthenticated | FE/FACE/RENDER | 高 | 已解決 |
| S19 | 渲染換人／失真（男生渲染後變女生，身分沒守住） | RENDER | 中 | 已解決 |
| S20 | 使用者可上傳非本人照片（肖像權／同意風險） | FE/RENDER | 中 | 待處理 |
| S21 | 前端 render prompt 暴露且由客戶端控制（策略洩漏＋可送任意 prompt） | FE/RENDER | 高 | 已解決 |
| S22 | Ollama 建議服務缺 API key | OLL | 高 | 已解決 |
| S23 | 非同步 job 只靠 jobId 可讀結果 | FACE/RENDER | 高 | 已解決 |
| S24 | 渲染輸入缺大小限制 | RENDER | 中 | 已解決 |
| S25 | 正式環境 CORS 設定可能漏設而退回 `*` | FACE/RENDER/OLL | 中 | 已解決 |
| S26 | 渲染背景 job 可能卡住且 Firestore 無限累積 | RENDER | 中 | 已解決 |
| S27 | ROI shadow 模型欄位可能被前端誤用 | FACE | 低 | 已解決 |
| S28 | 前端尚未帶 `resultToken` 輪詢新 job | FE | 高 | 已解決 |
| S29 | job_store cleanup/stat 單次可能掃太多 Firestore 文件 | FACE/RENDER | 中 | 已解決 |
| S30 | 後端與前端修正只在本機，線上尚未同步 | FE/FACE/RENDER | 高 | 已解決 |
| S31 | 比較頁右側 Ollama 妝容建議被誤刪 | FE | 中 | 已解決 |
| S32 | AI Gateway 指向失效的會員資料庫 Tunnel，登入回 503 | DB/FE | 高 | 已解決 |
| S33 | Firebase Hosting 原網址 `/` 未設 no-cache，使用者持續吃舊 config 導致商品頁卡載入 | FE/DB | 中 | 已解決 |
| S34 | 正常商品展示圖沿用粉色 demo 背景，降低商品圖片辨識度 | FE | 低 | 已解決 |
| S35 | 後台會員讀取呼叫已移除的 `_reLogin()`，手機版內容超出視窗難滑動 | FE | 高 | 已解決 |
| S36 | 後台管理介面與使用者前台配色不一致 | FE | 低 | 已解決 |
| S37 | 妝容收藏清單只顯示前 6 筆，後台只能看縮圖且無法同步刪除 | FE/DB | 中 | 已解決 |
| S38 | 後台會員管理缺少會員刪除操作，無法同步控制資料庫資料 | FE/DB | 高 | 已解決 |
| S39 | 後台收藏視窗寬度過小，三欄卡片造成圖片與文字被壓縮 | FE | 低 | 已解決 |
| S40 | 會員刪除 API 與管理員刪除收藏缺少完整資料庫欄位、權限與交易規格 | DB/FE | 高 | 待資料庫端實作 |
| S41 | 正式前端仍指向失效的會員資料庫 Quick Tunnel，誤顯示資料庫無法連線 | FE/DB | 高 | 已解決 |
| S42 | `service-endpoints.js` 覆寫 `config.local.js`，改設定檔無效導致商品與登入全失效 | FE | 高 | 已解決 |
| S43 | 上游金鑰輪替後前端未同步，臉部分析與 AI 渲染失效且錯誤偽裝成 CORS | FE/FACE/RENDER | 高 | 已解決 |
| S44 | Secret Manager 金鑰含 CRLF，Gateway 自建立起從未成功呼叫過任何上游 | RENDER/FACE | 高 | 已解決 |
| S45 | 前端存在兩份副本互相覆蓋，已部署功能被另一份蓋掉 | FE | 中 | 已解決 |
| S46 | 會員端點回 403，尚無法確定為驗證或授權問題 | DB/FE | 中 | 待釐清 |
| S47 | 會員資料庫設計上不支援 Gateway 代理，長期金鑰仍無法完全收斂 | DB | 中 | 待三方議定 |
| S48 | 線上臉部分析模型仍為 7/16 映像，7/19 選定的模型無法直接部署 | FACE | 中 | 待處理 |

---

## 2026-07-19 服務位址失聯、金鑰失效與 AI Gateway 遷移（S42～S48）

### 使用者看到的問題

- 商品頁空白、登入顯示「無法連線到會員資料庫」。
- 臉部分析按下「開始分析」後失敗，Console 顯示 CORS 錯誤。
- 後台原有的「專題展示」分頁消失。
- 修復過程中一度改為 502 `UPSTREAM_UNAVAILABLE`。

### 排查證據與根因

本日共四個獨立故障，彼此症狀互相掩蓋。

**S42：設定檔覆寫（商品與登入全失效）**

1. Console 出現矛盾現象：`api.js` 版本號是最新的，但請求打到的是舊網址。
2. 根因為 `js/service-endpoints.js` 在 `index.html` 的載入順序**排在 `config.local.js` 之後**，且以 `Object.assign` 覆寫 `memberDatabaseUrl` 與 `productUrl`。因此只改 `config.local.js` 完全無效。
3. Quick Tunnel 再次更換網址（`fact-mice-prince-voltage` → `yen-minute-from-ict`），與 S41 同類問題，但這次多了覆寫層而更難定位。

**S43：金鑰輪替後前端未同步（臉部分析／渲染失效）**

1. `/health` 回 `200`，但實際工作端點 `POST /v1/face/jobs/basic` 回 `500`。健康檢查未經過金鑰驗證，**不能作為服務可用的判準**。
2. Cloud Run 日誌顯示真正例外：

   ```text
   File "/app/Face_analyzer_BASIC.py", line 64, in _api_key_guard
       raise HTTPException(...)
   fastapi.exceptions.HTTPException: 401: {'error': {'code': 'FORBIDDEN', ...}}
   ```

3. 該 `HTTPException` 在 **middleware** 內拋出，FastAPI 的例外處理器只涵蓋 route handler，因此成為未處理例外，由 Starlette 回裸 `500`，**繞過 CORSMiddleware**、回應缺少 CORS header。瀏覽器因而報成 CORS 錯誤——症狀與真正的 CORS 設定錯誤完全相同。
4. 比對金鑰：前端持有 `face_dm_…`（28 碼），Secret Manager 為 `face_gw_…`（73 碼，7/18 19:20 建立）。渲染服務同樣情形（`rnd_dm_…` 27 碼 vs `render_g…` 75 碼），且尚無人回報。
5. 時間軸確認為一次「安全收尾」批次作業只做了一半：

   | 時間（UTC+8） | 動作 |
   |---|---|
   | 7/18 19:20:02～11 | 建立三把新 secret |
   | 7/18 19:20:42 | face-basic 重新部署，開始只認新金鑰 |
   | 7/18 19:21:52 | replicate-render 重新部署，開始只認新金鑰 |
   | 7/18 19:24～19:51 | ai-gateway 連續三次部署，啟用 session-only |

   後端全部換新，但**前端從未改為呼叫 Gateway**，因此自 7/18 19:21 起兩項功能同時失效。

**S44：Secret 含 CRLF（Gateway 從未成功呼叫上游）**

1. 前端遷移到 Gateway 後改報 502 `UPSTREAM_UNAVAILABLE`。
2. Gateway 日誌顯示失敗只花 **13 毫秒**，且**沒有對應的 `httpx HTTP Request` 記錄**（成功的 `/auth/login` 有），代表請求根本沒送出。上游直連正常、雙方 ingress 皆為 `all`、無 VPC 限制。
3. 檢查 secret 原始位元組發現結尾為 `0d 0d 0a`（`\r\r\n`），為在 Windows 建立 secret 時混入：

   ```text
   decorate-me-face-upstream-key    72 字元 + \r\r\n = 75 位元組
   decorate-me-render-upstream-key  74 字元 + \r\r\n = 77 位元組
   ```

4. HTTP 規範禁止 header 值含 CR/LF（防 header injection），函式庫在送出前即拋 `LocalProtocolError`（`httpx.HTTPError` 子類），被 Gateway 接住後回 502。
5. 因此 **Gateway 自 7/18 建立金鑰起，從未成功呼叫過 face 與 render**。此故障無任何明確錯誤訊息指向真因，上游端也完全沒有記錄。

**S45：前端兩份副本互相覆蓋**

1. 「專題展示」在本 repo 的 git 歷史中完全查不到（`git log -S` 無結果）。
2. 由 Codex session 記錄找到來源，其 patch 標頭為 `*** Update File: firebase-hosting-full/public/index.html`——功能做在**另一份前端副本**，且該檔案至今仍為未追蹤狀態。
3. 兩份副本互有新舊：`firebase-hosting-full` 有專題展示與 Gateway rewrites；`web_frontend` 有 7/18 商品後台改版與本日 Gateway 遷移。因此**任何一方部署都會覆蓋另一方**。本日從 `web_frontend` 連續部署，導致線上專題展示消失。

### 修正內容

**前端（`web_frontend`，分支 `dev_makeup`）**

- `js/service-endpoints.js`：更新為現行資料庫網址；`aiGatewayUrl` 刻意留空，改用同源路徑。
- `firebase.json`：加入 Gateway rewrites（`/auth`、`/admin-api`、`/face-basic`、`/face-pro`、`/text-suggestion`、`/render-service`、`/member-database`）與安全 headers（HSTS、`nosniff`、`X-Frame-Options`、Referrer-Policy、Permissions-Policy）。
- `js/api.js`：
  - `faceBasic`／`facePro`／`render` 的 baseUrl 改為同源 Gateway 路徑。
  - 新增 `loginToGateway()`，於既有登入成功後補換 Gateway session；**刻意不阻擋登入流程**，Gateway 異常時其他功能仍可用。
  - `X-API-Key` 全面移除，改帶 `Authorization: Bearer <gateway session>`。
  - 一併移除 `X-User-Email`／`X-User-Role`（瀏覽器可偽造，Gateway 亦不轉送）。
  - 新增 `_faceError()`，同時解析上游的 `{detail:{error}}` 與 Gateway 的 `{error:{code,message}}`；`401` 直接提示重新登入。
- 移植專題展示面板（`pages/admin.html`、`initAdminDemo()`、CSS、`js/config.js` 的 feature flag）。
- 點數紀錄代碼中文化（`check_in`→每日打卡、`task_first_analysis`→任務獎勵：完成第一次臉部分析等）。

**後端（Cloud Run／Secret Manager）**

```bash
# 去除金鑰尾端換行（金鑰值本身不變）
gcloud secrets versions access latest --secret=decorate-me-face-upstream-key --project=decorate-me \
  | tr -d '\r\n' \
  | gcloud secrets versions add decorate-me-face-upstream-key --data-file=- --project=decorate-me

# 讓四個服務重新讀取（Cloud Run 僅於執行個體啟動時解析 latest）
gcloud run services update face-basic       --region=asia-east1 --update-secrets=FACE_API_KEY=decorate-me-face-upstream-key:latest
gcloud run services update face-pro         --region=asia-east1 --update-secrets=FACE_API_KEY=decorate-me-face-upstream-key:latest
gcloud run services update replicate-render --region=asia-east1 --update-secrets=RENDER_API_KEY=decorate-me-render-upstream-key:latest
gcloud run services update ai-gateway       --region=asia-east1 --update-env-vars=SECRET_REFRESHED_AT=<時間戳>
```

**注意**：第 4 步若同樣使用 `--update-secrets` 指向 `:latest`，gcloud 會判定設定未變而**不建立新 revision**，舊執行個體將持續使用舊值。必須改動一個實際欄位才會強制產生新 revision。

**副本統一**

- 以 `web_frontend` 為唯一來源，併入另一份獨有的專題展示與 `firebase.json` 設定。
- 於 `firebase-hosting-full/` 放置 `DEPRECATED_請勿部署.md`，記錄已併入項目與尚未併入項目（商品稽核分頁）。

### 驗證與發布結果

- 前端 `node --check` 與 `frontend_smoke_check.js` 通過。
- Gateway 路由實測：`/face-basic/…` 未帶 session 回 `401 MEMBER_AUTH_REQUIRED` **且帶 CORS header**（與上游裸 500 不同）；`/auth/login` 回 `401 INVALID_CREDENTIALS`，確認已轉送至會員資料庫。
- 上游金鑰修復後直測：face job 建立回 `200` 並回傳 `jobId`；render 回 `422 VALIDATION_ERROR`（僅測試資料不合法，驗證已通過）。
- **臉部分析端對端驗收通過**（2026-07-19 12:12 UTC）：

  ```text
  POST /face-basic/v1/face/jobs/basic                    200  256ms
  GET  /face-basic/v1/face/jobs/JOB-xxxxxxxxxxxx         200  輪詢
  GET  /face-basic/v1/face/jobs/JOB-xxxxxxxxxxxx/result  200  取得結果
  ```

  修復後 8 分鐘內共 3 次完整分析、18 筆請求、**零失敗**，`/health` 預熱亦恢復正常。
- **AI 渲染尚未端對端驗收**（修復內容與臉部分析相同）。
- 前端分支 `dev_makeup`，部署 commit：`b5614b8`（`fix: route the render service through the AI Gateway`）。
- 正式網站：`https://decorate-me.web.app`。

### 後續處理原則

1. **`/health` 回 200 不等於服務可用**。健康檢查通常不經過金鑰驗證與模型載入，必須直接測試真正的工作端點。
2. **瀏覽器報 CORS 錯誤不一定是 CORS 問題**。後端未處理例外會使錯誤回應缺少 CORS header，症狀完全相同。應查後端日誌確認實際例外。
3. **切勿以無效憑證的測試結果斷定對方未實作**。假 token 被拒與不支援該驗證方式，回應可能一致。本日曾據此誤判資料庫端未實作 Bearer，後已更正。
4. **狀態碼須分辨**：302／401 為未通過身分驗證，403 為已通過但無權限。終端機測得 302 而瀏覽器得 403，代表兩者送出的憑證不同。
5. **建立 secret 後必須驗證原始位元組**：`gcloud secrets versions access latest --secret=X | xxd | tail -1`，確認結尾無 `0d`／`0a`。在 Windows 上以 `echo` 或管線建立極易混入 CRLF，且此類錯誤只會表現為連線層失敗，極難追查。
6. **金鑰輪替必須連同所有持有端一起完成**。本次後端已換、前端未換，造成兩項功能靜默失效近一日。長期解法是前端完全不持有金鑰（改走 Gateway），如此輪替對前端零影響。
7. **維持單一前端來源**。兩份副本並存時，任一方部署都會覆蓋另一方，且問題不會出現在 git 歷史中。

---

## 2026-07-17 會員資料庫網址不同步修正（S41）

### 使用者看到的問題

- 正式網站登入時顯示「無法連線到會員資料庫，請稍後再試」。
- 一開始依錯誤訊息懷疑會員資料庫服務離線，但資料庫組確認目前服務與新網址可正常運作。

### 排查證據與根因

1. Firebase 正式前端當時仍載入舊設定：
   `https://stainless-exit-spot-providing.trycloudflare.com`。
2. 舊網址的 `/health` 在瀏覽器回 `ERR_NAME_NOT_RESOLVED`，表示請求尚未到達資料庫，不能據此判定資料庫程式故障。
3. 資料庫端提供目前有效網址：
   `https://debut-exemption-lounge-depending.trycloudflare.com`。
4. 新網址實測：
   - `GET /health`：`200 OK`，回傳 `service=member-database`、`status=ok`。
   - `GET /api/products`：`200 OK`。
   - `OPTIONS /api/login`，Origin 為 `https://decorate-me.web.app`：`204 No Content`。
   - CORS 正確允許正式網站、credentials、`Content-Type`／`Authorization`，以及 `GET, POST, PATCH, DELETE, OPTIONS`。
5. 根因確定為 Cloudflare Quick Tunnel 更換後，我方正式前端仍保存舊網址；不是會員資料庫程式、登入格式或 CORS 故障。

### 修正內容

- 前端 `config.local.js`：
  - `memberDatabaseUrl` 更新為新網址。
  - `productUrl` 更新為新網址。
- 後端本機 `.env`：
  - `MEMBER_DATABASE_URL` 更新為新網址；`.env` 仍不進 Git。
- Cloud Run `ai-gateway` 經實際查詢，`MEMBER_DATABASE_URL` 原本已是新網址，因此不需重新部署 Gateway。
- Firebase Hosting 已重新部署，並從正式網站讀回 `config.local.js`，確認會員與商品網址皆為新網址。

### 驗證與發布結果

- 前端 `node --check` 與 `frontend_smoke_check.js` 通過。
- Firebase 正式網站：`https://decorate-me.web.app`。
- 前端 GitHub 分支：`dev_makeup`。
- 前端部署 commit：`a0677ab`（`deploy updated member database endpoint`）。
- 注意：`config.local.js` 依資安規則不進 Git；GitHub 提交只保留部署版本號，實際 runtime URL 由 Firebase 部署內容提供。

### 後續處理原則

- 看到「無法連線到會員資料庫」時，先比對「資料庫端目前網址」與「正式站實際載入的 `config.local.js`」，不要只測歷史文件裡的 Quick Tunnel。
- Quick Tunnel 每次重啟可能換網址；資料庫端換網址後，必須同步通知前端並重新部署 Firebase。
- 長期應改用固定 Cloudflare Named Tunnel 或正式網域，避免反覆修改與部署。

---

## 2026-07-16 收藏與後台資料庫控制修正

### 1. 妝容收藏不再只顯示六筆（S37）
- 前端會員中心原本使用 `slice(0, 6)`，導致 localStorage 或資料庫已有 15 筆時畫面只看得到 6 筆。
- 已改為顯示合併後的完整收藏清單；localStorage 仍保留最多 20 筆，避免無限制膨脹。
- 資料庫同步失敗的舊收藏仍保留在本機，畫面標示「本機快取」；這類資料若只有 base64/data URL，不能直接寫入資料庫的 String(500) 圖片欄位。

### 2. 前台與後台共用妝容對比詳情版面（S37）
- 後台會員的 saved_looks 卡片改成可點擊，開啟與前台相同的妝前／妝後對比詳情視窗。
- 後台收藏卡片提供刪除按鈕；只有資料庫 DELETE 成功後才更新後台畫面。
- 前台刪除已同步收藏時，先刪資料庫、成功後才刪 localStorage；資料庫失敗則保留本機資料並提示，不製造兩邊不一致。
- 對妝容文字與圖片 URL 增加輸出轉義及 `http(s)`／合法圖片 data URL 檢查，避免收藏資料被當成 HTML 注入。

### 3. 後台會員刪除同步資料庫（S38）
- 管理員列表新增「刪除會員」，呼叫會員資料庫 `DELETE /api/members/{email}`。
- API 成功後才從後台畫面移除該會員；目前登入中的管理員帳號不可自我刪除。
- 後台既有商品新增／編輯／刪除、會員權限 PATCH、妝容收藏刪除，均以資料庫成功回應作為畫面更新條件。

### 4. 後台收藏版面放大（S39）
- 收藏視窗由窄版 640px 改為最多 1100px，卡片由三欄壓縮改為兩欄大卡片。
- 桌面版放大圖片與文字欄位；手機版改為單欄，維持垂直滑動。

---

## 2026-07-12~2026-07-13 串接／部署流程更新

### 1. 渲染模型切換為 `openai/gpt-image-2`（經 Replicate 呼叫）
- 背景：原本渲染端以 Replicate 其他模型為主，這次改測 `openai/gpt-image-2`，目標是降低妝容失真、換人、性別改變等問題。
- 後端調整（RENDER 端）：
  - `replicate_render.py`、`replicate_render_api.py` 已加入 provider/model 切換邏輯。
  - 目前支援兩種模式：
    - `IMAGE_PROVIDER=replicate`：透過 Replicate 呼叫 `openai/gpt-image-2`
    - `IMAGE_PROVIDER=openai`：未來可直接走 OpenAI API
  - 針對 `openai/gpt-image-2` 的 Replicate schema，輸入欄位已改為 `input_images`，`output_format` 需使用 `png`、`jpeg`、`webp` 其中之一。
- 已踩到的錯誤：
  - 曾出現 `422 Input validation failed`，原因是 `output_format` 不符合 Replicate 對 `openai/gpt-image-2` 的限制。
  - 目前已修正為合法格式，避免再次因 schema 不合而渲染失敗。

### 2. Render prompt 改成前端可編輯
- 背景：為了降低妝容過重、臉型跑掉、性別改變等問題，需要能快速人工微調英文 prompt，而不是每次改程式。
- 前端調整（FE 端，`web_frontend`）：
  - `js/api.js`：`buildRenderPrompt(...)` 已支援額外帶入 `userCustomPrompt`，且優先使用。
  - `js/router.js`：compare 頁面已加入 `可編輯英文渲染指令` 輸入區；使用者送出渲染時，會把 textarea 內容一併送進 render prompt。
  - `pages/compare.html`：同步補上對應 textarea 區塊。
- 目的：後續做模型 A/B test 或 prompt 微調時，可直接在畫面上改，不用每次都改 JS。
- 後續決議（2026-07-13）：測試期結束後已移除這條流程。前端不再顯示、編輯或送出英文 prompt；正式作法見下方 S21 收斂紀錄。

### 3. 這次變更已實際部署，不是只有本機
- 後端部署：
  - Cloud Run render service 已重新部署。
  - 健康檢查已確認服務設定反映目前 provider/model 狀態。
- 前端部署：
  - `web_frontend` 已重新部署到 Firebase Hosting。
  - 線上網址：`https://decorate-me.web.app`
- 流程決議：
  - 自 2026-07-13 起，這個專案的功能修改預設採「本機修改 + 直接部署」。
  - 若是高風險、大改、或需要產品決策的內容，先列入待確認，不直接上線。

### 4. 渲染品質驗收結果
- `gpt-image-2` 接上後，已由專案負責人自行上線，以真人照片實際跑過完整流程，不是只憑模型名稱或健康檢查判定。
- 人工驗收包含人物身分是否維持、性別是否被改變、妝容是否確實套用，以及妝前／妝後比例是否一致。依目前實測結果已達展示版本標準，S19 改列「已解決」。
- 生成式影像仍具有非決定性；「已解決」代表目前模型、prompt 與參數組合已通過實際驗收，不代表未來每一張照片都能保證零偏差。若後續再次出現換人或明顯失真，應保存輸入、styleId、模型版本與輸出，另開迴歸問題追蹤。
- S21（前端可控 prompt）已於 2026-07-13 收斂：前端只送白名單 `styleId`，後端自行組合完整渲染指令。

---

## 2026-07-13 渲染品質、效能與體驗修正

接續上面的 `gpt-image-2` 切換。實際使用後回報「自訂 prompt 完全沒渲染上去」，追下去發現不只一個問題，一併處理。

### 5. 自訂 prompt 失效：前端 identityLock 疊加把妝容指令淹沒(已解決)
- 症狀：使用者在 compare 頁貼入完整的 soft baddie prompt，渲染回 200、圖也出得來，但妝完全沒上，輸出近乎原圖。
- 排查：Cloud Run log 顯示 `/render` 全數 200、無 exception，`/health` 確認模型確實是 `openai/gpt-image-2` → 排除「模型沒切換」與「請求失敗」。
- 根因（FE 端）：`js/api.js` 的 `buildRenderPrompt()` 回傳 `${makeupInstruction} ${identityLock}`，使用者自訂 prompt 只被當成 `makeupInstruction`，後面固定串上 10 句「Do not change / Keep identical / Preserve」。使用者的 prompt 本身已自帶完整身分鎖，疊加後整串約 20 餘句在要求「什麼都不准動」，只有數句在描述妝容。gpt-image-2 是重繪型模型，面對壓倒性否定約束，最保險的解就是輸出近乎原圖。
- 解決思路：有 `userCustomPrompt` 時直接原封不動送出，不再疊 `identityLock`；沒有自訂時維持原行為（Ollama 指令＋identityLock）。
- 驗證：`curl https://decorate-me.web.app/js/api.js` 確認線上為新版（前端在 OneDrive，需防同步延遲上傳到舊檔）。

### 6. 品質參數名稱不匹配，線上一直跑 medium(已解決)
- 問題：Cloud Run 設的環境變數是 `GPT_QUALITY=high`，但 `replicate_render.py` 讀的是 `REPLICATE_OPENAI_QUALITY`（預設 `medium`）。名稱對不上 → 該設定從未生效，線上實際一直是 medium。
- 解決思路：改用程式碼真正讀取的 `REPLICATE_OPENAI_QUALITY`，移除 `GPT_QUALITY`。
- 取捨：實測 quality 不是速度瓶頸（見下一節），最終仍選 `medium`，把畫質預算讓給速度；妝容濃度改由 prompt 文字控制。

### 7. 渲染逾時 504：改為非同步 job + 進度條(已解決)
- 問題：`gpt-image-2` 實測耗時 53～152 秒且浮動極大，同步 `/render` 多次撞上逾時，log 出現數筆 `504`（整整 120 秒）。
- 關鍵認知：浮動來自 Replicate 轉手 OpenAI 的排隊時間，不是我們算得慢，**壓低 quality 砍不掉這段**。因此「保品質又要快」在模型層面無解，只能改成不阻塞。
- 解決思路（RENDER 端）：
  1. 新增 `POST /render/jobs` → 立刻回 `jobId`，實際渲染丟背景 thread；`GET /render/jobs/{id}` 供輪詢。舊的同步 `/render` 保留不動，避免既有呼叫端壞掉。
  2. job 狀態走 **Firestore**（`job_store.py`）而非 process 記憶體——本服務 `maxScale=20`、`concurrency=4`，輪詢請求極可能被導到另一個 instance，記憶體裡的 job 在那邊不存在。
  3. Cloud Run 必須加 `--no-cpu-throttling`，否則回應送出後 CPU 被節流，背景 thread 形同凍住。
  4. 輪詢端點不掛 rate limit，否則前端每 2 秒一次的輪詢會把自己的配額燒光。
- 前端（FE 端）：`Api.renderMakeupAsync()` 送出後每 2 秒輪詢；compare 頁進度條 1→100。進度是**依經過時間估算**的體感值（Replicate 不回報真實進度），用指數曲線讓前段快、越接近 95% 越慢，跑完才跳 100，避免提前塞滿空轉。
- 驗證：以無效小圖送出 job，實測狀態依序寫入 `queued → running → failed`，跨請求輪詢讀得到 → 證明 Firestore 與 `--no-cpu-throttling` 均生效。

### 8. 輸出圖被鎖死正方形：aspect_ratio 未指定(已解決)
- 問題：Replicate 的 `openai/gpt-image-2` schema 中 `aspect_ratio` **預設為 `1:1`**，而我們從未傳這個參數。手機直式人像（3:4、9:16）送進去會被硬塞成正方形，臉遭裁切或壓扁，且妝前妝後兩張圖比例不一致，對比畫面跑掉。
- 解決思路（RENDER 端）：新增 `pick_aspect_ratio()`，用 Pillow 讀出輸入圖實際寬高，從模型支援的 `1:1 / 3:2 / 2:3 / 4:3 / 3:4 / 16:9 / 9:16` 中挑最接近者送入；讀不出尺寸時退回 `auto`（仍優於寫死 1:1）。`requirements.render.txt` 新增 `pillow`。
- 驗證：本機測 3024x4032→`3:4`、1080x1920→`9:16`、4032x3024→`4:3`、1000x1000→`1:1`，壞資料→`auto`。

### 9. 冷啟動：min-instances + 前端預熱(已解決)
- 問題：`face-basic`／`face-pro`／`replicate-render` 的 `min-instances` 皆為 0，閒置後容器縮到零，第一個使用者要等冷啟動（mediapipe 載模型尤其久）。
- 解決思路：
  1. 三支服務設 `min-instances=1` 常駐。**成本須知**：Cloud Run 永久免費層不涵蓋 min-instance 閒置費用，三支合計約 US$35/月，目前由學生 300 美元額度支應（可撐約 8 個月）。額度用盡前需改回 0。
  2. 另加零成本保險：前端進分析頁時呼叫 `Api.warmFaceServices()` 背景打 `/health` 叫醒容器，使用者選照片的數十秒剛好蓋掉冷啟動。
- 驗證：三支 `minScale=1`，`/health` 回應時間 0.3～0.7 秒。

### 10. 真人照片完整流程已驗收(已解決)
- 開發階段曾先以無效圖驗證非同步 job 的 `queued → running → failed` 狀態與 Firestore 跨請求讀寫；那只是在確認 job 管線，不是最終品質驗收。
- 後續已由專案負責人自行使用真人照片，在正式網站跑過上傳、分析、妝容建議、AI 渲染、完成輪詢與妝前／妝後比較的完整流程。
- 人工檢查已涵蓋原先待確認的三項：人物沒有被替換或明顯改臉、性別呈現沒有被錯改、輸出比例與對比版面正確。原本「尚未用真人照片測試」的紀錄已失效，不再列為剩餘工作。
- 驗收屬目前正式版本的人工結果，沒有建立大規模照片集的量化身分相似度指標；若未來更換模型或大幅修改 prompt，S19 必須重新做一次真人照片迴歸驗收。

## S1 會員個資可被任何人讀取(已解決)
- 問題：`GET /api/members` 無任何權限檢查，未登入也能撈全站會員 email／電話／姓名／角色。
- 根因：端點沒驗證呼叫者身分。
- 解決思路（DB 端）：在該端點加入「呼叫者 session 的 role 必須為 admin」的檢查——依身分回不同碼：未帶有效 session 回 `401`，登入但非 admin 回 `403`，admin session 才回 `200` 與清單。前端 `fetchAdminMembers` 以 `fetch(credentials:'include')` 帶 session cookie。
- 驗證：curl 匿名打回 `401`；admin 登入帶 cookie 回 `200` 且有 members。

## S2 AI 渲染服務可被無限呼叫燒錢(已解決)
- 問題：`allow_origins=["*"]`、`/render` 無金鑰、Cloud Run 公開，任何人掃到 URL 就能無限打，每次真花 Replicate 錢；前端的「每日次數」是 localStorage，直接打 API 可繞過。
- 解決思路（RENDER 端，`replicate_render_api.py`）：
  1. 金鑰：`/render` 掛 `Depends(require_api_key)`，比對 request header `x-api-key` 是否等於環境變數 `RENDER_API_KEY`，不符回 `401`。
  2. 限流：`enforce_render_rate_limit` 用 `dict[key] = deque[timestamp]` 做固定時間窗（預設 3600 秒／10 次），key = 呼叫者 IP + `X-User-Email`；把過期時間戳 popleft 掉，超量回 `429`（帶 retryAfter）。
  3. 去重：以 `sha256(image + prompt + strength)` 為 key，`_dedup_cache[key] = (timestamp, result)`，TTL 600 秒；命中且未過期就直接回上次結果，不再打 Replicate。
  4. CORS：`allow_origins = get_cors_origins()`（讀環境變數 `CORS_ORIGINS`），不用 `*`。
- 驗證：無 key 打 `/render` 回 `401`；`/health` 回 `api_key_required/rate_limit.enabled/dedup.enabled` 皆為 true。

## S3 臉部分析服務無 API key(已解決)
- 解決思路（FACE 端）：用 FastAPI `@app.middleware("http")` 寫 `_api_key_guard`——若環境變數 `FACE_API_KEY` 有設，且路徑不在 `{"/health","/"}`、method 非 OPTIONS，就檢查 header `x-api-key` 是否相符，不符直接回 `401`，不進端點。放行健康檢查與 CORS preflight。
- 驗證：`/health` 200；帶錯 key 的分析請求被 middleware 擋下。

## S4 前端金鑰進版控(已解決)
- 問題：`config.local.js`（含 render／face／ollama 金鑰）跟著 git 上傳。
- 解決思路（FE 端）：把 `config.local.js` 加進 `.gitignore` 停止追蹤；另建 `config.local.example.js` 當範本，欄位值填佔位符（如 `<render 服務的 X-API-Key>`）。程式一律從 `window.DECORATE_ME_CONFIG` 讀，不寫死金鑰。
- 驗證：`git ls-files` 確認 `config.local.js` 未被追蹤；範本檔內為佔位符。

## S5 git 歷史殘留 Ollama 金鑰(待處理)
- 問題：`config.local.js` 早期被 commit，git 歷史永久保留一把舊 Ollama 金鑰（文件不再記錄實際值；face／render 金鑰是 config 移出版控後才加，未進歷史）。
- 解決思路：git 歷史清除需 filter-branch／BFG 重寫歷史，會使全隊既有 clone 失效，成本高。畢業專題採「輪換金鑰」——請 Ollama 端換一把新金鑰，前端更新本機 `config.local.js`，舊金鑰即使外流也失效。
- 現況：目前追蹤檔已無金鑰；等 Ollama 端輪換。

## S6 渲染人臉圖存公開 GCS bucket(取捨)
- 問題：bucket `decorate-me-renders` 權限為 `allUsers:objectViewer`，渲染圖網址任何人有連結就能開，不需登入。
- 緩解：檔名用亂數 UUID（`rendered/{uuid.hex}.jpg`），無法用序號枚舉；殘餘風險是「網址一旦流出就收不回」。
- 取捨：改「私有 bucket + 短期簽名 URL」較安全，但收藏功能要存永久連結，改用會過期的簽名 URL 就得每次由後端重簽，複雜度上升。畢業專題階段維持公開並記錄此取捨；正式營運再切私有。

## S7 OTP 未真正防禦，可用假／不存在 email 建帳號並登入(待處理)
- 現象：用假的、甚至根本不存在的 email 也能完成註冊並進入 app，OTP 沒有實際擋下。
- 破口一（前端，直接原因）：`router.js` 的 `doVerifyOTP` 在 OTP 檢查通過後，是**前端自己呼叫 `Auth.setProfile(...)` 直接進 app**（純本機狀態），整條註冊路徑從頭到尾沒打後端 `login`。所以之前 DB 做的「未驗證帳號 `login` 回 403」防線，在註冊流程上根本沒被經過——前端自己就放行了。
- 破口二（資料庫，根本原因）：email 的**真實性從未被驗證**。只要格式合法，`register` 就把帳號建進 DB；不存在的信箱、拋棄式信箱（mailinator/example.com…）照收。若 `send-otp` 沒真的寄信、或 `verify-otp` 沒嚴格比對 DB 存的碼與時效，OTP 就形同虛設。
- 附註：前端有 `allowInsecureOtpBypass` 開關（驗證失敗仍放行任意 ≥4 碼），目前 `config.local.js` 已明確設為 `false`，不是當前破口；若真的要做純本機 demo，才可臨時改成 `true`，正式部署必須保持關閉。
- 解決思路（要前後端一起）：
  - 前端（已修，2026-07-11）：`doVerifyOTP` 改成「以後端為準」——`verify-otp` 成功後不再本機直接 `setProfile`，而是打後端 `Api.login(email, password)` 拿到 session 與 member 才算登入；login 失敗就導回登入頁。帳號沒真的驗證成功、login 過不了就進不了 app，移除了「本機即視為登入」的捷徑。**注意：此修法生效的前提是後端 `verify-otp` 真的把帳號設為可登入、且 `login` 對已驗證帳號會成功；若後端這塊還沒就緒，正常使用者註冊後也會卡在「驗證碼正確但登入未通過」——所以要搭配下面的資料庫項一起完成。**
  - 資料庫：`send-otp` 必須真的用 SMTP 把碼寄到該信箱（收得到碼＝這是能收信的真帳號）；`verify-otp` 嚴格比對 DB 存的碼＋時效＋嘗試次數，通過才 `email_verified=true`；未驗證帳號不得 `login` 且定期清除。可再加「拒收拋棄式／一次性信箱網域黑名單」與「註冊時對 email 網域查 MX 記錄」擋掉不存在的地址。
- 驗證方式（修好後應通過）：不存在的 email 註冊 → 收不到碼 → `verify-otp` 回錯 → 進不了 app；拋棄式網域直接被 `register` 拒。

## S8 session cookie 無效期＋跨站，頻繁掉線(已解決)
- 問題：登入種的 cookie 是 `SameSite=None; Secure; HttpOnly` 但無 `Max-Age`／`Expires`（session cookie）；前端在 `decorate-me.web.app`、資料庫在 `*.trycloudflare.com` 屬跨站，第三方 cookie 被瀏覽器積極清除，重整就掉、後台一直要重登。
- 解決思路（DB 端）：cookie 加 `Expires`（7 天），變成持久 cookie，重整／關瀏覽器後仍在。
- 過渡緩解（FE 端）：見 S12，401 時自動重登再重試。
- 驗證：2026-07-11 實測登入 `Set-Cookie` 帶 `Expires=... +7 天`。

## S9 CORS 缺 Allow-Credentials(已解決)
- 問題：只回精確 `Allow-Origin` 但缺 `Access-Control-Allow-Credentials: true`，瀏覽器把帶 cookie 的回應整個丟掉，後台寫入操作全失敗。
- 解決思路（DB 端）：所有需登入的端點回 `Access-Control-Allow-Credentials: true`＋精確 `Allow-Origin`（非 `*`）＋ `Vary: Cookie`；OPTIONS preflight 也帶上。

## S10 前端 admin@ email 後門(已解決)
- 問題：`AdminStore.isAdminProfile()` 只要 email 開頭是 `admin@` 就當管理員，不看後端回傳角色——任何人註冊 `admin@任意網域` 就被前端當 admin。
- 解決思路（FE 端）：移除 `email.startsWith('admin@')` 判斷，改成只信任後端回的 `role === 'admin'` 或 `level === '管理員'`。

## S11 明碼密碼存 localStorage(已解決)
- 問題：`Auth.saveRegisteredMember` 把整個 profile（含 password）存進 localStorage，明碼、且永久保留。
- 解決思路（FE 端）：儲存前用解構 `const { password, ...safe } = profile` 剝掉密碼，只存 `safe`；並對既有資料 `delete existing.password` 清掉舊殘留。登入一律走 API 驗證，本機不需留密碼。
- 驗證：grep 確認 localStorage 寫入不再含 password。

## S12 自動重登暫存帳密(已解決)
- 背景：為緩解 S8 的掉線，登入成功時把帳密存進 sessionStorage（`beautyAuthCreds`，關分頁即清）。
- 最終決議（2026-07-13）：移除登入成功後寫入 `beautyAuthCreds` 的程式；頁面載入時也主動清除舊值。`_reLogin()` 不再讀密碼重登，會員 cookie 失效時由使用者重新登入。
- S18 新流程只把短期 `beautyAiAccessToken` 放在 sessionStorage。這是有期限、只能呼叫 AI Gateway 的 access token，不是會員密碼；登出時一併清除。
- 使用影響：跨站會員 cookie 若真的失效，後台操作不再無感自動恢復，畫面會要求重新登入。這是刻意取捨，用一次重新登入換掉前端保存明文密碼的風險。
- 驗證：正式站 `js/api.js` 不再包含 `setItem('beautyAuthCreds', ...)`，但包含 `beautyAiAccessToken` 的建立與登出清除流程。

## S13 臉部分析上傳無大小上限(已解決)
- 問題：端點 `contents = await file.read()` 先把整個上傳檔讀進記憶體才 resize，無大小上限；上傳超大檔可把服務記憶體塞爆（OOM/DoS）。
- 解決思路（FACE 端）：加 `_reject_if_too_large(contents)`——`len(contents) > MAX_UPLOAD_BYTES`（環境變數，預設 8MB）就 `raise HTTPException(413)`。BASIC 的 analyze／pose／job 三個上傳點都呼叫；PRO 集中在 `_read_image` 呼叫一次涵蓋所有上傳。
- 驗證：實測 9MB 檔（帶正確 key）→ `413`；小檔通過大小檢查後才進後續處理。

## S14 例外外洩內部錯誤(已解決)
- 問題：`except Exception as e: raise HTTPException(500, detail=str(e))` 把內部例外訊息（可能含路徑、堆疊線索）回給前端。
- 解決思路（FACE/RENDER 端）：500 級例外改成 `except Exception: logging.exception(...)` 把細節寫進伺服器 log，對外只回通用訊息（如「臉部分析服務發生錯誤，請稍後再試」）；400 級的使用者驗證訊息（如「沒偵測到人臉」）保留，因為那對使用者有用且不洩漏內部。

## S15 內部檔被部署到公開網站(已解決)
- 問題：`firebase.json` 只忽略 dotfiles／node_modules，導致 `.bak` 原始碼備份、內部 `.md` 文件、log 被一起上傳到公開網站（可被人抓）。
- 解決思路（FE 端）：`firebase.json` 的 `ignore` 加入 `**/*.md`、`**/*.bak*`、`**/*.log`、`**/*.docx`、`**/*.pdf`、`**/*.drawio`、`docs/`、`deploy.ps1` 等，讓部署只上傳網站實際需要的檔。
- 驗證：實測 `README.md`、`js/api.js.bak-before-split` 等在公開網址回 `404`；正常網站檔仍 `200`。

## S16 妝容收藏跨帳號互看(已解決)
- 問題：收藏存在固定的 localStorage key `beautySuggestions`，未依帳號區分；同一瀏覽器切換帳號時，會讀到同一份，等於看到別人的收藏。
- 解決思路（FE 端）：新增 `looksKey()` 回傳 `'beautySuggestions_' + 登入email.toLowerCase()`（未登入用 guest），把所有讀寫收藏的地方改用這個依帳號分開的 key；跨裝置的真實資料仍以後端 `saved_looks` 為準。
- 驗證：不同帳號登入讀到各自的 key，互不干擾。

## S17 登入無法區分未註冊 vs 密碼錯(已解決)
- 背景：後端對「email 沒註冊」和「密碼打錯」回一樣的 `401 INVALID_CREDENTIALS`，前端無法精準引導。
- 資安權衡：**能區分＝對外洩漏「哪些 email 有註冊」（帳號枚舉風險）**；高資安做法反而是「不區分」。是否區分屬產品決策。
- 最終決議（2026-07-13）：正式流程維持不區分，帳號不存在與密碼錯誤一律回 `401 INVALID_CREDENTIALS`。AI Gateway 的會員登入代理也採相同訊息，不把會員後端更細的錯誤直接轉給瀏覽器。
- 補強：Gateway 以來源 IP 做登入速率限制，預設 10 分鐘最多 10 次；超量回 `429` 與 `Retry-After`。
- 驗證：以不存在的測試 email 呼叫 Gateway，回應只有 `INVALID_CREDENTIALS`，不會指出該 email 是否已註冊。

## S18 Cloud Run 服務 allow-unauthenticated(已解決)

### 原本的問題

face-basic、face-pro、replicate-render 都由瀏覽器直接呼叫，所以三個 Cloud Run 服務同時存在 `allUsers:roles/run.invoker`，且停用了 Invoker IAM check。雖然應用程式會驗 `X-API-Key`，但 key 必須放在前端，任何人開瀏覽器開發者工具都能取得；對會消耗模型費用的 render 來說，不能把它當成會員身分驗證。

### 2026-07-13 完成的架構

1. 新增 `ai-gateway` Cloud Run 服務。瀏覽器不再直連三個核心服務，正式 Firebase 網域會固定改走：
   - `/face-basic/...`
   - `/face-pro/...`
   - `/render-service/...`
2. 使用者登入時仍先由既有會員後端驗證帳密；成功後，前端再呼叫 Gateway 的 `POST /auth/login`。Gateway 會自行向會員後端重驗一次，只有在後端回傳非空會員資料、且 email 與登入請求一致時，才簽發 2 小時的 AI access token。
3. 前端只把短期 access token 放在 sessionStorage。後續 face/render 請求必須同時帶 `X-API-Key` 與 `Authorization: Bearer <token>`；缺少、過期或簽章錯誤都回 `401`。
4. Gateway 對可代理的路徑採白名單，不提供任意 URL 轉送；請求本文上限 13 MiB，只轉送必要的 content type、job token 與經 Cloud Run 判定的來源 IP。瀏覽器自報的 email、role、Authorization 都不會送進核心服務。
5. Gateway 使用專用服務帳號 `ai-gateway`。它只在 face-basic、face-pro、replicate-render 三個服務取得 `roles/run.invoker`，呼叫時用目標服務 URL 當 audience 取得 Google 簽署的 ID token，放在 `X-Serverless-Authorization`。
6. 三個核心服務已移除 `allUsers`，並重新啟用 Invoker IAM check。直接呼叫三個原始網址現在都是 `403`；只有 Gateway 的服務帳號可以進入。
7. Gateway 的 session 簽章金鑰放在 Secret Manager，不寫入環境設定檔、映像或 Git。正式 revision 固定使用指定 secret version，避免 `latest` 被意外替換後立即影響線上 session。
8. CORS 只允許正式 Firebase 網域，並明確放行 `Authorization`、`X-API-Key`、`X-Job-Token`。`/js/api.js` 設為 no-store，避免瀏覽器沿用舊版直連設定。

### 為什麼 Gateway 本身仍是公開服務

瀏覽器無法持有 Google 服務帳號，因此 `ai-gateway` 的 Cloud Run 網址仍允許未登入 Google 帳號者連線；公開的是入口，不代表 AI 路由可以匿名使用。公開健康檢查只回服務狀態；會員登入端點要 API key、帳密驗證與速率限制；所有 face/render 代理路徑還要短期會員 token。真正持有模型、儲存與運算權限的三個核心服務已改為 IAM 私有。

### 使用與維運影響

- 已登入但尚未取得 AI token 的舊分頁，需要登出再登入一次；之後同一分頁在 2 小時內可持續呼叫 AI。
- 新登入會多一次 Gateway 到會員後端的驗證。會員後端中斷時無法建立新的 AI session；已簽發且未過期的 token 不受影響。
- 會員後端目前是外部網址。若該網址更換，除前端設定外，也要同步更新 Gateway 的 `MEMBER_DATABASE_URL`。
- token 是無狀態短期簽章，帳號停權後最長可能到 token 到期才完全失效。正式商用若要求立即登出所有裝置，可再加入 token version／撤銷清單；目前以 2 小時效期控制風險。

### 實際驗證

- Gateway revision：`ai-gateway-00004-tmj`，100% 流量。
- 三個核心服務匿名直連：face-basic `403`、face-pro `403`、replicate-render `403`。
- 以有效會員格式 token 經 Gateway 呼叫：BASIC `200`、PRO `200`、Render `200`。
- Gateway 缺 API key：`401`；有 API key 但缺會員 token：`401`；錯誤會員帳密：統一 `401 INVALID_CREDENTIALS`。
- Firebase CORS preflight：正式網域 `200`，回應允許 Authorization 與必要自訂標頭。

### 本機測試環境與新手操作教學

這一節專門記錄 Windows／PowerShell 要怎麼驗證前端語法與 AI Gateway 政策。重點不只是把指令跑完，而是要能分辨「程式真的壞了」和「目前叫到的 Python 沒裝專案依賴」。兩種狀況在畫面上都可能是紅字，但處理方式完全不同。

#### 1. 先理解系統 Python 和專案 `.venv` 的差別

在 PowerShell 輸入 `python` 時，Windows 會依 PATH 找一套系統 Python。這套 Python 可以執行語法，但不一定安裝本專案需要的 `FastAPI`、`httpx`、`PyJWT`、`google-auth`。因此同一份程式在 PyCharm 裡可以跑，直接在 PowerShell 執行卻可能出現 `ModuleNotFoundError`。

專案根目錄的 `.venv` 是這個專案自己的虛擬環境。依賴只裝在 `.venv` 裡，不會污染其他 Python 專案，也不需要依賴電腦目前 PATH 指到哪一套 Python。本專案在 PowerShell 測試時統一直接指定：

```powershell
.venv/Scripts/python.exe
```

這種寫法不需要先執行 `Activate.ps1`，也不會被常見的 PowerShell execution policy 擋住。路徑使用 `/` 或 `\` 都可以；文件使用 `/` 是為了比較容易閱讀。

#### 2. 進入正確的專案目錄

先開啟 PowerShell，執行：

```powershell
Set-Location "C:\Users\isach\PycharmProjects\PythonProject12"
Get-Location
git status --short --branch
```

預期 `Get-Location` 顯示 `C:\Users\isach\PycharmProjects\PythonProject12`。如果不是這個位置，後面的 requirements、JavaScript 和測試檔都可能找不到。

`git status --short --branch` 用來確認目前分支和未提交檔案。正常情況會看到 `Isa...origin/Isa`；如果下面還列出其他檔案，不要直接刪除或還原，先確認是不是自己尚未提交的工作。

#### 3. 確認 Node.js 和 `.venv` 可用

```powershell
node --version
Test-Path .venv/Scripts/python.exe
.venv/Scripts/python.exe --version
.venv/Scripts/python.exe -c "import sys; print(sys.executable)"
```

判讀方式：

1. `node --version` 應回傳版本號。若顯示找不到 `node`，要先安裝 Node.js，否則不能使用 `node --check`。
2. `Test-Path` 應回 `True`，代表專案虛擬環境存在。
3. `sys.executable` 應指向本專案的 `.venv\Scripts\python.exe`，不能指向使用者目錄下的系統 Python。

如果 `Test-Path` 回 `False`，表示這台電腦還沒有建立虛擬環境。可在專案根目錄執行：

```powershell
py -3.10 -m venv .venv
```

如果電腦只有 Python 3.11，也可以用 `py -3.11 -m venv .venv`。建立完成後，再執行一次 `Test-Path .venv/Scripts/python.exe` 確認。

#### 4. 依 requirements 同步 Gateway 依賴

建立 `.venv` 不等於依賴已經裝好。每次 requirements 有新增套件、剛拉下新 commit，或看到 `ModuleNotFoundError`，都先執行：

```powershell
.venv/Scripts/python.exe -m pip install -r requirements.gateway.txt
```

一定要用 `.venv/Scripts/python.exe -m pip`，不要只輸入 `pip`。只輸入 `pip` 可能把套件裝進系統 Python，結果專案 `.venv` 還是找不到。

安裝後可先做快速 import 檢查：

```powershell
.venv/Scripts/python.exe -c "import fastapi, httpx, jwt, google.auth; print('gateway dependencies ok')"
```

看到 `gateway dependencies ok` 才表示 Gateway 測試需要的主要套件都能載入。套件名稱和 import 名稱有一個容易踩到的差異：requirements 裡安裝的是 `PyJWT`，程式中使用的是 `import jwt`，不要另外安裝名稱相似但不是本專案使用的套件。

#### 5. 前端 JavaScript 語法檢查

本專案有正式完整版與 hotfix 備援版，兩份都要檢查：

```powershell
node --check firebase-hosting-full/public/js/api.js
node --check firebase-hosting-hotfix/public/js/api.js
```

成功時通常完全沒有文字輸出，直接回到 PowerShell 提示符號。沒有輸出不代表沒執行；對 `node --check` 而言，沒有錯誤訊息且 exit code 為 0 就是通過。

可立即確認上一個指令的 exit code：

```powershell
$LASTEXITCODE
```

預期為 `0`。如果 JavaScript 少括號、字串沒有結束或有其他語法錯誤，Node.js 會印出檔名、行號和錯誤位置，exit code 會不是 0。先依行號修正，再重新檢查兩份檔案。

`node --check` 只確認 JavaScript 能被解析，不會開啟網頁，也不會驗證登入 API、CORS、按鈕事件或畫面流程。正式網站的功能仍要另外做瀏覽器與線上 API 驗收。

#### 6. 後端 Gateway 語法與政策測試

先檢查 Python 檔能不能編譯：

```powershell
.venv/Scripts/python.exe -m py_compile ai_gateway.py ai_gateway_test.py
```

成功時同樣不會輸出文字。接著執行完整 Gateway 單元測試，建議加 `-v`，讓每一項測試名稱都顯示出來：

```powershell
.venv/Scripts/python.exe -m unittest -v ai_gateway_test.py
```

目前共有 5 個政策測試：

1. `test_only_known_routes_are_allowed`：只允許既定 face/render 路徑，拒絕目錄跳脫、OpenAPI 和不合法 jobId。
2. `test_client_key_is_required`：正確 API key 可通過，錯誤 key 必須回 `401`。
3. `test_forwarded_ip_uses_cloud_run_caller_position`：從 Cloud Run 的轉送鏈取可信的呼叫端位置，不直接相信使用者塞在最前面的假 IP。
4. `test_headers_strip_untrusted_identity`：不把瀏覽器自報的 email、role 或 Authorization 直接送進核心服務；只保留必要標頭並改用 Google 服務身分。
5. `test_member_access_token_is_required_and_verified`：有效簽章 token 可通過，亂填 token 必須回 `401`；token 到期則由同一套 JWT `exp` 驗證規則拒絕。

成功時結尾應類似：

```text
Ran 5 tests in 0.001s

OK
```

執行秒數可能不同，不需要剛好是 `0.001s`；判斷重點是 `Ran 5 tests` 和最後一行 `OK`。

#### 7. 如何判斷錯誤屬於哪一類

**A. `ModuleNotFoundError: No module named 'httpx'`**

代表目前使用的 Python 沒裝 `httpx`。如果錯誤發生在 `import ai_gateway` 階段，測試甚至還沒有開始，不能記成 Gateway 政策失敗。先確認 interpreter：

```powershell
python -c "import sys; print(sys.executable)"
.venv/Scripts/python.exe -c "import sys; print(sys.executable)"
```

兩行通常會指向不同位置。後續改用第二行的 `.venv` Python，再安裝 requirements。

**B. `ModuleNotFoundError: No module named 'jwt'`**

代表 `.venv` 尚未同步新增的 `PyJWT`。修正方式不是改程式中的 `import jwt`，而是：

```powershell
.venv/Scripts/python.exe -m pip install -r requirements.gateway.txt
```

**C. 顯示 `FAILED (failures=1)`**

這才表示測試有成功啟動，而且至少一個 assertion 結果不符合預期，通常屬於程式邏輯或測試預期需要檢查。從 traceback 最下方往上看，先找失敗的測試名稱和實際值／預期值，不要先重裝所有套件。

**D. 顯示 `FAILED (errors=1)`**

代表測試執行過程發生未處理例外。可能是程式問題，也可能仍是環境問題；要看 traceback。若錯誤仍是 import 或找不到檔案，先處理環境；若已進入某個函式才拋例外，再檢查程式。

**E. PowerShell 不允許執行 `Activate.ps1`**

不需要修改整台電腦的 execution policy，也不必用系統管理員權限。直接使用 `.venv/Scripts/python.exe` 執行所有 pip、compile 和 unittest 指令即可。

#### 8. 這次實際測試發生的完整順序

1. 先對正式版與 hotfix 的 `api.js` 執行 `node --check`，兩份都沒有語法錯誤，exit code 為 0。
2. 第一次直接用 PowerShell 預設 `python -m unittest ai_gateway_test.py`，系統 Python 缺少 `httpx`，在 import 階段出現 `ModuleNotFoundError`。此時沒有任何政策測試被執行，所以不是程式邏輯失敗。
3. 改用專案 `.venv/Scripts/python.exe` 後，`httpx` 已存在，但 `.venv` 尚未同步這次 Gateway 新增的 `PyJWT`，因此出現 `No module named 'jwt'`。這仍是依賴未同步，不是 assertion 失敗。
4. 執行 `.venv/Scripts/python.exe -m pip install -r requirements.gateway.txt`，依 requirements 補齊 `PyJWT`、`google-auth` 與相關執行依賴。
5. 再執行 `.venv/Scripts/python.exe -m unittest ai_gateway_test.py`，結果為 `Ran 5 tests`、`OK`，確認 5 個 Gateway 政策測試全部通過。
6. 這兩次 import failure 都不需要修改 Gateway 程式碼；修正的是本機虛擬環境，使它和已提交的 requirements 一致。

#### 9. 日後每次修改 Gateway／前端後的標準順序

建議依下列順序執行，不要一改完就直接部署：

```powershell
Set-Location "C:\Users\isach\PycharmProjects\PythonProject12"

# 1. 確認分支與未提交內容
git status --short --branch

# 2. 同步 Gateway 依賴
.venv/Scripts/python.exe -m pip install -r requirements.gateway.txt

# 3. 檢查兩套前端 JavaScript 語法
node --check firebase-hosting-full/public/js/api.js
node --check firebase-hosting-hotfix/public/js/api.js

# 4. 檢查 Gateway Python 語法
.venv/Scripts/python.exe -m py_compile ai_gateway.py ai_gateway_test.py

# 5. 執行 Gateway 政策測試
.venv/Scripts/python.exe -m unittest -v ai_gateway_test.py

# 6. 檢查 Git diff 是否有空白或 patch 格式問題
git diff --check
```

所有步驟通過後，才進入 Cloud Build、Cloud Run、Firebase Hosting 與線上 IAM／CORS 驗證。本機 5 個測試通過代表 Gateway 核心政策符合程式預期，但不能取代線上服務帳號、Secret Manager、Cloud Run IAM 和正式網域 CORS 的部署後驗證。

## 2026-07-13 後端資安第二輪整理（S22~S27）

這一輪不是新增功能，而是把「服務已經能跑」往「不容易被濫用、出錯時比較好收尾」推進。前面 S2、S3 已經把 face/render 的 API key 補上，但後來全系統檢查時又看到幾個缺口：Ollama 文字建議服務沒跟上同一套保護、非同步 job 只靠 jobId 取結果、render 輸入可以塞很大的 base64、CORS 正式環境可能漏設、render 背景 thread 掛掉後 job 會卡住、ROI shadow 模型結果也可能被前端誤當正式答案。

處理原則是：本機 demo 不要被繁瑣設定卡住；正式部署要有明確防線。也就是環境變數沒設時仍可本機開發，但只要進 production，就不能靠「大家記得要設」這種口頭約定。

### S22 Ollama 建議服務缺 API key(已解決)

當時 README 架構圖寫的是前端對 Ollama 也會帶 `X-API-Key`，但實際 `Ollama_suggestion.py` 的 `/suggest` 和 `/suggest/stream` 沒有驗 key。這代表只要建議服務被部署到公開網址，任何人都能直接丟分析結果進來產生妝容文案。雖然 Ollama 不一定像 Replicate 那樣直接燒錢，但它仍會吃機器資源，也可能被拿來做任意 prompt 測試。

修法是在 suggestion 服務加入 `SUGGESTION_API_KEY`。兩個建議端點都掛 `Depends(require_api_key)`，未帶或帶錯 `X-API-Key` 就回 `401 FORBIDDEN`；`/health` 保持開放，並多回一個 `api_key_required`，部署後一眼能看出現在到底有沒有啟用保護。`docker-compose.yml` 也同步把 `SUGGESTION_API_KEY=${SUGGESTION_API_KEY:-}` 傳進容器，避免 compose 起來後環境變數漏掉。

前端要配合的地方很單純：呼叫 `/suggest` 或 `/suggest/stream` 時，跟 face/render 一樣帶 `X-API-Key`。正式環境必須設定 `SUGGESTION_API_KEY`，不然仍會是本機開發模式。

### S23 非同步 job 只靠 jobId 可讀結果(已解決)

face BASIC/PRO 和 render 都有非同步 job。原本流程是：建立 job 拿 `jobId`，之後用 `GET /jobs/{jobId}` 輪詢，完成後再取 result。問題是 jobId 本身等同讀取憑證，誰拿到 jobId 誰就能看結果。這對臉部分析尤其敏感，因為 result 裡有使用者臉部特徵、膚色與後續渲染資料。

這次改成建立 job 時同時產生 `resultToken = uuid.uuid4().hex`。建立 job 的回應會回 `jobId` 和 `resultToken`；之後輪詢或取結果時，必須帶 `X-Job-Token: <resultToken>`。為了方便測試，也支援 `?result_token=`，但正式前端應優先用 header，避免 token 出現在網址、瀏覽器歷史或分享連結裡。

為了避免部署瞬間把舊 job 全部弄壞，驗證邏輯採用相容策略：新 job 有 `resultToken` 就必須驗；舊 job 若資料裡沒有 token，就照舊放行。`backend_smoke_test.py` 也同步改成建立 job 後保存 token，再用 `X-Job-Token` 輪詢與取 result。

前端接法：
1. 建立 face/render job 後，把回應裡的 `resultToken` 跟 `jobId` 一起存在當前流程狀態。
2. 輪詢 job 狀態時加 header：`X-Job-Token: resultToken`。
3. 取 `/result` 或 render job 狀態時也帶同一個 header。
4. 不要把 `resultToken` 寫進 localStorage 長期保存；它只需要活在這次分析/渲染流程裡。

### S24 渲染輸入缺大小限制(已解決)

render API 的 `image` 是 base64 data URL，原本 `RenderRequest.image` 和當時的 `prompt` 沒有限制長度，底層 `replicate_render.py` 也直接 `base64.b64decode`。如果有人塞一個超大的 JSON body，服務會先吃記憶體，這種壓力不一定會被 Replicate rate limit 擋住，因為它在真正呼叫模型前就已經發生了。

現在 render API 入口會先做兩件事：
1. `image` 必須是 `data:image/...;base64,...`，格式錯回 `400 INVALID_IMAGE`。
2. data URL 長度超過 `MAX_RENDER_IMAGE_CHARS` 回 `413 IMAGE_TOO_LARGE`。

當時另有 `MAX_RENDER_PROMPT_CHARS`，但 S21 完成後公開 API 已不再接收客戶端 prompt，改驗證短小的白名單 `styleId`，因此該長度限制與環境變數一併移除。舊客戶端即使夾帶 prompt，也會在資料模型解析時被忽略。

底層也補了第二層防線：`data_url_to_bytes` 改用 `base64.b64decode(..., validate=True)`，並用 `MAX_RENDER_IMAGE_BYTES` 限制解碼後大小。也就是 API 入口擋一次，真正轉 bytes 時再擋一次，避免未來有別的程式直接呼叫底層函式時繞過限制。

### S25 正式環境 CORS 漏設會退回 `*`(已解決)

原本 `get_cors_origins()` 的預設是 `*`，這對本機測試很方便，但正式環境如果忘了設 `CORS_ORIGINS`，服務也會安靜地用 `*` 跑起來。這種錯最麻煩，因為不是程式壞掉，而是安全設定悄悄退回寬鬆模式。

現在加了兩個開關：`APP_ENV=production` 或 `REQUIRE_EXPLICIT_CORS=1`。只要其中一個成立，`CORS_ORIGINS` 就不能空，也不能是 `*`；否則服務啟動時直接丟 `RuntimeError`。本機沒設 production 時仍維持原本行為，避免大家開發時被 CORS 設定卡住。

正式部署建議：
```text
APP_ENV=production
CORS_ORIGINS=https://decorate-me.web.app
```

若臨時測試多個前端網址，用逗號分隔即可。

### S26 render 背景 job 卡住與資料累積(已解決)

render job 是用背景 thread 跑長任務。這能避開 Cloud Run request timeout，但也有一個現實問題：如果 instance 被回收、thread 被中斷、或模型呼叫卡住，Firestore 裡的 job 可能永遠停在 `queued` 或 `running`。原本沒有 timeout cleanup，久了 collection 也會一直累積。

現在 render 服務補了 `_cleanup_render_jobs()`，在 `/health` 和建立新 job 前都會跑：
1. `queued` / `running` 超過 `RENDER_JOB_TIMEOUT_SECONDS` 會被標成 `failed`，寫入錯誤訊息與 `finishedAt`。
2. `completed` / `failed` 超過 `RENDER_JOB_RETENTION_SECONDS` 會刪除。
3. collection 超過 `RENDER_JOB_MAX_COUNT` 時，會從最舊的 job 開始刪。

這不是最終型的任務系統；正式產品如果渲染量變大，應改 Cloud Tasks、Pub/Sub 或專門 worker。但在目前架構下，至少 job 不會無限卡住，也不會無限堆資料。

### S27 ROI shadow 模型欄位可能被前端誤用(已解決)

ROI CNN shadow 的定位是「暗跑模型、累積規則式 vs 模型的對照資料」，不是正式分類來源。它目前的 macro accuracy 還沒過門檻，如果把 `模型分類` 回給前端，未來前端有人看到欄位就可能拿來顯示，等於把實驗資料當正式結果。

現在處理方式改成：shadow 仍然會跑，仍然會寫 log 做對照，但預設不再把 `模型分類` 放進 API response。若內部驗收真的需要看模型輸出，可以設定 `ROI_SHADOW_EXPOSE_RESPONSE=1` 暫時打開。

這樣做的好處是正式 API 對前端保持乾淨，使用者只看到規則式穩定輸出；模型團隊仍然能從 log 裡累積真實流量上的一致率與差異案例。

### 這一輪驗證方式

- 語法檢查：`python -m compileall -q Face_analyzer_BASIC.py Face_analyzer_PRO.py Ollama_suggestion.py replicate_render_api.py replicate_render.py backend_smoke_test.py dev_server_utils.py basic_roi_shadow.py`
- 空白檢查：`git diff --check`
- 建議線上補測：
  - 未帶 `X-API-Key` 打 `/suggest` 應回 `401`。
  - 建立 face/render job 後，不帶 `X-Job-Token` 輪詢新 job 應回 `403`。
  - 超大 render data URL 應回 `413 IMAGE_TOO_LARGE`。
  - `APP_ENV=production` 且未設 `CORS_ORIGINS` 時，服務應拒絕啟動。
  - render job 手動改成過期 `running` 後，打 `/health` 應把它標成 `failed`。

## 2026-07-13 可自行補完項目（S28~S29）

前一輪後端已經開始要求新 job 帶 `resultToken`，但前端如果沒有跟上，結果會變成「後端保護是對的，使用者流程卻輪詢失敗」。所以這一輪把 repo 裡能找到的兩份前端都補上：`firebase-hosting-full` 和 `firebase-hosting-hotfix`。

### S28 前端尚未帶 `resultToken` 輪詢新 job(已解決)

完整流程現在是：
1. 前端呼叫 `createFaceJob()` 或 `createFaceProJob()`。
2. 後端回 `jobId` 與 `resultToken`。
3. 前端把 `resultToken` 寫進 `analysisPackage.async.resultToken`，只作為這次分析流程的臨時憑證。
4. `Api.waitForFaceJob(...)` 呼叫 `getFaceJob(...)` 與 `getFaceJobResult(...)` 時，把 token 放進 `X-Job-Token` header。

`firebase-hosting-full/public/js/api.js` 已新增 `_faceJobHeaders(service, resultToken)`，並讓 `getFaceJob`、`getFaceJobResult`、`waitForFaceJob` 都接受 token。`router.js` 建立 job 後會保存 `resultToken`，輪詢時也會傳回去。

`firebase-hosting-hotfix` 也同步補齊。hotfix 原本比 full 舊，連 face/text/render 的 `X-API-Key` header 都不完整；這次順手補上 `faceApiKey`、`textSuggestionApiKey`、`renderApiKey` 的 header 組裝，避免日後拿 hotfix 部署時又把後端保護繞掉。

前端仍要注意：`resultToken` 不要長期放 localStorage，不要放分享網址。它是一次分析/渲染流程用的 bearer token，存在當前頁面的 draft 狀態已經足夠。

### S29 job_store cleanup/stat 單次掃太多 Firestore 文件(已解決)

原本 `job_store.all_jobs()` 直接 `collection.stream()`，資料量小時沒感覺，但 job collection 如果長期累積，health、cleanup、stats 都可能變慢，甚至讓健康檢查本身變成負擔。

這次先做低風險收斂：`all_jobs(col, limit=None)` 新增掃描上限，預設讀 `JOB_STORE_SCAN_LIMIT=500`。也就是目前仍維持原本資料結構與 cleanup 寫法，不要求馬上建立 Firestore 索引或改多段 query，但至少不會無上限掃整個 collection。

這不是最終最佳化。正式流量上來後，下一步應該把 face/render job 的 cleanup 改成依 `status`、`createdAt`、`finishedAt` 做 Firestore query，並建立對應索引；目前這版先以「不大改、不破壞現有流程」為優先。

### S30 後端與前端修正同步上線(已解決)

只改 repo 不夠，因為這個專案前端與 Cloud Run 都是直接對外服務。這輪已把能部署的部分一起推上線：

1. Cloud Run 環境變數已補：
   - `face-basic` / `face-pro`：`FACE_API_KEY`、`APP_ENV=production`、`CORS_ORIGINS=https://decorate-me.web.app`、`JOB_STORE_SCAN_LIMIT=500`。
   - `replicate-render`：`APP_ENV=production`、`CORS_ORIGINS=https://decorate-me.web.app`、`JOB_STORE_SCAN_LIMIT=500`、`RENDER_JOB_TIMEOUT_SECONDS=600`、`RENDER_JOB_RETENTION_SECONDS=3600`、`RENDER_JOB_MAX_COUNT=200`。
2. Cloud Run 映像已重建並部署：
   - `face-basic`：`backend:codex-20260713-185013`
   - `face-pro`：`backend:codex-20260713-185013`
   - `replicate-render`：`replicate-render:codex-20260713-185013`
3. Firebase Hosting 已部署 `firebase-hosting-full`，線上 `https://decorate-me.web.app/js/api.js` 已確認包含 `X-Job-Token` 與 `_faceJobHeaders`。
4. Firebase Hosting ignore 已補上 `*.md`、`*.log`、`*.py`、`docs/**`、`frontend_smoke_check.js`，部署結果顯示只上傳 39 個網站需要的檔案；線上 `/README.md` 回 404，避免內部文件再被公開。

線上驗證：
- `https://face-basic-258021445391.asia-east1.run.app/health` 回 `service=face-analyzer-basic`。
- `https://face-pro-258021445391.asia-east1.run.app/health` 回 `service=face-analyzer-pro`。
- `https://replicate-render-258021445391.asia-east1.run.app/health` 回 `api_key_required=true`，且有 render job timeout/retention/max_count。
- 未帶 API key 打 face job endpoint 回 401。
- 線上前端 `api.js` 已含 `X-Job-Token`。

---

## 2026-07-13 S21 前端 render prompt 收斂

### 為什麼要拿掉

前端可編輯 prompt 在模型測試期很方便，但不適合正式流程。使用者可以在瀏覽器開發者工具改 request，送入任意文字；同時完整的身分鎖定、妝容策略也會直接出現在頁面與網路請求中。只移除 textarea 不夠，因為舊程式仍會在 JavaScript 裡組 prompt，再以 JSON 傳給 `/render`。

### 現在的資料流

1. 使用者在前端七種妝容風格中選擇一種。
2. 前端送出 `image`、`styleId` 與相容用的 `strength`，不再包含 `prompt`。
3. render 後端以 `RENDER_STYLE_PROMPTS` 驗證 `styleId`。未知值回 `422 INVALID_RENDER_STYLE`，不會拿使用者文字拼接 prompt。
4. 後端把白名單風格描述放進固定的真人感與身分保留模板，再呼叫 Replicate。
5. 同圖去重也改用後端生成後的 prompt 計算，客戶端無法靠修改文字製造不同請求。

為了讓後端先部署、前端後部署的短暫交接期不中斷，`styleId` 暫時以 `natural` 為預設值。Pydantic 會忽略舊版送來的多餘 `prompt` 欄位，實際渲染仍只使用後端模板；因此相容舊請求不等於繼續信任舊 prompt。

### 前端清理

- `firebase-hosting-full` 與 `firebase-hosting-hotfix` 的 `Api.renderMakeup()` 都改成只送 `styleId`。
- 兩份 `router.js` 不再讀取 `renderPromptEn` 或呼叫 `buildRenderPrompt()`，選定風格會保存到 `analysisPackage.render.styleId`。
- compare 頁面的英文 prompt 預覽已移除。
- 舊分析草稿即使還留有 `renderPromptEn`，送出渲染時也不再使用。

### 驗證標準

- 前端程式內不存在 `buildRenderPrompt()`，render request body 不含 `prompt`。
- 後端 `RenderRequest` 不定義 prompt，手動夾帶該欄位也不影響產生的後端指令。
- 七種正式風格與 `natural` fallback 都能產生固定 prompt；任意 styleId 必須回 `422`。
- `/health` 回傳 `render_prompt_policy.client_prompt_accepted=false`，部署後可直接確認政策已生效。

### 部署與實測結果

- Cloud Build 映像：`replicate-render:codex-20260713-192323`。
- Cloud Run revision：`replicate-render-00035-dwv`，已承接 100% 流量。
- Firebase Hosting 已重新部署，線上 `api.js` 確認 request body 使用 `styleId`，不存在 `buildRenderPrompt()` 與舊的 prompt body。
- 線上 compare 頁仍保留「生成妝容」按鈕，但已無英文渲染指令標題與 `comparePromptPreview`。
- 帶正確 API key、任意 prompt 與非法 `styleId` 實測回 `422 INVALID_RENDER_STYLE`，沒有呼叫模型。
- 瀏覽器以訪客模式進入網站與 `#compare` 路由，頁面正常載入，console error 為 0。

---

## 2026-07-13 S31 比較頁右側恢復 Ollama 妝容建議

### 使用者看到的問題

進入「妝容對比圖」頁面後，左側照片和右側的目前風格、風格標籤、操作按鈕都還在，但右側原本用來顯示 Ollama 分析文字的位置變成大片空白。這不是 Ollama 沒有回應，也不是分析結果在換頁時被清掉，而是 S21 移除前端可編輯 render prompt 時，compare 頁的文字顯示區一起被拿掉了。

這次修正的目標是恢復使用者需要閱讀的 Ollama 中文妝容建議，但不把已移除的「前端可編輯渲染指令」加回來。兩者用途不同，不能混在一起：

- **Ollama 妝容建議**：分析服務回傳給使用者閱讀的中文內容，例如整體方向、底妝、眼妝、修容、唇妝和注意事項。這次恢復的是這一份。
- **render prompt**：送給圖片模型的內部渲染指令。S21 已改為後端依 `styleId` 從白名單產生，前端不能查看、編輯或送入任意 prompt。這次沒有把它恢復。

### 先確認資料到底在哪裡

排查順序如下：

1. `Ollama_suggestion.py` 的 `/suggest` 仍會回傳 `suggestion`。
2. 前端完成分析後，仍會把文字放在 `Router.analysisPackage.generativeText.suggestion`。
3. 使用者從分析頁走到選擇風格，再進入 compare 頁時，`analysisPackage` 還在，所以文字沒有遺失。
4. 檢查 `firebase-hosting-full/public/pages/compare.html` 和 `router.js` 的 compare 備援模板，兩者都沒有顯示該欄位的 HTML。
5. 因此根因確定是「畫面漏接現有資料」，不需要修改 Ollama API，也不需要重新呼叫一次 Ollama。

這個判斷很重要。如果誤以為後端沒回應，可能會在 compare 頁再呼叫一次模型，增加等待時間和費用，還可能讓同一次分析得到兩份不同文字。現在的做法只是顯示已完成分析時保存的結果，不增加 API 呼叫。

### 實際修改步驟

#### 第 1 步：把唯讀顯示區加回 compare 頁

修改 `firebase-hosting-full/public/pages/compare.html`，在「目前風格」和操作按鈕中間加入：

- `compareOllamaPanel`：整個 Ollama 建議區塊。
- `compareOllamaSuggestion`：實際放中文建議的文字節點。
- `aria-live="polite"`：內容更新時，螢幕閱讀器可以用不打斷使用者的方式讀出更新。

這個專案的 router 會先載入 `pages/compare.html`，載入失敗時才使用 `router.js` 內的 compare 備援模板。因此兩個地方都要同步修改。只改 HTML，頁面片段載入失敗時功能會消失；只改 router 備援模板，正常情況反而不會看到新區塊。

`firebase-hosting-hotfix/public/js/router.js` 的備援模板也同步更新，避免日後改用 hotfix 部署時又退回沒有 Ollama 文字的舊畫面。

#### 第 2 步：從既有分析結果取回文字

在 `PageInit.compare()` 內依序處理：

1. 讀取 `Router.analysisPackage?.generativeText?.suggestion`。
2. 完整版前端若可使用 `splitOllamaTwoPartSuggestion()`，先用它移除舊資料中可能殘留的第二段英文 render prompt，只留下中文建議。
3. hotfix 若沒有這個 helper，則退回一般字串清理，確保舊版本不會因函式不存在而整頁報錯。
4. 有內容時，把文字寫入 `compareOllamaSuggestion`。
5. 沒內容時顯示「尚未產生妝容建議」，而不是留下無法判斷狀態的空白。
6. 空資料時替區塊加上 `is-empty`，讓提示文字使用較淡的顏色。

寫入方式使用 `textContent`，沒有使用 `innerHTML`。因此即使模型文字裡出現 `<script>` 或其他 HTML 標籤，瀏覽器也只會把它當普通文字顯示，不會執行，避免模型輸出成為 XSS 注入入口。

#### 第 3 步：補上長文字與手機版排版

`firebase-hosting-full/public/css/main.css` 和 `firebase-hosting-hotfix/public/css/main.css` 都加入相同的 `.compare-ollama` 樣式：

- 以細的上下分隔線和目前風格、按鈕區分開，不再額外套一張卡片。
- 保留 Ollama 原始換行，六個建議段落不會全部黏成一行。
- 使用 `overflow-wrap:anywhere`，遇到很長且沒有空格的內容也不會撐破右欄。
- 桌機文字區最高 `300px`，手機最高 `240px`；內容更長時只在文字區內垂直捲動，不會讓操作按鈕被推到非常遠。
- 手機版仍維持單欄排列，沒有水平捲軸。

這兩份 CSS 過去受到根目錄 `.gitignore` 的全域忽略規則影響，雖然本機和 Firebase 部署時有使用，卻沒有進 Git。這次會用 Git 強制加入兩份 `main.css`，確保其他人重新 clone GitHub 專案時，也能取得 Ollama 區塊的完整樣式。

#### 第 4 步：修正 Node smoke test 的瀏覽器模擬相容性

執行 `frontend_smoke_check.js` 時，測試環境會建立一個簡化的 `window`，但沒有 `window.location`。`getAiGatewayUrl()` 原本只判斷 `window` 是否存在，測試因此在讀取 `window.location.hostname` 時中止。

完整版與 hotfix 的 `api.js` 都改成先判斷：

```javascript
if (typeof window === 'undefined' || !window.location) return '';
```

這只影響 Node 測試環境的防呆。真正瀏覽器一定有 `window.location`，正式站判斷 production host 和選擇 AI Gateway 的行為沒有改變。

### 新手驗收教學

#### 方法 A：直接驗收正式網站

1. 開啟 `https://decorate-me.web.app`。
2. 登入或使用網站提供的訪客流程。
3. 上傳一張本人有權使用的正面照片。
4. 等待五官分析與 Ollama 妝容建議完成。
5. 進入風格選擇頁，選一個風格，例如 `Soft Baddie`。
6. 進入「妝容對比圖」頁。
7. 看右側「目前風格」下方，應看到 `OLLAMA RESPONSE` 和「Ollama 妝容建議」。
8. 文字應是剛才分析得到的中文建議，並保留段落換行。
9. 文字很多時，在文字區塊內向下捲動，應能看完內容。
10. 頁面上不應出現可編輯 prompt 的文字框，也不應出現舊的英文 render prompt 預覽。
11. 按「生成妝容」後，渲染流程仍只使用已選擇的 `styleId`，Ollama 顯示文字不會被當成 render request 送出。

如果右側顯示「尚未產生妝容建議」，代表這次流程的 `analysisPackage.generativeText.suggestion` 沒有內容。此時應回到分析步驟檢查 Ollama 建議是否成功，而不是在 compare 頁手動填 prompt。

#### 方法 B：開發者在本機重跑檢查

在專案根目錄開 PowerShell：

```powershell
cd C:\Users\isach\PycharmProjects\PythonProject12\firebase-hosting-full\public
node --check js\router.js
node --check js\api.js
node frontend_smoke_check.js
```

預期結果：

- 兩個 `node --check` 沒有輸出錯誤，並以 exit code 0 結束。
- smoke test 顯示 `frontend smoke check passed`。

若直接雙擊 HTML，有些 router 的 fetch 行為會受 `file://` 限制。可在 `public` 目錄啟動本機網站：

```powershell
C:\Users\isach\PycharmProjects\PythonProject12\.venv\Scripts\python.exe -m http.server 4173
```

再用瀏覽器開啟 `http://127.0.0.1:4173/`。若 4173 已被其他程式使用，可換成 4174，瀏覽器網址也要一起改。

### 本次驗證結果

- 完整版 `router.js`、`api.js` 語法檢查通過。
- hotfix `router.js`、`api.js` 語法檢查通過。
- `frontend_smoke_check.js` 通過，輸出 `frontend smoke check passed`。
- `git diff --check` 通過，沒有空白格式錯誤。
- 桌機以 `1280 x 720` 檢查，右欄寬度約 343px，文字區塊與按鈕沒有重疊，頁面沒有水平溢出。
- 手機以 `390px` 寬度檢查，compare 版面正常改為單欄，頁面寬度與內容寬度一致，沒有水平捲軸。
- Firebase Hosting 已部署成功，正式網址為 `https://decorate-me.web.app`。
- 從正式網址重新抓取 `pages/compare.html`、`js/router.js`、`css/main.css` 驗證：三者都包含新的 Ollama 顯示功能；compare 頁沒有 textarea，也沒有 `comparePromptPreview`。

### 本次修正沒有改動的範圍

- 沒有修改 Ollama system prompt 或輸出格式。
- 沒有增加一次新的 Ollama API 呼叫。
- 沒有把前端可編輯 render prompt 加回來。
- 沒有改變 render 後端的 `styleId` 白名單政策。
- 沒有把 Ollama 中文建議送給 Replicate。
- 沒有修改真人照片的渲染品質參數。

---

## 2026-07-15 「分析失敗：job token 不正確或未提供」——S28 修正被部署蓋掉的迴歸(已解決)

### 使用者看到的問題

在正式網站上傳照片按下分析後，跳出「分析失敗：job token 不正確或未提供」，分析流程中斷、草稿保留在失敗狀態。

### 排查過程

1. 錯誤前綴「分析失敗：」來自前端 `router.js` 的 catch；「job token 不正確或未提供」這句在前端程式碼裡找不到，確定是後端回的。
2. 在後端 `Face_analyzer_BASIC.py` / `Face_analyzer_PRO.py` 找到 `_verify_job_token()`：S23 之後，建立 job 會發 `resultToken`，之後 `GET /v1/face/jobs/{id}` 與 `/result` 必須帶 `X-Job-Token`，否則回 `403` 與這句訊息。
3. 檢查實際部署中的前端（OneDrive `web_frontend`）：`js/api.js` 的 `getFaceJob` / `getFaceJobResult` 輪詢時只帶 `X-API-Key`，完全沒有 `X-Job-Token`——建立 job 回應裡的 `resultToken` 被直接丟掉。
4. 對線上 face-basic 實測重現：建 job 拿到 `resultToken` 後，不帶 token 輪詢回 `{"detail":{"error":{"message":"job token 不正確或未提供"}}}`；帶 `X-Job-Token` 輪詢回 200 且 job 正常完成。與使用者症狀完全吻合。

### 根因：兩份前端分家，部署來源切換時把修正蓋掉

S28（2026-07-13）確實已把 token 傳遞修好並部署——**但修的是 repo 裡的 `firebase-hosting-full/public` 這份副本**（Codex 那輪連同 AI Gateway 架構一起做的）。實際日常維護與部署的前端是 **OneDrive `web_frontend`** 這份，它從未合入 S28 的修正。後來改回從 OneDrive 部署 Firebase Hosting，線上 `api.js` 就退回「不帶 token」的舊版；而後端的 token 檢查仍在，於是每一次新分析 job 的輪詢都被 403 擋下。

教訓：**同一個站有兩份前端原始碼（OneDrive `web_frontend` 與 repo `firebase-hosting-full`），誰後部署誰生效**。後端加防線時，必須確認「目前實際部署來源」那份前端有跟上，不能只看 repo 裡的副本已修。

### 修正內容（OneDrive `web_frontend`，最小移植）

只移植 token 傳遞，不動 `firebase-hosting-full` 裡的 AI Gateway／`beautyAiAccessToken` 那套架構：

- `js/api.js`：新增 `_faceJobHeaders(service, resultToken)`（在 `X-API-Key` 之外加上 `X-Job-Token`）；`getFaceJob`、`getFaceJobResult`、`waitForFaceJob` 都改為接受並傳遞 `resultToken`。
- `js/router.js`：建立 job 後改呼叫 `Api.waitForFaceJob(mode, job.jobId, job.resultToken, onProgress)`，把建立回應中的 `resultToken` 帶進整段輪詢。
- 後端 CORS 為 `allow_headers=["*"]`，`X-Job-Token` 不需額外放行。

### 驗證

- 部署前先用 curl 對線上 face-basic 完整走過：`POST /v1/face/jobs/basic`（帶 `X-API-Key`）→ 拿到 `jobId` + `resultToken` → 不帶 token 輪詢 `403`、帶 `X-Job-Token` 輪詢 `200`，job `queued → completed`。
- `firebase deploy --only hosting` 後，curl 線上 `js/api.js` 與 `js/router.js`（加時間戳參數避開快取）確認都含 `X-Job-Token` / `_faceJobHeaders` / `job.resultToken`。
- 同日背景：資料庫組員的 Cloudflare Tunnel 網址更換，`config.local.js` 的 `memberDatabaseUrl`/`productUrl` 已一併更新為 `https://ser-agree-tyler-diagnostic.trycloudflare.com` 並隨同部署（與本錯誤無關，僅記錄同批上線內容）。

---

## 2026-07-15～2026-07-16 「無法連線到會員資料庫，請稍後再試」——S32 AI Gateway 的會員服務網址失效(已解決)

### 使用者看到的問題

在登入畫面送出帳號密碼後，跳出「無法連線到會員資料庫，請稍後再試」，因此無法完成登入。這不是帳號或密碼輸入錯誤；前端收到的是會員驗證服務暫時不可用。

### 後端邏輯與線上實測

1. `ai_gateway.py` 的 `POST /auth/login` 會把登入資料轉送到環境變數 `MEMBER_DATABASE_URL` 所指向的 `/api/login`。
2. 若會員服務連線失敗，Gateway 會捕捉 `httpx.HTTPError`，回傳 HTTP `503` 與 `MEMBER_SERVICE_UNAVAILABLE`；前端再把它顯示成「無法連線到會員資料庫，請稍後再試」。因此畫面訊息符合目前後端的錯誤映射。
3. 正式環境 AI Gateway 的 `/health` 實測回 HTTP `200`，Cloud Run service 與 revision 皆為 Ready，表示 Gateway 本身有正常啟動。
4. 但 Cloud Run 上的 `MEMBER_DATABASE_URL` 仍指向舊的 quick tunnel：`https://component-developmental-disclaimer-count.trycloudflare.com`。該主機目前 DNS 無法解析，根網址與 `/api/login` 都連不上。
5. 直接呼叫正式 AI Gateway 的 `/auth/login` 可穩定重現 HTTP `503`，錯誤代碼為 `MEMBER_SERVICE_UNAVAILABLE`。因此根因不是前端裝置或一般使用者網路，而是 Gateway 到會員資料庫之間的上游連線已斷。

### 根因

會員資料庫使用 Cloudflare quick tunnel，Tunnel 重啟後網址會更換，舊網址也可能直接失效。前端同日已更新到新的會員資料庫網址，但 `ai-gateway` Cloud Run 的 `MEMBER_DATABASE_URL` 沒有同步更新，造成兩邊部署設定不一致：前端知道新網址，Gateway 還在呼叫已失效的舊網址。

本機 `.env` 目前也沒有 `MEMBER_DATABASE_URL` 與 `GATEWAY_SESSION_SECRET`。若直接在本機啟動 AI Gateway，登入會因驗證設定不完整而回 `AUTH_NOT_CONFIGURED`；這與正式環境的舊 Tunnel 失效是兩個不同設定問題。

### 修正與驗證

1. 已確認目前會員資料庫 Tunnel 為 `https://debut-exemption-lounge-depending.trycloudflare.com`，`/api/products` 回 HTTP `200`，正式站 Origin 也取得正確 CORS。
2. 已更新 Cloud Run `ai-gateway` 的 `MEMBER_DATABASE_URL`，產生 revision `ai-gateway-00007-2cd` 並切換 100% 流量。
3. 正式 AI Gateway `/health` 回 HTTP `200`；以無效測試帳密呼叫 `/auth/login` 回 HTTP `401`，不再回 `503 MEMBER_SERVICE_UNAVAILABLE`。
4. 本機開發環境仍不寫入密鑰；`GATEWAY_SESSION_SECRET` 維持由 Secret Manager／Cloud Run secret 設定。
5. 長期仍應把會員資料庫部署到固定網域或穩定託管服務；quick tunnel 更換時，前端與 AI Gateway 必須同步更新。

### 狀態

截至 2026-07-16，問題已修復並完成線上驗證，改列「已解決」。

---

## 2026-07-16 商品頁一直「商品載入中」——S33 Firebase 原網址快取漏設(已解決)

### 使用者看到的問題

使用者開正式站原網址 `https://decorate-me.web.app/#products` 時，商品推薦頁一直停在「商品載入中」，admin 商品管理也曾只顯示部分資料。畫面看起來像商品資料庫或 Cloudflare Tunnel 壞掉，但直接開新 Tunnel 的 `/api/products` 可以看到商品資料。

同日還出現登入彈窗「無法連線到會員資料庫，請稍後再試」。這個訊息容易被誤判成資料庫掛掉，但後續實測發現會員／商品 API 本身已有回應，前端拿到的設定才是關鍵。

### 線上實測與排除

1. 舊前端設定仍指向失效的 quick tunnel：`https://component-developmental-disclaimer-count.trycloudflare.com`，公共 DNS 查詢回 `Non-existent domain`。
2. 使用者提供新的有效 Tunnel：`https://stainless-exit-spot-providing.trycloudflare.com/`。
3. 實測新 Tunnel：
   - `/api/products` 回 `200 OK`，商品資料可讀，當時共回 575 筆。
   - `/api/members` 未登入時回 `401 AUTH_REQUIRED`，屬正常保護。
   - 帶 `Origin: https://decorate-me.web.app` 測 `/api/products`，回應有 `Access-Control-Allow-Origin: https://decorate-me.web.app` 與 `Access-Control-Allow-Credentials: true`，表示 CORS 已放行正式站。
4. 已把 `firebase-hosting-full/public/config.local.js` 的 `memberDatabaseUrl` 與 `productUrl` 更新成新 Tunnel，並部署 Firebase Hosting。
5. 但使用者開原網址仍看到「商品載入中」。進一步用 curl 檢查正式站 header 後發現：
   - `https://decorate-me.web.app/config.local.js` 已更新且可 no-cache。
   - `https://decorate-me.web.app/` 與 `https://decorate-me.web.app/#products` 仍回 `Cache-Control: max-age=3600`。

### 根因

Firebase Hosting 的 header 規則只對 `/index.html` 設了 `Cache-Control: no-cache, no-store, must-revalidate`，但使用者平常開的是 `/` 或 `/#products`。瀏覽器與 Firebase CDN 會把 `/` 視為另一個路徑，因此仍套用預設 `max-age=3600`，造成原網址可能持續吃舊版 HTML 與舊版 script query 版本。

也就是說：資料庫與商品 API 已經恢復，前端也已部署新設定，但「正式站原網址的 HTML 殼」仍可能被快取 1 小時，導致使用者端還載入舊 config 或舊 JS 狀態。

### 修正內容

- `firebase-hosting-full/public/config.local.js`
  - `memberDatabaseUrl` 更新為 `https://stainless-exit-spot-providing.trycloudflare.com`
  - `productUrl` 更新為 `https://stainless-exit-spot-providing.trycloudflare.com`
- `firebase-hosting-full/public/js/router.js`
  - 移除 admin 商品表格的 `ADMIN_PRODUCT_ROWS_LIMIT = 30`
  - 後台商品表格改為渲染 `products.map(...)`，不再只顯示前 30 筆。
- `firebase-hosting-full/public/index.html`
  - script query 版本更新為 `v=20260716-0237`，強制瀏覽器重新抓 `config.local.js`、`api.js`、`router.js`。
- `firebase-hosting-full/firebase.json`
  - 新增 `/config.local.js` no-cache header。
  - 追加 `/` no-cache header，修正原網址 `/` 與 `/#products` 仍被快取的漏網問題。

### 驗證

- 部署後 curl 驗證：
  - `https://decorate-me.web.app/` 回 `Cache-Control: no-cache, no-store, must-revalidate`
  - `https://decorate-me.web.app/#products` 回 `Cache-Control: no-cache, no-store, must-revalidate`
  - `https://decorate-me.web.app/config.local.js?v=20260716-0237` 指向新 Tunnel。
  - 新 Tunnel 的 `/api/products` 帶正式站 Origin 回 `200 OK`。
- 用瀏覽器實測正式站商品頁：
  - `https://decorate-me.web.app/#products` 可顯示 `575 件商品`。
  - 商品卡可正常渲染，不再停在「商品載入中」。

### 教訓

Firebase Hosting 的 SPA 首頁快取不能只設 `/index.html`。如果使用者實際入口是 `/`、`/#products` 或其他 rewrite 到首頁的路徑，也要確認這些入口回應的 `Cache-Control`。否則即使 JS 與 config 已更新，使用者仍可能因 HTML 殼快取而載入舊資源。

### 新手版：為什麼「我明明部署了，畫面還是舊的？」

把網站想成四層檔案：

1. **HTML 殼**：`index.html`
   - 它像一本書的目錄，負責告訴瀏覽器要載入哪個 CSS、哪個 JS、哪個 config。
   - 使用者開 `https://decorate-me.web.app/` 或 `https://decorate-me.web.app/#products` 時，實際拿到的就是首頁 HTML。
   - 這次一開始只對 `/index.html` 設 no-cache，但使用者開的是 `/`，所以 `/` 還是舊快取。

2. **設定檔**：`config.local.js`
   - 它告訴前端目前會員資料庫、商品資料庫、渲染服務、文字建議服務各自在哪個網址。
   - Cloudflare quick tunnel 每次重開都可能換網址，所以這個檔案最常需要更新。
   - 如果它被快取，前端就會繼續打舊 tunnel，看起來就像「資料庫掛了」。

3. **程式邏輯**：`js/api.js`、`js/router.js`、`js/data.js`
   - `api.js` 負責打 API。
   - `router.js` 負責換頁、渲染商品、admin 表格、登入後畫面。
   - 例如這次 admin 商品只顯示 30 筆，就是 `router.js` 裡有 `ADMIN_PRODUCT_ROWS_LIMIT = 30`。

4. **樣式**：`css/main.css`
   - 它控制畫面長相、顏色、排版。
   - 如果 JS/config 都是新的，但 CSS 還被快取，功能可能已修好，畫面卻還像舊版。
   - 這次後續又發現 `main.css` 還是 `max-age=3600`，所以管理中台看起來仍像舊版。

所以「網站還是舊的」不一定只有一種舊。可能是：

- HTML 舊：還在引用舊版 `?v=...`
- config 舊：還在打舊 Cloudflare tunnel
- JS 舊：功能邏輯沒更新
- CSS 舊：畫面樣式沒更新
- 瀏覽器分頁舊：使用者分頁停在舊 runtime 狀態，重新整理才會重跑

### 這類問題的典型症狀

- 已部署，但使用者畫面仍長得像舊版。
- 商品頁一直「商品載入中」，但直接打 `/api/products` 有資料。
- admin 顯示數字或資料不對，但 API 回傳看起來正常。
- 登入或商品錯誤訊息像是資料庫掛掉，但 curl 測 API 是通的。
- Codex／另一台瀏覽器看起來正常，使用者自己的 Chrome 不正常。
- 加上 `?v=xxxx` 後正常，不加就不正常。

這些都優先懷疑「快取或載入到舊檔」，不要第一時間就判定後端壞掉。

### 新手排查流程：從外到內查

1. 先確認 API 本身是否活著：

```powershell
curl.exe -I https://目前的-tunnel.trycloudflare.com/api/products
curl.exe -i https://目前的-tunnel.trycloudflare.com/api/products
```

如果 `/api/products` 回 `200 OK` 且有 JSON 商品資料，代表商品資料庫不是完全掛掉。

2. 確認 CORS 是否允許正式站：

```powershell
curl.exe -I -H "Origin: https://decorate-me.web.app" https://目前的-tunnel.trycloudflare.com/api/products
```

要看到類似：

```text
Access-Control-Allow-Origin: https://decorate-me.web.app
Access-Control-Allow-Credentials: true
```

如果沒有，代表前端瀏覽器可能會被 CORS 擋。

3. 確認正式站 HTML 是否新：

```powershell
curl.exe -I https://decorate-me.web.app/
```

正確應該要看到：

```text
Cache-Control: no-cache, no-store, must-revalidate
```

如果看到：

```text
Cache-Control: max-age=3600
```

代表首頁殼可能被快取，使用者會繼續拿舊 HTML。

4. 確認 HTML 引用的版本號：

```powershell
(Invoke-WebRequest -Uri "https://decorate-me.web.app/" -UseBasicParsing).Content |
  Select-String -Pattern "main.css|config.local|api.js|router.js"
```

要確認 CSS / JS / config 都是同一批新版本，例如：

```html
css/main.css?v=20260716-0237
config.local.js?v=20260716-0237
js/api.js?v=20260716-0237
js/router.js?v=20260716-0237
```

如果 CSS 還是舊的 `v=20260626...`，畫面就可能還像舊版。

5. 確認 config 是否指向新 Tunnel：

```powershell
(Invoke-WebRequest -Uri "https://decorate-me.web.app/config.local.js?v=目前版本" -UseBasicParsing).Content |
  Select-String -Pattern "memberDatabaseUrl|productUrl"
```

要確認它不是舊的 `component-developmental-disclaimer-count.trycloudflare.com`，而是目前有效 tunnel。

6. 確認 CSS 是否也 no-cache：

```powershell
curl.exe -I "https://decorate-me.web.app/css/main.css?v=目前版本"
```

正確也應該回：

```text
Cache-Control: no-cache, no-store, must-revalidate
```

### Firebase Hosting 的標準修法

`firebase-hosting-full/firebase.json` 至少要讓這些入口／核心檔案不快取：

```json
{
  "source": "/",
  "headers": [
    {
      "key": "Cache-Control",
      "value": "no-cache, no-store, must-revalidate"
    }
  ]
}
```

```json
{
  "source": "/index.html",
  "headers": [
    {
      "key": "Cache-Control",
      "value": "no-cache, no-store, must-revalidate"
    }
  ]
}
```

```json
{
  "source": "/config.local.js",
  "headers": [
    {
      "key": "Cache-Control",
      "value": "no-cache, no-store, must-revalidate"
    }
  ]
}
```

```json
{
  "source": "/js/api.js",
  "headers": [
    {
      "key": "Cache-Control",
      "value": "no-cache, no-store, must-revalidate"
    }
  ]
}
```

```json
{
  "source": "/js/router.js",
  "headers": [
    {
      "key": "Cache-Control",
      "value": "no-cache, no-store, must-revalidate"
    }
  ]
}
```

```json
{
  "source": "/css/main.css",
  "headers": [
    {
      "key": "Cache-Control",
      "value": "no-cache, no-store, must-revalidate"
    }
  ]
}
```

注意：`/index.html` 和 `/` 要分開確認。不能只設 `/index.html` 就以為首頁原網址也一定不快取。

### 每次部署前後固定檢查清單

部署前：

- `config.local.js` 是否已換成最新 Cloudflare tunnel。
- `index.html` 裡 `main.css`、`config.local.js`、`api.js`、`router.js` 的 `?v=` 是否一起更新。
- `firebase.json` 是否包含 `/`、`/index.html`、`/config.local.js`、`/css/main.css`、`/js/api.js`、`/js/router.js` 的 no-cache header。
- 若改的是畫面樣式，記得 CSS 也要 bump 版本，不能只 bump JS。

部署後：

```powershell
firebase deploy --only hosting --project decorate-me
```

接著檢查：

```powershell
curl.exe -I https://decorate-me.web.app/
curl.exe -I https://decorate-me.web.app/config.local.js?v=目前版本
curl.exe -I https://decorate-me.web.app/css/main.css?v=目前版本
```

三個都應該看到：

```text
Cache-Control: no-cache, no-store, must-revalidate
```

再檢查首頁引用：

```powershell
(Invoke-WebRequest -Uri "https://decorate-me.web.app/" -UseBasicParsing).Content |
  Select-String -Pattern "main.css|config.local|api.js|router.js"
```

最後用瀏覽器確認：

- 商品頁顯示商品數，不停在「商品載入中」。
- admin 商品表格顯示完整資料，不再只有 30 筆。
- 管理中台樣式不是舊版。

### 以後看到「還是舊的」時的判斷表

| 現象 | 最可能原因 | 先查什麼 |
|---|---|---|
| 商品一直載入中 | config 舊、API 被 CORS 擋、JS 沒重跑 | `config.local.js` 內容、`/api/products`、瀏覽器 Network |
| 直接 API 有資料，但網站沒有 | 前端拿到舊 config 或 CORS 問題 | `Access-Control-Allow-Origin`、正式站載入的 config |
| 功能修了但畫面長得舊 | CSS 被快取 | `main.css?v=...` 與 `/css/main.css` header |
| Codex 測正常，使用者 Chrome 不正常 | 使用者瀏覽器還有舊快取或舊分頁 runtime | 原網址 header、`Ctrl+R`、Network 實際載入檔案 |
| 加 `?v=...` 才正常 | 原網址 HTML 被快取 | `/` 的 `Cache-Control` |
| `/index.html` 正常但 `/` 不正常 | Firebase header 只設 index 沒設 `/` | `curl -I https://decorate-me.web.app/` |

### 這次的最終結論

這次不是單一錯誤，而是連續兩層快取問題：

1. 第一層：`/` 沒有 no-cache，導致原網址可能吃舊 HTML。
2. 第二層：`/css/main.css` 沒有 no-cache，且 `index.html` 仍引用舊 CSS query，導致畫面看起來還像舊版。

所以以後只要改前端，不能只問「有沒有 deploy」。要問：

- 使用者實際打開的入口是不是 no-cache？
- HTML 裡引用的 CSS/JS/config 版本是不是新的？
- 這些 CSS/JS/config 自己的 header 是不是 no-cache？
- 瀏覽器 Network 裡實際抓到的是哪一個檔案？

---

## 處理原則
- 金鑰一律走環境變數，不寫進程式；`config.local.js` / `.env` 不進版控。
- 服務間串接一律帶 `X-API-Key`；跨站認證用 session cookie（`SameSite=None; Secure; HttpOnly`，加 `Expires`）。
- 對外錯誤訊息不洩漏內部細節；使用者驗證訊息可對使用者友善。
- 已知但暫不修的（S6 公開圖、S18 公開服務）明確記錄取捨，能對外說明理由。

## 版本控制／Issue／部署硬性流程（2026-07-16 起）

從 2026-07-16 起，之後所有功能修改、bug 修正、設定調整、部署修補，都必須進入 GitHub 版本控制與 issue 流程，不能只停留在本機檔案或口頭紀錄。

### 每次改動都要做的事

1. **先建立或指定 GitHub issue**
   - 每個改動都要有對應 issue。
   - issue 要寫清楚：問題現象、影響範圍、預期結果、驗收方式。
   - 如果是臨時救火，也要事後補 issue，不可省略。

2. **在 Git 裡保留完整版本紀錄**
   - 修改前先確認目前分支與 `git status`。
   - 改動要集中在合理範圍，不混入無關檔案。
   - 完成後要能用 `git diff` 看出本次改了什麼。
   - 需要上線的修正，應建立 commit；commit message 要能對應 issue。

3. **測試與驗證要寫清楚**
   - 前端至少檢查 JS 語法，例如 `node --check`。
   - 串接問題要用 curl 或瀏覽器 Network 驗證實際 API。
   - 快取／部署問題要檢查正式站 header，例如 `Cache-Control`。
   - 驗證結果要寫進 issue 或本歷史追蹤文件。

4. **需要使用者看到的改動必須部署上雲**
   - 前端改動要部署到 Firebase Hosting。
   - 後端改動要部署到對應 Cloud Run 或服務環境。
   - 不能只說「本機已修」；只要使用者要在線上看到，就必須完成雲端部署。

5. **部署後要做線上確認**
   - 直接測正式網址，不只測本機。
   - 例如：
     - `https://decorate-me.web.app/`
     - `https://decorate-me.web.app/#products`
     - 對應 API endpoint
   - 若改到快取、config、CSS、JS，要確認線上實際載入的是新版本。

6. **回填紀錄**
   - issue 要補上修正內容、commit、部署結果、驗證方式。
   - 若是串接／資安／部署事故，還要補進本文件。

### 這條規則的目的

避免再次發生：

- 本機改好了但雲端沒更新。
- 雲端部署了但 GitHub 沒有紀錄。
- 修正散落在不同資料夾，之後不知道哪份才是正式版。
- 使用者看到舊畫面，但沒有 issue 可以追原因。
- 下次遇到同樣問題時，又重新踩一次坑。

### 簡單判斷

如果改動會影響正式站、會員、商品、分析、渲染、登入、權限、資料庫、API、部署設定或使用者畫面，就必須：

`GitHub issue → Git diff/commit → 測試驗證 → 部署上雲 → 線上確認 → 回填紀錄`

## 前端唯一來源規則（2026-07-16 起）

### 背景

本專案曾同時存在兩份前端：

- 主專案 repo：`C:\Users\isach\PycharmProjects\PythonProject12\firebase-hosting-full\public`
- OneDrive 日常前端：`C:\Users\isach\OneDrive\桌面\web_frontend`

這造成多次「已修但線上還是舊的」：

- Codex 修了 `firebase-hosting-full/public`，但使用者實際看到的是 OneDrive `web_frontend` 版面。
- OneDrive 有新版 admin console（深色左側欄、營運總覽、會員管理、商品管理、爬蟲匯入），但 repo 版還是舊 admin 版型。
- admin 商品 30 筆限制在其中一份移除，另一份仍保留 `ADMIN_PRODUCT_ROWS_LIMIT = 30`。
- Firebase Hosting 可能從不同資料夾部署，導致「誰後部署誰生效」。

### 最終決議

從 2026-07-16 起，只保留一份正式前端來源：

```text
C:\Users\isach\PycharmProjects\PythonProject12\firebase-hosting-full\public
```

這份是 canonical frontend，也就是唯一可部署到 Firebase Hosting 的前端。

OneDrive `web_frontend` 只可當作舊資料來源或臨時參考，不再直接部署。若 OneDrive 有新版畫面或功能，必須先合入 canonical frontend，再從 canonical frontend 部署。

### 2026-07-16 已執行的收斂

- 將 OneDrive `web_frontend` 的新版 admin console 同步進 `firebase-hosting-full/public`。
- 線上 admin 版面改為深色左側欄版本，包含：
  - `01 營運總覽`
  - `02 會員管理`
  - `03 商品管理`
  - `04 爬蟲匯入`
- 移除 canonical frontend 中 admin 商品列表的 30 筆限制。
- 統一首頁資源版本：
  - `css/main.css?v=20260716-admin-console`
  - `config.local.js?v=20260716-admin-console`
  - `js/api.js?v=20260716-admin-console`
  - `js/router.js?v=20260716-admin-console`
- 從 canonical frontend 執行 Firebase Hosting 部署。

### 以後操作規則

1. 不再從 `C:\Users\isach\OneDrive\桌面\web_frontend` 直接 `firebase deploy`。
2. 所有前端修改都在 `firebase-hosting-full/public` 進行。
3. 若使用者提供 OneDrive 截圖或檔案，先確認它是否已存在於 canonical frontend。
4. 每次部署前要確認目前工作目錄是：

```powershell
pwd
```

必須是：

```text
C:\Users\isach\PycharmProjects\PythonProject12\firebase-hosting-full
```

5. 部署指令固定：

```powershell
firebase deploy --only hosting --project decorate-me
```

6. 部署後必查：

```powershell
curl.exe -I https://decorate-me.web.app/
```

並確認首頁引用的是 canonical frontend 的最新版本號。

---

## 名詞解釋

- **X-API-Key**：一組寫在 HTTP 請求「標頭（header）」裡的密鑰字串（我們的欄位名叫 `X-API-Key`）。服務收到請求時，把這個 header 的值跟自己環境變數裡存的金鑰比對，一致才處理、不一致直接回 `401` 拒絕。作用是「只有知道金鑰的人（我們的前端）才能呼叫這支服務」，擋掉別人掃到 Cloud Run 網址就直接濫用（例如狂打渲染燒錢）。它跟「使用者帳密登入」不同——這是**服務對服務的簡單認證**，驗的是「呼叫方有沒有金鑰」，不是「這是哪個使用者」。金鑰存在伺服器環境變數與前端設定檔，不寫進程式碼。

- **CORS（跨來源資源共享）**：瀏覽器的安全機制。網頁在 A 網域（decorate-me.web.app），想打 B 網域（Cloud Run／tunnel）的 API 時，瀏覽器會先問 B「你允許 A 來打嗎」，B 要在回應標頭寫 `Access-Control-Allow-Origin` 允許，瀏覽器才放行。若要連 cookie 一起帶，B 還要回 `Access-Control-Allow-Credentials: true`（見 S9）。

- **session cookie**：使用者登入後，後端種在瀏覽器的一小段憑證。之後每次請求瀏覽器自動帶上，後端用它認出「這是已登入的誰」。屬性 `HttpOnly`（JavaScript 讀不到，防 XSS 偷）、`Secure`（只走 HTTPS）、`SameSite=None`（允許跨網域帶，因為前端和資料庫不同網域）、`Expires`（有效期，見 S8）。

- **OTP（一次性驗證碼）**：註冊時寄到使用者信箱的一組短碼，用來證明「這個 email 真的是本人的」。後端必須強制驗過才讓帳號生效（見 S7），否則等於沒驗證。

- **Cloudflare Tunnel**：把組員本機跑的服務，透過 Cloudflare 對外開一個 `https://xxx.trycloudflare.com` 網址讓外網能連。快速版（quick tunnel）每次重開網址就換，這也是專案裡「網址一直變、要更新設定重部署」的來源。

- **ΔE（Delta E）**：兩個顏色在 CIELAB 色彩空間裡的距離；數字越小代表兩色越接近。商品推薦用它把「使用者的膚色／唇色」跟「商品色號」比對，找最接近的商品。

- **allow-unauthenticated**：Cloud Run 服務的部署選項，代表「不需要 Google 帳號授權，任何人都能打這個網址」。我們的 face／render 用這個（因為前端是瀏覽器直打），改用應用層的 X-API-Key 來擋濫用（見 S18）。

---

## S19｜2026-07-16｜Render 成本、Job、GCS 與前端憑證安全加固

### 本次目的

先處理高風險、可獨立驗證的安全與營運問題；依目前決議，Web 全面改成 Gateway 唯一入口仍等爬蟲端、Ollama 端與資料庫端穩定後再做，保留未來 iOS 可沿用 GitHub 後端 API 的空間。

### 已完成

- 前端不再將明文密碼寫入 `sessionStorage`；登入、登出與 profile 讀取會清除舊版 `beautyAuthCreds`。前端不再用密碼自動重登入，會員端應改由 Bearer token／HttpOnly session 處理續期。
- Render 增加每小時請求限流、每日 provider quota、key 數量上限與 `Retry-After`；有 Firestore 時 quota 使用原子固定窗口計數，跨 Cloud Run instance 共同計數。
- Render 增加同一圖片／後端 prompt／strength 的並發去重鎖；跨 instance 透過 Firestore dedupKey 查詢，重複進行中回 `409 DUPLICATE_IN_PROGRESS`，避免重複扣 Replicate 額度。
- Render 輸入驗證補上 JPEG／PNG／WebP、base64、檔案大小、影像格式與像素數限制，analysis package 也有 JSON 大小上限。
- GCS 圖片限定 `rendered/` 前綴，物件寫入 retention metadata，新增 `DELETE /render/jobs/{job_id}` 清理 job 與永久圖片，並新增 `gcs-lifecycle.json` 預設 30 天自動刪除政策。
- Render、BASIC、PRO job 補上 guarded status transition、timeout 錯誤碼、`updatedAt` 與可重試錯誤；逾時後晚到的 worker 不得覆蓋失敗狀態，也會清理晚到的 GCS 圖片。
- 新增共用 API error envelope、`X-Request-ID` 與 method/path/status/duration logging；不記錄密碼、圖片或 token。
- 更新前端本機資料庫與商品 API URL：`https://debut-exemption-lounge-depending.trycloudflare.com`。
- `saved_looks` 只接受 HTTP(S) 圖片 URL 且限制 500 字元；File、Blob、data URL／base64 不會送進資料庫，未先完成檔案上傳時只保留本機收藏。

### 驗證與部署狀態

- 新增 `render_api_test.py`，涵蓋圖片驗證、錯誤格式、Render 去重、Job 狀態保護、GCS URL 邊界與前端密碼儲存檢查。
- 已執行 `render_api_test.py` + `ai_gateway_test.py` 共 13/13 通過、Python compile、前端 `node --check`、`git diff --check` 與 `.venv` 的 `pip check`；部署前再次檢查同樣通過。
- `config.local.js` 已更新新的資料庫 tunnel，但因含 API key 仍維持本機忽略，不上傳 GitHub；正式環境請用環境變數／Secret Manager。
- Firebase Hosting 已部署並驗證 `https://decorate-me.web.app`；線上 `config.local.js` 指向新 Tunnel，前端 URL-only guard 已生效。
- Cloud Run 已部署並驗證：`face-basic-00017-smb`、`face-pro-00011-v2c`、`replicate-render-00039-fsn`、`ai-gateway-00007-2cd`；四支 `/health` 均回 `status: ok`。
- GCS bucket `decorate-me-renders` 已套用 `rendered/` 前綴、物件 30 天自動刪除 lifecycle policy。
- GitHub 版本控制已推送至 `Isa` 分支；依要求不保留 Codex 分支、未建立 PR。爬蟲端、Ollama 端與資料庫端仍維持分段穩定後再做 Gateway 全面收斂的決議。

### 相關設定

- `RENDER_QUOTA_MAX_REQUESTS`／`RENDER_QUOTA_WINDOW_SECONDS`：provider quota，預設每日 30 次。
- `RENDER_RATE_LIMIT_MAX_REQUESTS`／`RENDER_RATE_LIMIT_WINDOW_SECONDS`：請求限流，預設每小時 10 次。
- `GCS_RENDER_RETENTION_DAYS`：應與 GCS lifecycle policy 一致，預設 30 天。
- `TRUST_FORWARDED_FOR=1` 只應在確認前方 proxy 會正確重寫 `X-Forwarded-For` 時開啟；預設不信任用戶自行提供的 forwarded header。

## S34｜2026-07-16｜正常商品 demo 圖片改為白底

- 問題：商品卡片的 `.ph` 圖片框與無商品圖片的 demo SVG 使用粉／米色背景，商品展示不一致。
- 修正：正常商品圖片框改為純白 `#fff`；無商品圖片的分類 demo SVG 也統一輸出白底，保留分類文字與金色內框。
- 範圍：只調整正常商品展示圖；風格靈感卡、品牌形象 demo 與後台灰白主題不變。
- 驗證：`node --check firebase-hosting-full/public/js/data.js`、`git diff --check`，並以正式商品頁檢查圖片底色來源。

## S35｜2026-07-16｜後台會員讀取錯誤與手機版滑動修正

- 問題：會員清單收到 `401` 時仍呼叫已移除的 `this._reLogin()`，畫面因此顯示 `this._reLogin is not a function`；後台手機版的頂部操作列與摘要卡片也可能超出視窗，無法像使用者前台順暢滑動。
- 修正：會員 API 改走現有 HttpOnly session cookie；若登入回應提供短期 Bearer token，只暫存於本分頁 `sessionStorage` 並只送往會員資料庫，不保存明文密碼，也不再自動重登入。
- 手機版：後台導覽改為可觸控橫向滑動，內容區、摘要卡片、頂部操作列補上寬度約束與垂直／橫向滑動邊界，避免頁面被固定寬度撐出螢幕。
- 失效處理：401 保留為可理解的登入逾時提示，由管理員重新登入，不再拋出 JavaScript 函式不存在錯誤。

## S36｜2026-07-16｜後台配色統一前台玫瑰米白主題

- 問題：後台使用深灰側欄、冷灰內容區與綠色操作色，與使用者前台的玫瑰米白、棕紅與金色視覺不一致。
- 修正：後台改沿用前台 `--ivory`、`--paper`、`--white`、`--gold-deep`、`--terra-deep` 與文字色彩；保留啟用、警告、錯誤的辨識度。
- 範圍：只調整後台管理頁的視覺配色，不改變會員權限、商品資料與 API 行為。
## S37｜2026-07-19｜Gateway 安全代理改造（新手說明）

### 先記住一句話

Gateway（API Gateway／API Proxy）就是「前端和真正服務中間的安全櫃台」。瀏覽器把請求交給 Gateway，Gateway 再用伺服器端保存的憑證呼叫 Face、Render、文字建議與會員資料服務。真正的服務金鑰不放進網頁、不放進 GitHub，也不寫進一般 Log。

可以把它想成餐廳：

- 前端是客人，只能點餐，不能進廚房拿廚師的鑰匙。
- Gateway 是櫃台，先確認客人的會員 session、請求路徑和流量，再把訂單轉給正確廚房。
- Face／Render／會員資料服務是廚房；廚房的內部門牌和工作用金鑰由 Gateway 保管。

### 為什麼不能讓前端直接呼叫 AI 服務

`config.local.js` 會被瀏覽器下載。只要使用者開啟開發者工具，就能看到其中的網址和任何寫在裡面的 key。即使把 key 混淆或改名，也不能當成秘密。被看見的 key 可能被拿去耗用額度、偽造請求或讀取不該讀的資料。

所以前端只保留 Gateway 網址（這個網址本身不是秘密），例如同源的 `/face-basic`、`/face-pro`、`/text-suggestion`、`/render-service` 和 `/member-database`。上游 API key、Cloud Run IAM、`GATEWAY_SESSION_SECRET` 只能放在 Cloud Run 環境變數或 Secret Manager。

### 舊流程和新流程

舊流程：

```text
瀏覽器 ──(上游 URL + API key)──> Face / Render / 會員資料服務
```

新流程：

```text
瀏覽器 ──(登入 session + 請求)──> Gateway ──(伺服器端 key/IAM)──> 私有服務
                                      ├── Face Basic / Face Pro
                                      ├── Render
                                      ├── 文字建議
                                      └── 會員資料庫
```

前端送出的 `Authorization` 只代表目前登入會員；Gateway 會驗證它，再自行帶上上游服務需要的憑證。Gateway 不信任瀏覽器自行附加的 `X-User-Email`、`X-User-Role`，也不會把瀏覽器的 token 原封不動轉給私有 AI 服務。

### 這次已經寫進程式的內容

- [x] AI Gateway 增加 `text-suggestion` 代理路徑。
- [x] AI Gateway 增加受限的 `member-database` 代理路徑，只允許會員功能需要的固定 API 路徑。
- [x] 登入、註冊、寄送 OTP、驗證 OTP 改有 Gateway 公開入口；每個入口仍使用固定路徑和登入嘗試限流。
- [x] 增加 `GATEWAY_SESSION_ONLY=true` 模式。開啟後，已登入功能只依賴短效會員 session，不再要求把 Gateway client key 放在瀏覽器。
- [x] `health` 會顯示目前是 `member-session-only` 還是舊的 `client-key-and-member-session` 模式。
- [x] Firebase Hosting 增加 `/face-basic/**`、`/face-pro/**`、`/text-suggestion/**`、`/render-service/**`、`/member-database/**` rewrite 到 `ai-gateway`。
- [x] 前端在有 `gatewayUrl` 設定時，自動改用 Gateway 路徑，並忽略原本的 face/render/text API key 欄位。
- [x] 公開的 `config.local.js` 已移除上游 key、會員資料庫網址與 AI 服務直連設定；現在只放 Gateway、產品公開路由和展示開關。
- [x] 本機測試：JavaScript 語法檢查、前端 smoke check、Admin API contract test、Gateway 單元測試均通過。

### 新手照做的部署順序

1. **先輪替舊 key。** 到 Face、Render、文字建議服務的管理端停用已經出現在舊前端設定中的 key，重新產生新 key。不要把新 key 貼到聊天、截圖或 GitHub。
2. **在 Gateway 的 Cloud Run 設定秘密。** 設定 `FACE_BASIC_URL`、`FACE_PRO_URL`、`TEXT_SUGGESTION_URL`、`RENDER_URL`、`MEMBER_DATABASE_URL`；把上游憑證放在 `UPSTREAM_FACE_API_KEY`、`UPSTREAM_TEXT_SUGGESTION_API_KEY`、`UPSTREAM_RENDER_API_KEY`。另外產生至少 32 字元的 `GATEWAY_SESSION_SECRET`。
3. **啟用 session-only。** 設定 `GATEWAY_SESSION_ONLY=true`。這代表登入後使用短效 session；前端不需要保存 Gateway client key。登入入口仍有 IP 限流，正式環境也要設定正確的 `CORS_ORIGINS`。
4. **建置並更新 Gateway。** 使用專案的 `Dockerfile.gateway` 和 `cloudbuild.gateway.yaml` 建置映像，再把映像部署到 Cloud Run 的 `ai-gateway` 服務。部署時把上面的環境變數接到 Secret Manager，避免在 PowerShell 歷史或 YAML 裡寫明文。
5. **先測 Gateway。** 開啟 `/health`，應看到 `status: ok` 和 `browserAuth: member-session-only`。用測試會員登入，再測一個 Face job、文字建議和 Render job；401、503 時先看錯誤代碼，不要把 token 貼進紀錄。
6. **檢查前端包。** 在部署資料夾執行搜尋，確認沒有 `faceApiKey`、`renderApiKey`、`textSuggestionApiKey` 的實際值，也沒有 `.env`、JWT、完整 email 或完整生成 prompt。只看到 Gateway URL 是正常的。
7. **最後才部署 Firebase Hosting。** Hosting 部署前先確認 `config.local.js` 是 Gateway-only 設定。若部署掃描仍說有 apparent secret，先找出來源並移除或輪替，不要用參數繞過掃描。

### 常見錯誤怎麼看

| 畫面錯誤 | 新手解讀 | 先檢查哪裡 |
|---|---|---|
| `401 Invalid or missing API key` | Gateway 還在舊的 client-key 模式，或請求送錯路徑 | Cloud Run 是否真的有 `GATEWAY_SESSION_ONLY=true`，前端是否已更新 |
| `401 MEMBER_AUTH_REQUIRED` | 使用者尚未登入，或前端沒有帶短效 session | 先重新登入；不要把上游 key 填回前端 |
| `401 MEMBER_AUTH_INVALID` | session 過期、簽章不一致或 Gateway 重啟後秘密不一致 | `GATEWAY_SESSION_SECRET` 是否同一把，系統時間是否正確 |
| `503 NOT_CONFIGURED` | Gateway 找不到上游網址或秘密 | Cloud Run 服務的環境變數／Secret Manager 版本 |
| `502 UPSTREAM_UNAVAILABLE` | Gateway 找得到設定，但上游服務目前連不到 | 上游服務健康狀態、Cloud Run IAM 或 Tunnel 是否在線 |
| 瀏覽器 CORS 錯誤 | 預檢請求被拒絕 | `CORS_ORIGINS` 是否包含正式前端網址；`OPTIONS` 應回 204 |

### 目前還不能勾的雲端工作

- [ ] 在 Face、Render、文字建議服務端完成舊 key 輪替。
- [ ] 在正式 Cloud Run `ai-gateway` 設定 Secret Manager 版本與 `GATEWAY_SESSION_ONLY=true`。
- [ ] 以正式網址完成登入、Face、推薦、Render、會員刪除同步的端到端測試。
- [ ] 確認 Firebase Hosting 部署掃描不再發現前端 apparent secret 後，再發布新版本。

### 最後的判斷方式

Gateway 不是「把密碼改放另一個網址」；它的價值是把秘密移到伺服器，並在同一個入口統一做身份驗證、路徑白名單、限流、CORS 和錯誤處理。只要瀏覽器仍能直接拿到上游 key，安全問題就還沒真正解決；只要瀏覽器只拿到 Gateway 路徑、登入 session 短效且上游服務由 Gateway 驗證，才算完成這個安全邊界。

### 專題展示期間的 Ollama 例外

目前文字建議服務仍透過公開 Tunnel，原因是專題展示需要即時觀察 Ollama 的提示與回應。這不是永久商用設定；Gateway 只有在 `GATEWAY_ALLOW_EXTERNAL_TEXT_UPSTREAM=true` 時才會啟用這條路徑，預設值是關閉。展示期間只能使用假會員、假分析資料和縮短後的提示，不可送出真實 email、照片、權杖、完整分析包或生成提示全文。

- [x] 外部文字服務改成明確環境開關，不會因忘記設定而默默開啟。
- [ ] Ollama 調整完成後，將 `GATEWAY_ALLOW_EXTERNAL_TEXT_UPSTREAM` 改回 `false`。
- [ ] 移除 `TEXT_SUGGESTION_URL` 和公開 Tunnel，改用同一個受信任 Cloud Run／私有網路服務。
- [ ] 專題結束後確認日誌不再保存完整 prompt，只保留請求 ID、階段、狀態與耗時。

### 2026-07-19 實際部署結果

- [x] Face Basic、Face Pro、Render 已改用 Secret Manager 新金鑰。
- [x] AI Gateway 已部署至 Cloud Run，正式流量使用 `member-session-only`。
- [x] Firebase Hosting 已發布 Gateway-only 前端；線上 `config.local.js` 不含 face/render/text API key。
- [x] 線上 Gateway `/health` 回報 `status: ok`。
- [ ] Ollama 外部 Tunnel 暫不接入正式 Gateway；等待可信任 Cloud Run／私有端點後再開啟。

### 2026-07-19 最新程式重新部署確認

- [x] AI Gateway 最新程式已部署，正式流量為 `ai-gateway-00013-h5p` 100%。
- [x] Gateway health：`status: ok`、`browserAuth: member-session-only`。
- [x] Ollama 外部文字上游開關目前為 `disabled`。
- [x] Firebase Hosting 最新前端已發布至 `https://decorate-me.web.app`。
- [x] 線上打卡頁與管理頁均回傳 HTTP 200。
- [x] 線上 `config.local.js` 沒有 Face、Render 或文字建議 API key 欄位。

### 2026-07-19 前端圖片速度與安全標頭

- [x] 七張風格圖片改為 720px WebP，總容量由 10,008,310 bytes（約 10 MB）降為 263,136 bytes（約 263 KB），減少 97.4%。
- [x] 圖片加入延遲載入、非同步解碼、固定尺寸與低優先序下載，減少首屏阻塞和版面跳動。
- [x] `/assets/**` 啟用一年 immutable 快取；檔名使用版本號，更新圖片時需改新檔名。
- [x] Firebase Hosting 加入 HSTS、`nosniff`、防 iframe、Referrer Policy 與 Permissions Policy。
- [x] 前端語法、冒煙測試、Admin API 契約測試與 Firebase 設定檢查通過。
- [x] 正式站已重新部署，線上 WebP 回傳 HTTP 200，快取與安全標頭已確認生效。
- [ ] 下一階段替商品圖產生 320／640／1280px 縮圖並使用 `srcset`，避免直接載入外站大圖。
- [ ] 拆分 `router.js` 與管理端程式，使用者未進入的頁面不要預先下載。

完整待辦請見：`系統資安效率流程改善清單.md`。

### 2026-07-19 專題展示流程列排版

- [x] 管理端專題展示標題由「AI 工作流程監控」改為「工作流程監控」。
- [x] 工作階段的無障礙標籤移除 AI 字樣。
- [x] 移除內容區重複標題，讓 01–05 流程列往上靠近管理端標題。
- [x] 移除流程區的大卡片外框、底色與陰影，改成融入頁面的扁平分頁列。
- [x] 手機版保留橫向捲動，避免五個階段被擠成狹窄直欄。
- [x] 前端與 Admin API 測試通過，正式站已部署並確認新版 HTML、CSS、JavaScript 生效。

### 2026-07-19 商品 1040 筆完整同步

- [x] 確認前台只有 50 件不是圖片優化造成，而是商品 API 預設每頁只回傳 50 件。
- [x] 確認 API `total=1040`、第一頁 50 件，並以 `nextCursor` 提供下一頁。
- [x] 前台改為每次 100 件，自動依 `nextCursor` 讀完所有分頁；第一批先顯示，其餘在背景補齊。
- [x] Admin 商品管理同步改為讀取完整分頁，不再只顯示總數 1040 卻只持有第一頁資料。
- [x] 商品圖片保留 `loading="lazy"` 與非同步解碼，不會因 1040 件商品一次下載全部圖片。
- [x] 加入分頁契約測試與游標重複保護，避免 API 異常造成無限請求。
- [x] 正式站已部署；線上實測讀取 11 頁、共 1040 筆、1040 個商品 ID 皆不重複。

### 2026-07-19 商品載入恢復 GitHub 舊版

- [x] 依使用者指定，參照 GitHub `Isa` 分支恢復舊商品載入流程。
- [x] 前台恢復單次 `Api.listProducts()`，移除 11 頁自動游標讀取。
- [x] Admin 恢復單次 `Api.listAdminProducts()`。
- [x] 移除本次新增的完整分頁函式、進度文字與分頁契約測試。
- [x] 保留其他管理端、會員、圖片 WebP、lazy loading 與資安修改。
- [x] 前端與 Admin API 測試通過，正式站已重新部署並確認舊載入程式生效。

> 注意：目前線上商品 API 單次只回傳 50 件；GitHub 舊前端已恢復，但若資料庫端仍維持新版每頁 50 件限制，前台也只會取得該次回應內容。

### 2026-07-19 商品頁再次恢復完整 1040 件版本

- [x] 釐清使用者指定的是「商品頁一次排出完整 1040 件」版本，不是只呼叫一次而只取得 50 件的 GitHub 舊流程。
- [x] 恢復自動依 `nextCursor` 讀取所有商品分頁。
- [x] 前台與 Admin 均恢復完整商品清單。
- [x] 保留圖片 lazy loading，頁面可排出 1040 件但不會立即下載全部 1040 張圖片。
- [x] 測試通過並重新部署；正式站已確認 `listAllProducts` 與 `listAllAdminProducts` 生效。
