# Ollama 端介面與金鑰修正需求

更新日期：2026-07-28  
用途：提供 Ollama 服務維護者與 Gateway／渲染端對接時使用。  
安全原則：本文件不保存任何實際 API 金鑰，只描述金鑰名稱、位置與驗證方式。

## 1. 目前正式呼叫流程

使用者前端不應直接呼叫 Ollama，也不應保存或傳送 Ollama 金鑰：

```text
瀏覽器
  → Firebase Hosting /text-suggestion/suggest
  → AI Gateway
  → Ollama 服務 /suggest
```

Gateway 會在伺服器端加入 `X-API-Key`。因此前端沒有 `X-API-Key` 並不是錯誤，禁止為了修正 401 而把金鑰寫入前端。

渲染服務另有一條伺服器對伺服器流程：

```text
Render 服務
  → SUGGESTION_SERVICE_URL/suggest
  → Ollama 服務
```

這條流程也只能由 Render 服務在伺服器端加入金鑰。

## 2. 金鑰名稱與責任

| 名稱 | 放置位置 | 用途 |
|---|---|---|
| `SUGGESTION_API_KEY` | Ollama 服務環境／Secret Manager | Ollama 驗證進入 `/suggest` 的服務金鑰 |
| `UPSTREAM_TEXT_SUGGESTION_API_KEY` | AI Gateway 環境／Secret Manager | Gateway 呼叫 Ollama 時送出的 `X-API-Key`；值必須與 Ollama 端一致 |
| `SUGGESTION_SERVICE_API_KEY` | Render 服務環境／Secret Manager | Render 直接呼叫 Ollama 時送出的 `X-API-Key`；值必須與 Ollama 端一致 |
| `GATEWAY_TEXT_SUGGESTION_API_KEY` | 僅在 Gateway 非 session-only 模式使用 | 瀏覽器呼叫 Gateway 的額外服務金鑰；不得放入正式前端 |

正式環境建議使用 `GATEWAY_SESSION_ONLY=true`，由會員 session 驗證前端，不要求瀏覽器持有 Gateway client key。

## 3. Ollama 端必須完成

- [ ] 正式環境設定 `SUGGESTION_API_KEY`，缺少時服務拒絕啟動。
- [ ] `/suggest` 與 `/suggest/stream` 都驗證 `X-API-Key`。
- [ ] 錯誤金鑰或缺少金鑰固定回傳 HTTP 401，不回傳金鑰內容。
- [ ] `/health` 僅回傳 `api_key_required: true` 等狀態，不回傳環境變數、完整 URL、Prompt 或會員資料。
- [ ] `SUGGESTION_API_KEY` 只放 Secret Manager／服務環境，不放 Git、前端、README、Issue、截圖或一般 Log。
- [ ] 金鑰輪替時先同步更新 Gateway 的 `UPSTREAM_TEXT_SUGGESTION_API_KEY` 與 Render 的 `SUGGESTION_SERVICE_API_KEY`，再重啟 Ollama；完成驗證後撤銷舊金鑰。
- [ ] Log 只記錄錯誤代碼與 request ID，不記錄 `X-API-Key`、完整 Prompt、Email、照片或完整分析包。

## 4. `/suggest` 輸入與輸出契約

### Request

```json
{
  "analysisPackage": { "faceAnalysis": {} },
  "faceAnalysis": {},
  "style": "日常自然妝",
  "language": "zh-TW",
  "userNote": "可選，需限制長度",
  "model": null
}
```

`faceAnalysis` 可直接提供，或放在 `analysisPackage.faceAnalysis`；至少一處必須存在，否則回傳 HTTP 400。

### Response

```json
{
  "status": "completed",
  "provider": "ollama",
  "model": "gemma3",
  "fallbackUsed": false,
  "createdAt": "2026-07-28T00:00:00Z",
  "suggestion": "中文妝容建議"
}
```

目前 Ollama `/suggest` 的正式文字欄位是 `suggestion`。

## 5. 必須與渲染端確認的欄位問題

目前 `replicate_render.py` 呼叫 `/suggest` 後讀取 `renderPromptEn`，但 Ollama `/suggest` 回傳的是 `suggestion`。這會造成渲染端判定沒有文字建議，改用固定 style prompt。

請二選一，並在部署前固定契約：

### 建議方案 A：維持 Ollama 回傳 `suggestion`

由 Render 端改讀取：

```python
prompt = (response.json().get("suggestion") or "").strip()
```

### 方案 B：Ollama 額外回傳 `renderPromptEn`

只有在確實需要英文渲染指令時才採用，並明確定義：

```json
{
  "suggestion": "中文建議",
  "renderPromptEn": "English render instruction"
}
```

不得讓兩端各自假設不同欄位名稱。

## 6. 驗收方式（不公開真實金鑰）

### Ollama 直連測試

僅在 Ollama 維護主機執行，使用環境變數代入金鑰：

```powershell
$env:OLLAMA_KEY = '<從 Secret Manager 讀取>'
curl.exe -X POST 'https://<ollama-service>/suggest' `
  -H 'Content-Type: application/json' `
  -H "X-API-Key: $env:OLLAMA_KEY" `
  -d '{"faceAnalysis":{"faceShape":"oval"},"style":"日常自然妝","language":"zh-TW"}'
```

預期：HTTP 200，回應包含 `suggestion`。

不帶金鑰或使用錯誤金鑰時，預期固定 HTTP 401。

### 正式前端流程

前端不自行加 `X-API-Key`，由 Gateway session 與伺服器端 Secret 完成驗證。若失敗，依 HTTP 狀態判斷：

| 狀況 | 可能原因 |
|---|---|
| 503 `EXTERNAL_TEXT_UPSTREAM_DISABLED` | Gateway 的文字服務開關尚未開啟 |
| Gateway 401 `UNAUTHORIZED` | 非 session-only 模式缺少 Gateway client key |
| 上游 401／`FORBIDDEN` | Gateway 或 Render 沒有送出正確的 Ollama key |
| HTTP 200 但渲染沒有使用建議 | `suggestion`／`renderPromptEn` 欄位契約不一致 |

## 7. 不可接受的做法

- 不把真實金鑰寫入 `config.local.js`、前端 JavaScript、GitHub Issue 或文件。
- 不要求教授或一般使用者輸入 Ollama 金鑰。
- 不讓瀏覽器直接連 Ollama tunnel。
- 不在 Log 記錄完整 Prompt、照片、Email、權杖或完整分析包。
- 不以「curl 直連成功」取代 Gateway→Ollama→前端的端對端驗收。

## 8. 完成判定

- [ ] Ollama 端回報 `api_key_required=true`。
- [ ] Gateway 與 Render 的 Secret 已設定且與 Ollama 端一致（只驗證，不在文件記錄值）。
- [ ] 缺少／錯誤金鑰測試均回 401。
- [ ] Gateway 正式流程回 200，前端不含金鑰。
- [ ] Render 端與 Ollama 的回應欄位已統一。
- [ ] 金鑰輪替後舊金鑰已撤銷。
- [ ] 驗收結果已寫入歷史流程追蹤文件。
