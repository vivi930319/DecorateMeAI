# 五官判斷回饋 · 端對端流程說明

**日期：** 2026-07-27
**相關 issue：** #24（五官分類：資料擴充與後續重測流程）
**狀態：** 前端與後端都已實作並**部署完成**（2026-07-28）。
`face-basic-00029-2cf`／`face-pro-00016-4h5`／`ai-gateway-00055-9bh`／`replicate-render-00050-fcm`。
尚未在線上實際送出過一筆回饋，驗收步驟見 §六。

---

## 一、這件事在解什麼問題

issue #24 的表：

| 部位 | 目前分數 | 距 0.70 門檻 | 資料量 | 類別數 |
|---|---:|---:|---:|---:|
| 眼型 | 0.387 | −31.3 | 603 | 6 |
| 唇型 | 0.451 | −24.9 | 352 | 5 |
| 臉型 | 0.522 | −17.8 | 386 | 5 |
| 眉型 | 0.667 | −3.3 | 336 | 3 |

**缺的是標註資料，而使用者一直在免費產生它。**

前端分析結果下方本來就有「這些判斷準嗎？」的修正面板。那些選項是照
`models/basic_features_roi/*_classes.json` 排的，所以使用者每改一次，就是一筆
**已經對齊模型類別、不需要再做對應的人工標註**——而且他最會去改的正好是眼型，
也就是分數最低、資料最少的那一項。

**在這次改動之前，那些修正只寫進瀏覽器的 localStorage，登出就被清掉，從來沒有人收到過。**

---

## 二、完整資料流

```
① 使用者在分析結果下方改「眼型」→ 杏仁眼
        │
        │  前端：applyAnalysisCorrections()
        ├─► Router.analysisResult['眼型'] = '杏仁眼'
        ├─► analysisPackage.faceAnalysis 用 fromRawFaceAnalysis() 重導
        ├─► AnalysisDraft 存回 sessionStorage
        ├─► History 那一筆分析紀錄一起改
        ├─► 分析頁上的結果格重繪
        └─► 已產生的 Ollama 建議標成 stale（提示要重新生成）
        │
        │  ※ 這一段在「改下拉的當下」就發生，不必按送出。
        │     否則畫面顯示新答案、送出去的還是舊的。
        ▼
② 使用者按「送出回饋」
        │
        │  前端：Api.sendAnalysisFeedback()
        ▼
POST /face-basic/v1/face/jobs/{jobId}/feedback     （Firebase Hosting rewrite）
   Header: X-Job-Token: <建 job 時發的 resultToken>
   Body:   { packageId, predicted, corrections, confirmed }
        │
        ▼
③ Firebase Hosting → Cloud Run ai-gateway
        │
        ├─ 路徑白名單比對（ai_gateway.py UPSTREAMS[...].allowed_paths）
        ├─ 會員 session 驗證
        └─ build_upstream_headers() 轉送 X-Job-Token、補上 UPSTREAM_FACE_API_KEY
        ▼
④ Face Basic / Face Pro（Cloud Run）
        │
        ├─ job_store.get(_COL, job_id)          → 找不到就 404 JOB_NOT_FOUND
        ├─ _verify_job_token()                  → token 不符就 403
        └─ face_feedback.save(mode, job_id, payload)
                 │
                 ├─ confirmed=true 或 corrections 空 → 刪掉既有紀錄（見 §四.1）
                 ├─ validate()：每個部位、每個值都要在該部位的 *_classes.json 裡
                 └─ job_store.create("face_feedback", job_id, doc)
        ▼
⑤ Firestore collection `face_feedback`
        文件 id = jobId（重送會覆蓋，因為使用者可以改完再改）
```

---

## 三、每一段的實作位置

### 前端（`web_frontend`）

| 做什麼 | 在哪裡 |
| --- | --- |
| 修正面板 UI 與下拉 | `js/router.js` `renderAnalysisFeedback()` |
| 把修正套進本機所有下游 | `js/router.js` `applyAnalysisCorrections()` |
| 送出回饋 | `js/api.js` `sendAnalysisFeedback()` |
| 端點路徑 | `js/api.js` `faceBasic.jobFeedbackPath` / `facePro.jobFeedbackPath` |
| job token 存放處 | `analysisPackage.async.resultToken` |
| 本機暫存（端點到位後可移除） | `js/api.js` `AnalysisFeedback` |

### 後端（`PythonProject12`）

| 做什麼 | 在哪裡 |
| --- | --- |
| 驗證與儲存的核心 | `face_feedback.py`（新檔） |
| BASIC／PRO 路由 | `face_feedback.register_route()`，兩支 analyzer 各呼叫一次 |
| Gateway 路徑放行 | `ai_gateway.py` `UPSTREAMS["face-basic"|"face-pro"].allowed_paths` |
| 分類表來源 | `models/basic_features_roi/{part}_classes.json` |

---

## 四、三個設計決定，以及它們的代價

### 0. 回饋只在 job 還活著時送得出去

路由第一步是 `job_store.get(_COL, job_id)`，找不到就 404。job 受
`FACE_JOB_RETENTION_SECONDS`（預設 3600 秒）管，**所以使用者只有大約一小時可以送出修正**，
過了就再也送不出去（前端會靜默忽略，不打擾他）。

這是刻意的：token 存在 job 上，job 沒了就無法證明送出者是這個 job 的主人，而未經驗證的
標註會直接污染訓練資料。要延長就調 `FACE_JOB_RETENTION_SECONDS`，不要繞過驗證。

### 1. `confirmed` 的那些不存，而且會刪掉舊的

使用者說「判斷正確」時，後端**刪掉這個 job 既有的紀錄**，然後回 204。

**刪除而不是略過，這一點很重要。** 使用者可以改完送出、再改回「判斷正確」重送
（前端寫著「已送出，可再修改」）。如果這裡只是早退不寫，那筆已經寫進去的錯誤修正
就會永遠留在訓練集裡——而且無聲無息，你在重訓時看到的分數會被它拖著卻查不出原因。

- **好處：** 集合不會被大量「模型答對了」的紀錄塞滿。重訓要的是答錯的那些。
- **代價：** **算不出準確率的分母。** 你會知道「使用者改了幾次、改成什麼」，但不知道
  「總共問了幾次」。所以 `face_feedback` 的筆數**不能**拿來估線上準確率——那是有偏的樣本。
- **想要分母時：** 在 `face_feedback.save()` 裡對 confirmed 的情況也寫一筆精簡文件
  （只留 jobId + createdAt + 部位清單），空間成本遠低於完整紀錄。

`confirmed` 由**前端**在送出當下判定並明寫，後端不用 `corrections` 是否為空去反推。
語意留在產生它的那一刻，接收端不做推論。

### 2. 存在獨立集合，不寫回 job

`face_feedback` 是獨立 collection，不是 job 文件的欄位。

**原因：** job 一小時就會被 `FACE_JOB_RETENTION_SECONDS`（預設 3600）清掉，標註卻要活到
下一次重訓。寫回 job 等於一小時後全部消失。

文件 id 用 `jobId`：前端寫著「已送出，可再修改」，同一個 job 重送就覆蓋，不會累積成
好幾筆互相矛盾的標註。

### 3. 用 `*_classes.json` 驗證，不自己抄一份清單

`face_feedback.allowed_classes()` 直接讀類別檔，跟 `basic_roi_shadow` 同一個 `MODEL_DIR`。

**原因：** 抄一份就會有「模型已經合併類別、驗證還在擋舊類別」這種對不上的狀況。
`basic_roi_shadow._retired_labels()` 的 docstring 已經警告過同一個坑。

**髒標籤的代價不是「多一筆沒用的資料」，而是重訓分數變得不可信**——你會分不出模型是
真的變差，還是訓練集混進了模型根本沒有的類別。所以類別檔讀失敗時，驗證會擋掉**全部**
修正而不是放行全部。寧可少收一筆。

---

## 五、隱私，以及標註怎麼接回影像

**這個端點永遠不會收到照片。** 送出的只有類別字串與 jobId / packageId。前端在畫面上
也是這樣對使用者說的：「這一步不會上傳你的照片，只送出判斷結果與你的修正。」
`face_feedback.save()` 只挑 `predicted` 裡屬於已知部位的欄位存，其餘丟棄。

### 那影像在哪裡？

**在渲染端。** `/media/render/{renderJobId}/before` 就是使用者的原始照片，由渲染服務保存、
經 Gateway 驗證後提供（`ai_gateway.py` `_safe_render_gateway_url`）。臉部分析這端從頭到尾
沒有保存過任何影像——job 只帶 `result`，照片處理完就從記憶體丟掉。

### 兩邊怎麼接起來

標註在臉部分析的 job，影像在渲染的 job，兩個 id 不同。所以**渲染請求會帶上 `faceJobId`**：

```
臉部分析 job ──► face_feedback（標註，無影像）
      │
      │ faceJobId
      ▼
渲染 job ──────► /media/render/{id}/before（影像）＋ faceJobId 欄位
```

前端在 `runMakeupRender()` 的 payload 裡送 `faceJobId`，渲染端存進 job 文件
（`replicate_render_api.py`）。重訓時用它把兩邊 join 起來。

**這個設計刻意不讓臉部分析端碰到影像，連 URL 都不給。** 它只知道「哪一次分析被改了什麼」；
要看影像必須走渲染端那條需驗證的路徑，權限沒有因此放寬。

### 仍然要注意的兩點

1. **不是每一筆標註都接得到影像。** 使用者通常看完分析就修正，那時還沒渲染。只有後來
   真的按了 Step 2 的那些才有 `faceJobId` 可以 join。
2. **影像本身有保存期限。** 渲染 job 受 `RENDER_JOB_RETENTION_SECONDS` 管，過期就沒了，
   但標註會一直留著。要拿影像重訓就得在期限內把配對撈出來，或另外決定保存策略——
   那是新的同意範圍，不能從這條路悄悄延長。

---

## 六、怎麼驗證

### 本機

```bash
# 1. 起 Face Basic
python Face_analyzer_BASIC.py          # 預設 8001

# 2. 建一個 job，記下回傳的 jobId 與 resultToken
curl -X POST http://127.0.0.1:8001/v1/face/jobs/basic -F "file=@某張臉.jpg"

# 3. 送一筆修正
curl -i -X POST http://127.0.0.1:8001/v1/face/jobs/<jobId>/feedback \
  -H "Content-Type: application/json" \
  -H "X-Job-Token: <resultToken>" \
  -d '{"packageId":null,"predicted":{"眼型":"圓眼"},"corrections":{"眼型":"杏仁眼"},"confirmed":false}'
# 預期 204
```

**要確認會被擋下來的情況：**

| 測試 | 預期 |
| --- | --- |
| 不帶 `X-Job-Token` | 403 |
| 用別的 job 的 token | 403 |
| jobId 不存在 | 404 `JOB_NOT_FOUND` |
| `corrections: {"眼型":"三角眼"}`（不在分類表） | 400 `INVALID_FEEDBACK` |
| `corrections: {"髮型":"長髮"}`（不是五官部位） | 400 `INVALID_FEEDBACK` |
| `confirmed: true` | 204，且 Firestore 那一筆被刪掉（先送修正、再送 confirmed 驗證） |

### 線上

在 <https://decorate-me.web.app> 完成一次臉部分析 → 改一項五官 → 按「送出回饋」
→ 到 Firestore 看 `face_feedback` 有沒有那一筆。

---

## 七、怎麼把資料拿出來重訓

```python
import job_store
rows = job_store.all_jobs("face_feedback", limit=None)
for r in rows:
    # r["corrections"] 是 {部位中文名: 正確類別}
    # r["predicted"]   是模型當初的答案
    # 兩個併看就是「模型錯在哪」
    ...
```

**不需要影像就能做的**（所有標註都適用）：

- **線上混淆矩陣** —— 模型最常把 A 誤判成 B。用來決定下一批標註資料要補哪些類別。
- 驗證 issue #24 裡「眼型最弱」的判斷，在真實使用者身上是否成立。

**要影像的話**，用 `faceJobId` 跟渲染 job join（見 §五）：

```python
import job_store

feedback = job_store.all_jobs("face_feedback", limit=None)
renders = {r.get("faceJobId"): r for r in job_store.all_jobs(RENDER_JOBS_COLLECTION, limit=None) if r.get("faceJobId")}

for f in feedback:
    r = renders.get(f["jobId"])
    if not r:
        continue          # 這一筆沒有渲染過，只有標註沒有影像
    # f["corrections"] 是正確答案，f["predicted"] 是模型當初的答案
    # r 這一側可以取得 /media/render/{r['jobId']}/before
```

**兩個限制照抄 §五：** 只有後來真的按了 Step 2 的才 join 得到；渲染 job 有保存期限
（`RENDER_JOB_RETENTION_SECONDS`），過期影像就沒了而標註還在。

---

## 八、待辦

- [x] 部署 Face Basic / Face Pro（含 `face_feedback.py`）
- [x] 部署 ai-gateway（路徑白名單）
- [x] 部署 replicate-render（`faceJobId`）
- [ ] **線上驗證一次**（還沒做過，見 §六）
- [ ] 累積一段時間後，跑一次線上混淆矩陣，回報到 issue #24
- [ ] 端點穩定後，移除前端的 `AnalysisFeedback` 本機暫存
      （`js/api.js` 註解已標「端點到位後會改成送出即清除」）
- [ ] 決定要不要保留 `confirmed` 的計數以取得準確率分母（見 §四.1）
