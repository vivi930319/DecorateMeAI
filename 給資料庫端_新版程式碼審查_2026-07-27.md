# 給資料庫端：7/26 新版程式碼審查

> 日期：2026-07-27
>
> 對象：`lavien` 分支（2026-07-26 16:31 的「Add files via upload」整包更新）
>
> 這份是我們對新版程式碼的閱讀結果，重點在**串接會不會斷**與**憑證安全**。
> 先講結論：有兩項要立刻處理，其中一項已經造成 7/27 整站無法登入。
>
> 禁止寫入本文件、Log、Git 或訊息：真實密碼、完整 Cookie 值、API Key、JWT、
> 完整會員 email、會員照片。本文件描述憑證時只寫欄位名稱與長度，不寫內容。

---

## 一、【P0 資安】`OTP.env` 把實際的 SMTP 帳號密碼提交進版控了

### 現況

`lavien` 分支根目錄的 `OTP.env` 內含**真實值**（我們只檢查有無內容，沒有讀取或複製）：

| 欄位 | 狀態 |
| --- | --- |
| `SMTP_HOST` | 有值 |
| `SMTP_USER` | 有值 |
| `SMTP_PASS` | **有值（29 字元）** |
| `REDIS_HOST` / `REDIS_PORT` | 有值 |

同一批上傳的前一個 commit 是 **`Delete .gitignore`**。`.gitignore` 被刪掉之後，
`.env` 這類本來就該被排除的檔案就跟著進版控了。

### 為什麼要立刻處理

這把寄信密碼現在在 GitHub 的歷史裡。**把檔案刪掉不等於密碼失效**：

- 任何 clone 過這個 repo 的人，本機都還有完整歷史
- 就算之後重寫歷史，強制推送前的物件在 GC 完成前仍可能被取得

這與 issue #26（外洩的 Ollama 金鑰）是同一種狀況，唯一能真正解除風險的動作是**換發新密碼**。

另外提供一個值得優先確認的線索：**你們回報的「OTP 沒有寄出」，有可能就是這件事造成的。**
多數郵件服務商偵測到憑證外流時會直接停用該應用程式密碼，寄信會安靜地失敗。
這是推測不是結論，但換發密碼本來就要做，順序上先做這件事最划算。

### 請處理

1. **換發 SMTP 應用程式密碼**（舊的作廢），這一項最重要
2. 把 `.gitignore` 加回來，至少排除 `*.env`、`__pycache__/`、`*.msi`
3. 從版控移除 `OTP.env`，改提供不含真實值的 `OTP.env.example`
4. 順帶：`Redis.msi`（7 MB 安裝檔）也不該進版控，clone 會變慢

---

## 二、【P0 串接】新增的 `X-Gateway-Key` 我們沒有在送，這正是 7/27 全面 401 的原因

### 新版程式碼做了什麼

`app.py:62-97` 新增一道閘門，套用在四支「登入前」端點上：

```python
PUBLIC_ENDPOINTS = frozenset({
    "/api/register", "/api/send-otp", "/api/verify-otp", "/api/login",
    "/send-otp", "/verify-otp", "/register", "/login",
})

def _check_gateway_key():
    provided = request.headers.get("X-Gateway-Key", "")
    if not provided:
        if GATEWAY_KEY_LOOSE_MODE:      # 預設 true
            return None
        return error_response("UNAUTHENTICATED", "缺少 Gateway 認證", 401)
    ...
```

### 為什麼確定是這一項

7/27 實測到的症狀與這段程式碼的涵蓋範圍**完全吻合**：

| 端點 | 是否在 `PUBLIC_ENDPOINTS` | 7/27 實測 |
| --- | --- | --- |
| `/api/login` | 是 | 401 |
| `/api/register` | 是 | 401 |
| `/api/send-otp` | 是 | 401 |
| `/api/products` | **否** | 200（正常） |

用一個從未註冊過的全新信箱測 `/api/register` 也是 401，代表不是帳號狀態的問題。

版控裡沒有任何設定關掉寬鬆模式，所以判斷是**在執行環境把
`GATEWAY_KEY_LOOSE_MODE` 設成了 false**，而我們沒有收到通知，Gateway 也就沒有帶這個標頭。

### 我們已經先做好的部分

Gateway 已經加上送出邏輯：對會員資料庫的請求（登入、註冊、寄驗證碼、驗證碼確認、
以及 session 前置檢查）都會帶 `X-Gateway-Key`。目前**未設定金鑰時完全不送**，
行為與現在相同——刻意不送空字串，因為那會讓你們的 `hmac.compare_digest` 判定不符而回 401。

### 我們需要的

**請把金鑰的值給我們**（走私訊或密碼管理工具，不要貼進 issue 或文件）。
拿到後我們只要在 Cloud Run 設一個環境變數即可生效，不需要再部署一次。

另外請一併確認：

1. 標頭名稱確定是 `X-Gateway-Key`？
2. 你們讀的環境變數名稱是 `UPSTREAM_MEMBER_API_KEY`，我們這邊用同一個名字對應，可以嗎？
3. 目前執行環境的 `GATEWAY_KEY_LOOSE_MODE` 是 true 還是 false？

### 建議：預設值反過來

`GATEWAY_KEY_LOOSE_MODE` 目前預設 `true`（忘記設定 = 沒有防護）。
建議預設 `false`，過渡期由設定明確打開寬鬆模式。安全機制的預設值應該是「開」，
忘記設定時失敗總比忘記設定時沒有防護好。

---

## 三、其他發現（不急，但建議修）

### 3.1 `before_request` 有一層沒有作用的判斷

`app.py:93-96`：

```python
if request.path in PUBLIC_ENDPOINTS or request.path.startswith("/api/"):
    if request.path in PUBLIC_ENDPOINTS:
        return _check_gateway_key()
```

外層的 `or request.path.startswith("/api/")` 不影響任何結果，因為內層又檢查了一次同樣的條件。
留著會讓之後維護的人以為 `/api/*` 全都有檢查，實際上只有那四支。建議刪掉外層判斷。

### 3.2 CORS 的允許標頭沒有 `X-Gateway-Key`

`app.py:352` 與 `:363` 的 `Access-Control-Allow-Headers` 是
`Content-Type, Authorization, Cookie, X-Requested-With`，不含 `X-Gateway-Key`。

我們是伺服器對伺服器呼叫，不受 CORS 限制，所以**不影響目前的串接**。
但如果你們預期有瀏覽器會直接帶這個標頭，preflight 會擋掉。

---

## 四、順帶更正我們自己的錯誤

`給資料庫端_待修清單_2026-07-25.md` 與 issue #30 裡，我們把管理員帳號寫成
`admin@decoratme.local`（少一個 e）。**正確拼法是 `admin@decorateme.local`**，
已經更正。造成困擾抱歉。

同時，新版程式碼裡有：

```python
ADMIN_EMAIL_WHITELIST = frozenset({"admin@decorateme.local"})
```

代表這個帳號在你們那邊是被特別處理的、確實存在。所以 **issue #30
（後台改權限全部失敗）的「admin 這筆 row 讀不到」推論可以放棄了**，
真正的原因幾乎確定是第二節的 `X-Gateway-Key`。請不要花時間去查那筆 row。

---

## 五、驗收

金鑰設定完成後，我們會實際打這幾條確認：

```
POST /auth/register（全新信箱）   →  200
POST /auth/send-otp              →  200，且信箱確實收得到
POST /auth/login（正確帳密）      →  200
後台改會員權限並儲存               →  全部成功
```

在第二節解決之前，整站無法登入、無法註冊，Demo 完全無法進行。
