# 給 Ollama 端：待辦彙總規格（2026-07-25）

> 從《系統資安效率流程改善總表》抽出所有標 ⛔、且屬 Ollama／文字建議服務端的項目。
> 不含任何實際金鑰／Email／照片／權杖／完整資料包／完整生成提示。
> 我方（Gateway／Render）已完成的相關前置一併標註。

---

## 0. 我方目前狀態（對接前提）

- 正式 Gateway 的外部 Ollama tunnel **預設關閉**（`ALLOW_EXTERNAL_TEXT_UPSTREAM` 未開時，`/text-suggestion/*` 一律回 `EXTERNAL_TEXT_UPSTREAM_DISABLED`）——這是刻意的：不把臉部摘要／prompt 送到未受控的公開 tunnel。
- Render 服務會**自己**去跟建議服務要個人化的 `renderPromptEn`（`SUGGESTION_SERVICE_URL` + `/suggest`），前端不經手 prompt。要接上時，網址與金鑰設在 Render 的環境變數，不寫進程式碼、不進版控。
- 我方送給建議服務的請求只帶 `faceAnalysis`（結構化摘要）與 `style` 名稱，不帶原始照片。

---

## 1. 金鑰輪替（P0-7 / S5，高）

**規格**：
- 輪替所有曾出現在前端／文件／Log／備份／Git 歷史的金鑰，含 Ollama 端的服務金鑰（S5）。
- 新金鑰只放 Secret Manager／部署環境變數，不進程式碼、不進文件、不進聊天訊息。
- 輪替後確認舊金鑰失效（用舊金鑰呼叫回 401）。

**驗收**：舊金鑰全數失效；新金鑰只存在受控的 secret 儲存。

---

## 2. 服務身分限制、固定端點（P0-6，高）

**現況問題**：建議服務目前跑在組員的 Mac、走 Cloudflare Quick Tunnel，網址一重啟就換，且對外公開、任何人都打得到。

**規格**：
- Ollama 若要正式使用，改 Cloud Run／VPC／固定端點（不再用 `trycloudflare.com`）。
- 端點加身分驗證：只接受 Render 服務的請求（service account 或共享金鑰 `X-API-Key`，固定時間比較）。
- 匿名直連一律拒絕。

**驗收**：
- 匿名（無金鑰）呼叫 `/suggest` 回 401/403。
- 端點網址固定，不隨重啟改變。

---

## 3. Prompt 展示 Demo 後移除（P0-3，中）

**現況**：Demo 期間 Ollama 完整 Prompt 暫留在**受控展示回應**，方便專題展示「後端暫存資料」。

**規格**：
- 完整 Prompt **不得寫入一般 Log**（僅限受控展示回應）。
- Ollama 調整完成、或專題展示結束後，把完整 Prompt 從展示回應中移除。
- 對外的一般回應只回 `renderPromptEn`（要下給圖像模型的英文指令）與中文建議，不回內部推理過程。

**驗收**：一般 Log 無完整 Prompt；展示結束後展示回應也不再含完整 Prompt。

---

## 4. 回應契約（對接 Render，中）

**規格**（維持我方 Render 端已預期的格式）：
- `POST /suggest`，body：`{ "faceAnalysis": {...}, "style": "<風格中文名>" }`。
- 回 `{ "renderPromptEn": "<英文渲染指令>", "suggestion": "<中文建議>", ... }`。
- `renderPromptEn` 只負責「要上什麼妝」；**identity lock（不可改變長相）由 Render 端自己疊上去**，Ollama 不要決定「可不可以改長相」。
- 服務不可用時容許回空／錯誤，Render 會自動退回 styleId 白名單的固定 prompt，不讓渲染失敗。

**驗收**：帶結構化 `faceAnalysis` + `style` 呼叫，回得到可用的 `renderPromptEn`；服務關閉時 Render 仍能用固定 prompt 完成渲染。

---

## 回報格式

回覆修改檔案、端點網址（不含金鑰）、身分驗證方式、測試結果與部署／回滾步驟；不得附任何正式憑證或完整 Prompt。
