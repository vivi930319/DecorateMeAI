# Decorate Me 優化與改善清單

> 產出：2026-07-09
> 範圍：整個系統（前端 / 後台 / 後端 / 部署）的邏輯、健壯性、效能、成本、可維護性
> 圖例：**✅ 已完成（本輪）**／**🔧 我方可自己做**／**⛔ 需組員配合**／**🧭 需先決策**

---

## 本輪已完成（2026-07-09）

- ✅ 後台管理 TDZ 崩潰修復、saved-looks 前後端串接、後台顯示各會員收藏數＋點數
- ✅ 登入失敗自動重登（跨站 cookie 掉線的緩解）＋ 後台 401 一鍵重登
- ✅ 收藏時給明確雲端同步回饋（不再靜默失敗）
- ✅ 會員中心「後端判定／開通」等開發者字樣全數改為正常文案；移除 PRO Demo 死卡
- ✅ 效能：後台搜尋加 200ms debounce、自動刷新加 3 秒節流、5 支 JS 加 `defer` 並行下載
- ✅ JS 版本號手動 bump（本次已更新）

---

## 🔴 真正的邏輯不一致（最影響 demo 可信度）

### 1. 點數有「兩套數字」互相打架 🧭 需先決策
- 會員中心顯示 **localStorage 的 demo 點數**（`MemberRewards.getPoints`），後台顯示 **資料庫點數**（現在都 0）。
- 同一個會員：自己頁面可能看到 50 點、後台看到 0 點。整個成長系統（點數／簽到／任務／推薦碼）都是前端 localStorage，跟資料庫脫節。
- **選項**：(A) 把加點真的寫進 DB 的 points 端點（`POST points/adjust`）；(B) demo 前先把這些純前端功能藏起來，避免自打嘴巴。
- 建議：時程緊就先做 B，之後再做 A。

### 2. 登入狀態雙來源脫節 ⛔ 需組員配合（根治）
- 「我是誰」存 sessionStorage、「API 憑證」是跨站 cookie，兩者會不同步 → 「畫面在、但 API 401」。
- 已用自動重登緩解；根治要組員做 token（見 `MEMBER_DATABASE_SESSION_TOKEN_REQUEST.md`）。

### 3. 商品推薦 404 是「靜默失敗」 🔧 我方可自己做
- `recommendProducts` 打到 404 就回 `null`，使用者做完分析看不到推薦、也沒錯誤提示，只有空白。
- **修法**：失敗時顯示「推薦服務暫時無法使用」；並確認 `productUrl` 指向正確的推薦服務網址（目前 `/recommend-products` 在 `quickly-tend-sun-rod` 回 404，端點位置要跟商品組員確認）。

### 4. 分析歷史只存 localStorage ⛔ 需組員配合
- 換裝置／清快取就全沒了，「分析次數」統計也是本機。要真正可用得接 DB（需要組員建歷史表 + 端點）。

---

## 🟠 健壯性 / 錯誤處理

### 5. 大量 fetch 失敗「靜默回空」 🔧 我方可自己做
- `listProducts`、`recommendProducts`、`getRegisteredMember` 等失敗都回 null/空陣列，使用者分不清「沒資料」還是「壞了」。
- **修法**：統一封裝，失敗給明確訊息或重試提示。

### 6. JS 版本號要手動 bump，容易忘 🔧 我方可自己做
- `index.html` 的 `?v=` 靠手動改（「收藏沒進去」就是這個害的）。
- **修法**：部署腳本自動帶時間戳／檔案雜湊當 cache-buster，一勞永逸。

---

## 🟡 效能 / 成本

### 7. Cloud Run 冷啟動 → 第一次分析/渲染卡 10–30 秒 🔧（需 gcloud 指令）
- face-basic / face-pro / render 閒置會縮到 0 實例，冷啟動 + 載模型很慢。
- **Demo 前**：把三個服務設 `min-instances=1`，或口試前先各點一次暖機。
  ```
  gcloud run services update face-basic --region=asia-east1 --min-instances=1
  gcloud run services update face-pro   --region=asia-east1 --min-instances=1
  gcloud run services update replicate-render --region=asia-east1 --min-instances=1
  ```
  （注意：min-instances=1 會有常駐費用，demo 完可以改回 0。）

### 8. 後台逐會員抓 saved-looks + points = N×2 請求 🔧 我方可自己做
- 5 人 OK；會員一多要改成「點開某會員才抓那一位」。

### 9. 收藏 before 圖是 base64 存 DB 🔧/⛔
- 上傳前雖已壓縮（1024px / JPEG 0.78 / Web Worker，這塊做得好 ✅），但收藏存 DB 的 before 圖仍是 base64，每人上限 50 筆可能很肥。
- **修法**：before 圖也上 GCS 存網址，別把 base64 塞資料庫（需前後端一起改）。

### 10. render 重複請求可加去重 🔧 我方可自己做（可選）
- render 是唯一燒錢的外部呼叫（已加 API key + 每 IP 限流 ✅）。可再加：同圖同 prompt 短時間重複就回上次結果，避免使用者狂按重跑。

---

## ⚪ 可維護性

### 11. 巨檔與重複邏輯 🔧 我方可自己做（工程較大）
- `router.js` 3000+ 行、`api.js` 也大；有多個收藏入口重複邏輯。
- **修法**：按模組拆檔（auth / admin / product / member），之後維護更輕鬆。

---

## 建議處理順序

1. **Demo 前必做**：第 7 項（暖機 / min-instances）——最有感、零風險。
2. **可信度優先**：第 1 項（點數矛盾，先決策）、第 3 項（推薦 404 給提示）。
3. **低風險我方可順手做**：第 5（統一錯誤訊息）、第 6（版本號自動化）。
4. **等組員**：第 2（token）、第 4（歷史接 DB）。
5. **有空再還**：第 8/9/10（效能）、第 11（拆檔）。
