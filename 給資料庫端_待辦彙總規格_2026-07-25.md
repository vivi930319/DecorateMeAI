# 給資料庫端：待辦彙總規格（2026-07-25）

> 從《系統資安效率流程改善總表》抽出所有標 ⛔、且屬資料庫端的項目，整理成單一可對接文件。
> 不含任何實際金鑰／Email／照片／權杖／完整資料包。
> 我方（Gateway／前端）已完成的相關前置一併標註，方便你知道對接點在哪。

---

## 0. 我方目前狀態（對接前提）

- 所有前端請求走同源 Gateway；瀏覽器只帶 `HttpOnly` session cookie，不再直連資料庫網址。
- 寫入一律帶 `X-Expected-Actor`（跨分頁帳號隔離）；管理員寫入另需 `X-CSRF-Token`（double-submit）。
- Gateway 會把「操作者身分」以 `X-User-ID`（opaque actor id，雜湊 email）帶給上游；**請以此為主鍵，不要信 path/body 的 email／role／owner。**
- 登入限流：我方只計「失敗」登入，且辨識不出來源 IP 時不設 IP 維度（見 §7）。
- access log 已遮罩 email；一般 log 不記 body／token／照片。

---

## 1. 會員硬刪除：可重跑的 deletion job（P0-2，高）

**現況問題**：目前刪除可能 Admin 收到 200 就當全部完成，但跨服務（GCS、Render job、Face job、payload）不一定真的清乾淨。

**規格**：
- 刪除改成可追蹤狀態機：`pending → deleting → completed / failed`，回一個可重試的 `deletionJobId`。
- 一次刪除要涵蓋：會員主資料、session、點數、任務、主題兌換、Face job、Render job、分析欄位、收藏、暫存／原圖／渲染圖／payload。
- 開始刪除時：**停用帳號＋撤銷所有 session＋禁止建立新工作**（避免邊刪邊產生新資料）。
- 任一步失敗即保留 `failed` 狀態並可重試，**不得回報假成功**；失敗要能自動重試並告警。
- 完成後逐項核對「查不到資料與物件」；只留不含個資的稽核證明。

**驗收測試**：
- 刪除後逐項確認查不到（自動化整合測試，需有效帳號端到端跑）。
- 中途故障後 job 停在 `failed` 且可重跑，不留半清狀態。

---

## 2. 立即撤銷 session：`sessionVersion` / `tokenVersion`（P0-4，高）

**規格**：
- 會員資料增加 `sessionVersion`（或 `tokenVersion`）欄位。
- 登出、停權、降權、改密碼、刪除帳號時把它 +1，使所有舊 session／token 立即失效。
- 與《給資料庫端_多重Session多帳號分頁規格》一致。

**驗收**：帳號被登出／停權／降權／刪除後，舊 session 立即回 401/403。

---

## 3. 會員 API 改用 memberId / `/me`（P0-3，中）

**規格**：對外接口逐步由 `/api/members/{email}/…` 改為 `/me` 或不透明 `memberId`，從源頭避免 email 進 URL（access log／備份不再殘留會員名冊）。

**驗收**：新接口路徑不含 email；舊路徑保留期間 Gateway 仍會遮罩，但新路徑要能完全取代。

---

## 4. Job token 只存雜湊（P0-8，高）

**規格**：資料庫只保存 Job token 的 HMAC／雜湊，不存明碼；token 在工作過期／取消／刪除後立即失效。

**背景**：我方 Gateway 已做到「Render 輪詢要 token＋擁有者都對」，但 token 若明碼落庫，一次外洩就能讀結果。

**驗收**：DB 內查不到明碼 token；過期／取消後拿舊 token 查詢回 403/404。

---

## 5. 不再保存舊公開 URL、改存 object path（P0-1 / P1，高）

**規格**：
- 資料庫只存 GCS 的 object path，**不存永久公開網址**。
- 讀圖時由後端驗證擁有者後簽 5～15 分鐘短效 Signed URL；Signed URL 不落庫、不寫 log。
- 確認清掉所有既存的舊公開 URL 欄位。

**驗收**：DB 內無 `storage.googleapis.com/...` 之類的永久公開網址；讀圖一律經簽名短效網址。

---

## 6. 工作資料分層與敏感 payload 自動清除（P1，高）

**規格**：
- 分開四類：一般 Log／Job 索引／敏感 payload／永久妝容紀錄。
- `jobs/{jobId}` 只放非敏感摘要；`job_payloads/{jobId}` 限 Worker 存取。
- 以內部不可變 `userId` 關聯工作，**不用 email 當主鍵**；前端只取摘要，不取完整 landmarks／prompt。
- 敏感 payload 設 `expiresAt` 自動清除：完成／取消／失敗逾期／會員刪除後即清。

**驗收**：一般查詢拿不到完整 landmarks／prompt；payload 到期後自動消失。

---

## 7. 登入限流以真實使用者為準（實測問題，高）

**現況**：使用者實測回報「不同帳號、不同裝置也被 429」。若資料庫端也有自己的登入限流，且它看到的來源永遠是 Gateway 的單一 egress IP，就會把所有人算成同一來源一起鎖。

**規格**：
- 若資料庫端有登入限流，請以 `X-Forwarded-For` 的**真實使用者位址**（或 Gateway 帶的 actor id）計算，不要用連線 IP。
- OTP 加 email／IP／裝置／每日總量限制（防濫發）。

**我方對接**：Gateway 回上游 429 時已改用 `MEMBER_SERVICE_RATE_LIMITED` 區分是哪一層在限流。

---

## 8. 點數／歷史／收藏／購物車端點（第五節，中）

**現況**：這些資料目前只在前端 localStorage，換裝置就沒。

**規格**：
- **購物車**：Gateway 側 `api/members/{email}/cart` 已放行並加授權測試（只能存取自己的車，別人 403）；**等你把 cart 端點建起來就會通**。
- **收藏**：`favorites` 欄位若為 `String(500)` 需擴充（存不下多筆）。
- **分析歷史**：新增端點，供跨裝置讀取。
- **點數**：成長系統改接 DB 會員加點端點，統一「會員中心」與「後台」兩套數字（目前 localStorage vs DB 不一致）。
- 全部走登入 session、以 actor id 授權、具 idempotency（不得重複扣點／發獎）。

---

## 9. renderQuota 真正來源定案（第五節，中）

**現況**：目前只有 server-side rate limit，未做會員綁定配額。

**規格**：定案 `renderQuota` 存哪（member DB／render 呼叫會員 API／後端簽 token 驗證），並提供讀寫端點；額度依可信 `userId` 計，不信前端數字。

---

## 10. Admin 權限儲存 bulk 化（第五節，中）

**現況**：逐筆 `PATCH /api/members/{email}` 中途失敗會半成功。

**規格**：改 bulk 端點一次寫多筆，或明確回每筆成功／失敗清單，讓前端能顯示「哪幾筆沒寫進去」。

---

## 11. 效率：縮圖、批次端點、索引、健康檢查（P1／效率，中）

- 商品資料庫建 320／640／1280 縮圖（供前端 `srcset`／`sizes`）。（與爬蟲端協作）
- 補回商品稽核紀錄端點；Admin 改批次 summary 端點避免 N+1。
- 建 `memberId`／`createdAt`／`status` 索引。
- `/health` 不做 job 清理或大量掃描；另建 `/ready`；查詢走索引不掃全表；Firestore TTL＋Cloud Scheduler 分頁清理；監控 read/write 費用。

---

## 12. 搬離 trycloudflare、schema migration、隱私（P0-6／P2，中）

- 會員／商品資料庫由 `trycloudflare.com` 搬到固定 Cloud Run／正式網域（tunnel 網址會變、且不受控）。
- schema migration 可重跑、可回復。
- 隱私告知：蒐集目的、保存期限、刪除方法、第三方 AI 處理者；提供刪單筆紀錄與刪帳號功能；Demo 與正式資料用不同專案或 Bucket。

---

## 13. 註冊冪等 / EMAIL_EXISTS 區分（實測問題，中）

**現況**：使用者用亂碼 email 註冊也回「此信箱已註冊」——推測資料庫在 `register` 當下就建了未驗證列，重試就撞 `EMAIL_EXISTS`。

**規格**：
- 註冊對「未驗證帳號」要能重跑（允許重新註冊或重寄 OTP），或 `EMAIL_EXISTS` 明確區分「已驗證」與「未驗證」，前端才能給對的引導。
- 與 S7（OTP 假信箱）一併處理。

---

## 回報格式

回覆修改檔案、migration、API contract、測試結果與部署／回滾步驟；不得附任何正式憑證。
