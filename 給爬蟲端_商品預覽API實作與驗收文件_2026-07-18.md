# 給爬蟲端：商品預覽 API 實作與驗收文件

建立日期：2026-07-18  
系統：Decorate Me  
正式前端：<https://decorate-me.web.app/#admin>

目前資料庫／商品／爬蟲服務 Base URL：

```text
https://yen-minute-from-ict.trycloudflare.com
```

網址更新原則：Cloudflare Tunnel 每次更換後，以最新提供的網址為唯一有效來源；`memberDatabaseUrl`、`productUrl`、`crawlerUrl` 必須同步替換，不得繼續呼叫或自動退回舊網址。

## 一、目前問題

Admin 後台「爬蟲匯入」已完成前端介面，但正式商品後端尚未提供爬蟲預覽 API。

2026-07-18 實測結果：

| API | 結果 |
|---|---:|
| `GET /health` | `200` |
| `GET /api/products` | `200` |
| `POST /api/crawler/product-preview` | `404` |
| `POST /crawler/product-preview` | `404` |

因此，目前輸入 Chanel 或其他商品網址時，前端只能顯示：

```text
商品後端尚未部署爬蟲預覽 API
```

這不是前端網址欄位錯誤，也不是 Chanel 單一網站的問題，而是正式後端沒有這支路由。

---

## 二、爬蟲端必須實作的 API

```http
POST /api/crawler/product-preview
Content-Type: application/json
```

用途：

1. Admin 輸入單一商品頁網址。
2. 後端抓取並解析商品資料。
3. 回傳預覽資料。
4. 不可直接把商品寫入正式商品資料庫。
5. Admin 確認或補齊欄位後，才透過商品管理 API 新增商品。

### 請求格式

```json
{
  "url": "https://www.example.com/product/123",
  "source": "manual_admin_import",
  "adminId": "admin@example.com"
}
```

欄位：

| 欄位 | 類型 | 必填 | 說明 |
|---|---|---:|---|
| `url` | string | 是 | 完整的 `http` 或 `https` 商品頁網址 |
| `source` | string | 否 | 前端固定傳送 `manual_admin_import` |
| `adminId` | string/null | 否 | 相容用欄位，不可只靠此欄位判斷 Admin |

注意：`adminId` 由前端送出，可能被偽造。後端必須依登入 Session、JWT 或可信任的 Gateway 身分驗證 Admin。

---

## 三、成功回應格式

HTTP 狀態：`200 OK`

```json
{
  "success": true,
  "status": "ok",
  "message": "商品資料已建立預覽",
  "data": {
    "sourceUrl": "https://www.example.com/product/123",
    "sourceSite": "example.com",
    "sourceProductId": "123",
    "productName": "商品名稱",
    "brand": "品牌名稱",
    "price": 1650,
    "currency": "TWD",
    "description": "商品說明",
    "imageUrl": "https://www.example.com/images/main.jpg",
    "imageUrls": [
      "https://www.example.com/images/main.jpg"
    ],
    "category": "lipstick",
    "specs": {
      "color": "Rose",
      "volume": "3.5g"
    },
    "missingFields": [],
    "lastCrawledAt": "2026-07-18T16:30:00+08:00"
  }
}
```

### 前端會使用的欄位

| 後端欄位 | 前端用途 |
|---|---|
| `productName` 或 `name` | 商品名稱 |
| `brand` | 品牌 |
| `price` | 價格，必須是數值 |
| `currency` | 幣別，例如 `TWD` |
| `description` | 商品描述 |
| `imageUrl`／`imageUrls[0]` | 預覽圖片 |
| `category` | 帶入商品分類 |
| `sourceUrl` | 商品來源網址 |
| `specs` | 規格預覽 |
| `missingFields` | 提醒 Admin 補資料 |

---

## 四、部分成功回應

頁面可抓取，但缺少品牌、價格或分類時，不要整筆回傳失敗。

HTTP 狀態：`200 OK`

```json
{
  "success": true,
  "status": "partial",
  "message": "部分欄位缺漏，請由管理員補齊",
  "data": {
    "sourceUrl": "https://www.example.com/product/123",
    "sourceSite": "example.com",
    "productName": "商品名稱",
    "brand": null,
    "price": null,
    "currency": "TWD",
    "description": "商品說明",
    "imageUrls": [],
    "category": null,
    "specs": {},
    "missingFields": [
      "brand",
      "price",
      "imageUrl",
      "category"
    ]
  }
}
```

前端收到 `status: "partial"` 後，會顯示「需要補資料」，並允許 Admin 將現有欄位帶入商品表單。

---

## 五、錯誤回應格式

錯誤回應必須保持一致：

```json
{
  "success": false,
  "error": {
    "code": "UNSUPPORTED_SITE",
    "message": "目前不支援此商品網站",
    "detail": "optional detail"
  }
}
```

### 錯誤碼

| HTTP | code | 使用情況 |
|---:|---|---|
| 400 | `INVALID_URL` | URL 格式錯誤、非 HTTP(S)、不是允許的網域 |
| 401 | `UNAUTHORIZED` | 未登入 |
| 403 | `ADMIN_REQUIRED` | 不是 Admin 或沒有爬蟲權限 |
| 404 | `NO_PRODUCT_FOUND` | 頁面存在但找不到商品資料 |
| 408/504 | `FETCH_TIMEOUT` | 目標網站逾時 |
| 422 | `UNSUPPORTED_SITE` | 網站尚未支援 |
| 422 | `PARSE_FAILED` | 頁面可取得但無法解析 |
| 429 | `RATE_LIMITED` | 超過 Admin 爬取額度 |
| 502 | `SCRAPE_BLOCKED` | 遭到目標網站阻擋、驗證頁或機器人防護 |
| 500 | `CRAWLER_INTERNAL_ERROR` | 爬蟲內部錯誤 |

不要只回傳 HTML 錯誤頁或純文字 traceback。

---

## 六、Chanel 等品牌網站的處理

Chanel 等網站可能使用 JavaScript 動態載入、地區跳轉、Cookie、機器人防護或商品結構化資料。

建議解析順序：

1. 驗證 URL 與重新導向後的最終網址。
2. 解析 JSON-LD `Product`。
3. 解析 Open Graph：`og:title`、`og:image`、`og:description`。
4. 解析網站專用 selector。
5. 必要時才使用受控的 Headless Browser。
6. 無法取得時回傳 `SCRAPE_BLOCKED` 或 `UNSUPPORTED_SITE`，不可偽造商品資料。

測試網址必須使用真正的單一商品頁，不能使用分類頁、活動頁或搜尋結果頁。例如使用者截圖中的網址：

```text
https://www.chanel.com/tw/fragrance-beauty/special-editions/...
```

看起來可能是活動或分類頁，而非單一商品頁。即使 API 完成，也應回傳：

```json
{
  "success": false,
  "error": {
    "code": "NO_PRODUCT_FOUND",
    "message": "此頁不是可辨識的單一商品頁"
  }
}
```

---

## 七、資安要求

### SSRF 防護

- 只允許 `http`、`https`。
- 禁止 `localhost`、`127.0.0.1`、`::1`。
- 禁止私有網段、link-local 與 metadata IP。
- DNS 解析後再次檢查實際 IP。
- 每次重新導向都要重新驗證網域及 IP。
- 限制重新導向次數。
- 禁止 `file://`、`ftp://`、`data:` 等協定。

### 下載限制

- 連線與讀取必須有 timeout。
- 限制 HTML 最大下載大小。
- 不下載影片或任意二進位檔案。
- 圖片只保存 URL，不在這支預覽 API 下載並永久保存。
- 設定每位 Admin、每個來源網域與全系統 rate limit。

### 權限與紀錄

- 後端必須驗證 Admin，不能只信任 `adminId` 或 email header。
- Log 可記錄 request ID、Admin 內部 ID、來源網域、結果與耗時。
- Log 不可記錄 Session、JWT、API key 或完整頁面內容。
- URL 若含敏感 query parameter，寫 Log 前必須清除或遮罩。

---

## 八、CORS 與正式環境

正式前端來源：

```text
https://decorate-me.web.app
```

後端需允許：

```http
Access-Control-Allow-Origin: https://decorate-me.web.app
Access-Control-Allow-Credentials: true
Access-Control-Allow-Headers: Content-Type, Authorization
Access-Control-Allow-Methods: POST, OPTIONS
```

如果使用 Cookie Session：

```text
Secure
HttpOnly
SameSite=None
```

不可在正式環境使用：

```http
Access-Control-Allow-Origin: *
Access-Control-Allow-Credentials: true
```

---

## 九、前端目前的呼叫方式

前端設定：

```javascript
crawler: {
  baseUrl: RuntimeApiConfig.crawlerUrl || RuntimeApiConfig.productUrl || '',
  previewPath: '/api/crawler/product-preview'
}
```

前端會發送：

```javascript
fetch(`${baseUrl}/api/crawler/product-preview`, {
  method: 'POST',
  credentials: 'include',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    url: sourceUrl,
    source: 'manual_admin_import',
    adminId: currentAdminEmail
  })
});
```

爬蟲端只要依本文件完成路由，前端不需要再更改 API 路徑。

---

## 十、部署前驗收清單

- [ ] `OPTIONS /api/crawler/product-preview` 回應正確 CORS header。
- [ ] 未登入呼叫回傳 `401`。
- [ ] 一般會員呼叫回傳 `403`。
- [ ] Admin 呼叫有效商品網址回傳 `200 + status=ok`。
- [ ] 缺少部分欄位回傳 `200 + status=partial`。
- [ ] 非商品頁回傳 `NO_PRODUCT_FOUND`。
- [ ] 不支援網站回傳 `UNSUPPORTED_SITE`。
- [ ] 被網站阻擋回傳 `SCRAPE_BLOCKED`。
- [ ] 逾時回傳 `FETCH_TIMEOUT`。
- [ ] 私有 IP／localhost URL 被拒絕。
- [ ] 回應不包含頁面完整 HTML、Cookie 或 Token。
- [ ] API 部署到前端目前使用的 `productUrl`，或另提供正式 `crawlerUrl`。
- [ ] Firebase 正式前端實測可以看到商品預覽。
- [ ] 只有 Admin 確認後才新增到商品資料庫。

---

## 十一、完成後通知前端組

請提供以下資訊：

```text
1. 正式 crawler base URL：
2. API 路徑：POST /api/crawler/product-preview
3. 支援的網站清單：
4. 不支援的網站與原因：
5. 是否使用 Cookie Session／Bearer Token：
6. CORS 是否已加入 https://decorate-me.web.app：
7. Rate limit：
8. Timeout：
9. 部署版本／Commit：
10. 測試成功的單一商品網址：
```

完成以上資訊後，前端組即可進行正式串接驗收。
