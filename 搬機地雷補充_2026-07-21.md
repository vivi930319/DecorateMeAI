# 搬機地雷補充清單（2026-07-21）

這份是 `專案交接手冊_搬機版_2026-07-17.md` 的補充。
手冊寫於 7/17，之後 7/18～7/21 又踩到一批坑，這裡只記**手冊沒寫、而且新機器一定會撞到**的部分。

搬機時的閱讀順序：先看這份的第 1、2 節（不看會白花半天），再回去看原手冊的第 8 節安裝流程。

---

## 1. 兩個資料夾、同一個 GitHub repo、不同分支

這是最容易在新機器上把東西弄壞的一件事。

| 本機資料夾 | 內容 | 目前分支 | 遠端 |
|---|---|---|---|
| `Desktop/PythonProject12` | 後端（Gateway、Face API、訓練） | `Isa` | `github.com/vivi930319/DecorateMeAI.git` |
| `Desktop/web_frontend` | **前端唯一來源** | `dev_makeup` | 同一個 repo |

兩個資料夾推到**同一個 GitHub repo 的不同分支**。後果：

- `PythonProject12` 裡面**也有**一個本機 `dev_makeup` 分支，追蹤 `origin/dev_makeup`，
  而 `origin/dev_makeup` 裝的是**前端的內容**。
  在後端資料夾 `git checkout dev_makeup` 會把工作區換成前端檔案。**不要這樣做。**
- 新機器 clone 的時候，只 clone 一次是不夠的，也不能 clone 同一個 URL 就當兩份用。
  正確做法：clone 兩次到兩個資料夾，各自 checkout 自己的分支。

```bash
git clone https://github.com/vivi930319/DecorateMeAI.git PythonProject12
cd PythonProject12 && git checkout Isa

git clone https://github.com/vivi930319/DecorateMeAI.git web_frontend
cd web_frontend && git checkout dev_makeup
```

另外 `main` 分支（`f356ee3 Remove iOS from platform flow`）跟這兩條線都不同步，
不要以為 main 是最新的。`origin` 上還有 `Amy`、`dev`、`lavien` 是別的組員的線。

**歷史被改寫過**：`backup/devmakeup-before-rewrite` 顯示 `ahead 82, behind 82`，
`backup/isa-before-rewrite` 也還在。舊的 clone 直接 pull 會衝突，新機器請用全新 clone。

---

## 2. `.gitignore` 是「全部封鎖，逐一放行」

`PythonProject12/.gitignore` 第一行就是：

```gitignore
*
```

**預設所有檔案都不進 git**，後面每個 `!xxx` 才是放行。這代表：

> 你在新機器上新增任何檔案，除非手動加一行 `!檔名` 到 `.gitignore`，
> 否則 `git status` 是乾淨的、`git add` 也加不進去，檔案永遠不會上 GitHub。
> 下次再搬機就沒了，而且沒有任何錯誤訊息會提醒你。

要確認一個檔案為什麼沒被追蹤，用：

```bash
git check-ignore -v 你的檔名
```

它會直接告訴你是被哪一行擋掉的。要強制加入用 `git add -f`，但記得補 `.gitignore` 放行規則，
不然之後任何人重新 clone 都拿不到。

已經發現的失效規則（不影響現況，但會誤導）：

- `!api_errors_test.py` — 這個檔案**根本不存在**，是規劃過但沒寫的測試。
- `!給資料庫端_會員Session完整接口規格_Cline版_2026-07-21.md` — 實際檔名是 **`Cursor版`**
  （7/21 把對接對象從 Cline 改成 Cursor 時改了檔名，但 `.gitignore` 沒跟著改）。
  該檔目前是追蹤中的，因為 gitignore 管不到已追蹤檔案；但只要哪天它被移出索引，
  這條規則就救不回來。

檔案尾端還有一條 `*.md` + `!README.md`，會**蓋掉上面所有針對 .md 的個別放行**
（這是刻意的，註解有寫）。所以新增的說明文件預設都不上 GitHub，
要保留就得把 `!檔名.md` 加在**檔案最後面**那條 `*.md` 之後。這份文件就是這樣加的。

---

## 3. 新機器 clone 完「還缺什麼」

clone 下來是跑不動的。以下都不在 git 裡，必須另外帶：

### 3.1 `PythonProject12/.env`（後端秘密）

不在 git（正確做法，別加進去）。需要以下 10 個 key，值請從舊機器或密碼管理器帶：

```
IMAGE_PROVIDER=
REPLICATE_API_TOKEN=
REPLICATE_MODEL=
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=
OLLAMA_BASE_URL=
OLLAMA_MODEL=
MEMBER_DATABASE_URL=
PRODUCT_DATABASE_URL=
PRODUCT_ADMIN_API_KEY=
```

`MEMBER_DATABASE_URL` / `PRODUCT_DATABASE_URL` 是 trycloudflare 網址，**會過期**，見第 5 節。

### 3.2 `web_frontend/config.local.js`

被 gitignore 擋掉，只有 `config.local.example.js` 在 git 裡。複製一份改名即可。
現在這個檔案**已經不放網址了**（上游網址改由 Gateway 的 `/public-config` 下發），
內容只剩一個開關：

```js
window.DECORATE_ME_CONFIG = { allowInsecureOtpBypass: false };
```

如果你在新機器上看到舊版本裡有 `memberDatabaseUrl` / `productUrl`，那是舊寫法，不要沿用。

### 3.3 `models/basic_features_roi/dinov2_vits14.onnx`（88 MB）

刻意不入版控（Meta 的公開模型）。新機器用 `tools/export_dinov2_heads.py` 重新匯出。

同理，`*.pt` 訓練權重也不在 git，**只有 `*.onnx` 推論權重進版控**。
要重新訓練才需要 `.pt`，一般部署不需要。

注意 dinov2 shadow 分類頭**只有 3 個五官有**：`eye` / `face` / `nose`。
`brow`（眉）和 `lip`（唇）沒有 dinov2 head，只有傳統 CNN 的 `.onnx`。
不是檔案掉了，是本來就沒訓練，別花時間找。

### 3.4 資料集與臉部照片

`data/`、`style-images-dev-makeup/`、`training-backup-*/` 都不在 git（敏感資料）。
原手冊第 2.3 節有說明。

---

## 4. Firebase Hosting 的兩個坑（7/20～7/21 才踩到）

### 4.1 Hosting 會過濾 cookie，session 會憑空消失

Firebase Hosting **只轉發特定的 cookie**，其他一律丟掉。
現在的解法是：把 session 塞進那個唯一會被轉發的 cookie 裡一起帶
（commit `a32f8ff` "Carry the session in the one cookie Firebase forwards"）。

**症狀長這樣**：本機測完全正常，一部署到 `decorate-me.web.app` 就一直 401、一直要重登。
如果新機器上又看到這個症狀，**先想 Hosting 的 cookie 過濾，不要去改後端登入邏輯** —— 那邊沒問題。

### 4.2 已停用的前端副本還躺在磁碟上

`PythonProject12/firebase-hosting-full/` 和 `firebase-hosting-hotfix/` **都已經廢棄**，
git 裡一個檔案都不追蹤了（commit `252c284` "Drop the two retired frontend copies"），
`firebase-hosting-full/` 底下還放了一份 `DEPRECATED_請勿部署.md`。

但 `.gitignore` 裡**還留著十幾行針對它們的放行規則**，看起來像還在用。不要被騙。

> **前端唯一來源是 `Desktop/web_frontend`**，部署走那邊的 `deploy.ps1` / `firebase.json`。

新機器上建議直接不要複製這兩個資料夾過去。

---

## 5. trycloudflare 通道網址會換

資料庫端（會員／商品）走 Cloudflare quick tunnel，**網址每次重開就換**，舊網址直接 DNS 查不到。

歷史上已經換過至少 5 次（`component-developmental-disclaimer-count`、
`stainless-exit-spot-providing`、`debut-exemption-lounge-depending`、
`ser-agree-tyler-diagnostic`、`fact-mice-prince-voltage`…），
每次都表現成「商品載入中卡住」或「登入打不通」，看起來很像程式壞了。

**連不上資料庫時，第一件事是跟資料庫端要現在的 tunnel 網址**，不要先 debug 程式。

驗證指令：

```bash
curl -I https://目前的-tunnel.trycloudflare.com/api/products
curl -I -H "Origin: https://decorate-me.web.app" https://目前的-tunnel.trycloudflare.com/api/products
```

第二條要看得到 `Access-Control-Allow-Origin: https://decorate-me.web.app`
和 `Access-Control-Allow-Credentials: true`，否則 CORS 沒放行。

換址之後要更新的地方：Cloud Run 上 `ai-gateway` 的 `MEMBER_DATABASE_URL` /
`PRODUCT_DATABASE_URL` 環境變數（**不是**前端設定檔了，前端已經改成跟 Gateway 拿）。

`ai_gateway_test.py:255` 有一條測試在擋「設定裡不可以出現 trycloudflare.com」，
就是為了避免有人又把 tunnel 網址寫死進程式。

**根治方向**（還沒做，`Gateway_API管理架構與流程總覽` 第 364 行也列著）：
請資料庫端改用固定網址（Cloud Run 或付費 ngrok）。搬機是重談這件事的好時機。

---

## 6. 專案放在 OneDrive 底下

完整路徑是 `C:\Users\user\OneDrive - 淡江大學\Desktop\PythonProject12`。

兩個後果：

1. **部署可能上傳到舊檔**。OneDrive 同步有延遲，改完檔案立刻 deploy，
   有機會傳到還沒同步完的版本。歷史紀錄裡已經發生過，所以才會有
   「`curl https://decorate-me.web.app/js/api.js` 確認線上是新版」這個驗收步驟。
   **部署後一定要用 curl 驗線上內容，不要只看本機檔案。**
2. **Git 曾經被 OneDrive 弄壞過**。磁碟上還留著證據：
   `.git.corrupt-20260717-140358/`、`.git-repair-clone/`、
   `git-metadata-backup-20260717-135716.zip`（8.6 MB）。

新機器**強烈建議把專案移出 OneDrive**（例如 `C:\dev\DecorateMe`），
需要備份就靠 GitHub。路徑裡的中文和空白（`淡江大學`、`OneDrive - `）
也常常讓工具鏈出問題，一併解決。

---

## 7. 兩個 Python 虛擬環境

- `.venv` — 跑服務用（`requirements.txt`）
- `.venv-train` — 訓練用（torch 等重依賴）

分開是刻意的，訓練依賴不要污染部署環境。
虛擬環境**不可跨機器複製**，新機器兩個都要重建，見原手冊 8.3 節。

還有 `.mpl-cache/`、`.training-cache/` 是快取，不用帶。

---

## 8. 搬機驗收：最短路徑

原手冊第 10 節有完整清單，這裡是最快能確認「沒搬壞」的五件事：

```bash
# 1. 兩個 repo 各自在對的分支
cd PythonProject12  && git branch --show-current   # 要是 Isa
cd ../web_frontend  && git branch --show-current   # 要是 dev_makeup

# 2. 秘密檔案都補回來了
test -f PythonProject12/.env && echo ok
test -f web_frontend/config.local.js && echo ok

# 3. 大模型檔在
ls -l PythonProject12/models/basic_features_roi/dinov2_vits14.onnx

# 4. 後端測試過
cd PythonProject12 && python -m pytest ai_gateway_test.py

# 5. 線上服務還活著（tunnel 是否又換址）
curl -s https://decorate-me.web.app/public-config
```

最後一條回傳的 JSON 裡會有目前的上游網址，可以一眼看出 tunnel 是不是又死了。

---

## 附：這份文件本身

`.gitignore` 是全封鎖模式，所以這份文件已經在檔案最後加了放行規則：

```gitignore
!搬機地雷補充_2026-07-21.md
```

**沒有那行的話，這份文件不會進 GitHub，搬機時就跟著舊電腦一起消失了。**
之後再寫任何要跨機器保留的文件，記得比照辦理。
