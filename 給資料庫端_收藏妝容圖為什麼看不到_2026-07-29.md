# 給資料庫端 — 收藏妝容（saved_looks）的圖為什麼「看不到」

**日期：** 2026-07-29
**提出方：** DECORATE ME 前端 + AI Gateway
**背景：** 後台看得到使用者的渲染妝容，但資料庫端說看不到資料。

---

## 0. 先講結論

**資料在你們那邊，沒有遺失。** 後台顯示的收藏就是打你們的
`GET /api/members/{email}/saved-looks` 拿到的——後台沒有第二個資料來源。

看不到的是**圖**，不是資料。原因是：

> `saved_looks` 存的是**一段網址**，不是圖片。
> 而那段網址指向我們的 Gateway，**需要登入中的會員 session 才打得開**。
> 直接在瀏覽器貼上或用 curl 打，一律回 401。

---

## 1. 我們實際送給你們什麼

```http
POST /api/members/{email}/saved-looks
Content-Type: application/json
Cookie: __session=...           ← 會員登入 session
```

```json
{
  "style": "港風",
  "afterImageUrl": "https://decorate-me.web.app/media/render/3f2a91c8d5e74b06a1cc82e4f7b03d19",
  "beforeImageUrl": "",
  "analysisSummary": {
    "faceShape": "鵝蛋臉", "browShape": "一字眉", "eyeShape": "鳳眼",
    "noseShape": "標準鼻", "lipShape": "薄唇", "skinSeason": "夏"
  }
}
```

四個欄位就這些。**沒有任何圖片二進位資料、沒有 base64。**

---

## 2. `afterImageUrl` 那段網址是什麼

```
https://decorate-me.web.app/media/render/<32 位十六進位的 job id>
```

它由 **AI Gateway** 提供，不是靜態檔案。打進來時 Gateway 會：

1. 驗會員 session（`require_member_access`）
2. 檢查這個 job 的擁有者是不是你，或你是不是管理員
3. 通過才 302 轉址到一個 **10 分鐘有效**的 GCS 私有網址

所以：

| 你怎麼打 | 結果 |
| --- | --- |
| 瀏覽器貼上網址（沒登入） | **401** |
| curl / Postman（沒帶 session cookie） | **401** |
| 用別人的帳號登入後打 | **403**（不是擁有者） |
| 在已登入的前端頁面裡 `<img src>` | ✅ 正常顯示 |

**這就是「看不到」的直接原因。** 圖是私有的，設計上就不能被匿名存取——
它是使用者的臉。

---

## 3. `beforeImageUrl` 幾乎都是空字串（這是設計，不是壞掉）

妝後圖由渲染服務產生，一定有網址。
**妝前圖是使用者自己上傳的照片，系統裡沒有任何管道能讓它變成網址**——
Gateway 只有 `GET /media/render` 與 `/media/legacy`，**沒有上傳端點**。

所以妝前圖在前端永遠是 `data:image/...;base64,...`（常常 90 萬字元），
而 `saved_looks.beforeImageUrl` 是 `String(500)`。我們刻意**不送**它：

- 送了會超長，被你們擋掉，整筆收藏寫不進去
- 使用者的原始臉部照片不進伺服器，也符合隱私方向

> 這裡有過一次事故（我方 S57）：先前連妝前圖一起做網址檢查，
> 結果**每一筆收藏都在送出前被自己擋掉**，畫面顯示一切正常，資料庫一筆都沒進。
> 已修，現在妝前圖沒網址就留空，不影響整筆寫入。

如果你們希望妝前圖也存得到，那需要另外設計上傳流程與同意機制，
**不能從這條路悄悄加進來**。

---

## 4. 圖會過期，資料列不會

兩層保留期，都在我們這邊：

| 對象 | 保留期 | 說明 |
| --- | --- | --- |
| render job 紀錄 | **1 小時**（`RENDER_JOB_RETENTION_SECONDS`） | 使用者按下收藏時會呼叫 `/retain` 把它保住 |
| GCS 圖片物件 | **30 天**（`GCS_RENDER_RETENTION_DAYS`） | retained 的才會留到 30 天 |

所以會出現這種情況：

> `saved_looks` 有一列，`afterImageUrl` 也在，但圖已經不在了 → `/media/render/<id>` 回 404。

前端對這個情況有處理，會把破圖換成「此妝容圖已過期」。
**如果你們在資料庫看到一堆 URL 打不開，先確認是 401（沒登入）還是 404（真的過期）——
這兩件事的意義完全不同。**

---

## 5. 你們可以怎麼自己驗證

### 5.1 確認資料真的在

```sql
SELECT id, member_email, style, after_image_url, before_image_url, created_at
FROM saved_looks ORDER BY created_at DESC LIMIT 20;
```

`after_image_url` 應該長得像 `https://decorate-me.web.app/media/render/<32 hex>`。
**看得到這一列，就代表資料是完整的。**

### 5.2 確認圖打得開（需要一個登入中的 session）

最簡單的做法是在瀏覽器登入 DECORATE ME 之後，直接在同一個瀏覽器分頁貼上那段網址。
會員只能看自己的；管理員可以看全部。

用 curl 的話要帶 `__session` cookie，而那是 HttpOnly 的，
**請不要為了測試把它複製貼上到任何地方**——換一個瀏覽器分頁測就好。

---

## 6. 需要你們確認的兩件事

1. **`saved_looks` 的列數與後台顯示的一致嗎？**
   後台完全依賴 `GET /api/members/{email}/saved-looks`，如果數量對不上，
   那是查詢或權限的問題，請告訴我們。

2. **`before_image_url` 大量為空，你們的驗證會不會擋？**
   目前我們送空字串。如果你們的欄位是 NOT NULL 或有 URL 格式驗證，
   請確認空字串是被接受的——先前 `INVALID_AFTER_IMAGE_URL` 那類驗證曾經擋掉整筆寫入。

---

## 7. 相關端點一覽

| 方法 | 路徑 | 用途 |
| --- | --- | --- |
| GET | `/api/members/{email}/saved-looks` | 列出收藏（後台與前台都用這支）|
| POST | `/api/members/{email}/saved-looks` | 新增收藏 |
| DELETE | `/api/members/{email}/saved-looks/{id}` | 刪除收藏 |

全部經由 AI Gateway 轉送，帶會員 session；我們不會直接從瀏覽器打你們的服務。
