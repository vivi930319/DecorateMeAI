# GitHub 工程化成果（2026-07-10）

> Decorate Me 專題 · repo：`vivi930319/new_poject`
> 目的：提升 GitHub 版控與專業度（配合得獎/評分）

---

## 一、分支整理（Before → After）

| | Before | After |
|---|--------|-------|
| 分支數 | 12（含空/重複/死/雜項） | **6（全是實際模組）** |
| 預設分支 main | 空的（只有 1 個 yetta 檔） | **專案總覽門面 + 架構圖 + 分工表** |

**刪除的分支**（皆先記錄 SHA 可還原）：
- `yetta`、`isa`（小寫舊）、`latest_branch`：空/死/雜項
- `Yunn`、`fafa`、`Iris`：經確認後刪除

**保留的 6 個分支：**
| 分支 | 模組 | 負責 |
|------|------|------|
| `main` | 專案總覽 + LICENSE | — |
| `Isa` | 臉部分析 + AI 渲染後端 | 你 |
| `dev_makeup` | 網頁前端 | 你 |
| `lavien` | 會員/商品/推薦資料庫 | lavien |
| `Amy` | Ollama 文字建議服務 | amy |
| `dev` | iOS App | Fafa |

---

## 二、README

- **`main`**：新增專案總覽（系統架構圖、模組分工表、核心技術、各分支連結）。
- **`Isa`（後端）**：從 1739 行的每日日誌，重寫成乾淨系統 README（架構圖、端點、技術、安全、環境變數、部署、專案結構）。舊日誌完整備份在本機。
- **`dev_makeup`（前端）**：新增乾淨 README（架構、技術、專案結構、部署）。

---

## 三、版本與 Issue

- **v1.0 tag（合併版）**：標記各模組整合的第一個完整版本。
- **6 個代表性 Issue**：4 個功能/修正已完成（Closed）、2 個 bug 追蹤中（Open）。

---

## 四、Git 衛生與資安

- **只留程式碼/資產**：文件檔（docx/pdf/drawio/日誌）移出版控。
- **資安掃描**：目前追蹤的程式無金鑰外洩、`config.local.js` 與 `.env` 皆未進版控。
- **後端 `tools/` 整理**：ML 資料工程腳本歸類，服務程式留根目錄。
- **commit 訊息規範**：採 `feat/fix/docs(scope):` 格式。
- **加入 MIT LICENSE**。

---

## 五、後續待辦（分工）

| 事項 | 負責 |
|------|------|
| 到 Releases 按 Publish，把 v1.0 tag 變成正式 Release | 你 |
| 清掉 Amy 分支的 `.venv`（1900+ 檔）與 exe（教學已提供） | Amy |
| 輪換曾進 git 歷史的 Ollama 金鑰 `textSuggestionApiKey` | Ollama 組員 |
| 其他分支各補一份乾淨 README | 各組員 |
