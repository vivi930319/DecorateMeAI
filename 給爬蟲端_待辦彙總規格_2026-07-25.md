# 給爬蟲端：待辦彙總規格（2026-07-25）

> 從《系統資安效率流程改善總表》抽出所有標 ⛔、且屬爬蟲／商品資料服務端的項目。
> 不含任何實際金鑰／Email／照片／權杖。
> 我方（Gateway）已完成的相關前置一併標註。

---

## 0. 我方目前狀態（對接前提）

- Admin 商品操作經 Gateway `proxy_admin_request`：後端已確認 `role=admin`，並要求 `X-CSRF-Token`（double-submit）；一般會員不得寫商品。
- Gateway 對商品／爬蟲寫入的稽核已上線（`admin_audit.py`，只記雜湊 actor 與商品 ID，不記內容）。
- Render 端的 `fetch_remote_image_bytes` 已擋 SSRF：只允許 https＋白名單 host（`replicate.delivery`／GCS）＋443 埠＋不跟隨轉址、串流逐塊卡上限。**這是渲染端的圖片抓取**；商品圖片來源驗證仍需爬蟲／商品端自己做（見下）。

---

## 1. 管理員輸入的商品資料 schema 驗證（P1，高）

**規格**：對管理員／爬蟲寫入的商品欄位做嚴格 schema 驗證，拒絕不合規資料：
- **圖片／來源 URL**：只允許 `https`；只允許白名單圖片 host（或經 image proxy）；拒絕內網位址（`10.*`／`192.168.*`／`169.254.169.254` 等）與非圖片副檔名；不跟隨轉址（防 SSRF）。
- **名稱／描述**：長度上限、去除控制字元；輸出到前端前一律 escape（我方前端已對商品欄位 escape，但來源端也應清洗）。
- **價格／ID／分類**：型別與範圍驗證；ID 白名單字元集。

**驗收**：
- 帶內網位址或非 https 的圖片 URL 被拒。
- 帶 `<script>` 的名稱／描述不會造成 XSS（前後端各一道）。
- 非法型別／超長欄位回明確的 `VALIDATION_ERROR`。

---

## 2. 後端 SSRF／host 驗證（P1，高）

**規格**：任何「後端去下載一個外部 URL」的行為（抓商品圖、預覽爬蟲結果）都要：
- 只允許 https＋白名單 host＋443 埠。
- 不跟隨轉址（白名單 host 也可能 302 轉到內網）。
- 串流下載並逐塊卡位元組上限與像素上限，不整包吞進記憶體。
- 解碼驗證是真圖片（magic bytes＋實際解碼），拒絕偽 MIME。

（可參考我方 `replicate_render.fetch_remote_image_bytes` 與 `image_safety.py` 的做法。）

**驗收**：對內網位址、轉址、超大回應、偽圖片的請求都被擋。

---

## 3. 商品縮圖 320／640／1280（效率，中；與資料庫端協作）

**規格**：
- 爬到／存商品圖時產生 320／640／1280 三種寬度縮圖，供前端 `srcset`／`sizes` 使用。
- 外部圖經 image proxy／CDN 轉 WebP／AVIF。
- 首屏主圖標 `fetchpriority="high"`（前端負責，來源端提供多尺寸即可）。

**驗收**：商品 API 回傳多尺寸網址；前端能依螢幕寬度取對應尺寸。

---

## 4. 補回商品搜尋預覽端點（第五節，中）

**現況**：前端有商品搜尋／爬蟲預覽入口，但後端端點曾缺失（`/api/crawler/search-preview`、`product-preview`）。

**規格**：
- 恢復搜尋預覽與商品預覽端點，或若不再提供則明確回應讓前端移除入口（不要留無後端的死按鈕）。
- 端點經 Gateway，套用 Admin 驗證與 CSRF。
- 回應格式統一 `error.{code,message,retryable}`。

**驗收**：搜尋預覽端點可用並經 Admin 驗證；未授權呼叫回 403。

---

## 5. 搬離 trycloudflare（P0-6，中）

**規格**：爬蟲服務由 `trycloudflare.com` 搬到固定 Cloud Run／正式網域（tunnel 網址會變、且不受控）。

**驗收**：端點網址固定；匿名直連受限。

---

## 回報格式

回覆修改檔案、端點契約、驗證規則、測試結果與部署／回滾步驟；不得附任何正式憑證。
