.idea/
node_modules/
dist/
build/
.env
.env.*
*.log
frontend_server*.log
.DS_Store
Thumbs.db
__pycache__/
.pytest_cache/

# Firebase 本機快取
.firebase/
.claude/

# 文件檔一律不進 git（只留程式碼）；但 README.md 放行當專案門面
*.md
!README.md
!前端完整交接手冊_搬機版_2026-07-17.md
*.docx
*.pdf
*.drawio
*.pptx
docs/
config.local.js
*.bak-before-split

# 進場動畫的版本比較稿，只在本機看，不進版控也不部署。
# 先前被 f02d02c 移除過又回來——因為沒有寫進這裡，任何一次 git add -A 都會把它撿回去。
進場動畫比較.html

# demo_makeup_flow/ 是這個前端的另一份完整副本（54 檔、15MB），只用於展示流程；
# firebase.json 與 .dockerignore 都已排除它，不會部署。放進版控等於把整個前端
# 存兩份，而其中一份不會被任何人維護。
demo_makeup_flow/

# 風格圖的未壓縮原檔（每張約 1.9MB，五張共 8MB）。實際使用的是 .webp 版本，
# firebase.json 還設了 301 把舊的 .png/.jpg 導過去。這幾張沒有被任何
# js/html/css 引用過——確認過才加這條。原檔要保存請放設計素材庫，不要放 repo：
# 二進位檔進了 git 歷史就拿不掉，而它們比整個原始碼還大。
style-hongkong.png
style-japanese.png
style-korean.jpg
style-rich.png
style-yandere.png

# 跨品牌部署的暫存副本（css/js 各一份），由手動部署流程產生，不是原始碼。
# 沒寫進來的話，任何一次 git add -A 都會把整份前端的副本撿進版控。
.deploy_cross_brand/

# 商品圖：2592 張、41MB，刻意不進 git。
#
# 它**必須存在於本機**才能部署（firebase.json 的 public 是 "."，部署是整份取代，
# 本機沒有就等於把線上那份刪掉——2026-09-23 已經發生過一次）。
# 但 41MB 的二進位檔進版控太重，所以改放 GCS：
#   gcloud storage rsync -r gs://decorate-me-datasets/site_assets/product-images product-images
#
# 不 ignore 的話它會永遠讓 git status 顯示未追蹤，而 deploy.ps1 要求乾淨工作目錄，
# 結果是每次部署都得加 -AllowDirty——那個開關是留給緊急狀況的，不該變成日常。
# deploy.ps1 另有一道檢查會比對線上清單，確保它真的在本機，所以 ignore 掉不會漏。
# 取得方式與事故經過見 docs/product-images-recovery.md。
product-images/
