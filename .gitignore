# 只上傳 API 主程式與 Docker 部署檔，其餘本地檔案一律忽略
*

!.gitignore
!.dockerignore
!.gcloudignore
!.gcloudignore.gateway
!.gcloudignore.render
# CI 設定要進版控才會在 GitHub 上跑（deny-all 底下目錄要逐層放行）
!.github/
!.github/workflows/
!.github/workflows/ci.yml
# 只放行根目錄那一份。`!README.md` 沒有斜線，git 會拿它去比對每一層——
# docs/README.md 與 training/README.md 因此也被放行，而 docs/README.md 是
# 那 103 份未入版控文件的索引：推上 GitHub 就是一整頁指向不存在檔案的連結，
# 剛好把「這裡只留 README」這個決定拆掉。加斜線把它綁回根目錄。
!/README.md
!Face_analyzer_BASIC.py

# ROI CNN（BASIC 五官分類）：訓練腳本、共用裁切定義、shadow 推論、訓練歷程
!face_roi.py
!basic_roi_shadow.py
!eye_features.py
# 五官判斷回饋（BASIC 與 PRO 都 import）。
#
# 這個 repo 有三份「* 全擋 + 逐項放行」的清單：.gitignore、.dockerignore、.gcloudignore。
# 新增一支會被 import 或 COPY 的檔案時，三份都要加。少了這一份最難發現——
# 本機跑得動、gcloud builds submit 也上得去（它從本機磁碟打包），
# 但檔案根本沒進版控，任何人重新 clone 下來兩支 analyzer 都會 ImportError。
!face_feedback.py
!face_corrections.py
!prepare_roi_cache.py
!train_basic_cnn_roi.py
!predict_basic_roi.py
!eval_rule_baseline.py
# md 文件預設不上 GitHub —— 訓練歷程/紀錄書/規格書等留在本機與組內流通。
# 例外只有兩份，都在上方／下方明確放行：
#   README.md
#   臉部分析模型_完整發展歷程規格書_2026-07-29.md
#     └ 2026-07-29 使用者決定：臉部模型的歷程要有單一份可讀通的紀錄，且要進版控。
#       它整併了 CNN訓練歷程、模型訓練記錄、DINOv2規格書、隱私說明等多份文件的內容。
#       ⚠️ 這份文件記載了一個**尚未修補**的權限缺口（管理員可讀任何會員的妝前原圖，
#          且該路徑沒有稽核）。目前 repo 是 private 所以沒問題，
#          **在 未套用_渲染圖稽核與妝前圖權限_20260729.patch 套用之前，這個 repo 不可轉為 public。**
# 模型權重（.onnx / .pt）刻意不入版控：二進位檔每次重訓都會讓 repo 永久肥一份。
# 部署不受影響（Cloud Build 走 .gcloudignore 從本機上傳）；要重建模型就重跑
# tools/build_identity_map.py -> prepare_roi_cache.py -> train_basic_cnn_roi.py。
!Face_analyzer_PRO.py
!Ollama_suggestion.py
!dev_server_utils.py
!cloud_start.py
!Dockerfile
!docker-compose.yml
!requirements.txt
!backend_smoke_test.py
!ollama_suggestion_smoke_test.py
!analysis_package.py
!job_store.py
!api_errors.py
# 高風險管理操作的稽核紀錄（只存雜湊 actorId，不存 PII）
!admin_audit.py
# 上傳圖片的共用安全處理（分段讀取／magic bytes／EXIF 清除），四個服務共用
!image_safety.py
!image_safety_test.py
# 五官修正回饋的測試：它產出的是訓練資料與線上信任分數的分母，兩邊都不能靜默壞掉。
!face_feedback_test.py
# 目錄本身要先放行，否則裡面逐檔案的 ! 規則不會生效——git 不會走進一個
# 被排除的目錄去找例外。tools/ 與 training/ 上面已經是這樣寫的。
!tests/
# CI 的 backend-tests 靠它組出容器裡那個扁平的 import 命名空間。
# 這個檔一直沒進版控，於是 CI 上 `import ai_gateway` 永遠 ModuleNotFoundError，
# 而本機因為讀得到它所以全綠——本機過、CI 掛，找起來最花時間的那一種。
!pytest.ini
# 訓練批次寫回的測試：釘住「批次失敗會推出告警指標」。這行要是被拿掉，
# 症狀是失敗時**沒有任何人收到通知**——沉默不會讓任何測試變紅，所以要測。
!training_run_store_test.py
# 訓練機的睡眠抑制：重點是**有沒有還原**。忘了還原的話這支常駐程式活著的期間
# 電腦永遠睡不著，而那是沒有人會發現、只會覺得電池很快沒電的壞法。
!training_worker_test.py
# 貢獻樣本的讀取與刪除：動的是真的臉部影像，而且刪除不可逆。
# 比對規則錯一個字元就是刪掉別人的臉。
!face_contributions_test.py
# 去識別化彙總：這支產的檔案會離開這棟樓，該沒有的東西真的要沒有。
!export_feedback_aggregate_test.py
# 修正快取：舊分類名稱沒正規化會一路流到 prompt 而且不報錯。
!face_corrections_test.py
# 眼型幾何：旋正與比例不變性。壞掉的樣子是分類悄悄變差，不會報錯。
!eye_features_test.py
# 使用者同意提供的訓練樣本（只存部位 ROI，不存原圖）。face_feedback 會 import。
!face_contributions.py
!replicate_render.py
!replicate_render_api.py
!requirements.render.txt
!Dockerfile.render
!ai_gateway.py
!ai_gateway_test.py
!render_api_test.py
!api_errors_test.py
!gcs-lifecycle.json
!requirements.gateway.txt
!Dockerfile.gateway
!cloudbuild.gateway.yaml
!deploy_render_cloudrun.ps1
!tools/
!tools/*.py
!tools/face_models_manifest.json
# 訓練機的啟動器。它是被 Windows 工作排程器叫起來的，換一台電腦要重新設排程，
# 所以這支必須跟著 repo 走，不能只留在本機。
!tools/start_training_worker.ps1
# 重建訓練機排程。設定原本只活在這台機器的工作排程器裡，而其中兩項是實測
# 出來的（Once 看門狗、Duration 留空），換機器時查文件查不到。
!tools/setup_training_task.ps1
# 部署腳本共用的錯誤診斷。三支部署腳本都 dot-source 它，少了它們連解析都過不了。
!tools/deploy_helpers.ps1
# training/ 跟 tools/ 一樣是原始碼，不是產物，所以整個資料夾的 .py 都要進版本控制。
#
# 先前這裡沒有放行，而這份 .gitignore 是「* 全擋」的白名單，於是 training/ 底下只有
# 被 `git add -f` 硬塞進去的 9 支在版本控制裡，其餘 10 支都不在——包括 _bootstrap.py
# 與 _dataset.py，而那兩支正是已追蹤的腳本 import 的東西。clone 下來會 ModuleNotFoundError。
# 2026-08-23 的 commit「Add one readable training script per model」新增的五支
# basic_*_shape.py 也一支都沒進去，那個標題因此名不副實（2026-08-24 發現並補上）。
!training/
!training/*.py
!deploy_face_cloudrun.ps1
!deploy_gateway_cloudrun.ps1
!setup.bat
!start_backend_local.bat
!start_full_stack_local.bat

# 搬機與交接文件／前端無秘密設定範本
# firebase-hosting-full/ 是 web_frontend 的另一份副本（63 檔、20MB），兩份會互蓋。
# 這些放行規則從來沒有生效過——目錄下 0 個檔案進過版控。真正在部署的是
# OneDrive 底下那份 web_frontend（它有自己的 repo）。留著放行只會讓某天有人
# `git add .` 就把 20MB 的重複前端推進歷史，而歷史刪不掉。
# 需要的話請到 web_frontend 那個 repo 取用。
!firebase-hosting-full/public/pages/checkin.html
!firebase-hosting-full/public/pages/admin.html
!firebase-hosting-full/public/js/
!firebase-hosting-full/public/js/config.js
!firebase-hosting-full/public/admin_api_contract_test.js
firebase-hosting-full/public/.gitignore
firebase-hosting-full/public/README.md

# 系統改善清單需要納入版本控制，完成項目才能長期追蹤。

# 本機新手教學文件，不上傳 Git
local_only_notes/

# 除下列例外，所有 Markdown 都只留在本機，不上傳 GitHub。
# 規則放在檔案最後，覆蓋上方曾為交接文件設定的個別例外——
# 所以新的例外也必須加在這裡，加在上面沒有用（後面的規則優先）。
*.md
# 只放行根目錄那一份。`!README.md` 沒有斜線，git 會拿它去比對每一層——
# docs/README.md 與 training/README.md 因此也被放行，而 docs/README.md 是
# 那 103 份未入版控文件的索引：推上 GitHub 就是一整頁指向不存在檔案的連結，
# 剛好把「這裡只留 README」這個決定拆掉。加斜線把它綁回根目錄。
!/README.md

# 五官分類模型。
#
# ⚠️ 2026-07-31：**ONNX 不再入版控**。
#
# 上面第 38 行本來就寫了「模型權重刻意不入版控」，但這裡的白名單又放行
# *_shape.onnx —— 兩條規則互相矛盾。MobileNetV3 只有 6MB，所以一直沒撞到牆；
# 換成 ConvNeXt-Tiny（106MB）之後 push 直接被 GitHub 的 100MB 單檔上限擋下。
#
# 依原本的政策修正，理由不只是大小：
#
#   1. ONNX 是**建置產物**，由 .pt 權重與訓練腳本重新產生，不是原始事實
#   2. 部署走 Cloud Build 從**本機磁碟**打包（.gcloudignore），不經 git ——
#      git 裡有沒有它們，對線上完全沒有影響
#   3. 真正的持久化產物是 **Artifact Registry 的映像**，它含模型且由 tag 版本化，
#      要回滾就用 gcloud run deploy --image=<舊 tag>
#
# 代價（要知道）：**fresh clone 沒有模型檔，服務起不來**。
# 重建方式見規格書 §十；或直接從 Artifact Registry 拉一個既有映像。
#
# *_classes.json 與 *_dinov2_head.npz 仍然入版控 —— 它們是**契約**（類別順序）
# 與小體積權重，而且 classes.json 決定線上輸出的標籤，必須可追溯。
!models/
!models/basic_features_roi/
!models/basic_features_roi/*_classes.json
!models/basic_features_roi/*_dinov2_head.npz

# 模型部署紀錄：搬機與交接都需要，納入版控。

# 搬機地雷清單：交接手冊（7/17）之後才踩到的坑，換機器一定要帶。

# 會員端點數／打卡／任務／推薦碼規格：對資料庫端的正式需求，要能追蹤版本。

# 待修清單（Cursor 執行版）：實測後的完整版，取代上一份的第 2～4 節。

# 隱私說明：臉照存在哪、誰看得到。口試會問，搬機也要帶。

# 後端完整技術文件：架構／端點／資安機制／環境變數的單一權威參考，納入版控。

# Demo 前設定與走查清單：Demo 前一天照著跑，納入版控。

# 待修清單（7/25）：登入修好之後實測到的三項，接續 7/22 那份，要能追蹤版本。

# 7/27 P0：會員端點全面 401，整站登入註冊歸零。事故紀錄要留在版控裡。

# 7/26 資料庫端新版程式碼的審查結果：SMTP 憑證外洩與 X-Gateway-Key 串接。

# 文字建議服務接入規格：給跑 Ollama 那台的同學，含金鑰輪替要求（#26）。

# Agent 設定：issue tracker 位置、triage 標籤、domain docs 採單一 context。
# 換機器或換人接手時，沒有這份就得重新摸索一次。
!CLAUDE.md

# 2026-08-26：GitHub 上只留 README.md。
#
# 先前這裡有三十幾條逐檔案的 .md 放行，再加一條 `!/*.md` 把根目錄整批收進來——
# 結果是 103 份工作文件（交接紀錄、給組員的規格書、實驗紀錄）都在公開的 repo 裡，
# 而那不是任何人當初決定的，是規則長出來的。
#
# 文件現在依用途分在 docs/ 底下（見 docs/README.md），留在本機與組內流通，不入版控。
# 例外只有三種，都在上面明確放行：README.md（GitHub 首頁）、CLAUDE.md 與
# docs/agents/*.md（這兩者是 agent 的設定，不是文件——少了它們，重新 clone 之後
# 工具不知道這個專案的規則）。

# Ollama 資料包契約：schemaVersion 規則與每個欄位的語意，跨端對接的權威來源。

# 膚色可信度旗標 labReliable：講明什麼時候 lab 不可以拿去比色號。
# 推薦端照這份實作，出錯就是粉底配錯色號，要能追蹤版本。

# 給演算法端的對接前提與待辦，含 job 失敗三種狀態碼的區分
# （只有 FACE_IMAGE_UNUSABLE 的 message 可以原樣顯示給使用者）。

# 跨端規格與修改單：對接對象照這些實作，內容都是介面約定，沒有個資也沒有金鑰。

# Agent 設定指向的三份文件。CLAUDE.md 已入版控卻參照到未入版控的檔案，
# 換機器就會讀到「檔案不存在」。
!docs/
!docs/agents/
!docs/agents/*.md

# 搬機環境地雷：PowerShell 版本、Big5 編碼、防毒攔憑證、私有 Cloud Run 認證。
# 2026-08-06 全端部署那天卡住的每一件事都在這裡，換機器不看會照撞一次。

# 跨類別重複清單：要給標註端逐組判讀「這張到底屬於哪一類」。
# 資料集本身不入版控，但這份是待辦清單不是資料，只有檔名與雜湊前綴。
!data/
!data/basic_full/
!data/basic_full/跨類別重複清單_*.csv

# 固定保留測試集：跨版本比較的唯一基準，**一定要入版控**。
# 它就是「同一份考卷」——換機器、換人接手都必須拿到同一份，否則分數不能互相比較，
# 整套評估流程就退回「每次換考卷」的狀態（見 tools/build_holdout_split.py 的說明）。
# 檔案裡只有 sha256、相對路徑與標籤，沒有影像。
!data/roi_cache/
!data/roi_cache/holdout_split_*.json
# v1 的作廢說明，跟切分檔放在一起才不會有人先看到 json 就拿去用。

# 各版本模型在保留集上的分數，累積比較用。
!models/holdout_scores.json

# ── 2026-08-06：根目錄文件改為預設入版控 ──────────────────────────────
#
# 原本的政策是「所有 Markdown 只留在本機」，再逐份加 ! 放行。那是把 GitHub 當備份
# 在想。但 GitHub 這個 repo 的用途是**給評審看**——文件本身就是成果的一部分，
# 藏在本機等於沒做。逐份放行也擋不住實際需求：真的要給人看的文件會愈來愈多，
# 每加一份就要改一次這個檔，遲早有人忘記，而忘記的症狀是靜默的。
#
# 所以改成：根目錄的 .md 預設收，例外逐份排除（就在這條下面）。
# `/` 開頭代表只管根目錄，子目錄（docs/、tmp/、.pnpm-store/、local_only_notes/、
# .venv/ …）不受影響，維持原本的全擋。

# ⚠️ 唯一的例外：MEMBER_DATABASE_SECURITY_ISSUE.md 刻意**不**入版控。
# 它報的是「GET /api/members 無權限驗證、會員個資全公開」，而它證明的方式是把當時
# 實測到的回應原樣貼進去——裡面有真實姓名、真實 Gmail、真實電話。
# 推上公開 repo 等於親手完成那份報告要阻止的外洩；去識別化又會讓證據失去說服力。
# 所以整份留在本機，要對外講這個問題請引用 GitHub issue，不要引用這個檔案。
/MEMBER_DATABASE_SECURITY_ISSUE.md

# 工作過程的交接、回報與現況文件留在本機，不進 GitHub。
# 它們是寫給當下那次協作看的（「今日變更」「待重測清單」「現況與剩餘」），
# 過幾天就對不上實際程式碼，留在 repo 裡只會讓後來的人讀到過期的事實。
# 需要對外講的內容請開 issue 或寫進正式規格書。
/交接_2026-08-14_今日變更與組員重測清單.md
/今日工作與現況_2026-08-14.md
/全系統審查_2026-07-10.md
/前端文字總表_2026-08-14.md
/實驗紀錄_模型訓練.md
/流程優化建議_2026-07-08.md
/五官分類交叉驗證報告_2026-08-13.md
/給Ollama端_五官欄位讀不到_2026-08-14.md
/給Ollama端_渲染指令的分工與格式_2026-08-15.md
/給演算法端_每類主推一件與80分門檻_2026-08-14.md
/給演算法端_膚色可信度降級的回報欄位_2026-08-14.md
/給資料庫端_商品顏色資料遺失回報_2026-08-13.md
/隱私說明_渲染圖公開讀_給新手.md
/ollama_personalized_advice_database_architecture.md
/recommendation_system_design.md

# ── 2026-08-08：原始檔補進版控 ────────────────────────────────────────
#
# 盤點時發現根目錄有 48 個檔沒進版控，不是因為判斷過不該進，是因為第一行的 `*`
# 預設全擋、而這些從來沒人想到要加 `!`。其中有系統架構圖的**原始檔**與跑實驗的
# 腳本——那些是原始碼，掉了要重畫、重寫。
#
# 加進來之前掃過三項：真實金鑰格式、寫死的 `KEY = "值"`、內嵌的資料庫或通道網址，
# 都沒有。（`.drawio` 裡的「X-API-Key」是箭頭上的標籤文字，不是金鑰。）

# 系統架構圖與介面示意圖的原始檔。匯出的 PNG 不能改，這個才能。
!/*.drawio

# 跑各次實驗的驅動腳本。訓練紀錄寫的是結論，這些是「當時到底下了什麼指令」，
# 少了它們，紀錄裡的數字就沒辦法照原樣重跑一次。
!/run_*.sh

# 早期與週邊的 Python 腳本。逐份列出而不是寫 `!/*.py`：
# 那樣的話往後任何一個隨手寫的暫存腳本都會自動進版控，包括還沒檢查過的。
# 這個 repo 的預設是「擋」，要放行就該是明確的動作。
!/Face_test.py
!/audit_proposal_docx.py
!/build_drawio_wireframes.py
!/check_csv.py
!/rerun_basic_from_csv.py
!/train_basic_features.py
!/train_rf_classifiers.py

# 仍然不入版控，理由各不相同：
#   .env                     真的線上金鑰（Replicate／OpenAI／商品後台）
#   *.zip                    18 MB 的二進位備份，git 對它沒有壓縮優勢
#   *.log                    實驗的原始輸出，結論已經在訓練紀錄與 holdout_scores.json
#   未套用_*.patch           還沒套用的修補，套用後應該是 commit 而不是檔案

# 保留集的說明文件。上面的 `*.md` 把它們一起擋掉了，而它們正是那份 json 的
# 使用說明：v1 的檔案還在，但已經作廢，唯一寫著「不要用它」的地方就是這份 md。
# 只有 json 進版控、說明留在本機，等於把一顆已知有問題的基準交給下一個人。
!data/roi_cache/holdout_split_*.md
