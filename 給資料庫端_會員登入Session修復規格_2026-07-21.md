# 給資料庫端：會員登入 Session 修復規格

> 日期：2026-07-21
>
> 正式前端：<https://decorate-me.web.app>
>
> 目前會員資料庫入口：<https://programmers-planners-convenient-had.trycloudflare.com>
>
> 目的：修復會員登入後，Gateway 無法持續讀取會員資料而回傳 `/auth/login 503` 的問題。

## 一、先說結論

目前問題不在登入頁排版，也不是前端是否保存帳號。正式前端只呼叫同源 Gateway：

```text
瀏覽器
  → POST /auth/login
  → AI Gateway
  → POST {會員資料庫}/api/login
  → 會員資料庫回傳會員資料 + Set-Cookie
  → Gateway 帶同一個 Cookie 呼叫 GET {會員資料庫}/api/members/{email}
  → 成功後 Gateway 才建立自己的 HttpOnly Session
```

現在失敗點位於「會員資料庫登入後的 Session 建立或驗證」。Gateway 必須在登入完成後，使用會員資料庫回傳的 Cookie 立即讀回同一位會員；若做不到，就不會讓前端進入會員頁。

因此請資料庫端先檢查：

1. `/api/login` 是否穩定回傳 `200`。
2. 成功登入是否真的帶有 `Set-Cookie` 回應標頭。
3. 使用該 Cookie 呼叫 `/api/members/{email}` 是否立即回傳 `200`。
4. Tunnel、伺服器重啟或多程序執行時，Session 是否仍能被下一個請求辨識。
5. 會員查詢端點是否額外要求了 Gateway 目前未提供的 API Key。

## 二、資料庫端必須符合的 API 契約

### 1. 登入

```http
POST /api/login
Content-Type: application/json

{
  "email": "demo@example.com",
  "password": "使用者輸入的密碼"
}
```

成功回應必須同時具備以下兩項：

```http
HTTP/1.1 200 OK
Set-Cookie: member_session=<高熵隨機值>; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=7200
Content-Type: application/json
```

```json
{
  "success": true,
  "member": {
    "email": "demo@example.com",
    "role": "member",
    "status": "active"
  }
}
```

必要規則：

- `member.email` 必須存在，且忽略大小寫後要和登入 email 相同。
- 會員物件也可使用欄位名稱 `user`；但建議固定使用 `member`。
- Cookie 名稱可自訂，但登入成功一定要有 `Set-Cookie`。
- Cookie 內容不可直接使用 email、密碼或可猜測流水號。
- 不要只在 JSON 回傳 token 而沒有 `Set-Cookie`；現行 Gateway 使用 Cookie Session。
- 錯誤帳密回 `401`；停權或禁止登入可回 `403`；不要把帳密錯誤回成 `200`。

### 2. 登入後讀取會員

Gateway 會立刻使用登入回傳的 Cookie 呼叫：

```http
GET /api/members/demo%40example.com
Accept: application/json
Cookie: member_session=<登入時收到的值>
```

有效 Session 必須回：

```http
HTTP/1.1 200 OK
Content-Type: application/json
```

回應可以是會員物件或既有資料庫格式，但不可因為請求來自 Gateway、沒有瀏覽器 Origin，或沒有前端 API Key 而拒絕已具有效 Cookie 的請求。

無效、過期或已登出的 Cookie 應回 `401`；無權讀取其他會員時應回 `403`。不要把驗證失敗回成 `500`。

### 3. 登出

登出時應在資料庫端撤銷該 Session，並清除 Cookie：

```http
Set-Cookie: member_session=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0
```

登出後再次使用舊 Cookie 呼叫會員端點，必須回 `401`。

## 三、Session 建議保存方式

建議資料庫新增 Session 儲存區，至少包含：

| 欄位 | 用途 |
|---|---|
| `session_hash` | 只保存 Session 隨機值的雜湊，不保存原始 Cookie 值 |
| `member_id` | 對應會員主鍵，不直接以 email 當秘密 |
| `created_at` | 建立時間 |
| `expires_at` | 到期時間 |
| `revoked_at` | 登出、停權或刪除會員時撤銷 |
| `last_seen_at` | 選用，用於追蹤有效工作階段 |

若服務使用多程序、多台主機或會重新啟動，不可只把 Session 放在單一 Python 程序記憶體；否則登入請求和下一次會員查詢落在不同程序時就會失效。可使用既有 PostgreSQL、Redis 或其他共用 Session Store。

## 四、資料庫端自行驗收方式

請只使用 Demo 測試帳號，不要把正式密碼寫進文件、Git 或終端截圖。

```powershell
$dbBase = 'https://programmers-planners-convenient-had.trycloudflare.com'
$loginBody = @{ email = 'DEMO_EMAIL'; password = 'DEMO_PASSWORD' } | ConvertTo-Json

$login = Invoke-WebRequest `
  -Uri "$dbBase/api/login" `
  -Method Post `
  -ContentType 'application/json' `
  -Body $loginBody `
  -SessionVariable memberSession

$login.StatusCode
$login.Headers['Set-Cookie']

$encodedEmail = [uri]::EscapeDataString('DEMO_EMAIL'.ToLowerInvariant())
$member = Invoke-WebRequest `
  -Uri "$dbBase/api/members/$encodedEmail" `
  -Method Get `
  -WebSession $memberSession

$member.StatusCode
```

合格結果：

- 第一次登入：`200`。
- 登入回應：存在 `Set-Cookie`。
- 相同 Cookie 立即讀會員：`200`。
- 連續讀取至少 5 次：全部 `200`。
- 伺服器有多個 worker 時重複測試：全部 `200`。
- 假 Cookie、過期 Cookie、登出後 Cookie：`401`。
- 不會在 Log 顯示密碼、完整 Cookie、API Key 或完整會員 email。

## 五、目前 Gateway 如何判斷錯誤

| 情況 | Gateway 對前端回應 |
|---|---|
| 會員資料庫無法連線、逾時，或驗證會員端點回 5xx | `503 MEMBER_SERVICE_UNAVAILABLE` |
| 登入成功但沒有 `Set-Cookie` | `502 MEMBER_SESSION_MISSING` |
| Cookie 立刻被會員端點以 401／403／404 拒絕 | `502 MEMBER_SESSION_UNUSABLE` |
| 帳密錯誤 | `401 INVALID_CREDENTIALS` |
| 登入成功且 Cookie 驗證成功 | 前端收到成功結果與 Gateway HttpOnly Session |

所以目前畫面看到 `/auth/login 503`，代表會員資料庫 URL、Tunnel 連線、登入請求，或登入後的會員驗證端點至少有一項不可用。它不是由前端會員頁造成。

## 六、不要採用的修法

- 不要把會員資料庫改成公開且不驗證。
- 不要讓前端直接保存資料庫 Cookie、JWT 或 API Key。
- 不要信任瀏覽器自行傳入的 `X-User-Email` 或角色。
- 不要把密碼、完整 Cookie、API Key 或完整 email 寫進 Log。
- 不要為了暫時通過驗收而移除 `/api/members/{email}` 的 Session 驗證。
- 不要將正式金鑰或測試帳密提交到 GitHub。

## 七、修好後請回覆的資料

請資料庫端只回覆以下非敏感資訊：

1. 修復後的正式／測試資料庫基底網址。
2. `/api/login` 成功回應是否有 `Set-Cookie`：回答「有／沒有」即可。
3. 同 Cookie 呼叫 `/api/members/{email}` 的 HTTP 狀態碼。
4. Session Store 使用 PostgreSQL、Redis、簽章 Cookie或其他方式；不要提供秘密內容。
5. Session 有效時間。
6. 若會員端點仍要求服務 API Key，說明標頭名稱即可；金鑰本身應放 Secret Manager，不要貼在文件或訊息中。

資料庫端通過以上驗收後，再由 Gateway 端執行正式端到端測試：登入、會員頁、點數、打卡、收藏、刪除妝容與刪除會員。
