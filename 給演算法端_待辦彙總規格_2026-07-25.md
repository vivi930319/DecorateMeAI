# 給演算法端：待辦彙總規格（2026-07-25）

> 從《系統資安效率流程改善總表》抽出所有標 ⛔、且屬演算法／推薦／背景工作端的項目。
> 不含任何實際金鑰／Email／照片／權杖／完整資料包。
> 我方（Gateway／Face／Render）已完成的相關前置一併標註。

---

## 0. 我方目前狀態（對接前提）

- Face BASIC／PRO、Render 都會把「分析」與「結果封裝」分段標狀態，2026-08-03 起分析段再分成兩種：照片本身的問題回 `stage=unusable_image` ＋ `FACE_IMAGE_UNUSABLE`，`message` 是可直接顯示給使用者的中文指引；分析內部出錯回 `stage=failed` ＋ `FACE_ANALYSIS_ERROR`，`message` 是固定句、細節只留在 log。分析成功但封裝失敗仍是 `stage=analysis_completed` ＋ `PACKAGE_BUILD_FAILED`。演算法端若接手工作處理，請沿用這個區分——重點是**只有 `FACE_IMAGE_UNUSABLE` 的 `message` 可以原樣顯示**，另外兩者的訊息不要當成使用者指引。
- Render Job 讀取要 token＋擁有者都對（`_require_job_owner`）；Gateway 會帶 `X-User-ID`（opaque actor）。
- Render prompt 已結構化為四段（底妝／眉眼／腮紅修容／唇妝），identity lock 由 Render 疊上。

---

## 1. 商品推薦端點與演算法（第五節，高）

**現況問題**：`/recommend-products` 曾回 404、`style` 參數被忽略。

**規格**：
- 恢復 `/recommend-products` 端點（經 Gateway）。
- 修演算法：確實吃 `style` 參數，回符合該風格的商品。
- 回應格式統一 `error.{code,message,retryable}`；無結果回空清單而非錯誤。
- 推薦依可信的 `faceAnalysis` 結構化摘要計算，不吃前端自由文字。

**驗收**：帶不同 `style` 回不同結果；端點不再 404；`style` 參數確實生效。

**備註**：商品色值可靠度——`lab` 欄位存在，但眼影／眉筆抓到的是包裝色，不能直接算 ΔE。演算法若用到顏色距離，需先確認該品類的色值來源可靠。

---

## 2. renderQuota 額度依可信 userId 計（第五節，中）

**現況**：目前只有 server-side rate limit，未做會員綁定配額。

**規格**：
- Face／Render 每會員配額與全站成本上限，額度依可信 `userId`（Gateway 帶的 opaque actor）計，不信前端數字。
- 重複 job 用 idempotency key（含模型版／prompt 版／參數／會員範圍），避免重複燒額度。
- 與資料庫端定案 `renderQuota` 真正來源。

**驗收**：同一 idempotency key 重送不重算；超過會員配額或全站上限時擋下並回明確錯誤。

---

## 3. 服務內資料最小化與 TTL 清理（工作資料分層，高）

**規格**：
- 服務內只保留必要資料：`jobs/{jobId}` 只放非敏感摘要；完整 landmarks／prompt／payload 放限存取的位置，不隨一般查詢外流。
- 敏感 payload 設 `expiresAt` 自動清除：完成／取消／失敗逾期／會員刪除後即清。
- 用內部不可變 `userId` 關聯工作，不用 email 當主鍵。

**驗收**：一般查詢拿不到完整 landmarks／prompt；payload 到期自動消失。

---

## 4. Job owner 驗證（P0-8，高）

**規格**：
- 查詢工作結果一律驗「登入會員＝job 擁有者」，不只靠 `jobId`／token。
- token 只存 HMAC／雜湊（與資料庫端協作），過期／取消／刪除後立即失效。

**驗收**：拿別人的 jobId／token 查詢回 403；過期 token 失效。

---

## 5. 統一 Job 狀態與背景工作可靠性（源自 07-18，高）

**規格**：
- 各服務統一狀態與階段命名；狀態轉換用 transaction／guard（**禁止已完成工作退回 running**）。
- 長工作不再用程序內 daemon thread／`BackgroundTasks`，導入 Cloud Tasks／Pub/Sub 持久佇列；分離 API 與 Worker。
- Worker 加：lease／heartbeat／逾時回收、最大重試＋指數退避、dead-letter queue、idempotency、以及「provider 成功但 DB 失敗」的補償。

**驗收**：
- 完成的 job 不會被延遲的 worker 覆寫回 running（我方 `patch_if_status` 已示範 guard）。
- 程序重啟後未完成工作能被接手，不會遺失或重複執行。

---

## 6. 效率：圖片正規化與 CPU 密集工作（效率，中）

**規格**：
- 圖片正規化一次，供 InsightFace／MediaPipe／ROI 共用，不各自重做。
- CPU 密集工作移出 async event loop（用執行緒／程序池），避免卡住請求處理。
- 模型只載一次並 warmup；記錄各階段耗時。
- dedup key 含模型版／prompt 版／參數／會員範圍。

**驗收**：單張圖只正規化一次；event loop 不被解碼／推論卡住；有各階段耗時 log。

---

## 回報格式

回覆修改檔案、端點契約、狀態機定義、測試結果與部署／回滾步驟；不得附任何正式憑證、完整 landmarks 或完整 Prompt。
