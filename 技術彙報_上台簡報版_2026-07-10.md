# Decorate Me 技術彙報（上台簡報版 · 最詳盡）

> 產出：2026-07-10
> 主題：臉部分析 × AI 妝容渲染 × 商品推薦 的美妝系統
> 涵蓋：前端 / 後台 / 臉部分析端 / AI 渲染端 / 資安 / 資料串流，所有技術細節

---

## 〇、系統總覽（一句話講清楚）

使用者上傳一張自拍 →「臉部分析」用電腦視覺抽出五官與膚色特徵 →「文字建議」用本地 LLM 生成妝容建議與英文渲染指令 →「AI 渲染」用擴散模型把妝容畫回同一張臉 → 結果存雲端、可收藏、並推薦對應商品。**前後端分離、微服務架構、多個服務用 API key 串接。**

技術棧一覽：

| 層 | 技術 |
|---|---|
| 前端 | 原生 JavaScript（無框架）+ Firebase Hosting |
| 臉部分析 | Python FastAPI + InsightFace + MediaPipe + OpenCV，部署 Cloud Run |
| AI 渲染 | Python FastAPI + Replicate(flux-kontext-pro) + GCS，部署 Cloud Run |
| 文字建議 | Ollama（本地 LLM），Cloudflare Tunnel |
| 會員/商品資料庫 | 組員服務，Cloudflare Tunnel |
| 非同步 job | Firestore |

---

## 一、前端技術（Frontend）

### 1.1 架構選型
- **原生 JavaScript（Vanilla JS），刻意不用 React/Vue**：檔案直接靜態託管、零 build、載入快、學習成本低。以模組物件（object literal）組織：`Api`、`Auth`、`AdminStore`、`MemberRewards`、`Fav`、`Cart`、`History`、`AnalysisPackage`。
- **設定注入**：程式不寫死任何網址/金鑰，全部從 `window.DECORATE_ME_CONFIG`（`config.local.js`）注入，換環境只改設定檔、不動程式。

### 1.2 路由與頁面載入
- **Hash 路由**（`#dashboard`/`#analysis`…）+ `router.js` 監聽 `hashchange`。
- **頁面動態載入**：`fetch('pages/{page}.html')` 抓 HTML 片段塞進 `#mainContent`，做到單頁應用（SPA）效果又不需框架。

### 1.3 影像前處理（效能關鍵）
- 上傳前在**瀏覽器端壓縮**：Canvas 縮到**最長邊 1024px、JPEG 品質 0.78**，用 **Web Worker** 在背景執行緒跑、不卡 UI。
- **Gamma 亮度校正**：對 Lab 色彩空間的 L 通道做 gamma 曲線提亮中間調（膚色），自然不過曝。
- 好處：上傳體積小、後端算力省、使用者體感快。

### 1.4 狀態管理
- `sessionStorage`：登入身分（`beautyProfile`）、自動重登暫存帳密。
- `localStorage`：收藏、購物車、分析歷史、點數、分析草稿（離線快取）。
- **分析資料包 `AnalysisPackage`**：把一次流程（分析→建議→渲染→推薦）的結果集中在一個物件，跨頁共享。

### 1.5 認證與韌性
- 登入走 `fetch(credentials:'include')`，靠後端 session cookie。
- **自動重登**：跨站 cookie 常被瀏覽器清除 → 任何 API 遇 `401` 就用暫存帳密**背景重登再重試一次**，使用者無感。
- **失敗不靜默**：商品/推薦/收藏失敗都有明確提示（「服務暫時無法載入」而非空白）。

### 1.6 部署
- **Firebase Hosting**（`decorate-me.web.app`），CDN 分發。
- **`deploy.ps1` 自動 cache-busting**：部署時把 JS 的 `?v=` 戳成時間戳，避免使用者讀到舊快取。
- JS 用 `defer` 並行下載；`Cache-Control: no-cache` 讓瀏覽器每次重驗、確保最新。

---

## 二、後台管理技術（Admin）

- **權限模型**：後端會員物件帶 `role`（member/admin）、`level`（一般/VIP/管理員）、`status`（active/suspended）、`allowedPages`（可存取頁面清單）。前端**只信任後端回傳值**，不再本機發權限。
- **管理端點（走 admin session cookie）**：
  - `GET /api/members` 讀會員清單（只有 admin，匿名 401、非 admin 403）
  - `PATCH /api/members/{email}` 改角色/等級/權限/停權
  - `POST/PATCH /api/products` 商品 CRUD
- **即時資料檢視**：後台每位會員即時顯示「妝容收藏數」「點數」（逐會員打 API），切回分頁**自動刷新**（3 秒節流防猛打）。
- **效能/韌性**：搜尋 200ms debounce、401 一鍵重登（session 過期時）。

---

## 三、臉部分析技術（Face Analysis）— 系統最核心

### 3.1 技術棧
Python **FastAPI** + **InsightFace**（人臉偵測與姿態）+ **MediaPipe FaceMesh**（468 點臉部網格）+ **OpenCV/NumPy**（影像與幾何運算）。部署在 **Google Cloud Run**（BASIC、PRO 兩個服務）。

### 3.2 模型
- **InsightFace `buffalo_l`**：模組 `detection` + `landmark_3d_68`，取得人臉框、偵測分數、**3D 頭部姿態（yaw/pitch/roll）**。CPU 推論（`CPUExecutionProvider`）。
- **MediaPipe FaceMesh**：**468 個臉部特徵點**，提供精細幾何（臉輪廓、眉、眼、鼻、唇）。
- **模型快取**：用 module-level getter（`_get_insight` / `_get_face_mesh`）只載一次，並在服務啟動時**預熱**，避免每次請求重載。

### 3.3 分析管線（Pipeline）
```
上傳圖 → 解碼 → 縮到 1024px → InsightFace 偵測人臉 + 姿態
       → MediaPipe 取 468 landmarks → 以雙眼為基準做角度校正(align)
       → 幾何特徵計算 → 五官分類 + 膚色分析 → 輸出 JSON
```
- **姿態導向的拍攝角色判定**：依 yaw 判 `front / angle45 / side`，引導使用者拍正臉。

### 3.4 五官分類（規則法，閾值以 CelebA 校正）
- **臉型**：以**高寬比 (hw)**、下顎寬、顴骨/下顎比、額寬等幾何比例 + 閾值 → 長形臉/方形臉/圓形臉/鵝蛋臉/菱形臉/梯形臉。閾值用 CelebA 100 張的分位數校正。
- **眉型**：用眉毛**輪廓 5 點的最高點**（弓頂）判斷，而非固定骨架點，量的是「實際可見眉形」。
- **眼型/鼻型/嘴型**：同樣用 landmark 幾何比例 + 校正閾值分類。
- **雙眼對齊**：所有量測前先用左右眼（landmark 33/263）把臉**旋轉校正到水平**，消除拍照歪頭誤差。

### 3.5 膚色與個人色彩（Personal Color）
- 在臉部皮膚遮罩區取 **Lab / HSV** 色彩統計。
- **冷暖判定（undertone）**：以 Lab 的 **b 軸**（黃-藍）閾值 → warm / cool / neutral。
- **四季型**：結合明度(bright)、清濁(clear/soft) → **春季/夏季/秋季/冬季**（韓系個人色彩理論）。
- 同時輸出膚色分級與 Lab 值，供後續**商品顏色比對（ΔE）**用。

### 3.6 ML 擴充（未來展望，可講）
- 已建 **Random Forest 分類器 pipeline**（scikit-learn）：`face/brow/eye/nose/lip` 五個 `rf_*.joblib`，用 FaceAnalyzer 抽的幾何特徵當輸入。
- 目前線上跑的是**規則法**（可解釋、不需大量標註）；RF pipeline 已通、**等更多標註資料重訓**後可切換成機器學習分類。

### 3.7 BASIC vs PRO
- **BASIC**：單張正臉，回全套五官+膚色。支援檔案上傳與相機拍照。
- **PRO**：**正臉 + 側臉**，側臉用於未來鼻型精細分類/側臉輪廓（深度特徵）。PRO 走**非同步 job**（建 job → 輪詢進度 → 取結果）。

### 3.8 非同步 job 架構
- 分析建立 job 存 **Firestore（`job_store`）**，前端**每秒輪詢**狀態（stage/progress%），完成後取結果。避免長請求逾時、支援多 worker。

---

## 四、AI 妝容渲染技術（Render）

### 4.1 技術棧
Python **FastAPI** + **Replicate**（`black-forest-labs/flux-kontext-pro` 擴散模型）+ **Google Cloud Storage**。部署 Cloud Run。

### 4.2 渲染流程
```
前端送(原圖 + 英文 prompt) → 去重快取檢查
   → 呼叫 Replicate flux-kontext-pro 生成上妝圖
   → 上傳 GCS(decorate-me-renders) 得永久網址
   → 回 afterImageUrl(永久) + replicateTempUrl(暫存) + isPermanent
```

### 4.3 Prompt 工程（關鍵）
渲染 prompt = **Ollama 生成的妝容指令** + **身分鎖定句（identity lock）**：
- 明確要求**臉型/五官/膚色/膚質/毛孔/髮型/姿勢/背景/光線/表情完全不變**，「只上妝、不改人」。
- 解決擴散模型常見的「換臉/變形」問題，確保是「同一個人上妝」。
- `guidance` 由環境變數 `RENDER_GUIDANCE` 可調（現 4.5）。

### 4.4 儲存
- 渲染圖上傳 **GCS 永久網址**（不像 Replicate 暫存網址會過期），收藏功能才能長期保存。

---

## 五、資安（Security）— 貫穿所有服務

| 面向 | 做法 |
|---|---|
| **服務認證** | 所有後端服務（face-basic/pro、render、Ollama）都要 **`X-API-Key`**（middleware / Depends 驗證），擋掉直接掃 Cloud Run URL 的濫用 |
| **CORS** | 精確 `Allow-Origin`（不用 `*`）、`Allow-Credentials: true`、`Vary: Cookie` |
| **Session** | 登入 cookie 設 `SameSite=None; Secure; HttpOnly`（跨站可送、防 XSS 讀取）|
| **限流** | 渲染服務每 IP+email **固定時間窗限流**（預設每小時 10 次），超量回 `429`，防燒 Replicate 錢 |
| **去重** | 同圖同 prompt 短時間重複 → 回快取結果（sha256 key，TTL 600s），防狂按重複付費 |
| **權限** | `GET /api/members` 只有 admin session 可讀；一般會員 403、匿名 401 |
| **金鑰管理** | 前端金鑰全走 `config.local.js`（已移出版控）；後端 `.env` 不進 git；程式無寫死金鑰 |
| **輸入驗證** | 臉部分析驗空檔、亮度參數範圍；圖片一律縮到 1024 |

**已知風險與對策（誠實面對，加分項）**：
- 渲染圖存**公開 GCS bucket**（隨機檔名難枚舉，但網址流出擋不住）→ 隱私取捨，正式營運可改「私有 bucket + 短期簽名網址」。
- **OTP 目前只在前端擋、後端未強制** → 需後端建 pending 狀態並在未驗證時擋 login。
- git 歷史曾含一把 Ollama 金鑰 → 已規劃**輪換金鑰**。
- 服務以 `--allow-unauthenticated` 部署、靠應用層 key 擋 → 展示可接受，要收斂可上 IAM。

---

## 六、資料串流與整合（Streaming / Integration）

### 6.1 多服務編排（一次分析的完整串接）
```
瀏覽器
  │ 1. 上傳(壓縮後) ──X-API-Key──▶ 臉部分析(Cloud Run) ──▶ Firestore(job) ──▶ 回五官/膚色
  │ 2. 分析結果 ─────X-API-Key──▶ Ollama(Tunnel) ─────────▶ 妝容建議 + 英文 renderPrompt
  │ 3. 原圖+prompt ──X-API-Key──▶ 渲染(Cloud Run) ─▶ Replicate ─▶ GCS ─▶ 回永久網址
  │ 4. 收藏/推薦 ───session cookie▶ 會員資料庫(Tunnel) ──▶ 存 saved_looks / 回推薦商品
```

### 6.2 串流細節
- **非同步輪詢**：臉部分析/PRO 用 job + 每秒輪詢，前端顯示即時進度百分比。
- **Ollama 串流**：文字建議支援串流輸出（逐段回傳）。
- **Cloudflare Tunnel**：組員的會員庫與 Ollama 用 tunnel 對外，**網址重啟就變**，前端 `config.local.js` 同步更新。
- **降級處理**：任一外部服務失敗都有 fallback 或明確錯誤提示，不讓整頁崩掉。

### 6.3 商品推薦串接
- 分析結果（faceShape / skinTone.lab / lipLab / style）→ `POST /recommend-products`。
- **顏色比對**：底妝/口紅用膚色/唇色 **Lab 值算 ΔE** 找最接近色號；風格類（眼影/腮紅…）依 `style` 篩選。

---

## 七、可以講的技術亮點（上台重點）

1. **微服務 + 前後端分離**：五個獨立服務、各自可擴充，用 API key 串接。
2. **電腦視覺 pipeline**：InsightFace + MediaPipe 468 點，雙眼對齊消歪頭誤差，幾何規則可解釋 + RF ML 未來擴充。
3. **個人色彩演算法**：Lab 冷暖 + 四季型，落地韓系個人色彩理論。
4. **擴散模型 + 身分鎖定 prompt**：解決 AI 換臉/變形，「只上妝不改人」。
5. **工程韌性**：自動重登、去重快取、限流、cache-busting、非同步 job、降級處理。
6. **資安意識**：全服務 API key、精確 CORS、HttpOnly cookie、金鑰移出版控、且能講出已知風險的取捨。
