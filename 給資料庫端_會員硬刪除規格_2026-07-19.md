# 給資料庫端：會員刪除改為硬刪除規格

版本：2026-07-19
對象：會員資料庫／後端 API 維護者
前端版本：`dev_makeup` 分支，已部署至 <https://decorate-me.web.app>
前置文件：`給資料庫端_會員刪除與妝容收藏刪除更新規格_2026-07-16.md`
對應 Issue：[#22](https://github.com/vivi930319/DecorateMeAI/issues/22)

---

## 1. 這次要解決的問題

`DELETE /api/members/{email}` 目前採**軟刪除**：資料列保留，只把 `status` 標記為已刪除。

這造成該 email **被永久佔用**，實際發生的狀況如下：

1. 管理員在後台刪除某帳號。
2. 該使用者嘗試登入，後端回應帳號已停權。
3. 使用者依提示前往註冊。
4. 註冊時後端回 `409 EMAIL_EXISTS`——因為資料列還在，email 仍被佔用。
5. 使用者卡住，且看到的錯誤與真正原因無關。

也就是說：**被刪除的使用者永遠無法用同一個信箱回來，即使那次刪除是誤操作。**

這不是新規格。`給資料庫端_會員刪除與妝容收藏刪除更新規格_2026-07-16.md` 原本就同時定義了兩種模式：

| 位置 | 內容 |
|---|---|
| 第 48 行 | 軟刪除：`status` 增加 `deleted`，所有 API 預設排除 |
| 第 50 行 | 硬刪除：row 一併消失，但稽核表仍須保留操作者、時間與目標識別 |
| 第 77 行 | 硬刪除模式使用 `ON DELETE CASCADE` |
| 第 142 行 | 回應中的 `mode` 欄位表明本次採用哪種模式 |

本文件要求的是**把模式從 `soft` 切換為 `hard`**，並補齊切換時必須一併處理的細節。

---

## 2. 需求摘要

| 項目 | 現況 | 目標 |
|---|---|---|
| `DELETE /api/members/{email}` | 軟刪除，`status` 標記 | **硬刪除，實際移除資料列** |
| 回應 `mode` | `"soft"` | `"hard"` |
| 原 email 可否重新註冊 | **否** | **是** |
| 稽核紀錄 | — | **必須保留，不可隨會員一起刪除** |
| 關聯資料 | 留存 | **級聯刪除** |
| GCS 圖片 | 留存 | **一併刪除** |

**唯一不可妥協的條件：同一個信箱必須能夠再次註冊。**

---

## 3. 必須級聯刪除的資料

會員主資料刪除時，以下資料一併移除：

| 資料 | 說明 |
|---|---|
| `saved_looks` | 妝容收藏 |
| `tryon_records` | 試妝紀錄 |
| 點數餘額與點數異動紀錄 | 含簽到、任務、兌換紀錄 |
| 商品收藏 | |
| 購物車 | |
| OTP／驗證暫存 | 若有留存 |

### GCS 物件必須一起處理

資料庫刪除但物件留在儲存空間，會產生兩個問題：

1. **孤兒檔案**：沒有任何資料列指向它們，永遠不會被清理。
2. **個資殘留**：那些是使用者的人臉照片與妝後圖，屬於個資，資料庫刪了但檔案還在等同沒刪。

需要刪除的物件至少包含：

```text
使用者上傳的原始照片
妝後渲染結果圖
分析過程的暫存影像
```

若無法在同一個交易內完成，請採「先寫入待刪清單，再由背景工作清理」的方式，並確保待刪清單不會因會員刪除而遺失。

---

## 4. 不可級聯刪除的資料

### 稽核紀錄

前置文件第 99 行已明訂：**稽核資料不可讓一般會員讀取，也不可因會員刪除而 cascade 掉。**

`admin_audit_logs` 需保留：

| 欄位 | 說明 |
|---|---|
| 操作者 email | 執行刪除的管理員 |
| 操作時間 | |
| 目標識別資訊 | 被刪除帳號的 email 或 member id |
| 操作類型 | `delete_member` |
| request id | 便於追查 |

因此稽核表**不可**對 `members` 設定 `ON DELETE CASCADE`，外鍵請改為 `ON DELETE SET NULL`，或乾脆不設外鍵、只存文字識別。

---

## 5. API 規格

### 路徑

```http
DELETE /api/members/{email}
Authorization: Bearer <admin_token>
```

`{email}` 需經 URL encode，例如 `user%40example.com`。

### 權限

僅管理員（`role = admin` 且 `status = active`）可呼叫。沿用前置文件第 4 節的權限規則，本次不變更。

### 成功回應

HTTP `200`：

```json
{
  "success": true,
  "mode": "hard",
  "deleted": {
    "email": "user@example.com",
    "savedLooks": 12,
    "tryonRecords": 5,
    "storageObjects": 17
  },
  "requestId": "req_..."
}
```

`mode` 請確實回 `"hard"`，前端會依此判斷。
`deleted` 內的計數為選填，但有助於後台顯示刪除結果與事後核對。

### 錯誤回應

沿用前置文件第 4 節，本次新增一項：

| HTTP | code | 說明 |
|---|---|---|
| `403` | `CANNOT_DELETE_SELF` | 管理員嘗試刪除自己（既有） |
| `404` | `USER_NOT_FOUND` | 該 email 不存在（既有） |
| `409` | `DELETE_HAS_DEPENDENCIES` | **新增**：關聯資料未能完整刪除，整筆操作已回滾 |

`409` 用於「級聯刪除中途失敗」的情況。此時**不可留下半刪除狀態**——會員仍在但收藏已消失，比完全沒刪更糟。

---

## 6. 交易規則

```text
BEGIN
  刪除關聯資料（saved_looks、tryon_records、點數、收藏、購物車）
  寫入稽核紀錄
  刪除 members 資料列
COMMIT
  → 成功後才觸發 GCS 物件刪除
```

要求：

1. **稽核紀錄必須在同一交易內寫入**，否則刪除成功但稽核失敗會留下無法追查的操作。
2. **GCS 刪除放在交易之後**：物件儲存無法參與資料庫交易，若放在交易內失敗會導致回滾困難。
3. 任一步驟失敗即整筆回滾，回 `409 DELETE_HAS_DEPENDENCIES`。

---

## 7. 建議 migration（請依實際資料庫語法調整）

```sql
-- 關聯資料改為級聯刪除
ALTER TABLE saved_looks
  DROP CONSTRAINT IF EXISTS saved_looks_member_fk,
  ADD CONSTRAINT saved_looks_member_fk
  FOREIGN KEY (member_id) REFERENCES members(id) ON DELETE CASCADE;

ALTER TABLE tryon_records
  DROP CONSTRAINT IF EXISTS tryon_records_member_fk,
  ADD CONSTRAINT tryon_records_member_fk
  FOREIGN KEY (member_id) REFERENCES members(id) ON DELETE CASCADE;

-- 點數、收藏、購物車比照辦理

-- 稽核表相反：不可 cascade
ALTER TABLE admin_audit_logs
  DROP CONSTRAINT IF EXISTS admin_audit_logs_member_fk;
-- 建議改存文字識別，不設外鍵：
ALTER TABLE admin_audit_logs
  ADD COLUMN IF NOT EXISTS target_email VARCHAR(254);
```

執行前請先在測試環境驗證，並確認現有的軟刪除資料要如何處置（見第 9 節）。

---

## 8. 替代方案：若必須保留資料列

若因稽核或法規因素無法真正刪除資料列，**至少要釋放 email**，例如：

```sql
UPDATE members
SET email = CONCAT('deleted_', id, '_', MD5(email), '@deleted.local'),
    status = 'deleted',
    name = '已刪除會員',
    phone_number = NULL,
    password_hash = NULL
WHERE id = ?;
```

要點：

- 原 email 釋出，可重新註冊。
- 個資欄位一併清空或匿名化，只保留稽核需要的最小識別。
- `status = 'deleted'` 的資料列必須被所有查詢 API 排除。

此方案的 `mode` 請回 `"anonymized"`，前端會與 `"hard"` 做相同處理。

---

## 9. 既有軟刪除資料的處置

切換模式前，資料庫中已存在被軟刪除的帳號。請一併決定：

| 選項 | 做法 |
|---|---|
| A | 依新規則實際刪除，並補寫稽核紀錄 |
| B | 依第 8 節匿名化，釋放 email |
| C | 維持現狀，僅新的刪除操作套用新規則 |

**不建議選 C**：既有被刪除的使用者仍然無法回來，原問題只解決一半。

請告知採用哪個選項，以及受影響的帳號數量。

---

## 10. 前端現況

前端已於 commit `4270e41` 完成對應調整，**同時支援 soft 與 hard 兩種模式**，資料庫端切換後不需要再改前端：

| 項目 | 行為 |
|---|---|
| 停權／刪除帳號登入 | 顯示「請聯繫管理員，重新註冊不會生效」，**不再引導去註冊** |
| 登入失敗（未分類） | 只顯示原因，不再預設猜測「使用者尚未註冊」 |
| `Api.register` | 回傳後端真正的錯誤，不再一律顯示「連線失敗」 |
| 撞到 `EMAIL_EXISTS` | 提供「返回登入」出口 |

改為硬刪除後，被刪除的使用者重新註冊會正常成功，上述提示自然不會再出現。

---

## 11. 驗收清單

### 刪除行為

- [ ] 管理員刪除會員後，`SELECT * FROM members WHERE email = ?` 查無資料
- [ ] 回應的 `mode` 為 `"hard"`
- [ ] 該 email **可以成功重新註冊**
- [ ] 重新註冊的帳號是全新的，看不到任何舊資料

### 關聯資料

- [ ] `saved_looks`、`tryon_records`、點數、收藏、購物車皆已刪除
- [ ] GCS 上的照片與渲染結果已刪除
- [ ] 沒有殘留指向已刪除會員的資料列

### 稽核

- [ ] 稽核紀錄仍在，包含操作者、時間、目標 email
- [ ] 稽核紀錄**不會**因會員刪除而消失
- [ ] 一般會員無法讀取稽核資料

### 錯誤處理

- [ ] 刪除不存在的帳號回 `404 USER_NOT_FOUND`
- [ ] 管理員刪除自己回 `403 CANNOT_DELETE_SELF`
- [ ] 級聯刪除中途失敗時整筆回滾，回 `409`，**不留下半刪除狀態**
- [ ] 非管理員呼叫回 `403`

---

## 12. 需要回覆的事項

1. 採用硬刪除或第 8 節的匿名化方案？
2. 既有軟刪除資料採第 9 節的哪個選項？受影響帳號數量？
3. GCS 物件刪除是同步或背景工作？
4. 預計完成時間。
