# 給資料庫端：會員刪除與妝容收藏刪除更新規格

版本：2026-07-16  
對象：會員資料庫／後端 API 維護者  
前端版本：`Isa` 分支，已部署至 <https://decorate-me.web.app>

---

## 1. 這次要解決的問題

前端後台現在需要真正控制會員資料庫，而不是只改瀏覽器畫面：

1. 使用者刪除自己的妝容對比圖時，資料庫的 `saved_looks` 也要同步刪除。
2. 管理員可以在後台刪除會員的妝容對比圖。
3. 管理員可以刪除會員，刪除後會員不能再出現在後台清單或正常登入。
4. 任何資料庫刪除失敗，前端都不會先移除本機／畫面資料，避免「看起來刪掉但資料庫還在」。

本文件是本次更新的後端契約。前端已經會呼叫下列 API，但會員刪除 API 需要資料庫端新增實作。

---

## 2. API 變更總覽

| 方法 | 路徑 | 狀態 | 權限 |
|---|---|---|---|
| `GET` | `/api/members/{email}/saved-looks` | 既有 | 本人；管理員可讀取後台指定會員資料 |
| `POST` | `/api/members/{email}/saved-looks` | 既有 | 僅本人 |
| `DELETE` | `/api/members/{email}/saved-looks/{id}` | 既有，需擴充管理員權限 | 本人或管理員 |
| `DELETE` | `/api/members/{email}` | **新增** | 僅管理員 |

所有會員資料 API 都必須在後端驗證登入 session 或 `Authorization: Bearer <token>`。不能只相信前端傳來的 email，也不能用 `admin@` 字串判斷管理員。

---

## 3. 必須新增的資料欄位

### 3.1 `members` 表

目前已有 `email`、`role`、`level`、`status`、`allowed_pages` 等欄位的前提下，建議新增下列欄位，支援安全刪除與稽核：

| 欄位 | 建議型別 | Nullable | 用途 |
|---|---|---:|---|
| `deleted_at` | `TIMESTAMP WITH TIME ZONE` | 是 | 會員被刪除／停用的時間；未刪除為 `NULL` |
| `deleted_by` | `VARCHAR(254)` | 是 | 執行刪除的管理員 email；未刪除為 `NULL` |
| `deletion_reason` | `VARCHAR(255)` | 是 | 刪除原因或管理員備註 |
| `deletion_request_id` | `VARCHAR(64)` | 是 | 本次刪除請求的 idempotency／稽核識別碼 |

若採「軟刪除」，`status` 請增加 `deleted` 狀態，所有登入、會員清單、會員資料 API 預設排除 `status = 'deleted'` 的資料。

若資料庫決定採「硬刪除」，會員主資料刪除後上述四個欄位會隨 row 一起消失；仍建議在稽核表保留刪除操作者、時間與目標識別資訊。

### 3.2 `saved_looks` 表

這次不一定需要新增欄位，但請確認已有下列欄位：

| 欄位 | 建議型別 | 必要條件 |
|---|---|---|
| `id` | `BIGINT`／`UUID` | Primary Key，DELETE 依此定位 |
| `member_email` | `VARCHAR(254)` | 必須有 index，且與路徑 email 比對 |
| `style` | `VARCHAR(120)` | 後端驗證長度 |
| `before_image_url` | `TEXT`／`LONGTEXT` | 可存壓縮 data URL 或 http(s) URL，依資料庫容量政策限制 |
| `after_image_url` | `VARCHAR(500)`／`TEXT` | 建議只存永久 http(s) URL，不存 Replicate 臨時網址 |
| `analysis_summary` | `JSON`／`TEXT` | JSON 格式，限制最大 payload 大小 |
| `created_at` | `TIMESTAMP WITH TIME ZONE` | 新到舊排序 |

請建立索引：

```sql
CREATE INDEX IF NOT EXISTS idx_saved_looks_member_created
ON saved_looks (member_email, created_at DESC);
```

若資料庫支援外鍵，建議：

```text
saved_looks.member_email → members.email
ON DELETE CASCADE（硬刪除模式）
```

軟刪除模式則可不直接 cascade，但會員被標記 deleted 後，所有 saved-looks 查詢必須被後端阻擋；若要釋放圖片資源，請另外執行刪除或匿名化流程。

### 3.3 建議新增 `admin_audit_logs` 表

會員刪除屬高風險操作，建議新增稽核表，不要只依靠應用程式 log：

| 欄位 | 建議型別 | 用途 |
|---|---|---|
| `id` | `UUID`／`BIGINT` | Primary Key |
| `request_id` | `VARCHAR(64)` | 對應一次 API 請求，避免重複記錄 |
| `actor_email` | `VARCHAR(254)` | 操作者，必須是後端驗證出的 admin |
| `action` | `VARCHAR(80)` | 例如 `delete_member`、`delete_saved_look` |
| `target_email` | `VARCHAR(254)` | 被操作會員 |
| `target_id` | `VARCHAR(80)` | 被刪除收藏 id；刪會員時可為 NULL |
| `result` | `VARCHAR(20)` | `success`／`failed` |
| `reason` | `VARCHAR(255)` | 原因或備註 |
| `metadata_json` | `JSON`／`TEXT` | 刪除筆數、錯誤碼等非敏感資訊 |
| `created_at` | `TIMESTAMP WITH TIME ZONE` | 稽核時間 |

稽核資料不可讓一般會員讀取，也不可因會員刪除而 cascade 掉。

---

## 4. 新增 API：刪除會員

### Request

```http
DELETE /api/members/{email}
Authorization: Bearer <admin_token>
Content-Type: application/json
```

Request body 可為空。若要傳刪除原因，最多 255 字：

```json
{
  "reason": "會員要求刪除帳號"
}
```

### 後端處理規則

1. 驗證 token／session。
2. 從資料庫查詢操作者的 `role`，只有 `admin` 可以繼續。
3. 禁止刪除自己目前登入中的帳號。
4. 建議禁止刪除最後一個 active admin，避免後台失去管理員。
5. 用 transaction／資料庫交易鎖定目標會員，避免同時刪除造成半套資料。
6. 處理會員關聯資料：`saved_looks`、商品收藏、購物車、分析歷史、點數交易、任務資料不可留下無主資料。
7. 寫入 `admin_audit_logs`。
8. 完成後才 commit；任何一步失敗都 rollback。

### 成功回應

建議回 `200`，方便前端與 log 驗收：

```json
{
  "ok": true,
  "action": "member_deleted",
  "member": {
    "email": "user@example.com",
    "mode": "soft",
    "deletedAt": "2026-07-16T12:34:56+08:00"
  },
  "deletedCounts": {
    "savedLooks": 15,
    "favorites": 8,
    "cartItems": 2,
    "history": 6
  },
  "requestId": "req_xxx"
}
```

也可回 `204 No Content`；前端只判斷 HTTP 2xx，但建議正式環境回 JSON 方便追查。

### 錯誤回應

```json
{ "error": { "code": "ADMIN_REQUIRED", "message": "只有管理員可以刪除會員" } }
```

| HTTP | code | 情境 |
|---:|---|---|
| `401` | `UNAUTHENTICATED` | 沒有有效 session／Bearer token |
| `403` | `ADMIN_REQUIRED` | 一般會員呼叫 |
| `403` | `CANNOT_DELETE_SELF` | 管理員嘗試刪除自己 |
| `404` | `MEMBER_NOT_FOUND` | 目標不存在 |
| `409` | `LAST_ADMIN_PROTECTED` | 目標是最後一個管理員 |
| `422` | `INVALID_EMAIL` | 路徑 email 格式錯誤 |

前端收到非 2xx 時會保留會員資料，不會先從畫面或本機快取移除。

---

## 5. 修改既有 API：管理員刪除妝容

### 路徑

```http
DELETE /api/members/{member_email}/saved-looks/{id}
```

### 權限

- 本人：可以刪除自己的收藏。
- 管理員：可以刪除指定會員收藏，但後端必須依 token 的 admin role 授權。
- 不允許只改 URL 的 email 或 id 來越權刪除；查詢必須同時使用 `member_email` 與 `id`。

### 成功回應

```json
{
  "ok": true,
  "action": "saved_look_deleted",
  "look": {
    "id": 123,
    "memberEmail": "user@example.com"
  }
}
```

### 交易規則

1. 先驗證操作者是否為本人或 admin。
2. 以 `WHERE id = :id AND member_email = :email` 定位資料。
3. 找不到資料回 `404 LOOK_NOT_FOUND`，不可刪除其他會員同 id 的資料。
4. DB row 刪除成功後，再安排 GCS 圖片刪除；GCS 失敗不可把 DB 刪除 rollback 成半成功狀態，應寫入 cleanup job／error log 重試。
5. 寫入 `admin_audit_logs`（只有管理員刪除時需要）。

---

## 6. 既有新增收藏 API 的驗證確認

```http
POST /api/members/{email}/saved-looks
```

請確認後端維持下列規則：

- POST 只能由本人呼叫，路徑 email 必須與登入者一致。
- `style` 最長 120 字。
- `afterImageUrl` 最好只接受永久 `http(s)` URL，拒絕 Replicate 臨時 URL。
- `beforeImageUrl` 若接受 data URL，必須限制大小；不可讓任意超大 base64 造成資料庫膨脹。
- `analysisSummary` 必須是 JSON object，限制最大大小並拒絕任意 HTML／script 內容。
- 收藏數量建議每位會員最多 50 筆；超過時回 `409 SAVED_LOOK_LIMIT`。

---

## 7. CORS、Bearer 與 preflight

因為前端會從 Firebase Hosting 呼叫資料庫端，請確認：

- `Access-Control-Allow-Origin` 必須是 `https://decorate-me.web.app`，不可在 credentials 模式使用 `*`。
- `Access-Control-Allow-Credentials: true`。
- `Access-Control-Allow-Methods` 包含 `GET, POST, PATCH, DELETE, OPTIONS`。
- `Access-Control-Allow-Headers` 至少包含 `Authorization, Content-Type`。
- `OPTIONS /api/members/{email}` 與 `OPTIONS /api/members/{email}/saved-looks/{id}` 回 `204`，不要要求登入 token 才能完成 preflight。
- Bearer token 不要寫入 URL、log 或錯誤訊息。

---

## 8. 建議 migration（請依實際資料庫語法調整）

以下是 PostgreSQL 風格範例，不能直接假設適用所有資料庫：

```sql
ALTER TABLE members
  ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ NULL,
  ADD COLUMN IF NOT EXISTS deleted_by VARCHAR(254) NULL,
  ADD COLUMN IF NOT EXISTS deletion_reason VARCHAR(255) NULL,
  ADD COLUMN IF NOT EXISTS deletion_request_id VARCHAR(64) NULL;

CREATE INDEX IF NOT EXISTS idx_members_status_deleted_at
  ON members (status, deleted_at);

CREATE INDEX IF NOT EXISTS idx_saved_looks_member_created
  ON saved_looks (member_email, created_at DESC);

CREATE TABLE IF NOT EXISTS admin_audit_logs (
  id UUID PRIMARY KEY,
  request_id VARCHAR(64) NOT NULL,
  actor_email VARCHAR(254) NOT NULL,
  action VARCHAR(80) NOT NULL,
  target_email VARCHAR(254) NULL,
  target_id VARCHAR(80) NULL,
  result VARCHAR(20) NOT NULL,
  reason VARCHAR(255) NULL,
  metadata_json JSONB NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_admin_audit_target_created
  ON admin_audit_logs (target_email, created_at DESC);
```

---

## 9. 驗收清單

### 權限

- [ ] 未登入呼叫會員 DELETE 回 `401`。
- [ ] 一般會員呼叫會員 DELETE 回 `403 ADMIN_REQUIRED`。
- [ ] 管理員刪除自己回 `403 CANNOT_DELETE_SELF`。
- [ ] 管理員可刪除其他會員，且重整後會員不再出現在 `GET /api/members`。
- [ ] 一般會員不能刪除其他人的 saved look。
- [ ] 管理員可以刪除指定會員 saved look。

### 一致性

- [ ] 刪除會員後沒有無主 `saved_looks`、favorites、cart、history、points 關聯。
- [ ] 任一 transaction 失敗會 rollback。
- [ ] API 成功回 2xx 後，前端才移除畫面與 localStorage 資料。
- [ ] API 失敗時，前端仍保留原資料並顯示錯誤。

### 資安與稽核

- [ ] 後端依 token 內的 admin role 授權，不依賴前端傳值。
- [ ] 會員刪除與管理員刪除妝容都有 audit log。
- [ ] DELETE API 已防止 IDOR：email 與 look id 必須同時比對。
- [ ] CORS preflight 支援 DELETE，且沒有把 token 寫入 URL。
- [ ] 圖片刪除失敗有 cleanup job／重試記錄。

---

## 10. 前端目前行為摘要

前端已部署的行為如下：

- 前台刪除已同步收藏：先呼叫 saved-looks DELETE，成功後才刪 localStorage。
- 後台刪除會員收藏：先呼叫 saved-looks DELETE，成功後才從後台清單移除。
- 後台刪除會員：呼叫 `DELETE /api/members/{email}`，成功後才從後台清單移除。
- 任一 DELETE 非 2xx：保留畫面資料並提示使用者，不做假刪除。

請資料庫端完成本文件第 3、4、5、7 節後，再使用第 9 節逐項回報驗收結果。
