# 給 Ollama 端：文字建議服務接入規格書

> 日期：2026-07-25
>
> 對象：負責跑 `Ollama_suggestion.py` 那台機器的同學。
>
> 這份文件回答一件事：**線上的妝容文字建議目前是關閉的，要重新打開，需要你先做三件事。**
> 對應 GitHub issue [#19](https://github.com/vivi930319/DecorateMeAI/issues/19)（服務不可用）
> 與 [#26](https://github.com/vivi930319/DecorateMeAI/issues/26)（金鑰未輪替）。
>
> 禁止寫入本文件、Log、Git 或訊息：API Key 本體、完整會員 email、會員照片。
> 金鑰請走私訊或密碼管理工具傳遞，不要貼進 issue、commit 或這份文件。

---

## 〇、先講結論

線上站台 `https://decorate-me.web.app` 的文字建議打不通，**不是網路問題，也不是你的服務壞掉**。
是我們這邊刻意把整條線關著，而鑰匙在你手上。

2026-07-25 實測：

```
GET https://decorate-me.web.app/text-suggestion/health   →   503
```

這個 503 是 Gateway 在**還沒碰到你的服務之前**就擋掉的（`ai_gateway.py:1674`），
錯誤碼 `EXTERNAL_TEXT_UPSTREAM_DISABLED`。程式碼裡的註解寫的是
「disabled until the trusted service is ready」——在金鑰換掉之前，這道閘門不會打開。

---

## 一、為什麼關著：外洩的金鑰還沒換

`config.local.js` 早期被 commit 進版控，git 歷史裡曾經永久保留一把 Ollama 金鑰。
2026-07-21 已經用 `filter-branch` 把該檔從 `dev_makeup` 全部 112 個 commit 移除並強制推送，
GitHub 網頁上已經查不到。

**但是重寫歷史不等於金鑰失效：**

- 強制推送前的 git 物件，在 GitHub 完成 GC 之前仍可能用完整 SHA 直接取得
- 既有 fork、各種快取，以及每台機器上重寫前的舊 clone 都還持有
- 任何在重寫前 clone 過的人，本機都還有完整歷史

唯一能真正解除風險的動作是**換發新金鑰**，讓舊的即使外流也無效。

而這把金鑰是由**你那台機器**驗證的——`Ollama_suggestion.py:34` 的 `SUGGESTION_API_KEY`。
我們這邊改不了，只能請你換。這就是整條線被鎖住的原因。

---

## 二、請你做的三件事

### 2.1 換發 `SUGGESTION_API_KEY`

產生一把新的隨機金鑰（建議 32 bytes 以上的隨機值），設進你那台的環境變數：

```bash
SUGGESTION_API_KEY=<新的隨機金鑰>
```

注意 `Ollama_suggestion.py:35` 的 `enforce_service_api_key` 是 **fail closed**：
沒設這個變數，服務會直接拒絕啟動。這是刻意的，不要為了方便把它拿掉。

新金鑰請用私訊傳給我們，**不要**貼進 GitHub issue、commit message 或任何文件。

### 2.2 把服務跑起來，確認 Ollama 本體是通的

```bash
ollama pull gemma3          # Ollama_suggestion.py:26 的預設模型
python Ollama_suggestion.py # 預設聽 8010 埠
```

然後在你自己那台確認：

```bash
curl http://127.0.0.1:8010/health
```

要看到這三個欄位是對的：

| 欄位 | 期望值 | 不對的話代表 |
| --- | --- | --- |
| `ollama.reachable` | `true` | Ollama 本體沒開，或 `OLLAMA_BASE_URL` 指錯 |
| `ollama.model` | `gemma3` | 模型名稱與實際 pull 的不符 |
| `api_key_required` | `true` | `SUGGESTION_API_KEY` 沒設進去，2.1 沒生效 |

`api_key_required` 是 `false` 的話**絕對不要**把網址給我們——那代表你的服務對外裸奔，
任何人拿到網址都能無限制呼叫你的顯示卡。

### 2.3 開一條 https 通道，把網址給我們

我們的 Gateway 跑在 Google Cloud Run 上，連不到你家的內網，所以需要一個公開的 https 網址。

**請優先用固定網址的 Named Tunnel，不要用 Quick Tunnel。**
Quick Tunnel（`xxx-xxx-xxx.trycloudflare.com`）每次重啟都會換一組網址，
每換一次我們就要重新設定一次 Cloud Run 並等它重新部署。
會員資料庫那邊已經在吃這個苦頭（我們甚至為此寫了一支 `update-db-url.ps1`），
Demo 前不要再多一條。

---

## 三、介面規格（這部分你已經寫好了，僅供核對）

以下是我們的 Gateway 會依賴的行為。你改動 `Ollama_suggestion.py` 時，這些不能變。

### 3.1 驗證方式

每個請求會帶這個 header：

```
X-API-Key: <SUGGESTION_API_KEY>
```

金鑰不符要回 **401**（`Ollama_suggestion.py:38-43`）。

### 3.2 `GET /health`

不需要金鑰。回應如 2.2 所述。

### 3.3 `POST /suggest`

請求 body（`SuggestRequest`）：

```json
{
  "analysisPackage": { "faceAnalysis": { } },
  "faceAnalysis":    { },
  "style":     "日常自然妝",
  "language":  "zh-TW",
  "userNote":  "使用者的備註文字",
  "model":     null
}
```

`analysisPackage.faceAnalysis` 與 `faceAnalysis` 二擇一，兩個都沒有要回 **400**。
`model` 留空就用預設的 `gemma3`。

成功回應：

```json
{
  "status":       "completed",
  "provider":     "ollama",
  "model":        "gemma3",
  "fallbackUsed": false,
  "createdAt":    "2026-07-25T...Z",
  "suggestion":   "（六段式的繁體中文妝容建議）"
}
```

Ollama 本體掛掉時回 **502**，錯誤碼 `OLLAMA_UNAVAILABLE`、`retryable: true`
（`Ollama_suggestion.py:303-313`）。我們前端已經有這個碼的中文對照，會顯示
「文字建議服務目前無法連線，請稍後再試」，其他功能不受影響。

---

## 四、已知的坑（請先看過，否則會「本機測都好好的，上線就不通」）

### 4.1 `/suggest/stream` 過不了我們的 Gateway

你的服務有 `/suggest/stream`（`Ollama_suggestion.py:325`），
但我們 Gateway 的路徑白名單（`ai_gateway.py:105-108`）**只放行 `health` 和 `suggest` 兩條**。

這是我們這邊的限制，不是你的問題。目前前端不使用串流，所以先維持現狀。
如果之後要做逐字輸出的效果，**先告訴我們**，我們要先把它加進白名單，
否則你那條路由在本機測起來完全正常，一上線就會被擋掉。

### 4.2 輸出長度

`OLLAMA_NUM_PREDICT` 預設 4096（`Ollama_suggestion.py:31`）。
這個值不要往下調——實測調低會讓輸出在 900 中文字左右斷在句子中間，
六段只生到第五段，「避免事項」和「總結與建議」永遠出不來。

### 4.3 逾時

`OLLAMA_TIMEOUT` 預設 120 秒。我們 Gateway 那側對這個服務的讀取逾時是 60 秒
（`ai_gateway.py:171`）。也就是說**你那邊撐到 120 秒才回，我們早在 60 秒就放棄了**。
如果你的機器生成一次要超過 60 秒，請告訴我們實際耗時，我們調整 Gateway 那側，
不要自己把 `OLLAMA_TIMEOUT` 調更長——那只會讓兩邊的數字更不一致。

---

## 五、你交付之後，我們這邊做什麼

你給我們兩樣東西：**通道網址** 和 **新金鑰**。我們設定 Cloud Run `ai-gateway` 三個環境變數：

| 環境變數 | 值 |
| --- | --- |
| `TEXT_SUGGESTION_URL` | 你的通道網址 |
| `UPSTREAM_TEXT_SUGGESTION_API_KEY` | 你的新金鑰 |
| `GATEWAY_ALLOW_EXTERNAL_TEXT_UPSTREAM` | `true` |

金鑰只會存在 Cloud Run 的環境變數／Secret Manager 裡，**不會**發布到前端。
瀏覽器永遠只看得到同源路徑 `/text-suggestion/...`，看不到你的網址，也拿不到金鑰。

---

## 六、驗收

三件事做完之後，我們會實際打這兩條確認：

```
GET  https://decorate-me.web.app/text-suggestion/health   →  200（不再是 503）
POST https://decorate-me.web.app/text-suggestion/suggest  →  200，且 suggestion 有六段
```

通過之後我們會關掉 issue #19 與 #26。

**在那之前，麻煩不要把舊金鑰繼續用在新環境上**——那等於這輪什麼都沒做。

---

## 附錄：你那台需要的環境變數一覽

```bash
SUGGESTION_API_KEY=<新的隨機金鑰>          # 必填，沒有會拒絕啟動
OLLAMA_BASE_URL=http://127.0.0.1:11434    # 預設值，Ollama 在同一台就不用改
OLLAMA_MODEL=gemma3                        # 預設值
OLLAMA_TIMEOUT=120                         # 預設值，見 4.3
OLLAMA_NUM_PREDICT=4096                    # 預設值，不要調低，見 4.2
OLLAMA_SUGGESTION_PORT=8010                # 預設值，要換 port 才需要設
```
