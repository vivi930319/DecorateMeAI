# Admin 後台商品爬蟲整合規格書

日期：2026-07-15  
用途：提供爬蟲端與後台端整合商品匯入功能時使用  
對象：爬蟲端組員、後端組員、前端後台管理介面組員

---

## 1. 功能目標

目前系統已有後台管理員手動新增商品的功能。後續希望新增「管理員透過後台輸入商品網址，由系統爬取商品資料並協助建立商品」的流程。

本功能不是大量全自動爬取商品，也不是讓爬蟲直接寫入正式商品資料庫，而是設計成「後台管理員輔助匯入」：

1. 管理員登入後台。
2. 管理員進入商品管理頁面。
3. 管理員貼上商品網址。
4. 後台呼叫爬蟲端 API。
5. 爬蟲端回傳商品名稱、價格、圖片、描述、來源網址等資料。
6. 後台顯示預覽結果。
7. 管理員人工確認與修改。
8. 管理員按下儲存後，才正式新增商品到系統資料庫。

此設計可以降低錯誤商品資料直接進入正式系統的風險，也能讓教授看到「後台管理員可以用爬蟲輔助商品建檔」的完整流程。

---

## 2. 系統角色分工

### 2.1 後台端負責範圍

後台端主要負責管理員操作流程與商品資料儲存。

需要處理：

- 管理員登入後導向 admin dashboard。
- 建立 admin 專用商品管理頁面。
- 建立「從網址匯入商品」介面。
- 將商品網址送到後端或爬蟲 API。
- 顯示爬蟲回傳的商品預覽資料。
- 讓管理員修改商品名稱、價格、分類、圖片、描述等欄位。
- 管理員確認後，將資料寫入正式商品資料庫。
- 顯示成功、失敗、資料不完整等狀態。

### 2.2 爬蟲端負責範圍

爬蟲端主要負責「根據網址抓取商品資訊」，不直接決定商品是否上架。

需要處理：

- 接收後台端傳來的商品網址。
- 判斷網址是否可爬。
- 爬取商品頁 HTML 或動態內容。
- 解析商品名稱、價格、圖片、描述、品牌、規格等欄位。
- 統一整理成指定 JSON 格式回傳。
- 若爬取失敗，要回傳清楚的錯誤原因。
- 若部分欄位抓不到，也要回傳可用資料與缺漏欄位。

### 2.3 後端資料庫端負責範圍

正式商品資料是否新增，由後端商品 API 處理，不建議讓爬蟲端直接寫入資料庫。

需要處理：

- 商品新增 API。
- 商品欄位驗證。
- 圖片 URL 或圖片檔案儲存策略。
- 商品分類、膚色標籤、風格標籤等系統內部欄位轉換。
- 管理員權限驗證。

---

## 3. 建議整體流程

```text
管理員登入
  ↓
判斷身分為 admin
  ↓
導向 /admin/dashboard
  ↓
進入商品管理
  ↓
點選「從網址匯入商品」
  ↓
輸入商品網址
  ↓
後台呼叫爬蟲端 API
  ↓
爬蟲端回傳商品資料
  ↓
後台顯示預覽表單
  ↓
管理員確認或修改資料
  ↓
送出正式新增商品 API
  ↓
商品進入系統資料庫
```

---

## 4. 後台頁面規劃

### 4.1 管理員登入後導向

原本系統可能是「使用者介面 + 管理分頁」混合在一起。建議調整成角色分流：

```text
一般使用者登入 → 使用者前台頁面
管理員登入 → admin 後台 dashboard
```

建議路由：

```text
/admin/dashboard
/admin/products
/admin/products/create
/admin/products/import
/admin/products/edit/:id
```

### 4.2 商品匯入頁面

頁面名稱可以是：

```text
從網址匯入商品
```

頁面應包含：

- 商品網址輸入框
- 開始爬取按鈕
- loading 狀態
- 爬取成功後的商品預覽表單
- 爬取失敗訊息
- 儲存商品按鈕

### 4.3 預覽表單欄位

爬蟲成功後，後台應顯示可編輯表單，而不是直接存入資料庫。

建議欄位：

- 商品名稱
- 商品價格
- 商品圖片
- 商品描述
- 商品品牌
- 商品分類
- 商品來源網址
- 適合膚色標籤
- 商品風格標籤
- 商品狀態

其中「適合膚色標籤」、「商品風格標籤」如果爬蟲端無法判斷，可以先留空，由管理員人工選擇。

---

## 5. 爬蟲端 API 規格

### 5.1 API 目的

此 API 用於接收商品網址，回傳該商品頁面可解析出的商品資訊。

### 5.2 建議 API

```http
POST /api/crawler/product-preview
```

如果爬蟲服務是獨立服務，也可以使用：

```http
POST /crawler/product-preview
```

### 5.3 Request 格式

```json
{
  "url": "https://example.com/product/123",
  "source": "manual_admin_import",
  "adminId": "admin_001"
}
```

欄位說明：

| 欄位 | 型別 | 必填 | 說明 |
|---|---|---|---|
| url | string | 是 | 管理員輸入的商品頁網址 |
| source | string | 否 | 呼叫來源，建議固定為 manual_admin_import |
| adminId | string | 否 | 管理員 ID，方便記錄操作來源 |

---

## 6. 爬蟲端 Response 規格

### 6.1 成功回傳格式

```json
{
  "success": true,
  "status": "ok",
  "message": "商品資料爬取成功",
  "data": {
    "sourceUrl": "https://example.com/product/123",
    "sourceSite": "example.com",
    "productName": "水光保濕粉底液",
    "brand": "Example Brand",
    "price": 899,
    "currency": "TWD",
    "description": "商品描述文字",
    "imageUrls": [
      "https://example.com/images/product-main.jpg"
    ],
    "category": "foundation",
    "specs": {
      "color": "自然色",
      "volume": "30ml"
    },
    "rawText": "可選，商品頁主要文字摘要",
    "missingFields": []
  }
}
```

### 6.2 部分成功回傳格式

有些網站可能抓得到名稱與圖片，但抓不到價格。此時不要直接視為完全失敗，建議回傳 partial。

```json
{
  "success": true,
  "status": "partial",
  "message": "商品資料部分爬取成功，部分欄位需要管理員手動補齊",
  "data": {
    "sourceUrl": "https://example.com/product/123",
    "sourceSite": "example.com",
    "productName": "水光保濕粉底液",
    "brand": null,
    "price": null,
    "currency": "TWD",
    "description": "商品描述文字",
    "imageUrls": [
      "https://example.com/images/product-main.jpg"
    ],
    "category": null,
    "specs": {},
    "rawText": "可選，商品頁主要文字摘要",
    "missingFields": [
      "brand",
      "price",
      "category"
    ]
  }
}
```

### 6.3 失敗回傳格式

```json
{
  "success": false,
  "status": "failed",
  "message": "無法爬取此商品頁，可能是網站阻擋或網址格式不支援",
  "error": {
    "code": "SCRAPE_BLOCKED",
    "detail": "403 Forbidden"
  }
}
```

---

## 7. 必要欄位與選填欄位

### 7.1 建議爬蟲端至少回傳

最少需要：

- sourceUrl
- productName
- imageUrls
- price

如果能回傳這四項，後台就能建立基本商品預覽。

### 7.2 可選欄位

可選欄位：

- brand
- description
- category
- specs
- currency
- rawText
- sourceSite

### 7.3 後台可人工補齊欄位

以下欄位不一定要求爬蟲端提供：

- 適合膚色標籤
- 商品風格標籤
- 系統內部分類
- 商品狀態
- 是否推薦商品
- 商品排序權重

---

## 8. 商品欄位對應建議

| 爬蟲端欄位 | 後台表單欄位 | 正式商品資料欄位 | 備註 |
|---|---|---|---|
| productName | 商品名稱 | name | 必填 |
| price | 商品價格 | price | 建議轉成數字 |
| currency | 幣別 | currency | 預設 TWD |
| imageUrls[0] | 主圖 | imageUrl | 可先使用第一張圖 |
| imageUrls | 商品圖片 | images | 多圖可選 |
| description | 商品描述 | description | 可由管理員修改 |
| brand | 品牌 | brand | 抓不到可留空 |
| category | 分類 | category | 可能需要後台重新選 |
| sourceUrl | 來源網址 | sourceUrl | 建議保留 |
| sourceSite | 來源網站 | sourceSite | 方便追蹤 |
| specs | 商品規格 | specs | JSON 格式 |

---

## 9. 錯誤狀態建議

爬蟲端請盡量回傳明確錯誤代碼，方便後台顯示對應訊息。

| error code | 說明 | 後台顯示建議 |
|---|---|---|
| INVALID_URL | 網址格式錯誤 | 請輸入有效商品網址 |
| UNSUPPORTED_SITE | 不支援此網站 | 目前尚未支援此商品來源 |
| FETCH_TIMEOUT | 請求逾時 | 網站回應過慢，請稍後再試 |
| SCRAPE_BLOCKED | 被網站阻擋 | 此網站可能阻擋爬蟲 |
| PARSE_FAILED | 解析失敗 | 無法解析商品資料，請手動新增 |
| NO_PRODUCT_FOUND | 找不到商品內容 | 此網址可能不是商品頁 |
| UNKNOWN_ERROR | 未知錯誤 | 系統發生錯誤，請稍後再試 |

---

## 10. 爬蟲技術建議

### 10.1 靜態網頁

如果商品資料直接存在 HTML 中，可使用：

- requests
- BeautifulSoup
- lxml

適合：

- 商品名稱、價格、圖片直接在 HTML 原始碼內。
- 不需要等待 JavaScript 載入。

### 10.2 動態網頁

如果商品資料由 JavaScript 動態載入，建議使用：

- Playwright
- Selenium

適合：

- 商品內容需要瀏覽器渲染後才出現。
- 圖片、價格、規格由 API 或 JS 動態載入。

### 10.3 優先解析來源

建議爬蟲端優先嘗試以下資料來源：

1. JSON-LD 結構化資料。
2. Open Graph meta tag。
3. 商品頁 HTML selector。
4. 頁面文字 fallback。

JSON-LD 常見於商品頁，若有 `Product` 結構，通常能取得較乾淨的商品名稱、圖片、價格與品牌資料。

---

## 11. 圖片處理策略

### 11.1 第一階段建議

第一階段可以先回傳圖片 URL，不一定要由爬蟲端下載圖片。

```json
{
  "imageUrls": [
    "https://example.com/product.jpg"
  ]
}
```

後台顯示圖片預覽即可。

### 11.2 第二階段可擴充

若後續要更穩定，可以改成：

1. 爬蟲端取得圖片 URL。
2. 後端下載圖片。
3. 上傳到自己的 storage。
4. 商品資料庫存自己的 imageUrl。

這樣可以避免原網站圖片失效，但會增加後端與儲存空間負擔。

---

## 12. 安全與權限限制

此功能只允許管理員使用。

建議限制：

- 只有 admin token 可以呼叫商品匯入 API。
- 不開放一般使用者使用爬蟲功能。
- 限制單次只能爬一個網址。
- 限制短時間內呼叫次數，避免被當成大量爬蟲工具。
- 爬蟲端不要直接寫入正式商品資料庫。
- 所有爬取結果都必須經管理員確認。

---

## 13. 合法性與展示說法

此功能在專題中建議描述為：

```text
管理員商品資料輔助匯入功能
```

不要描述成：

```text
自動大量爬取全網商品
```

原因是大量爬取可能牽涉網站條款、反爬蟲機制、圖片與文字版權問題。專題展示時可以強調：

- 管理員手動輸入網址。
- 系統只做輔助擷取。
- 管理員確認後才新增商品。
- 此功能主要提升商品建檔效率。

---

## 14. 測試案例

### 14.1 成功案例

輸入有效商品頁網址。

預期：

- 回傳 success true。
- status 為 ok。
- 至少有 productName、price、imageUrls、sourceUrl。
- 後台可顯示預覽。

### 14.2 部分成功案例

輸入商品頁，但價格抓不到。

預期：

- 回傳 success true。
- status 為 partial。
- missingFields 包含 price。
- 後台顯示資料不完整提示。
- 管理員可手動補價格。

### 14.3 失敗案例

輸入非商品頁網址。

預期：

- 回傳 success false。
- error code 為 NO_PRODUCT_FOUND 或 PARSE_FAILED。
- 後台顯示無法匯入，請手動新增。

### 14.4 網址錯誤案例

輸入錯誤網址格式。

預期：

- 回傳 success false。
- error code 為 INVALID_URL。
- 後台提示請輸入有效網址。

---

## 15. 第一階段最小可行版本

第一階段不需要一次做太完整，建議先完成以下功能：

### 爬蟲端

- 接收一個商品網址。
- 回傳商品名稱。
- 回傳商品價格。
- 回傳商品主圖 URL。
- 回傳商品描述。
- 失敗時回傳錯誤原因。

### 後台端

- 建立 admin 商品匯入頁。
- 管理員可輸入商品網址。
- 呼叫爬蟲 API。
- 顯示商品預覽表單。
- 管理員確認後存入商品資料庫。

### 暫時不做

- 批次爬取。
- 自動上架。
- 自動分類。
- 自動判斷膚色適合度。
- 圖片自動下載與轉存。
- 多網站完整支援。

---

## 16. 爬蟲端交付內容

爬蟲端組員建議交付：

1. 爬蟲 API endpoint。
2. Request/Response 格式符合本文件。
3. 至少一個可成功爬取的測試來源網站。
4. 錯誤處理與錯誤代碼。
5. 簡單測試說明。
6. 若有支援網站限制，需列出目前支援哪些網站。

---

## 17. 後台端與爬蟲端對接確認清單

正式串接前請確認：

- API URL 是否確定。
- Request 欄位是否一致。
- Response 欄位名稱是否一致。
- 價格是否一定回傳數字。
- 圖片是否回傳完整 URL。
- 失敗時是否一定有 error code。
- partial 狀態是否會回傳 missingFields。
- 爬蟲端是否會直接寫資料庫。
- 後台端是否有權限檢查。
- 管理員是否可以修改爬取結果。

---

## 18. 對教授說明版本

可以向教授說明：

```text
原本後台已有手動新增商品功能，後續會將管理員介面與一般使用者介面分流，讓管理員登入後直接進入專門的 admin dashboard。在商品管理中新增「從網址匯入商品」功能，管理員輸入商品頁網址後，系統會呼叫爬蟲端取得商品名稱、價格、圖片與描述，並先顯示預覽表單。管理員確認與修改後，才會正式新增到商品資料庫。這樣可以提升商品建檔效率，同時避免爬蟲資料錯誤直接進入正式系統。
```

---

## 19. 結論

此功能可行性高，但建議定位為「後台管理員輔助匯入商品」，不要定位成大量自動爬蟲。  
第一階段只需要支援單一網址匯入與人工確認，就能完成一個完整且合理的後台爬蟲整合流程。

