# 給資料庫端：待修清單（2026-07-25）

> 日期：2026-07-25
>
> 這份接續 `給資料庫端_待修清單_Cursor版_2026-07-22.md`，不取代它。
> 內容全部是 **2026-07-25 當天在正式站 `https://decorate-me.web.app` 實測到的結果**，
> 不是讀程式碼推測的。每一項都附了我們怎麼確認「問題在資料庫端、不在 Gateway」的證據。
>
> 禁止寫入本文件、Log、Git 或訊息：真實密碼、完整 Cookie 值、API Key、JWT、
> 完整會員 email、會員照片。
>
> 本文件提到的 `admin@decorateme.local` 是系統管理帳號、`gfrghhjryj` 是測試用的無效識別碼，
> 兩者都不是真實會員個資，故保留原字串以便定位。

---

## 〇、先謝謝你們

`members.email_verified_at` 欄位不存在導致 `/api/login` 全面 500 的問題（issue #29）
**已經修好了**，我們當天實測確認：

```
POST <tunnel>/api/login（不存在的帳號）        →  401   （先前是 500）
POST https://decorate-me.web.app/auth/login   →  401   （先前是 502）
```

登入流程恢復正常。以下是修好登入之後，接著浮出來的四件事。

---

## 一、【Demo 阻斷級】未驗證的註冊會把信箱永久佔住，使用者無法自救

> 對應 issue #32。這一項排在最前面：第二項只影響我們幾個管理員，這一項影響**現場每一個要註冊的人**。

### 現況

`POST /api/register` 當下就把信箱佔走了，`emailVerified` 只是那筆資料上的旗標。
實測（2026-07-25，正式站）：

```
POST /auth/register（全新信箱）  →  200，body 含完整 member 物件，"emailVerified": false
POST /auth/register（同一信箱）  →  409
```

也就是說，帳號在**驗證碼還沒寄出去之前**就已經建立並佔用信箱了。

### 造成的死結

只要驗證信沒送達（今天實際發生了），那個信箱的擁有者三條路全部被堵死：

| 使用者想做的事 | 實際結果 |
| --- | --- |
| 重新註冊 | **409** —— 信箱被自己那筆未驗證資料佔住 |
| 直接登入 | **403 `EMAIL_NOT_VERIFIED`** —— Gateway 有專門處理（`ai_gateway.py:1295`） |
| 輸入驗證碼 | **400** —— 他手上根本沒有碼 |

**使用者完全無法自救**，只能等人進資料庫手動處理。這和 issue #22（軟刪除導致信箱永久卡住）是同一個病根。

Demo 當天只要有一個人沒收到信，他就再也註冊不了，現場沒有任何人能幫他解開。

### 請你們處理（三項，第 1 項最重要）

1. **未驗證狀態下用同一個信箱重新註冊時，不要回 409** —— 覆寫那筆未驗證資料並重寄驗證碼。
   已完成驗證的帳號才回 409。這是業界標準做法，也是唯一能讓使用者自己脫困的路。
2. **未驗證的註冊要有存活時間**（例如 24 小時），到期自動清除，不要讓死資料永久佔著信箱。
3. **查驗證信為什麼沒寄出** —— SMTP 設定、寄件配額、退信 log。這是根因；前兩項只是讓人不會被鎖死。

### 我們這邊已經做的止血

前端原本的註冊按鈕在請求期間沒有鎖住，而寄信很慢，使用者以為沒反應就會再點一下——
第二下必然拿到 409，於是「一次驗證碼都沒收到，卻被告知信箱已註冊」。已修：送出期間鎖定按鈕、
按鈕顯示「寄送驗證碼中…」，並把 EMAIL_EXISTS 的主要出口從「返回登入」改成「重寄驗證碼」。

但這只是止血。信寄不出去、第 1、2 項沒做，使用者還是會卡住。

---

## 二、【會擋住整個後台】管理員的 session 前置檢查

### 症狀

管理員在後台改會員權限並按儲存，**8 筆全部失敗**，訊息一律是：

```
目前無法確認登入狀態，為避免寫錯帳號已中止這次操作，請稍後再試。
```

請注意：這些 PATCH **根本沒送到你們那邊**，你們的 log 不會有任何紀錄。
這是前端在送出前的自我檢查就中止了，所以「查不到請求」是正常的，不代表沒發生。

### 我們懷疑的原因

Gateway 的 `/auth/session` 不只驗自己的 JWT，它會**拿管理員的 session cookie 回頭跟你們確認一次**
（`ai_gateway.py:798`）：

```
GET {MEMBER_DATABASE_URL}/api/members/<sub>
Cookie: <該管理員的 session cookie>
```

其中 `<sub>` 取自 JWT，目前實際值是 **`admin@decorateme.local`**。

這條只要回 **401 / 403 / 404**，Gateway 就判定整個 session 無效
（`ai_gateway.py:812-816`，回 `MEMBER_SESSION_INVALID`），前端接著就中止所有寫入。

**也就是說：只要 `admin@decorateme.local` 不是一筆「帶著自己的 cookie 讀得到」的 member row，
管理員就永遠通不過這道檢查——即使帳號密碼正確、role 確實是 admin、後台畫面看起來完全正常。**

### 請你們確認

帶著 admin 自己的 session cookie，打這條：

```
GET /api/members/admin@decorateme.local
```

| 回應 | 代表 |
| --- | --- |
| **200** | 這條沒問題，請告訴我們，我們回頭查前端 |
| **404** | admin 帳號不在 `members` 表裡（或查詢條件排除了它）→ 請補成可讀取的 row |
| **401 / 403** | 這個帳號讀不到自己的資料 → 授權判斷需要放行「讀自己」 |

只要不是 200，整個後台的寫入都會是死的。

### 附註

這一項我們標成「待確認」而不是「確定的 bug」——也可能只是那張 cookie 過期
（Gateway 的 session 有效期是 2 小時）。我們這邊會先重新登入排除過期的可能，
但上面那條 `GET /api/members/admin@decorateme.local` 無論如何都該回 200，
麻煩一併確認。

---

## 三、`/api/members/<非 email 的識別碼>` 回 422，且資料表裡有壞資料

### 症狀

後台載入會員清單時，對其中一筆發出的請求回 **422**：

```
GET /member-database/api/members/gfrghhjryj/points        →  422
GET /member-database/api/members/gfrghhjryj/saved-looks   →  422
```

`gfrghhjryj` 不是 email——它是 `members` 表裡一筆測試帳號的識別碼。

### 為什麼確定不是 Gateway 的問題

- Gateway 的路徑白名單有放行 `api/members/[^/]+/points`（`ai_gateway.py:124`），沒有擋
- 同一條路徑**不帶 cookie** 打，回的是 **401**（授權層），不是 422
- Gateway 對這條路由是原樣透傳上游狀態碼

所以 422 是你們回的驗證錯誤，行為本身合理——問題是**資料庫裡不該有這筆資料**。

### 請你們處理

1. 清掉 `members` 表裡 email 欄位不合法的測試資料（至少 `gfrghhjryj` 這筆）
2. 如果註冊流程允許非 email 字串寫進 email 欄位，請補上格式驗證——否則清掉還會再長出來

我們這邊也會補一道前端防護，不對格式不合法的識別碼發請求。但根本原因在資料，
前端擋住只是讓畫面不報錯，那筆壞資料還是在。

---

## 四、`GET /api/admin/product-audit-logs` 尚未實作

### 症狀

後台商品稽核分頁載入失敗：

```
GET https://decorate-me.web.app/admin-api/product-audit-logs?limit=100   →  404
```

### 為什麼確定不是 Gateway 的問題

不帶 cookie 打同一條，回的是 **401 而不是 404**：

```
GET /admin-api/product-audit-logs?limit=1   →  401
GET /admin-api/products                     →  401
```

Gateway 如果沒有這條路由，不管帶不帶 cookie 都會 404。回 401 就證明**路由存在、
管理員授權也通過了**，那個 404 是從你們那邊原樣轉回來的
（`ai_gateway.py:1603` 直接透傳上游狀態碼）。

Gateway 的轉發目標是（`ai_gateway.py:1627-1629`）：

```
GET {PRODUCT_DATABASE_URL}/api/admin/product-audit-logs
```

### 請你們處理

實作這支端點。這對應我們這邊的 issue #20（商品稽核分頁尚未併入正式前端），
優先度最低——前面三項不修，註冊與後台都是不能用的；這一項只是少一個分頁。

介面沿用你們既有的 admin 慣例即可：

- 認證：`Authorization: Bearer <PRODUCT_ADMIN_API_KEY>`（Gateway 會帶）
- Gateway 另外會帶 `X-Admin-Actor`（雜湊過的管理員識別碼）與 `X-Request-ID`
- 查詢參數：`limit`
- 回應：JSON 陣列或 `{items: [...]}`，欄位請告訴我們，我們配合前端

---

## 五、延續 issue #29 的提醒：正式環境請關掉 debug 錯誤頁

7/25 早上那個 500，回的是完整的 Werkzeug traceback 網頁，內容包含 SQL 語句與資料表欄位名。
現在打不出 500 了，所以我們無法確認這件事有沒有一併處理。

正式環境的錯誤請回結構化 JSON（例如 `{"success": false, "error": {"code": "..."}}`），
traceback 只寫進伺服器 log。否則任何打得到那個網址的人，都能靠觸發錯誤把你們的 schema 撈出來。

---

## 六、驗收

四項修完後，我們會實際打這幾條確認：

| 檢查 | 期望 |
| --- | --- |
| 用一個已存在但未驗證的信箱重新註冊 | 不再回 409，改為重寄驗證碼 |
| 註冊後收得到驗證信 | 收得到 |
| 後台改權限並儲存 | 全部成功，不再出現「目前無法確認登入狀態」 |
| `GET /api/members/admin@decorateme.local`（帶 admin cookie） | 200 |
| 會員清單中不再出現 email 格式不合法的帳號 | — |
| `GET /admin-api/product-audit-logs?limit=100`（帶 admin cookie） | 200 |

---

## 附錄：通道網址

7/25 當天網址換了三次，最後一次是 `https://whale-lone-football-booking.trycloudflare.com`，
我們都有同步更新，商品與會員兩條線都通。

**注意：資料庫每重啟一次，它先前發出的 session cookie 就全部失效，線上所有使用者立刻被登出。**
7/25 下午換三次網址＝全場被踢出去三次。這在 Demo 當天會是明顯的事故。

Quick Tunnel 每次重啟都會換網址，**換址時麻煩主動通知我們**（issue #23），
我們要同步更新 Cloud Run 上 `ai-gateway` 的 `MEMBER_DATABASE_URL` /
`PRODUCT_DATABASE_URL`。在那之前，換址期間整站的會員與商品功能都是斷的。

長期解法還是請改用固定網址的 Named Tunnel，Demo 前尤其重要。
