# Decorate Me 系統資安、效率與流程改善總表

> **這是單一權威版**，已合併並取代下列四份（原檔可刪）：
> - 系統資安效率流程改善清單（2026-07-21）
> - 系統改善待辦清單（2026-07-18）
> - 全系統審查（2026-07-10）
> - 流程優化建議（2026-07-08）
>
> 最後更新：2026-07-23
> 使用方式：完成一項就把 `[ ]` 改成 `[x]`；取消的項目保留並註明原因，不直接刪。
> 禁止記錄：實際 API key、JWT、密碼、完整 email、照片、完整分析包與完整生成提示。
> Demo 例外：Ollama 完整 Prompt 暫留在受控展示回應，**不得寫入一般 Log**，等 Ollama 調整完成後移除。
> 相關文件：`歷史流程更改追蹤.md`（完整歷程）、`全系統資安CodeReview驗收紀錄_2026-07-23.md`（最新驗收）。
> 分工圖例：**🔧 我方可做／已做**　**⛔ 需外部組（資料庫／Ollama／爬蟲／演算法）**　**🧭 需先決策**

---

## 一、目前狀態快照（截至 2026-07-23，已上線）

### 資安（已完成）

- [x] 前端不再保存 Face／Render／文字建議上游 API key；公開 `config.local.js` 不含金鑰。
- [x] Face／Render／Gateway 正式秘密移至 Secret Manager；範例設定與文件不含秘密值。
- [x] 前端登入、會員、商品、Admin 請求統一走同源 Gateway；瀏覽器只保存 HttpOnly session。
- [x] Gateway 以 Cloud Run IAM 呼叫 Face／Render；三個核心服務移除 `allUsers`，匿名直連皆 `403`。
- [x] Firebase Hosting 加入會員／商品／Admin／媒體同源 rewrite；瀏覽器拿不到資料庫原始網址。
- [x] 正式 Gateway 的外部 Ollama Tunnel 關閉。
- [x] `decorate-me-renders` bucket 已私有（Public Access Prevention＋Uniform BLA），匿名直開物件 `403`。
- [x] **跨分頁帳號隔離上線**：會員／收藏／點數／打卡／任務／主題／Admin 寫入一律帶 `X-Expected-Actor`，不符或缺少即在到達上游前回 `409`；`api/members` 名冊只給 Admin。
- [x] **前端 XSS 收斂**：`showToast`、問候語、Avatar、購物車、商品卡欄位全部 escape；圖片走 `lookImageSrc` 驗 scheme；外部連結走 `safeExternalUrl`（只允許 http(s)）。
- [x] **Access log 去識別化**：路徑中的 email（含 `%40`）遮成 `<member>`；一般 log 只記 requestId／method／path／status／耗時，不記 body、照片、token、prompt。
- [x] **登入限流集中化＋帳號維度**：Firestore 交易式固定視窗（跨 instance 一致），另加以雜湊帳號為鍵的維度（擋分散式暴力破解），in-memory 僅本機 fallback。
- [x] **Render SSRF 防護**：`fetch_remote_image_bytes` 只允許 https＋白名單 host（`replicate.delivery`／GCS，可用 `RENDER_FETCH_ALLOWED_HOSTS` 覆寫）＋443 埠＋不跟隨轉址，並串流下載、逐塊卡 `MAX_RENDER_IMAGE_BYTES`。
- [x] **登出／換帳號清當前帳號 localStorage PII**（收藏臉圖 URL、分析回饋）；只清當前帳號，不動其他帳號收藏。
- [x] 私有媒體以 job＋ownerId 授權；legacy sign/read/delete 非 owner 回 404。
- [x] 圖片解壓縮炸彈：BASIC／PRO／Render 都有像素數上限（不只驗位元組）。
- [x] `ai-gateway` 服務帳號無專案層角色（最小權限），invoker 為 per-service。

### 效率（已完成）

- [x] 上傳照片有 Web Worker／OffscreenCanvas 壓縮；預設壓到最長邊 1024px、JPEG 0.78。
- [x] 商品／風格圖有原生 `loading="lazy"` 與 `decoding="async"`。
- [x] Render 用非同步 job，前端不需等單一長連線；有短時間去重避免重複消耗額度。
- [x] 七張風格圖轉 720px WebP，總大小約 10 MB → 約 263 KB（-97%）。
- [x] `/assets/**` 一年 immutable 快取；HTML／JS／CSS 設 `no-cache`。
- [x] 後台搜尋 debounce、自動刷新節流、JS `defer` 並行下載。
- [x] Render 服務 CORS 由 `*` 收斂為讀 `CORS_ORIGINS` 白名單（與 BASIC／PRO 一致）。

### 流程（已完成）

- [x] Gateway、Hosting、Face、Render 有基本健康檢查。
- [x] 歷史流程追蹤與 Gateway 新手文件、CodeReview 驗收紀錄齊備。
- [x] 前端 `deploy.ps1` 部署前執行語法檢查、smoke test、秘密掃描並檢查部署結果；線上 `/public-config` 只回同源路徑。
- [x] 依賴鎖版本（`requirements.gateway.txt`／`requirements.render.txt`）。
- [x] 最小 CI（GitHub Actions）：後端測試＋秘密掃描（阻擋）＋依賴漏洞掃描（諮詢）。
- [x] 兩個 repo（後端 `Isa`、前端 `dev_makeup`）已 commit 與 push。

---

## 二、P0：正式／Demo 前必須完成

### P0-1 私有 GCS 與短效 Signed URL

- [x] bucket 已私有、移除 `allUsers`；登入後透過 Gateway 媒體端點讀圖。
- [x] Lifecycle 2 天後刪 `temporary/`；`retained/` 不受影響。新渲染先存 `temporary/`，收藏後移至 `retained/{opaqueOwnerId}/{jobId}`。
- [x] 前端保存穩定 Gateway 媒體路徑，不把一次性 provider URL 當永久收藏；Gateway 回應移除 provider 暫存網址。
- [ ] ⛔🧭 真正的 GCS V4 Signed URL：需只對指定服務帳號授予自簽 Token Creator（目前用登入驗證串流代理替代）。
- [ ] ⛔ 資料庫端確認不再保存任何舊公開 URL。

### P0-2 會員刪除跨服務清除

- [x] GCS 擁有者目錄用不可逆雜湊 opaque owner ID，不用完整 email。
- [x] 刪 saved look 時 Gateway 同步刪對應 Render job 與 GCS 物件；新收藏保留 `renderJobId`。
- [x] 刪物件失敗即保留 job、標 failed 並可重試，不回報假成功（R7）。
- [ ] ⛔ 會員刪除改用可重跑也安全的 deletion job（`pending → deleting → completed／failed`），Admin 不能收到 200 就當全部完成。
- [ ] ⛔ 清除會員主資料、session、點數、任務、主題兌換、Face job 與資料庫端所有分析欄位（需有效帳號端到端確認）。
- [ ] ⛔ Face Pro／Render／Gateway 限流集合 TTL 建立中，待雲端狀態全 `ACTIVE`。
- [ ] ⛔ 建立刪除後逐項確認查不到資料與物件的自動化整合測試。

### P0-3 Log 去識別化

- [x] Access log 路徑對 email（含 `%40`）遮罩；一般 log 不記 body、照片 base64、分析包、權杖、Prompt。
- [x] Admin audit 與畫面用雜湊 `actorId`，不保存完整管理員 email。
- [ ] ⛔ 會員 API 改用 `memberId`／`/me`，從源頭避免 email 進 URL。
- [ ] 🔧 例外 stack trace 過濾 URL query、Authorization 與上游錯誤內容。
- [ ] 🧭 設定 Cloud Logging 保存期限與存取權限。
- [ ] Demo 例外：Ollama 完整 Prompt 暫留受控展示，Ollama 完成後移除；不得寫入一般 Log。

### P0-4 登入 Session 與立即撤銷

- [x] 正式同源模式只用 `HttpOnly + Secure + SameSite=Lax` cookie；session-only 模式不在 JSON 回傳 access token。
- [x] 進私有頁前先 `/auth/session`，Gateway 用封裝 cookie 實讀會員端點確認兩層 session 有效，失敗不載入資料。
- [x] 前端第一個 `401`／`409` 統一清除殘留 session、取消同批請求、停止背景刷新、回登入頁（只處理一次）。
- [x] 正式前端移除 `sessionStorage.memberAccessToken`；完整分析包改本分頁 `sessionStorage`＋30 分鐘 TTL。
- [ ] 🧭 Token TTL 由 2 小時評估縮短至 15～30 分鐘。
- [ ] ⛔ 資料庫增加 `sessionVersion`／`tokenVersion`，停權／刪除／改密碼時立即撤銷 session（另見多帳號多重 session 規格）。

### P0-5 Gateway 成為前端唯一 API 入口（已完成）

- [x] `/public-config` 不再交出資料庫原始網址；登入只呼叫 Gateway；會員／收藏／點數／任務／主題／商品／Admin 統一經 Gateway。
- [x] Gateway 通用 Proxy 補上 `PATCH`，GET／POST／PATCH／DELETE 均有測試涵蓋。

### P0-6 臨時 Tunnel 退出正式環境

- [ ] ⛔ 會員／商品資料庫由 `trycloudflare.com` 搬到固定 Cloud Run／正式網域；爬蟲服務同。
- [ ] ⛔ Ollama 若正式使用改 Cloud Run／VPC／固定端點。
- [ ] 不把臉部摘要、prompt 或會員資料送到未受控公開 Tunnel；Tunnel 只允許 Demo 假資料並設到期日。

### P0-7 驗證與服務存取（源自 07-18）

- [ ] 🔧 正式環境缺 `FACE_API_KEY`／`SUGGESTION_API_KEY`／`RENDER_API_KEY` 時服務拒絕啟動；本機免驗證只能靠明確旗標（如 `ALLOW_INSECURE_LOCAL_DEV=1`）。
- [x] API key 與 Job token 使用固定時間比較。
- [x] Face／Render／Suggestion 禁止匿名直呼，只允許 Gateway service account。
- [ ] ⛔ 建立金鑰輪替流程，輪替曾出現在前端／文件／Log／備份／Git 歷史的金鑰（含 Ollama S5）。

### P0-8 Job token 與工作結果權限

- [x] 只接受 `X-Job-Token` header，查詢 Job 同時驗證登入會員、Job 擁有者與 token；禁止只靠 `jobId` 取結果。
- [ ] ⛔ 資料庫只保存 Job token 的 HMAC／雜湊；token 在工作過期／取消／刪除後立即失效。
- [x] Token／JWT／API key／Signed URL 不寫入一般 Log。

### P0-9 圖片上傳安全

- [x] Gateway／平台層限制 request body 大小；BASIC／PRO／Render 有位元組與像素上限。
- [ ] 🔧 上傳採分段讀取，超限立即停止；驗證 MIME magic bytes 與實際解碼結果。
- [ ] 🔧 移除 EXIF／GPS；只允許 JPEG／PNG／WebP 白名單；解碼 timeout；不把錯誤或超大圖送第三方。

### P0-10 測試與部署門檻

- [x] 前端 smoke test；後端 `ai_gateway_test.py`＋`render_api_test.py`（47 測試）。
- [x] CI 執行後端測試、secret scan、dependency 漏洞掃描。
- [ ] 🔧 補 `pytest`、正式環境缺金鑰拒啟動測試、跨會員 Job token 測試、偽 MIME／壓縮炸彈測試。
- [ ] 🔧 補 BASIC／PRO／Suggestion smoke test；CI 加 lint／type check／Docker image scan；測試失敗禁止部署 production。

---

## 三、P1：近期應完成

### 前端 XSS 與安全標頭

- [x] HSTS、`nosniff`、`DENY frame`、Referrer Policy、Permissions Policy；使用者／DB／爬蟲文字輸出 escape；外部 URL 驗 scheme。
- [ ] 🔧 inline script／style／`onclick` 改事件監聽器，之後啟用嚴格 CSP（不用 `unsafe-inline`）。
- [ ] ⛔ 管理員輸入的商品圖片／來源 URL／名稱／描述做 schema 驗證。
- [ ] 🔧 Admin 修改／刪除加 CSRF token 或 double-submit cookie（目前以 `X-Expected-Actor` 緩解跨帳號寫入）。

### 權限與管理員

- [x] Admin 讀寫由後端確認 `role=admin`（Gateway `proxy_admin_request`）；前端隱藏按鈕不當權限控制。
- [ ] 🧭 Admin Demo 專題結束立即關閉，不等 2026-12-31；高風險操作加重新驗證與二次確認。
- [ ] 🔧 商品刪除、會員停權／刪除保存不含 PII 的 audit event。
- [ ] 🧭 Secret Accessor 只授權需要的服務帳號，移除共用 compute service account。

### 限流與防濫用

- [x] 登入限流跨 instance（Firestore）＋帳號維度。
- [ ] ⛔ OTP 加 email／IP／裝置／每日總量限制。
- [ ] ⛔ Face／Render 每會員配額與全站成本上限；重複 job 用 idempotency key；額度依可信 `userId` 計。
- [ ] 🔧 429 一致回 `Retry-After` 並在前端顯示等待時間；限制輪詢頻率並退避。

### 圖片與媒體安全

- [x] `temporary/` 獨立暫存＋2 天生命週期自動刪未收藏工作。
- [ ] 🔧 上傳驗 MIME magic bytes、移除 EXIF／GPS、限像素／檔案／解碼記憶體、轉碼成 JPEG／WebP 再送分析。
- [ ] 🔧 Avatar 不存 sessionStorage base64，改存私有 object name。

### 工作資料分層（源自 07-18，多為 ⛔）

- [ ] ⛔ 分開一般 Log／Job 索引／敏感 payload／永久妝容紀錄；`jobs/{jobId}` 只放非敏感摘要，`job_payloads/{jobId}` 限 Worker 存取。
- [ ] ⛔ 用內部不可變 `userId` 關聯工作，不用 email 當主鍵；前端只取摘要不取完整 landmarks／prompt。
- [ ] ⛔ 敏感 payload 設 `expiresAt` 自動清除；完成／取消／失敗逾期／會員刪除後清除。

### 統一 Job 狀態與背景工作可靠性（源自 07-18，多為 ⛔）

- [ ] ⛔ 各服務統一狀態與階段命名，狀態轉換用 transaction／guard（禁止完成工作退回 running）。
- [ ] ⛔ Render／Face 長工作不再用程序內 daemon thread／`BackgroundTasks`，導入 Cloud Tasks／Pub/Sub 持久佇列；分離 API 與 Worker。
- [ ] ⛔ Worker 加 lease／heartbeat／逾時回收、最大重試＋指數退避、dead-letter queue、idempotency、provider 成功但 DB 失敗的補償。

### 私人 GCS 與生命週期、會員刪除連動（源自 07-18，多為 ⛔）

- [ ] ⛔ 資料庫只存 object path 不存永久公開網址；驗證後簽 5～15 分鐘短效 Signed URL，不落庫、不寫 Log。
- [ ] ⛔ 會員刪除：停用帳號＋撤銷 session＋禁止建新工作；可重試 `deletionJobId`；刪暫存／原圖／渲染／payload／紀錄／收藏／各 Job；核對無殘留；只留不含個資的稽核證明；失敗自動重試並告警。

### Log、隱私與權限（源自 07-18）

- [x] 一般 log 不含照片／base64／Signed URL／完整分析包／權杖／prompt；用 requestId／jobId／stage／status／duration／errorCode。
- [ ] ⛔ 隱私告知：蒐集目的、保存期限、刪除方法、第三方 AI 處理者；送第三方渲染前取得同意；提供刪單筆紀錄與刪帳號功能；保存矩陣與 TTL／lifecycle 一致。

### 效率：圖片載入 / JS-CSS / API-DB / Firestore / 模型（源自 07-21、07-18、07-10）

- [ ] ⛔ 商品資料庫建 320／640／1280 縮圖；前端用 `srcset`／`sizes`；外部圖經 image proxy／CDN 轉 WebP／AVIF；首屏主圖 `fetchpriority="high"`。
- [ ] 🔧 `router.js`／`main.css` 巨檔按頁拆 chunk，Admin 程式只在管理員進入時載入；正式 build pipeline minify／tree-shaking／hash；建立 Lighthouse／Core Web Vitals 基準。
- [ ] ⛔ 補回商品稽核紀錄與爬蟲搜尋預覽端點（或移除無後端的前端入口）；Admin 改批次 summary 端點避免 N+1；建 memberId／createdAt／status 索引。
- [ ] 🔧 Render 輪詢改指數退避或 SSE／WebSocket；大照片改 multipart／私有 object upload 不放 JSON base64。
- [ ] ⛔ `/health` 不做 Job 清理或大量掃描；建 `/ready`；查詢用索引不掃 `all_jobs()`；Firestore TTL＋Cloud Scheduler 分頁清理；監控 read/write 費用。
- [ ] 🔧 圖片正規化一次供 InsightFace／MediaPipe／ROI 共用；CPU 密集工作移出 async loop；模型只載一次並 warmup；記錄各階段耗時；dedup key 含模型版／prompt 版／參數／會員範圍。
- [ ] 🔧 收藏 before 圖改上 GCS 存網址，不以 base64 存 DB（每人 50 筆很肥）。

### Docker 與供應鏈（源自 07-18、07-21）

- [x] Requirements 鎖定精確版本；定期 dependency vulnerability scan（CI 諮詢性）。
- [ ] 🔧 Docker base image 固定版本＋digest；改非 root `USER`；runtime 移除編譯工具；各服務最小獨立映像。
- [ ] 🔧 產生並保存 SBOM；定期掃 Git 歷史／備份／Log／文件中的秘密；正式密鑰全在 Secret Manager。

### Demo 穩定性（源自 07-21、07-18）

- [ ] 🔧 Demo 前必要 Cloud Run 設 `min-instances=1`，結束後調回 0 控成本；準備固定假帳號／照片／離線備援結果並標示。
- [ ] 🔧 Demo 前一天測登入／上傳／分析／推薦／渲染／保存／刪除；修正 `start_full_stack_local.bat` 舊路徑；補回前端 `dev_server.py`；本機一鍵啟動＋健康檢查。
- [x] Admin 專題展示：Job ID／status／stage／progress，遮罩資料，分展「前端可見／後端暫存／系統 Log」。
- [ ] 🔧 資料包摘要分人臉／妝容／推薦／渲染四段；展示私有 GCS＋擁有者驗證＋Signed URL；展示會員刪除跨服務清理進度。

---

## 四、P2：流程與維運成熟度

### 開發與部署流程

- [ ] 🔧 建 dev／staging／production 三環境與不同秘密；不再從未提交工作目錄部署，標示舊 Hosting 目錄停用。
- [x] 前端部署前跑語法／smoke／secret scan；後端有 CI；commit 可追溯。
- [ ] 🔧 部署後自動檢查 Gateway health／主要頁面／公開設定；Cloud Run 無流量 revision → smoke → 逐步切流；保留一鍵 rollback。
- [ ] ⛔ 資料庫 schema migration 可重跑、可回復。
- [ ] 🔧 每項需求開 Issue（範圍／風險／驗收）；功能分支；PR 至少一人 review；合併後自動部署 staging。

### 監控與事件處理

- [ ] 🧭 建 401／403／429／5xx／模型失敗率／延遲告警；每日 Render 成本／配額／異常流量報表。
- [ ] 🧭 Secret 洩漏處理流程（輪替／停用／稽核）；資料刪除失敗告警與人工重試入口；每季備份還原演練。
- [ ] 🔧 建 circuit breaker；provider 每分鐘／每日請求與金額上限。

### 資料治理

- [ ] 🧭 定義照片／分析包／Render 圖／Log／會員資料保存期限；區分必要／選擇性／Demo 資料。
- [ ] ⛔ 會員資料下載與刪除申請流程；隱私政策說明 AI 供應商／保存時間／刪除方式；Demo 與正式資料用不同專案或 Bucket。

### 程式可維護性

- [x] 共用錯誤回應與 request ID middleware（`api_errors.py`）、共用 CORS。
- [ ] 🔧 抽出共用 API key／會員驗證、圖片驗證與清理、Job token 驗證模組；Pydantic 評估 `extra="forbid"`；全面 formatter／lint／type check。
- [ ] 🔧 清查 `.bak`／舊 Log／重複 hosting 目錄／Git 備份，確認後移出 Repo；中文程式與文件統一 UTF-8。
- [ ] 🔧 建單一 OpenAPI 契約與版本政策；完整不含秘密的環境變數範本；資料保存與刪除矩陣；服務擁有人責任表；故障排除／金鑰輪替／資安事件 runbook；模型版本／資料集／品質指標／回滾紀錄；文件整理為 `docs/current`／`docs/archive`。

---

## 五、邏輯不一致與待補端點（源自 07-10、07-08，多為 ⛔ 需外部組）

- [ ] ⛔🧭 點數兩套數字（會員中心 localStorage vs 後台 DB）：成長系統改接 DB 會員加點端點。
- [ ] ⛔ 分析歷史、商品收藏、購物車只存 localStorage，換裝置就沒：接 DB 端點（favorites String(500) 要修、cart 要建）。
- [ ] ⛔ 商品 `/recommend-products` 曾 404、`style` 參數被忽略：恢復端點＋修演算法。
- [ ] ⛔ `renderQuota` 真正資料來源定案（存 member DB／render 呼叫會員 API／後端簽 token 驗證）；目前只有 server-side rate limit，未做會員綁定配額。
- [ ] ⛔ Render 失敗不要回 `200 + status:failed`，改非 `2xx` 並統一 `error.{code,message,retryable}`（部分已於 Gateway 統一）。
- [ ] ⛔ Admin 權限儲存 bulk 化（現逐筆 `PATCH /api/members/{email}` 中途失敗會半成功），或明確回每筆成功／失敗。
- [ ] 🔧 分析成功但 package 封裝失敗要拆狀態（`analysis_completed` vs `package_build_failed`），不要整包標 failed。
- [ ] 🔧 PRO 文案與實際能力對齊（目前為正面分析＋側面膚色平均，非完整側臉特徵分類）。
- [ ] 🔧 登入後 profile 只信後端回傳，減少本機 `name/phone/age` 舊資料混入。
- [ ] 🔧（低）Render prompt 再結構化為固定段落（底妝／眉眼／腮紅修容／唇妝）。

---

## 六、里程碑

- **里程碑一 安全 Demo**：P0 驗證與 Job token、上傳限制與圖片驗證、Demo 前端與備援、資料包進度展示。
- **里程碑二 可靠非同步工作**：Queue 與獨立 Worker、Job 狀態／重試／冪等／補償、Firestore TTL 與 Scheduler 清理。
- **里程碑三 資料生命週期**：私人 GCS 與 Signed URL、敏感 payload 隔離與自動清除、會員刪除跨服務清理與驗證。
- **里程碑四 正式營運**：CI/CD／staging／回滾／安全掃描、監控／告警／費用熔斷／Runbook、隱私告知／保存期限／事件流程。

---

## 七、待決策（🧭）

- [ ] 原始照片、完整分析包、完整生成提示的保存期限；Signed URL 有效時間。
- [ ] 操作 Log 與安全稽核 Log 保存期限。
- [ ] Queue 用 Cloud Tasks 或 Pub/Sub。
- [ ] 正式會員 ID 格式與 email 遷移方式。
- [ ] 點數是否真接 DB；渲染圖是否由公開改私有＋簽名網址。
- [ ] Token TTL 是否由 2 小時縮短至 15～30 分鐘。
- [ ] 同瀏覽器多帳號分頁是否要做（見《給資料庫端_多重Session多帳號分頁規格》）。

---

## 八、分工總覽

- **🔧 我方（已做／可做）**：本表「一、目前狀態快照」全部已上線；剩餘我方可做的多為前端拆包、CSP、圖片驗證細節、Docker 加固、監控與部署流程。
- **⛔ 需外部組**：
  - 資料庫端：OTP 假信箱（S7）、登入枚舉（S55）、會員硬刪除跨服務清理與 `sessionVersion` 撤銷、多重 session、點數／歷史／收藏／購物車端點、Job token 雜湊、縮圖與批次端點、schema migration。
  - Ollama 端：金鑰輪換（S5）、服務身分限制、Prompt 展示 Demo 後移除。
  - 爬蟲端：後端 SSRF／host 驗證、商品資料 schema 驗證。
  - 演算法端：服務內資料最小化、TTL 清理、job owner 驗證。
- **🧭 需決策**：見「七、待決策」。
