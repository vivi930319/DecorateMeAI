# 會員資料庫端 — 待辦、端點、欄位（統整版）

> 產出：2026-07-10　｜　對象：會員 / 商品資料庫維護者（lavien）
> 這是**唯一一份、自包含**的文件：要做什麼行為（端點）＋要建什麼欄位（Schema）＋為什麼，都在這裡。
> 狀態以前端 curl 實測為準。目前 tunnel：`heard-complete-offices-terrain.trycloudflare.com`（會變，換了請告訴前端）。

---

## 0. 已經 OK、不用動（先讓你安心）
- ✅ 登入 / 註冊 / 會員清單 / 權限 / CORS / session cookie
- ✅ **妝容收藏 `saved_looks` 寫入正常**（實測有資料、含渲染圖網址）
- ✅ 商品列表 `/api/products`

---

## 待辦總覽（依急迫度）

| # | 項目 | 類型 |
|---|------|------|
| 1 | 點數：會員「賺不到」點（無法加點）→ 後台永遠 0 | 🔴 建端點+欄位 |
| 2 | 商品推薦 `/recommend-products` 回 404 | 🔴 恢復端點 |
| 3 | 登入無法區分「未註冊」vs「密碼錯」 | 🔴 改回應 code |
| 4 | OTP 後端未強制、可跳過 | 🔴 改行為 |
| 5 | 登入 session 易掉線 | 🟠 token / Max-Age |
| 6 | 商品推薦 `style` 參數被忽略 | 🟠 改演算法 |
| 7 | 商品收藏 favorites 500 + 缺 GET | 🟠 修/建端點 |
| 8 | 購物車、分析歷史沒端點 | 🟠 建端點 |

---

# 一、端點與行為要改

## 1. 🔴 點數：讓會員「賺得到」點（最急）
**問題**：會員做簽到/任務賺點，但**無法把點寫進資料庫**（`points/adjust` 只有 admin 能用），所以後端/後台點數永遠 0。

**要做**：提供「**會員本人可呼叫、伺服器端驗證**」的加點端點（防刷）：
| 方法 | 路徑 | 用途 |
|------|------|------|
| `POST` | `/api/members/{email}/check-in` | 每日簽到（伺服器判定今天是否已簽），加點寫 transactions，回新餘額 |
| `GET` | `/api/members/{email}/check-in` | 查今天是否已簽、連續天數 |
| `POST` | `/api/members/{email}/tasks/{taskId}/claim` | 領任務獎勵（驗證已達成、未領過）|
| `GET` | `/api/members/{email}/tasks` | 各任務 done/claimed 狀態 |
| `POST` | `/api/members/{email}/theme-shop/{themeId}/redeem` | 兌換主題（扣點）|
| `GET` | `/api/members/{email}/points` | **建議多回 `lifetime`**：`{balance, lifetime, transactions[]}` |

**點數規則（前端現行，請比照）**
- 簽到：每日 +10；連續第 3/7/14/30 天額外 +5/+20/+40/+100（疊加）。中斷歸 1。
- 任務：完成分析 +20、收藏商品 +10、成功推薦 +20、每日簽到 +5（各一次性，daily_checkin 每日）。
- 推薦碼：新會員註冊帶推薦碼 → 發 +20 給推薦人，擋自我推薦/重複。
- 主題商店（扣點）：rose 80、jade 120、noir 180（classic 免費）。
- 累計點數 `lifetime`：只增不減（等級門檻：銀卡 100 / 金卡 300）。

**驗收**：會員 `POST check-in` → `200` 且餘額增加（不是 401/403）；admin 讀該會員 points 看得到真實累積值。

## 2. 🔴 商品推薦 `/recommend-products` 回 404
實測新 tunnel 上 `POST /recommend-products` 仍 **404**。使用者做完分析看不到推薦商品。請確認/恢復正確路徑。前端送的 body：
```json
{ "faceShape":"...", "eyeShape":"...", "skinTone":{"season":"...","level":"...","lab":{"L":..,"A":..,"B":..}}, "lipLab":{"L":..,"A":..,"B":..}, "style":"港風" }
```

## 3. 🔴 登入要能區分「未註冊」vs「密碼錯」
**現況**：兩者都回一樣的 `401 INVALID_CREDENTIALS`，前端分不出來。
**要做**：用不同 `code`：
| 情況 | 建議 code | 前端會做 |
|------|-----------|----------|
| email 沒註冊 | `USER_NOT_FOUND` | 引導去註冊（帶入剛輸入的帳密）|
| 密碼錯 | `WRONG_PASSWORD` | 提示密碼錯誤，重新輸入 / 忘記密碼 |
```json
{ "error": { "code": "USER_NOT_FOUND", "message": "此帳號尚未註冊" } }
{ "error": { "code": "WRONG_PASSWORD", "message": "密碼錯誤" } }
```
> ⚠️ 資安提醒（口試會問）：區分這兩者＝洩漏「哪些 email 有註冊」，屬帳號枚舉風險。標準高資安做法是不區分。畢業專題以 UX 為重可區分，但建議搭配「登入失敗次數限制 / rate limit」。
> **前端已預留**：你一回這兩個 code 就自動生效，還沒回時退回「一律提供去註冊」。

## 4. 🔴 OTP 後端未強制
實測：註冊後**不驗 OTP 直接 login 也成功**。請：register 建帳號為 `pending`（`email_verified=false`）→ `verifyOTP` 過才 active → **未驗證帳號 login 一律擋**（`403 EMAIL_NOT_VERIFIED`）。OTP 由後端發、有效期、嘗試次數上限。

## 5. 🟠 登入 session 易掉線
登入 cookie 是 `SameSite=None; Secure; HttpOnly` 且**無 `Max-Age`**（session cookie），跨站＋重整易被瀏覽器清掉 → 後台一直要重登。二選一：
- **A（推薦）**：login 回 body 多帶 `token`，端點同時接受 `Authorization: Bearer`（cookie 保留）。
- **B（快）**：cookie 加 `Max-Age`（如 7 天）。

## 6. 🟠 商品推薦 `style` 被忽略
同臉部/膚色資料，只換 `style` 回傳商品一模一樣。底妝/口紅顏色比對維持不變；眼影/眉/腮紅/修容/打亮要依 `style` 篩選。（此項要第 2 項端點恢復才驗得到。）

## 7. 🟠 商品收藏 favorites
`POST /api/favorites/toggle` 實測 **500**，且缺讀取端點。請：
| 方法 | 路徑 | 用途 |
|------|------|------|
| `POST` | `/api/members/{email}/favorites/toggle` | 加/移除商品收藏（body `{item_id, item_type}`）|
| `GET` | `/api/members/{email}/favorites` | 取得收藏清單 |

## 8. 🟠 購物車、分析歷史
| 方法 | 路徑 | 用途 |
|------|------|------|
| `GET` / `PUT` | `/api/members/{email}/cart` | 取得 / 整包覆蓋購物車 |
| `GET` / `POST` / `DELETE` | `/api/members/{email}/history` | 分析歷史（讀 / 新增 / 刪）|

---

# 二、資料表與欄位（Schema）

> 型別通用寫法；JSON 若不支援用 TEXT 存 JSON 字串。`email` 當關聯鍵；若你主鍵是 member_id，換成外鍵即可。

## `members`（補欄位）
| 欄位 | 型別 | 說明 | 狀態 |
|------|------|------|------|
| 既有 | | id/email/password/name/phone_number/age/role/level/status/allowed_pages/vip_requested/render_quota | ✅ |
| `email_verified` | BOOLEAN 預設 false | OTP 是否通過（第 4 項）| 🆕 |
| `points_balance` | INT 預設 0 | 點數餘額 | ✅ |
| `lifetime_points` | INT 預設 0 | 累計獲得點數，只增不減 | 🆕 |
| `referred_by_email` | VARCHAR | 註冊時綁定的推薦人 | 🆕 |
| `active_theme` | VARCHAR 預設 classic | 目前主題 | 🆕 |

## `points_transactions`（點數明細）🆕
`id` PK / `email` / `delta` INT / `balance_after` INT / `reason` VARCHAR / `created_at` TIMESTAMP

## `daily_checkins`（簽到）🆕
`id` PK / `email` / `checkin_date` DATE / `streak` INT / `awarded` INT / `created_at`
　→ UNIQUE(email, checkin_date)

## `task_claims`（任務領取）🆕
`id` PK / `email` / `task_id` VARCHAR / `claimed_at`　→ UNIQUE(email, task_id)

## `unlocked_themes`（已解鎖主題）🆕
`id` PK / `email` / `theme_id` VARCHAR / `unlocked_at`　→ UNIQUE(email, theme_id)

## `referrals`（推薦紀錄）🆕
`id` PK / `referrer_email` / `referred_email`(UNIQUE) / `points_awarded` INT / `created_at`

## `otp_codes`（OTP）🆕
`id` PK / `email` / `code` VARCHAR / `expires_at` / `attempts` INT / `created_at`

## `saved_looks`（收藏妝容）✅ 已有
`id` / `email` / `style` / `before_image_url` LONGTEXT / `after_image_url` VARCHAR / `analysis_summary` **JSON/TEXT**（前端會送 faceShape/eyeShape/skinSeason，未來可能擴充）/ `created_at`

## `analysis_history`（分析歷史）🆕
`id` / `email` / `mode`(basic|pro) / `result` JSON / `analysis_package_id` / `created_at`

## `favorites`（商品收藏）🆕
`id` / `email` / `item_id` BIGINT / `item_type` VARCHAR / `created_at`　→ UNIQUE(email, item_id)

## `cart`（購物車）🆕
`id` / `email` / `item_id` BIGINT / `qty` INT / `updated_at`　→ UNIQUE(email, item_id)

## `products`（商品）✅ 已有
id / name / brand / price / description / **image_src**（前端讀這個欄位名）/ type / hex_primary / lab / color_vector / sale_page_id / shade_name

---

# 順手清理：刪掉測試帳號

前端測試時建了兩筆測試帳號，麻煩從資料庫刪除（含它們的 saved_looks / points 等關聯資料）：
- `probe_otp_check_1783526684@example.com`
- `probe_conn_test_20260708@example.com`

---

# 三、麻煩回覆前端

1. 第 1 項點數的加點端點（check-in/tasks）做好告訴我 → 前端把簽到/任務改成呼叫你的 API，後台就看得到真實點數。
2. 第 2 項 `/recommend-products` 正確路徑？
3. 第 3 項要不要做「未註冊/密碼錯」區分（做了前端自動生效）？
4. 第 5 項 session 你選 A（token）還 B（Max-Age）？
