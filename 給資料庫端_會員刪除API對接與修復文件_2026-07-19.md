# 給資料庫端：會員刪除 API 對接與修復文件

版本：2026-07-19  
前端：Firebase Hosting（`https://decorate-me.web.app`）  
目的：修復管理員後台顯示「會員刪除失敗」的問題

## 1. 已確認的前端行為

管理員在會員管理頁按下「刪除會員」後，前端會送出：

```http
DELETE /api/members/{email}
Cookie: <member session cookie>
Authorization: Bearer <member access token>
Origin: https://decorate-me.web.app
```

實際路徑範例：

```http
DELETE /api/members/user%40example.com
```

前端已確認：

- 使用 `DELETE` 方法。
- email 已使用 URL encoding。
- 請求包含 `credentials: include`，會攜帶 session cookie。
- 登入 API 若有回傳 access token，會加上 `Authorization: Bearer ...`。
- 只有收到 HTTP 2xx，前端才會把會員從畫面移除。
- API 失敗時不會假刪除會員資料。
- CORS preflight 已實測成功，後端目前回應允許 `GET, POST, PATCH, DELETE, OPTIONS`。

因此目前問題應由會員後端的刪除路由、管理員授權或資料庫交易處理進一步檢查。

## 2. 必須提供的 API

### Request

```http
DELETE /api/members/{email}
Authorization: Bearer <admin token>
```

Request body 可以為空。若後端需要刪除原因，也可以接受：

```json
{
  "reason": "管理員依會員要求刪除帳號"
}
```

### 成功回應

建議回傳 HTTP 200：

```json
{
  "ok": true,
  "action": "member_deleted",
  "member": {
    "email": "user@example.com",
    "mode": "soft"
  },
  "deletedCounts": {
    "savedLooks": 3,
    "favorites": 5,
    "cartItems": 0,
    "history": 2,
    "pointTransactions": 4
  },
  "requestId": "req_xxx"
}
```

也可以回 HTTP 204；前端會把任何 2xx 視為成功。

## 3. 後端授權規則

後端不可只相信前端傳入的 email 或畫面上的管理員身分，必須：

1. 從 HttpOnly session cookie 或 Bearer token 找出目前登入者。
2. 從資料庫確認登入者的 `role` 為 `admin`。
3. 未登入回 `401 UNAUTHENTICATED`。
4. 非管理員回 `403 ADMIN_REQUIRED`。
5. 禁止管理員刪除自己，回 `403 CANNOT_DELETE_SELF`。
6. 建議禁止刪除最後一位有效管理員，回 `409 LAST_ADMIN_PROTECTED`。
7. 不可用 email 是否以 `admin@` 開頭判斷權限。

## 4. 資料庫交易處理

會員刪除必須在同一筆 transaction 中處理，避免只刪除一半：

```text
BEGIN
  鎖定並查詢目標會員
  驗證目標存在且允許刪除
  處理 saved_looks
  處理 favorites
  處理 cart items
  處理 analysis/history
  處理 points 與 point transactions
  處理 tasks、referrals、themes 等會員關聯資料
  軟刪除或刪除 members 主資料
  寫入 admin audit log
COMMIT
```

任一步驟失敗都必須 `ROLLBACK`，並回傳明確錯誤，不可回 2xx。

### 硬刪除

若使用硬刪除，請確認所有外鍵都有正確的 `ON DELETE CASCADE`，或先按正確順序刪除子表，再刪除 `members`。

### 軟刪除（建議）

可將會員更新為：

```text
status = 'deleted'
deleted_at = current timestamp
deleted_by = admin email
```

採用軟刪除時：

- `GET /api/members` 預設不可回傳已刪除會員。
- `POST /api/login` 必須拒絕已刪除會員。
- saved looks、收藏、點數等 API 必須拒絕已刪除會員繼續使用。
- email 是否允許重新註冊，需由資料庫端明確定義。

## 5. 標準錯誤格式

請統一使用：

```json
{
  "error": {
    "code": "ADMIN_REQUIRED",
    "message": "只有管理員可以刪除會員",
    "requestId": "req_xxx"
  }
}
```

前端亦可解析 FastAPI 常見的巢狀格式：

```json
{
  "detail": {
    "error": {
      "code": "ADMIN_REQUIRED",
      "message": "只有管理員可以刪除會員"
    }
  }
}
```

建議錯誤對照：

| HTTP | code | 說明 |
|---:|---|---|
| 401 | `UNAUTHENTICATED` | session/token 無效或過期 |
| 403 | `ADMIN_REQUIRED` | 登入者不是管理員 |
| 403 | `CANNOT_DELETE_SELF` | 管理員刪除自己 |
| 404 | `MEMBER_NOT_FOUND` | 找不到目標會員 |
| 409 | `LAST_ADMIN_PROTECTED` | 不可刪除最後一位管理員 |
| 409 | `MEMBER_DELETE_CONFLICT` | 外鍵或關聯資料阻擋刪除 |
| 422 | `INVALID_EMAIL` | email 格式錯誤 |
| 500 | `MEMBER_DELETE_FAILED` | transaction 或資料庫執行失敗 |

請勿只回傳：

```json
{ "message": "會員刪除失敗" }
```

這種訊息無法判斷是權限、外鍵、SQL 或 transaction 問題。

## 6. CORS 設定

目前 preflight 已成功，但正式設定仍需保持：

```http
Access-Control-Allow-Origin: https://decorate-me.web.app
Access-Control-Allow-Credentials: true
Access-Control-Allow-Methods: GET, POST, PATCH, DELETE, OPTIONS
Access-Control-Allow-Headers: Content-Type, Authorization, X-Requested-With
```

`OPTIONS` 不應要求使用者先通過登入驗證。

## 7. 稽核紀錄

會員刪除屬高風險操作，建議記錄：

- request ID
- 管理員 email
- 被刪除的會員 email
- 刪除時間
- 軟刪除或硬刪除
- 各關聯表處理筆數
- 成功或失敗
- 失敗錯誤碼，但不可記錄 token、密碼或完整 session

## 8. 資料庫端驗收步驟

請使用測試會員執行，禁止用正式會員做第一次測試。

1. 未登入呼叫 DELETE，應回 401。
2. 一般會員呼叫 DELETE，應回 403 `ADMIN_REQUIRED`。
3. 管理員刪除自己的帳號，應回 403 `CANNOT_DELETE_SELF`。
4. 管理員刪除不存在會員，應回 404 `MEMBER_NOT_FOUND`。
5. 管理員刪除測試會員，應回 2xx。
6. 再呼叫 `GET /api/members`，被刪除會員不可出現。
7. 被刪除會員重新登入必須失敗。
8. 檢查所有會員關聯表，不可留下無主資料。
9. 模擬其中一張關聯表刪除失敗，確認整筆 transaction rollback。
10. 確認 audit log 有記錄操作者、目標與結果。

## 9. 請資料庫端回覆的資訊

完成修復後，請回覆前端：

- 實際部署的 API base URL。
- 使用 session cookie、Bearer token，或兩者皆支援。
- 採用軟刪除或硬刪除。
- 成功回應是 HTTP 200 或 204。
- 完整錯誤碼清單。
- 關聯資料的處理方式。
- 測試環境與測試會員 email。
- 部署完成時間或版本號。

## 10. 前端對接位置

- API 呼叫：`firebase-hosting-full/public/js/api.js` 的 `Api.deleteMember(email)`。
- 後台按鈕流程：`firebase-hosting-full/public/js/router.js` 的會員管理刪除事件。
- 契約測試：`firebase-hosting-full/public/admin_api_contract_test.js`。

前端目前不需要更改 API 路徑。資料庫端完成並部署 `DELETE /api/members/{email}` 後，即可進行端到端驗收。
