*
!face
face/*
!gateway
gateway/*
!render
render/*
!shared
shared/*
!suggestion
suggestion/*
!face/Face_analyzer_BASIC.py
!face/Face_analyzer_PRO.py
!suggestion/Ollama_suggestion.py
!shared/dev_server_utils.py
!shared/api_errors.py
# 上傳圖片共用安全處理（Render／BASIC 都 import）與高風險 Admin 稽核（Gateway import）。
!shared/image_safety.py
!gateway/admin_audit.py
# BASIC Dockerfile 會 COPY 這三支；先前漏在 .dockerignore 外，只有 docker build 會踩到
# （正式部署走 .gcloudignore 不受影響）。
!face/mediapipe_ascii.py
!face/basic_rule_trees.py
!face/pro_nose_side_model.py
!face/rule_features.py
!face/eye_features.py
!tools
tools/*
!tools/download_face_models.py
!tools/face_models_manifest.json
!face/cloud_start.py
!face/Dockerfile
!face/requirements.txt
!face/analysis_package.py
!shared/job_store.py
!render/replicate_render.py
!render/replicate_render_api.py
!render/requirements.txt
!render/Dockerfile
!gateway/ai_gateway.py
!gateway/requirements.txt
!gateway/Dockerfile
!face/face_roi.py
!face/basic_roi_shadow.py
# 五官判斷回饋（BASIC 與 PRO 都 import）。新增會被 COPY 的檔案時，
# .dockerignore 與 .gcloudignore 兩份都要放行，少一邊就會在該邊的建置炸掉。
!face/face_feedback.py
# 使用者同意提供的訓練樣本（只存部位 ROI，不存原圖）。face_feedback 會 import。
!face/face_contributions.py
!face/face_corrections.py

# ROI CNN shadow 模型。只放推論要用的 .onnx 與類別表；
# .pt（torch state dict，訓練用）不進映像，那是 30MB 的死重量。
!models
models/*
!models/basic_features_roi
models/basic_features_roi/*
!models/basic_features_roi/*.onnx
!models/basic_features_roi/*_classes.json
# PRO 側臉鼻型模型：只放行推論用的 .onnx 與類別表（cv_results/label_conflicts 是訓練
# 產物，不進映像）。少了這幾行，models/* 會把整個 pro_nose_side 排除，Dockerfile 的
# COPY models/pro_nose_side/ 在 build 時就會失敗——側臉鼻型形同沒部署。
!models/pro_nose_side
models/pro_nose_side/*
!models/pro_nose_side/*.onnx
!models/pro_nose_side/*_classes.json
# 膚色取樣的臉部皮膚分割模型（selfie_multiclass，16MB）。少了這幾行，models/* 會把
# 整個 mediapipe 目錄排除，source 上傳不含它，Dockerfile 的 COPY models/mediapipe/
# 就會以 "file not found in build context" 失敗——而那個訊息看起來像檔案沒建好，
# 不像被忽略規則擋掉。
!models/mediapipe
models/mediapipe/*
!models/mediapipe/*.tflite
# DINOv2 線性分類器的權重（npz）。分類頭只存 coef／intercept，很小，留著不佔空間。
#
# ⚠️ 2026-07-31 起 backbone（dinov2_vits14.onnx，88MB）**不再放行**：
# 五個部位改用 ConvNeXt-Tiny 之後 DINOv2 完全退場（見 basic_roi_shadow 的說明），
# 那 88MB 進映像只是死重量。head 檔留著是為了保留「改環境變數就能開回 shadow」的
# 能力——但真要開回來，得先把 backbone 加回這個白名單，否則 _load_dinov2 會
# 找不到檔案而永久停用（它會記一行 log，不會拋例外）。
!models/basic_features_roi/*.npz
models/basic_features_roi/dinov2_vits14.onnx
# 幾何決策樹（joblib 是樹本體、json 是類別與特徵清單，兩份都要）。
#
# 先前兩份 ignore 都沒放行，於是映像裡根本沒有這兩個檔——basic_rule_trees._load()
# 找不到就記一行 warning、回空 dict，線上的臉型與眼型「一直都是 CNN 在答」，
# 跟規格書 §11.1 畫的架構圖不一致，而且沒有人發現。
#
# 目前 RULE_TREE_PARTS 是空的（規則式已全面退場，見規格書 §7.11），所以這兩個檔
# 不會被載入。放行是為了讓**回滾路徑真的可用**：把部位加回 RULE_TREE_PARTS 時
# 線上要真的吃得到樹，而不是安靜地繼續用 CNN。
#
# joblib 是 pickle，會綁 sklearn 版本。目前本機與映像都是 1.7.2；requirements.txt
# 沒有釘版本，哪天飄掉的話 _load() 會 catch 住並退回原本答案（不會拖垮分析）。
!models/basic_features_roi/*_rule_tree.json
!models/basic_features_roi/*_rule_tree.joblib
# PRO 側臉鼻型（ConvNeXt-Tiny，整張圖 224；不走 ROI 裁切，理由見 pro_nose_side_model）。
# 只放行 onnx 與類別檔——cv_results.json 與 label_conflicts.json 是研究紀錄，不進映像。
!models/pro_nose_side
models/pro_nose_side/*
!models/pro_nose_side/nose_shape_side.onnx
!models/pro_nose_side/nose_shape_side_classes.json
