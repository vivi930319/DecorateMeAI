*
!Face_analyzer_BASIC.py
!Face_analyzer_PRO.py
!Ollama_suggestion.py
!dev_server_utils.py
!api_errors.py
# 上傳圖片共用安全處理（Render／BASIC 都 import）與高風險 Admin 稽核（Gateway import）。
!image_safety.py
!admin_audit.py
# BASIC Dockerfile 會 COPY 這三支；先前漏在 .dockerignore 外，只有 docker build 會踩到
# （正式部署走 .gcloudignore 不受影響）。
!mediapipe_ascii.py
!basic_rule_trees.py
!rule_features.py
!cloud_start.py
!Dockerfile
!requirements.txt
!analysis_package.py
!job_store.py
!replicate_render.py
!replicate_render_api.py
!requirements.render.txt
!Dockerfile.render
!ai_gateway.py
!requirements.gateway.txt
!Dockerfile.gateway
!face_roi.py
!basic_roi_shadow.py
# 五官判斷回饋（BASIC 與 PRO 都 import）。新增會被 COPY 的檔案時，
# .dockerignore 與 .gcloudignore 兩份都要放行，少一邊就會在該邊的建置炸掉。
!face_feedback.py
!face_corrections.py

# ROI CNN shadow 模型。只放推論要用的 .onnx 與類別表；
# .pt（torch state dict，訓練用）不進映像，那是 30MB 的死重量。
!models
models/*
!models/basic_features_roi
models/basic_features_roi/*
!models/basic_features_roi/*.onnx
!models/basic_features_roi/*_classes.json
# DINOv2 線性分類器的權重（npz）。backbone 是 .onnx，分類頭只存 coef／intercept；
# 沒放行的話映像裡只有 backbone，DINOv2 shadow 會整組跳過。
!models/basic_features_roi/*.npz
# 幾何決策樹（joblib 是樹本體、json 是類別與特徵清單，兩份都要）。
#
# 先前兩份 ignore 都沒放行，於是映像裡根本沒有這兩個檔——basic_rule_trees._load()
# 找不到就記一行 warning、回空 dict，線上的臉型與眼型「一直都是 CNN 在答」，
# 跟規格書 §11.1 畫的架構圖不一致，而且沒有人發現。
#
# 目前 RULE_TREE_PARTS 是空的（規則式已全面退場，見規格書 §7.11），所以這兩個檔
# 不會被載入。放行是為了讓**回滾路徑真的可用**：把部位加回 RULE_TREE_PARTS 時
# 線上要真的吃得到樹，而不是安靜地繼續用 CNN。
#
# joblib 是 pickle，會綁 sklearn 版本。目前本機與映像都是 1.7.2；requirements.txt
# 沒有釘版本，哪天飄掉的話 _load() 會 catch 住並退回原本答案（不會拖垮分析）。
!models/basic_features_roi/*_rule_tree.json
!models/basic_features_roi/*_rule_tree.joblib
