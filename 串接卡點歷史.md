# Decorate Me 串接整合卡點歷史

用途：記錄前後端／各服務整合過程中，串接「卡住」的問題、根因與解法。多數不是資安問題，而是介面對接、環境、時序上的坑，供追蹤與口試說明「整合真實會遇到什麼」。
最後更新：2026-07-11
狀態：多數(已解決)，環境類為(持續存在)。

---

## 總覽

| # | 卡點 | 類型 | 狀態 |
|---|------|------|------|
| I1 | 後台一進去就崩潰，讀不到會員 | 前端程式缺陷 | 已解決 |
| I2 | 商品圖片全部不顯示 | 介面欄位名對不上 | 已解決 |
| I3 | 妝容收藏沒進資料庫 | 端點好了但前端沒接 | 已解決 |
| I4 | 收藏跳回來臉部分析／風格不見 | 欄位 key 中英文對不上 | 已解決 |
| I5 | 商品推薦一片空白 | 端點消失＋格式改變 | 已解決 |
| I6 | 註冊被擋 400 MISSING_FIELDS | 誤判＋前端送殘缺請求 | 已解決 |
| I7 | 後台重整就要重新登入 | 跨站 session cookie 掉線 | 已解決 |
| I8 | 後台寫入（改權限/商品）全失敗 | CORS 缺 Allow-Credentials | 已解決 |
| I9 | 資料庫網址一直換、要一直改設定 | Cloudflare quick tunnel | 持續存在 |
| I10 | 後台整片讀不到（連不上資料庫）| 組員服務沒開／502 | 持續存在（協調）|
| I11 | 後台點數顯示不出來 | 前端用並發抓取而非清單欄位 | 已解決 |

---

## I1 後台一進去就崩潰，讀不到會員(已解決)
- 現象：進管理中台，會員清單讀不到、顯示「資料庫連不上」，Console 報 `Cannot access 'render' before initialization`。
- 根因：`admin()` 裡 `render` 是用 `const` 宣告的函式，但 `loadAdminMembers()` 在 `render` 那行還沒執行到時就被呼叫（JavaScript 的暫時性死區 TDZ），整個後台初始化當場拋錯中斷，`fetchAdminMembers` 根本沒機會跑。
- 解法（前端）：把 `loadAdminMembers()` 的呼叫移到 `render` 定義之後。伺服器端其實一切正常（curl 帶 admin cookie 讀 members 是 200），純前端載入順序 bug。
- 教訓：巨檔裡函式呼叫順序容易踩 TDZ；「畫面壞了」不一定是後端問題，要先看 Console。

## I2 商品圖片全部不顯示(已解決)
- 現象：商品列表、推薦、後台商品都沒有圖。
- 根因：後端商品的圖片欄位叫 `image_src`，前端 `_normalizeProduct` 的 fallback 只找 `imageUrl / image_url / img`，沒有 `image_src`，取到空字串。
- 解法（前端）：把 `image_src` 加進 fallback 鏈（`imageUrl || image_url || image_src || img || image`）。實測圖片網址本身 200 可讀，純欄位名對不上。
- 教訓：介面欄位命名要先對齊（駝峰 vs 底線、url vs src）。

## I3 妝容收藏沒進資料庫(已解決)
- 現象：使用者收藏了妝容，後台／跨裝置都看不到。
- 根因：後端 `saved_looks` 端點（GET/POST/DELETE）其實已經好了、可持久化，但**前端整個沒有呼叫它**，收藏只寫在本機 localStorage。
- 解法（前端）：新增 `listSavedLooks / createSavedLook / deleteSavedLook` 三個 API 方法；收藏時 POST 上去、進會員中心時背景 GET 拉回、刪除時連動 DELETE，localStorage 當離線快取。
- 教訓：「後端端點好了」不等於「功能通了」，前端要實際接上去才算。

## I4 收藏跳回來臉部分析／風格不見(已解決)
- 現象：渲染成功第一次收藏沒存到圖；跳回去重收藏時圖有了，但原本的臉部分析 JSON 跟風格細節不見了。
- 根因（兩層）：
  1. 第一次收藏用的是渲染前就建好的快照（那時渲染圖網址還空的）。
  2. 送後端的 `analysisSummary` 讀英文 key（`faceShape` 等），但前端 `analysisResult` 其實是**中文 key**（`臉型`／`膚色.四季型`），讀不到就送成 null；之後背景從後端拉回（資訊較少的摘要）覆蓋掉本機完整資料。
- 解法（前端）：收藏當下重新建記錄（拿最新渲染圖）；`analysisSummary` 改成兼容中英文 key（`a.faceShape || a['臉型']`）；背景拉回時，本機已同步的完整版優先保留、不被後端摘要覆蓋。
- 教訓：同一份資料在不同地方用不同 key（中文顯示 vs 英文送 API）是隱形地雷；「後端為主覆蓋本機」的策略，前提是後端存的資料不能比本機少。

## I5 商品推薦一片空白(已解決)
- 現象：做完臉部分析進商品頁，推薦是空的。
- 根因（先後兩個）：
  1. 一開始 `/recommend-products` 直接回 `404`——推薦服務不在當時的商品網址上。
  2. 後來端點恢復了，但**輸入／輸出格式都改了**：後端要「頂層」的色彩資料（`lab`／`hex`／`hsv`／`vector`），前端卻送「巢狀」的 `skinTone.lab`＋一堆臉型欄位；回應 key 也從 `products` 變成 `recommendations`。格式對不上就回 400 或前端讀不到。
- 解法（前端）：把膚色 `lab` 提到請求頂層送、讀取改吃 `recommendations`（相容舊 `products`）。實測後端用 `lab` 能回 12 筆含 `delta_e` 的商品。
- 教訓：端點「能打通」不代表「格式對」；對接要確認 request/response 的實際欄位。

## I6 註冊被擋 400 MISSING_FIELDS(已解決)
- 現象：JSON 打 `/api/register` 一直回 `400 MISSING_FIELDS`，一度以為是 CSRF 擋。
- 根因：其實是**欄位不齊**。register 需要 `phone_number/name/email/password/age` 五欄；而前端 `sendOTP()` 的 fallback 會把只帶 `{email}` 的請求也丟去 `/api/register`，觸發缺欄位錯誤，被誤判成 CSRF。
- 解法（前端）：把 `sendOtpPaths` 裡的 `/api/register` fallback 拿掉，只打正規發碼端點。補齊五欄後 register 回 200。
- 教訓：錯誤訊息會誤導（以為 CSRF，其實欄位不齊）；要用實測（補齊欄位再打一次）確認真因。

## I7 後台重整就要重新登入(已解決)
- 現象：登入後進後台正常，一重整就變「請先登入」，商品管理也一起壞。
- 根因：登入靠的是跨站 cookie（前端 web.app、資料庫 trycloudflare 屬不同網站），而 cookie 沒有效期又是第三方，瀏覽器重整後就不送了；但前端的「已登入」狀態存在 sessionStorage 還在，於是畫面在、API 卻 401。
- 解法：後端給 cookie 加 `Expires`（7 天）根本解決；前端加「401 自動用暫存帳密重登再重試」當過渡。
- 教訓：跨站 cookie 很脆弱；「畫面顯示已登入」和「API 認得你」是兩回事，要一致。

## I8 後台寫入全失敗(已解決)
- 現象：後台改權限／新增商品等寫入操作在瀏覽器一律失敗，即使後端有正確處理。
- 根因：後端 CORS 回了精確 `Allow-Origin` 但少了 `Access-Control-Allow-Credentials: true`，瀏覽器把「帶 cookie 的回應」整個丟掉。
- 解法（後端）：補 `Access-Control-Allow-Credentials: true`＋ cookie 設 `SameSite=None; Secure`。
- 教訓：跨站又要帶 cookie，CORS 的 credentials 設定缺一不可。

## I9 資料庫網址一直換、要一直改設定(持續存在)
- 現象：組員的會員資料庫用 Cloudflare quick tunnel，每次她重開服務，網址就換一個（`quickly-tend-sun-rod` → `heard-complete-offices-terrain` → `component-developmental-disclaimer-count`），前端就連不上。
- 根因：quick tunnel 每次啟動給的是隨機臨時網址。
- 解法（前端流程）：改 `config.local.js` 的 `memberDatabaseUrl`／`productUrl` → 跑 `deploy.ps1` 重新部署。已把這步做成固定流程。
- 根治建議：請組員改用固定網址（Cloudflare named tunnel，或直接部署到雲端 Cloud Run/Railway），就不用每次改設定。
- 教訓：依賴會變的網址是整合痛點；發表前一定要確認當下網址、重新部署、實測登入。

## I10 後台整片讀不到（連不上資料庫）(持續存在／協調)
- 現象：重整後台顯示連不上資料庫。
- 根因：組員（資料庫負責人）還在開發，服務時開時關；關著時 tunnel 回 502（tunnel 在、後面服務沒跑）或整個連不上。
- 解法：這不是程式 bug，是協調問題——要用時請組員把服務開著。前端該接的都接好了、失敗也有提示，服務一起來就通。
- 教訓：跨人服務的可用性要靠協調；發表當天務必先確認組員服務開著。

## I11 後台點數顯示不出來(已解決)
- 現象：後台會員清單的點數欄顯示不出來（—），但資料庫其實有點數（如 65）。
- 根因：前端後台用「逐會員各打一支 `GET points`」的方式抓，9 個會員並發到 tunnel 時部分逾時就顯示不出；而其實後端的 members 清單**已經直接帶 `points` 欄位**，不必再逐個抓。
- 解法（前端）：後台點數改成直接讀清單自帶的 `member.points`，抓不到才退回逐會員抓。
- 教訓：後端提供了更省事的資料形式（清單直接帶欄位）就該用它，少一堆並發請求也更穩。

---

## 歸納：這些卡點的共同教訓
1. **介面契約要先對齊**（欄位名、巢狀結構、回應 key、中英文）——I2/I4/I5/I11 都是這類。
2. **端點通 ≠ 功能通**——後端好了前端要實際接、還要格式對（I3/I5）。
3. **跨站認證脆弱**——cookie 效期、CORS credentials、自動重登都要顧（I7/I8）。
4. **依賴會變的環境要有流程**——tunnel 換網址／服務沒開，要有固定的更新與確認步驟（I9/I10）。
5. **錯誤訊息會騙人**——用實測找真因，別被表面錯誤帶偏（I6）。
