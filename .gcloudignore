# gcloud builds submit 上傳建置來源時用這個檔決定要傳什麼。
#
# 這個檔必須存在：沒有它，gcloud 會 fallback 去套用 .gitignore，而 .gitignore 是「* 全擋」的
# 白名單、且沒放行 models/，於是模型檔不會被上傳，Docker build 會在 COPY models/ 直接失敗。
#
# 內容要跟 Dockerfile 的 COPY 清單對得起來。

*

!.dockerignore
!Dockerfile
!Dockerfile.render
!Dockerfile.gateway
!requirements.txt
!requirements.render.txt
!requirements.gateway.txt
!Face_analyzer_BASIC.py
!Face_analyzer_PRO.py
!Ollama_suggestion.py
!replicate_render.py
!replicate_render_api.py
!ai_gateway.py
!dev_server_utils.py
!cloud_start.py
!job_store.py
!api_errors.py
# Dockerfile.gateway 會 COPY 這支。漏放行的話 build 會停在
# 「COPY failed: file not found in build context」，而檔案其實好端端躺在本機——
# 是 `*` 全擋把它濾掉了，不是檔案不見（2026-07-26 部署時踩到）。
!admin_audit.py
!gcs-lifecycle.json
!analysis_package.py
!face_roi.py
!basic_roi_shadow.py
# 五官判斷回饋（BASIC 與 PRO 都 import）。同上：漏放行的話 build 會停在
# 「COPY failed: file not found in build context」。
!face_feedback.py
# 使用者同意提供的訓練樣本（只存部位 ROI，不存原圖）。face_feedback 會 import。
!face_contributions.py
!face_corrections.py

# 以下四支 Dockerfile 一直都有 COPY，但從來沒放行過——臉部映像因此建不起來
# （2026-07-28 部署時停在 image_safety.py）。gateway 與 render 沒踩到，是因為它們
# 的 Dockerfile 不需要這幾支。
# 對照方式：把三個 Dockerfile 的 COPY 清單跟這份白名單比一次，差集就是會炸的檔案。
!image_safety.py
!mediapipe_ascii.py
!basic_rule_trees.py
!pro_nose_side_model.py
!rule_features.py
!tools
tools/*
!tools/download_face_models.py
!tools/face_models_manifest.json

# ROI CNN shadow 模型：只傳推論用的 .onnx 與類別表，.pt 是訓練用的不進映像
!models
models/*
!models/basic_features_roi
models/basic_features_roi/*
!models/basic_features_roi/*.onnx
!models/basic_features_roi/*_classes.json
# DINOv2 線性分類器的權重（npz）。分類頭只存 coef／intercept，很小。
# ⚠️ 2026-07-31 起 backbone（88MB）不再上傳：DINOv2 已退場，那是死重量。
# 要開回 shadow 對照就把下面那行刪掉（.dockerignore 也要一起改）。
!models/basic_features_roi/*.npz
models/basic_features_roi/dinov2_vits14.onnx
# 幾何決策樹（joblib 是樹本體、json 是類別與特徵清單，兩份都要）。
# 先前兩份 ignore 都沒放行，映像裡沒有這兩個檔，線上的臉型與眼型一直都是 CNN 在答。
# 目前 RULE_TREE_PARTS 是空的，放行是為了讓回滾路徑真的可用（見 .dockerignore 的說明）。
!models/basic_features_roi/*_rule_tree.json
!models/basic_features_roi/*_rule_tree.joblib

# PRO 側臉鼻型（ConvNeXt-Tiny，整張圖 224）。只傳 onnx 與類別檔；
# cv_results.json 與 label_conflicts.json 是研究紀錄，不進映像。
!models/pro_nose_side
models/pro_nose_side/*
!models/pro_nose_side/nose_shape_side.onnx
!models/pro_nose_side/nose_shape_side_classes.json
