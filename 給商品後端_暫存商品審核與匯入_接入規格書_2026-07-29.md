# 給商品後端 · 暫存商品審核與匯入 接入規格書

**日期：** 2026-07-29
**提出方：** DECORATE ME 前端 + AI Gateway
**對象：** 商品資料庫後端維運者
**背景：** 爬蟲端已於 2026-07-28 改為只寫 `public.crawler_staging_products`，不再提供
`/api/crawler/*` HTTP API，也不再與前端直連。
**狀態：** 我方（前端 + Gateway）已實作完成並上線，等待商品後端提供 API。

---

## 0. 先講三件已經定好的事

你的五個問題裡有三個**不需要討論**，因為現有基礎設施已經決定了。照著用就好，不要另外發明。

### 0.1 管理員認證方式 —— 已定案

**你不需要處理會員登入、session 或角色判斷。** 那些在 Gateway 就擋掉了。

```
瀏覽器 ──HttpOnly __session cookie + dm_csrf/X-CSRF-Token + X-Expected-Actor──► AI Gateway
                                                                                   │
                         Gateway 驗證：CSRF、session 有效、role == admin            │
                                                                                   ▼
                                                          Authorization: Bearer {PRODUCT_ADMIN_API_KEY}
                                                          X-Admin-Actor: <雜湊後的操作者，不是 email>
                                                          X-Request-ID: <追蹤用>
                                                                                   │
                                                                                   ▼
                                                                            你的商品後端
```

**你這一端只要檢查 `Authorization: Bearer` 是不是那把 `PRODUCT_ADMIN_API_KEY`** ——
跟你現在的 `/api/products` 完全一樣，不必改。

`X-Admin-Actor` 是**去識別化的操作者代號**（雜湊，不可逆），要寫稽核就用它，不要期待拿到 email。

### 0.2 正式商品 API Base URL —— 已定案

就是你現在這個 `PRODUCT_DATABASE_URL`，跟 `/api/products` 同一個 host。
**不要另外開一個網域或連接埠**，Gateway 只認這一個環境變數。

### 0.3 正式商品 GET／POST／PATCH／DELETE —— 已經在跑了

你列的「正式商品 GET／POST／PATCH／DELETE API」**已經存在而且前端正在用**：

| Gateway 對外 | 轉到你這邊 | 方法 |
| --- | --- | --- |
| `/admin-api/products` | `/api/products` | GET, POST |
| `/admin-api/products/{id}` | `/api/products/{id}` | GET, PATCH, DELETE |
| `/admin-api/product-audit-logs` | `/api/admin/product-audit-logs` | GET |
| `/product-api/api/products` | `/api/products` | GET（公開，不需管理員） |

這一段不用動。

---

## 1. 真正需要你決定的：暫存商品的四支 API

前端要蓋一個「待審核清單 → 檢視 → 核准／退回 → 匯入」的畫面。需要這四支：

### 1.1 列表

```
GET /api/crawler-staging/products?status=pending&page=1&pageSize=20
```

回應：

```jsonc
{
  "items": [
    {
      "id": 123,                          // 暫存表主鍵，後續操作都用它
      "status": "pending",                // pending | approved | rejected | imported | failed
      "sourceSite": "MAC",
      "sourceProductId": "MAC-FOUNDATION-001",
      "sourceUrl": "https://www.maccosmetics.com.tw/product/...",  // 原始商品頁，審核者要點回去核對

      "category": "foundation",
      "name": "...",
      "brand": "...",
      "price": 1200,
      "currency": "TWD",
      "description": "...",
      "imageUrls": ["https://.../original.jpg"],
      "imageVariants": { "320": "...", "640": "...", "1280": "..." },  // 有就給，沒有可省略
      "hex": "#C08A6E",                   // 選填
      "failedReason": null,               // status=failed 時必填，見 §2.3
      "reviewReason": null,               // status=rejected 時填 §1.3 送來的 reason
      "importedProductId": null,          // status=imported 時填正式 products 的 id
      "crawledAt": "2026-07-28T10:00:00Z",
      "reviewedAt": null,
      "reviewedBy": null                  // X-Admin-Actor 的值
    }
  ],
  "total": 137,
  "page": 1,
  "pageSize": 20
}
```

- `status` 可省略 → 回全部。**不需要支援逗號多選** —— 前端的篩選列一次只送一個值。
- **請一定要有分頁。** 爬蟲會持續寫入，沒有分頁前端第一次載入就會把整張表拉下來。

### 1.2 單筆詳細 —— **不需要做**

列表已經帶齊所有欄位，前端不呼叫單筆詳細，對應的方法也已經移除。

Gateway 仍然放行 `GET /admin-api/crawler-staging/products/{id}`（跟 PATCH 共用一條路由），
你們沒做就是 404，不影響任何功能。**不要為了它花時間。**

### 1.3 審核

```
PATCH /api/crawler-staging/products/{id}
Content-Type: application/json

{ "status": "approved" }        // 或 "rejected"
{ "status": "rejected", "reason": "圖片抓錯商品" }   // rejected 建議帶原因
```

- 只允許 `pending → approved` 與 `pending → rejected`。其餘轉換回 **409**。
- 成功回 200 加更新後的物件（含 `reviewedAt`、`reviewedBy`）。
- **`reason` 要存下來，並在之後的列表回應裡以 `reviewReason` 回傳。** 前端會把它顯示在
  已退回的卡片上——下一個看到這筆的人才知道為什麼被退，否則「已退回」是個沒有理由的結論。

### 1.4 匯入正式商品

```
POST /api/crawler-staging/products/{id}/import
```

- 只允許 `approved → imported`，其餘回 **409**。
- **狀態更新與寫入正式 products 必須在同一個交易裡。** 匯入成功卻沒改狀態，
  下一次審核的人會重複匯入一次。
- 成功回 200：

```jsonc
{ "id": 123, "status": "imported", "importedProductId": 4567 }
```

- 失敗時把狀態改成 `failed` 並寫 `failedReason`，不要留在 `approved`——
  停在 `approved` 的話畫面上看起來像「還沒匯入」，實際上是「試過而且失敗了」。

**批次匯入需要嗎？** 前端可以逐筆呼叫。若你們預期一次核准上百筆，請提供
`POST /api/crawler-staging/products/import` 帶 `{"ids":[...]}`，並告訴我們部分失敗時怎麼回。

---

## 2. 欄位格式的約定

### 2.1 `status` 用小寫英文，不要中文

`pending` / `approved` / `rejected` / `imported` / `failed`。前端自己做中文對照與配色，
你這邊回中文的話，之後要改字面就得動資料庫。

### 2.2 金額與時間

- `price`：數字，不要帶貨幣符號或千分位。幣別放 `currency`（ISO 4217，例如 `TWD`）。
- 所有時間欄位用 **ISO 8601 UTC**（`2026-07-28T10:00:00Z`）。前端負責轉本地時區。

### 2.3 `failed` 一定要有原因

`failedReason` 是給人看的字串，會直接顯示在畫面上。「匯入失敗」這種沒有資訊的字不要寫，
寫「正式商品表已有相同 sourceProductId」這種看得出下一步該做什麼的。

### 2.4 圖片

- `imageUrls[0]` 當主圖。
- 全部必須是 **HTTPS**，前端頁面是 HTTPS，混合內容會被瀏覽器擋掉。
- `imageVariants` 有就給，前端會優先用 640 當清單縮圖，省流量。

---

## 3. 錯誤格式

沿用現有商品 API 的格式，前端已經能解析：

```jsonc
{ "error": { "code": "STAGING_NOT_FOUND", "message": "找不到這筆暫存商品" } }
```

建議的代碼與前端會顯示的訊息：

| code | HTTP | 前端顯示 |
| --- | --- | --- |
| `STAGING_NOT_FOUND` | 404 | 找不到這筆暫存商品，可能已被匯入或刪除。 |
| `INVALID_STATUS_TRANSITION` | 409 | 這筆的狀態已經變了，請重新整理後再試。 |
| `ALREADY_IMPORTED` | 409 | 這筆已經匯入過了。 |
| `IMPORT_FAILED` | 500 | 匯入失敗：{message} |

---

## 4. 我方已經全部做完，在等你們的 API

**前端與 Gateway 已經實作完成並上線。** 你們的四支 API 一上線，這一頁就會開始運作，
我方不需要再部署。

| 我方項目 | 狀態 |
| --- | --- |
| Gateway 四條代理 `/admin-api/crawler-staging/*` | ✅ 已上線（`ai-gateway-00057-8rq`） |
| 前端四支 API 方法（`js/api.js`） | ✅ 已上線 |
| 管理中台「暫存商品審核」頁 | ✅ 已上線（`20260729-021148`） |

現在的行為：管理員打開那一頁 → 呼叫 `GET /admin-api/crawler-staging/products?status=pending`
→ 你們還沒做，所以拿到 404 → 畫面顯示「商品後端的暫存商品 API 若尚未上線，這裡會是 404」。

### 4.1 路徑不一樣沒關係，改一行

上游路徑集中在 `ai_gateway.py` 的一個常數：

```python
_STAGING_BASE = "/api/crawler-staging/products"
```

**你們用什麼路徑就告訴我們，改這一行即可** —— 四條 Gateway 路由、前端四支方法、
那一頁的程式都不必動。所以 §1 的路徑只是提案，不要為了配合它去改你們的設計。

### 4.2 欄位名不一樣的成本比較高

路徑好改，但如果回傳的欄位名不同（例如 `source_site` 而不是 `sourceSite`、
`image_urls` 而不是 `imageUrls`），前端的卡片渲染要跟著改。

**所以 §1 的 JSON 形狀是我方目前實作的依據。** 你們如果要用別的命名，
請在回覆時一併列出實際欄位名，我方一次改完，不要等上線才發現對不上。

---

## 5. 已經做完的部分（我方）

**爬蟲舊路徑的清理：**

- 前端移除 `POST /api/crawler/product-preview` 與 `POST /api/crawler/search-preview` 的呼叫。
- 前端移除 `crawlerUrl` 設定與 `services.crawler`。`/public-config` 仍會回 `crawlerUrl` 欄位，
  前端忽略它——**你們不需要為此改 `/public-config`。**
- Gateway 移除 `/admin-api/crawler/product-preview`、`/admin-api/crawler/search-preview`
  兩條代理（它們原本會把請求轉去已經不存在的上游）。

**新流程的實作：**

- Gateway 新增四條 `/admin-api/crawler-staging/*` 代理，走既有的 `proxy_admin_request`
  （自動帶 CSRF、admin 驗證與去識別化稽核，與商品管理同一套）。
- 前端新增 `listStagingProducts` / `getStagingProduct` / `reviewStagingProduct` /
  `importStagingProduct` 四支方法。
- 管理中台「商品網址匯入」改名為「暫存商品審核」，內容換成：狀態篩選、分頁、
  核准、退回（可填原因）、匯入正式商品。按鈕依狀態出現——`pending` 才有核准／退回，
  `approved` 才有匯入。

**尚未驗證：** 因為沒有可回應的後端，那個介面**沒有跑過任何一筆真實資料**。
你們端點上線後的第一次載入，可能會撞到欄位名對不上——見 §4.2。

---

## 6. 需要你們回覆的事

1. 四支暫存商品 API 的**實際路徑**（不必配合 §1 的提案，我方改一行就好）
2. **實際回傳的欄位名**——若與 §1 的 JSON 不同請列出，這一項改起來比路徑麻煩（§4.2）
3. §1.2 的單筆詳細**要不要做**（列表已帶齊全部欄位的話可以不做，直接說不做即可）
4. §1.4 的**批次匯入要不要做**，若要，部分失敗如何回應
5. `imageVariants` 是否提供，key 是不是 `320`/`640`/`1280`
6. 預計完成時間

**不需要回覆的：** 管理員認證方式、正式商品 Base URL、正式商品 CRUD ——
見 §0，那三項已經定案且正在運作，照現有的做就好。

---

## 附錄 · 我方版本

| 項目 | 版本 |
| --- | --- |
| ai-gateway | `ai-gateway-00057-8rq` |
| 前端 | `20260729-021148` |
| Gateway commit | `ba9d77a` |
| 前端 commit | `7760478` |
