# 給資料庫端：資安修復與驗收規格

> 不包含任何實際金鑰、Email、照片、權杖或完整資料包。

## 2026-07-23 Gateway 側現況（供對接）

我方 Gateway 與前端已上線本輪資安修復（gateway revision `ai-gateway-00039-nzc`、前端版本 `20260723-212540`）：跨分頁 actor 隔離（會員／收藏／點數／打卡／任務／主題／Admin 寫入一律帶 `X-Expected-Actor`，不符或缺少即在到達資料庫前回 `409`）、`api/members` 名冊只給 Admin、私有媒體改以 job＋owner 授權、access log 已遮罩會員 email。以下為貴端仍需實作者，可即刻對接。

## 必修項目

- 登入成功後建立可撤銷 Session；Cookie 使用 `HttpOnly + Secure`，跨站部署需求由 Gateway 統一處理，資料庫端不直接暴露給瀏覽器。
- 每次請求以 Session 主體決定操作者，不相信 path/body 傳入的 Email、role 或 owner。
- Admin 清單、停權、改權限、刪除會員必須後端再次驗證 Admin；一般會員不得列出所有會員。
- 新增 `sessionVersion` 或等價欄位：登出、重設密碼、停權、降權、刪除帳號時立即撤銷所有舊 Session。
- 對外接口逐步由 `/members/{email}` 改為 `/me` 或 opaque member ID，避免 Email 進入 access log。
- 會員硬刪除使用可追蹤狀態：`pending → deleting → completed/failed`；刪除媒體、收藏、分析結果、工作紀錄任一步失敗都需可重試。
- 資料庫交易必須確保收藏、點數、打卡、任務、主題兌換具備 idempotency；不得重複扣點或重複發獎。
- 密碼使用 Argon2id 或合適的 bcrypt 成本，OTP 僅保存雜湊且有短效 TTL、嘗試上限及用途綁定。
- **（S7，高）Email 真實性必須驗證，OTP 不能形同虛設。** 目前可用不存在或假的 email 完成註冊並登入。`send-otp` 必須真的以 SMTP 寄碼到該信箱（收得到＝可送達的真帳號）；`verify-otp` 嚴格比對 DB 存的碼＋時效＋嘗試次數，通過才 `email_verified=true`；未驗證帳號不得 `login` 且定期清除。並拒收拋棄式／一次性信箱網域、對註冊 email 網域查 MX 記錄擋掉不存在的地址。
- **（S55，中）登入端點不得可被枚舉帳號。** 「帳號未註冊」與「密碼錯誤」必須回相同錯誤（統一 `INVALID_CREDENTIALS`），不得用不同碼或訊息透露某 email 是否已註冊；Gateway 代理已採統一訊息，資料庫端本身也必須一致。
- 所有 SQL 使用參數化查詢；排序、欄位名與動態條件採白名單。
- CORS 僅允許 Gateway／正式來源；資料庫服務本身不提供 `*`。
- Log 只記 request ID、固定錯誤碼、opaque actor、操作與狀態，不記完整 Email、密碼、Cookie、Token 或 body。

## 必交測試

- 一般會員列會員清單、改角色、改他人資料均為 403。
- Session 被登出／停權／降權／刪除後，舊 Session 立即為 401/403。
- 跨帳號收藏、點數、妝容紀錄均無法讀寫。
- 同一 idempotency key 重送不會重複扣點、發點或新增紀錄。
- 刪除流程故障後會保留 failed job 並可重試，不會回報假成功。
- 不存在的 email 與拋棄式網域無法完成註冊或登入；未驗證帳號無法 `login`（S7）。
- 登入回應無法區分帳號是否已註冊——不存在帳號與密碼錯誤回應完全一致（S55）。
- SQL injection、暴力登入、OTP 重放及大量請求測試。

## 回報格式

回覆修改檔案、migration、API contract、測試結果與部署／回滾步驟；不得附任何正式憑證。
