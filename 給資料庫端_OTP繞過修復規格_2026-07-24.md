# 給資料庫端：OTP 驗證可被繞過 — 五期修復規格

> 對象：會員資料庫端
> 嚴重度：**高**（可用假信箱建立可登入帳號，且可完全繞過 Gateway 的所有防線）
> 對應追蹤編號：**S7**（見《歷史流程更改追蹤》，狀態自 2026-07-09 起為「待處理」）
> 撰寫日期：2026-07-24
> 相關文件：`MEMBER_DATABASE_SECURITY_ISSUE.md`、`MEMBER_DATABASE_INTEGRATION_SPEC.md`、
> `系統資安效率流程改善總表.md`

---

## 零、一句話問題

**OTP 目前形同虛設。** 註冊之後不驗證任何驗證碼，直接呼叫 `POST /api/login` 就會回
`200` 並拿到可用帳號——2026-07-09 實測如此，2026-07-24 複查程式碼路徑後確認防線
仍未建立。

前端在 2026-07-11 已經改成「以後端為準」，但那只是移除了前端自己放行的捷徑；
**真正的閘門必須在資料庫端**，前端補不了。

---

## 一、三層破口

### 破口一（根因）：`login` 不檢查帳號是否通過 OTP

`register` 直接把帳號建成可登入狀態，`verify-otp` 是否成功並不影響 `login` 的結果。
於是完整的攻擊只有兩個請求：

```
POST /api/register  { email: "whatever@example.com", password: "...", ... }   → 201
POST /api/login     { email: "whatever@example.com", password: "..." }        → 200 + session
```

中間那一步 OTP，跳過完全沒有代價。

### 破口二：email 的真實性從未被驗證

只要格式合法就收單。不存在的信箱、拋棄式信箱（mailinator、example.com…）都能註冊。
若 `send-otp` 沒有真的寄出、或 `verify-otp` 沒有嚴格比對「資料庫存的碼 + 時效 + 次數」，
那麼即使補上破口一，OTP 仍然只是一道形式。

### 破口三（本次新發現）：資料庫端點不需認證即可直接呼叫

Gateway 呼叫 `/api/register`、`/api/send-otp`、`/api/verify-otp`、`/api/login` 時
**沒有帶任何認證標頭**（`ai_gateway.py` 的 `proxy_public_member_request()` 與
`login()` 都只送 `Content-Type`／`Accept`）。

而會員資料庫目前掛在公開的 `*.trycloudflare.com` 網址上。兩者相加的後果是：
**任何知道該網址的人都能直接打資料庫，完全繞過 Gateway。** 被繞過的包括：

| Gateway 的防線 | 直接打資料庫時 |
| --- | --- |
| 登入限流（Firestore 交易式固定視窗＋帳號維度） | 無效 |
| 請求大小上限（256KB） | 無效 |
| HttpOnly／Secure 的封裝 session cookie | 無效 |
| 跨分頁帳號隔離（`X-Expected-Actor`） | 無效 |
| Access log 去識別化 | 無效 |

也就是說，就算破口一、二都補好了，只要資料庫端點仍可匿名直呼，暴力破解與大量
註冊仍然不受任何限制。

---

## 二、我方（Gateway／前端）同時要做的事

這一節不是要求貴端做的，是說明我們會配合改什麼，以免兩邊改法互相打架。

### 2.1 Gateway 目前會把 `403` 壓成 `401`（**會影響你們的回應設計**）

`ai_gateway.py` 的登入代理現在是這樣：

```python
if response.status_code in {401, 403, 404}:
    raise HTTPException(status_code=401,
        detail={"error": {"code": "INVALID_CREDENTIALS", ...}})
```

**所以貴端即使實作了 `403 EMAIL_NOT_VERIFIED`，使用者也只會看到「帳號或密碼錯誤」**，
不會被引導去收驗證信。我們會改成把「未驗證」這個狀態原樣透傳。

→ **請採用下方第三期指定的錯誤碼**，我們會依該碼做對應。

### 2.2 前端的開發用繞過旗標

前端有一個 `allowInsecureOtpBypass` 旗標，開啟時「驗證碼長度 ≥ 4 就放行」。
正式設定檔預設為 `false`，且有 smoke test 盯著。

但必須說清楚：**這東西不構成任何防護**，因為它是瀏覽器端的值，
在 devtools 打一行就能改。它只是開發便利，不是安全機制——真正的閘門仍然只有貴端。
我們會把它移除或改成只在本機主機名生效。

---

## 三、五期修復規格

分期的用意是**每一期結束都能單獨上線且系統仍可用**，不必等全部做完才驗收。

### 第一期：帳號狀態機（阻止最直接的繞過）

| 項目 | 要求 |
| --- | --- |
| 新增欄位 | `email_verified`（bool，預設 `false`）、`email_verified_at`（timestamp，可為 null） |
| `POST /api/register` | 帳號建立時 `email_verified = false`。**回應不得包含可用的 session／token** |
| `POST /api/verify-otp` | 驗證成功才把 `email_verified` 設為 `true` 並寫入 `email_verified_at` |
| `POST /api/login` | `email_verified = false` 時一律拒絕，**不論密碼是否正確** |

拒絕時的回應（Gateway 會依 `code` 做對應，請勿更動字串）：

```json
HTTP 403
{ "error": { "code": "EMAIL_NOT_VERIFIED",
             "message": "此帳號尚未完成信箱驗證。",
             "retryable": false } }
```

**既有帳號的處理**：請不要一次把所有舊帳號設為 `false`，那會把現有使用者全部鎖在外面。
建議以「本期上線時間」為界，之前建立的帳號視為已驗證（`email_verified = true`），
之後的一律走新流程。此決定請回覆確認，我們好同步前端文案。

### 第二期：OTP 本身要能擋

| 項目 | 要求 |
| --- | --- |
| 產生 | 每次 `send-otp` 產生新碼，**用密碼學安全的亂數**（不是 `random()`／時間戳） |
| 儲存 | **只存雜湊**（如 HMAC-SHA256），不存明碼。與 Job token 的做法一致 |
| 有效期 | 10 分鐘，逾期即失效 |
| 嘗試次數 | 同一組碼最多 5 次，超過即作廢並要求重新發送 |
| 比對 | **固定時間比較**（`hmac.compare_digest`），不可用 `==`——否則可用回應時間逐字元試出驗證碼 |
| 一次性 | 驗證成功後立即作廢，不可重複使用 |
| 發送頻率 | 同一 email 60 秒內只能發一次，單日上限請自訂並回 `429` |

> 固定時間比較這一項我們這端剛踩過：`hmac.compare_digest` 對**含非 ASCII 字元的
> str** 會丟 `TypeError`，讓本來乾淨的 401 變成 500。請比對 **bytes**。

### 第三期：錯誤碼與列舉防護

錯誤碼契約（Gateway 依此對應，前端依此顯示）：

| 情境 | HTTP | code |
| --- | --- | --- |
| 帳號未驗證 | 403 | `EMAIL_NOT_VERIFIED` |
| 驗證碼錯誤 | 400 | `OTP_INVALID` |
| 驗證碼過期 | 400 | `OTP_EXPIRED` |
| 驗證碼嘗試次數過多 | 429 | `OTP_TOO_MANY_ATTEMPTS` |
| 發送過於頻繁 | 429 | `OTP_RATE_LIMITED` |
| 帳號或密碼錯誤 | 401 | `INVALID_CREDENTIALS` |

**列舉防護（對應 S55）**：`register` 與 `send-otp` 對「信箱已存在」與「信箱不存在」
必須回**相同的狀態碼與訊息**，且回應時間不得有明顯差異，否則可用來枚舉哪些信箱
已註冊。唯一例外是 `register` 的 `EMAIL_EXISTS`——那是前端需要的既有行為，
請維持現狀，但**不要在 `send-otp` 洩漏**。

### 第四期：端點認證（關掉破口三）

| 項目 | 要求 |
| --- | --- |
| 共用密鑰 | 資料庫端要求所有請求帶 `X-Gateway-Key: <secret>`，缺少或不符一律 `401` |
| 密鑰來源 | 由我方以環境變數 `UPSTREAM_MEMBER_API_KEY` 提供，請提供一組供我們設定 |
| 比較方式 | 同樣要固定時間比較 |
| 適用範圍 | **包含** `/api/register`、`/api/send-otp`、`/api/verify-otp`、`/api/login`——目前這四條是唯一沒有保護的 |

我方會在 Gateway 呼叫時補上該標頭（`proxy_public_member_request()` 與登入代理）。

> **切換順序很重要**：請先做成「有帶就驗、沒帶也放行」的寬鬆模式並通知我們，
> 待我們部署完帶標頭的版本、確認線上正常後，再切成強制。直接切強制會讓正式站
> 立刻全面登入失敗。

### 第五期：正式網域與退場

| 項目 | 要求 |
| --- | --- |
| 退出臨時通道 | 會員資料庫由 `*.trycloudflare.com` 遷至固定網域／Cloud Run（對應 P0-6）|
| 存取來源限制 | 僅允許 Gateway 的來源存取，不對公開網際網路開放 |
| 舊通道關閉 | 遷移完成後關閉舊 Quick Tunnel，避免舊網址仍可直連 |

第四期是「有鎖」，第五期是「不放在大街上」。兩者都要，順序可以並行但不可互相取代。

---

## 四、驗收方式

每一期結束後請執行對應項目並回報結果。以下 `$DB` 代表資料庫基底網址。

### 第一期驗收

```bash
# 1) 註冊一個全新的假信箱
curl -s -X POST "$DB/api/register" -H 'Content-Type: application/json' \
  -d '{"email":"otp-test-01@example.com","password":"Test12345","name":"t","phone_number":"0900000000","age":"20"}'

# 2) 不驗證 OTP，直接登入 —— 這一步必須失敗
curl -s -i -X POST "$DB/api/login" -H 'Content-Type: application/json' \
  -d '{"email":"otp-test-01@example.com","password":"Test12345"}'
```

- [ ] 第 2 步回 **`403`** 且 `error.code == "EMAIL_NOT_VERIFIED"`
- [ ] 完成 `verify-otp` 之後，同一組帳密登入回 `200`
- [ ] 本期上線前既有的帳號仍可正常登入（未被誤鎖）

### 第二期驗收

- [ ] 錯誤的驗證碼回 `400 OTP_INVALID`
- [ ] 10 分鐘後的驗證碼回 `400 OTP_EXPIRED`
- [ ] 同一組碼連續錯 5 次後回 `429 OTP_TOO_MANY_ATTEMPTS`，且該碼作廢
- [ ] 驗證成功的碼再用一次會失敗
- [ ] 60 秒內重複 `send-otp` 回 `429 OTP_RATE_LIMITED`
- [ ] 資料庫中查不到任何 OTP 明碼（只有雜湊）

### 第三期驗收

- [ ] 對「已註冊」與「未註冊」的信箱呼叫 `send-otp`，狀態碼與訊息完全相同
- [ ] 六種錯誤情境各自回傳規格指定的 `code`

### 第四期驗收

- [ ] 不帶 `X-Gateway-Key` 直接呼叫四條端點，全部回 `401`
- [ ] 帶正確金鑰時行為與現況一致
- [ ] 寬鬆模式期間，未帶標頭的舊版 Gateway 仍可運作

### 第五期驗收

- [ ] 舊的 `*.trycloudflare.com` 網址已無法連線
- [ ] 由 Gateway 以外的來源直接存取新網域會被拒絕

---

## 五、優先順序建議

若無法一次做完，請照這個順序：

1. **第一期**——這是唯一能立刻止血的一項。只要 `login` 擋住未驗證帳號，
   「用假信箱建立可用帳號」就不成立了。
2. **第四期**——關掉匿名直呼，Gateway 的限流才真正有意義。
3. 第二期 → 第三期 → 第五期。

---

## 六、需要貴端回覆的三件事

1. 既有帳號如何處理（建議以上線時間為界，之前視為已驗證）——確認後我們同步前端文案。
2. `X-Gateway-Key` 的金鑰值與切換時程（請先寬鬆模式）。
3. 第一期預計完成時間——在那之前，我方會在前端移除開發用繞過旗標，
   但**這不會修好任何東西**，真正的閘門只有貴端能建。
