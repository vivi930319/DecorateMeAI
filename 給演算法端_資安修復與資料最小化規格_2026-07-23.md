# 給演算法端：資安修復與資料最小化規格

## 2026-07-23 Gateway 側現況（供對接）

我方 Gateway 已上線：只轉送必要 content type、job token 與經 Cloud Run 判定的來源 IP，並改用 Google 服務身分呼叫；瀏覽器自報的 email／role／Authorization 一律不轉送，會員以不含 email 的 opaque actor 表示；access log 已遮罩 email。核心服務維持 IAM 私有（直連回 403）。**下列資料最小化、TTL 清理、輸入防護與 job owner 驗證仍需貴端於服務內實作**。

## 資料邊界

- 只接收完成任務所需的壓縮圖片與白名單分析欄位；不接收 Email、會員名稱、權杖、Cookie、完整會員物件。
- 圖片使用 opaque job ID／actor ID；不得用 Email 作檔名、路徑或 Log 標籤。
- 原始圖片與中間特徵只在工作期間存在，完成或失敗後由 TTL 清理；需有可測試的刪除接口。
- 回傳結果採固定 schema 與版本號，未知欄位拒絕或忽略，不把模型內部物件直接序列化。

## 輸入與資源防護

- 解碼前驗證 magic bytes、檔案大小、像素上限、圖片張數與總 payload。
- 移除 EXIF/GPS；拒絕損壞、多幀或造成異常記憶體配置的圖片。
- 每個 actor 設定併發、頻率、最大執行時間與取消機制。
- 模型檔與依賴需固定版本、校驗 checksum；不得在啟動時從任意網址下載未驗證模型。

## 權限與 Log

- 服務只允許 Gateway service account 呼叫；不得公開 `allUsers`。
- 服務金鑰只由 Secret Manager 注入，不寫入程式、文件、前端或測試輸出。
- Log 不包含照片、base64、landmarks 全量、完整分析包或可識別會員資料。

## 必交測試

- 假 MIME、巨大像素、截斷檔、多圖超額、逾時與取消。
- 未授權、錯 actor、重放 job token、讀取他人 job。
- TTL 與會員刪除後無法再取得圖片／分析結果。
- BASIC／PRO schema contract 與錯誤碼一致性。
