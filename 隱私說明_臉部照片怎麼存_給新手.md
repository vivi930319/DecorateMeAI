# 使用者的臉照存在哪、誰看得到？（新手版說明）

> 對象：專案負責人（isach），把你當第一次接觸雲端儲存來解釋
> 更新：2026-07-22
> 取代舊檔 `隱私說明_渲染圖公開讀_給新手.md` —— 那份描述的「公開讀取」狀態
> 在 2026-07-21 已經改掉了，整份都過期，留著會誤導。

---

## 一句話結論

現在有**兩張**臉部照片會存到雲端：使用者上傳的原圖（妝前）和 AI 生成的妝後圖。
兩張都**存在私有空間**，網址不是永久的，**必須登入而且必須是本人（或管理員）才讀得到**。
使用者刪掉收藏時，兩張都會被刪除。

比七月初的狀態嚴格很多，但**存的東西也變多了**（多了一張原圖），
所以不能再說「原始照片不會離開你的瀏覽器」——那句話現在不成立。

---

## 一、現在的流程長怎樣

1. 使用者上傳自己的臉照。
2. 後端把臉照送去 AI（Replicate）渲染出「上妝後」的圖。
3. **兩張圖都上傳到 Google Cloud Storage（GCS）** 的 `decorate-me-renders` 這個空間（bucket）。
4. 前端拿到的**不是** GCS 的網址，而是我們自己的路徑：

   ```
   /media/render/<32碼亂數>          ← 妝後圖
   /media/render/<32碼亂數>/before   ← 妝前圖（原圖）
   ```

5. 瀏覽器要看圖時，這條路徑會經過 **AI Gateway**。Gateway 會：
   - 檢查你**有沒有登入**
   - 檢查這張圖**是不是你的**（或者你是不是管理員）
   - 通過才去換一條**十分鐘後失效**的臨時網址，讓瀏覽器去抓圖

**關鍵**：bucket 是**私有**的。直接拿 GCS 網址貼到瀏覽器打不開。

---

## 二、跟七月初差在哪

| | 2026-07 初（舊文件描述的） | 現在 |
|---|---|---|
| bucket 權限 | **公開讀**（`allUsers: objectViewer`），有網址就能看 | **私有**，直接開打不開 |
| 前端拿到的網址 | GCS 永久公開網址 | 我們自己的 `/media/render/...`，要驗證身分 |
| 網址流出去 | **永久有效，收不回** | 臨時網址十分鐘失效；固定路徑沒登入也打不開 |
| 存幾張臉 | 只有妝後圖 | **妝前圖＋妝後圖** |

舊文件當時的建議是「畢業專題階段可以先維持公開」。**那個建議已經被推翻了，而且是往好的方向** ——
現在做的正是它列為「時間夠再做」的那個方案。

---

## 三、為什麼要多存一張妝前圖？

因為收藏功能要顯示**前後對比**。先前只存妝後圖，使用者打開收藏只看得到「後」，
沒有「前」可以比。

**這是一個取捨，要誠實面對**：多存一張原圖，等於多一份使用者的臉在我們的伺服器上。
我們判斷這個增量可以接受，理由是：

- 妝後圖**本來就是使用者的臉**（那是他的照片加工出來的），已經在存了
- 兩張圖走**完全相同**的權限檢查，妝前圖沒有比較鬆
- 使用者刪掉收藏時**兩張一起刪**

但「原始照片不會離開瀏覽器」這句話**以後不能再說**。要說就說：
「原始照片會加密儲存，只有你本人看得到，你刪掉收藏時我們也會刪掉它。」

---

## 四、圖會存多久？

| 情況 | 保留多久 |
|---|---|
| 渲染完**沒有**收藏 | 暫存區 `temporary/`，**2 天後自動刪除** |
| 渲染完**有**收藏 | 搬到 `retained/`，**保留到使用者刪除為止** |
| 使用者刪掉收藏 | 妝前圖＋妝後圖**立即刪除** |
| 使用者刪除帳號 | 該會員所有圖片一併刪除 |

另外，**渲染完一小時內沒收藏的話，就算圖還在，也讀不到了** ——
因為要靠 job 紀錄才能找到檔案，那筆紀錄一小時後就清掉。這是刻意的：
沒被收藏的圖沒有理由一直留著。

---

## 五、還沒解決的一件事（要誠實記著）

**會員資料庫目前掛在公開的 Cloudflare 臨時通道上。**

臉部照片不受影響（那是 GCS，另一套），但**會員的帳號、點數、收藏、妝容紀錄**
是存在那個資料庫裡的，而那個通道**任何人拿到網址就能直接連**，
繞過我們 Gateway 的所有檢查。

這是目前最大的資安缺口，比臉照的問題嚴重。已經寫進給資料庫端的待修清單，
根本解法是請他們改用固定的、非公開的網址。

---

## 六、口試被問時可以這樣回答

> 「使用者的臉部照片有兩張：上傳的原圖和渲染後的妝容圖，都存在 Google Cloud Storage
> 的**私有** bucket。前端拿到的不是儲存空間的網址，而是經過我們 Gateway 的路徑，
> Gateway 會驗證登入狀態與圖片擁有者，通過之後才簽發一條十分鐘失效的臨時網址。
>
> 這是七月中改過的 —— 原本 bucket 是公開讀取，只靠亂數檔名防止被枚舉，
> 我們評估後認為『網址一旦流出就收不回』的風險不能接受，所以改成私有加簽名網址，
> 並把收藏改成存我們自己的固定路徑，讀取時再即時簽發。
>
> 保存期限方面，沒被收藏的圖兩天後自動刪除，收藏的保留到使用者刪除為止，
> 刪除時原圖與妝容圖一起清掉。」

這段話的價值在於：你**先有一個不夠好的版本，自己發現風險，然後改掉**。
評審想聽的是這個過程，不是「我們一開始就做對了」。

---

## 七、實作上最容易出錯的地方（給接手的人）

**刪除必須兩張都刪。** 妝前圖與妝後圖共用同一個 job，程式裡有三個地方要成對處理：
`_delete_job_artifact`、渲染逾時後的清理、共用物件的引用檢查。
漏掉任何一個，使用者刪了收藏但臉還留在 bucket 裡——**那不是浪費空間，是隱私事故**。

**搬移到永久區時要用不同的檔名。** 兩張圖共用 job id，目的地名稱若只用 job id 會撞在一起，
而且程式碼裡的「目的地已存在就跳過」檢查**不會報錯**，只會讓妝前圖指向妝後那張。
所以妝前圖的檔名多一段 `-before`。

---

## 名詞小抄

- **GCS / bucket**：Google 的雲端檔案空間，bucket 就是一個「空間／資料夾」。
- **私有 bucket**：只有拿到授權的人能看；直接貼網址打不開。
- **Signed URL（簽名網址）**：後端臨時發的、有時效的通行證網址，過期就失效。
- **枚舉（enumeration）**：用「1、2、3…」把檔案一個個猜出來的攻擊手法。
- **Gateway**：全系統唯一的對外入口，所有上游服務的網址與金鑰都只存在它裡面。
