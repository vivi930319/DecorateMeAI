# 給爬蟲端：資安修復與 SSRF 防護規格

## 2026-07-23 前端／Gateway 側現況（供對接）

我方已上線：前端把爬蟲抓回的商品名稱、價格、品牌、來源 URL 全部視為不可信——輸出一律 escape，商品來源頁連結只接受 `http(s)`（`safeExternalUrl`），圖片 URL 經 scheme 驗證；Admin 的預覽／匯入等寫入一律經同源 Gateway 並帶 `X-Expected-Actor`（跨分頁換帳號時第一個寫入即被擋）。**因此下列後端 SSRF／來源驗證項目仍需貴端實作**，前端防線不能取代伺服器端驗證。

## 必修項目

- 爬蟲預覽／匯入只允許 `https:`；拒絕使用者資訊、非標準 port、IP literal、localhost、私有／保留／link-local／metadata IP。
- DNS 解析後及每一次 redirect 都重新驗證目的 IP，避免 DNS rebinding；redirect 次數設上限。
- 僅允許核准的商品來源網域，子網域匹配需正確，不能用字串 `endsWith` 誤放行。
- 連線、讀取與整體逾時分開設定；串流下載並限制 Content-Length、實際位元組、HTML 大小、圖片大小與頁面資源數。
- 不執行任意頁面腳本，不下載檔案，不存 Cookie；若必須使用瀏覽器引擎，放在無雲端憑證、無內網路由的隔離環境。
- 解析出的名稱、價格、圖片 URL、來源 URL 皆視為不可信資料；後端驗證，前端輸出時 escape，href/src 僅允許安全 scheme。
- 匯入商品需 Admin Session、CSRF／預期 actor 防護、idempotency 與稽核紀錄。
- 商品圖片採代理／重編碼時移除 metadata，驗證內容，不保存第三方追蹤參數。
- Log 不記完整帶 query 的來源 URL、頁面內容、Cookie 或驗證資訊。

## 必交測試

- `localhost`、`127.0.0.1`、IPv6 loopback、私有 IP、十進位 IP、metadata 位址及 redirect 到內網均拒絕。
- DNS rebinding、超大 HTML、慢速回應、redirect loop、假圖片與惡意 HTML。
- 一般會員不能預覽後匯入；Admin 跨分頁換帳號時第一個寫入即被阻擋。
- `javascript:`／`data:` 來源 URL 不會進入可點擊連結。


