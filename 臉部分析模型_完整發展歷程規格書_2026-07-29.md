# 臉部分析模型 — 完整發展歷程規格書

**專案：** DECORATE ME（裝識你的美）· BASIC 五官分類
**文件日期：** 2026-07-29（**2026-07-30 修訂**：§七.6 起的全套數字重跑，見下）
**涵蓋範圍：** 從最初的幾何規則到目前正在進行的實驗，含每一次改動的理由、數據與結論
**用途：** 專題歷史紀錄

> **2026-07-30 修訂說明**：發現 07-30 凌晨那批「桃杏眼／唇型 4 類」的數字是在 identity map
> 過期的狀態下量的（**桃杏眼 221 張只有 1 張進過驗證集**，詳見 §七.6）。
> 已重建身分聚類並把 §十 全套流程重跑一遍，本文件的數字都換成重跑後的值。
> 被推翻的舊值一律保留並標明，不直接刪掉。

---

## 目錄

- [零、給趕時間的人：一頁摘要](#零給趕時間的人一頁摘要)
- [一、任務定義](#一任務定義)
- [二、世代 0：MediaPipe 幾何閾值規則](#二世代-0mediapipe-幾何閾值規則)
- [三、世代 1：整臉 CNN（已淘汰）](#三世代-1整臉-cnn已淘汰)
- [四、世代 2：部位 ROI + 按人切分](#四世代-2部位-roi--按人切分)
- [五、世代 3：資料清理與 5-fold（2026-07-24）](#五世代-3資料清理與-5-fold2026-07-24)
- [六、世代 4：資料統一與三模型對決（2026-07-29）](#六世代-4資料統一與三模型對決2026-07-29)
- [七、世代 5–6：超參數、Hybrid、桃杏眼與唇型合併](#七世代-56超參數hybrid桃杏眼與唇型合併2026-07-29--30)
  - [7.6 事故：搬檔案讓合併後的類別退出驗證集](#76-事故搬檔案讓合併後的類別退出驗證集)
  - [7.7 唇型合併：第二個明確有效的類別調整](#77-唇型合併第二個明確有效的類別調整)
  - [7.8 下游沒跟上分類表（已修）](#78-下游沒跟上分類表2026-07-30-已修)
  - [7.9 完整方格：十一個變體同組 fold 對決](#79-完整方格十一個變體同組-fold-對決)
  - [**7.10 結論：目前的最好組合**](#710-結論目前的最好組合)
  - [7.11 幾何決策樹全面退場](#711-幾何決策樹全面退場fallback-一律落在-cnn)
- [八、全部數據總表](#八全部數據總表)
- [九、方法論：這個專案學到的教訓](#九方法論這個專案學到的教訓)
- [十、重現方式](#十重現方式)

---

## 零、給趕時間的人：一頁摘要

臉部分析要從一張照片判斷五個部位：臉型、眉型、眼型、鼻型、唇型。

**四個世代，每一代都是被前一代的失敗推出來的：**

| 世代 | 做法 | 為什麼被推翻 |
| --- | --- | --- |
| 0 | MediaPipe landmark 幾何閾值 if-else | 與人工標籤只有 33% 一致；某些類別的門檻是死碼，永遠不會被觸發 |
| 1 | 整張臉 resize 160×160 餵 CNN + 隨機切分 | 鼻子只剩幾十個像素；隨機切分讓同一張臉同時進 train/val，分數虛高 |
| 2 | 裁部位 ROI 餵 CNN + 按人切分 | 分數可信了，但發現 val 只有 43–105 張，雜訊 ±0.1，比實驗差距還大 |
| 3 | 內容去重 + 5-fold + 決策樹 / DINOv2 對照 | 發現 314 個重複檔；先前的高分是假的 |
| 4 | 資料統一 + 三模型同組 fold 對決 | 發現瓶頸是**類別定義**，不是模型也不是資料量 |
| 5 | 超參數 + Hybrid + 桃杏眼 | 量出雜訊底線 ±0.016；再次證明類別定義是最大槓桿 |
| 6 | 唇型 4 類 + 修 identity map + 全套重跑 | 合併類別的方向再次成立（唇型 +0.068）；但 identity map 過期讓合併後的類別**幾乎沒進過驗證集**，那批數字作廢 |

**目前最好的數字（2026-07-30 完整方格，5-fold identity 切分，macro accuracy）：**

| 部位 | 類別數 | 亂猜 | 最佳 | 由誰達成 | 07-29 舊值 |
| --- | --- | --- | --- | --- | --- |
| nose_shape | 2 | 0.50 | **0.868** | CNN 40ep/**6e-4** | 0.881 |
| lip_shape（4 類）| 4 | 0.25 | **0.565** | CNN 40ep/**6e-4** | 0.497（5 類）|
| brow_shape | 3 | 0.33 | **0.543** | DINOv2+LogReg（CNN 0.542，平手）| 0.543 |
| eye_shape（桃杏眼 5 類）| 5 | 0.20 | **0.542** | DINOv2+SVM | 0.556 |
| face_shape | 5 | 0.20 | **0.522** | CNN 40ep/**6e-4** | 0.516 |

**建議部署組合：四個部位用 CNN(mobilenet, 40ep, 6e-4)，眼型用 DINOv2+SVM。**
完整方格與逐折配對見 §7.9、§7.10。

**最重要的一個結論（重跑後仍然成立，而且又多一個例證）：**
**合併定義不存在的類別，是這個專案唯一穩定有效的槓桿。**

```
眼型 6 類 → 桃杏眼 5 類    0.473 → 0.542   +0.069   (DINOv2)
唇型 5 類 → 4 類           0.492 → 0.560   +0.068   (CNN)
```

兩者都遠超雜訊底線 ±0.016，而同期所有「換模型／調參數／加資料」沒有一項做到這個幅度。
**分數低的主因是類別界線本來就不存在**——連標註同學自己都分不開（見 §6.4、§7.4、§7.7）。

---

## 一、任務定義

### 1.1 五個部位與官方分類表

分類表由標註同學定義，是唯一事實來源。2026-07-29 由使用者再次確認：

```
face_shape : 圓形臉 心形臉 方形臉 長形臉 鵝蛋臉          (5 類)
brow_shape : 一字眉 彎月眉 落尾眉                        (3 類)
eye_shape  : 下垂眼 圓眼 桃杏眼 細長眼 鳳眼               (5 類)
nose_shape : 寬鼻 標準鼻                                 (2 類)
lip_shape  : 厚唇 微笑唇 花瓣唇 薄唇                      (4 類)
```

> 2026-07-30 起唇型是 **4 類**（M型唇併入花瓣唇，見 §7.7）。
> 分類表的唯一事實來源是 `prepare_roi_cache.py` 的 `CANONICAL_LABELS`，
> 線上推論則讀 `models/basic_features_roi/<part>_classes.json`；兩邊目前一致。

**已淘汰的類別與併入對象：**

| 舊類別 | 併入 | 何時 | 原因 |
| --- | --- | --- | --- |
| 丹鳳眼 | 鳳眼 | 2026-07-24 | 與鳳眼視覺上無法區分，且大量重複檔 |
| 瞇縫眼 | 細長眼 | 2026-07-24 | 僅 33 張，且門檻是死碼（見 §2.2） |
| 窄鼻 | 標準鼻 | 2026-07-24 | **「窄鼻」的鼻翼比「標準鼻」還寬**（見 §5.5） |
| 蒜頭鼻 | 寬鼻 | 2026-07-24 | 標註者用詞不同，實為同一類 |
| 杏仁眼 | **桃杏眼** | 2026-07-30 | 與桃花眼的界線不存在（見 §7.4）|
| 桃花眼 | **桃杏眼** | 2026-07-30 | 同上；合併後衝突樣本由 57 組歸零 |
| M型唇 | **花瓣唇** | 2026-07-30 | recall 只有 0.30，錯的均勻散到另外四類——「這一類沒有可辨識特徵」的樣貌（見 §7.7）|

### 1.2 評估指標：為什麼用 macro accuracy 而不是 accuracy

**macro accuracy = 各類別 recall 的平均。**

用 accuracy 的話，一個只會回答「一字眉」的模型在眉型上就有 50% 的分數（因為一字眉佔一半），
看起來還行，實際上完全沒用。macro 會把它壓到 0.33（亂猜的水準），因為另外兩類的 recall 是 0。

**這不是理論顧慮**——世代 0 的規則式就是這樣壞的（§2.3）。

### 1.3 驗證方式：為什麼一定要按人（identity）切分

資料是自行拍攝與截圖，同一個人常有多張照片。隨機切分會讓同一張臉同時出現在
train 與 val，模型在驗證集上看到的是它訓練時背過的臉，分數會虛高。

實測的洩漏幅度最高 **+0.113**（§4.3）。

所有可信的數字都用 `split_kfold_by_identity`，身分由 `tools/build_identity_map.py` 聚類產生。

### 1.4 驗收門檻（原始規格書訂的，至今沒有一項達標）

`DINOv2_臉部特徵分類模型規格書.md` §驗收 訂的正式採用門檻：

| 部位 | 門檻（macro F1） | 目前最佳（07-30 完整方格）| 差距 |
| --- | --- | --- | --- |
| face_shape | 0.70 | 0.522 | **−0.18** |
| brow_shape | 0.75 | 0.543 | **−0.21** |
| eye_shape | 0.70 | 0.542（桃杏眼 5 類）| **−0.16** |
| nose_shape | 0.70 | 0.868 | ✅ **+0.17** |
| lip_shape | 0.70 | 0.565（4 類）| **−0.14** |
| PRO nose_shape_side | 0.75 | 見 PRO 紀錄 | — |

**五個部位裡只有鼻型達標，而它只有 2 類（亂猜就有 0.50）。**

以「比亂猜好多少」來看，實際排序是：

```
nose_shape  0.868 vs 0.50 亂猜  →  +0.368
eye_shape   0.542 vs 0.20 亂猜  →  +0.342  (桃杏眼 5 類)
face_shape  0.522 vs 0.20 亂猜  →  +0.322
lip_shape   0.565 vs 0.25 亂猜  →  +0.315  (合併後 4 類)
brow_shape  0.543 vs 0.33 亂猜  →  +0.210
```

**這樣看仍然是眉型最弱**——它只有 3 類，門檻卻訂得最高（0.75）。
不過 07-30 的學習曲線顯示眉型**沒有飽和**（0.434 → 0.506 單調上升，見 §6.7 修訂），
這一點與 07-29 的判斷相反。

### 1.5 元件分工

原始規格書定義的分工，目前實作與它一致：

```
InsightFace        人臉偵測、頭部姿態、正臉／側臉品質檢查
MediaPipe FaceMesh landmark、ROI 裁切定位、幾何特徵
DINOv2             抽取部位 embedding（ViT-S/14，凍結不微調）
分類器             CNN(MobileNetV3) / LogisticRegression / SVM / 幾何決策樹
```

**DINOv2 不取代 MediaPipe，是重新分工。** MediaPipe 仍然負責「這張圖的眼睛在哪」，
DINOv2 負責「這雙眼睛長什麼樣」。

---

---

## 二、世代 0：MediaPipe 幾何閾值規則

### 2.1 做法

`Face_analyzer_BASIC.py` 用 MediaPipe FaceMesh 取 468 個 landmark，
算出幾何比值後用手寫 if-else 判類別。例如眼型看：

```
ear            開合比（eye aspect ratio）
angle          眼角傾斜角度
ratio_to_face  眼睛相對整張臉的大小
```

**優點是真的存在的：** 可讀、可解釋、推論成本為零、不需要訓練資料。
所以它到今天仍然是 fallback。

### 2.2 死碼：有些門檻永遠不會成立

用專案實際資料驗證每個門檻的觸發率，發現三個**死碼**：

```
瞇縫眼 需要 ratio < 0.12   但資料中實際最小值是 0.194  → 永遠判不出瞇縫眼
窄鼻   需要 <= 0.205        但實際分布不在這個範圍       → 永遠判不出窄鼻
```

門檻是從 CelebA（西方人臉為主）調出來的，套到本專案的亞洲人臉資料上直接失效。
**這類錯誤不會報錯，只會安靜地讓某些類別再也不出現。**

### 2.3 與人工標籤只有 33% 一致

抽驗眉型：資料夾標籤有「一字眉／落尾眉」，但規則式對**每一張**都吐「彎月眉」。

這件事同時證明了兩件事：

1. **規則式壞了**（退化成幾乎只輸出一個答案）
2. **標籤不是規則式產生的**——這很重要，因為 repo 裡有一條
   `auto_label_basic_dataset.py` → `organize_basic_results.py` 的自動標註工具鏈。
   如果標籤是規則式產生的，CNN 學到的只會是在模仿一個壞掉的規則，**所有分數都沒有意義**。
   實測否定了這個懷疑，標籤是可信的 ground truth。

### 2.4 現況

眼型與臉型的正式答案改由**學習出來的幾何決策樹**（`basic_rule_trees`）提供，
手寫 if-else 降為 fallback。2026-07-29 已用統一後的資料重訓（§6.6）。

---

## 三、世代 1：整臉 CNN（已淘汰）

`train_basic_features.py`：整張臉 resize 成 160×160 餵 CNN。

**兩個結構性缺陷：**

1. **餵整張臉去判鼻型**——鼻子在 160×160 的圖裡只剩幾十個像素，
   模型很容易改去學「這個人是誰」而不是「這個鼻子什麼形狀」。
2. **隨機切分**——同一個人的多張照片會同時進 train 與 val。

這一版沒有留下可信的數字，因為兩個缺陷都會讓分數往上虛報。腳本保留但不再使用。

---

## 四、世代 2：部位 ROI + 按人切分

`train_basic_cnn_roi.py`。這是目前仍在用的主線。

### 4.1 兩個關鍵改動

1. **餵部位 ROI，不是整張臉。** 依 `face_roi.ROI_SPECS` 從 landmark 裁出各部位：
   臉型 128×128、其餘 96×96。
2. **按 identity 切分**，並同時跑「隨機切分」當對照組，把洩漏幅度實際量出來。

### 4.2 訓練設定

```
模型      MobileNetV3-small（ImageNet 預訓練）
優化器    AdamW, lr=3e-4, cosine 排程   ← 世代 2 當時的值；現行最佳是 6e-4（§7.9.1）
損失      label smoothing 0.05
不平衡    WeightedRandomSampler
增強      刻意不做水平翻轉
```

> **為什麼不做水平翻轉**：落尾眉翻轉後會變成上揚眉，等於餵錯標籤。
> 這是幾乎所有影像分類預設會開、但在這個任務上會直接造成錯誤的增強。

### 4.3 結果（1297 張時期）

**第一輪 25 epochs：**

| 部位 | 類別 | 隨機切分 | 按人切分 | 洩漏 |
| --- | --- | --- | --- | --- |
| face_shape | 5 | 0.483 | **0.530** | −0.048 |
| brow_shape | 3 | 0.493 | **0.603** | −0.110 |
| eye_shape | 7 | 0.368 | **0.346** | +0.022 |
| nose_shape | 3 | 0.749 | **0.773** | −0.024 |
| lip_shape | 5 | 0.498 | **0.386** | +0.112 |

**第二輪 40 epochs：**

| 部位 | 隨機切分 | 按人切分 | 洩漏 | vs 第一輪 |
| --- | --- | --- | --- | --- |
| face_shape | 0.588 | **0.475** | +0.113 | −0.055 |
| brow_shape | 0.590 | **0.589** | +0.000 | −0.014 |
| eye_shape | 0.481 | **0.378** | +0.103 | +0.032 |
| nose_shape | 0.771 | **0.666** | +0.105 | −0.107 |
| lip_shape | 0.479 | **0.452** | +0.027 | +0.066 |

### 4.4 這一輪最重要的發現：分數本身不可靠

epochs 從 25 加到 40，**三個部位變差、兩個變好，方向隨機**。

原因：val set 只有 43–105 張，一張分類結果就值 1–2 個百分點，
**分數的隨機波動約 ±0.1**。

> **「0.53 vs 0.47」這種差距在當時的資料量下沒有意義，不能拿來選模型或調參。**

這個認知直接催生了世代 3 的 5-fold 交叉驗證——因為單次切分根本量不準。

---

## 五、世代 3：資料清理與 5-fold（2026-07-24）

> **這一輪最大的產出不是新模型，是發現訓練資料本身有系統性問題。**
> 在修掉它們之前，任何「準確度變好」的說法都不能採信。

### 5.1 314 個重複檔

對 1908 個檔案做 MD5 內容比對：

- **254 組**內容逐位元組相同，多餘檔案 **314 個**
- 其中 **195 組是同一張圖出現在兩個不同類別的資料夾**

```
丹鳳眼/IMG_3603.jpg   ==   鳳眼/IMG_3603.JPG
一字眉/IMG1.JPG       ==   彎月眉/IMG_7300.jpg
```

**多數不是標註打架，是「類別合併只做了一半」**——舊類別的圖被*複製*到新類別資料夾，
舊資料夾沒刪。套上官方分類表後就變成同類重複。

### 5.2 先前的高分是假的

`per_image` 切分模式下，同一份像素被切到 train 與 val 兩邊，
模型在驗證集上看它背過的東西。

> 舊紀錄的鼻型 0.828、眉型 0.667 都出自這個模式，**不是真實表現**。

這一點必須記住：本文件 §8 的總表裡，`per_image` 那一列的數字**不可採信**。

### 5.3 清理後的可用量

| 部位 | 原始 | 內容去重 | 剩餘真衝突 | **乾淨可用** |
| --- | --- | --- | --- | --- |
| 臉型 | 386 | 358 | 28 | **330** |
| 眉型 | 336 | 203 | 14 | **189** |
| 眼型 | 603 | 455 | 68 | **387** |
| 鼻型 | 231 | 231 | 0 | **231** |
| 唇型 | 352 | 347 | 5 | **342** |

去重後類別分布**反而變均衡**：眉型原本看起來是 `一字眉 183／彎月眉 82／落尾眉 71`，
去重後是 `彎月眉 67／一字眉 61／落尾眉 61`——**那個 183 的優勢是重複檔堆出來的**。

### 5.4 identity_map 過期，鼻型驗證幾乎是空的

寬鼻 98 張裡**只有 11 張**曾進過驗證集。也就是說「鼻型 macro 中，寬鼻那一半是建立在
11 張圖上」——那個數字沒有意義。修法是重建身分聚類。

### 5.5 窄鼻的鼻翼比標準鼻還寬

量出來的鼻翼寬度中位數：

```
寬鼻 0.310  >  窄鼻 0.291  >  標準鼻 0.283
```

**「窄鼻」比「標準鼻」還寬。** 標註者判斷窄鼻時看的顯然不是鼻翼寬度
（可能是鼻頭大小或鼻樑高度），而模型只看得到鼻翼。

結果是 33 個標準鼻裡有 30 個被判成窄鼻，**整個類別塌陷**。
併成兩類重訓後才恢復正常。

> 這是本專案第一次遇到「類別定義本身有問題」——世代 4 會再遇到一次，而且更嚴重。

### 5.6 規則式 vs CNN（同一組 5-fold）

| 部位 | 最佳規則式 | CNN | 勝場 | 當時結論 |
| --- | --- | --- | --- | --- |
| 眼型 | **0.4059** | 0.3317 | **5/5** | **改用規則式** |
| 臉型 | **0.5439** | 0.4769 | **4/5** | **改用規則式** |
| 眉型 | 0.5026 | 0.5747 | 2/5 | 維持 CNN |
| 鼻型 | 0.8126 | 0.8952 | 1/5 | 維持 CNN |
| 唇型 | 0.3569 | 0.4300 | 1/5 | 維持 CNN |

眼型與臉型改用幾何決策樹上線（`basic_rule_trees`）。理由：兩者都是**幾何量而非紋理**
——眼型是開合比例與眼角角度，臉型是不同高度的寬度比值——在幾百張的規模下
CNN 得從像素重新學會這些量，直接算反而準。

⚠️ **這個結論在世代 4 被推翻**（§6.5）。

---

## 六、世代 4：資料統一與三模型對決（2026-07-29）

### 6.1 起點：訓練集散落在三個資料夾

```
data/basic_full/grouped/       ← prepare_roi_cache.py 唯一會讀的
data/basic_full/grouped_yun/   ← 組員新給的一批，沒進訓練
data/group_yun/                ← 更早的一批，沒進訓練
```

`prepare_roi_cache.py` 第 29 行 `ROOT` 是寫死的，**放在別的地方永遠不會進訓練**。
`data/group_yun` 有 58 張鼻型照片就是這樣一直沒被用到。

處理：全部併入 `grouped`，資料夾實體正規化成官方分類（不再只靠程式對照表），
全程只搬不刪、每一步都有 CSV 紀錄。最終稽核 **0 張照片遺失**。

### 6.2 去重鍵由「內容雜湊」改為「檔名」

世代 3 用 MD5 內容去重，那在當時是對的（有大量複製檔）。
但對這批新資料是**錯的**：

```
face_shape/心形臉/IMG_7859.jpg   746×1110  全臉
lip_shape/薄唇/IMG_7859.jpg      391×486   嘴部特寫
```

**同一張原始照片，為不同部位裁成不同大小。** 按內容雜湊會把它們當成兩張不相干的圖，
同一個人的臉型與唇型標註因此接不起來；實測有 **164 個檔名**落在這種情況。

標註同學的約定是「同名就是同一張照片」，改成依檔名去重後：

| 部位 | 改前可用 | 改後可用 | 變化 |
| --- | --- | --- | --- |
| eye_shape | 570 | **754** | +184 |
| brow_shape | 296 | **441** | +145 |
| face_shape | 490 | **530** | +40 |
| lip_shape | 534 | **543** | +9 |

抽 ROI 時取同名檔案裡**尺寸最大**的那份——部位特寫常常小到偵測不到完整人臉。

### 6.3 統一後的訓練集

| 部位 | 類別與張數 | 合計 |
| --- | --- | --- |
| face_shape | 圓形臉 116 / 心形臉 100 / 方形臉 106 / 長形臉 103 / 鵝蛋臉 105 | 530 |
| eye_shape | 下垂眼 110 / 圓眼 120 / 杏仁眼 109 / 桃花眼 119 / 細長眼 118 / 鳳眼 107 | 683 |
| brow_shape | 一字眉 220 / 彎月眉 118 / 落尾眉 103 | 441 |
| nose_shape | 寬鼻 138 / 標準鼻 132 | 270 |
| lip_shape | M型唇 109 / 厚唇 112 / 微笑唇 100 / 花瓣唇 117 / 薄唇 106 | 543 |

**2026-07-30 合併後的現況**（`data/roi_cache/index.json` 實際可用數）：

| 部位 | 類別與張數 | 合計 |
| --- | --- | --- |
| face_shape | 圓形臉 116 / 心形臉 100 / 方形臉 106 / 長形臉 103 / 鵝蛋臉 105 | 530 |
| eye_shape | 下垂眼 110 / 圓眼 120 / **桃杏眼 221** / 細長眼 118 / 鳳眼 107 | 676 |
| brow_shape | 一字眉 220 / 彎月眉 118 / 落尾眉 103 | 441 |
| nose_shape | 寬鼻 137 / 標準鼻 132 | 269 |
| lip_shape | 厚唇 112 / 微笑唇 99 / **花瓣唇 225** / 薄唇 106 | 542 |

> 合併後最大類別（桃杏眼 221、花瓣唇 225）都是次大類別的近兩倍。
> 這在 macro accuracy 下不會像 accuracy 那樣被掩蓋，但**訓練仍靠
> `WeightedRandomSampler` 撐著**，不要以為合併是零成本的。

### 6.4 三模型對決（同一組 5-fold，可直接比較）

| 部位 | 幾何決策樹 | CNN | DINOv2 | 最佳 | 領先次佳 |
| --- | --- | --- | --- | --- | --- |
| face_shape | 0.445 | **0.470** | 0.424 | CNN | +0.046 |
| brow_shape | 0.448 | 0.490 | **0.543** | DINOv2 | +0.053 |
| eye_shape | 0.379 | 0.388 | **0.473** | DINOv2 | +0.085 |
| nose_shape | 0.782 | 0.827 | **0.846** | DINOv2 | +0.019 |
| lip_shape | 0.336 | **0.485** | 0.446 | CNN | +0.039 |

**沒有任何一項的領先幅度超過一個標準差**（DINOv2 的 std 是 0.038–0.105）。
唯一站得住的結論是：**幾何決策樹五個部位全部墊底**，而且這是在它已經用
`calibrate_rule_thresholds.py` 重新校準過之後——不是「沒校準才輸」。

### 6.5 世代 3 的結論被推翻

| 部位 | 7/24（規則 vs CNN） | 7/29（規則 vs CNN） | 變化 |
| --- | --- | --- | --- |
| **眼型** | 0.406 vs 0.332 → 用規則式 | **0.379 vs 0.388** | CNN 反超 |
| **臉型** | 0.544 vs 0.477 → 用規則式 | **0.445 vs 0.470** | CNN 反超 |

原因：規則式的幾何門檻是在**舊資料**上校準的，資料換了一批就退步
（眼型 0.406 → 0.379、臉型 0.544 → 0.445）。

> **教訓：「規則式比較穩」是錯覺。** 它的門檻同樣是從資料擬合出來的，
> 只是擬合過程不叫訓練。資料一變，它一樣會退步，而且沒有任何機制提醒你。

### 6.6 眼型合併：唯一明確有效的改動

```
6 類（下垂/圓/杏仁/桃花/細長/鳳）   macro 0.388 ± 0.047
4 類（杏仁+桃花 併入 圓眼）        macro 0.523 ± 0.034     +0.135
各 fold: 0.502, 0.574, 0.535, 0.531, 0.472
```

**不只平均提高，標準差還變小（0.047 → 0.034），五折全部在 0.47 以上。**

證據鏈完整：`prepare_roi_cache.py` 偵測到「內容相同但標成不同類別」的樣本共 94 組，
眼型佔 57 組，其中前兩名正是：

```
圓眼 vs 杏仁眼      17 組
細長眼 vs 鳳眼      17 組
杏仁眼 vs 桃花眼     7 組
```

**標註同學自己就分不開這三類。** 模型學不會的東西，是因為那條邊界本來就不存在。

⚠️ 代價是眼型從 6 類降為 4 類，**這是產品決定不是技術決定**，尚未套用。

### 6.7 學習曲線：還需要多少張圖

用 DINOv2 embedding + LogisticRegression，按**身分**抽樣成 20%–100% 五種規模，
每種跑 5-fold：

| 部位 | 20% | 40% | 60% | 80% | 100% | 再加 100 張的效果 |
| --- | --- | --- | --- | --- | --- | --- |
| face_shape | 0.301 | 0.367 | 0.407 | 0.377 | 0.422 | **+0.035** |
| brow_shape | 0.465 | 0.455 | 0.518 | 0.512 | 0.506 | −0.007 |
| eye_shape | 0.286 | 0.372 | 0.417 | 0.424 | 0.437 | +0.010 |
| nose_shape | 0.679 | 0.802 | 0.820 | 0.831 | 0.828 | −0.007 |
| lip_shape | 0.284 | 0.326 | 0.340 | 0.403 | 0.392 | −0.010 |

**除了臉型，其餘四個部位已經飽和。** 眉型從 60%（252 張）之後就沒再長過。

#### 2026-07-30 修訂：眉型其實沒有飽和

修好 identity map、換成合併後的類別重跑同一支 `tools/learning_curve.py`：

| 部位 | 20% | 40% | 60% | 80% | 100% | 走勢 |
| --- | --- | --- | --- | --- | --- | --- |
| face_shape | — | — | — | — | 0.422 | 與舊值相同 |
| **brow_shape** | 0.434 | 0.429 | 0.463 | 0.489 | **0.506** | **單調上升 +0.072** |
| eye_shape（桃杏眼 5 類）| 0.360 | 0.443 | 0.431 | 0.433 | 0.465 | 仍在長，但很平 |
| nose_shape | 0.683 | 0.818 | 0.783 | 0.834 | 0.828 | 飽和 |
| lip_shape（4 類）| 0.319 | 0.343 | 0.309 | 0.401 | 0.399 | 80% 後飽和 |

**眉型從「60% 之後就沒再長過」變成「一路長到 100%」。** 舊曲線的 20% 起點是 0.465、
新的是 0.434，末端則一樣是 0.506——差別在低比例那幾點，也就是**身分抽樣抽到哪些人**
的影響。眉型只有 203 個身分，抽 20% 只剩 41 個人，那個數字的 ±std 是 0.082，
比整條曲線的落差還大。

> **教訓：學習曲線的低比例端不可信。** 「已經飽和」這種結論要看的是高比例端的斜率，
> 而不是頭尾兩點的差。以 80%→100% 的斜率看，眉型與臉型還有空間，鼻型與唇型沒有。

外推（log 擬合，**0.60 以上僅供參考數量級**）：

| 部位 | 現在 | 到 0.50 | 到 0.60 |
| --- | --- | --- | --- |
| face_shape | 530 | 1,487 | 5,611 |
| eye_shape | 683 | 1,142 | 3,147 |
| lip_shape | 542 | 2,208 | 8,765 |

**唇型要到 0.60 需要 8,765 張（現在的 16 倍）**——專題規模不可能達到。

> **結論：不要再蒐圖。** 眼型從 397 加到 683 張（+72%），macro 只從 0.417 到 0.437。
> 而合併類別一個動作就是 +0.135。

---

## 七、世代 5–6：超參數、Hybrid、桃杏眼與唇型合併（2026-07-29 ～ 30）

> §7.1–7.5 是**世代 5**（07-29～30 凌晨）；§7.6–7.7 是**世代 6**（07-30 重跑）。
> 分界點就是 §7.6 那場事故——它讓 §7.4 的數字作廢，也讓 §7.7 的唇型合併
> 必須在修好之後才量得準。

### 7.1 先量雜訊底線

同一組設定（40 epochs / 3e-4）完整重跑一次，只有隨機性不同：

| 部位 | 第一次 | 第二次 | 差 |
| --- | --- | --- | --- |
| face_shape | 0.516 | 0.500 | −0.016 |
| nose_shape | 0.881 | 0.866 | −0.015 |
| lip_shape | 0.492 | 0.504 | +0.012 |
| eye_shape | 0.402 | 0.411 | +0.010 |
| brow_shape | 0.521 | 0.516 | −0.005 |

> **雜訊底線 ±0.016。** 本文件所有結論都以這個為尺：差距小於它就是「沒有差別」，
> 不管數字看起來多誘人。這一步應該更早做。

### 7.2 超參數：原本是訓練不足

| 設定 | face | brow | eye | nose | lip | 平均 |
| --- | --- | --- | --- | --- | --- | --- |
| 基準 20ep / 3e-4 | 0.470 | 0.490 | 0.388 | 0.827 | 0.485 | 0.532 |
| **40ep / 6e-4** | **0.520** | 0.514 | **0.434** | 0.867 | **0.497** | **0.566** (+0.034) |
| **40ep / 3e-4** | 0.516 | **0.521** | 0.402 | **0.881** | 0.492 | 0.562 (+0.030) |
| 40ep / 1e-4 | 0.487 | 0.502 | 0.387 | 0.833 | 0.473 | 0.537 (+0.004) |
| 20ep / 1e-4 | 0.443 | 0.482 | 0.378 | 0.809 | 0.458 | 0.514 (−0.018) |

**兩個因素乾淨分離：** epochs 20→40 一律變好；lr 往下調一律變差。

原本的假設是「val 震盪代表學習率太高」，往下調結果全面變差——**真相是訓練不足**，
20 epochs 還沒收斂就停了，lr 太低讓收斂更慢，等於錯上加錯。

> **那組反向對照（6e-4）救了這個判斷。** 只試假設的方向，會停在「調低沒用」然後放棄。
> 每次調參都要有一組往相反方向的對照。

⚠️ 但 6e-4 與 3e-4 只差 0.004，**在雜訊內**。結論只能是「40 epochs 有效，學習率調哪個都差不多」。

> **⛔ 這句話在 2026-07-30 被推翻了，見 §7.9。**
> 修好 identity map 後重跑，6e-4 在**五個部位全部**贏 3e-4、25 折裡贏 18 折。
> 當初判成「在雜訊內」，是因為只看了**五個部位的平均**這一個數字——
> 平均會把方向一致的小幅度稀釋掉。**該看的是符號一致性，不是平均差。**

### 7.3 Hybrid 門檻：無效，而且原因有價值

`tools/tune_hybrid.py`：CNN 信心夠高就用 CNN，否則退回規則式。

**CNN = 40ep / 3e-4：**

| 部位 | 規則式 | CNN | hybrid | 門檻 | 採用 CNN |
| --- | --- | --- | --- | --- | --- |
| face_shape | 0.360 | 0.404 | 0.404 | 0.00 | 100% |
| brow_shape | 0.513 | 0.465 | 0.491 | 0.46 | 93% |
| eye_shape | 0.495 | 0.429 | 0.458 | 0.38 | 80% |
| nose_shape | 0.814 | **0.913** | 0.883 | 0.70 | 69% |
| lip_shape | 0.295 | **0.469** | 0.391 | 0.72 | 38% |

**CNN = 40ep / 6e-4：** 五個部位挑出來的門檻**全部是 0.00**，
也就是演算法的結論是「無論 CNN 多沒把握都別聽規則式」。

**hybrid 沒有一項贏過純 CNN**，nose 0.913 → 0.883、lip 0.469 → 0.391，都是被拉低。

> **原因：兩者錯的是同一批樣本。** hybrid 假設「CNN 沒把握時規則式會答對」，
> 但 CNN 沒把握的正是界線模糊的臉，規則式在那些臉上一樣分不開。
> 混進來只是用更差的答案取代了不確定但仍較準的答案。
>
> **這同時說明了三模型投票為什麼不值得做**——模型之間不夠互補。

### 7.4 桃杏眼：使用者定的分類比演算法的合併更好

2026-07-30 使用者決定：**杏仁眼 + 桃花眼 → 桃杏眼**（圓眼保持獨立），眼型五類。

| 分類方式 | 類別 | 亂猜 | CNN | DINOv2 | 最佳 | 比亂猜好 |
| --- | --- | --- | --- | --- | --- | --- |
| 原始 | 6 | 0.17 | 0.388 | 0.473 | 0.473 | +0.303 |
| `--merge-eye`（併進圓眼） | 4 | 0.25 | 0.523 | — | 0.523 | +0.273 |
| ~~桃杏眼（07-30 01:06 版）~~ | 5 | 0.20 | ~~0.466~~ | ~~0.556~~ | ~~0.556~~ | — |
| **桃杏眼（07-30 重跑，可信）** | **5** | 0.20 | 0.463 | **0.542** | **0.542** | **+0.342** |

⚠️ **劃掉的那一列是壞的**：那次執行時 identity map 已經過期，桃杏眼 221 張裡只有 1 張
進過驗證集，而那 1 張剛好答對，於是這個類別的 recall 被記成 **1.000**——
macro 五分之一的權重是一張圖撐出來的。詳見 §7.6。

**重跑後桃杏眼真實 recall = 0.633（221 張全部驗過），仍然是眼型最高的一類。**
巧的是 macro 總分幾乎沒變（0.466 → 0.463、0.556 → 0.542），
**但「幾乎沒變」是運氣，不是驗證**。

**桃杏眼仍是眼型有史以來最好的數字，而且比 4 類版本多保留一個類別。**

`--merge-eye` 把杏仁＋桃花都塞進圓眼，等於用「圓眼」代表三種不同的眼睛；
獨立成「桃杏眼」讓模型要學的邊界更乾淨，圓眼也保住自己的定義。

**剔除的標註衝突從 57 組歸零**——那些「同一張臉標成杏仁眼又標成桃花眼」的樣本
不再是衝突，它們現在都是桃杏眼。

### 7.5 DINOv2 首次出現站得住的領先

```
eye_shape (桃杏眼 5 類)  ← 07-30 重跑後的可信值
  CNN         0.463 ± 0.033
  DINOv2+SVM  0.542 ± 0.042      領先 +0.079
                                 （07-29 舊值：0.466 vs 0.556，領先 +0.090）
```

**+0.079 遠超過雜訊底線 ±0.016**，是這個專案所有「模型 A vs 模型 B」比較裡
第一個明確站得住的差距。先前那些全在一個標準差內。

**這個結論是少數在 §7.6 事故後倖存下來的**——幅度從 +0.090 縮到 +0.079，方向沒變。

> **⚠️ 但這是拿沒調好的 CNN 比的。** §7.9 把 CNN 換成 40ep/**6e-4** 之後：
>
> ```
> eye_shape   CNN 6e-4     0.495 ± 0.028
>             DINOv2+SVM   0.542 ± 0.042     領先只剩 +0.047（4/5 折）
> ```
>
> **+0.047 已經落回 std（0.042）的範圍內**，而且其中一折就貢獻了 +0.114。
> DINOv2 在眼型仍是最好的選擇，但「第一個明確站得住的差距」這個說法**要收回**——
> 它站得住是因為對手還沒調參。
>
> **教訓：宣告 A 比 B 好之前，先確認 B 已經調到它的最佳狀態。**
> 否則量到的是「A 比沒調好的 B 好」，那不是同一件事。

其餘四個部位重跑後的對照（同一組 5-fold）：

| 部位 | CNN (40ep) | DINOv2 最佳 | 差距 | 判定 |
| --- | --- | --- | --- | --- |
| eye_shape | 0.463 | **0.542** (SVM) | +0.079 | **DINOv2 勝**（> std 0.042）|
| face_shape | **0.496** | 0.424 (SVM) | +0.072 | **CNN 勝** |
| lip_shape | **0.560** | 0.513 (LogReg) | +0.047 | CNN 勝（略大於 std 0.035）|
| brow_shape | 0.517 | 0.543 (LogReg) | +0.026 | 在 std 0.074 內，不下定論 |
| nose_shape | 0.845 | 0.846 (SVM) | +0.001 | 平手 |

> **沒有哪一個模型全面勝出。** 眼型該用 DINOv2、臉型與唇型該用 CNN，
> 眉型與鼻型隨便。這也是為什麼 §7.3 的 hybrid 投票沒有用——
> 互補性存在於「部位之間」，不存在於「同一個部位的兩個模型之間」。

### 7.6 事故：搬檔案讓合併後的類別退出驗證集

**這是本專案第二次踩同一個坑**（第一次是 §5.4 的寬鼻），而且這次更隱蔽。

#### 發生了什麼

```
07-29 16:47   建 identity_map.json —— 鍵是「照片的絕對路徑」
07-30 00:31   杏仁眼/桃花眼 的檔案「實體搬進」桃杏眼 資料夾 → 路徑變了
07-30 01:43   M型唇 的檔案實體搬進 花瓣唇 → 路徑變了
07-30 01:06+  在這之後跑的每一次 CV
```

`prepare_roi_cache.py` 查不到路徑就給 `identity = -1`，而 `-1` 的語意是
**「聚類失敗，一律留在 train，永不進任何 fold 的 val」**——那個設計本身是對的
（不知道是誰的照片，就不能拿它當考題）。錯的是**搬完檔案沒有重建身分聚類**。

結果：

| 部位 | 類別 | 總張數 | 能進 val | 實際發生的事 |
| --- | --- | --- | --- | --- |
| eye_shape | **桃杏眼** | 221 | **1 張** | 那 1 張答對 → recall 記成 **1.000** |
| lip_shape | **花瓣唇** | 225 | 131 張 | 只有原花瓣唇進 val，併進來的 M型唇全在 train |

全資料集共 **315 筆**的 identity 掉成 `-1`。

#### 為什麼沒被發現

1. **不報錯。** `-1` 是合法值，訓練照跑、ONNX 照匯出、分數照印。
2. **分數看起來很合理。** 桃杏眼 macro 0.466，跟 6 類的 0.388 相比是漂亮的進步，
   而且方向符合「合併有效」的預期——**符合預期的數字最不會被檢查**。
3. **總分幾乎沒變。** 重跑後 0.466 → 0.463。如果當時有人抽查總分，會覺得沒問題。
   真正壞掉的是**單一類別的 recall**（1.000 → 0.633），不是總分。

#### 修法與驗證

```bash
# 1) 重建身分聚類（搬過檔案就必須做）
.\.venv\Scripts\python.exe -X utf8 tools/build_identity_map.py
# 2) 重建 ROI 快取
.\.venv\Scripts\python.exe -X utf8 prepare_roi_cache.py
```

修完的驗證數字：`identity == -1` 由 **315 筆降到 2 筆**（那 2 張是真的偵測不到臉），
桃杏眼 **221/221 全部可進 val**。

#### 應該加的防線（尚未實作）

`prepare_roi_cache.py` 現在只在**找不到 identity_map 檔案**時警告。它應該進一步：

```
若 identity == -1 的比例超過某個門檻（例如 5%）→ 大聲失敗，不要只印一行
```

理由見 §9.4：**這個專案的災難全部是靜默降級**，而 `-1` 的比例是這件事唯一的體溫計。
DINOv2 的 embedding 快取已經有類似守衛（比對筆數，見 `dinov2_cv_experiment.py`），
identity map 沒有。

> **另外一件事**：embedding 快取的守衛只比對**筆數**，不比對內容或順序。
> 這次重建 ROI 快取後筆數不變（2311），順序也確實沒變（已逐筆比對過路徑），
> 所以沿用舊 embedding 是安全的——**但那是驗證過才知道的，不是守衛保證的**。
> 若哪天資料增減剛好抵銷成同一個筆數，這道守衛會直接放行。

### 7.7 唇型合併：第二個明確有效的類別調整

2026-07-30：**M型唇 → 花瓣唇**，唇型五類降為四類。

| 分類方式 | 類別 | 亂猜 | CNN (40ep) | 比亂猜好 |
| --- | --- | --- | --- | --- |
| 原始 | 5 | 0.20 | 0.492 | +0.292 |
| **M型唇併入花瓣唇** | **4** | 0.25 | **0.560 ± 0.070** | **+0.310** |

**+0.068，遠大於雜訊底線 ±0.016。**

判斷依據跟眼型那次一樣，是看混淆矩陣而不是看總分：

```
M型唇 recall 0.296 —— 108 張裡只有 32 張判對
錯的去哪裡：微笑唇 23、花瓣唇 21、厚唇 15、薄唇 17   ← 均勻散開
（來源：models/basic_features_roi/cv_summary_hp_ep40_lr3e4.json 的 pooled 混淆矩陣）
```

**「均勻散開」跟「集中錯到某一類」是兩種完全不同的病。**
集中錯代表兩類該合併；均勻散開代表**這一類沒有模型看得到的特徵**——
M型唇與花瓣唇的共同點是唇峰明顯，而兩者的差異在照片解析度下分不出來。

⚠️ **但要誠實記一筆：矩陣本身並沒有指向花瓣唇。** M型唇最常被認成的是
**微笑唇（23 張）**，花瓣唇只排第二（21 張）。選花瓣唇當合併對象是**依據
唇形定義（唇峰）做的領域判斷**，不是從混淆矩陣讀出來的。
結果證明有效（+0.068），但這是「領域知識贏過統計」的案例，
不能反過來說「看矩陣就會知道要併哪兩類」。

合併後四類的 recall（07-30 重跑）：

```
厚唇   0.652
花瓣唇 0.612
薄唇   0.585
微笑唇 0.394   ← 現在最弱的一類
```

> **下一個候選是微笑唇。** 它的 recall 0.394 是四類中唯一低於 0.5 的。
> 但先別急著合併——微笑唇是**唯一與表情有關**的類別，
> 它可能該修的是拍攝規範（請使用者不要笑），不是分類表。

### 7.8 下游沒跟上分類表（2026-07-30 已修）

模型端已經全部對齊（`*_classes.json`、ONNX、規則樹、DINOv2 head 都是 5 類），
`analysis_package.py` 的 `EYE_SHAPE_CODES` 也有 `桃杏眼 → peach_almond`。
**但再往下的消費端沒跟上。**

#### 實測到的三個症狀

用真實路徑重現（`normalize_face_analysis` → 取 `eyeShape` 代碼 → `_map_or_raw` → `EYE_LOGIC`）：

| 檔案 | 位置 | 症狀 |
| --- | --- | --- |
| `Ollama_suggestion.py` | `MAP_EYE` | 沒有 `peach_almond` → **英文代碼直接寫進中文 prompt** |
| `Ollama_suggestion.py` | `EYE_LOGIC` | 沒有 `桃杏眼` → 描述詞是空字串 |
| `replicate_render.py` | `FACE_TERM_EN` | 沒有 `桃杏眼` → 妝後圖 prompt **整句眼型描述被略過** |

修復前，餵給建議模型的 prompt 實際長這樣：

```
眼型：peach_almond（）
```

> ⚠️ **原本這一節寫「會落到 unknown → 未知眼型」，那是錯的。**
> `_map_or_raw` 查不到是**原樣回傳**，不是回傳 unknown——所以外洩的是英文代碼本身。
> 「未知眼型」至少還是句中文；`peach_almond` 是把一個 enum 丟給中文語言模型。
> **推測失敗模式跟實測失敗模式是兩回事，這一節原本只做了前者。**

#### 順帶抓到的：鳳眼被顯示成已淘汰的丹鳳眼

同一張 `MAP_EYE` 裡：

```
analysis_package:  鳳眼 → "phoenix"
Ollama_suggestion: "phoenix" → 丹鳳眼        ← 2026-07-24 就併進鳳眼了
```

**每一次鳳眼的分析結果，繞一圈回來都變成「丹鳳眼」**——一個不在現行分類表、
前端回饋選項也沒有的名字。這個 bug 比桃杏眼那個早六天，一直沒被發現，
因為它**不會空白、不會外洩英文**，只是名字不對。

#### 修法：現行分類表是唯一基準，舊值一進來就換掉

走過兩個錯誤的版本才收斂：

```
第一版  把已淘汰的名稱也加進下游對照表
        → 錯。等於讓合併掉的類別在下游復活，每次合併都要記得補三張表。

第二版  下游只留現行標籤，但保留舊「代碼」（almond、peach_blossom…）
        → 仍然不夠。代碼也是類別的一種寫法，留著就是留著。

定案    每一張表只列現行類別；所有舊值在入口一次換成現行名稱。
```

**唯一保留舊名稱的地方是那張換算表本身**——它的職責就是「改成現行」，
不是「保留舊類別」。

```
analysis_package.py   LABEL_ALIASES（中文舊名 → 中文現行名）+ canonical_label()
                      內容與 prepare_roi_cache.LABEL_ALIASES 一致（同一組合併決定）
                      _code() 查表前先呼叫 canonical_label
                      *_CODES 五張表移除所有別名與非現行類別
                      （含 FACE_SHAPE_CODES 裡從未使用的 菱形臉／梯形臉）

face_corrections.py   apply() 改成 result[field] = canonical_label(value)
                      ← 舊標籤的另一個入口：快取存的是使用者當初送出的值，
                        原本不比對分類表，會把 M型唇 原樣寫回結果

Ollama_suggestion.py  MAP_EYE / EYE_LOGIC   只留現行五類
                      MAP_NOSE / NOSE_LOGIC 只留 標準鼻／寬鼻
replicate_render.py   FACE_TERM_EN          只留現行類別
```

#### 順帶抓到的第三個：鼻型顯示成分類表裡沒有的名字

`MAP_NOSE` 原本是：

```
standard → 直鼻      ← 現行分類表叫「標準鼻」
narrow   → 短鼻      ← 「窄鼻」2026-07-24 就併進標準鼻了，而且「短鼻」從來不是類別
wide     → 寬鼻      ← 只有這個是對的
```

**每一次鼻型結果都顯示成「直鼻」**，而使用者在回饋面板上只看得到「標準鼻／寬鼻」——
他想修正也選不到自己看到的那個名字。跟 §7.8 的鳳眼是同一個病。

#### 驗證（走真實路徑，五個部位全掃）

```
1. 每張表只含現行類別
   analysis_package 五張 CODES、Ollama 五張 MAP、五張 LOGIC、
   replicate_render.FACE_TERM_EN            —— 全部乾淨

2. 每個現行類別都查得到
   臉型/眉型/眼型/鼻型/唇型 × 代碼/顯示/描述/渲染 —— 全部 ✓

3. 舊值換算（走 _code 真實入口）
   丹鳳眼→鳳眼  瞇縫眼→細長眼  杏仁眼→桃杏眼  桃花眼→桃杏眼
   窄鼻→標準鼻  蒜頭鼻→寬鼻   M型唇→花瓣唇          —— 全部 OK

4. 端到端 prompt
   正常輸入      眼型：桃杏眼（眼型柔和明亮）、鼻型：標準鼻（鼻型比例勻稱）
   舊標籤輸入    杏仁眼/M型唇/窄鼻 → 輸出與正常輸入完全相同
```

**舊標籤輸入與正常輸入產生一模一樣的 prompt**，就是「改成現行」該有的樣子。

#### 這件事的通則

**`basic_roi_shadow.py` 的 `_retired_labels` 守衛只看了一個方向。**
它擋得住「模型吐出已淘汰類別」，擋不住「下游不認識新類別」。
兩者是同一個問題的兩半，而目前只有一半有守衛。

> 這次把「舊名稱」收斂到一張 `LABEL_ALIASES`，少了三處要同步的地方。
> 但**現行類別本身仍然抄在多處**（`prepare_roi_cache.CANONICAL_LABELS`、
> `*_classes.json`、`analysis_package.*_CODES`、`Ollama_suggestion.MAP_*`、
> `replicate_render.FACE_TERM_EN`）。下一步應該讓服務端從 `*_classes.json` 讀，
> **每多一份，就多一個會悄悄過期的地方。**

### 7.9 完整方格：十一個變體同組 fold 對決

2026-07-30，在修好的 identity map 上把**所有**訓練變體各跑一次，同一組 5-fold。
原始檔存在 `models/basic_features_roi/g6/`。

| 實驗 | face | brow | eye | nose | lip |
| --- | --- | --- | --- | --- | --- |
| **CNN mobilenet 40ep/6e-4** | **0.522** | 0.542 | 0.495 | **0.868** | **0.565** |
| CNN mobilenet 40ep/3e-4 | 0.496 | 0.517 | 0.463 | 0.845 | 0.560 |
| DINOv2 + SVM | 0.424 | 0.531 | **0.542** | 0.846 | 0.511 |
| DINOv2 + LogReg | 0.417 | **0.543** | 0.512 | 0.843 | 0.513 |
| CNN simple（無預訓練）40ep | 0.409 | 0.521 | 0.503 | 0.804 | 0.544 |
| 幾何規則（threshold）| — | 0.448 | — | 0.782 | — |
| 幾何規則（tree）| 0.462 | 0.424 | 0.428 | 0.730 | 0.422 |
| CNN 臉輪廓遮罩 224 | 0.475 | — | — | — | — |
| CNN 臉輪廓遮罩 128 | 0.461 | — | — | — | — |
| CNN 部位形狀遮罩 | — | 0.447 | — | — | 0.468 |
| DINOv2 臉輪廓 + SVM | 0.435 | — | — | — | — |
| 階層式決策樹（`cascade_face_shape.py`）| 0.398 | — | — | — | — |

#### 7.9.1 學習率 6e-4 勝，而且這次站得住

**沒有任何單一部位的領先超過自己的 std**——但方向一致性遠比單項幅度有力：

```
逐折配對（同一組 fold，25 次比較）
  face  +0.059 −0.021 +0.050 +0.031 +0.010    4/5
  brow  +0.009 −0.002 +0.079 +0.033 +0.008    4/5
  eye   +0.047 +0.095 +0.066 −0.025 −0.024    3/5
  nose  +0.022 +0.000 +0.036 +0.019 +0.037    4/5
  lip   +0.023 +0.022 −0.029 −0.004 +0.008    3/5

  合計 18/25 折勝出   平均 +0.0220   平均差 / 標準誤 = 3.39
  五個部位的平均差全部為正（5/5）
```

> **這是本文件第一次靠「一致性」而不是「幅度」下結論。**
> 五個部位是五個不同的資料集、不同的類別數、不同的難度；
> 它們**同時**往同一個方向動，比任何單一部位贏 0.05 都更難用運氣解釋。
>
> ⚠️ 誠實標記：25 折之間**不是完全獨立**（同一批身分反覆出現），
> 所以 3.39 這個值偏樂觀，不該當成正式的 p 值。真正撐住結論的是 5/5 的符號一致。

#### 7.9.2 三條死路，各自的死法不同

| 變體 | 結果 | 為什麼失敗 |
| --- | --- | --- |
| 輪廓二值遮罩（臉 128／224）| 0.461／0.475 vs RGB 0.522 | 丟掉的資訊比雜訊多。臉型不只看外框，還看下頜線的**陰影與轉折**，二值化把它抹平了。224 比 128 好一點（+0.014），代表**問題不是解析度** |
| 部位形狀遮罩（眉／唇）| 0.447／0.468 vs RGB 0.542／0.565 | 同上，而且更嚴重。眉型的關鍵是**眉尾走向與濃淡**，唇型是**唇峰的圓鈍**，兩者都在灰階裡 |
| 階層式臉型 | 0.398 → 修正後 0.435，仍輸扁平 CNN 0.522 | 早期層**搶著開火**把後面的類別吃光（詳見下方）|

##### 階層式：診斷錯了一次，修好之後仍然不該上線

第一版的判斷是「錯誤往下傳遞，第四層圓vs方只有 0.442，拖垮前面」。
**這個說法只對了一半，而且指錯了原因。**

該看的指標不是每層的 accuracy，是 **precision**。1-vs-rest 且類別比約 1:4 時，
accuracy 會被多數類撐高，看不出**誤抓**——而誤抓是階層式唯一致命的錯誤：
被前面的層抓走就鎖死了，後面再準也救不回來。改印 precision 才看到：

```
長形臉層  precision 0.515   誤吃 心形臉 38、鵝蛋臉 28
心形臉層  precision 0.257   誤吃 鵝蛋臉 42、圓形臉 34
鵝蛋臉層  precision 0.300
```

**鵝蛋臉 recall 0.171 不是因為第三層分不出來，
是因為它 105 個樣本裡有 70 個在輪到它之前就被前兩層吃掉了。**

病因是 `class_weight="balanced"`。一般分類任務要它，
**階層式正好相反——早期層要高 precision，不確定就該放行給下一層。**

| 版本 | macro | 鵝蛋臉 recall |
| --- | --- | --- |
| 原版（predict + balanced）| 0.398 | 0.171 |
| **拿掉 balanced** | **0.435** | **0.438** |
| 門檻 0.8 + balanced | 0.428 | 0.495 |
| 門檻在訓練折內選 + 不 balanced | 0.437 | 0.438 |

也試過 `predict_proba` 加高門檻，並用**巢狀切分在訓練折內**選門檻（不碰 val，§9.5）。
五折全部選到 0.5——**門檻沒有額外幫助，真正的問題就是 `class_weight`。**

修好之後：

```
階層式（修正後）  0.437 ± 0.032
扁平決策樹        0.462
CNN 40ep/6e-4     0.522   ← 臉型的正式答案來源
```

> **假設合理、實作有 bug、修好之後假設仍然不成立。** 三件事要分開講。
> 「每層只用該層需要的特徵」省下來的好處，補不回錯誤往下傳遞的代價。
>
> **教訓：說一個方法「行不通」之前，先確認量到的是方法本身，而不是實作的 bug。**
> 第一次量到 0.398 就下結論，會同時錯過「+0.039 的實作 bug」與「真正的失敗原因」。

#### 7.9.3 無預訓練的 simple_cnn 在眼型贏過 mobilenet

```
eye_shape   simple_cnn 0.503 ± 0.031   vs   mobilenet 6e-4 0.495 ± 0.028
```

差距在雜訊內，但值得記一筆：**眼型是唯一一個「ImageNet 預訓練沒有幫助」的部位。**
其餘四個部位 simple_cnn 都明顯落後（臉型 0.409 vs 0.522 差最多）。

可能的解釋是眼型 ROI 只有 96×96 且高度同質（都是一雙眼睛），
ImageNet 那套「分辨貓狗車」的低階特徵派不上用場。**沒有驗證，只是假設。**

### 7.10 結論：目前的最好組合

把 §7.9 的方格逐部位挑冠軍，再用逐折配對確認差距是否撐得住：

| 部位 | 選誰 | macro | 次佳 | 領先 | 撐得住嗎 |
| --- | --- | --- | --- | --- | --- |
| face_shape | **CNN 40ep/6e-4** | 0.522 ± 0.043 | CNN 3e-4 0.496 | +0.026 | DINOv2 輸 **0/5 折**、−0.10，這一格沒有懸念 |
| brow_shape | **CNN 40ep/6e-4** | 0.542 ± 0.043 | DINOv2+LogReg 0.543 | −0.001 | **真的平手**（逐折 +0.0015、3/5）→ 選 CNN 是為了少維護一條路徑 |
| eye_shape | **DINOv2 + SVM** | 0.542 ± 0.042 | CNN 6e-4 0.495 | +0.047 | 4/5 折，但落在 std 內（見 §7.5 的收回）。仍是最佳選擇 |
| nose_shape | **CNN 40ep/6e-4** | 0.868 ± 0.030 | DINOv2+SVM 0.846 | +0.022 | DINOv2 只勝 2/5 折 |
| lip_shape | **CNN 40ep/6e-4** | 0.565 ± 0.056 | CNN 3e-4 0.560 | +0.005 | DINOv2 勝 1–2/5 折，明顯落後 |

**一句話：四個部位用 CNN(mobilenet, 40ep, 6e-4)，眼型用 DINOv2+SVM。**

與 07-29 的部署決定相比，變的是：

```
CNN 學習率   3e-4 → 6e-4        五個部位全部受益（§7.9.1）
眼型         CNN  → DINOv2+SVM   +0.047（原本 shadow-only，見 §11.2）
臉型         規則樹 → CNN         0.462 → 0.522（2026-07-30 已套用）
```

**兩項都已套用**（2026-07-30）：`basic_rule_trees.RULE_TREE_PARTS` 由
`("eye_shape", "face_shape")` 清成 `()`，臉型 CNN 以 40ep/6e-4 重訓並重新匯出。

### 7.11 幾何決策樹全面退場，fallback 一律落在 CNN

`RULE_TREE_PARTS` 現在是空的，這個模組不再提供任何正式答案。分兩步做完：

```
臉型  樹 0.462  vs  CNN 0.522   → 改由 CNN 提供正式答案
眼型  樹 0.428  vs  CNN 0.495   → 正式答案是 DINOv2 0.542，fallback 改成 CNN
```

眼型原本把樹留著當「DINOv2 失效時的中間層」，**但那是降級**——
樹在眼型也輸 CNN 0.067，故障時退回樹等於在已經出事的情況下再扣一次分。

實測（同一張圖，關掉 DINOv2 模擬故障）：

```
舊 fallback（樹）   IMG_3531 眼型 = 下垂眼   ← 與正常答案不同
新 fallback（CNN）  IMG_3531 眼型 = 桃杏眼   ← 與正常答案一致
```

**降級應該是平滑的：主要路徑掛掉時，答案要盡量接近它本來會給的。**
退回一個系統性更差、而且傾向給出不同答案的模型，會讓故障同時變成
「準確度下降」與「答案不一致」兩件事，而使用者兩件都看不到。

模組保留不刪：它是規則式的完整實作，也是「規則式比較穩、比較可解釋」
這個錯覺的紀錄。要回頭做對照，把部位加回 `RULE_TREE_PARTS` 即可。

> **從世代 0 到這裡，規則式走了完整一圈**：唯一答案 → 與 CNN 對半分 →
> 只剩兩個部位 → 只剩 fallback → 完全退場。
> 每一次縮減都是同一個原因：**它的門檻也是從資料擬合出來的，
> 只是擬合過程不叫訓練，所以沒有人把它當成會過期的東西看。**

**沒有變的：幾何規則式在五個部位仍然全部墊底**，這是本專案量過最多次、最穩定的結論。

⚠️ **眼型改用 DINOv2 是產品決定，不只是技術決定。** DINOv2 佔整個分析 **57% 的時間**
（§11.2），目前是抽樣跑的 shadow mode。要讓它接管眼型的正式答案，得先確認
Cloud Run 的延遲吃得下——**這一項本文件不做決定**。

> 若不想承擔那個延遲，眼型退回 CNN 6e-4 的代價是 **−0.047**，
> 而且落在 std 內。以本專案的標準（§9.2：領先要大於 std 才算勝），
> **「眼型也用 CNN」是完全可辯護的選擇。**

---

## 八、全部數據總表

同一組 5-fold identity 切分。**`per_image` 那一列不可採信**（重複檔造成的假象）。

### 8.1 世代 4–5（2026-07-29，眼型 6 類／唇型 5 類）

| 實驗 | face | brow | eye | nose | lip |
| --- | --- | --- | --- | --- | --- |
| 基準（mobilenet + cluster） | 0.470 | 0.490 | 0.388 | **0.827** | **0.485** |
| **眼型合併 4 類** | — | — | **0.523** | — | — |
| DINOv2 + LogReg/SVM | 0.424 | **0.543** | **0.473** | **0.846** | 0.446 |
| 幾何決策樹（重新校準後） | 0.445 | 0.448 | 0.379 | 0.782 | 0.336 |
| 剔除衝突樣本 | 0.419 | 0.452 | 0.371 | 0.675 | 0.446 |
| ~~per_image 切分~~ | ~~0.470~~ | ~~0.667~~ | ~~0.283~~ | ~~0.828~~ | ~~0.451~~ |
| 輪廓遮罩輸入 | 0.391 | 0.455 | — | — | 0.303 |
| simple_cnn（無預訓練） | 0.354 | 0.485 | 0.289 | 0.521 | 0.410 |
| 40ep / 3e-4（最佳超參數）| 0.516 | 0.521 | 0.402 | **0.881** | 0.492 |

**除了眼型合併，沒有任何改動明確超越基準。**

### 8.2 世代 6（2026-07-30 重跑，眼型桃杏眼 5 類／唇型 4 類）

**identity map 修好後的可信值。** 全部 40 epochs / cluster / 同一組 5-fold，
學習率如各列標示（**6e-4 是最佳，見 §7.9.1**）。

| 實驗 | face | brow | eye | nose | lip |
| --- | --- | --- | --- | --- | --- |
| **CNN (40ep/6e-4)** ← 最佳 | **0.522** | 0.542 | 0.495 | **0.868** | **0.565** |
| CNN (40ep/3e-4) | 0.496 | 0.517 | 0.463 | 0.845 | 0.560 |
| DINOv2 + LogReg | 0.417 | **0.543** | 0.512 | 0.843 | 0.513 |
| DINOv2 + SVM | 0.424 | 0.531 | **0.542** | 0.846 | 0.511 |
| CNN simple（無預訓練）| 0.409 | 0.521 | 0.503 | 0.804 | 0.544 |
| 幾何規則（重新校準後最佳）| 0.462 | 0.448 | 0.428 | 0.782 | 0.422 |
| 輪廓／形狀遮罩 | 0.475 | 0.447 | — | — | 0.468 |
| 階層式決策樹（臉型）| 0.398 | — | — | — | — |
| **各部位最佳** | **0.522** | **0.543** | **0.542** | **0.868** | **0.565** |
| ±std（最佳者）| 0.043 | 0.074 | 0.042 | 0.030 | 0.056 |

完整方格（11 個變體）與逐折配對分析見 §7.9；部署建議見 §7.10。

**與 8.1 對照，整個專案至今唯一站得住的兩個進步都是合併類別：**

```
eye  0.473 → 0.542   +0.069   桃杏眼（6 類 → 5 類）
lip  0.492 → 0.560   +0.068   M型唇併入花瓣唇（5 類 → 4 類）
```

另外兩個部位往下走，要分開講：

```
face  0.516 → 0.496   −0.020   約等於雜訊底線的 1.3 倍，可以當成沒差別
nose  0.881 → 0.845   −0.036   超過雜訊底線的兩倍，不能當成沒差別
```

**鼻型那 −0.036 目前無法歸因。** 兩次執行之間動了兩件事（重建身分聚類、
重建 ROI 快取），沒有做單一變數對照，所以不知道是哪一個造成的。

逐 fold 看，差距其實集中在一折：

```
07-29  0.867, 0.833, 0.872, 0.944, 0.888    平均 0.881 ± 0.036
07-30  0.797, 0.870, 0.817, 0.889, 0.851    平均 0.845 ± 0.034
```

07-29 有一折衝到 **0.944**，07-30 沒有；兩輪的 ±std 都在 0.035 上下，
也就是**這個部位本來就會在 0.80–0.94 之間晃**。鼻型只有 269 張、2 類，
每折 val 約 54 張，一張就值將近 2 個百分點。

> 身分聚類重建後，鼻型有 1 張從 val 退出（`identity=-1`，真的偵測不到臉），
> 而且 fold 的成員組成整批換過——**這足以解釋一折的落差，但我沒有證明它就是原因。**
> 要下結論就得跑對照（固定快取只換種子），不要在這裡猜。

規則式仍然五個部位全部墊底，跟 §6.4 一致。

---

## 九、方法論：這個專案學到的教訓

這一節是本文件最該被讀的部分。以下每一條都是**踩過才寫下來的**。

### 9.1 先確認資料，再談模型

世代 3 花了整輪在清資料，沒有產出新模型——但在那之前的所有分數都是假的。
314 個重複檔、過期的 identity map、塌陷的窄鼻類別，任何一個都足以讓結論反向。

### 9.2 「分數變好」要先問「雜訊有多大」

世代 2 的 val 只有 43–105 張，雜訊 ±0.1。在那個規模下，
**0.53 vs 0.47 沒有意義**。5-fold 就是為了把這件事量出來。

判斷標準：**領先幅度要大於標準差**。今天量到的差距多數不滿足，所以結論是
「不下定論」而不是「A 比 B 好」。

### 9.3 分數低不一定是模型的錯

四次遇到「類別定義本身有問題」：

1. 窄鼻的鼻翼比標準鼻還寬 → 類別塌陷
2. 丹鳳眼與鳳眼視覺上無法區分 → 大量跨類重複
3. 杏仁眼／桃花眼／圓眼的界線不存在 → 併成桃杏眼後 **+0.069**
4. M型唇 recall 0.296、錯的均勻散開 → 併入花瓣唇後 **+0.068**（§7.7）

**在調模型之前，先看混淆矩陣裡哪兩類在互相打架。**

但也要記得 §7.7 那個反例：**矩陣告訴你「這一類有問題」，不會告訴你「該併去哪裡」。**
M型唇最常被認成微笑唇，正確的合併對象卻是花瓣唇——那一步靠的是唇形定義。

### 9.3b 每一種「合併類別」的改動，都要重建身分聚類

§7.6 的事故：合併類別如果是**實體搬檔**，identity map 的路徑鍵就全部失效，
被搬動的那一類會安靜地退出驗證集。**合併類別與重建聚類是同一個動作的兩半，
不是兩件事。**

判斷方法只有一個數字：

```
identity == -1 的比例    正常 < 0.1%（2026-07-30 是 2/2311）
                         出事 13.6%（事故當時是 315/2311）
```

**每次重建快取後看這個數字，比看 macro accuracy 重要。**

### 9.4 靜默降級比報錯危險

本專案出現過至少**六**次：

- 幾何門檻是死碼 → 某些類別永遠不出現，不報錯
- DINOv2 embedding 快取過期 → 差一點就拿 A 圖的特徵配 B 圖的標籤，
  **這次剛好越界才爆掉；沒越界的話整份結果全錯而沒有任何徵兆**
- 建議服務掛掉 → 渲染安靜退回罐頭 prompt，「妝變得比較泛用」而已
- 收藏的妝前圖檢查過嚴 → 每一筆都在送出前被擋掉，畫面正常、資料庫一筆都沒進
- **identity map 過期 → 合併後的類別整批退出驗證集，recall 記成 1.000（§7.6）**
- **DINOv2 head 寫到另一個目錄 → 差點拿舊 head 上線，兩個目錄名字很像（§十 第 7 步）**
- **下游的中文對照表沒跟上分類表 → 英文 enum 直接寫進中文 prompt；
  鳳眼被顯示成已淘汰的丹鳳眼長達六天（§7.8，已修）**
- **`dinov2_cv_experiment.py` 把 `per_image` 寫死在輸出檔名裡 → cluster 模式跑出的
  可信結果被貼上「不可採信」的檔名（§7.9，已修）**

**修法一律是：讓它大聲失敗。** 現在快取會對筆數、服務不可用會丟例外。

**但 §7.6 那一項還沒修。** `identity == -1` 的比例目前只是印出來，沒有門檻、
不會擋。它已經害過一次，下次還會。

**§7.8 那一項是修了症狀，沒修病因。** 全專案有五份各自維護的眼型清單，
這次補齊了，下次改分類表還是會有人漏掉其中一份。
**病因是「同一個事實抄了五份」，不是「有人忘了改」。**

### 9.5 不要用驗證集挑東西

本專案犯過一次：用 val 選決策樹深度，分數虛高 0.018。
`tools/tune_hybrid.py` 的檔頭把這條寫成鐵則。

### 9.6 預設的資料增強可能是錯的

水平翻轉是影像分類的標準增強，但**落尾眉翻轉後會變成上揚眉**，等於餵錯標籤。
領域知識要進到增強設定裡。

---

## 十、重現方式

> ⚠️ **第 1、2 步不是可選的。** 只要動過 `data/basic_full/grouped` 裡的檔案
> （搬檔、改資料夾名、合併類別），就**必須**從第 1 步重跑，否則會發生 §7.6 那件事。
> 順序也不能顛倒：identity map 是 ROI 快取的輸入。

```bash
# 0) MediaPipe 路徑修復是自動的（專案路徑含中文，見 mediapipe_ascii）

# 1) 重建身分聚類 —— 搬過任何一張圖就必須重跑
.\.venv\Scripts\python.exe -X utf8 tools/build_identity_map.py

# 2) 重建 ROI 快取（依檔名去重、套官方分類表）
.\.venv\Scripts\python.exe -X utf8 prepare_roi_cache.py

# 2b) 驗證：identity == -1 應該只剩個位數（2026-07-30 實測 2 筆）
.\.venv\Scripts\python.exe -X utf8 -c "import json,collections; r=json.load(open('data/roi_cache/index.json',encoding='utf-8'))['records']; bad=[x for x in r if x.get('identity',-1)==-1]; print('identity=-1:',len(bad),'/',len(r))"

# 3) CNN 5-fold（一律 cluster，不要用 per_image；40 epochs 見 §7.2）
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster --epochs 40 --learning-rate 6e-4

# 4) 幾何規則重新校準 + 5-fold
.\.venv\Scripts\python.exe -X utf8 tools/calibrate_rule_thresholds.py
.\.venv\Scripts\python.exe -X utf8 tools/cv_rule_baseline.py --refresh

# 5) DINOv2 5-fold（會沿用 embedding 快取，只比對筆數——見 §7.6 的守衛警告）
.\.venv\Scripts\python.exe -X utf8 tools/dinov2_cv_experiment.py

# 6) 學習曲線（回答「還要多少張圖」）
.\.venv\Scripts\python.exe -X utf8 tools/learning_curve.py

# 7) 匯出部署模型（決策樹 + CNN + DINOv2 shadow head）
.\.venv\Scripts\python.exe -X utf8 tools/train_final_rule_trees.py
.\.venv\Scripts\python.exe -X utf8 tools/train_final_selected_models.py
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --skip-random --epochs 40 --learning-rate 6e-4
.\.venv\Scripts\python.exe -X utf8 tools/export_dinov2_heads.py   # ← §十 原本漏了這支

# 8) 驗證：用真實分析路徑跑幾張圖，確認新類別真的載得起來
.\.venv\Scripts\python.exe -X utf8 tools/batch_basic_classify.py --source <某個資料夾> --output out.csv --limit 3

# 眼型合併對照
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster --merge-eye --parts eye_shape

# ── 完整方格（§7.9）：其餘變體，各自的輸出檔名不同才不會互相覆蓋 ──
# ⚠️ cv_summary 的檔名 tag 不含 epochs/lr，跑不同學習率要自己把輸出改名保存
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster --epochs 40 --learning-rate 3e-4
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster --epochs 40 --architecture simple_cnn
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster --epochs 40 --parts face_shape --face-input contour
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster --epochs 40 --parts face_shape --face-input contour --contour-file face_contour_224.npy
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster --epochs 40 --parts brow_shape lip_shape --contour-parts brow_shape lip_shape
.\.venv\Scripts\python.exe -X utf8 tools/dinov2_cv_experiment.py --parts face_shape --face-input contour
.\.venv\Scripts\python.exe -X utf8 tools/cascade_face_shape.py
```

**方格的原始輸出**存在 `models/basic_features_roi/g6/`（每個變體一份 JSON）。

> **第 7 步的 `export_dinov2_heads.py` 原本沒寫在這裡**，結果 07-30 重跑時
> `models/basic_features_roi/*_dinov2_head.npz` 差點留著舊檔——
> `train_final_selected_models.py` 寫的是 `models/final_features_20260719/`，
> 不是線上服務讀的 `basic_features_roi/`。兩個目錄的差別很容易漏。

> **第 8 步不能省。** 訓練腳本說「已匯出」只代表檔案寫出去了，
> 不代表 `Face_analyzer_BASIC` 載得起來（§11.2 那道類別守衛就會擋掉不合契約的 head）。
> 2026-07-30 實測 3/3 成功，輸出含 `桃杏眼`、唇型為 4 類，無已淘汰類別漏出。

### 產出檔案

| 路徑 | 內容 |
| --- | --- |
| `models/basic_features_roi/<part>.onnx` | 線上推論用的 CNN |
| `models/basic_features_roi/<part>_classes.json` | 類別表（線上推論讀這個）|
| `models/basic_features_roi/<part>_rule_tree.json` | 幾何決策樹（眼型、臉型）|
| `models/basic_features_roi/cv_summary.json` | CNN 5-fold 結果 |
| `models/basic_features_roi/rule_cv_results.json` | 規則式 5-fold 結果 |
| `models/basic_features_roi/dinov2_cv_results.json` | DINOv2 5-fold 結果 |
| `models/basic_features_roi/learning_curve.json` | 學習曲線 |
| `data/roi_cache/label_conflicts.json` | 標註衝突清單（供人工複核）|

---

## 十一、線上服務架構

### 11.1 一次分析走過的路

```
瀏覽器
  │ 上傳照片（multipart）
  ▼
AI Gateway ──驗 session／CSRF／actor──► face-basic（Cloud Run）
                                          │
                                          ├─ 影像安全檢查（image_safety）
                                          ├─ InsightFace 偵測人臉、判正面
                                          ├─ MediaPipe FaceMesh 取 landmark
                                          ├─ 幾何決策樹 → 眼型、臉型
                                          ├─ CNN(ONNX) → 眉型、鼻型、唇型
                                          ├─ 膚色 LAB／四季型
                                          └─ 修正快取套用（見 §12）
                                          ▼
                                    job_store（Firestore）
```

- **非同步 job**：`POST /v1/face/jobs/basic` 立刻回 jobId，前端輪詢。
  同步版會被 Cloud Run 請求逾時砍掉。
- **job 保留 1 小時**（`FACE_JOB_RETENTION_SECONDS`）
- **IAM**：`face-basic` / `face-pro` / `replicate-render` 都**只允許 `ai-gateway@` 呼叫**，
  沒有 `allUsers`。外面打得到的只有 Gateway。

### 11.2 DINOv2 shadow mode

DINOv2 目前**不提供正式答案**，只在背景跑並記錄與正式答案的差異（shadow）。
因為它佔整個分析 57% 的時間，所以是抽樣執行（`DINOV2_SAMPLE_RATE`）。

有一道守衛：**DINOv2 head 的類別若含已淘汰的名稱就不載入**。
2026-07-24 眼型由 8 類併為 6 類時，CNN 已重訓但 DINOv2 head 沒有跟上；
少了這道檢查，只要有人設 `ROI_DINOV2_MODEL_FIRST=1`，線上就會吐出「丹鳳眼」
這種不在契約裡的標籤，而前端回饋選項也沒有它——使用者選不到，看起來只像模型很爛。

### 11.3 臉部照片存在哪、誰看得到

**兩張**照片會存到雲端：使用者上傳的原圖（妝前）與 AI 生成的妝後圖。

```
/media/render/<32碼>          ← 妝後圖
/media/render/<32碼>/before   ← 妝前圖（原圖）
```

| 項目 | 內容 |
| --- | --- |
| 儲存位置 | GCS bucket `decorate-me-renders`（**私有**，直接打網址打不開）|
| 保留期 | render job 1 小時；GCS 物件 **30 天**（收藏時呼叫 `/retain` 保住）|
| 讀取條件 | 經 Gateway 驗證會員 session ＋ 是本人（或管理員）→ 才換 **10 分鐘**有效的臨時網址 |
| 會員資料庫存什麼 | **只有網址字串**（`String(500)`），沒有任何影像資料 |

**與七月初的差別：**

| | 2026-07 初 | 現在 |
| --- | --- | --- |
| bucket 權限 | 公開讀，有網址就能看 | **私有** |
| 前端拿到的網址 | GCS 永久公開網址 | 自己的 `/media/render/...`，要驗身分 |
| 網址流出 | **永久有效，收不回** | 臨時網址 10 分鐘失效 |
| 存幾張臉 | 只有妝後圖 | **妝前圖＋妝後圖** |

> ⚠️ **「原始照片不會離開你的瀏覽器」這句話現在不成立。** 舊文件這樣寫過，
> 但渲染流程會把原圖上傳到 GCS。口試若被問到，要照上表誠實回答。

**已知但尚未處理的一項（2026-07-29 決定暫緩）：**

管理員可以讀任何會員的 `/media/render/<id>/before`（`_require_job_owner` 在
`X-Admin-Request: 1` 時跳過擁有者檢查），而且**這條路沒有寫稽核紀錄**——
管理員看了誰的臉、看了幾次，系統裡查不到。

修法已寫成 patch（`未套用_渲染圖稽核與妝前圖權限_20260729.patch`），
因專題時程暫緩套用。**這一項在資安審查時會被問到。**

---

## 十二、使用者回饋與修正快取

### 12.1 回饋端點

使用者看到五官判斷後可以直接修正，送到：

```
POST /v1/face/jobs/{job_id}/feedback
```

存進獨立集合 `face_feedback`（不寫回 job——job 一小時就被清掉，標註要活到下一次重訓）。
**只收類別字串，永遠不碰照片**，且驗證每個值都出自目前的分類表。

前端的選項就是照 `models/basic_features_roi/*_classes.json` 排的，
所以每一筆修正都是**已經對齊模型類別的人工標註**——正是 issue #24 缺的那種資料。

**撤回要刪除，不能只是不寫**：使用者改完送出、又改回「判斷正確」重送時，
如果只是早退，那筆錯誤修正會永遠留在訓練集裡而且無聲無息。

### 12.2 修正快取：讓使用者的修正真的看得到效果

模型是靜態分類器，同一張圖永遠回同一個答案——這是必要的，否則無法重測。
但對使用者來說「我明明改過了，下次上傳還是同一個錯答案」等於回饋沒有意義。

`face_corrections.py` 補這個落差：以照片的 SHA-256 為鍵（**只存雜湊，不存影像**），
分析完成後套用這張臉先前的修正。

**必須守住的那條線：**

```
result[部位]           套用修正後的值（使用者看到的）
result["_modelRaw"]    模型的原始輸出（回饋一律回報這一份）
```

套用修正後畫面顯示的是「使用者的答案」。如果回饋面板接著把那個值當成
`predicted` 送回去，訓練資料就變成**模型在確認自己**——集合會被自我循環的假資料
填滿，而且從外面完全看不出來，重訓分數會憑空變好。

**代價（設計時就知道）：** 線上準確率不能再從使用者看到的結果推估。
要量模型真實表現看 `_modelRaw`，或跑離線評估。
關掉用 `FACE_CORRECTION_CACHE_ENABLED=0`。

### 12.3 目前收到的回饋

`face_feedback` 已有真實使用者修正，集中在：

```
臉型  鵝蛋臉 → 長形臉
眼型  鳳眼   → 圓眼
鼻型  寬鼻   → 標準鼻
眉型  彎月眉 → 一字眉
```

**與 issue #24 標示的最弱類別吻合**，也與 §6.6 量到的「杏仁／桃花／圓眼界線不存在」一致。

---

## 附錄：相關文件

| 文件 | 內容 |
| --- | --- |
| `CNN訓練歷程_BASIC五官分類.md` | 世代 1–2 的原始紀錄 |
| `模型訓練記錄_2026-07-24.md` | 世代 3 的完整細節 |
| `歷史流程更改追蹤.md` S71/S72 | 世代 4 的逐步紀錄 |
| `訓練資料夾整理指南_2026-07-29.md` | 資料夾整理與可刪清單 |
| GitHub issue #24 | 五官分類：資料擴充與後續重測流程 |
