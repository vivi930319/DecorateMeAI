# 搬機環境地雷：工具鏈與編碼（2026-08-06）

這份是 `搬機地雷補充_2026-07-21.md` 的續集。前一份講的是**專案本身**的坑
（兩個 repo、`.gitignore`、缺檔、OneDrive、中文路徑）；這一份講的是**機器環境**的坑——
2026-08-06 那次「全端重新部署＋契約測試」整天下來，每一次卡住都不是程式的問題，
是這台機器的軟體組態。新機器如果照舊裝，這些會**原封不動再撞一次**。

先講結論，免得誤判方向：**舊機器的硬體沒有問題。**

```
CPU   AMD Ryzen 9 5900X   12 核 / 24 緒
GPU   RTX 3070
RAM   16 GB
磁碟  C: 剩 1.7 TB / 1.9 TB
```

映像是 Cloud Build 建的、模型跑在 Cloud Run，本機只負責上傳原始碼。
真正拖慢工作的是下面這五件事，全部都是**裝什麼、怎麼設定**的問題。

搬機時的閱讀順序：第 1、2 節在裝環境前就要做，第 3 節在第一次跑測試前要做，
第 4、5 節是第一次部署才會撞到。

---

## 1. PowerShell 5.1 是預設，但你要的是 PowerShell 7

**症狀**：一堆理所當然的東西不能用，而且錯誤訊息不會告訴你是版本問題。

| 你會寫的 | PowerShell 5.1 的反應 |
| --- | --- |
| `cmd1 && cmd2` | 解析錯誤。要改成 `cmd1; if ($?) { cmd2 }` |
| `Invoke-RestMethod -Form @{...}` | `找不到符合參數名稱 'Form' 的參數`（上傳檔案沒有內建做法） |
| `$a ?? $b`、`$x?.y`、三元運算子 | 全都是解析錯誤 |
| `native.exe 2>$null` | 每行 stderr 被包成 `NativeCommandError`；在 `$ErrorActionPreference="Stop"` 下會**中止腳本**，即使該指令 exit code 是 0 |
| `Set-Content` 不指定編碼 | 寫出 ANSI（CP950），不是 UTF-8 |

最後兩項在 `deploy_face_cloudrun.ps1` 裡有大段註解記錄——那支腳本曾經**每次部署都報失敗，
但兩個服務其實都部署成功**，就是 `2>$null` 造成的。

**怎麼確認你中了**：

```powershell
$PSVersionTable.PSVersion      # 5.1.x = 舊的
Get-Command pwsh               # 找不到 = 沒裝 7
```

**做法**：裝 PowerShell 7，它跟 5.1 **並存不衝突**（不同執行檔 `pwsh.exe` / `powershell.exe`，
5.1 是 Windows 元件不會被移除）。

```powershell
winget install --id Microsoft.PowerShell --source winget
```

裝完之後終端機、VS Code、排程工作都要記得指向 `pwsh`，不然還是跑到 5.1。

> 專案裡的 `.ps1` 腳本目前是**寫給 5.1 也能跑**的（所以才有那些 `if ($?)` 和 try/finally）。
> 有 7 之後不必改寫，但新腳本可以直接用現代語法。

---

## 2. 系統 ANSI 是 950（Big5），所以含中文的 `.ps1` 一定要存成 UTF-8 with BOM

**症狀**：腳本裡的中文在畫面上變亂碼，程式邏輯卻完全正常。

```
開始暖機（第一次可能較久…）   ->   ���l�x���]�Ĥ@���i�शA�[�^
```

**原因**：Windows PowerShell 5.1 讀 `.ps1` 時，檔頭**沒有 BOM 就用系統 ANSI 解碼**。
這台的 ANSI 是 CP950（Big5），拿去解 UTF-8 位元組必然亂碼。

**怎麼確認**：

```powershell
(Get-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Control\Nls\CodePage').ACP   # 950 = 中招
```

**規則**：`.ps1` 只要含非 ASCII 字元，就必須帶 UTF-8 BOM（`EF BB BF`）。
純英文的腳本不需要——`deploy_gateway_cloudrun.ps1` 沒有 BOM 是正確的，因為它一個中文都沒有。

**判斷方式是「這個檔有沒有非 ASCII」，不是「其他腳本有沒有 BOM」。**
2026-08-06 的 code review 曾把 `deploy_face_cloudrun.ps1` 的 BOM 報成「無來由的編碼變動」，
拿沒有中文的 `deploy_gateway_cloudrun.ps1` 當對照組——那是無效比較。
反過來查才發現 `warmup.ps1`（demo 前暖機用、會 `Write-Host` 中文）**少了 BOM**，
是真的會在口試前印出亂碼。

**檢查全部腳本**：

```powershell
Get-ChildItem *.ps1 -Recurse | ForEach-Object {
  $b = [System.IO.File]::ReadAllBytes($_.FullName)
  $bom = $b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF
  $body = if ($bom) { $b[3..($b.Length-1)] } else { $b }
  $nonAscii = ($body | Where-Object { $_ -gt 127 }).Count
  if ($nonAscii -gt 0 -and -not $bom) { "缺 BOM 會亂碼: $($_.Name)" }
}
```

`Set-Content` / `Out-File` 有可能把 BOM 弄掉，改完中文腳本記得回頭驗一次。

> 新機器可以考慮打開「Beta: 使用 UTF-8 提供全球語言支援」（地區設定 → 系統地區設定），
> 把 ANSI 改成 65001。但那是**全系統**設定，會影響其他舊程式，搬機初期先別開，
> 等專案跑順了再評估。

---

## 3. 防毒軟體攔截 HTTPS，Python 因此驗不過任何憑證

**這一節最容易被誤判成「服務壞了」。**

**症狀**：Python 打任何 HTTPS 都失敗，但瀏覽器和 PowerShell 打同一個網址都正常。

```
requests.exceptions.SSLError: ... certificate verify failed:
unable to get local issuer certificate (_ssl.c:997)
```

2026-08-06 跑契約測試時，22 個測試同時掛在這裡，看起來像整個 Gateway 都倒了，
實際上服務全部健康。

**原因**：Avast 的 Web/Mail Shield 在中間解開 TLS 重新簽發。它的根憑證裝在
**Windows 憑證存放區**，所以瀏覽器和 PowerShell 信任它；但 Python 的 `requests`
用的是 `certifi` 自帶的憑證清單，裡面沒有 Avast，於是驗不過。

**怎麼確認**：

```powershell
Get-ChildItem Cert:\LocalMachine\Root, Cert:\CurrentUser\Root |
  Where-Object { $_.Subject -match 'Avast|AVG|Kaspersky|BitDefender|ESET|Zscaler' } |
  Select-Object Subject
```

有輸出就是中了。（這台的結果：`CN=Avast Web/Mail Shield Root`。）

**絕對不要用 `verify=False` 繞過。** 這個專案的測試有一大半在驗資安行為
（未登入要 401、錯 token 要 403、不得外洩金鑰），關掉憑證驗證等於讓那些測試失去意義。

**做法（擇一）**：

1. **最乾淨**：在 Avast 設定裡關閉 HTTPS 掃描。新機器如果不裝 Avast，這節可以整個跳過。
2. **不關掉的話**：把系統根憑證匯出成 PEM，指給 Python 用。

```powershell
$pem = "$env:USERPROFILE\win-roots.pem"
$sb = New-Object System.Text.StringBuilder
foreach ($store in @("Cert:\LocalMachine\Root","Cert:\CurrentUser\Root")) {
  Get-ChildItem $store | ForEach-Object {
    [void]$sb.AppendLine("-----BEGIN CERTIFICATE-----")
    [void]$sb.AppendLine([Convert]::ToBase64String($_.RawData,'InsertLineBreaks'))
    [void]$sb.AppendLine("-----END CERTIFICATE-----")
  }
}
[System.IO.File]::WriteAllText($pem, $sb.ToString())
$env:REQUESTS_CA_BUNDLE = $pem
```

### 3.1 陷阱：`REQUESTS_CA_BUNDLE` 會反過來把 gcloud 弄壞

設了這個環境變數之後，**gcloud 自己也會讀它**（它內部也是 Python + requests），
結果變成 `gcloud secrets versions access` 靜默回空值。

實際發生過：測試腳本因此拿不到 `FACE_API_KEY`，四個 face 測試全部 401，
看起來像 API key 設錯，其實是憑證變數的連帶傷害。

**順序很重要**——先取秘密，再設憑證變數：

```powershell
$env:FACE_API_KEY = (gcloud secrets versions access latest `
    --secret=decorate-me-face-upstream-key --project decorate-me).Trim()
$env:REQUESTS_CA_BUNDLE = "$env:USERPROFILE\win-roots.pem"   # 一定要放後面
```

### 3.2 gcloud 這台目前是「關掉驗證」在跑，而且補不回來（2026-08-07 查證）

這台的 gcloud 設定裡有：

```
[auth]
disable_ssl_validation = True
```

意思是**所有 gcloud 流量都不驗憑證**——包含拉訓練資料集、下載模型、取 Secret Manager 的金鑰。
每次跑都會噴 `InsecureRequestWarning`，很容易被當成雜訊略過。

既然第 3 節的做法是「把 Avast 根憑證加進信任清單」，照理 gcloud 也能比照辦理
（`gcloud config set core/custom_ca_certs_file`）。**試過了，不行**，原因值得記著：

```
SSLCertVerificationError: certificate verify failed:
Basic Constraints of CA cert not marked critical
```

Avast 那張根憑證的 `Basic Constraints` 擴充**沒有標記為 critical**。X.509 規範要求
CA 憑證把它標成 critical，OpenSSL 3.x 嚴格檢查，因此拒絕拿它當信任錨點。
這不是設定錯，是那張憑證本身不合規，加進清單也沒用。

有個地方特別會騙人：`gcloud storage ls gs://bucket/`（只列頂層）**會成功**，
但 `--recursive` 加萬用字元就失敗。所以「隨手測一下好像好了」是靠不住的，
要用 `tools/sync_datasets.py --verify` 這種真的會遞迴列出的指令測。

**唯一的真解是在 Avast 設定裡關閉 HTTPS 掃描**（設定 → 防護 → 核心防護 →
網頁防護 → 取消「啟用 HTTPS 掃描」）。關掉之後：

```powershell
gcloud config unset auth/disable_ssl_validation
.\.venv\Scripts\python.exe tools\sync_datasets.py --verify   # 要能列出正確數量
```

**新機器如果沒裝 Avast，不要把 `disable_ssl_validation` 一起搬過去。**
那是為了繞過這台的攔截才設的，搬過去就變成無緣無故不驗憑證。

---

## 4. 私有 Cloud Run 服務：本機打不到，要先裝 `cloud-run-proxy`

`face-basic` 和 `face-pro` 是私有服務（只接受 Gateway 與專案成員），
本機直接 `curl` 一定 403。

**做法**：

```powershell
gcloud components install cloud-run-proxy --quiet
gcloud run services proxy face-basic --region asia-east1 --project decorate-me --port 8001
gcloud run services proxy face-pro   --region asia-east1 --project decorate-me --port 8002
```

裝元件時如果 gcloud 回「非互動模式請先執行 copy-bundled-python」，照它說的做：

```powershell
$env:CLOUDSDK_PYTHON = (gcloud components copy-bundled-python | Select-Object -Last 1)
gcloud components install cloud-run-proxy --quiet
```

**proxy 會自己斷線**（token 過期或程序被回收），測試跑到一半出現
`ConnectionRefusedError [WinError 10061]` 就是它掉了，重開即可，不是服務有問題。

### 4.1 還有第二道關卡：服務自己的 `x-api-key`

proxy 只解決 Cloud Run **IAM**，服務內部還有一道 API key（`_api_key_guard`）。
少了它每支端點都回 `401 FORBIDDEN`。

```powershell
$env:FACE_API_KEY = (gcloud secrets versions access latest `
    --secret=decorate-me-face-upstream-key --project decorate-me).Trim()
```

`backend_smoke_test.py` 從 2026-08-06 起會自動帶這個變數。沒設就只能測本機開發伺服器。

### 4.2 部署腳本的 `/health` 一定會被略過，那是正常的

`deploy_face_cloudrun.ps1` 收尾會印：

```
face-basic Ready（修訂版 …）；/health 略過：目前憑證簽不出對應 audience 的 ID token
```

**這不是失敗。** 使用者帳號（`isachen2277@gmail.com` 這類）產不出指定 audience 的
ID token，`gcloud auth print-identity-token --audiences=` 會直接回
`Invalid account type for --audiences. Requires valid service account.`。
部署成不成功由前面的 Ready 判定，`/health` 只是加碼資訊。

要真的驗線上，用第 4 節的 proxy 自己打。

---

## 5. 新機器要裝什麼版本

舊機器的組合是**能跑，但全部過期**：

| 項目 | 舊機器 | 狀態 |
| --- | --- | --- |
| Windows 10 22H2（build 19045） | 2023-06 安裝 | **官方支援已於 2025-10-14 結束**，之後沒有安全更新 |
| PowerShell 5.1 | 系統內建 | 見第 1 節 |
| Python 3.10.0 | 專案 `.venv` | `.0` 表示 2021 年後沒有修補；`google.api_core` 已警告 2026-10-04 停止支援 |
| Node v24.11.0 | — | 沒問題，前端測試都正常 |

**新機器建議**：

- **Windows 11**（5900X 完全符合需求）。這是資安考量——這個專案處理會員個資與臉部照片。
- **PowerShell 7**（第 1 節）。
- **Python**：升級前先確認 `mediapipe` 與 `onnxruntime` 有對應版本的 wheel，
  這兩個是最容易卡住的。**不要在 demo 或口試前一週做這件事。**
- **路徑**：務必裝在**純 ASCII 且不在 OneDrive 底下**的位置（例如 `C:\dev\pp12`）。
  理由見 `搬機地雷補充_2026-07-21.md` 第 6、10 節——中文路徑會讓 MediaPipe 完全跑不起來。

**硬體**：不用為了這個專案換。唯一值得升的是 **RAM 16 GB → 32 GB**，
而且只有在你要**本機訓練模型**時才有感；跑推論、部署、測試都綽綽有餘。

---

## 6. 訓練資料集不在 git 裡，用 GCS 拉

新機器 clone 下來會**沒有訓練照片**——那是刻意的。

### 為什麼不放 git

訓練資料集有 436 MB、3174 個檔案，而且是人臉照片。git 對二進位檔沒有壓縮優勢，
每次補資料重訓都會讓 repo 永久長大，而且**刪掉也不會變小**（歷史還在，要拿掉得改寫
整個歷史，所有 clone 都要重來）。`.gitignore` 為了 ONNX 已經踩過同一個坑，
2026-07-31 的那段註解寫得很清楚。

分工：

```
GitHub   程式碼、訓練腳本、保留集切分、holdout_scores.json、全部訓練紀錄
GCS      照片本身（gs://decorate-me-datasets，私有、強制封鎖公開存取）
```

**可重現性不因此受損**：保留集用 sha256 當鍵（不是路徑），clone 下來配上還原的照片
就能重跑出同樣的數字。

### 怎麼拉

```powershell
# 先確認 gcloud 已登入且指向 decorate-me
gcloud auth login
gcloud config set project decorate-me

# 拉訓練資料集
.\.venv\Scripts\python.exe tools\sync_datasets.py --pull

# 只想先看要下載什麼
.\.venv\Scripts\python.exe tools\sync_datasets.py --pull --dry-run

# 確認本機與雲端一致
.\.venv\Scripts\python.exe tools\sync_datasets.py --verify
```

目前雲端的內容：

| 路徑 | 內容 |
| --- | --- |
| `basic_full/grouped_post-merge_20260806` | BASIC 五官，2455 檔（**目前這一版**） |
| `basic_full/grouped_pre-merge_20260806` | 眼型合併前，2472 檔（唯讀，對照實驗依據） |
| `pro_full/grouped_20260807` | PRO 側臉鼻型，719 檔 |
| `docs/技術文件書_詳細版` | 技術文件 16 份 `.md` ＋ 2 份 `.docx`（2026-08-07 加入） |

### 技術文件為什麼兩邊都放

文件在 git 裡就有，GCS 這一份是重複的，而且是刻意的：**git 是它們的歷史，
GCS 是它們的取用點**。還沒 clone、或 git 一時拿不到的時候仍然看得到。

所以雲端路徑**不帶日期**（照片才帶）——git 已經記得每一版長什麼樣，
雲端再壓一層版本只會兩邊對不起來。

改文件請改 repo 裡的那份再 `--push`。只改雲端那份，git 就不知道。

**那兩份 `.docx` 是例外，它們只有 GCS 有。** `.gitignore` 第一行是 `*`（全封鎖白名單制），
二進位檔沒被加進白名單，所以 Word 檔從來沒進過版控。2026-08-07 上傳之前，
它們只存在於這台電腦的硬碟上。

### ⚠️ 拉完一定要重建 identity map 與 ROI 快取

```powershell
.\.venv-train\Scripts\python.exe tools\build_identity_map.py
.\.venv-train\Scripts\python.exe prepare_roi_cache.py
```

`identity_map.json` 用**絕對路徑**當鍵，換機器之後路徑全變，不重建的話所有樣本
都會變成 `identity=-1`——而 `-1` 一律留在 train、永遠不會被驗證。**沒有任何地方會報錯。**
2026-08-06 就是這樣讓眼型有 15% 的資料悄悄不曾當過考題（見訓練紀錄 §5.3.1）。

`tools/build_holdout_split.py` 會偵測 `identity=-1` 的比例並提醒，那是目前唯一的防線。

### 拉取不會刪本機檔案

`--pull` 刻意不加 `--delete-unmatched-destination-objects`：本機多出來的檔案可能是
還沒推上去的新標註，靜默刪掉會弄丟人工成果。要清乾淨請自己動手。

---

## 附：搬機後的環境驗收（照順序跑）

```powershell
# 1) 基本版本
$PSVersionTable.PSVersion                  # 期望 7.x
python --version                           # 期望 3.11+
(Get-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Control\Nls\CodePage').ACP

# 2) 憑證攔截檢查（有輸出就先處理第 3 節）
Get-ChildItem Cert:\LocalMachine\Root |
  Where-Object { $_.Subject -match 'Avast|AVG|Kaspersky|ESET|Zscaler' } | Select-Object Subject

# 3) 訓練資料集（新機器一定要做，見第 6 節）
.\.venv\Scripts\python.exe tools\sync_datasets.py --pull
.\.venv-train\Scripts\python.exe toolsuild_identity_map.py
.\.venv-train\Scripts\python.exe prepare_roi_cache.py

# 4) 離線測試（不需要雲端）
.venv\Scripts\python.exe tools\verify_failure_contract.py    # 期望 24 項全過
.venv\Scripts\python.exe image_safety_test.py                # 期望 15 項全過
node ..\web_frontend\frontend_smoke_check.js
node ..\web_frontend\face_job_error_check.js                 # 期望 16 項全過

# 5) 線上測試（需要 gcloud 登入 + proxy）
$env:FACE_API_KEY = (gcloud secrets versions access latest `
    --secret=decorate-me-face-upstream-key --project decorate-me).Trim()
.venv\Scripts\python.exe backend_smoke_test.py `
    --image data\kaggle_asian_faces\generated_yellow-stylegan2\0.png `
    --basic-url http://127.0.0.1:8001 --pro-url http://127.0.0.1:8002
```

第 4 步全過代表程式碼與資料集搬對了；第 5 步全過代表雲端權限也接上了。
兩步之間如果只有第 5 步失敗，先照第 3、4 節排查環境，不要先懷疑服務。

---

## 附：這份文件本身

`.gitignore` 預設不收 `.md`，這份已經在檔尾加了 `!` 例外，所以會跟著 repo 走。
新機器 clone 下來就看得到，不必另外複製。

搬機三份文件的分工：

| 文件 | 講什麼 |
| --- | --- |
| `專案交接手冊_搬機版_2026-07-17.md` | 安裝流程與專案結構 |
| `搬機地雷補充_2026-07-21.md` | 專案本身的坑：兩個 repo、`.gitignore`、缺檔、OneDrive、中文路徑 |
| **這份** | 機器環境的坑：PowerShell、編碼、憑證攔截、Cloud Run 認證、版本選擇 |
