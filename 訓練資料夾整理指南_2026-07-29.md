# 訓練資料夾整理指南

**日期：** 2026-07-29
**目的：** 統一訓練集到 `data/basic_full/grouped`，並確認哪些資料夾可以安全刪除。

---

## 0. 唯一的訓練集

```
data/basic_full/grouped/
```

`prepare_roi_cache.py` 第 29 行 `ROOT = Path("data/basic_full/grouped")` 是**寫死的**，
訓練流程只讀這一個資料夾。其他任何地方的照片都不會進入訓練。

### 目前內容（2026-07-29 統一後）

| 部位 | 類別與張數 | 合計 |
| --- | --- | --- |
| face_shape | 圓形臉 116 / 心形臉 100 / 方形臉 106 / 長形臉 103 / 鵝蛋臉 105 | 530 |
| eye_shape | 下垂眼 110 / 圓眼 120 / 杏仁眼 109 / 桃花眼 119 / 細長眼 118 / 鳳眼 107 | 683 |
| brow_shape | 一字眉 220 / 彎月眉 118 / 落尾眉 103 | 441 |
| nose_shape | 寬鼻 138 / 標準鼻 132 | 270 |
| lip_shape | M型唇 109 / 厚唇 112 / 微笑唇 100 / 花瓣唇 117 / 薄唇 106 | 543 |
| | | **2467** |

**分類已經與官方清單完全相符**，而且是**實體資料夾**就相符，不是靠程式在載入時轉換：

- `丹鳳眼` → 併入 `鳳眼`
- `瞇縫眼` → 併入 `細長眼`
- `窄鼻` → 併入 `標準鼻`

以前這三個對照只寫在 `prepare_roi_cache.py` 的 `LABEL_ALIASES` 裡，資料夾本身還是舊分類，
所以任何人直接看資料夾都會以為眼型有八類。現在資料夾就是答案。

---

## 1. ✅ 可以刪掉的資料夾

以下每一個都經過**逐張檔名比對**：裡面的照片**全部**都在 `grouped` 裡，沒有任何一張是獨有的。

| 資料夾 | 大小 | 照片 | 訓練集沒有的 | 為什麼可以刪 |
| --- | --- | --- | --- | --- |
| `data/basic_full/grouped_yun/` | 329 MB | 2009 | **0** | 已全部併入。這就是 `grouped_yun(2).zip` 解開的內容 |
| `data/group_yun/` | 122 MB | 689 | **0** | 已全部併入（含它獨有的 58 張鼻型） |
| `data/basic_full/_replaced_by_yun_*/` | 7.8 MB | 61 | **0** | 合併時被改判的舊標註，保留副本 |
| `data/basic_full/_normalised_*/` | 1.5 MB | 8 | **0** | 資料夾正規化時的重複檔 |
| `data/roi_cache_backup_20260724/` | 220 MB | — | — | ROI 快取的舊備份，無程式引用，重跑 `prepare_roi_cache.py` 即可重建 |

**合計約 680 MB。**

> 刪除前的最後確認指令（在 `data/` 底下跑，會逐張比對後才回答）：
>
> ```bash
> ../.venv/Scripts/python.exe -X utf8 -c "
> import os,glob
> IMG=('.jpg','.jpeg','.png')
> def n(r):
>     s=set()
>     for root,d,fs in os.walk(r): s|={f.lower() for f in fs if f.lower().endswith(IMG)}
>     return s
> g=n('basic_full/grouped')
> for t in ['basic_full/grouped_yun','group_yun']+glob.glob('basic_full/_replaced_by_yun_*')+glob.glob('basic_full/_normalised_*'):
>     if os.path.isdir(t):
>         m=n(t)-g
>         print(('可刪  ' if not m else f'別刪（{len(m)} 張獨有）'), t)
> "
> ```

---

## 2. ⚠️ 不要刪的資料夾

| 資料夾 | 大小 | 誰在用 |
| --- | --- | --- |
| `data/basic_full/grouped/` | 368 MB | **訓練集本身** |
| `data/roi_cache/` | 468 MB | 訓練直接讀它。可刪但要重跑 `prepare_roi_cache.py`（約 10 分鐘） |
| `data/asian_faces/` | 114 MB | `backend_smoke_test.py`、`tools/measure_angle_sensitivity.py` |
| `data/pro_full/` | 71 MB | `train_pro_nose_side.py`（PRO 側臉鼻型） |
| `data/pro_roi_cache/` | 59 MB | `train_pro_nose_side.py` |

### `data/kaggle_asian_faces/`（12 GB）—— 你自己決定

**沒有任何程式引用它。** 但它是原始素材庫，刪掉之後就沒辦法再從裡面挑新照片來標註。

- 如果標註工作已經結束 → 可以刪，省 12 GB
- 如果還要再擴充訓練集 → 留著，或搬到外接硬碟

這一個我不幫你決定，因為它跟「之後還要不要繼續標」有關，而那是你才知道的事。

---

## 3. 之後要加新照片時的規矩

為了不要再發生這次的混亂，只需要一條規則：

> **新照片直接放進 `data/basic_full/grouped/<部位>/<官方類別>/`，不要另外開資料夾。**

官方類別只有這些，資料夾名必須完全一樣：

```
face_shape : 圓形臉 心形臉 方形臉 長形臉 鵝蛋臉
eye_shape  : 下垂眼 圓眼 杏仁眼 桃花眼 細長眼 鳳眼
brow_shape : 一字眉 彎月眉 落尾眉
nose_shape : 寬鼻 標準鼻
lip_shape  : M型唇 厚唇 微笑唇 花瓣唇 薄唇
```

放完之後跑：

```bash
.\.venv\Scripts\python.exe -X utf8 prepare_roi_cache.py
.\.venv\Scripts\python.exe -X utf8 train_basic_cnn_roi.py --cv 5 --identity-mode cluster
```

`prepare_roi_cache.py` 會自己處理三件事，不用手動：

1. **同名即同一張照片**（2026-07-29 起）。同一張臉為不同部位裁成不同大小、分放各部位
   資料夾是正常的，靠檔名接起來。
2. **舊類別自動對照**：`丹鳳眼→鳳眼`、`瞇縫眼→細長眼`、`窄鼻→標準鼻`、`蒜頭鼻→寬鼻`。
3. **不在官方清單的類別會被跳過並印警告** —— 看到警告就是資料夾名打錯了，去改資料夾。

---

## 4. 這次整理做了什麼（備查）

1. 併入 `basic_full/grouped_yun`（1371 張新照片，衝突以 yun 為準）
2. 併入 `data/group_yun`（補 58 張鼻型）
3. 資料夾實體正規化：丹鳳眼、瞇縫眼、窄鼻三個目錄併掉
4. `prepare_roi_cache.py` 去重鍵由**內容雜湊**改為**檔名**——原本會把同一張臉的全臉與
   部位特寫當成兩張不相干的圖，導致標註接不起來、還被誤判成衝突剔除（眼型一度少 184 張）
5. 全程用 move 進隔離區，**沒有刪除任何檔案**；每一步都有 CSV 逐筆紀錄
6. 最終稽核：**0 張照片遺失**

### 過程中我出的錯

正規化腳本因為 OneDrive 鎖住目錄而中斷、分兩次執行，一度把 71 張眼型照片留在隔離區。
已全部救回並經稽核確認。因為全程只搬不刪，這個錯誤才有辦法完全復原——
**這也是為什麼上面那些隔離資料夾在你確認之前我不會自己刪掉。**
