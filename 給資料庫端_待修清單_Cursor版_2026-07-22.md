# 給資料庫端：待修清單（Cursor 執行版）

> 日期：2026-07-22
>
> 這份取代 `給資料庫端_會員點數打卡任務推薦碼規格_2026-07-21.md` 的第 2、3、4 節。
> 前一份是「我們猜的問題」，這一份是**用真實會員帳號實測過的結果** —— 有兩項先前的請求
> 已經證實不需要做，直接從清單移除，不要浪費時間。兩份衝突時**以這份為準**。
>
> 禁止寫入本文件、Log、Git 或訊息：真實密碼、完整 Cookie 值、API Key、JWT、
> 完整會員 email、會員照片。

---

## 〇、給 Cursor 的使用方式

### 0.1 先建規則檔（做一次就好）

在專案根目錄建立 `.cursor/rules/member-data.mdc`：

```markdown
---
description: 會員資料端點修改的硬性限制
alwaysApply: true
---

修改會員資料相關程式時，以下限制違反就是錯的，寧可停下來問人：

- 不可以移除或放寬任何身分驗證
- 不可以信任請求 body / query / path 裡的 email、role 欄位來決定身分
- 不可以為了讓測試通過就回傳假資料或寫死 200
- 不可以修改既有的點數流水帳結構（delta / balance_after / reason / created_at）
- 發點、扣點一律走同一個交易，不可以分兩次寫
- 任何「是否已完成 / 是否已領取」的判斷都必須在伺服器端做，
  不可以相信前端送來的旗標
- 不確定規格就先問，不要自己猜
```

### 0.2 每個 TASK 的操作方式

用 `Ctrl+I` 開 Agent，把本檔案用 `@` 帶進 context，**一次只貼一個 TASK**：

```text
@給資料庫端_待修清單_Cursor版_2026-07-22.md

這是外部 Gateway 對本服務的接口契約，本服務目前不符合。

請只做第二節的 TASK 0，不要動其他 TASK 的範圍。
做完後：
1. 說明你改了哪些檔案、改了什麼
2. 執行該 TASK 底下的「驗收」並貼出結果
3. 停下來等我確認
```

### 0.3 注意事項

| 事項 | 說明 |
|---|---|
| **TASK 3、TASK 4 用 Ask 模式** | 這兩個是盤點與查證，不該改程式。Agent 模式會忍不住順手改 |
| **不要按 Accept All** | 逐檔看 diff。特別注意它有沒有把驗證整行刪掉 |
| **TASK 0 最急** | 它擋住整個妝容收藏功能，其他都可以排後面 |

---

## 一、先講不用做的（實測證明沒問題）

**這三項先前的文件有提到或暗示過，實測後確認不需要動，請跳過：**

| 項目 | 實測結果 |
|---|---|
| `saved_looks.before_image_url` 接受空字串 | ✅ 送 `""` 進去回 `201`。但**它是 NOT NULL** —— 送 `null` 會 500（見 TASK 12）。我們送 `""` 所以不受影響 |
| 中文編碼 | ✅ **正常**。送 `自然裸妝` 原樣存回。先前看到 `?????` 是我們客戶端的編碼問題，不是你們的 |
| 點數流水帳結構 | ✅ **設計良好**。`delta` / `balance_after` / `reason` / `created_at` 的 append-only 帳本，`balance` 與 `lifetime` 分離，這是標準做法。**請不要重構它** |

登入 Session 契約（前一份 Cursor 版文件的內容）也已經實測全部通過，`/api/login` →
`Set-Cookie` → `/api/members/{email}` 這條鏈路完整可用，那份可以結案。

---

## 二、要修的（照急迫度）

### TASK -1｜🔴🔴 **最優先**：正式環境開著 Flask 除錯模式

**這一項請排在所有其他 TASK 之前。**

2026-07-23 我們在測試欄位行為時，讓 `POST /api/members/{email}/saved-looks` 觸發了一個
資料庫錯誤。回來的**不是 JSON 錯誤，是完整的 Werkzeug 除錯頁**：

```
sqlalchemy.exc.IntegrityError: (psycopg2.errors.NotNullViolation)
  null value in column "before_image_url" of relation "saved_looks"
[SQL: INSERT INTO saved_looks (member_email, style, before_image_url,
       after_image_url, analysis_summary) VALUES (...)]
[parameters: {'member_email': 'isachen2277@gmail.com', 'style': ..., ...}]

var CONSOLE_MODE = false, EVALEX = false, SECRET = "FWcNj..."
```

**洩漏了什麼**

- 完整 SQL 語句與**資料表結構**（欄位名、關聯名）
- 實際參數值，包含**會員 email**
- 伺服器上的檔案路徑與套件版本
- Werkzeug 的 `SECRET`

**為什麼特別危險**

`EVALEX = false`，所以互動式 console 目前是關的 —— **現在還不能直接遠端執行程式碼**。
但那只差一個設定值。而且你們的服務掛在**公開的 Cloudflare tunnel** 上，
任何人都連得到，也就是**任何人都能故意送壞資料觸發錯誤，然後讀這一頁**。

**要改成**

```python
# 正式環境
app.run(debug=False)          # 或者根本不要用 app.run，改用 gunicorn/uvicorn
```

並確認 `FLASK_DEBUG` / `FLASK_ENV` 環境變數沒有設成 development。

**錯誤回應請統一成 JSON**，跟你們其他端點一致：

```json
{"error": {"code": "INTERNAL_ERROR", "message": "系統暫時無法處理", "requestId": "req_..."}}
```

細節寫進伺服器 log，不要回給呼叫端。

**驗收**

```bash
# 故意送一個會違反限制的請求，回應不可以是 HTML
curl -i -b jar.txt -X POST https://<你們的網址>/api/members/<測試帳號>/saved-looks \
  -H "Content-Type: application/json" \
  -d '{"style":"x","beforeImageUrl":null,"afterImageUrl":"https://example.com/a.png","analysisSummary":{}}'
```

`Content-Type` 要是 `application/json`，body 裡不可以出現 `Traceback`、`SQL`、`sqlalchemy`。

---

### TASK 0｜🔴 放寬 `afterImageUrl` 的格式驗證

**現在的行為**

```
POST /api/members/{email}/saved-looks
{"style":"自然裸妝","beforeImageUrl":"","afterImageUrl":"/media/render/a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6"}

→ 400
{"error":{"code":"INVALID_AFTER_IMAGE_URL","message":"afterImageUrl 必須是 http(s) URL"}}
```

**為什麼要改**

2026-07-21 我們把渲染圖從「公開可讀的 GCS 網址」改成「需驗證的私有路徑」，
因為舊的公開網址等於任何人拿到連結就能看到別人的臉。改完之後 `afterImageUrl` 的形狀變了：

| 時期 | 值長什麼樣 |
|---|---|
| 舊（7/21 前） | `https://storage.googleapis.com/decorate-me-renders/temporary/xxx.png` |
| 新 | `/media/render/<32碼小寫hex>` |

**這個形狀變更當時沒有通知你們**，是我們的疏失，抱歉。你們的驗證規則還停在舊形狀，
所以每一筆收藏都被擋掉。

**要改成**

`afterImageUrl` 除了 http(s) URL，也要接受這個形式：

```
/media/render/<32碼小寫 hex>
```

正規表示式：

```python
r"^(?:https://[^/]+)?/media/render/[0-9a-f]{32}$"
```

（主機前綴可有可無 —— 我們兩種都可能送。）

**為什麼不是完整網址**

那是一條**相對路徑**，刻意的：同一條路徑在本機開發與正式站都成立，不用把網域寫死；
長度固定 38 字元，`String(500)` 綽綽有餘。它要透過 Gateway 才讀得到，
Gateway 會驗證讀取者是圖片擁有者或 admin，再換發十分鐘有效的簽名網址。

**我們目前的暫時繞法**：前端在存入時自己補上 `https://decorate-me.web.app`。
這能動，但把網域寫死進每一筆資料，網域一變舊資料就壞。**所以還是麻煩你們改。**

**驗收**

```bash
# 1. 登入取得 cookie（密碼請用你們自己的測試帳號，不要貼進任何文件）
curl -i -c jar.txt -X POST https://<你們的網址>/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"<測試帳號>","password":"<密碼>"}'

# 2. 相對路徑要能成功建立（目前會回 400）
curl -i -b jar.txt -X POST https://<你們的網址>/api/members/<測試帳號>/saved-looks \
  -H "Content-Type: application/json" \
  -d '{"style":"驗收測試","beforeImageUrl":"","afterImageUrl":"/media/render/0123456789abcdef0123456789abcdef","analysisSummary":{}}'

# 3. 絕對網址也要繼續能用（不要為了收相對路徑就把 http(s) 擋掉）
curl -i -b jar.txt -X POST https://<你們的網址>/api/members/<測試帳號>/saved-looks \
  -H "Content-Type: application/json" \
  -d '{"style":"驗收測試2","beforeImageUrl":"","afterImageUrl":"https://decorate-me.web.app/media/render/0123456789abcdef0123456789abcdef","analysisSummary":{}}'

# 4. 亂七八糟的值要繼續被擋
curl -i -b jar.txt -X POST https://<你們的網址>/api/members/<測試帳號>/saved-looks \
  -H "Content-Type: application/json" \
  -d '{"style":"應該失敗","beforeImageUrl":"","afterImageUrl":"data:image/png;base64,AAAA","analysisSummary":{}}'
```

第 2、3 步要 `201`，第 4 步要 `400`。做完記得把測試資料刪掉。

---

### TASK 1｜🔴 `/tasks` 補上 `done` 欄位

**現在回的**

```json
{ "tasks": [
  { "taskId": "first_analysis", "reward": 20, "claimed": true },
  { "taskId": "first_favorite",  "reward": 10, "claimed": true },
  { "taskId": "first_referral",  "reward": 20, "claimed": true },
  { "taskId": "daily_checkin",   "reward": 5,  "claimed": true }
] }
```

**問題**

沒有 `done`。前端的判斷是 `done: t.done !== false`，而 `undefined !== false` 的結果是
**`true`** —— 所以**每一個任務都被畫成「已完成、可領取」**。

現在剛好看不出來，因為那四筆 `claimed` 都是 `true`、按鈕本來就 disabled。
但只要出現一筆還沒領的任務，使用者就能領走一個根本沒完成的任務。

**要改成**

每一筆補上 `done`（布林值），代表這個任務的**達成條件**有沒有滿足：

```json
{ "taskId": "first_analysis", "reward": 20, "done": true, "claimed": true }
```

`done` 與 `claimed` 是兩件事：`done` 是「條件達成了」，`claimed` 是「獎勵領走了」。
只有 `done: true, claimed: false` 才該讓使用者按得下領取。

我們前端也會同步把 `t.done !== false` 改成 `t.done === true`，兩邊一起補，
才不會再有一次靠預設值矇混過去。

**驗收**

```bash
curl -s -b jar.txt https://<你們的網址>/api/members/<測試帳號>/tasks
```

每一筆都要有 `done` 欄位。

---

### TASK 2｜🔴 `daily_checkin` 每日重置

**現在的行為**

`daily_checkin` 這筆任務的 `claimed` 從 2026-07-17 之後就永遠是 `true`。
點數流水帳裡 `task_daily_checkin` 只出現過一次：

```
7/17 01:54:28  +5  task_daily_checkin     ← 只有這一筆
```

**問題**

`daily_checkin` 是**每日任務**，前端定義是 `daily: true`，每天應該重新可領。
但你們把「已領取」記成 `(會員, 任務)` 的一個布林值，**沒有日期維度**，
7/17 領過一次就永久算領過了。

使用者從 7/18 起每天的 5 點都沒拿到。

**要改成**

任務領取紀錄要能區分「哪一天領的」。建議把主鍵改成 `(member_id, task_id, claim_date)`，
每日任務用當天日期，一次性任務用固定值（例如 `NULL` 或 `1970-01-01`）。

**「今天」請以固定時區判斷（建議 UTC+8）**，不要用請求端送來的時間，
否則使用者改系統時鐘就能重複領。

**這一項也是使用者點數卡住的真正原因**，不是點數計算有問題 —— 是任務表少了一個維度。

**驗收**

```bash
# 今天第一次領：應該成功
curl -i -b jar.txt -X POST https://<你們的網址>/api/members/<測試帳號>/tasks/daily_checkin/claim
# 今天第二次領：應該失敗
curl -i -b jar.txt -X POST https://<你們的網址>/api/members/<測試帳號>/tasks/daily_checkin/claim
```

第一次 `200`，第二次要有明確錯誤（不要靜默成功、也不要重複發點）。
隔天再測一次第一次，要能再領到。

---

### TASK 3｜🟠 `claim` 有沒有檢查達成條件（Ask 模式，先查再改）

**現象**

`first_referral`（成功推薦一位好友，20 點）的 `claimed` 是 `true`，
點數帳本裡也有 `task_first_referral +20`（7/17 01:54:21）。

**但推薦碼功能在你們那邊根本還沒有**（`/api/members/{email}/referral` 回 403）。
前端那套是純瀏覽器 demo，從來沒有真的推薦成功過任何人。

所以這 20 點是在「不可能真的完成」的情況下發出去的。

**請先查明是哪一種**

1. 從舊的前端 localStorage demo 資料匯入的？
2. 還是 `claim` 端點**沒有檢查達成條件**，呼叫就給？

**如果是第 2 種，那是嚴重問題** —— 代表任何人都能直接呼叫 API 把所有任務的獎勵領一遍，
跟 TASK 1 是同一個根因：**達成與否的判斷不在伺服器端**。

注意 7/17 那四筆任務是在 **20 秒內**全部領完的（01:54:16 → 01:54:28），
這個時間密度比較像是連續呼叫 API，不像正常使用者操作。

**查完先回報，不要直接改。** 確認是第 2 種再開 TASK 修。

---

### TASK 4｜🟡 打卡基礎點數有沒有被吃掉（Ask 模式，純查證）

**現象**

點數流水帳裡的打卡紀錄：

```
7/17 01:54:36  +10  check_in
7/18 19:14:03  +10  check_in
7/19 17:28:57  +15  check_in_streak_bonus_3d     ← 沒有另外的 +10
7/20 03:36:11  +15  check_in_streak_bonus_4d     ← 沒有另外的 +10
7/21 02:18:49  +15  check_in_streak_bonus_5d     ← 沒有另外的 +10
```

7/17、7/18 有 `check_in +10`。7/19 之後**只剩 streak bonus，基礎的 +10 沒有另外記一筆**。

**請確認是哪一種**

- **A**：15 = 10 基礎 + 5 連續獎勵，合併記成一筆 → 沒問題，只是 `reason` 命名誤導，
  建議拆成兩筆或改名成 `check_in_with_streak_bonus_5d`
- **B**：有連續獎勵的那天就把基礎分吃掉了 → 少給了，要修

**順便回報連續獎勵的正確規則**：天數門檻與對應點數各是多少？
前端目前暫定 3 / 7 / 14 / 30 天，但實測第 5 天發了 15 點，跟前端的表對不起來。
**以你們的為準**，但請告訴我們正確的表，我們好對齊顯示文字。

---

### TASK 5｜🟠 未實作的端點請回 404，不要回 403

**現象**

```
GET /api/members/{email}/referral            → 403
GET /api/recommend/personal                  → 404
GET /api/admin/product-audit-logs            → 404
```

`/referral` 回 403，另外兩條真的不存在的端點回 404。

**問題**

403 代表「這條路徑存在，但你沒權限」，404 代表「沒這條路徑」。
`/referral` 回 403 讓我們無法分辨是**還沒做**還是**權限不足**，排查時只能猜。

如果你們有一個 catch-all 對未知的 `/api/members/{email}/*` 一律回 403，
那更糟 —— 所有打錯的路徑都會看起來像權限問題。

**要改成**：未實作的路徑回 `404`。

順便回覆：`/api/recommend/personal` 和 `/api/admin/product-audit-logs`
**要做還是我們從 Gateway 的白名單拿掉？**

---

### TASK 6｜🟠 登入端點可列舉已註冊帳號（issue #27）

**現象**

```
帳號存在、密碼錯 → 401  {"error":{"code":"WRONG_PASSWORD","message":"密碼錯誤"}}
帳號不存在       → 401  {"error":{"code":"USER_NOT_FOUND","message":"此帳號尚未註冊"}}
```

狀態碼一樣，但**錯誤碼不同**。拿一組假密碼逐一測 email，就能掃出哪些信箱是已註冊會員。

Gateway 這層有做遮蔽（把上游的 401/403/404 一律轉成 `INVALID_CREDENTIALS`），
但**你們的服務掛在公開 Quick Tunnel 上，不經過 Gateway 就能直接呼叫**，所以形同虛設。

**要改成**：兩種情況的**狀態碼、錯誤碼、訊息、回應時間**四項全部一致。

回應時間也算 —— 如果「帳號不存在」跳過了密碼雜湊比對而明顯比較快，一樣測得出來。
做法是不論帳號存不存在，都跑一次假的雜湊比對再回應。

**驗收**

```bash
# 兩者的 body 與狀態碼要完全相同
curl -i -X POST https://<你們的網址>/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"<一個確定存在的帳號>","password":"definitely-wrong"}'

curl -i -X POST https://<你們的網址>/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"definitely-not-registered-xyz@example.com","password":"definitely-wrong"}'
```

---

### TASK 7｜🟡 推薦碼：要做嗎？先回覆再開工

前端現有那套是純瀏覽器 demo，有三個**設計層級**的問題，不能沿用：

1. 推薦碼是 email 的雜湊值（`hash * 31 + charCodeAt`，取 6 碼）。
   知道別人 email 就能算出他的推薦碼，反過來也能爆破，還會撞碼。
2. 反查推薦人只在本機 localStorage 裡找 —— 新會員 B 填 A 的推薦碼，
   除非 A 曾在 B 那台電腦登入過，否則一律回「推薦碼不存在」。**正常情境必定失敗。**
3. 就算成功，點數是加在 B 的瀏覽器裡，A 換裝置永遠看不到。

**如果要做**，需要：

- 會員資料表加 `referral_code` 欄位，**隨機產生**（不可從 email 推算），唯一索引。
  建議 8 碼、排除易混淆字元（`0/O`、`1/I/l`），例如 `K7M9XQ4T`
- `GET  /api/members/{email}/referral` → `{"code":"K7M9XQ4T","referredCount":3,"rewardPoints":50}`
- `POST /api/members/{email}/referral/apply` body `{"code":"K7M9XQ4T"}`

必須由**伺服器端**擋掉的情況：

| 情況 | 期望 |
|---|---|
| 碼不存在 | `ok:false`，但**註冊本身仍要成功**，只是不發獎勵 |
| 用自己的碼 | `ok:false` |
| 同一帳號套用第二次 | `ok:false` |

⚠️ **防刷警告**：搭配 issue #7（OTP 沒有真正強制，可用任意 email 註冊），
現在等於「隨便填 email 就能自己推薦自己賺 50 點」。
**建議 OTP 真正強制之後再開放推薦碼發點**，否則上線就是送分。

**如果現在人手不夠，這一項可以整個延後** —— 前端會把那張卡片收起來。
但**請不要維持現狀**：現在畫面上有一組看起來能用、實際永遠失敗的推薦碼，比沒有更糟。

---

### TASK 8｜🟢 長期：請退出公開 Quick Tunnel

你們現在掛在 `*.trycloudflare.com` 的 quick tunnel 上，這一件事同時造成三個問題：

1. **網址會換**，換了我們就連不上。歷史上已經換過至少五次，
   每次都表現成「商品載入中卡住」或「登入打不通」，我們要花時間才確認不是程式壞掉。
2. **公開可直連**，任何人都能繞過 Gateway 直接打你們的 API，TASK 6 的遮蔽因此失效。
3. **換址時沒有通知**，我們都是壞了才發現。

建議改成固定網址（Cloud Run 或付費 ngrok 都可以）。**這一件事解掉三個問題。**

在那之前，**換網址時麻煩通知我們一聲** —— 我們要同步更新 Cloud Run 上
`ai-gateway` 的 `MEMBER_DATABASE_URL` 環境變數。

---

### TASK 9｜🟡 三條商品端點：要做還是我們移除？

**先講好消息**：`/api/products` 給的資料**比我們以為的完整**。每一筆都有：

```json
{ "hex_primary": "#b40000",
  "lab": [37.3, 61.68, 51.72],
  "color_vector": [...12 維...],
  "qdrant_vector_12d": [...12 維...] }
```

我們前端的註解一直寫著「商品清單 API 沒有 hex，只有單品詳情 API 才有」——**那是錯的**，
資料一直都在，只是我們沒接上。已經修好，不用你們做任何事。

**接著是問題**：前端呼叫的三條端點，在你們那邊都是 404。

| 端點 | 上游實測 | 我們原本想用它做什麼 |
|---|---|---|
| `GET /api/product/{type}/{id}` | `404` | 抓單品的 hex / lab |
| `GET /api/recommend/{type}/{id}` | `404` | 以色找色（相似色號） |
| `POST /recommend-products` | `404` | 商品推薦 |

第三條特別提一下：**它在我們 Gateway 的白名單裡**（`PUBLIC_PRODUCT_PATHS`），
但上游沒有，是條從來沒生效過的死規則。

**重點：這三條我們現在都不需要了。**

- 單品 hex / lab → 清單 API 就有，已改用清單資料
- 以色找色 → 已改成在瀏覽器端用 `lab` 算 CIE94 色差。1041 筆的計算是微秒級，
  不打 API、不用等你們，而且效果驗證過了（開架唇釉對到 MAC Velvet Teddy，ΔE 0.71）

**所以請回覆一句就好：這三條是「規劃中要做」還是「不會做」？**

- 不會做 → 我們把 `recommend-products` 從 Gateway 白名單移除，前端也清掉相關程式
- 要做 → 告訴我們大概時程，我們先不動，避免之後重工

**請不要為了配合我們特地去做這三條。** 我們已經有可用的解法了。

---

### TASK 10｜🟡 `shade_name` 1040 筆全空

這一項在 `給資料庫端_商品資料現況與需求_2026-07-20.md` 第 2.4 節問過，這裡補上**用途**，
因為現在它有實際價值了。

**現況**：1040 筆商品的 `shade_name` 全部是空字串。

**為什麼現在變得有用**：以色找色已經上線，使用者看一支唇彩會看到三支最接近的色號。
目前只能顯示色塊和 hex 碼：

```
■ #90504a    ΔE 0.71
```

如果有色號名稱，就能顯示成：

```
■ 617 Velvet Teddy 泰迪甜莓
```

**差別很大** —— 使用者是靠色號名稱在記化妝品的，`#90504a` 對他們沒有意義。

**要確認的**：爬蟲的原始資料裡有沒有色號／色名？

- **有** → 麻煩回填 `shade_name`
- **沒有** → 我們就從 `name` 欄位自己剖析（目前商品名稱長這樣：
  `MACximal極自我柔霧唇膏 - 617 Velvet Teddy 泰迪甜莓`，破折號後面就是色號名，
  剖析得出來，只是不如你們直接給乾淨）

回一句有或沒有就好。

---

### TASK 11｜🟠 購物車要進資料庫（照收藏那套做就好）

**現況**：購物車是**純瀏覽器 localStorage**，一行 API 都沒有。

```js
const Cart = { _key: 'beautyCart', list() { return JSON.parse(localStorage.getItem(this._key) || '[]'); }, ... };
```

**為什麼要進資料庫，兩個理由**

1. **換裝置就沒了。** 使用者在手機加了五樣東西，換電腦登入看到空的。
   收藏已經解決了這件事（伺服器有存），購物車還沒有。

2. **收藏和購物車都是推薦演算法的輸入。** 這一點比第一點重要。
   加入購物車代表的購買意願**比收藏更強**，是很有價值的訊號。
   但它現在只寫進使用者自己的瀏覽器 —— **一千個人把同一支唇膏加進購物車，
   伺服器端看到的仍然是 0**。這個訊號目前 100% 遺失。

   我們前端的 `popularity` 欄位是這樣取的：

   ```js
   popularity: product.popularity ?? product.sales ?? product.views
            ?? product.reviews ?? product.favorite_count ?? product.score ?? 0
   ```

   其中只有 `favorite_count` 是我們真的在寫的。`sales` / `views` / `reviews`
   我們沒有任何管道產生。加上購物車之後，熱度才有第二個真實訊號。

**要的東西：照收藏那套複製一份就好**

收藏現在的形狀是可用的，購物車照抄，差別只是多一個數量：

```
GET  /api/members/{email}/cart
→ { "cart": [ { "item_id": 2656, "item_type": "lipsticks", "qty": 2 } ] }

POST /api/cart/set
     { "item_id": 2656, "item_type": "lipsticks", "qty": 2 }
→ { "ok": true }
```

- `qty: 0` 代表移除，不需要另外開 DELETE
- `(member, item_type, item_id)` 唯一，重複 POST 就是覆寫數量
- 身分一律從 session 判斷，**不要相信 body 裡的任何 email 欄位**

**明確不要做的**

- ❌ **不要做訂單、結帳、金流。** 這個站沒有金流（付費是人工審核升級），
  購物車在這裡是「意願標記」，不是交易的前一步。
- ❌ 不要做購物車過期／自動清空。
- ❌ 不要為了熱度另外做一張事件表 —— 對這張購物車表做聚合就夠了，
  多一張表就多一份要對帳的資料。
- ❌ **不要為了省容量改存商品名稱。**

**關於容量：請存 `(item_type, item_id)`，不要存商品名稱**

這一點特別寫出來，因為「存名稱比較省」是很直覺但相反的判斷：

```
存 id  ： member_id + item_type(~10 bytes) + item_id(4) + qty(2)        ≈ 20~40 bytes/列
存名稱 ： "MACximal極自我柔霧唇膏 - 617 Velvet Teddy 泰迪甜莓"           ≈ 90 bytes
```

中文在 UTF-8 是每字 3 bytes，**名稱大約是 id 的三倍**。

而且總量本來就微不足道。就算 1000 個會員每人放 20 樣：

```
20,000 列 × 40 bytes ≈ 800 KB
```

不到 1 MB —— 比你們資料庫裡任何一張商品圖都小。

**更重要的是名稱會壞：**

1. **名稱會變。** 爬蟲重抓就可能改字，那筆紀錄就對不回任何商品了。
2. **名稱不唯一。** 同支唇膏不同色號常常只差破折號後面那段，而 `shade_name` 目前全空。
3. **熱度算不出來。** 熱度要 `GROUP BY (item_type, item_id)`，用名稱就得做字串比對，
   一個空格或全形半形差異就會被算成兩個商品。
4. **`id` 本來就重號。** 你們知道 `id=1` 對應 7 個不同商品 —— 所以真正的鍵是
   `(type, id)`，這正是上面欄位組合的原因。單獨用 `item_id` 會對到錯的商品。

**我們這邊的狀態**：前端**還沒寫**對接程式，等你們確認端點形狀之後才寫。

這是刻意的。今天才發現前端有一批程式在呼叫 `/api/product/{type}/{id}` 和
`/api/recommend/{type}/{id}`——那兩條上游從來不存在，程式寫了、部署了、
每次都 404，但因為失敗被靜默吞掉，沒有人發現。**先講好契約再寫程式**，
不要再有第二批對著不存在的端點講話的程式。

---

### TASK 12｜🟡 `before_image_url` 請改成允許 null

實測：送 `""` 可以建立，送 `null` 會 500（那個 500 就是 TASK -1 那個除錯頁）。
也就是這個欄位目前是 **NOT NULL**。

我們前端送的是空字串，所以現在沒踩到。但這個限制沒有道理：
**「這筆收藏沒有妝前圖」是正常狀態，不是錯誤** ——
舊的收藏、或渲染時原圖上傳失敗的情況，都會是這樣。

用 `""` 表示「沒有」會讓查詢變麻煩（要同時判斷 null 與空字串），
而且任何一個呼叫端「順手」把空字串改成 null 就會 500。

```sql
ALTER TABLE saved_looks ALTER COLUMN before_image_url DROP NOT NULL;
```

改完之後我們前端會改送 `null`，語意比較清楚。改之前我們維持送 `""`。

---

## 三、優先順序總表

| TASK | 內容 | 急迫度 | 阻擋了什麼 |
|---|---|---|---|
| 0 | 放寬 `afterImageUrl` 驗證 | 🔴 最急 | 妝容收藏整個功能 |
| 1 | `/tasks` 補 `done` | 🔴 | 使用者可領取未完成的任務 |
| 2 | `daily_checkin` 每日重置 | 🔴 | 每天 5 點領不到 |
| 3 | 查 `claim` 有無檢查條件 | 🟠 | 可能是任務獎勵可被任意領取 |
| 4 | 查打卡基礎分 | 🟡 | 可能少發點數 |
| 5 | 未實作端點回 404 | 🟠 | 排查困難 |
| 6 | 登入端點帳號列舉 | 🟠 | 資安 |
| 7 | 推薦碼 | 🟡 | 功能缺失（可延後） |
| **-1** | **正式環境開著 Flask 除錯模式** | 🔴🔴 | **洩漏 SQL、資料表結構、會員 email、SECRET；公開可觸發** |
| 11 | 購物車進資料庫 | 🟠 | 換裝置就沒了；推薦演算法少一個訊號 |
| 9 | 三條商品端點的去留 | 🟡 | **只需回一句話**，不用寫程式 |
| 10 | `shade_name` 全空 | 🟡 | **只需回一句話**，相似色號顯示不了色號名 |
| 12 | `before_image_url` 允許 null | 🟡 | 一行 ALTER TABLE |
| 8 | 離開公開 tunnel | 🟢 | 長期穩定性與資安 |

**TASK 0、1、2 做完，會員端就完整可用了。** 其餘可以排在後面。

**TASK 9 與 10 只要回答，不用動工。** 先回這兩題可以讓我們把死程式清掉。

---

## 四、需要你們回覆的

1. **TASK -1 什麼時候可以關掉除錯模式？這一項請最優先。**
2. TASK 0 什麼時候可以改好？（這個擋住整個收藏功能）
2. TASK 3 查證結果：`first_referral` 那 20 點是怎麼發出去的？
3. TASK 4 查證結果：打卡基礎 10 點有沒有被吃掉？連續獎勵的正確天數／點數表？
4. TASK 5：`/api/recommend/personal`、`/api/admin/product-audit-logs` 要做還是拿掉？
5. TASK 7：推薦碼要做嗎？大概什麼時候？
6. TASK 9：`/api/product/{type}/{id}`、`/api/recommend/{type}/{id}`、`recommend-products`
   這三條是規劃中要做，還是不會做？（**不用為我們特地做**，我們已有解法）
7. TASK 10：爬蟲原始資料裡有沒有色號名稱？有的話能回填 `shade_name` 嗎？
8. TASK 11：購物車那兩支端點大概什麼時候能給？形狀照收藏複製就好。
   我們前端等你們確認後才開始寫。
9. 有沒有專用的 **demo 測試帳號**可以給我們？
   （這次驗證是用真實會員帳號跑的，長期不該這樣）

第 6、7 題只要回一句話，卻能讓我們決定要不要清掉一批死程式，麻煩優先回。

有問題直接找我們討論，不用等文件往返。
