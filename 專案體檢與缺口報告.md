# Decorate Me 專案體檢與缺口報告

> 初版產出：2026-07-08　｜　**本次更新：2026-07-09（逐項實測/讀碼核對）**
> 範圍：前端、後台、臉部分析後端、AI 渲染、整合現況
> 說明：標「✅ 確認」是直接讀原始碼/實測到的；標「⚠️ 建議查證」是依架構推論，值得你確認的。
> 圖例：**✅ 已修**（本輪核對已解決）／**🟠 仍待**／**🟡 次要**。

---

## 07-09 更新摘要：原報告 5 大項的最新狀態

| # | 原問題 | 07-09 狀態 |
|---|--------|-----------|
| 1 | 渲染服務可被無限呼叫燒錢（A-1）| **✅ 已修**：`/render` 已加 `RENDER_API_KEY` + 每 IP rate limit + 精確 CORS（需查證 Cloud Run env 有設 key）|
| 2 | 會員個資全公開（A-2）| **✅ 已修**：實測匿名 `401`、非 admin `403`，不再外洩 |
| 3 | 前端 repo 含 API 金鑰明碼（A-3）| **🟠 部分**：`config.local.js` 已移出版控，但金鑰仍留在 git 歷史 → 需輪換 |
| 4 | 渲染人臉圖公開網址（A-4）| **🟠 仍待**：維持公開 bucket，屬設計取捨（見下） |
| 5 | 註冊 OTP 假的（B-1）| **🟠 轉移**：前端 fallback 已預設關閉；但**後端未強制 OTP**，可直接打 API 跳過（見新報告 `MEMBER_DATABASE_SECURITY_ISSUE_OTP.md`）|

### 本輪另外修掉的（原報告沒有、這幾天冒出來的）
- **✅ 後台管理中台 TDZ 崩潰**：`router.js` 的 `loadAdminMembers()` 在 `render`（const）初始化前被呼叫，整個後台載入炸掉、members 讀不到。已把呼叫移到 `render` 定義後。
- **✅ 商品圖片全部不顯示**：API 圖片欄位是 `image_src`，前端 `_normalizeProduct` 沒認這個名字。已補進 fallback 鏈。
- **✅ 後台寫入串接已實測打通**：用資料庫 admin 帳號實測 `login → GET /api/members` 回 `200` + 完整清單；CORS/session cookie（`SameSite=None; Secure; HttpOnly`）皆正確。

---

## A. 資安漏洞

### A-1. 渲染服務保護 ✅ 已修（07-09 正式環境實測確認）
- 原問題：`allow_origins=["*"]`、`/render` 無 API key、可被無限呼叫燒 Replicate 錢。
- 現況（`replicate_render_api.py`）：`/render` 已掛 `require_api_key`（`RENDER_API_KEY`）+ `enforce_render_rate_limit`（預設每 3600 秒 10 次）+ `allow_origins=get_cors_origins()`（不再 `*`）。超量回 `429 RATE_LIMITED`。
- 實測 Cloud Run `/health`：`api_key_required: true`、`token_configured: true`、`storage_configured: true`；不帶 `X-API-Key` 打 `/render` → `401`。**保護已在正式環境生效**，env key 有設對。

### A-2. 會員個資公開 ✅ 已修（組員端）
- `GET /api/members` 現在匿名回 `401 AUTH_REQUIRED`、非 admin 會員回 `403 FORBIDDEN`，admin session 才回 `200`。實測 07-09 通過。原始回報見 `MEMBER_DATABASE_SECURITY_ISSUE.md`。

### A-3. 前端金鑰明碼 🟠 部分修（金鑰待輪換）
- `config.local.js` 已 **移出版控**（前端 `.gitignore` 有列、`git ls-files` 確認未追蹤），改用 `config.local.example.js` 範本 ✅。
- 文字建議的歷史金鑰曾存在舊 commit。實際值不得寫入文件；Git 歷史無法只靠刪除目前檔案清除，因此該秘密一律視同曾外洩並完成輪替。
- 建議：請 Ollama 組員**換一把新 key**，前端更新 `config.local.js`（本機檔，不進版控）。

### A-4. 渲染人臉圖公開可讀 🟠 仍待（設計取捨）
- GCS bucket `decorate-me-renders` 為公開讀（`allUsers: objectViewer`），渲染結果網址任何人有連結就能看。隨機檔名降低枚舉風險但仍是「公開」。
- 取捨：公開網址讓前端顯示與收藏永久連結最簡單。若要私有化需改「私有 bucket + 短期簽名 URL」，收藏永久連結會變複雜。畢業專題可接受，但口試被問「使用者隱私」要能答出這個取捨。

### A-5. Cloud Run 服務公開端點 🟡 大致收斂（建議查證 env）
- face-basic / face-pro 依最近 commit（`1f3f63d Face services: require X-API-Key on analysis endpoints`）已要求 `X-API-Key`。與 render 同樣屬「有設 env key 才擋」，請一併確認 Cloud Run 已設對應金鑰。
- 服務本身仍以 `--allow-unauthenticated` 部署（靠應用層 key 擋），展示階段可接受。

### A-6. 前端 admin email fallback ✅ 已修
- `api.js` `AdminStore.isAdminProfile()` 現在只判斷 `role === 'admin' || level === '管理員'`，**已無 `email.startsWith('admin@')` 後門**。

---

## B. 正確性問題

### B-1. 註冊 OTP ✅ 前端已收斂 / 🟠 後端未強制（風險轉移）
- 前端：`verifyOtpWithOptionalBypass()` 現在預設**不放行**——`Api.verifyOTP()` 失敗就直接報錯中止，只有在明確開 `window.DECORATE_ME_CONFIG.allowInsecureOtpBypass` 時才走「長度≥4」的舊 bypass（正式 `config.local.js` 沒開）✅。
- **但後端沒有真正強制 OTP**：07-09 實測「註冊 → 不驗 OTP → 直接 `POST /api/login`」回 `200`。也就是任何人直接打 API 就能跳過 OTP、用假 email 建可用帳號。
- 這是後端該補的，前端補不了。已寫成 `MEMBER_DATABASE_SECURITY_ISSUE_OTP.md`（建議：帳號建成 pending、`verifyOTP` 過才 active、未驗證的 `login` 一律擋 `403 EMAIL_NOT_VERIFIED`）。

### B-2. 舊收藏臨時網址會失效 🟡 仍待
- GCS 永久儲存上線前收藏的圖存的是 `replicate.delivery` 臨時網址，會過期。只有新渲染的是永久網址。可在收藏頁對 `replicate.delivery` 開頭的圖顯示「此圖可能已失效」提示。

### B-3. 登入多打一次無效請求 ✅ 實質解除
- `login()` 仍是「先帶 `credentials:'include'`，被擋才退回不帶」。但組員 CORS 已補 `Allow-Credentials: true`（A-2 一併驗證），第一次就會成功，不再觸發第二次白打請求。程式碼結構可留，行為已無損耗。

### B-4. 收藏 data URL 可能過大 🟡 仍待（待你決策）
- 收藏時 `beforeImageUrl` 可能是數百 KB base64，每人上限 50 筆 → 單一會員可能吃十幾 MB。規格已提醒組員，值得你決定「是否只存渲染後那張」。

---

## C. 成本與效能

- **C-1** render 現已有伺服器端 rate limit（見 A-1），外部費用風險大幅下降；`guidance` 由 `RENDER_GUIDANCE` env 控制（現 4.5）。
- **C-2** 臉部分析模型有快取 getter（`_get_insight`/`_get_face_mesh`），OK ✅。
- **C-3** 商品頁一次抓全量（約 499 筆）目前可接受；成長到數千筆再改分頁。

---

## D. 技術債（不急，但會越拖越痛）

- **D-1** `router.js` 3000+ 行、`api.js` 也大，建議按模組拆檔（auth / admin / product / member-rewards）。本輪的 TDZ 崩潰就是巨檔難維護的徵兆。
- **D-2** 會員成長系統（點數/簽到/任務/推薦/等級/PRO訂閱）仍全是前端 localStorage，跨裝置失效，須接資料庫（見 `00_資料庫整合_請先讀我_READ_FIRST.md`）。
- **D-3** 渲染次數配額仍存前端 localStorage，應移到後端（render 已有 IP rate limit，但非「綁會員」配額）。
- **D-4** 無自動化測試 / CI；靠 `frontend_smoke_check.js`、`backend_smoke_test.py` 手動跑。展示前建議接成 git pre-push 或 GitHub Action。
- **D-5** 本機 `.venv` 裝不了 `google-cloud-storage`（protobuf 與 mediapipe 衝突），render 的 GCS 上傳只能在 Cloud Run 測。

---

## E. 專案還缺什麼（功能與整合缺口）

### 等組員資料庫端（見 `00_資料庫整合_請先讀我_READ_FIRST.md`）
- [x] ~~`GET /api/members` 補權限~~ ✅ 已修
- [x] ~~CORS 補 `Allow-Credentials` + cookie `SameSite=None; Secure`~~ ✅ 已修
- [ ] `/api/register`：欄位問題已釐清（非 csrf，是欄位不齊，前端已修）；**OTP 後端強制**仍待（見 `MEMBER_DATABASE_SECURITY_ISSUE_OTP.md`）
- [ ] `saved_looks` 表 + 端點（實測 `405→403`，端點疑似已建、待用 session 驗證讀寫）
- [ ] 會員成長系統端點：簽到、連續簽到、推薦碼、任務、點數商店、累計點數欄位（銀卡/金卡）
- [ ] 商品推薦 `style` 參數要真正影響結果
- [ ] 提供一組 `role:admin` 測試帳號正式化（目前用 `admin@decorateme.local`，已可讀寫）

### 等 Ollama 組員（已交 `OLLAMA_RENDER_PROMPT_FIX_RECOMMENDATION.md`）
- [ ] `renderPromptEn` 內容品質（目前空洞，導致渲染妝效不明顯）
- [ ] 輪換 `textSuggestionApiKey`（A-3，已進過 git 歷史）

### 我方（前端/後端）自己還能做
- [x] ~~render 服務加 API key 保護~~ ✅ 已修（查證 Cloud Run env）
- [x] ~~`config.local.js` 移出版控~~ ✅ 已修
- [x] ~~拿掉 `admin@` email 前端 fallback~~ ✅ 已修
- [x] ~~後台管理中台 TDZ 崩潰~~ ✅ 已修
- [x] ~~商品圖片不顯示（`image_src`）~~ ✅ 已修
- [ ] PRO 側臉鼻型分類：資料夾已建，等側臉照片（每類 50 張）
- [ ] RF 分類模型：pipeline 已通，等標註資料重訓
- [ ] 收藏妝容改接 `saved_looks`（等端點驗證）
- [ ] 分析歷史目前完全沒存——若要「歷史紀錄」功能需另設計
- [ ] （可選）後台改 token 驗證，讓擋第三方 cookie 的瀏覽器也能用

### 產品面尚未開始
- [ ] PRO 付費是 demo（無真實金流）
- [ ] 沒有錯誤監控 / log 聚合
- [ ] 沒有使用者行為分析 / 轉換追蹤

---

## 結論：建議動作順序（07-09 修訂）

1. **查證 face 服務 env key**：render 已實測確認 `api_key_required: true`；剩 face-basic/face-pro 也請同樣打 `/health` 確認金鑰有設（A-5），否則保護等於沒開。
2. **催組員收兩件資安**：後端 OTP 強制（`MEMBER_DATABASE_SECURITY_ISSUE_OTP.md`）、輪換外洩的 `textSuggestionApiKey`。
3. **驗證半成品端點**：`saved_looks`、商品 `style` 參數，判斷是「已可用」或「還要組員補」。
4. **有空再還**：技術債 D 區（尤其 D-1 拆檔、D-4 CI）、PRO 側臉資料收集。
