# 規則式閾值 Bug：完整診斷記錄（新手版）

> **這份文件在講什麼**
>
> 我們發現線上的臉部分析有一個嚴重 bug：**不管你是誰，它都說你是「彎月眉 + 桃花眼 + 標準鼻」**。
> 這份文件從頭記錄：我們怎麼發現的、怎麼查的、為什麼會這樣、怎麼修。
>
> 寫給沒有機器學習背景的人看。每個名詞第一次出現時都會解釋。

---

## 目錄

1. [先把名詞講清楚](#1-先把名詞講清楚)
2. [背景：我們原本在做什麼](#2-背景我們原本在做什麼)
3. [第一步：訓練 CNN（踩了兩個坑）](#3-第一步訓練-cnn踩了兩個坑)
4. [第二步：把 CNN 掛上線但不讓它影響使用者](#4-第二步把-cnn-掛上線但不讓它影響使用者)
5. [第三步：一個「順手做的對照」，炸出了真正的問題](#5-第三步一個順手做的對照炸出了真正的問題)
6. [第四步：診斷閾值 bug（本文重點）](#6-第四步診斷閾值-bug本文重點)
7. [為什麼會發生這種事](#7-為什麼會發生這種事)
8. [怎麼修](#8-怎麼修)
9. [這件事教我們的三件事](#9-這件事教我們的三件事)
10. [你可以自己動手跑的指令](#10-你可以自己動手跑的指令)

---

## 1. 先把名詞講清楚

看下去之前，這幾個詞會一直出現。

### 1.1 landmark（特徵點）

MediaPipe 這個工具會在人臉上標出 **468 個點**，每個點有固定編號。
例如 `第 46 號點` 永遠是左眉的外側末端、`第 234 號點` 永遠是左臉頰最外側。

有了這些點的座標，我們就可以算距離、算角度。例如「臉的寬度」就是第 234 號點到第 454 號點的距離。

### 1.2 規則式（rule-based）

**用人訂的數學公式和門檻來判斷**，不需要訓練。例如：

```
鼻子寬度 ÷ 臉寬度 ≥ 0.325  →  判定為「寬鼻」
鼻子寬度 ÷ 臉寬度 ≤ 0.205  →  判定為「窄鼻」
其他                        →  判定為「標準鼻」
```

這裡的 `0.325` 和 `0.205` 就叫做**閾值（threshold）**，也就是「門檻」。
**這份文件的整個 bug，就出在這些門檻上。**

規則式的優點是快、不用訓練、看得懂它為什麼這樣判。缺點是門檻要人去訂，而且**訂錯了不會有任何錯誤訊息**——
它會安靜地一直給出錯誤答案。

### 1.3 CNN（卷積神經網路）

**讓機器自己從大量圖片中學規則**，不需要人訂公式。你給它 1000 張標好「這是彎月眉」的圖，
它自己學會什麼叫彎月眉。

優點是能學到人寫不出來的複雜規則。缺點是需要大量資料，而且它學到什麼你看不見（黑盒子）。

### 1.4 ground truth（標準答案）

**人工標好的正確答案**。我們的 `data/basic_full/grouped/` 資料夾就是 ground truth ——
組員一張一張看過、手動分類放進資料夾的。

評估任何方法，都是拿它的答案去跟 ground truth 比對，看對幾題。

### 1.5 val set（驗證集）

我們有 1297 張標好的圖。**不能拿全部去訓練 CNN，必須留一部分當考題。**

- **訓練集（train set）**：給 CNN 看的，讓它學。（我們用 75%）
- **驗證集（val set）**：**藏起來不給它看**，訓練完拿來考它。（我們用 25%）

為什麼要藏？因為如果考題就是它看過的題目，它背下來就好，考 100 分也不代表它真的會。

### 1.6 macro accuracy（宏平均準確率）

衡量「答對幾題」的方式，但**每個類別的權重一樣**。

假設眉型有三類，考題是「一字眉 17 題、彎月眉 18 題、落尾眉 16 題」，某個方法**全部答彎月眉**：

| 算法 | 計算 | 結果 | 問題 |
|---|---|---|---|
| 普通 accuracy | 答對 18 題 ÷ 51 題 | **0.35** | 看起來還好？ |
| **macro accuracy** | (一字眉答對率 0% + 彎月眉 100% + 落尾眉 0%) ÷ 3 | **0.33** | 誠實反映它只會一招 |

**我們一律用 macro accuracy**，因為它不會被「猜最大的類別」這種偷懶策略騙到。

而且要記住一個關鍵數字：**三個類別隨機亂猜 = 0.333**。
所以如果某個方法的 macro accuracy 是 0.333，代表**它跟丟骰子一樣**。

---

## 2. 背景：我們原本在做什麼

Decorate Me 的 BASIC 功能會分析使用者的自拍，判斷五個部位：

```
臉型：鵝蛋臉 / 圓形臉 / 方形臉 / 長形臉 / 心形臉
眉型：一字眉 / 彎月眉 / 落尾眉
眼型：瞇縫眼 / 下垂眼 / 圓眼 / 丹鳳眼 / 細長眼 / 桃花眼 / 杏仁眼
鼻型：標準鼻 / 寬鼻 / 窄鼻
嘴型：厚唇 / 薄唇 / M型唇 / 微笑唇 / 花瓣唇
```

**線上目前用的是規則式**（第 1.2 節那種寫死門檻的做法），程式在
`Face_analyzer_BASIC.py` 的 `get_face_shape()` / `get_eyebrow_shape()` 等函式。

我們原本的目標很單純：**訓練一個 CNN 來取代規則式，看能不能更準。**

結果做著做著，發現了一件比「CNN 準不準」嚴重得多的事。

---

## 3. 第一步：訓練 CNN（踩了兩個坑）

### 3.1 這次的做法：只餵「部位」給 CNN，不餵整張臉

之前的版本把整張臉縮成 160×160 的小圖丟給 CNN，然後問它「鼻型是什麼」。
問題是：**鼻子在那張小圖裡只剩幾十個像素**，模型看不清楚，反而容易走捷徑去記「這個人是誰」。

這次改成：先用 landmark 把**每個部位單獨裁出來**（叫做 ROI，Region of Interest，感興趣區域），
每個部位訓練一個專屬的小模型。

```
原圖 → MediaPipe 找出 468 個點 → 依部位裁切 → 五張小圖 → 五個 CNN
                                     ├─ 臉型 128×128
                                     ├─ 眉型 96×96
                                     ├─ 眼型 96×96
                                     ├─ 鼻型 96×96
                                     └─ 嘴型 96×96
```

### 3.2 坑一：BGR / RGB 顛倒（會讓模型上線後無聲掉分）

**電腦存彩色圖片有兩種順序**：

- **RGB**：紅、綠、藍（大部分程式的標準）
- **BGR**：藍、綠、紅（OpenCV 這個函式庫的怪癖，順序是反的）

我們的裁切函式回傳 **BGR**，訓練時直接拿去用；但**線上推論**的程式會先把 BGR 轉成 RGB 再餵給模型。

結果就是：**訓練時模型看到的是藍紅顛倒的圖，上線後看到的是正常的圖。**

這種 bug 最可怕的地方是：**它不會報錯**。離線測試分數正常，一上線就默默變差，你完全不知道為什麼。

**修法**：在存快取時就統一轉成 RGB，讓訓練和上線看到同一種順序。

### 3.3 坑二：同一個人的照片同時出現在考題和練習題（資料洩漏）

我們的資料是「同一個人有好幾張照片」（不同角度、不同場景）。

如果隨機切分 train/val，**同一個人的照片會同時落在兩邊**。這樣 CNN 只要學會「認人」——
「喔這是那個誰，他是彎月眉」——就能答對，但換一個沒見過的人就完全不會。

這叫做**資料洩漏（data leakage）**，考試分數會虛高。

**修法**：用 InsightFace（人臉辨識工具）算出「哪些照片是同一個人」，切分時**整組人一起分到同一邊**。
這叫**按人切分（split by identity）**。

我們兩種切法都跑了，實測隨機切分的分數**虛高最多 +0.11**。

> **順便踩到的坑**：用 InsightFace 找人臉時，**1297 張裡有 545 張（42%）偵測不到人臉**。
> 檢查後發現：這批圖是截圖裁出來的大頭照，**臉幾乎佔滿整個畫面**。
> 人臉偵測器需要臉的周圍留一點空白才框得住，臉太大反而找不到。
>
> 解法很土但有效：**在圖片外面補一圈邊框**，等於把臉「縮小」回偵測器習慣的比例。
> 補了之後 20 張測試圖 20 張全部找回來，重跑後 1296/1297 全部成功。

### 3.4 訓練結果

| 部位 | CNN（按人切分） | 隨機猜 |
|---|---|---|
| face_shape | 0.475 | 0.200 |
| brow_shape | 0.589 | 0.333 |
| eye_shape | 0.378 | 0.143 |
| nose_shape | 0.666 | 0.333 |
| lip_shape | 0.452 | 0.200 |

**都比亂猜好，但都沒到規格書要求的 0.70。** 當時的結論是「CNN 還不夠好，不能上線」。

> **一個重要的觀察**：我把訓練回合數從 25 增加到 40，結果**三個部位變差、兩個變好，方向完全隨機**。
> 原因是 val set 只有 43~105 張，**一張題目就值 1~2 個百分點，分數的隨機波動有 ±0.1**。
> 這代表：在這個資料量下，「0.53 和 0.47 誰比較好」這種問題是沒有意義的，那個差距只是運氣。

---

## 4. 第二步：把 CNN 掛上線但不讓它影響使用者

因為 CNN 沒到門檻，不能直接讓使用者看到它的答案。所以用一種叫 **shadow mode（影子模式）** 的做法：

```
使用者上傳照片
   ↓
規則式判斷  ──→ 這個答案回給使用者（跟以前完全一樣）
   ↓
CNN 也判斷一次 ──→ 這個答案「只寫進 log」，使用者看不到
   ↓
把兩者的差異記錄下來，累積比較資料
```

**好處**：模型在真實流量上跑起來了，可以累積「規則式 vs CNN 誰對誰錯」的資料，
但使用者的體驗**完全不受影響**——他看到的答案一個字都沒變。

（部署過程踩的三個坑寫在 `CNN訓練歷程_BASIC五官分類.md`，這裡不重複。）

---

## 5. 第三步：一個「順手做的對照」，炸出了真正的問題

做完 shadow 之後，有一個問題一直沒有答案：

> **CNN 的 0.475 到底是好還是壞？**

這個問題**沒辦法單看 CNN 的分數回答**。因為：

- 如果規則式在同一批考題上只有 **0.30**，那 CNN 的 0.475 是**大幅進步**。
- 如果規則式有 **0.60**，那 CNN 的 0.475 是**退步**。

所以我們必須讓**規則式考同一份考卷**。這叫做 **baseline（基準線）對照**。

> **關鍵：一定要是「同一份考卷」。** 如果 CNN 考的是 A 卷、規則式考的是 B 卷，
> 分數再漂亮也不能比。所以評估腳本 `eval_rule_baseline.py` 直接 import 訓練腳本的切分函式，
> 用**同一個亂數種子（seed=42）**重算出**完全一樣的 val set**。

結果出來，所有人都愣住了：

| 部位 | **規則式** | CNN | **隨機亂猜** |
|---|---|---|---|
| face_shape | 0.326 | 0.475 | 0.200 |
| brow_shape | **0.333** | 0.589 | **0.333** |
| eye_shape | **0.143** | 0.378 | **0.143** |
| nose_shape | 0.354 | 0.666 | 0.333 |
| lip_shape | 0.331 | 0.452 | 0.200 |

**注意看粗體的地方：**

- 眉型的規則式是 **0.333**，隨機亂猜也是 **0.333**。
- 眼型的規則式是 **0.143**，隨機亂猜也是 **0.143**。

**分數精準等於亂猜。** 這種巧合只有一個解釋：**它根本沒有在分類。**

查它的答案分佈，果然：

| 部位 | 規則式在 val set 上的答案分佈 |
|---|---|
| **眉型** | **51 張考題，51 張全部答「彎月眉」** |
| **眼型** | 105 張裡 **101 張答「桃花眼」**（96%） |
| **鼻型** | 43 張裡 **42 張答「標準鼻」**（98%） |
| 臉型 | 只出現 3 種答案，**從不輸出「心形臉」「長形臉」** |
| 嘴型 | 只出現 3 種答案，**從不輸出「M型唇」「薄唇」** |

而且這不只是離線測試的現象。我拿不同人的照片打**線上正式 API**：

```
第 1 人   臉型=鵝蛋臉   眉型=彎月眉   眼型=桃花眼   鼻型=標準鼻
第 2 人   臉型=方形臉   眉型=彎月眉   眼型=細長眼   鼻型=標準鼻
第 3 人   臉型=鵝蛋臉   眉型=彎月眉   眼型=桃花眼   鼻型=標準鼻
```

**線上每一位使用者，都被告知自己是「彎月眉 + 標準鼻」。**

這不是「準確率不夠高」的問題。這是**產品實質上壞掉了**，而且沒有人發現，因為它看起來很正常——
它確實回傳了一個專業的答案，只是**對每個人都給同一個答案**。

---

## 6. 第四步：診斷閾值 bug（本文重點）

### 6.1 診斷的思路

規則式的程式碼長這樣（以鼻型為例，最單純）：

```python
ratio_width = 鼻子寬度 / 臉寬度

if ratio_width >= 0.325: return "寬鼻"
if ratio_width <= 0.205: return "窄鼻"
return "標準鼻"                        # ← 以上都不符合就走這裡
```

98% 的人被判成「標準鼻」，代表：**幾乎沒有人符合前兩個條件**，全部掉進最後一行。

那麼問題就變成：**`ratio_width` 這個數字在真實資料上到底是多少？**

這就是診斷的核心思路 —— **不要猜，把數字印出來看**。

### 6.2 診斷方法：印出「特徵值的分佈」

我們對每一張人工標好的圖，算出它的 `ratio_width`，然後**按照人工標籤分組**，看每一組的數字範圍。

腳本是 `tools/diagnose_rule_thresholds.py`。結果：

#### 鼻型：`ratio_width`（鼻寬 ÷ 臉寬）

| 人工標的真實類別 | 張數 | 最小 | 25% | **中位數** | 75% | 最大 |
|---|---|---|---|---|---|---|
| 寬鼻 | 61 | 0.265 | 0.303 | **0.310** | 0.320 | 0.353 |
| 標準鼻 | 30 | 0.260 | 0.276 | **0.283** | 0.294 | 0.309 |
| 窄鼻 | 65 | 0.259 | 0.283 | **0.291** | 0.298 | 0.321 |

**全部資料的範圍是 0.259 ~ 0.353。**

現在把門檻放上去看：

| 程式裡的條件 | 判定 |
|---|---|
| 寬鼻需 `>= 0.325` | 只有 **4%** 的資料符合 |
| 窄鼻需 `<= 0.205` | **死碼！門檻 0.205 比全部資料的最小值 0.259 還小 —— 這一行永遠不會執行** |

**真相大白：**
「窄鼻」這個分支是**死程式碼**，它永遠不可能被觸發，因為**沒有任何一個人的鼻寬比例會低於 0.205**。
而「寬鼻」的門檻又設得太高（只有 4% 的人達標）。
所以 96% 的人掉進最後一行 → **標準鼻**。

### 6.3 三個部位的完整診斷結果

#### 眉型 brow_shape

程式碼：
```python
if tail_ratio > 0.100:                             return "落尾眉"
if arch_ratio < 0.115 and abs(tail_ratio) < 0.080: return "一字眉"
if arch_ratio > 0.155 and abs(tail_ratio) < 0.115: return "彎月眉"
return "彎月眉"        # ← fallback（保底答案）也是彎月眉
```

| 條件 | 門檻 | 資料實際範圍 | 判定 |
|---|---|---|---|
| 落尾眉需 `tail_ratio > 0.100` | 0.100 | **最大只到 0.090** | **死碼，永遠不成立** |
| 一字眉需 `arch_ratio < 0.115` | 0.115 | **最小只到 0.138** | **死碼，永遠不成立** |

**兩個門檻都設在資料範圍之外。** 落尾眉的門檻比所有人的最大值還高，一字眉的門檻比所有人的最小值還低。

剩下的每一個人，不是走第三條（彎月眉），就是走最後的 fallback（**也是彎月眉**）。
**結果就是 100% 彎月眉。**

#### 眼型 eye_shape

程式碼：
```python
if ratio_to_face < 0.12: return "瞇縫眼"
elif angle > 6:          return "下垂眼"
elif ear > 0.40:         return "圓眼"
elif ear < 0.20:         return "細長眼"
elif ear <= 0.24:        return "丹鳳眼" if angle < -2 else "細長眼"
elif ear <= 0.35:
    if angle < -1 and ratio_to_face > 0.16: return "桃花眼"     # ← 96% 的人走這裡
    elif angle > 3:                          return "下垂眼"
    else:                                    return "杏仁眼"
else:
    return "桃花眼" if angle < -1 else "圓眼"
```

| 條件 | 門檻 | 資料實際範圍 | 判定 |
|---|---|---|---|
| 瞇縫眼需 `ratio_to_face < 0.12` | 0.12 | 最小 0.194 | **死碼，永遠不成立** |
| 下垂眼需 `angle > 6` | 6.0 | 最大 0.603 | **死碼，永遠不成立** |
| 圓眼需 `ear > 0.40` | 0.40 | 最大 0.392 | **死碼，永遠不成立** |
| 桃花眼需 `ratio_to_face > 0.16` | 0.16 | 最小 0.194 | **恆真！所有人都符合** |
| 桃花眼需 `angle < -1` | −1.0 | — | **98% 的人符合** |

**三個死碼 + 一個恆真條件。**

所以：87% 的人落在 `ear <= 0.35` 這個分支，進去之後 `angle < -1`（98% 成立）
且 `ratio_to_face > 0.16`（**100% 成立**）→ **全部判成桃花眼**。

> **這裡我自己也差點搞錯，值得寫下來。**
>
> 「門檻比資料最小值還低」這件事，意思完全取決於比較方向：
> - 條件是 `< 0.12`，而資料最小是 0.194 → **永遠不成立**（死碼）
> - 條件是 `> 0.16`，而資料最小是 0.194 → **永遠成立**（恆真）
>
> 兩者**都是 bug，但意義相反**：一個是「這條路沒人走」，一個是「這條路每個人都走」。
> 我第一版診斷腳本把兩者混為一談，標錯了。**寫診斷工具時，比較方向不能省。**

#### 鼻型 nose_shape

見 6.2 節。一個死碼（窄鼻）+ 一個過高的門檻（寬鼻）→ 98% 標準鼻。

### 6.4 統計總表

| 部位 | 死碼條件 | 恆真條件 | 結果 |
|---|---|---|---|
| 眉型 | 2 個 | 0 | 100% 彎月眉 |
| 眼型 | 3 個 | 1 個 | 96% 桃花眼 |
| 鼻型 | 1 個 | 0 | 98% 標準鼻 |

---

## 7. 為什麼會發生這種事

**答案就寫在程式碼的註解裡：**

```python
# 閾值以 CelebA 100 張分位數校正
```

**CelebA 是一個以西方人臉為主的公開資料集。**
而我們的使用者、我們的訓練資料，是**亞洲人的自拍照**。

臉部比例的分佈完全不同 —— 例如亞洲人的鼻寬相對臉寬的比例普遍較大，
所以「窄鼻 ≤ 0.205」這個在 CelebA 上合理的門檻，**搬到亞洲人臉上就變成沒有人達標**。

**這就是整個 bug 的根因：門檻是在一個分佈完全不同的資料集上訂的，然後直接搬過來用。**

而且因為規則式**不會報錯**，它只是安靜地一直回答「彎月眉」，所以這個 bug 存活了很久，
直到我們做 baseline 對照才發現。

---

## 8. 怎麼修

### 8.1 第一件事：門檻要用「自己的資料」重新訂

不要從別的資料集抄門檻。**用你自己的 ground truth 算出來。**

以眉型為例，我們用 train set 找最佳切點（**注意：只能用 train set 找，不能用 val set，
否則等於偷看考題**），然後在 val set 上驗證：

```python
# 修正前（兩個死碼分支）
if tail_ratio > 0.100:                             return "落尾眉"   # 資料最大只到 0.090
if arch_ratio < 0.115 and abs(tail_ratio) < 0.080: return "一字眉"   # 資料最小只到 0.138
if arch_ratio > 0.155 and abs(tail_ratio) < 0.115: return "彎月眉"
return "彎月眉"

# 修正後（用自己資料算出來的切點）
if tail_ratio < -0.014: return "一字眉"
if tail_ratio >  0.030: return "落尾眉"
return "彎月眉"
```

效果：

| 方案 | 眉型 macro accuracy |
|---|---|
| 隨機亂猜 | 0.333 |
| **修正前（壞掉）** | **0.333** |
| **修正後** | **0.485** |
| CNN | **0.589** |

**從「完全沒在分類」變成「有在分類」，但仍然不算準。**

### 8.2 第二件事：有些特徵要直接放棄

修門檻不是萬靈丹。**有些特徵本身就沒有鑑別力，門檻怎麼調都沒用。**

怎麼判斷？我們用一個叫**分離度**的簡單指標：

```
分離度 = 各類別中位數的最大差距 ÷ 類別內部的平均標準差
```

白話說就是：**「不同類別之間的差異」有沒有大過「同一類別內部的雜訊」。**

- 分離度 **> 1**：類別間的差距大於類別內的雜訊 → 這個特徵有用
- 分離度 **< 1**：**類別內部的雜訊比類別之間的差距還大** → 這個特徵沒用，調門檻也救不了

看眉型的兩個特徵：

| 特徵 | 一字眉 | 彎月眉 | 落尾眉 | 類別間差距 | 類別內標準差 | **分離度** |
|---|---|---|---|---|---|---|
| `arch_ratio`（眉毛弓起程度） | 0.155 | 0.161 | 0.166 | 0.012 | 0.029 | **0.40** ❌ |
| `tail_ratio`（眉尾相對眉頭的高低） | −0.030 | −0.006 | 0.007 | 0.036 | 0.036 | **1.01** ⚠️ |

**`arch_ratio` 的分離度只有 0.40** —— 三個類別的中位數是 0.155 / 0.161 / 0.166，
幾乎重疊在一起，而每個類別內部的標準差卻高達 0.029（比類別間的差距大兩倍多）。

**這個特徵根本分不開三種眉型。** 硬留著它，只會製造出「永遠不成立的條件」。
所以修正後的程式碼**直接不用 `arch_ratio`**，只用 `tail_ratio`。

### 8.3 第三件事：規則式修好之後，還是輸給 CNN

| 部位 | 規則式（壞掉） | 規則式（修好） | CNN |
|---|---|---|---|
| 眉型 | 0.333 | 0.485 | **0.589** |

所以最終方案是**兩個一起用**（規格書 13.2 節的 hybrid 做法）：

```
CNN 的信心夠高  →  用 CNN 的答案
CNN 的信心不足  →  用（修好的）規則式的答案
```

這樣可以拿到兩邊的好處：CNN 準的時候用 CNN，CNN 不確定的時候有一個講道理的保底。

---

## 9. 這件事教我們的三件事

### (1) 「有回應」不等於「正確」，更不等於「有在工作」

線上 API 一直好好地回傳答案，沒有錯誤、沒有當機、回應時間正常。
**它只是對每個人都給同一個答案。**

如果沒有人拿標準答案去對照，這個 bug 可以再活好幾年。

**→ 任何會「給出答案」的系統，都必須定期拿標準答案去考它。**

### (2) 借來的門檻，一定要用自己的資料重新驗證

那組閾值在 CelebA 上大概是對的。錯的是**直接搬過來用，而且沒有驗證**。

**→ 任何從別的資料集/論文/教學抄來的數字，先印出你自己資料的分佈，看看那個數字落在哪裡。**

### (3) 診斷的第一步永遠是「把數字印出來」

我們沒有猜、沒有一行行讀邏輯、沒有加一堆 print 到處試。
就只是**把特徵值按類別分組，印出最小值、中位數、最大值**，bug 立刻現形 ——
門檻 0.205，資料最小值 0.259，一眼就看出來這條路沒人走。

**→ 面對「輸出很奇怪」的問題，先量分佈，再看程式碼。**

---

## 10. 你可以自己動手跑的指令

```bash
# 1. 看規則式在真實資料上的特徵分佈，找出死碼與恆真條件（本文第 6 節）
.venv\Scripts\python.exe tools\diagnose_rule_thresholds.py

# 2. 看規則式 vs CNN 在同一份考卷上的分數（本文第 5 節）
.venv\Scripts\python.exe eval_rule_baseline.py

# 3. 丟一張自己的照片，看 CNN 怎麼判（--save-roi 會存下裁切出來的部位圖）
.venv\Scripts\python.exe predict_basic_roi.py 你的照片.jpg --save-roi roi_out\

# 4. 完整重跑訓練（大約 30 分鐘）
.venv\Scripts\python.exe tools\build_identity_map.py     # 找出哪些照片是同一個人
.venv\Scripts\python.exe prepare_roi_cache.py            # 裁切五個部位並快取
.venv\Scripts\python.exe train_basic_cnn_roi.py          # 訓練五個 CNN
```

---

## 相關文件

- `CNN訓練歷程_BASIC五官分類.md` —— 訓練與部署的完整技術記錄（含踩過的坑）
- `模型紀錄書_BASIC五官分類.md` —— 三個方案的成績對照表（可持續填寫）
- `DINOv2_臉部特徵分類模型規格書.md` —— 原始的模型規格與準確率門檻
