# 模型訓練記錄 2026-07-24

> 本輪目標：把 BASIC 新增的人工標註照片納入重訓，並為 PRO 側臉鼻型建立第一個模型。
>
> 訓練資料來源（**只有這兩處**，其餘副本一律不採用）：
> - BASIC：`data/basic_full/grouped`
> - PRO：`data/pro_full/grouped/nose_shape_side`
>
> 相關文件：`模型紀錄書_BASIC五官分類.md`、`CNN訓練歷程_BASIC五官分類.md`、
> `DINOv2_臉部特徵分類模型規格書.md`

---

## 零、先講最重要的結論

**這輪最大的產出不是新模型，是發現訓練資料本身有系統性問題。**
在修掉它們之前，任何「準確度變好」的說法都不能採信。

1. **資料裡有 314 個位元組完全相同的重複檔**，其中 195 組是同一張圖出現在
   兩個不同類別的資料夾底下。眉型有 40% 的檔案是重複的。
2. **這些重複多數不是標註錯誤，是「類別合併只做了一半」**——舊類別的圖被
   *複製* 到新類別資料夾，但舊資料夾沒刪。套上官方分類表後它們就變成同類重複。
3. **`per_image` 切分的高分是重複檔造成的假象，不是身分洩漏。** 同一份像素被切到
   train 與 val 兩邊，模型是在驗證集上看它背過的東西。舊紀錄的鼻型 0.828、
   眉型 0.667 都出自這個模式，**不是真實表現**。
4. **`identity_map` 過期**，導致鼻型驗證幾乎是空的：寬鼻 98 張裡只有 11 張
   曾進過驗證集。

修法：套官方分類表 → 依內容雜湊去重 → 剔除真正的標註衝突 → 重建身分聚類 →
重跑 CV。細節見第二節。

**清理之後最可行動的一項結論**：眼型改用幾何規則（決策樹）而非 CNN，
在同一套 5-fold 上**每一個 fold 都贏**（0.406 vs 0.332）。眼型是全系統最弱的
部位，這是相對 22% 的提升，而且規則可讀、推論成本為零。見 5.3.1。

---

## 一、資料現況

### 1.1 BASIC（`data/basic_full/grouped`）

人工分類資料夾的張數（不是 `*_manifest.csv`——那是另一批 celeba 自動標的東西，
本輪未使用）：

| 部位 | 類別數 | 張數 | 類別分布 |
| --- | --- | --- | --- |
| 臉型 | 5 | 386 | 心形 87／圓形 82／長形 75／鵝蛋 72／方形 70 |
| 眉型 | 3 | 336 | 一字 183／彎月 82／落尾 71 |
| 眼型 | 8 | 603 | 細長 106／杏仁 88／桃花 87／圓 82／鳳 75／下垂 72／丹鳳 60／瞇縫 33 |
| 鼻型 | 2 | 230 | 標準 132／寬 98 |
| 唇型 | 5 | 352 | 花瓣 77／M型 76／厚 73／薄 66／微笑 60 |

### 1.2 官方分類表（標註同學定義，唯一事實來源）

| 部位 | 官方類別 |
| --- | --- |
| 臉型 | 圓形臉／心形臉／方形臉／長形臉／鵝蛋臉 |
| 眉型 | 一字眉／彎月眉／落尾眉 |
| 眼型 | 細長眼（**併入瞇縫眼**）／桃花眼／杏仁眼／圓眼／鳳眼（**併入丹鳳眼**）／下垂眼 |
| 鼻型 | 標準鼻（**併入窄鼻**）／寬鼻（**併入蒜頭鼻**） |
| 唇型 | 薄唇／花瓣唇／微笑唇／厚唇／M型唇 |

實作方式刻意用**標籤對照表**（`prepare_roi_cache.py` 的 `LABEL_ALIASES`
與 `CANONICAL_LABELS`），不搬動標註資料夾——原始人工分類是事實來源，搬了就沒辦法
反悔，也看不出當初怎麼歸的。對照表套完若出現不在官方清單裡的類別，會印警告並跳過，
不會安靜地訓進去。

蒜頭鼻併入寬鼻的效果：寬鼻 61 → 99，跟標準鼻（132）的平衡從 61:132 改善為 99:132。
輸出契約維持兩類，**前端不用動**。

### 1.3 被排除的資料夾

`data/group_yun`、`data/basic_full/grouped_yun` 與 `data/basic_full/grouped` 的標籤
**逐筆雜湊完全相同**（`idhash=9c8542a6`），是同一批的副本。本輪只取 `grouped` 一份。

`nose_shape/nose_shape/` 是 macOS 拷貝殘留的巢狀目錄（含 `.DS_Store` 與一份 PDF），
其中 132 張標準鼻與外層完全重複。`collect_images()` 不遞迴所以本來就撈不到，
本輪另外加了顯式跳過，避免日後有人「修好」成遞迴反而把重複資料吃進來。

### 1.4 PRO（`data/pro_full/grouped/nose_shape_side`）

側臉鼻型，6 類共 438 張，**先前完全沒有模型**：

| 類別 | 張數 |
| --- | --- |
| 塌鼻 | 149 |
| 朝天鼻 | 65 |
| 直挺鼻 | 61 |
| 翹鼻 | 61 |
| 駝峰鼻 | 60 |
| 蒜頭鼻 | 42 |

---

## 二、必須先修的資料與評估問題

### 2.0 重複檔：本輪最重要的發現

對 `data/basic_full/grouped` 底下 1908 個檔案做 MD5 內容比對：

- **254 組**內容逐位元組相同的重複圖，多餘檔案 **314 個**
- 其中 **195 組是同一張圖出現在兩個不同類別的資料夾**

範例（同一份資料，只差副檔名大小寫）：

```
丹鳳眼/IMG_3603.jpg   ==   鳳眼/IMG_3603.JPG
瞇縫眼/…              ==   細長眼/…
一字眉/IMG1.JPG       ==   彎月眉/IMG_7300.jpg
```

**這些多數不是標註打架，是類別合併只做了一半**：把舊類別的圖 *複製* 到新類別
資料夾之後，舊資料夾沒有刪掉。套上官方分類表（丹鳳眼→鳳眼、瞇縫眼→細長眼）之後，
它們就從「跨類衝突」變成「同類重複」，交給去重處理即可。

套表＋去重＋剔除真衝突後的可用量：

| 部位 | 原始檔案 | 內容去重 | 剩餘真衝突 | **乾淨可用** |
| --- | --- | --- | --- | --- |
| 臉型 | 386 | 358 | 28 | **330** |
| 眉型 | 336 | 203 | 14 | **189** |
| 眼型 | 603 | 455 | 68 | **387** |
| 鼻型 | 231 | 231 | 0 | **231** |
| 唇型 | 352 | 347 | 5 | **342** |

去重後的類別分布反而變均衡。眉型原本看起來是 `一字眉 183／彎月眉 82／落尾眉 71`，
去重後是 `彎月眉 67／一字眉 61／落尾眉 61`——**原本那個 183 的優勢是重複檔堆出來的**。

剩下的 115 組才是標註同學之間真正的判斷分歧（同一張圖被標成一字眉與彎月眉之類），
一律剔除不訓，清單匯出到 `data/roi_cache/label_conflicts.json` 供人工複核。

### 2.1 `per_image` 切分的高分是重複檔造成的

`train_basic_cnn_roi.py --identity-mode per_image` 會把 `identities` 設成
`np.arange(n)`，也就是**每張圖都是獨立身分**，等於完全關閉身分分組。
程式註解的理由是「單一部位內每張圖都是獨立人物」。

**這個假設不成立。** 用 identity map 逐部位核對：

| 部位 | 有身分的張數 | 實際身分數 | 同一人多張造成的重複 |
| --- | --- | --- | --- |
| 眉型 | 336 | 141 | 195 張（58%） |
| 眼型 | 603 | 266 | 337 張（56%） |
| 唇型 | 351 | 190 | 161 張（46%） |
| 臉型 | 386 | 234 | 152 張（39%） |
| 鼻型 | 142 | 88 | 54 張（38%） |

**但真正的洩漏來源不是「同一個人」，是「同一個檔案」。** 去重之前，內容完全相同的
圖會被 `per_image` 當成兩個獨立樣本切到 train 與 val 兩邊——模型不是「認出這個人」，
是直接背答案。這解釋了為什麼 `per_image` 的分數一律高於 `cluster`：

| 部位 | cluster（誠實） | per_image（洩漏） | 虛高 |
| --- | --- | --- | --- |
| 鼻型 | 0.585 | 0.828 | +0.243 |
| 眉型 | 0.538 | 0.667 | +0.129 |

**結論：`per_image` 產生的所有數字不得用於選型或對外報告。本輪一律使用 `cluster`。**

#### identity 聚類到底可不可信

「同一張臉的五官是分開標的，所以鎖定同一個人沒意義」是合理的質疑，實測結果是
**部分成立，但不是主要問題**。判準：同一個人的眉型不會在兩張照片之間改變，
所以同一身分內若出現不同標籤，就是誤併。

| 部位 | 多張照片的身分數 | 其中標籤矛盾 | 矛盾率 |
| --- | --- | --- | --- |
| 鼻型 | 42 | 1 | **2%** |
| 臉型 | 62 | 5 | **8%** |
| 眉型 | 32 | 6 | 19% |
| 眼型 | 69 | 15 | 22% |
| 唇型 | 65 | 18 | 28% |

去重前這個數字是 38%，去重後降到 2–28%——**原本的高矛盾率主要是重複檔造成的，
不是聚類誤併**。鼻型 2%、臉型 8% 表示 InsightFace 確實抓到了真的同一個人。
唇型與眼型偏高，那裡確實有誤併或標註浮動。

決定：**保留 identity 分組**。分組只會讓驗證更嚴格、分數更低，永遠不會灌高；
就算有誤併，代價也只是評估偏保守。會灌高分的是重複檔，而那個已經清掉了。

### 2.2 `identity_map` 過期，鼻型驗證幾乎是空的

`prepare_roi_cache.py` 用 `path_to_identity.get(str(path), -1)` 取身分，
**沒收錄的路徑直接得到 -1**；而 identity = -1 的樣本依政策永遠留在 train、不進 val。

舊的 `identity_map.json` 停在 2026-07-19，不含後來加入的 `IMG_34xx` 那批寬鼻照片：

| | 寬鼻總數 | identity = -1 | 實際可驗證 |
| --- | --- | --- | --- |
| 舊 cache | 61 | 50 | **11** |
| 新 cache（併入蒜頭鼻後） | 98 | 87 | **11** |

也就是說，**過去「鼻型」的 macro accuracy 中，寬鼻那一半是建立在 11 張圖上**。
這不是準不準的問題，是根本沒量過。

處理：本輪重建 `identity_map`（InsightFace buffalo_l，cosine 相似度門檻 0.45），
涵蓋全部 1907 張，再重跑 CV。

---

## 三、環境問題與修復：MediaPipe 在非 ASCII 路徑下無法初始化

建 ROI 快取時 MediaPipe 一律拋：

```
FileNotFoundError: The path does not exist:
    ...\site-packages\mediapipe/modules/face_landmark/face_landmark_front_cpu.binarypb
```

檔案其實存在。原因是 MediaPipe 的 C++ 層用系統 mbcs 編碼開檔，而專案路徑含
「淡江大學」，cp950 轉換後對不回原字串。純 Python 層（`cv2.imdecode` +
`np.fromfile`）不受影響，所以只有真的要初始化 FaceMesh 的步驟會壞。

早期能建起快取，是因為專案當時放在 `C:\Users\isach\PycharmProjects\`——純英文路徑。
搬到 OneDrive 之後才開始壞。

**修法**：新增 `mediapipe_ascii.py`。它在 `import mediapipe` 之前把套件複製到
`%LOCALAPPDATA%\decorateme\mp_ascii` 並插入 `sys.path`，讓 `mediapipe.__file__`
變成 ASCII。特意做成自動：靠來源指紋判斷是否需要重做，換機器／重建 venv／升級
mediapipe 都會自己處理，**搬機不需要任何手動步驟**；路徑本來就是 ASCII 時
（Linux／Docker／Cloud Run）整支 no-op。

已接上的呼叫端：`Face_analyzer_BASIC.py`、`predict_basic_roi.py`、
`prepare_roi_cache.py`、`tools/prepare_face_contour_cache.py`、
`tools/prepare_brow_lip_contour_cache.py`、`tools/select_basic_usable_images.py`。

> 連帶：`Dockerfile` 是逐檔 `COPY` 的，已補 `COPY mediapipe_ascii.py .`，
> 否則線上 BASIC 會在 import 階段就掛掉。
>
> 連帶：`.gitignore` 第二行是 `*`，新檔案會被**靜默忽略**（`git status` 不顯示），
> `mediapipe_ascii.py` 與本文件都需要 `git add -f`。

---

## 四、PRO 側臉鼻型：為什麼不能沿用 BASIC 的 ROI pipeline

BASIC 走「MediaPipe FaceMesh 抓 landmark → 依 `ROI_SPECS` 裁部位」。這條路在側臉
上不成立。實測 438 張側臉的偵測率：

| 類別 | 張數 | FaceMesh 成功 | 偵測率 |
| --- | --- | --- | --- |
| 塌鼻 | 149 | 90 | 60.4% |
| 朝天鼻 | 65 | 42 | 64.6% |
| 直挺鼻 | 61 | 55 | 90.2% |
| 翹鼻 | 61 | 57 | 93.4% |
| 蒜頭鼻 | 42 | 34 | 81.0% |
| 駝峰鼻 | 60 | 44 | 73.3% |
| **總計** | **438** | **322** | **73.5%** |

（另測 FaceDetection：僅 48.4%，更差。）

關鍵不是 73.5% 這個數字本身，而是**失敗率依類別嚴重偏斜**：塌鼻 60.4% vs 翹鼻 93.4%。
丟掉偵測失敗的樣本不是隨機損失，而是把塌鼻從 149 張砍成 90 張，把類別分布整個扭曲，
訓出來的模型會系統性偏向「好偵測」的鼻型。

**因此 PRO 改用整張圖（resize 224），不做 landmark 裁切，438 張全部保留。**

做法比較（同一組 5-fold，`seed=42`）：
- DINOv2 ViT-S/14 凍結 encoder + LogisticRegression／LinearSVC
- MobileNetV3-small fine-tune

身分切分同樣用 InsightFace 聚類（檔名 `IMG_8055/8057/8058` 明顯是連拍，
不分組會洩漏）；抽不到臉的標 -1，留在 train。

腳本：`train_pro_nose_side.py`

---

## 五、訓練結果

<!-- 待填：identity_map 重建完成後執行 -->

### 5.1 BASIC 五官

設定：MobileNetV3-small、5-fold、`--identity-mode cluster`、seed 42、20 epochs、
資料為套官方分類表＋內容去重＋剔除標註衝突後的 1478 張。

| 部位 | 類別數 | 隨機猜 | **本輪 macro** | ±std | 各 fold |
| --- | --- | --- | --- | --- | --- |
| 鼻型 | 2 | 0.500 | **0.895** | 0.088 | 0.974 / 0.937 / 0.881 / 0.731 / 0.954 |
| 眉型 | 3 | 0.333 | **0.575** | 0.059 | 0.549 / 0.647 / 0.611 / 0.474 / 0.592 |
| 臉型 | 5 | 0.200 | **0.477** | 0.066 | 0.390 / 0.490 / 0.517 / 0.572 / 0.416 |
| 唇型 | 5 | 0.200 | **0.430** | 0.071 | 0.508 / 0.325 / 0.366 / 0.478 / 0.473 |
| 眼型 | 6 | 0.167 | **0.332** | 0.045 | 0.291 / 0.307 / 0.311 / 0.418 / 0.332 |

與舊紀錄的對照（舊值取同為 cluster 模式者）：

| 部位 | 舊 cluster | 新 cluster | 差 | 備註 |
| --- | --- | --- | --- | --- |
| 鼻型 | 0.585 | **0.895** | **+0.310** | 類別數由 3 → 2，且舊值的寬鼻只有 11 張進過驗證 |
| 眉型 | 0.538 | 0.575 | +0.037 | 資料由 336 → 189，但類別變均衡 |
| 臉型 | 0.444 | 0.477 | +0.033 | |
| 唇型 | 0.424 | 0.430 | +0.006 | 本來就幾乎沒有重複檔（僅 5 組） |
| 眼型 | 0.298 | 0.332 | +0.034 | 類別數由 8 → 6，**兩者不同尺，不可直接比** |

**誠實的解讀**：

- **鼻型是真正的贏家。** 0.895 甚至高於舊的「洩漏版」0.828，而這是嚴格切分下的值。
  三個原因疊加：蒜頭鼻併入使寬鼻 61 → 99（平衡 61:132 → 99:132）、identity map
  修好讓寬鼻真的進得了驗證集、窄鼻併入標準鼻。鼻型也是唯一零重複檔的部位。
- **眼型仍是最弱環節，而且「變好」是假象。** 類別數從 8 降到 6，隨機猜從 0.125
  升到 0.167。以「相對隨機猜的提升倍數」看，舊 2.38 倍、新 1.99 倍——實際上退步。
  絕對值好看只是因為題目變簡單了。
- **唇型幾乎沒動**（+0.006），因為它本來就沒什麼重複檔可清。它的瓶頸是別的東西。

### 5.2 PRO 側臉鼻型

設定：整張圖 resize 224、5-fold、`seed=42`、434 張（去重後）、
其中 249 張抽得到 identity，185 張無身分（只進 train）。

| 做法 | mean macro | ±std |
| --- | --- | --- |
| MobileNetV3-small fine-tune | 0.4432 | 0.1012 |
| DINOv2 ViT-S/14 + LinearSVC | 0.4223 | 0.0747 |
| DINOv2 ViT-S/14 + LogReg | 0.4163 | 0.0571 |

**兩種做法在統計上分不出高下。** MobileNet 名目領先 0.021，但它的 fold 間波動是
±0.101（0.289～0.581），差距遠小於自身雜訊。真要選，DINOv2 穩定得多（±0.057）
而且不需要 fine-tune。

逐類別 recall（MobileNet）顯示模型基本上只學會「是不是塌鼻」：

| 類別 | 驗證張數 | recall |
| --- | --- | --- |
| 塌鼻 | 110 | 0.900 |
| 翹鼻 | 36 | 0.556 |
| 直挺鼻 | 32 | 0.469 |
| 朝天鼻 | 26 | 0.385 |
| 蒜頭鼻 | 22 | 0.182 |
| 駝峰鼻 | 23 | 0.174 |

**結論：PRO 側臉鼻型還不能上線。** 駝峰鼻與蒜頭鼻的 recall 只比隨機猜（0.167）
好一點點；塌鼻佔 149/434，模型靠猜最大類就能拿到不錯的總分。要做起來得先補
駝峰鼻與蒜頭鼻的資料，並壓低塌鼻的佔比。

---

### 5.3 幾何／規則式重新校準（BASIC）

規則式的閾值原本是用「未清理、且分類表尚未合併」的資料校準的（當時眉 194、
眼 376、鼻 156 張）。清理後的數量完全不同（眉 189、眼 387、鼻 231），因此重新校準。

同時修掉 `tools/calibrate_rule_thresholds.py` 兩個會說謊的地方：鼻型寫死三分類
（清理後只有兩類，會 `KeyError: '窄鼻'`）、眼型訊息寫死「7 類」、以及頁尾寫死一組
早已過期的 CNN 對照分數。

| 部位 | 重新校準後的規則式 | CNN 5-fold | 勝方 |
| --- | --- | --- | --- |
| 眼型 | **0.374** | 0.332 | **規則式** |
| 鼻型 | 0.828 | **0.895** | CNN |
| 眉型 | 0.429 | **0.575** | CNN |

校準出來的門檻：

- 眉型：`tail_ratio < -0.014 → 一字眉`、`> 0.036 → 落尾眉`、其餘 → 彎月眉
- 鼻型：`ratio_width < 0.302 → 標準鼻`，否則 → 寬鼻
- 眼型：深度由 train 內部 CV 選出的決策樹，特徵為 `ear`／`angle`／`ratio_to_face`

**重點：眼型的幾何規則勝過 CNN。** 眼型是全場最弱的部位（CNN 0.332），而一棵淺
決策樹只用三個幾何量就拿到 0.374。這符合直覺——眼型的差異本來就是「開合比例」與
「眼角角度」這類幾何量，不是紋理，CNN 在 387 張的規模下學不出它。

鼻型只用**單一門檻**就逼近 CNN（0.828 vs 0.895），而且可讀、可解釋、零推論成本。

> **比較上的保留**：規則式用單次切分（`val_ratio=0.25`），CNN 用 5-fold，
> 兩者評估協定不同，上表不能直接並列下結論。已於 5.3.1 用同一套 5-fold 重做。

### 5.3.1 規則式接上同一套 5-fold（結論版）

`tools/cv_rule_baseline.py`：規則式改用與 CNN **完全相同的**
`split_kfold_by_identity(labels, identities, 5, 42)`。門檻只在每個 fold 的 train
上 grid search，眼型決策樹的深度也在 train 內部再切一次 CV 選，val 全程不參與。

| 部位 | 規則式 5-fold | ±std | CNN 5-fold | 判定 |
| --- | --- | --- | --- | --- |
| 眼型 | **0.4059** | 0.047 | 0.3317 | **規則式勝** |
| 眉型 | 0.4542 | 0.107 | **0.5747** | CNN 勝 |
| 鼻型 | 0.8126 | 0.061 | **0.8952** | CNN 勝 |

逐 fold 配對比較（同一個 fold 直接比，比看平均可靠）：

| 部位 | 規則式勝幾個 fold | 平均差 | ±std |
| --- | --- | --- | --- |
| 眼型 | **5 / 5** | **+0.074** | 0.038 |
| 眉型 | 1 / 5 | −0.120 | 0.134 |
| 鼻型 | 1 / 5 | −0.083 | 0.107 |

**結論一：眼型應該改用幾何規則，不要用 CNN。** 規則式在**每一個 fold** 都贏，
平均差 +0.074 是其標準差（0.038）的兩倍。眼型是全系統最弱的部位，改用規則式等於
把它從 0.332 拉到 0.406（相對提升 22%），而且規則可讀、可解釋、推論成本為零。
這也符合直覺：眼型的差異本來就是「開合比例（ear）」與「眼角角度（angle）」這類
幾何量，不是紋理，CNN 在 387 張的規模下學不出來。

**結論二：眉型與鼻型維持 CNN，但這個結論比表面上弱。** 兩者 CNN 雖勝 4/5，
但**逐 fold 差的標準差（0.134、0.107）都大於平均差（0.120、0.083）**，
而且各有一個 fold 是規則式贏。這是傾向，不是定論——真要下判斷需要更多資料或
重複多組 seed。單看平均值會高估這個結論的強度。

單次切分與 5-fold 的差異也印證了先前的保留是必要的：眼型的優勢從 +0.042
（單次切分）變成 +0.074（5-fold），方向一致但幅度差很多。

### 5.4 PRO 側臉加入幾何特徵：負面結果

依「側臉鼻型本質是鼻樑角度與曲率」的想法，新增 `pro_nose_geometry.py`，從
FaceMesh 鼻部中線（168→6→197→195→5→4→1→2）計算 9 個量：鼻樑傾角、鼻樑凸度
（正=駝峰／負=塌）、最大偏離、鼻尖上旋角、鼻尖前突比、鼻長比、鼻小柱角、直挺度、
上下半段比。全部以「鼻根→鼻基底」距離正規化，並建立局部座標系讓臉朝左右都對齊。

在**同一批 319 張**（有 landmark 的樣本）上比較：

| 特徵 | 樣本 | mean macro |
| --- | --- | --- |
| DINOv2 單獨（全量對照） | 434 | 0.409 |
| DINOv2 單獨 | 319 | **0.370** |
| 幾何 + DINOv2 | 319 | 0.362 |
| 幾何單獨 | 319 | **0.236** |

**加幾何沒有幫助，反而略微有害**（0.362 vs 0.370）。幾何單獨只有 0.236，
隨機猜是 0.167。

> 這裡刻意加了 `dinov2_geomsubset` 這個對照組。沒有它的話，會誤把
> 「幾何+DINOv2 0.362 低於全量 DINOv2 0.409」解讀成幾何拖累，
> 但真正的原因是**這個子集本來就比較難**。

**推測原因：FaceMesh 在側臉上「偵測成功」不代表點是對的。** 它是為正面臉訓練的，
側臉時被遮蔽的半邊由模型估計補出，鼻部中線那幾點並未真正落在鼻樑輪廓上，
因此幾何量是從不可靠的座標推導出來的。

這也補強了第四節的結論：側臉不能沿用 BASIC 的 landmark 路線，**不只是偵測率
73.5% 的問題，而是那 73.5% 的品質本身就不可信**。

---

### 5.3.2 五個部位全跑：規則式 vs CNN 的最終結論

臉型與唇型原本沒有規則式特徵函式，本輪補上（`rule_features.face_features`、
`lip_features`），五個部位都用決策樹在同一套 5-fold 上跑過一次。眉、鼻另外保留
單一門檻版，取兩者較佳者與 CNN 比較。

| 部位 | 最佳規則式 | CNN | 平均差 | ±std | 勝場 | 結論 |
| --- | --- | --- | --- | --- | --- | --- |
| 眼型 | **0.4059** | 0.3317 | **+0.074** | 0.038 | **5/5** | **改用規則式** |
| 臉型 | **0.5439** | 0.4769 | **+0.067** | 0.059 | **4/5** | **改用規則式** |
| 眉型 | 0.5026 | 0.5747 | −0.072 | 0.176 | 2/5 | 維持 CNN（差在雜訊內）|
| 鼻型 | 0.8126 | 0.8952 | −0.083 | 0.107 | 1/5 | 維持 CNN（差在雜訊內）|
| 唇型 | 0.3569 | 0.4300 | −0.073 | 0.101 | 1/5 | 維持 CNN（差在雜訊內）|

**眼型與臉型改用幾何決策樹。** 兩者都是幾何量而非紋理——眼型是開合比例與眼角角度，
臉型是不同高度的寬度比值——在幾百張的規模下 CNN 得從像素重新學會這些量，直接算反而準。

其餘三個部位維持 CNN。要注意這個結論比表面弱：三者的逐 fold 差標準差都大於平均差，
屬於傾向而非定論，所以是「不值得為此更動既有部署」而不是「CNN 確定較好」。

> 量到的是**新學出來的決策樹**，不是服務裡原本那組手寫 if-else。手寫規則沒有
> 重新評估過，不能拿這裡的數字替它背書。

---

## 五之二、部署變更

### 6.1 眼型與臉型改由幾何決策樹提供正式答案

- 新增 `rule_features.py`（特徵定義唯一來源）與 `basic_rule_trees.py`（推論路徑）。
  特徵定義放在專案根目錄而非 `tools/`，因為 `Dockerfile` 逐檔 `COPY` 不含 `tools/`；
  留在工具目錄線上會載不到，或被迫複製第二份而與離線版本各自漂移。
- 最終樹由 `tools/train_final_rule_trees.py` 以 100% 清理後資料訓練並匯出
  （`{part}_rule_tree.joblib` ＋ `.json`，JSON 內含可讀規則文字）。
- 接在 `Face_analyzer_BASIC.export_json()` 的 CNN 之後，`分類來源` 會標記
  `rule_tree`。整段包 `try`，樹壞掉不影響正式分析；`ROI_RULE_TREE_ENABLED=0` 可關掉。

實測輸出：`分類來源 = {臉型: rule_tree, 眉型: roi_cnn, 眼型: rule_tree, 鼻型: roi_cnn, 嘴型: roi_cnn}`

### 6.2 眼型八類併為六類的契約同步

| 位置 | 變更 |
| --- | --- |
| `models/basic_features_roi/eye_shape_classes.json` | 8 → 6 類（重訓後自動產生）|
| `Face_analyzer_BASIC.get_eye_shape()` | fallback 規則改輸出六類名稱 |
| `analysis_package.EYE_SHAPE_CODES` | 改為六類；舊名保留為別名以讀取歷史資料 |
| `web_frontend/js/api.js` 的 `AnalysisFeedback.OPTIONS` | 眼型選項改為六類 |

順帶修掉一個既有缺陷：`EYE_SHAPE_CODES` **原本沒有「鳳眼」**，模型輸出鳳眼時會被
`_code()` 落成 `unknown`。

### 6.3 防止舊分類的模型偷偷上線

`eye_shape_dinov2_classes.json` 仍是舊的八類（DINOv2 head 未隨之重訓）。少了防護，
只要有人設 `ROI_DINOV2_MODEL_FIRST=1`，線上就會吐出「丹鳳眼」這種已不在契約裡、
前端回饋選項也沒有的標籤——使用者選不到，看起來只會像模型很爛。

`basic_roi_shadow._retired_labels()` 在載入時比對 head 的類別與 `{part}_classes.json`，
發現已淘汰的名稱就跳過該部位並記警告。臉型與鼻型的 head 類別一致，不受影響。

### 6.4 用清理後資料重訓並匯出部署模型

`train_basic_cnn_roi.py --skip-random` 重訓五個部位並匯出 ONNX 與類別檔到
`models/basic_features_roi/`。單次 identity 切分的 val macro（僅供參考，泛化請看
5.1 的 5-fold）：臉 0.490、眉 0.431、眼 0.392、鼻 0.946、唇 0.427。

---

## 六、重現方式

```bash
# 0) MediaPipe 路徑修復是自動的，不需要手動步驟

# 1) 重建身分聚類（涵蓋所有現存標註圖）
python tools/build_identity_map.py

# 2) 重建 ROI 快取（含蒜頭鼻→寬鼻的標籤對照）
python prepare_roi_cache.py

# 3) BASIC 五官 5-fold（一律用 cluster，不要用 per_image）
python train_basic_cnn_roi.py --cv 5 --identity-mode cluster

# 4) PRO 側臉鼻型：像素 vs 幾何 vs 兩者合併，同一組 fold
python train_pro_nose_side.py --folds 5

# 5) 規則式／幾何閾值重新校準（需要 .venv，因為要 import Face_analyzer_BASIC）
.\.venv\Scripts\python.exe tools/calibrate_rule_thresholds.py

# 6) 規則式 5-fold，與 CNN 同一組 fold（要下「規則式 vs CNN」結論就看這個）
.\.venv\Scripts\python.exe tools/cv_rule_baseline.py
#    ROI 快取重建後要加 --refresh，否則會沿用舊的特徵快取

# 7) 訓練並匯出最終的幾何決策樹（眼型、臉型的正式答案來源）
.\.venv\Scripts\python.exe tools/train_final_rule_trees.py

# 8) 重訓並匯出部署用的 CNN（ONNX ＋ 類別檔）
.\.venv-train\Scripts\python.exe train_basic_cnn_roi.py --skip-random
```

> 環境注意：`.venv-train` 有 torch／mediapipe／insightface 但**沒有 fastapi**，
> 所以任何 import `Face_analyzer_BASIC` 的腳本要用 `.venv`。`.venv` 兩者皆備
> （torch 2.7.1+cu118、fastapi 0.139.2），是比較保險的預設。

備份：本輪動工前已把舊的 `rois.npz`、`index.json`、`identity_map.json` 複製到
`data/roi_cache_backup_20260724/`。
