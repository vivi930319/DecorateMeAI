# 【緊急】會員端點全面回 401，線上無法登入／註冊／收驗證碼

> 日期：2026-07-27
>
> 嚴重度：**P0，整站會員功能歸零**。沒有人能登入、沒有人能註冊、沒有人能收到驗證碼。
> 商品瀏覽仍正常。
>
> 這份是當天在正式站 `https://decorate-me.web.app` 實測的結果，不是推測。
>
> 禁止寫入本文件、Log、Git 或訊息：真實密碼、完整 Cookie 值、API Key、JWT、
> 完整會員 email、會員照片。

---

## 一、現況

三支會員端點對**任何**請求都回 401，包含用全新、從未註冊過的信箱：

| 端點 | 2026-07-26 | 2026-07-27 |
| --- | --- | --- |
| `POST /auth/login` → `/api/login` | 401 僅在帳密錯誤時 | **401（帳密正確也是）** |
| `POST /auth/send-otp` → `/api/send-otp` | **200** | **401** |
| `POST /auth/register` → `/api/register` | **200**（成功建立帳號） | **401** |
| `GET /product-api/api/products` | 200 | 200（正常） |
| `GET /health` | 200 | 200（正常） |

7/27 的註冊測試用的是一個全新的信箱 `probe-check-8k2@example.com`，
從來沒有註冊過，一樣回 401。所以**不是帳號狀態的問題，是端點本身拒絕了所有請求**。

## 二、我們已經排除的原因

請不要往這幾個方向查，都確認過了：

- **不是通道**：`/health` 回 200，`service: member-database`
- **不是通道網址換掉**：今天沒有換過，Gateway 上設定的與實際運行的是同一組
- **不是 Gateway**：商品 API 走**同一台主機、同一條通道、同一個 Gateway**，回 200
- **不是資料被清空**：`/api/products` 回報 total = 1041
- **不是使用者的密碼**：全新信箱的註冊也是 401，那條路徑根本不檢查密碼
- **不是限流**：限流會回 429 並帶 `Retry-After`，不是 401

## 三、我們的判斷

看起來是在 `/api/*`（至少是會員那幾支）上新增了需要憑證的驗證中介層，
但我們這邊不知道要帶什麼，也沒有收到通知。

補充一個佐證：直接打通道（不經 Gateway）也是 401，兩邊行為一致，
所以不是 Gateway 少傳了什麼既有的標頭，而是這些端點現在需要一個**以前不需要**的東西。

## 四、我們需要你們回答的

1. `/api/login`、`/api/register`、`/api/send-otp` 是不是新增了驗證要求？
2. 如果是，需要帶什麼？（標頭名稱、憑證從哪裡取得、有沒有有效期）
3. 如果不是刻意的，請看這三支端點的 401 是從哪一層丟出來的（中介層？反向代理？）

拿到答案我們會在 Gateway 這側補上，前端不需要改動——瀏覽器永遠不會看到這個憑證。

**注意：登入與註冊屬於「登入前」的端點，設計上不可能要求使用者先有登入憑證。**
如果新的驗證要求是「呼叫端服務的金鑰」（像商品端 `PRODUCT_ADMIN_API_KEY` 那樣），
那沒問題，給我們金鑰即可，我們存進 Secret Manager。
如果是要求使用者的 token，那這三支端點就不能套用——會變成先有雞還是先有蛋。

## 五、這件事可能也解釋了 issue #30

7/26 管理員後台改權限全部失敗，我們當時懷疑是 `admin@decoratme.local` 這筆
member row 讀不到。但如果**所有**會員端點都回 401，那 Gateway 的
`/auth/session` 拿 cookie 回去打 `GET /api/members/admin@decoratme.local`
自然也是 401 → 判定 session 無效 → 前端擋掉所有寫入。

**所以 #30 很可能不是 admin 帳號的問題，而是這次鎖定的早期症狀。**
請先解決本文件這一項，再回頭看 #30 還在不在——不要花時間去查 admin 那筆 row。

## 六、驗收

修好之後我們會打這三條確認：

```
POST /auth/register（全新信箱）      →  200
POST /auth/send-otp（該信箱）        →  200
POST /auth/login（正確帳密）         →  200
```

在那之前，**Demo 完全無法進行**——連登入畫面都過不去。
