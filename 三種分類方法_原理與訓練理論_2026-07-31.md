# 三種分類方法：原理、訓練理論與實測比較

**專案：** DECORATE ME（裝識你的美）· 臉部五官分類
**文件日期：** 2026-07-31
**用途：** 畢業專題繳交文件 — 方法論章節
**對應實作：** `rule_features.py` / `basic_rule_trees.py`、`train_basic_cnn_roi.py`、`tools/dinov2_cv_experiment.py`

---

## 目錄

- [零、問題設定](#零問題設定)
- [一、方法 A：幾何特徵 + 決策樹](#一方法-a幾何特徵--決策樹)
- [二、方法 B：卷積神經網路（遷移學習）](#二方法-b卷積神經網路遷移學習)
- [三、方法 C：自監督視覺基礎模型 + 線性探針](#三方法-c自監督視覺基礎模型--線性探針)
- [四、三者的本質差異](#四三者的本質差異)
- [五、實測比較](#五實測比較)
- [六、組合策略：融合與投票](#六組合策略融合與投票)
- [七、共用的實驗方法論](#七共用的實驗方法論)

---

## 零、問題設定

### 0.1 任務形式

輸入一張正面人臉照片，輸出五個部位的類別：

| 部位 | 類別數 | 類別 |
| --- | --- | --- |
| 臉型 face_shape | 5 | 圓形臉／心形臉／方形臉／長形臉／鵝蛋臉 |
| 眉型 brow_shape | 3 | 一字眉／彎月眉／落尾眉 |
| 眼型 eye_shape | 4 | 下垂眼／圓眼／桃杏眼／鳳眼 |
| 鼻型 nose_shape | 2 | 寬鼻／標準鼻 |
| 唇型 lip_shape | 4 | 厚唇／微笑唇／花瓣唇／薄唇 |

形式上是**五個獨立的多類別單標籤分類問題**，共用同一張輸入影像。

### 0.2 這個任務的三個結構性難點

**（1）資料量小。** 每個部位 269–676 張，每類約 100–225 張。這個規模下從零訓練
一個深層網路必然過擬合——參數量遠大於樣本數。三種方法的差異，很大程度上就是
**「如何在小資料上取得足夠的歸納偏置（inductive bias）」**的不同答案。

**（2）類別界線由人定義，且不一定存在。** 這不是「貓 vs 狗」那種物理上互斥的類別。
「杏仁眼 vs 桃花眼」的界線是標註者的主觀判斷，實測發現**同一張照片被不同標註者
標成不同類別**的情形達 57 組。模型學不會的東西，有一部分是因為那條邊界本來就不存在。

**（3）判別訊號的空間尺度極小。** 鼻型的差異在鼻翼寬度的幾個百分點；眉型的差異在
眉尾走向。整張臉 resize 到 160×160 之後，鼻子只剩幾十個像素——這直接決定了
**必須先把部位裁出來（ROI）再送進模型**，這一點三種方法共用。

### 0.3 評估指標：macro accuracy

$$\text{macro-acc} = \frac{1}{C}\sum_{c=1}^{C} \frac{TP_c}{N_c}$$

即**各類別 recall 的算術平均**，每一類權重相同。

不用整體 accuracy 的理由：眉型的「一字眉」佔 50%，一個永遠回答「一字眉」的
退化模型 accuracy 有 0.50，看起來尚可；但 macro 會把它壓到 0.33（另外兩類 recall = 0），
與亂猜同級。**這不是理論顧慮——本專案的第一版規則式就是這樣壞的**（對每一張
都輸出「彎月眉」）。

---

## 一、方法 A：幾何特徵 + 決策樹

### 1.1 理論基礎

**核心假設：** 人臉的形狀類別，本質上是**可以用少數幾個比值與角度描述的幾何量**。
若假設成立，就不需要讓模型從像素學起——直接量出那些數字，再用一個淺層分類器切分。

實作分兩層：

```
MediaPipe FaceMesh  →  468 個 3D landmark
        ↓  rule_features.py
幾何特徵向量（比值、角度、對稱性）
        ↓  DecisionTreeClassifier(max_depth=4)
類別
```

### 1.2 特徵工程：從 landmark 到幾何量

MediaPipe FaceMesh 用一個輕量 CNN 回歸出 468 個標準化的臉部關鍵點座標
$\{(x_i, y_i, z_i)\}_{i=1}^{468}$。特徵抽取的原則是**全部取「比值」而非絕對距離**，
因為絕對像素距離會隨拍攝距離與影像解析度變動：

**臉型特徵**（`rule_features.EXTRACT["face_shape"]`）

$$
\text{height\_width} = \frac{\text{臉長}}{\text{顴骨寬}},\quad
\text{forehead\_to\_jaw} = \frac{\text{額寬}}{\text{下顎寬}},\quad
\text{cheek\_to\_jaw} = \frac{\text{顴骨寬}}{\text{下顎寬}}
$$

再加上下頜線角度 `jaw_slope`、下顎角 `gonial_angle`（判別圓 vs 方的關鍵）。

**眼型特徵**

$$
\text{EAR} = \frac{\|p_2 - p_6\| + \|p_3 - p_5\|}{2\|p_1 - p_4\|}
$$

即 eye aspect ratio（眼睛開合比），配合眼角傾斜角 `angle` 與眼睛相對臉的大小
`ratio_to_face`。

**鼻型特徵**：鼻翼寬 ÷ 臉寬。

> **對齊的必要性：** 所有寬度都先依兩眼連線做旋轉正規化（`_align_points_by_eyes`），
> 否則頭部側傾會讓「臉寬」量到的是投影長度而非真實寬度。

### 1.3 分類器：為什麼是決策樹

選擇 `DecisionTreeClassifier(max_depth=4, class_weight="balanced")` 的理由：

**（1）與手寫 if-else 同構，但門檻由資料決定。** 世代 0 的做法是人工寫死門檻
（例如「EAR < 0.264 判細長眼」），問題是那些門檻來自 CelebA（西方人臉），套到
亞洲人臉直接失效——實測出現**死碼**：

```
瞇縫眼 需要 ratio_to_face < 0.12   但資料中實際最小值是 0.194
窄鼻   需要 ratio_width  <= 0.205  但實際分布不在這個範圍
```

這兩個類別因此**永遠不會被輸出**，而且不會報錯。決策樹把「門檻」變成從訓練資料
擬合出來的參數，解決了這個問題。

**（2）深度限制即為正則化。** `max_depth=4` 表示最多 4 次二分，最多 16 個葉節點。
在 500 張、9 維特徵的規模下，這個容量足以表達「三種不同的判斷依據」，又不至於
把每個樣本各切一片。

**（3）`class_weight="balanced"`** 讓損失依類別頻率倒數加權：

$$w_c = \frac{N}{C \cdot N_c}$$

因為評估指標是 macro accuracy，每類等重，訓練時的加權必須與之對齊。

### 1.4 這個方法的優勢與限制

**優勢**
- **推論成本近乎為零**：landmark 本來就要算（用於 ROI 裁切），幾何特徵只是幾次除法
- **完全可解釋**：可以印出整棵樹，逐條說明「為什麼判成這類」
- **不需要 GPU**

**限制（實測證實）**
- **表達力受限於人工設計的特徵。** 若某個類別的判別訊號不在那 9 個數字裡（例如
  唇型的「唇峰圓鈍程度」），模型完全看不到 → 唇型只有 0.42，五個部位最低。
- **對特徵抽取失敗極度敏感。** landmark 抽不到（側臉、遮擋）就整個失效。
- ⚠️ **「規則式比較穩」是錯覺。** 它的門檻同樣是從資料擬合出來的，只是擬合過程
  不叫「訓練」。本專案實測：資料換一批之後，眼型 0.406 → 0.379、臉型 0.544 → 0.445，
  **而且沒有任何機制會提醒你它退步了**。

---

## 二、方法 B：卷積神經網路（遷移學習）

### 2.1 理論基礎

**核心假設：** 判別訊號存在於**紋理與局部形狀**中，而這些特徵無法用人工設計的
少數幾個數字表達，但可以由卷積層從像素中學出來。

CNN 的兩個歸納偏置正好對應影像的性質：

- **局部連接（locality）**：卷積核只看鄰近像素，符合「有意義的視覺特徵是局部的」
- **權重共享（translation equivariance）**：同一個核掃過整張圖，
  參數量從 $O(H \times W)$ 降到 $O(k^2)$，這是小資料能訓得動的關鍵

### 2.2 遷移學習：為什麼不從零訓練

500 張影像不足以從零學出好的低階濾波器（邊緣、角點、紋理）。遷移學習的假設是：

> 在 ImageNet（120 萬張、1000 類）上學到的**低階與中階特徵是通用的**，
> 只有最後的分類層需要換成本任務的類別。

實作上載入 `MobileNet_V3_Small_Weights.DEFAULT`，只把最後一層線性層換掉：

```python
model = mobilenet_v3_small(weights=MobileNet_V3_Small_Weights.DEFAULT)
model.classifier[3] = nn.Linear(model.classifier[3].in_features, n_classes)
```

**全網路微調（full fine-tune），不凍結。** 因為部位 ROI（眼睛特寫、鼻子特寫）
與 ImageNet 的自然影像分布差距不小，只訓最後一層不足以適應。

**實測支持這個設計：** 同架構但**不載入預訓練權重**（`--architecture simple_cnn`）
的對照組，五個部位平均低約 0.05，臉型差最多（0.414 vs 0.521）。
唯一例外是眼型（simple_cnn 0.583 vs mobilenet 0.562），推測是眼型 ROI 只有 96×96
且高度同質，ImageNet 的「分辨貓狗車」低階特徵派不上用場——**此為推測，未驗證**。

### 2.3 MobileNetV3-small 的架構要點

選它而非 ResNet 的理由是**推論成本**（線上服務要在 Cloud Run 上跑）：

- **深度可分離卷積（depthwise separable convolution）**：把標準卷積拆成
  depthwise + pointwise 兩步，計算量從 $D_K^2 \cdot M \cdot N$ 降到
  $D_K^2 \cdot M + M \cdot N$，約 8–9 倍的節省
- **Squeeze-and-Excitation 模組**：對通道做全域池化後產生權重，等於讓網路
  自己學「哪些通道重要」
- **h-swish 激活**：$\text{h-swish}(x) = x \cdot \frac{\text{ReLU6}(x+3)}{6}$，
  近似 swish 但用整數運算友善的形式

參數量約 2.5M，ONNX 匯出後每個部位約 6MB。

### 2.4 訓練設定與每一項的理由

```
輸入      臉型 128×128、其餘 96×96 的部位 ROI（RGB）
優化器    AdamW，lr = 6e-4，weight_decay = 1e-4
排程      CosineAnnealingLR，T_max = epochs
損失      CrossEntropy + label smoothing 0.05
epochs    40
batch     32
取樣      WeightedRandomSampler
增強      隨機裁切／色彩抖動；**刻意不做水平翻轉**
```

**AdamW 而非 Adam**：AdamW 把 weight decay 從梯度更新中解耦，
在自適應學習率下才是真正的 L2 正則化。

**Label smoothing 0.05**：把 one-hot 目標 $y$ 換成
$y' = (1-\epsilon)y + \epsilon/C$。理由與本任務的性質直接相關——
**標註本身就有雜訊**（同一張圖被標成不同類別的情形確實存在），
讓模型對訓練標籤保留一點不確定性，比逼它 100% 確信更合理。

**WeightedRandomSampler**：以 $1/N_c$ 為權重取樣，使每個 batch 的類別分布接近均勻。
合併類別之後不平衡更明顯（眼型：桃杏眼 221 / 鳳眼 225 vs 下垂眼 110），
這一項是必要的。

**⚠️ 刻意不做水平翻轉——這是本專案最重要的領域知識之一。**
水平翻轉是影像分類幾乎必開的增強，但在這個任務上**會直接製造錯誤標籤**：

> 「落尾眉」水平翻轉後在視覺上變成「上揚眉」，但標籤仍然是落尾眉。
> 等於主動餵給模型一批錯的訓練資料。

**epochs = 40 而非 20**：實測 20 epochs 尚未收斂。逐折配對比較（25 次），
40 epochs 在 18/25 折勝出、五個部位方向全部為正。

**學習率 6e-4 而非 3e-4**：同樣以逐折配對決定。單看五部位平均只差 0.004
（看起來在雜訊內），但**逐折看是 18/25 勝、5/5 部位方向一致**——
平均會把「方向一致的小幅度」稀釋掉，**符號一致性才是該看的統計量**。

---

## 三、方法 C：自監督視覺基礎模型 + 線性探針

### 3.1 理論基礎

**核心假設：** 一個在**大規模無標註影像**上以自監督方式預訓練的模型，
其中間表示（embedding）已經編碼了足夠通用的視覺語意；
本任務只需要在這個表示空間上找一個**線性分界面**。

這與方法 B 的差別是根本性的：

| | 方法 B（CNN 微調）| 方法 C（凍結 + 線性探針）|
| --- | --- | --- |
| 可訓練參數 | 全部約 2.5M | 只有線性層 $384 \times C$ ≈ 1.5K |
| 過擬合風險 | 高（參數 ≫ 樣本）| 低 |
| 適應新分布的能力 | 高 | 受限於預訓練表示 |

在 500 張的規模下，**把可訓練參數壓到 1500 個以下**是很強的正則化。

### 3.2 DINOv2 的預訓練方式

DINOv2 是 Meta 提出的自監督視覺 Transformer，訓練時**完全不使用人工標籤**。
核心是**自蒸餾（self-distillation）**：

1. 同一張影像產生兩組不同的增強視圖（global crops 與 local crops）
2. 學生網路 $g_{\theta_s}$ 與教師網路 $g_{\theta_t}$ 各自輸出機率分布
3. 損失為交叉熵 $-\sum p_t \log p_s$，讓學生在**看到局部**時預測出教師**看到全局**時的輸出
4. 教師參數以學生的 EMA 更新：$\theta_t \leftarrow \lambda\theta_t + (1-\lambda)\theta_s$

避免塌陷（所有輸入映到同一點）的機制是對教師輸出做 **centering + sharpening**。
DINOv2 另外加入 iBOT 的 masked patch prediction 與大規模資料清洗（LVD-142M）。

**為什麼這對本任務有利：** 自監督學到的表示**沒有被 ImageNet 的 1000 個類別綁定**，
更偏向通用的形狀與紋理描述，對「眼睛的形狀」這種 ImageNet 標籤體系裡不存在的
概念可能保留了更多資訊。實測也支持：眼型 DINOv2 0.625 vs CNN 0.577。

### 3.3 ViT 架構與 embedding 抽取

使用 **ViT-S/14**：

1. 輸入 224×224 切成 $16 \times 16 = 256$ 個 14×14 patch
2. 每個 patch 線性投影成 384 維 token，前面接一個可學習的 `[CLS]` token
3. 過 12 層 Transformer block（多頭自注意力 + MLP）
4. 取最後一層的 `[CLS]` token 作為整張圖的表示 → **384 維向量**
5. L2 正規化：$e \leftarrow e / \|e\|_2$

自注意力的關鍵性質是**全域感受野**：第一層每個 token 就能看到所有其他 token，
不像 CNN 要靠堆疊層數慢慢擴大。對「眼角相對於眼球中心的位置」這類需要
比較遠距離關係的判斷，理論上有利。

### 3.4 線性探針：LinearSVC 與 LogisticRegression

在凍結的 384 維 embedding 上訓練線性分類器。兩種都試：

**LogisticRegression** 最小化

$$\mathcal{L} = -\sum_i \log p(y_i \mid x_i) + \frac{1}{2C}\|w\|^2$$

輸出**校準過的機率**，適合需要信心值的場合。

**LinearSVC** 最小化 hinge loss

$$\mathcal{L} = \sum_i \max(0, 1 - y_i(w^\top x_i + b)) + \frac{1}{2C}\|w\|^2$$

只關心**間隔（margin）**，對遠離邊界的樣本不再施力。在類別界線模糊、
標註有雜訊的情況下，這個性質通常較穩。實測眼型 SVM 0.625 > LogReg 0.592。

兩者都用 `class_weight="balanced"`，理由同 §1.3。

> **部署細節：** 線上不載入 joblib（pickle 會綁定 scikit-learn 版本，實測
> 版本不合時 `predict_proba` 直接拋 `AttributeError`），而是把 `coef_` 與
> `intercept_` 抽成 npz，用 numpy 算 $Xw^\top + b$。線性模型的推論本來就只是一次矩陣乘法。

### 3.5 限制

- **推論成本高**：ViT-S/14 佔整個分析流程約 **57%** 的時間，是 MobileNet 的數倍
- **表示是固定的**：若預訓練表示裡沒有本任務需要的資訊，線性探針救不回來
- **需要 224×224 輸入**：比 CNN 的 96×96 大得多，前處理成本也較高

---

## 四、三者的本質差異

| | A. 幾何 + 決策樹 | B. CNN 微調 | C. DINOv2 + 線性探針 |
| --- | --- | --- | --- |
| 歸納偏置來源 | **人工設計的特徵** | 卷積結構 + ImageNet 預訓練 | 自監督預訓練表示 |
| 可訓練參數 | 樹的分割點（數十個）| ~2.5M | ~1.5K |
| 看得到什麼 | 只有那 9 個數字 | 像素、紋理、局部形狀 | 通用視覺語意 |
| 可解釋性 | **完全可解釋** | 低 | 低 |
| 推論成本 | 近乎零 | 低 | **高（57% 時間）** |
| 對 landmark 失敗 | **完全失效** | 只影響 ROI 裁切 | 只影響 ROI 裁切 |

**一句話總結三者的關係：**
方法 A 把領域知識寫死在特徵裡，方法 B 從資料學特徵，方法 C 借用別人在更大資料上學好的特徵。
**三者在小資料上的取捨，就是「歸納偏置強度」與「表達力」的取捨。**

---

## 五、實測比較

同一組 5-fold、按 identity 分組切分（同一個人的所有照片只落在同一折），
macro accuracy。資料：2311 張照片、1015 個身分。

| 部位 | 亂猜 | A. 幾何樹 | B. CNN | C. DINOv2 | 最佳 |
| --- | --- | --- | --- | --- | --- |
| face_shape | 0.20 | 0.462 | **0.521** | 0.424 | B |
| brow_shape | 0.33 | 0.448 | 0.511 | **0.543** | C |
| eye_shape | 0.25 | 0.439 | 0.562 | **0.625** | C |
| nose_shape | 0.50 | 0.782 | **0.849** | 0.846 | B |
| lip_shape | 0.25 | 0.422 | **0.553** | 0.513 | B |

**三個結論：**

1. **幾何樹五個部位全部墊底**，而且是在門檻重新校準之後——不是「沒校準才輸」。
2. **沒有任何一種方法全面勝出**：CNN 贏臉型／鼻型／唇型，DINOv2 贏眉型／眼型。
3. **沒有一項達到專題規格書訂的 0.70 門檻**，除了只有 2 類的鼻型。

### 5.1 更重要的發現：瓶頸不在模型

同期量到的所有「換模型／調參數／加資料」的增益，都小於**合併類別**：

```
眼型 6 類 → 桃杏眼 5 類      +0.069
唇型 5 類 → 4 類（M型唇併入）  +0.068
換模型（CNN ↔ DINOv2）        多數在 ±0.03 內
學習率 3e-4 → 6e-4            +0.022
資料量 +72%（眼型）           +0.020
```

**分數低的主因是類別定義本身有問題**，而不是模型不夠強。證據：
`prepare_roi_cache` 偵測到「內容完全相同、卻被標成不同類別」的樣本共 94 組，
眼型佔 57 組——**標註者自己就分不開那幾類**。

學習曲線（DINOv2 + LogReg，按身分抽樣 20%～100%）也支持這點：
除了眉型與眼型仍在緩升，其餘三個部位在現有資料量已經飽和。外推顯示
唇型要達到 0.60 需要約 8,765 張（現有的 16 倍），專題規模不可能達成。

---

## 六、組合策略：融合與投票

既然三者互有勝負，自然的問題是**能不能合併使用**。實測兩種做法：

### 6.1 決策層：信心門檻切換（hybrid）

規則：CNN 信心高於門檻就用 CNN，否則退回幾何規則。
**五個部位全部被拉低**（鼻型 0.913 → 0.883、唇型 0.469 → 0.391）。

演算法挑出來的最佳門檻多數是 **0.00**——意思是「無論 CNN 多沒把握都別聽規則式」。

### 6.2 決策層：三模型多數決

三個模型各給答案，2/3 相同即採用；三票全異時取 DINOv2。

| 部位 | 最佳單模型 | 多數決 | 差 |
| --- | --- | --- | --- |
| face_shape | 0.5431 | 0.5017 | **−0.041** |
| brow_shape | 0.5296 | 0.5427 | +0.013 |
| eye_shape | 0.6244 | 0.6382 | +0.014 |
| nose_shape | 0.8482 | 0.8558 | +0.008 |
| lip_shape | 0.5790 | 0.5573 | **−0.022** |

**五個部位有兩個變差，其餘增益都在雜訊內。**

### 6.3 特徵層：幾何特徵 ⊕ DINOv2 embedding

把 9 維幾何特徵標準化後接在 384 維 embedding 後面，餵同一個 LinearSVC：

| 部位 | 最佳單模型 | 特徵合併 | 差 |
| --- | --- | --- | --- |
| face_shape | 0.5431 | 0.5449 | +0.002 |
| brow_shape | 0.5296 | 0.5398 | +0.010 |
| eye_shape | 0.6244 | 0.6367 | +0.012 |
| **nose_shape** | 0.8482 | **0.8920** | **+0.044** |
| lip_shape | 0.5790 | 0.5135 | **−0.066** |

**只有鼻型明確受益。**

### 6.4 為什麼？錯誤重疊率解釋了一切

集成方法有效的前提是**成員模型錯在不同的樣本上**。量出「同時答錯 ÷ 至少一個答錯」：

| 部位 | CNN∩DINOv2 | CNN∩幾何 | DINOv2∩幾何 | **三者皆錯** |
| --- | --- | --- | --- | --- |
| face_shape | 0.48 | 0.42 | 0.43 | **0.26** |
| brow_shape | 0.45 | 0.36 | 0.37 | **0.22** |
| eye_shape | 0.42 | 0.35 | 0.35 | **0.20** |
| **nose_shape** | 0.24 | 0.22 | **0.09** | **0.03** |
| lip_shape | 0.45 | 0.36 | 0.39 | **0.22** |

**臉／眉／眼／唇有 20–26% 的樣本是三個模型同時答錯**——那些是界線本來就模糊的臉，
任何投票或融合都救不回來，而且兩票錯的會蓋掉一票對的。

**鼻型的三者皆錯只有 3%，DINOv2 與幾何的重疊只有 0.09**——三者真的互補。
這也符合直覺：鼻型的判別依據（鼻翼寬度比）本來就是**幾何量**，
幾何特徵在這裡提供了 DINOv2 embedding 裡沒有明確編碼的資訊。

> **結論：集成不是萬靈丹，它的效果由成員的互補性決定，而互補性是可以事先量出來的。**
> 本專案的做法是先量錯誤重疊率，再決定要不要做集成——而不是先做了再看分數。

### 6.5 部署考量：延遲

即使多數決有效，成本也不可忽視：DINOv2 佔分析時間 57%，
三模型投票代表**五個部位全部都要付這筆錢**。目前只有眼型使用 DINOv2。

以「每 0.01 macro 要付多少延遲」衡量，特徵層融合明顯優於決策層投票——
幾何特徵在 ROI 裁切階段就已經算出來了，接進線性頭的邊際成本趨近於零。

---

## 七、共用的實驗方法論

三種方法能夠直接比較，前提是評估協定完全相同。以下每一條都是**踩過坑才建立的**。

### 7.1 按身分（identity）分組切分

資料來自自行拍攝與影劇截圖，同一個人常有多張照片。隨機切分會讓同一張臉同時出現在
train 與 val，模型只要「認出這是誰」就能猜對標籤。

作法：InsightFace 抽 512 維人臉 embedding，L2 正規化後階層式聚類
（cosine 相似度 > 0.45 視為同一人），同一身分整組落在同一折。

**實測洩漏幅度最高 +0.113。**

### 7.2 5-fold 交叉驗證

單次 25% 切分的驗證集只有 43–105 張，一張分類結果就值 1–2 個百分點，
**分數的隨機波動約 ±0.1**——比實驗之間的差距還大。

5-fold 讓每張圖都輪流當過考題，並提供標準差作為判斷尺度。

### 7.3 雜訊底線

同一組設定完整重跑一次（只有隨機性不同），量到五個部位的差異在
**±0.016** 以內。這個數字是所有結論的下限：**差距小於它就是「沒有差別」**。

### 7.4 判定規則

> **領先幅度要大於標準差才算「勝」，否則只能說「不下定論」。**

本專案量到的多數模型間差距都不滿足這個條件，因此誠實的結論是「並列」。
唯一的例外是靠**方向一致性**建立的：學習率 6e-4 在五個部位的平均差全部為正、
25 折中 18 折勝出——五個部位是五個不同的資料集、不同類別數、不同難度，
它們同時往同一方向動，比任何單一部位贏 0.05 都更難用運氣解釋。

### 7.5 不用驗證集挑東西

用 val 選超參數會讓分數虛高（本專案犯過一次，虛高 0.018）。
需要選門檻時採用**巢狀切分**：在訓練折內再切一次來選，不碰外層 val。

---

## 附錄：對應的程式檔案

| 方法 | 特徵抽取 | 訓練 | 推論 |
| --- | --- | --- | --- |
| A. 幾何 + 決策樹 | `rule_features.py` | `tools/train_final_rule_trees.py` | `basic_rule_trees.py` |
| B. CNN | `prepare_roi_cache.py`（ROI 裁切）| `train_basic_cnn_roi.py` | `basic_roi_shadow.py`（ONNX）|
| C. DINOv2 | `tools/dinov2_cv_experiment.py`（embedding）| `tools/train_final_selected_models.py` | `basic_roi_shadow.py`（npz 線性頭）|

| 實驗 | 檔案 |
| --- | --- |
| 三方法同組 5-fold 對決 | `tools/dinov2_cv_experiment.py`、`tools/cv_rule_baseline.py` |
| 融合與多數決、錯誤重疊率 | `models/basic_features_roi/fusion_vote.json` |
| 學習曲線 | `tools/learning_curve.py` |
| 類別合併候選 | `tools/suggest_merges.py` |
| 個別標籤錯誤 | `tools/find_label_errors.py` |
| 分類表一致性 | `tools/check_label_tables.py` |
