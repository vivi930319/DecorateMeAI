# 給資料庫端：多重 Session（同瀏覽器多帳號分頁）規格

> 日期：2026-07-23
> 目的：讓「同一個瀏覽器、不同分頁登入不同帳號、同時運作」成為可能，且**不犧牲現有 HttpOnly cookie 的安全性**。
> 不包含任何實際金鑰、Email、照片、權杖或完整資料包。

---

## 一、為什麼需要你們配合（先講清楚邊界）

同一個瀏覽器對同一個網域只有**一份 cookie jar**，所有分頁共用。所以「每個分頁一份獨立 cookie」在瀏覽器層面**做不到**。業界（如 Gmail 的 `/u/0/`、`/u/1/`）的做法是：

- Cookie 還是一份、還是 HttpOnly（安全不退）。
- 但這份 cookie **同時代表多個並存的 session**（多登入）。
- 每個分頁帶一個**帳號選擇器**，伺服器據此挑出對應的那個 session。

在我們的架構裡，這個「帳號選擇器」我方已經有現成的：Gateway 目前對每個寫入都要求 `X-Expected-Actor`（一個不含 email 的 opaque actor id）。多帳號版本會把它從「比對是否相符、不符就擋」升級成「用它**選出**要用哪一個 session」。

**因此工作分工是：**

| 端 | 負責 |
|---|---|
| **資料庫端（你們）** | 提供「同一裝置可同時持有多個互不影響的會員 session」，以及每個 session 獨立的識別與撤銷。**這是本文件的重點。** |
| **Gateway（我方）** | 一份 Gateway session 內封裝多個上游會員 session（依 actor 分槽），依分頁帶的選擇器挑出對應槽位轉送。 |
| **前端（我方）** | 每個分頁綁定一個帳號、提供「新增帳號／切換帳號」介面、每個請求帶選擇器。 |

你們**不需要**知道分頁、不需要處理選擇器、不需要改 cookie 命名——那些由 Gateway 處理。你們只要確保「多個 session 能同時有效、能各自撤銷」。

---

## 二、必修項目（資料庫端）

1. **並存且互不作廢的 session。** 同一裝置／來源在短時間內登入**不同帳號**時，新登入**不得作廢其他帳號**先前已發出的有效 session。兩個不同會員的 session 必須能同時處於有效狀態。
   - 檢查你們是否有「同一使用者只允許一個 session」「同一裝置只留最後一個 session」「以裝置指紋綁定唯一 session」之類的邏輯——若有，會破壞多帳號，需要放寬成「以 session 為單位」。

2. **每個 session 有穩定、opaque 的識別碼。** 登入成功時回傳一個不含 email／PII 的 `sessionId`（或等價欄位），在該 session 的生命週期內固定。Gateway 用它當槽位鍵、也用於精準撤銷。
   - 不要用 email 或可反推身分的值當這個 id。

3. **逐一（per-session）登出與撤銷。** 提供「只登出這一個 session」的能力：
   - `POST /api/logout`（或既有登出端點）帶上要撤銷的 `sessionId`／該 session 的 cookie，**只作廢那一個 session**，不影響同裝置其他帳號的 session。
   - 停權、改密碼、刪除帳號時，作廢**該帳號的所有 session**；但不得波及其他帳號。

4. **per-session 的 `sessionVersion`／撤銷版本。**（與《資安修復與驗收規格》的 session 撤銷需求一致）每個 session 綁一個版本；帳號發生敏感變更（停權／降權／改密碼／刪除）時遞增，使舊 session 立即失效。撤銷粒度是「單一 session」，不是「整個裝置」。

5. **每個 session 獨立驗證。** 既有的「用 session 讀 `/api/members/{me}` 確認仍有效」流程，必須對每一個 session 各自成立——Gateway 會分別拿 A 的 session 驗 A、拿 B 的 session 驗 B。

6. **並存數量上限與 TTL。** 為每個裝置／來源設定同時並存 session 的上限（建議 3～5），超過時淘汰最舊的；每個 session 沿用現有的效期與續期規則。避免有人無限累積 session 當成放大攻擊面。

7. **登入回應維持既有欄位並補上 `sessionId`。** 成功登入回傳現有的 `member` 等資料**外加** `sessionId`；不得回傳完整 token 給瀏覽器可讀處（維持 HttpOnly cookie 模式，`sessionId` 只是不含 PII 的識別碼，供 Gateway 分槽用）。

---

## 三、介面契約（建議）

> 端點命名可沿用你們既有的；重點是行為與欄位。

**登入（既有）**
```
POST /api/login   { email, password }
→ 200  { success, member, sessionId, ... }   # 新增 sessionId（opaque，不含 PII）
   Set-Cookie: <你們的 session cookie; HttpOnly; Secure; SameSite=Lax; 有效期>
```
- 新登入**不得**帶有作廢其他帳號 session 的副作用。

**驗證（既有）**
```
GET /api/members/{me}         # 以該 session 的 cookie 驗證，回該 session 對應會員
→ 200 有效 / 401 已失效或撤銷
```

**登出（需支援 per-session）**
```
POST /api/logout   { sessionId }   # 或以該 session 的 cookie 判定
→ 只作廢這一個 session；同裝置其他帳號 session 不受影響
```

**撤銷（帳號層級事件）**
```
帳號停權／改密碼／刪除 → 作廢該帳號所有 session（sessionVersion 遞增）
                        → 不影響其他帳號的 session
```

---

## 四、必交測試

- 同瀏覽器先登 A、再登 B：A 的 session 仍有效，B 的 session 也有效；兩者可各自讀到**自己**的會員資料。
- 登出 A：A 的 session 立即 401，**B 仍可正常操作**。
- A 被停權／改密碼：A 全部 session 失效，**B 不受影響**。
- 跨帳號隔離仍成立：拿 A 的 session 不可讀寫 B 的資料（403）。
- 並存上限：超過上限時最舊 session 被淘汰，且淘汰不影響其餘。
- 撤銷粒度：撤銷單一 sessionId 只影響該 session。

---

## 五、我方會對應做的（供你們理解全貌，非你們工作）

- **Gateway**：把目前「一份 Gateway session 封一個上游會員 cookie」擴充成「封裝多個，以 actor/`sessionId` 分槽」；每個請求依 `X-Expected-Actor`（現有機制）**選出**對應槽位轉送；選不到槽位時對該帳號回「需重新登入」，而非現在的一律 `409`。
- **前端**：每個分頁綁定一個帳號槽位；提供「新增帳號」（等於再走一次登入、把新 session 加進槽位而不踢掉舊的）與「切換帳號」；所有請求帶該分頁的選擇器。
- 目前**已上線**的跨分頁保護（`X-Expected-Actor` 不符即擋）是這套的前身；多帳號版本是把「不符就擋」升級為「用它選對帳號」。在你們支援多重 session 之前，維持現狀（一次一個帳號、換帳號會擋）是安全且正確的。

---

## 六、回報格式

回覆：修改的檔案／migration、session 資料模型（欄位與索引）、上述 API contract 的實際行為、必交測試結果，以及部署／回滾步驟。不得附任何正式憑證、Email 或 Token 實際值。
