# Decorate Me 系統資安、效率與流程改善清單

> 盤點日期：2026-07-21
>
> 使用方式：完成一項就把 `[ ]` 改成 `[x]`。
> 禁止記錄：實際 API key、JWT、密碼、完整 email、照片、完整分析包與完整生成提示。
> Demo 例外：依專題需求，Ollama 完整 Prompt 暫時保留在受控展示回應中，**不得寫入一般存取 Log**；等 Ollama 調整完成後再移除。

---

## 一、目前狀態

### 資安

- [x] 前端不再保存 Face、Render、文字建議的上游 API key。
- [x] Face、Render 與 Gateway 的正式秘密已移至 Secret Manager；前端範例設定與文件不保存秘密值。
- [x] 前端登入、會員、商品與 Admin 請求已統一走同源 Gateway；瀏覽器只保存 HttpOnly session。
- [x] Gateway 已使用 Cloud Run IAM 呼叫 Face／Render；Face BASIC、Face PRO、Render 的 `allUsers` Invoker 已移除，只保留 Gateway 服務帳號，三個服務匿名健康檢查均為 `403`。
- [ ] **進行中：**Gateway 已有固定路徑白名單、請求大小限制與 Firestore 登入限流程式；執行服務帳號尚未獲最小 Firestore 權限，因此正式環境目前會退回單一 instance 記憶體限流。
- [x] Firebase Hosting 已加入會員、商品、Admin 與媒體同源 rewrite；瀏覽器不再取得資料庫原始網址。
- [x] 正式 Gateway 的外部 Ollama Tunnel 目前關閉。
- [x] 公開 `config.local.js` 不含上游服務金鑰。
- [x] 基本 Log 只記錄 request ID、method、path、status 與耗時，不主動記錄 request body。

### 效率

- [x] 使用者上傳照片已有 Web Worker／OffscreenCanvas 壓縮流程。
- [x] 分析照片預設壓到最長邊 1024px、JPEG 品質 0.78。
- [x] 商品與風格圖片已有原生 `loading="lazy"` 與 `decoding="async"`。
- [x] Render 使用非同步 job，前端不需要等待單一長連線。
- [x] Render 有短時間請求去重，避免重複消耗模型額度。
- [x] 七張風格圖已轉成 720px WebP，總大小由 10,008,310 bytes（約 10 MB）降到 263,136 bytes（約 263 KB），減少 97.4%。
- [x] `/assets/**` 已設定一年 immutable 快取。
- [x] 圖片補上寬高、低優先載入與 Referrer Policy。

### 流程

- [x] 已有 Gateway、Hosting、Face、Render 的基本健康檢查。
- [x] 已建立歷史流程追蹤與 Gateway 新手文件。
- [x] 前端部署腳本會執行語法檢查、smoke test、秘密掃描並檢查 Firebase 部署結果；線上 `/public-config` 已驗證只回同源路徑。

---

## 二、P0：正式商用前必須完成

### P0-1：GCS 私有圖片與短效 Signed URL

目前 Bucket 已私有化。使用者透過登入後的 Gateway 媒體端點讀圖；真正的 GCS V4 Signed URL 尚待服務帳號自簽權限明確授權。

- [x] `decorate-me-renders` 已禁止 public access；匿名直開物件實測為 403。
- [x] 已啟用 Public Access Prevention、Uniform Bucket-Level Access，並移除 `allUsers` 讀取權限。
- [x] Lifecycle 只會在 2 天後刪除 `temporary/`；`retained/` 正式收藏不受暫存規則影響。
- [x] 新渲染先存 `temporary/`，收藏後移至 `retained/{opaqueOwnerId}/{jobId}`。
- [ ] **進行中：**收藏資料以穩定 Gateway 媒體路徑與 `renderJobId` 定位；仍需資料庫端確認不再保存任何舊公開 URL。
- [x] 已增加登入後讀取本人圖片的 Gateway `/media/render/{jobId}` 端點。
- [ ] **待明確 IAM 授權：**若要改成真正 GCS V4 Signed URL，需只對指定服務帳號授予自簽 Token Creator；目前使用安全的登入驗證串流代理。
- [x] 前端保存穩定媒體路徑，不把一次性 provider URL 當永久收藏資料。
- [x] Gateway 對外回應會移除 provider 暫存網址，不再把它當圖片 fallback。

驗收方式：未登入者直接開 object URL 應為 403；登入會員只能取得自己的圖片短效網址。

### P0-2：會員刪除必須真正跨服務清除

目前前端能呼叫 `DELETE /api/members/{email}`，但 repository 內沒有會員資料庫後端，因此無法從這個專案證明照片、渲染圖、分析結果、收藏和工作紀錄都已清除。

- [ ] 會員刪除改用不可重複執行也安全的 deletion job。
- [x] GCS 擁有者目錄使用不可逆雜湊後的 opaque owner ID，不使用完整 email。
- [ ] 清除會員主資料、session、點數、任務與主題兌換資料。
- [x] 刪除 saved look 時，Gateway 會同步刪除對應 Render job 與 GCS 圖片物件。
- [x] 新收藏會保留 `renderJobId`，並由 Gateway 維護物件位置；不把圖片內容寫入 Log。
- [ ] **進行中：**會員刪除已同步清除 Render job、渲染物件與工作 payload；Face job 與資料庫端所有分析欄位仍需有效帳號端到端確認。
- [ ] **進行中：**Face Basic TTL 已啟用；Face Pro、Render 與 Gateway 限流集合的 TTL 建立中，需等雲端狀態全部變成 `ACTIVE`。
- [x] 會員刪除會依 opaque owner ID 清除 `temporary/` 與 `retained/` 相關圖片。
- [ ] 失敗步驟需重試，並保存不含 PII 的刪除狀態。
- [ ] Admin 顯示 `pending → deleting → completed／failed`，不能收到 HTTP 200 就假設全部完成。
- [ ] 建立自動化整合測試，刪除後逐項確認查不到資料與物件。

### P0-3：Log 去識別化

目前共用 request logger 會記錄完整 URL path。會員 API 把 email 放在 URL path 時，Cloud Logging 可能留下完整 email。

- [ ] Log path 對 email、手機、token 與敏感 ID 做遮罩。
- [ ] 會員 API 改用 `memberId`，避免 `/members/{email}`。
- [x] 一般 access log 禁止記錄 request／response body、照片 base64、分析包、權杖與 Prompt。
- [ ] **專題展示例外：**目前 Ollama 完整 Prompt 依使用者要求保留在受控展示回應；Ollama 完成後再移除。不得寫入一般 Log。
- [x] Admin audit event 與畫面使用雜湊 `actorId`，不保存或顯示完整管理員 email。
- [ ] 例外 stack trace 需過濾 URL query、Authorization 與上游錯誤內容。
- [ ] 設定 Cloud Logging 保存期限與存取權限。

### P0-4：登入 Token 與立即撤銷

目前 Gateway 同時設定 HttpOnly cookie，並在 JSON 回傳 access token；前端會把 Token 放入 `sessionStorage`。若發生 XSS，JavaScript 仍可能讀到 Token。

- [x] 正式同源模式只使用 `HttpOnly + Secure + SameSite=Lax` cookie。
- [x] session-only 模式不在登入 response JSON 回傳 access token。
- [x] 前端受保護 API 遇到第一個 `401` 時會統一清除殘留 session、停止 Admin 背景刷新並回到登入頁；同批並行請求只處理一次，避免 401 請求風暴與重複彈窗。
- [x] 正式前端已移除 `sessionStorage.memberAccessToken`，並會清理舊版 token key。
- [x] 完整分析包已由 `localStorage` 改為本分頁 `sessionStorage`，並設定 30 分鐘 TTL。
- [ ] Token TTL 由 2 小時評估縮短至 15～30 分鐘。
- [ ] 會員停權、刪除、改密碼時能立即撤銷 session。
- [ ] 資料庫增加 `sessionVersion`／`tokenVersion`，敏感操作重新確認帳號狀態。

### P0-5：Gateway 成為前端唯一 API 入口

- [x] `/public-config` 不再把會員與商品資料庫原始網址交給瀏覽器。
- [x] 前端登入只呼叫 Gateway，同一組密碼不再送往兩個入口。
- [x] 會員、收藏、點數、任務、主題、商品與 Admin 操作已統一經過 Gateway。
- [x] Gateway 通用 Proxy 已補上 `PATCH`，GET／POST／PATCH／DELETE 均有測試涵蓋。

### P0-6：臨時 Tunnel 退出正式環境

- [ ] 會員資料庫由 `trycloudflare.com` 搬到固定 Cloud Run／正式 API 網域。
- [ ] 商品資料庫與爬蟲服務搬到固定受控服務。
- [ ] Ollama 若需要正式使用，改成 Cloud Run、VPC 或固定受控端點。
- [ ] 不把臉部摘要、prompt 或會員資料送到未受控公開 Tunnel。
- [ ] Tunnel 只允許 Demo 假資料，並設定明確到期日。

---

## 三、P1：近期應完成

### P1-1：前端 XSS 與安全標頭

- [x] 增加 HSTS、`nosniff`、`DENY frame`、Referrer Policy 與 Permissions Policy。
- [ ] 將大量 inline script、inline style、`onclick` 改成事件監聽器。
- [ ] 完成後啟用嚴格 Content Security Policy，不使用 `unsafe-inline`。
- [ ] 所有 API／資料庫文字輸出前統一 escape／sanitize。
- [ ] 管理員輸入的商品圖片 URL、來源 URL、名稱與描述做 schema 驗證。
- [ ] 加入 CSRF token 或 double-submit cookie 保護管理員修改與刪除操作。

### P1-2：權限與管理員功能

- [ ] Admin 所有讀寫都由後端確認 `role=admin`，前端隱藏按鈕不能當成權限控制。
- [ ] Admin Demo 功能在專題結束立即關閉，不等到 2026-12-31。
- [ ] 高風險操作加入重新驗證與二次確認。
- [ ] 商品刪除、會員停權與會員刪除保存不含 PII 的 audit event。
- [ ] Secret Accessor 只授權需要的服務帳號，移除共用 compute service account。

### P1-3：限流與防濫用

- [ ] Gateway 登入限流程式已支援 Firestore；仍待為執行服務帳號授予最小資料存取權限，正式環境才不會 fallback 到記憶體。
- [ ] OTP 加入 email、IP、裝置與每日總量限制。
- [ ] Face／Render 加入每會員配額與全站成本上限。
- [ ] 重複 job 使用 idempotency key，避免重新整理造成重複扣款。
- [ ] 429 回傳一致的 `Retry-After` 並在前端顯示等待時間。

### P1-4：圖片與媒體安全

- [ ] 上傳端驗證 MIME magic bytes，不只相信副檔名或 Content-Type。
- [ ] 移除 EXIF／GPS 資料。
- [ ] 限制像素數、檔案大小與解碼後記憶體。
- [ ] 圖片轉碼成 JPEG／WebP 後再送分析服務。
- [x] 使用 `temporary/` 獨立暫存區與 2 天生命週期規則，自動刪除未收藏工作。
- [ ] Avatar 不保存為 sessionStorage base64，改存私有 object name。

---

## 四、P1：前端與後端效率

### P1-5：圖片載入

- [x] 風格圖改成 WebP 與 720px 尺寸。
- [x] 卡片圖片 lazy loading、async decoding、低優先載入。
- [x] 版本化資產使用一年快取。
- [ ] 商品資料庫建立 320px、640px、1280px 三種縮圖。
- [ ] 前端使用 `srcset`／`sizes`，手機不下載桌面大圖。
- [ ] 外部商品圖片經受控 image proxy／CDN 轉成 WebP／AVIF。
- [ ] 首屏唯一主圖使用 `fetchpriority="high"`，其他圖片維持 lazy。
- [ ] 圖片失敗時顯示本機 placeholder，並避免無限重試。

### P1-6：JavaScript 與 CSS

目前 `router.js` 約 272 KB、`main.css` 約 158 KB，而且所有頁面一次載入。

- [ ] Router 按頁面拆分成 dashboard、analysis、products、profile、admin chunks。
- [ ] Admin 程式只有管理員進入時才載入。
- [ ] 使用正式 build pipeline 進行 minify、tree-shaking 與檔名 hash。
- [ ] 移除重複 CSS 規則與未使用樣式。
- [ ] Google Fonts 減少字體家族與 weight，或自架 WOFF2 子集。
- [ ] 建立 Lighthouse／Core Web Vitals 基準：LCP、CLS、INP。

### P1-7：API 與資料庫

- [x] Face BASIC／PRO 未帶或帶錯 Key 時一致回傳 `401`；兩個正式服務均已部署與驗證。
- [ ] 補回商品稽核紀錄與爬蟲搜尋預覽端點，或移除前端尚未有後端實作的入口。
- [ ] Admin 不要逐會員抓 saved looks 和點數，改成批次 summary endpoint。
- [ ] 避免 N+1 查詢，資料庫建立 memberId、createdAt、status 索引。
- [ ] Render job 輪詢使用指數退避，或改為 SSE／WebSocket／通知機制。
- [ ] 大型照片不要放入 JSON base64；改用 multipart 或私有 object upload。
- [ ] 分析包只保存必要欄位與 object ID，避免重複保存圖片內容。
- [ ] 為慢查詢、上游逾時和模型成本建立監控指標。

### P1-8：Demo 穩定性

- [ ] Demo 前將必要 Cloud Run 服務設定 `min-instances=1`，結束後調回 0 控制成本。
- [ ] 準備固定假帳號、固定測試照片和離線備援結果。
- [ ] Demo 流程不要依賴臨時 Tunnel 或即時爬取第三方網站。
- [ ] 首頁先載入必要資料，非必要 Admin／商品推薦延後載入。

---

## 五、P2：流程與維運成熟度

### P2-1：開發與部署流程

- [ ] 建立 dev、staging、production 三套環境與不同秘密。
- [ ] 統一正式前端原始碼位置與部署來源，不再從未提交的工作目錄部署，並清楚標示舊 Hosting 目錄已停用。
- [ ] 不再手動修改正式 `config.local.js`，改由部署流程產生公開設定。
- [ ] 更新已過期的 smoke test，並由 CI 在每次部署前自動執行語法、單元、API contract 與 secret scan。
- [ ] 部署前確認工作目錄乾淨，部署版本必須可以追溯至明確 commit。
- [ ] 部署後自動檢查 Gateway health、主要頁面與公開設定。
- [ ] Cloud Run 採用無流量 revision → smoke test → 逐步切流。
- [ ] 保留一鍵 rollback 指令與前一個已知正常 revision。
- [ ] 資料庫 schema migration 必須可重跑、可回復。

### P2-2：監控與事件處理

- [ ] 建立 401、403、429、5xx、模型失敗率與延遲告警。
- [ ] 建立每日 Render 成本、會員配額與異常流量報表。
- [ ] 建立 Secret 洩漏處理流程：輪替、停用、稽核、記錄。
- [ ] 建立資料刪除失敗告警與人工重試入口。
- [ ] 每季做一次備份還原演練。

### P2-3：資料治理

- [ ] 定義照片、分析包、Render 圖、Log、會員資料的保存期限。
- [ ] 區分必要資料、選擇性資料與 Demo 資料。
- [ ] 建立會員資料下載與刪除申請流程。
- [ ] 隱私政策說明 AI 供應商、保存時間與刪除方式。
- [ ] 專題 Demo 資料與正式會員資料使用不同專案或 Bucket。

---

## 六、建議執行順序

```text
第一階段：GCS 私有化 + Signed URL + 可驗證的會員刪除
    ↓
第二階段：移除 sessionStorage Token + Log 遮罩 + Tunnel 下線
    ↓
第三階段：CSP／CSRF／管理員權限與分散式限流
    ↓
第四階段：圖片 CDN／縮圖 + JS 拆包 + API 批次查詢
    ↓
第五階段：CI/CD、監控、備份還原與資料治理
```

最優先不是增加更多功能，而是先確保：會員刪除真的能清乾淨、圖片永遠不是公開 Bucket、Log 不會留下 email／照片／完整 prompt，以及瀏覽器拿不到任何長期秘密。
