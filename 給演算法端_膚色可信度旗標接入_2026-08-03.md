# 給演算法端 — 膚色可信度旗標 `labReliable` 接入說明

**日期：** 2026-08-03
**提出方：** 臉部分析端 + DECORATE ME 前端
**對象：** 商品推薦演算法維運者
**影響：** `analysisPackage.faceAnalysis.skinTone` 新增兩個欄位；`schemaVersion` 升為 `2026-08-v2`
**要你們做的事：** 粉底推薦在 `labReliable === false` 時不要做 ΔE 比色

---

## 0. 一句話

頭髮蓋住臉頰時，我們給你們的 `skinTone.lab` **會是錯的，而且不會有任何錯誤訊息**。現在多一個旗標告訴你們什麼時候不能信它。

---

## 1. 問題有多大

臉部分析用「膚色範圍過濾」把非皮膚像素排除，但那組範圍**分不開棕髮與皮膚**——兩者在 LAB／HSV／YCrCb 三個色彩空間裡都重疊。

實測（`tools/measure_hair_contamination.py`，40 張臉、160 組遮蔽案例，把每張照片自己的頭髮移植到臉頰）：

| 項目 | 數字 |
| --- | --- |
| 真實頭髮像素通過既有膚色過濾的比例 | 平均 **53%** |
| 膚色 ΔE > 5 的案例比例 | **19%** |
| 最差 ΔE | **45** |

ΔE 45 足以從「中等亮白自然色」跳到「健康玫瑰色」。也就是說**每五次就有一次粉底色號會明顯配錯**，而使用者跟你們都看不出來。

黑髮擋得住（LAB 的 `L>=35` 下限），**棕髮與染髮完全擋不住**。

---

## 2. 我們做了什麼

**紋理過濾**：皮膚平滑、頭髮有高頻紋理，這是顏色失效後還能用的訊號。最差 ΔE 從 45.01 降到 **22.41**，ΔE>5 從 19% 降到 15%。**有效但沒有消除。**

**可信度標記**：所以再加一個能預測「這次算錯了」的訊號。四個候選裡「頰部 ROI 內 L 的 MAD」分離度最好：

```
乾淨照片   中位 6.67   p95 9.55
算錯案例   中位 15.69
算對案例   中位 8.24
```

門檻取 9.5（乾淨照片的 p95）。端到端結果：

```
被標成『可信』的案例：最差 ΔE 4.05，ΔE>5 佔 0%   ← 零漏報
乾淨照片誤報率：5%
```

**所有實質算錯的案例都會被標出來**，代價是 5% 的乾淨照片被多標一次。

---

## 3. 你們會收到什麼

```jsonc
"skinTone": {
  "season": "autumn",
  "level": "健康玫瑰色",
  "lab": { "L": 62.3, "a": 12.1, "b": 17.4 },
  "labSource": "正面照",

  // 新增
  "labReliable": false,
  "labReliability": {
    "measured": true,
    "reliable": false,
    "spread": 15.7,      // 實測的 MAD
    "threshold": 9.5,
    "hint": "臉頰被頭髮或陰影遮住，膚色可能不準；把頭髮撥到耳後、在均勻光線下重拍會更準確。"
  }
}
```

**`lab` 照樣回傳**，因為它仍是當下最好的估計。要不要拿它去比色號，由你們決定。

**欄位缺漏時請視為 `true`**（舊資料包、舊版前端），維持既有行為。

---

## 4. 建議做法

| 商品類別 | `labReliable` 為 `true` | `labReliable` 為 `false` |
| --- | --- | --- |
| `base` 粉底 | 照舊：`skinTone.lab` ΔE 比色 | **不要做 ΔE 比色**，改用季型 + `skinTone.level` 排序 |
| `lip` 唇彩 | 照舊：`lipLab` ΔE | 不受影響（唇色取樣不在臉頰） |
| 其他 | 照舊 | 照舊 |

只有粉底受影響——它是唯一直接吃 `skinTone.lab` 做色差的類別。

如果你們能在回應裡標明「這次沒有用色差排序」，前端可以順勢提示使用者重拍，那會更好，但不是必要。

---

## 5. 相容性

- `schemaVersion` 已從 `2026-06-v1` 升為 `2026-08-v2`
- 欄位是**新增**，沒有任何既有欄位被改名或移除
- 不接這個旗標也不會壞，只是維持現在「有 19% 機率靜默配錯粉底」的狀態

---

## 6. 想自己重跑數字

```
python tools/measure_hair_contamination.py
```

那支腳本會把每張照片自己的頭髮移植到臉頰再量，所以不需要另外標註遮擋資料。如果之後調整 `SKIN_TEXTURE_STD_MAX` 或 `SKIN_SPREAD_UNRELIABLE`，請重跑一次並更新本文件的數字。

---

## 7. 已知限制，先說在前面

- **門檻是在頰部 ROI 上校準的。** 頰部太小（landmark 圈出來不足 100 px）時我們不宣稱可信度，`measured` 會是 `false`、`reliable` 給 `true`——那種情況等於沒有保護，不是保證安全。
- **PRO 的膚色只採正面照。** 側面照不再參與膚色平均（2026-08-05 起），所以 `labSource` 一律是 `正面照`，`labReliable` 就是正面那張的判定，沒有「一半資料沒把關」的問題。原因：側面照的頰部 ROI 是 FaceMesh 在透視壓縮下給的，遠側臉頰整塊是腦補的，可信度判定在上面量不到東西——實測 120 張側面照，MAD 中位數 2.75、只有 3% 被判不可信，而正面是 5.88／8%。也就是說同一個閘門套到側面照上比正面還鬆，剛好裝在最沒把握的那半邊。與其補一個量不準的閘門，不如不要把側面照混進膚色。側面照仍用於側臉鼻型。
- **`sidePhotoUsed` 不等於「膚色用了側面照」。** 它現在表示「這次分析有收到並用到側面照」（用途是側臉鼻型），膚色永遠只來自正面照。不要再從 `labSource` 反推有沒有側面照。
- **偵測率只有 50%** 看起來很低，但那是對的：另一半遮蔽案例本來就沒算錯，不該標。要看的是「被標成可信的案例最差 ΔE 4.05」這個數字。
