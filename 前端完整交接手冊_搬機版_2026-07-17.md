# Decorate Me 前端完整交接手冊（搬機版）

文件日期：2026-07-17  
本資料夾：`C:\Users\isach\OneDrive\桌面\web_frontend`  
目前分支：`dev_makeup`  
目前 Git 遠端：`https://github.com/vivi930319/new_poject.git`  
Firebase 專案：`decorate-me`  
線上網址：`https://decorate-me.web.app`

這份文件記錄「這個 web_frontend 資料夾現在實際包含什麼、怎麼啟動、怎麼部署、如何與後端／資料庫串接，以及搬機時要注意什麼」。

---

## 1. 目前版本判定

原本的 `web_frontend` 是較舊的 `dev_makeup` 前端版本，與根目錄目前完整版本 `firebase-hosting-full` 有差異。2026-07-17 已完成以下同步：

- 以根目錄 `firebase-hosting-full/public` 的最新程式版本覆蓋本資料夾的 `index.html`。
- 同步最新 `js/`、`css/`、`pages/`、圖片素材、`dev_server.py`、`frontend_smoke_check.js`。
- 加入目前完整版本的 `pages/ai-render.html`。
- 保留本資料夾原本的 `firebase.json`、`.firebaserc`、`deploy.ps1` 與既有文件。
- **保留本資料夾的 `config.local.js`**，因為它是環境專用設定，包含網址與秘密金鑰，不應被程式版本覆蓋。

同步前有修改的檔案已備份至：

`C:\Users\isach\AppData\Local\Temp\web_frontend_before_full_sync_20260717`

目前工作區原本的未提交修改不要用 `git reset --hard` 清掉；若要查看同步前內容，使用上述備份資料夾。

---

## 2. 重要安全警告

### 2.1 `config.local.js` 不能上傳 GitHub

`config.local.js` 會被 `index.html` 直接載入，也會被瀏覽器下載。這個檔案目前含有服務 API Key，因此：

- 不要把它 commit 或 push。
- 不要放到公開壓縮檔或傳給維修人員。
- 目前曾經公開過的 FACE、RENDER、SUGGESTION 金鑰應視為已曝光並輪替。
- 長期應改成前端只呼叫 AI Gateway，前端不再持有 AI 服務金鑰。

安全範本是：

```text
config.local.example.js
```

搬到新電腦時：

```powershell
Copy-Item config.local.example.js config.local.js
```

再由安全管道填入目前環境設定。

### 2.2 目前登入憑證狀態

同步後的完整版本已移除舊版「把帳號密碼存入 sessionStorage、遇到 401 自動重登」的流程。現在前端：

- 不保存明文密碼。
- 使用會員資料庫回傳的短期 `memberAccessToken`。
- Token 存在本分頁 `sessionStorage`。
- 對會員資料庫 API 以 `Authorization: Bearer <token>` 呼叫。
- 登出時清除 `memberAccessToken`、`beautyProfile`、`beautyUser`。

`sessionStorage` 的 Bearer Token 仍會受到 XSS 影響；長期建議改成 `HttpOnly + Secure + SameSite` Cookie。

### 2.3 外部網址不是秘密，但 Tunnel 不穩定

最後一次 `config.local.js` 記錄的服務網址：

```text
Face BASIC       https://face-basic-258021445391.asia-east1.run.app
Face PRO         https://face-pro-258021445391.asia-east1.run.app
Render           https://replicate-render-eu5pq7c53a-de.a.run.app
Suggestion      https://morning-deeper-kick-medium.trycloudflare.com
Member/Product  https://stainless-exit-spot-providing.trycloudflare.com
Crawler         未獨立設定，會 fallback 到 productUrl
```

Cloudflare Tunnel 重啟後可能改網址或失效。搬機後必須先測 `/health`，不能只相信設定檔裡的 URL。

---

## 3. 前端技術與檔案結構

這是原生 HTML／CSS／JavaScript 靜態網站：

- 不使用 React、Vue 或其他前端框架。
- 沒有 `package.json`，不需要 `npm install`。
- Firebase Hosting 只提供靜態檔案。
- 所有 API 與本機狀態集中在 `js/api.js`。
- Hash 路由、畫面、登入後流程與後台集中在 `js/router.js`。
- 商品、風格與展示資料集中在 `js/data.js`。
- 大圖壓縮使用 `js/image-worker.js`，不支援 Worker 時會退回主執行緒。

```text
web_frontend/
├── index.html                      登入、app shell、script 載入順序
├── 404.html                        Firebase Hosting 404 頁
├── firebase.json                   Hosting 根目錄為目前資料夾 `.`
├── .firebaserc                     指向 Firebase project decorate-me
├── .gitignore                      排除 local config、logs、docs、快取
├── config.local.js                 本機網址與金鑰，不進版控
├── config.local.example.js         無秘密設定範本
├── deploy.ps1                      戳版本號後部署 Firebase
├── dev_server.py                   5500 no-cache 靜態伺服器
├── frontend_smoke_check.js         Node 前端 smoke test
├── README.md                       專案簡介與快速說明
├── css/
│   └── main.css                    全站樣式、手機版、後台版面
├── js/
│   ├── api.js                      API、Auth、AdminStore、Cart、MemberRewards
│   ├── config.js                   runtime config 初始化
│   ├── config.production.example.js 正式 URL 範本
│   ├── data.js                     商品、風格、展示資料
│   ├── image-worker.js             圖片壓縮 Worker
│   └── router.js                   Hash 路由與所有主要畫面邏輯
├── pages/
│   ├── dashboard.html              使用者首頁
│   ├── analysis.html               BASIC／PRO 臉部分析
│   ├── style.html                  風格試妝
│   ├── compare.html                妝容前後對比
│   ├── suggestion.html             妝容建議
│   ├── products.html               商品推薦
│   ├── favorites.html              商品收藏
│   ├── history.html                分析紀錄
│   ├── profile.html                會員中心、點數、saved looks
│   ├── admin.html                  後台
│   └── ai-render.html              AI 渲染頁面
├── docs/                           既有規格、簡報與設計文件
└── 圖片素材                         各風格展示圖與靜態圖片
```

---

## 4. `index.html` 載入順序

目前順序不能任意調整：

```text
js/config.js
config.local.js
js/data.js
js/api.js
js/router.js
```

`config.local.js` 必須在 `api.js` 前載入，否則 `ApiConfig` 讀不到服務網址。若刪掉或改名，前端會顯示 API URL 未設定。

---

## 5. API 服務設定與端點

`js/api.js` 的 `ApiConfig.services` 有以下服務：

| service | 設定來源 | 主要端點 |
|---|---|---|
| `faceBasic` | `faceBasicUrl`、`faceApiKey` | `/v1/face/analyze/basic`、`/v1/face/pose`、`/v1/face/jobs/basic` |
| `facePro` | `faceProUrl`、`faceApiKey` | `/v1/face/analyze/pro`、`/v1/face/jobs/pro` |
| `textSuggestion` | `textSuggestionUrl`、`textSuggestionApiKey` | `/suggest` |
| `render` | `renderUrl`、`renderApiKey` | `/render`、`/render/jobs` |
| `product` | `productUrl` | `/api/products`、`/recommend-products` |
| `crawler` | `crawlerUrl` 或 `productUrl` | `/api/crawler/product-preview` |
| `memberDatabase` | `memberDatabaseUrl` | `/api/login`、會員／點數／收藏 API |

### 5.1 臉部分析

```text
GET  /health
POST /v1/face/pose
POST /v1/face/analyze/basic
POST /v1/face/analyze/pro
POST /v1/face/jobs/basic
POST /v1/face/jobs/pro
GET  /v1/face/jobs/{jobId}
GET  /v1/face/jobs/{jobId}/result
```

BASIC 流程：

```text
照片／相機 → ImagePipeline → POST basic job → 每秒輪詢 → 取得 result
```

PRO 流程：

```text
正面照 + 側面照 → POST pro job → 輪詢 → 合併 faceAnalysis
```

Job 建立後會回傳 `jobId` 與 `resultToken`。之後查詢狀態或結果必須帶：

```text
X-Job-Token: <resultToken>
```

前端 `waitForFaceJob()` 最多輪詢 120 次，每次間隔 1 秒；遇到 `completed` 取得結果，`failed` 直接顯示錯誤。

### 5.2 Ollama 妝容建議

```text
POST /suggest
POST /suggest/stream
```

前端送出：

```json
{
  "analysisPackage": {},
  "faceAnalysis": {},
  "style": "daily",
  "language": "zh-TW",
  "userNote": ""
}
```

回傳內容會寫入：

```text
analysisPackage.generativeText.suggestion
```

### 5.3 Render 妝容渲染

正式主要使用非同步 API：

```text
POST /render/jobs
GET  /render/jobs/{jobId}
```

前端只傳 `image`、`styleId`、`analysisPackage`、`strength`；後端應自行組合 prompt。前端每 2 秒輪詢一次，保險絲為 5 分鐘。

渲染完成後必須使用永久 `afterImageUrl`。`replicate.delivery` 是會過期的臨時網址，不應存入會員 saved looks。

### 5.4 會員資料庫

登入／註冊：

```text
POST /api/login
POST /api/register
POST /api/send-otp
POST /api/verify-otp
```

會員與後台：

```text
GET    /api/members
PATCH  /api/members/{email}
DELETE /api/members/{email}
GET    /api/members/{email}/points
GET    /api/members/{email}/check-in
POST   /api/members/{email}/check-in
GET    /api/members/{email}/tasks
POST   /api/members/{email}/tasks/{taskId}/claim
POST   /api/members/{email}/theme-shop/{themeId}/redeem
GET    /api/members/{email}/saved-looks
POST   /api/members/{email}/saved-looks
DELETE /api/members/{email}/saved-looks/{id}
```

商品與爬蟲：

```text
GET    /api/products
POST   /api/products
PATCH  /api/products/{id}
DELETE /api/products/{id}
POST   /api/crawler/product-preview
```

會員登入成功後，前端以資料庫回傳的 `role`、`level`、`status`、`allowedPages` 判定權限；不能用 Email 字串推論管理員。

### 5.5 會員資料庫的必要回應欄位

```json
{
  "name": "王小美",
  "email": "user@example.com",
  "level": "一般會員",
  "role": "member",
  "status": "active",
  "allowedPages": ["dashboard", "analysisBasic", "profile"],
  "vipRequested": false,
  "renderQuota": {
    "dailyLimit": 3,
    "remaining": 2,
    "resetAt": "2026-07-17T00:00:00+08:00"
  }
}
```

---

## 6. 會員、收藏、點數與購物車狀態

### 6.1 saved looks

建立資料使用：

```json
{
  "style": "daily",
  "beforeImageUrl": "https://storage.googleapis.com/...",
  "afterImageUrl": "https://storage.googleapis.com/...",
  "analysisSummary": {}
}
```

前端規則：

- 遠端成功後記錄 `remoteId`。
- 有 `remoteId` 的資料，刪除時先呼叫資料庫 DELETE。
- 資料庫刪除成功後才刪本機 localStorage。
- 資料庫失敗時保留本機資料並提示使用者。
- localStorage 是離線快取，不是正式資料庫。
- 本機快取最多保留 20 筆，遠端資料不應因此遺失。

### 6.2 點數

前端已接：

```text
GET  /api/members/{email}/points
GET  /api/members/{email}/check-in
POST /api/members/{email}/check-in
```

但 `MemberRewards` 仍保留 localStorage 的點數、簽到、任務與主題商店邏輯，作為舊功能／fallback。正式版應讓資料庫成為唯一來源：API 失敗時不可在前端自行加點或扣點。

### 6.3 購物車

目前 `Cart` 使用 localStorage key：

```text
beautyCart
```

資料格式概念：

```json
[
  { "id": 1, "qty": 2 }
]
```

目前沒有會員資料庫購物車 API，因此不能跨裝置同步；正式結帳前要由後端重新確認商品價格、庫存與會員資格。

---

## 7. 頁面與主要功能

| 頁面 | 功能 |
|---|---|
| `dashboard.html` | 品牌首頁、入口、風格推薦 |
| `analysis.html` | BASIC／PRO 照片上傳、相機、進度與分析結果 |
| `style.html` | 風格卡片、Ollama 建議、風格試妝 |
| `compare.html` | 妝前／妝後對比、長按／拖曳互動、收藏 |
| `suggestion.html` | 妝容文字建議、渲染結果、收藏 |
| `products.html` | 商品分類、商品詳情、加入購物袋、相似色推薦 |
| `favorites.html` | 商品收藏 |
| `history.html` | 分析紀錄 |
| `profile.html` | 會員資料、會員等級、點數、任務、主題、saved looks |
| `admin.html` | 會員、商品、爬蟲與資料庫即時狀態管理 |
| `ai-render.html` | AI 渲染相關獨立畫面 |

---

## 8. 後台功能

目前後台可做：

- 讀取會員列表。
- 修改會員 `role`、`level`、`status`、功能權限。
- 顯示會員 saved look 數量與點數。
- 點擊查看會員的妝容收藏縮圖。
- 修改與管理商品。
- 以商品網址呼叫爬蟲預覽，再由管理員確認上架。
- 會員 session 逾時時重新登入並重試。

目前完整版本已加入前端 `Api.deleteMember(email)` 與後台會員刪除流程，但是否能真正刪除資料，取決於會員資料庫端是否已實作：

```text
DELETE /api/members/{email}
```

若資料庫端點不存在，前端必須提示失敗，不能假裝刪除。會員刪除時資料庫端也要處理 saved_looks、tryon_records、points、favorites、cart 與 GCS 圖片清理。

---

## 9. 本機啟動

### 9.1 設定檔

```powershell
cd "C:\Users\isach\OneDrive\桌面\web_frontend"
Copy-Item config.local.example.js config.local.js
```

再填入目前可用的服務 URL。若沿用原本的 `config.local.js`，先確認會員資料庫 Tunnel 沒有過期。

### 9.2 啟動前端

推薦使用 no-cache server：

```powershell
python dev_server.py
```

或：

```powershell
python -m http.server 5500
```

瀏覽器開啟：`http://127.0.0.1:5500/?v=local#dashboard`

前端只是一個靜態伺服器，不會自動啟動 BASIC、PRO、Ollama、Render 或會員資料庫。那些服務需要另外啟動或使用已部署網址。

### 9.3 本機服務預設 Port

```text
Frontend             5500
Face BASIC           8001
Face PRO             8002
Ollama suggestion    8010
Render               8020
AI Gateway           8015
Member DB mock       8040
Product mock         8030
Crawler mock         8050
```

---

## 10. 前端測試與驗收

### 10.1 JavaScript 語法

```powershell
node --check js/api.js
node --check js/data.js
node --check js/router.js
```

### 10.2 Smoke test

```powershell
node frontend_smoke_check.js
```

Smoke test 會驗證：

- 所有 API service 設定存在。
- BASIC／PRO／suggestion URL 路徑正確。
- `Api` 的 Job、建議、爬蟲方法存在。
- `ImagePipeline` Worker 路徑與 fallback 方法存在。
- `AnalysisPackage` schemaVersion 為 `2026-06-v1`。
- `faceAnalysis` 欄位完整。
- `allowInsecureOtpBypass` 預設為 false。
- `config.local.js` 被 `.gitignore` 排除。

### 10.3 瀏覽器手動驗收

```text
1. 訪客進入首頁
2. 登入／註冊
3. BASIC 上傳正面照並完成分析
4. PRO 上傳正面與側面照並完成分析
5. 選一個風格並取得 Ollama 建議
6. 執行 AI 渲染並確認進度條
7. 查看妝前／妝後對比
8. 收藏 saved look
9. 重新整理後確認收藏仍可讀
10. 刪除收藏，確認資料庫成功後本機才移除
11. 管理員進入後台
12. 讀取會員、點數與收藏
13. 修改會員權限並確認資料庫回應成功
14. 測試商品與爬蟲預覽
```

---

## 11. Firebase Hosting 部署

`firebase.json` 的 Hosting 根目錄是目前資料夾 `.`，不是 `public/`。這一點和根目錄 `firebase-hosting-full/firebase.json` 不同，不能把兩份 Firebase 設定混用。

部署腳本：

```powershell
./deploy.ps1
```

腳本會取得時間戳、更新 `index.html` 內所有 JS 的 `?v=...`，再執行 `firebase deploy --only hosting`。

手動部署：

```powershell
firebase login
firebase use decorate-me
firebase deploy --only hosting
```

部署前確認：

- `config.local.js` 內容沒有錯誤或過期 Tunnel。
- 不要把真正 API Key 寫入 README、文件或 Git。
- `pages/ai-render.html`、`js/image-worker.js` 與所有圖片都存在。
- 最新 `index.html` 的版本號已被 `deploy.ps1` 更新。

---

## 12. 搬機方式

### 12.1 最安全方式

直接複製整個資料夾：

```text
C:\Users\isach\OneDrive\桌面\web_frontend
```

包含：`index.html`、`js/`、`css/`、`pages/`、圖片素材、`firebase.json`、`.firebaserc`、`deploy.ps1`、`config.local.example.js` 與既有 docs。

私密處理：

- `config.local.js` 不要放公開雲端；若要搬，使用加密方式。
- 會員資料庫帳密、Bearer Token、Cookie 不要搬到文件。
- 人臉照片、訓練資料與 log 另行加密保存。

### 12.2 GitHub 方式

```powershell
git clone https://github.com/vivi930319/new_poject.git
cd web_frontend
git checkout dev_makeup
```

目前 Git 追蹤 34 個檔案；`config.local.js`、文件、docs、log 與 Firebase 快取不在版控。clone 後仍要確認圖片、頁面與範本完整。

檢查：

```powershell
git ls-files
git status --ignored --short
```

### 12.3 新電腦第一次啟動

```powershell
cd "C:\Users\isach\OneDrive\桌面\web_frontend"
Copy-Item config.local.example.js config.local.js
node --check js/api.js
node --check js/data.js
node --check js/router.js
node frontend_smoke_check.js
python dev_server.py
```

---

## 13. 目前 Git 與修改狀態

同步前 `dev_makeup` 工作區已有未提交修改，包含：

```text
README.md
config.local.example.js
css/main.css
frontend_smoke_check.js
index.html
js/api.js
js/router.js
pages/admin.html
pages/compare.html
```

這些檔案已先備份，再被目前完整版本同步。同步後若要提交，請先確認 UI、後台與 API 內容，再執行：

```powershell
git status --short
git diff --stat
git diff --check
git add .
git commit -m "sync frontend with current complete version"
git push origin dev_makeup
```

不要使用 `git reset --hard`，除非確定要放棄目前工作區變更。

---

## 14. 已知問題與後續優先順序

### 立即處理

1. 輪替目前曾公開的 FACE／RENDER／SUGGESTION API Key。
2. 前端目前仍有直接呼叫 AI 服務的設計，應改成前端 → AI Gateway → 私有服務。
3. `config.local.js` 不能提交 Git，也不應被公開 Hosting 載入真正秘密。
4. Render 回傳的 `replicate.delivery` 臨時網址不得寫入 saved looks。

### 高優先

1. 點數應由資料庫作唯一來源，移除前端 localStorage 加點 fallback。
2. 購物車目前只有 localStorage，尚未跨裝置同步。
3. 會員刪除需要資料庫端真正實作 `DELETE /api/members/{email}` 並驗證 admin。
4. saved look 刪除要與 GCS 圖片刪除建立可靠清理流程。
5. 配額身份不能相信瀏覽器可修改的 `X-User-Email`。

### 中期

1. 補 Playwright 瀏覽器登入、分析、收藏、後台刪除流程測試。
2. 增加 CSP、`X-Content-Type-Options`、Referrer Policy 等 Hosting headers。
3. 將 Cloudflare Tunnel 改成穩定正式網址。
4. 將文件與必要前端資產整理進 Git，避免搬機只 clone 後缺檔。

---

## 15. 最短交接流程

```text
1. 確認本資料夾使用的是最新完整程式
2. 不覆蓋 config.local.js
3. 重新確認會員資料庫與 Ollama URL
4. node --check 三個主要 JS
5. node frontend_smoke_check.js
6. 啟動 dev_server.py
7. 測 BASIC／PRO／Ollama／Render
8. 測 saved look 新增與刪除
9. 測後台會員、商品、爬蟲
10. 驗收通過後才 commit／push／deploy
```
