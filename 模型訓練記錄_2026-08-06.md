# 模型訓練記錄 2026-08-06

> 本輪目標：把 `細長眼` 併入 `鳳眼`（分類表 2026-07 就改了，但磁碟上沒跟上），
> 清掉舊類別殘留資料夾，然後重測。
>
> 訓練資料來源：`data/basic_full/grouped`
>
> 相關文件：`模型訓練記錄_2026-07-24.md`、`模型三方公平比較_2026-07-19.md`、
> `CNN訓練歷程_BASIC五官分類.md`

---

## 零、先講結論

1. **資料合併完成**：眼型與唇型各剩正式的 4 類，5 個舊類別資料夾清空移除。
2. **建立了固定保留測試集**，這是本輪最重要的基礎建設。在它出現之前，這個專案
   從來沒有辦法回答「新模型比較好嗎」——所有分數都來自會隨資料重切的 k-fold。
3. **那 101 張眼型照片確實有幫助**：在同一份保留集上各跑三個 seed，
   合併後 0.529 對合併前 0.461，**平均高 0.067，且三對三完全分離**。
4. **但單次結果不可信**。鼻型當對照（資料完全沒變）跑四次，分數落在 0.731～0.873，
   **雜訊底線約 0.14**。任何小於這個幅度的單次差異都不能當結論——
   我自己就先被騙過一次（見 §六）。
5. **線上模型已更新**為 seed 43、全資料訓練的 ConvNeXt-Tiny（bundle 20260806）。
   代價是保留集 v1 就此作廢——新模型看過它的每一張照片（見 §七）。
6. **本輪踩到兩次同一類的地雷**：照 issue #24 的流程原樣執行會靜默覆蓋線上模型。
   第一次真的覆蓋了（已還原），第二次及時攔下。根因已修（見 §二）。
7. **仍有 71 組跨類別重複**（同一張圖被標成兩種類別），清單已匯出待標註端判讀。

---

## 一、資料合併

### 1.1 合併前的狀況

`data/basic_full/grouped/eye_shape` 有 8 個資料夾，但分類表只認 4 類：

```
下垂眼 110    圓眼 120    桃杏眼 221    鳳眼 107     ← 正式
丹鳳眼 0      杏仁眼 0     瞇縫眼 0                  ← 空的舊類別
細長眼 118                                         ← 舊類別，還有資料
lip_shape/M型唇 0                                  ← 空的舊類別
```

`tools/check_label_tables.py` 全部通過——因為正規化表早就有 `細長眼 → 鳳眼`，
程式碼與分類表一致，**只有磁碟沒跟上**。這種不一致不會被任何檢查抓到。

### 1.2 差點做錯的一步

第一直覺是「把 118 張搬進鳳眼」。先比對檔名，得到「零重疊」，看起來可以直接搬。

**那個判斷是錯的。** 比對的是精確字串，但 Windows 檔案系統不分大小寫。實際有 17 組：

```
細長眼/IMG_3609.JPG   vs   鳳眼/IMG_3609.jpg
```

sha256 比對確認**內容完全相同**——是同一張照片。也就是說合併做了一半：
17 張已經複製進鳳眼（副檔名被轉成小寫），101 張沒有。直接搬會製造 17 組重複。

> 這與 `模型訓練記錄_2026-07-24.md` §2.0 記載的是同一個模式：
> 「舊類別的圖被*複製*到新類別資料夾，但舊資料夾沒刪」。當時清掉 314 組。

### 1.3 實際做法

```
搬移 101 張、刪除 17 張重複
鳳眼 107 -> 208 張
移除空資料夾：細長眼、丹鳳眼、杏仁眼、瞇縫眼、M型唇
```

合併後：

```
eye_shape : 下垂眼(110)、圓眼(120)、桃杏眼(221)、鳳眼(208)
lip_shape : 厚唇(112)、微笑唇(100)、花瓣唇(226)、薄唇(106)
```

### 1.4 雲端備份

資料集不在版控裡，搬錯了 git 救不回來，所以合併前後各上傳一份到新建的私有 bucket：

```
gs://decorate-me-datasets/basic_full/grouped_pre-merge_20260806/    2472 物件
gs://decorate-me-datasets/basic_full/grouped_post-merge_20260806/   2455 物件
```

差 17 個物件，正好等於刪掉的重複數。bucket 設定：ASIA-EAST1、
uniform bucket-level access、**public access prevention: enforced**。

---

## 二、⚠️ 重測流程會覆蓋線上模型

**這是本輪最該被記住的一件事。**

issue #24 的重測流程第 3 步是：

```powershell
.\.venv-train\Scripts\python.exe train_basic_cnn_roi.py
```

照這樣執行，會發生兩件文件沒寫的事：

1. `--architecture` 的**預設值是 `mobilenet_v3_small`**
2. 不加 `--cv` 時，腳本會訓練並**直接覆寫** `models/basic_features_roi/` 底下的
   `*.onnx` 與 `*_classes.json`——那就是線上服務用的模型

結果是線上的 ConvNeXt-Tiny 被靜默換成 MobileNetV3-Small：

```diff
- "architecture": "convnext_tiny"
+ "architecture": "mobilenet_v3_small"
```

而 `b0ed57d`（「BASIC 與 PRO 全面改用 ConvNeXt-Tiny，DINOv2 退場」）是刻意的選型決定，
理由是同一組 5-fold 上三個大架構在五個部位全部贏過 MobileNetV3——15/15。

### 2.1 還原方式

```powershell
# classes.json 有進版控
git checkout -- models/basic_features_roi/

# ONNX 不在版控裡，從 GCS 還原（manifest 帶 sha256，會逐檔驗證）
.\.venv\Scripts\python.exe tools\download_face_models.py
```

驗證輸出 `face model bundle 20260731 verified` 代表位元組完全相同。

**線上服務全程未受影響**——那批 MobileNetV3 從未被部署。

### 2.2 建議修正 issue #24 的流程

第 3 步的指令應改成明確帶參數：

```powershell
# 評估用（--cv 只評估，不匯出 ONNX）
.\.venv-train\Scripts\python.exe train_basic_cnn_roi.py --architecture convnext_tiny --cv 5
```

要產出正式模型才拿掉 `--cv`，而且**永遠要明確指定 `--architecture convnext_tiny`**。

---

## 三、訓練結果

### 3.1 ConvNeXt-Tiny，5-fold（identity 切分）

```
部位            CV 平均    ±std     各 fold
face_shape      0.549    0.042    [0.482, 0.528, 0.584, 0.601, 0.549]
brow_shape      0.540    0.025    [0.564, 0.534, 0.561, 0.494, 0.549]
eye_shape       0.638    0.065    [0.593, 0.638, 0.743, 0.666, 0.552]
nose_shape      0.900    0.049    [0.835, 0.907, 0.871, 0.981, 0.907]
lip_shape       0.579    0.042    [0.582, 0.617, 0.624, 0.566, 0.507]
```

### 3.2 為什麼不能跟現行版本比較

`b0ed57d` 記載的現行基準（同樣是 ConvNeXt-Tiny、identity 切分 5-fold）：

```
                face   brow    eye   nose    lip
ConvNeXt-Tiny  0.561  0.603  0.693  0.881  0.640
```

表面上看是四降一升。**但這個比較不成立。**

我只動了眼型的資料，五個部位的分數卻全部改變。原因在 `data/roi_cache/rois.npz`
是**單一共用的影像清單**（2298 張），五個部位從同一批影像各自裁 ROI。
從眼型刪掉 17 張重複，等於改動了整個共用影像池 → identity 分群改變 →
**五個部位的 folds 全部重切**。

這正是 issue #24 寫的「資料變動後 folds 也隨之改變，等同換了一份考卷」，
只是影響範圍是全部部位，不是只有被改動的那一個。

**所以本輪不能宣稱任何部位變好或變差。**

### 3.3 其他架構的對照（同一批資料）

MobileNetV3-Small（誤觸那次的產出，仍是有效的評估數據）：

```
部位            隨機切分(虛高)   按人切分(可信)    洩漏
face_shape        0.506         0.475        +0.031
brow_shape        0.607         0.452        +0.155
eye_shape         0.565         0.571        -0.005
nose_shape        0.749         0.774        -0.025
lip_shape         0.567         0.467        +0.100
```

DINOv2 embedding + 線性分類器：

```
部位            LogReg          SVM           CNN 基準
face_shape   0.416 ± 0.070   0.442 ± 0.046   0.492 ± 0.035
brow_shape   0.543 ± 0.074   0.531 ± 0.047   0.519 ± 0.035
eye_shape    0.564 ± 0.069   0.587 ± 0.064   0.568 ± 0.077
nose_shape   0.843 ± 0.096   0.846 ± 0.079   0.841 ± 0.024
lip_shape    0.513 ± 0.035   0.511 ± 0.053   0.557 ± 0.058
```

ConvNeXt 在五個部位上都高於 MobileNetV3 與 DINOv2 線性頭，與 `b0ed57d` 的結論一致。

**眼型的洩漏是 −0.005**（隨機切分甚至略低於按人切分），代表本輪的合併確實把
眼型的重複問題清乾淨了。眉型 +0.155、唇型 +0.100 則還有明顯洩漏——見下一節。

---

## 四、仍未解決：71 組跨類別重複

合併後掃描整份資料集（sha256 逐檔比對），仍有同一張照片被標成兩種類別：

| 部位 | 受影響檔案 | 佔比 | 主要衝突 |
| --- | ---: | ---: | --- |
| eye_shape | 66 / 659 | 10% | 圓眼↔桃杏眼 17、桃杏眼↔鳳眼 11、下垂眼↔鳳眼 5 |
| face_shape | 40 / 530 | 8% | 圓形臉↔鵝蛋臉 5、心形臉↔長形臉 5 |
| brow_shape | 27 / 441 | 6% | 彎月眉↔落尾眉 9、一字眉↔彎月眉 4 |
| lip_shape | 10 / 544 | 2% | 厚唇↔花瓣唇 4 |
| nose_shape | 0 / 271 | 0% | 乾淨 |

同一張圖給模型兩個互相矛盾的答案，而且 5-fold 可能把它同時分到訓練與測試集。
這是眉型與唇型洩漏偏高的最可能原因。

**完整清單：`data/basic_full/跨類別重複清單_2026-08-06.csv`**（143 列、71 組，
含 sha256 前 12 碼、目前類別、同一張圖的其他類別）。

**這需要標註端判讀，不是工程端能決定的。** 「這張到底是圓眼還是桃杏眼」是標註定義問題。
`train_basic_cnn_roi.py` 有 `--drop-conflicts` 可以先剔除矛盾身分再訓練，
但那是繞過，不是解決。

---

## 五、其他發現

### 5.1 manifest CSV 已完全過期，但沒有程式在讀

`data/basic_full/grouped/*_manifest.csv` 五份的標籤是舊分類：

```
eye_shape_manifest.csv:  丹鳳眼 240、杏仁眼 87、桃花眼 1629、瑞鳳眼 171、細長眼 203、下垂眼 3
brow_shape_manifest.csv: 彎月眉 2205、標準眉 49、落尾眉 79
```

`桃花眼`、`瑞鳳眼`、`標準眉` 都不在現行分類表裡，數量也與實際資料夾對不上
（brow 實際是 一字眉 220、彎月眉 118、落尾眉 103）。

已確認**沒有任何程式讀取它們**（`grep -rn manifest` 只命中無關的文件產生器與模型 manifest）。
所以是無害的死檔案，但留著會誤導下一個看資料的人。建議刪除或移進 `_archive/`。

### 5.2 ROI 快取有 1 張照片偵測不到人臉

```
完成：2298 張成功、1 張失敗
  - IMG_8375.jpg: 沒偵測到人臉
```

比例極低，不影響結論，但該張照片應該從資料集移除或重拍。

---

## 五之三、固定保留測試集：這輪真正的產出

§3.2 說「不能比較」之後，接下來做的就是把「能比較」這件事建起來。

### 5.3.1 先修好 identity_map，否則保留集會切錯

保留集要按 identity 切（同一個人不能跨邊），所以 identity 必須是對的。而 `identity_map.json`
是 07-30 建的，鍵是**絕對路徑**——我把 101 張照片從 `細長眼` 搬到 `鳳眼` 之後，
那些路徑全部查無：

```
鳳眼 208 張
  舊 map 查得到 107 張 (51%)   ← 搬過來的 101 張全部查不到
  新 map 查得到 208 張 (100%)
```

查不到的樣本 `identity = -1`，而 `-1` 的樣本**一律留在 train，永遠不會被拿去驗證**。
也就是說眼型有 **15% 的資料從此不曾當過考題**，而且沒有任何地方報錯。

重建 identity map 後 `identity=-1` 從 101 張降到 2 張（那 2 張是真的偵測不到人臉）。

> **這是本輪最該記住的失敗模式：用路徑當鍵的對照表，會在資料搬動時靜默失效。**
> 新建的保留集因此改用 sha256 當鍵。

### 5.3.2 保留集 v1

`tools/build_holdout_split.py` 產出 `data/roi_cache/holdout_split_v1.json`（已入版控）：

```
保留 613 / 2298 張（26.7%），涵蓋 197 個 identity
每個類別至少 19 張
```

三個設計決定，每個都對應一個踩過的坑：

| 決定 | 為什麼 |
| --- | --- |
| 用 sha256 當鍵 | identity_map 用路徑當鍵而靜默失效（見 5.3.1） |
| 按 identity 切，`-1` 留 train | 資料是影劇截圖，同一藝人多張；隨機切會讓模型靠認人拿分 |
| 輸出檔入版控，定案後不重跑 | 會重新產生的保留集只是「另一份新考卷」，等於沒有基準 |

評估用 `tools/eval_on_holdout.py`，前處理直接 import `face_roi.roi_to_tensor`——
那是訓練與線上推論共用的同一支函式。自己重寫一份等於在量另一個模型。

---

## 五之四、對照實驗：那 101 張照片有沒有用

### 5.4.1 實驗設計

```
模型 A（合併前等效）  ConvNeXt-Tiny，--holdout-split v1 --exclude-list <101 張 sha256>
模型 B（合併後）      ConvNeXt-Tiny，--holdout-split v1
兩者都在同一份保留集上評分，各跑 seed 42/43/44
```

那 101 張的清單不是憑記憶列的，是從 GCS 的 pre-merge 快照算出來的：
`細長眼` 的 118 個內容雜湊，減去 `鳳眼` 原本就有的 107 個 = 101，正好對上搬移數量。
（直接用檔名會得到 118，因為那 17 張重複的內容還在 `鳳眼` 裡，而 Windows 比對不分大小寫。）

### 5.4.2 結果

眼型 macro recall（保留集 183 張）：

| | seed 42 | seed 43 | seed 44 | 平均 | 標準差 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A 合併前 | 0.456 | 0.464 | 0.464 | **0.461** | 0.004 |
| B 合併後 | 0.553 | 0.500 | 0.533 | **0.529** | 0.022 |

**平均差 +0.067，且三對三完全分離**——最差的 B（0.500）仍高於最好的 A（0.464）。
效果量是 A 標準差的 15 倍，完全分離在虛無假設下的機率是 1/20。

**結論：那 101 張照片對眼型有實質幫助。**

### 5.4.3 雜訊底線：為什麼需要多個 seed

鼻型當對照組，**訓練資料完全沒變**（兩次跑的 train=156、val=56 一模一樣），只換 seed：

```
0.837   0.731   0.873   0.786      平均 0.807，最大差 0.142
```

同樣的資料、同樣的程式、同樣的 seed 參數，分數可以差 **0.142**。
原因是 GPU 的非決定性（cuDNN 演算法選擇、非決定性 atomics）。

**所以雜訊底線約 0.14，任何小於這個幅度的單次差異都不是訊號。**

這不是理論警告——我自己就被騙過：第一次只跑單次，得到眼型
「A 0.517 → B 0.487，變差 0.030」，差點寫成「合併讓眼型退步」。跑滿三個 seed
才看出方向是相反的。

### 5.4.4 線上模型的分數為什麼看起來高很多

```
部位          線上模型 ⚠   A 合併前   B 合併後
eye_shape      0.843      0.461     0.529
nose_shape     0.975      0.837     0.807(4次平均)
```

**線上那一欄不能拿來比**，兩個原因疊在一起：

1. **洩漏**：它是用全部資料訓練的，包含保留集裡的每一張照片——等於考前看過考卷。
2. **資料量**：它用 100% 資料，A/B 只用了扣掉保留集後的約 73%。

所以**不能因為 A/B 分數低就說模型退步了**，那是不同的量法。線上模型的真實表現
未知，而且在保留集被「花掉」之前也無法得知（見 §七）。

---

## 六、待辦

- [x] **建立固定的保留測試集** —— 本輪完成，見 §五之三。issue #24 列了很久的那一項。
- [ ] **標註端判讀 71 組跨類別重複**（清單見 §4）——這是目前最能提升分數的一件事
- [ ] **決定要不要花掉保留集 v1 換取線上模型更新**（見 §七）——需要專案負責人拍板
- [ ] 修正 issue #24 重測流程的第 3 步指令（見 §2.2），並補上 `--out-dir`
- [ ] 眉型的 `落尾眉` recall 只有 0.08，接近崩潰，需要單獨看
- [ ] 保留集 613 張裡只有 542 張在 ROI 快取中（差 71 張是建快取時被排除的，
      含 2 張偵測不到人臉）。差距不大但該弄清楚為什麼
- [ ] 清理或封存過期的 manifest CSV（§5.1）
- [ ] 移除 `IMG_8375.jpg`（§5.2）

## 六之二、可重跑的完整流程

換機器之後 clone + 從 `gs://decorate-me-datasets` 還原資料集，照這五步就能重現本輪數字：

```powershell
# 1. identity 聚類。**路徑或檔名一有變動就必須重跑**，否則保留集會切錯（見 §5.3.1）
.\.venv-train\Scripts\python.exe tools\build_identity_map.py

# 2. ROI 快取與索引
.\.venv-train\Scripts\python.exe prepare_roi_cache.py

# 3. 保留集（已入版控，clone 下來就有；只有要建新版本時才跑）
.\.venv\Scripts\python.exe tools\build_holdout_split.py --version v2

# 4. 訓練候選模型。--out-dir 一定要給，不給會覆蓋線上模型
.\.venv-train\Scripts\python.exe train_basic_cnn_roi.py `
    --architecture convnext_tiny --holdout-split v1 --skip-random `
    --out-dir models\candidate_X

# 5. 在保留集上評分，結果累積進 models/holdout_scores.json
.\.venv\Scripts\python.exe tools\eval_on_holdout.py `
    --model-dir models\candidate_X --label 候選X
```

**第 4 步至少要跑三個 seed**（`--seed 42/43/44`）再比平均，理由見 §5.4.3：
雜訊底線約 0.14，單次結果會騙人。

## 七、線上模型已更新（2026-08-06 決定並執行）

```
face-basic   face-basic-00042-fqd → face-basic-00043-g5j
face-pro     face-pro-00026-d75   → face-pro-00027-k7g
映像          backend:20260806-223137
模型          ConvNeXt-Tiny, bundle 20260806, seed 43, 全資料訓練
```

### 7.1 選哪一個 seed

三個 seed 都用全部資料訓練（`--out-dir models\final_s4x`，不碰線上目錄），
然後在保留集 v1 上互比。這個比較**三者都同樣有洩漏**（都看過 v1），
所以它不是泛化指標，但可以回答「三個可部署的成品誰的表現好」：

| 部位 | 舊線上 | s42 | **s43（選這個）** | s44 |
| --- | ---: | ---: | ---: | ---: |
| face_shape | 0.575 | 0.763 | 0.751 | 0.773 |
| brow_shape | 0.719 | 0.603 | **0.752** | 0.660 |
| eye_shape | 0.843 | 0.847 | 0.741 | 0.808 |
| nose_shape | 0.975 | 0.931 | **0.981** | 0.886 |
| lip_shape | 0.709 | 0.463 | **0.780** | 0.774 |
| **平均** | 0.764 | 0.721 | **0.801** | 0.780 |

選 s43 的理由是**沒有單點崩壞**：s42 的唇型掉到 0.463、s44 的鼻型掉到 0.886，
s43 五個部位裡四個最好或接近最好。唯一較弱的是眼型（0.741 vs 舊的 0.843），
但那個 0.10 的差距落在 0.14 的雜訊範圍內（§5.4.3）。

`face_shape` 從 0.575 拉到 0.751 是最大的改善——那本來是最弱的部位。

### 7.2 部署時差點又出事：manifest 沒更新會把新模型洗掉

`deploy_face_cloudrun.ps1` 第一步是 `download_face_models.py`，它拿
`tools/face_models_manifest.json` 的 sha256 逐檔比對，**對不上就從 GCS 下載覆蓋**。

所以只換本機模型而不更新 manifest 的話，部署會把剛訓練好的模型換回舊的，
然後印 `face model bundle 20260731 verified` —— 看起來完全正常。

正確順序：

```
1. 上傳新 bundle 到 gs://decorate-me-models/20260806
   ⚠ object 路徑要照 manifest 的結構（basic_features_roi/xxx.onnx），不是扁平的
2. 更新 manifest 的 version、gcsPrefix、每一支的 size 與 sha256（從本機實際檔案算）
3. download_face_models.py --verify-only   → 應印 bundle 20260806 verified
4. **真實還原測試**：把本機某一支移開，跑 download_face_models.py 讓它從 GCS 抓回來，
   比對 sha256。這一步才真的證明雲端存的是新模型，前一步只證明本機與 manifest 一致
5. 部署
```

PRO 的 `nose_shape_side.onnx` 本輪未重訓，從 20260731 原樣複製到 20260806，
sha256 不變——bundle 是完整的，不是只有變動的部分。

### 7.3 怎麼確認線上真的換了

`/health` 只會說 `architecture: convnext_tiny`，新舊模型的回答一模一樣，**不能用它判斷**。
部署腳本的 `/health` 檢查又必定被略過（使用者帳號簽不出 audience-scoped token，見 §2）。

用實際預測比對才有結論。12 張照片 × 5 個部位，同時跑線上服務、本機新模型、
從 GCS 拉下來的舊模型：

```
線上結果 vs 本機新模型（seed 43）  60/60  (100%)
線上結果 vs GCS 舊模型 20260731    44/60  (73%)
```

16 項分歧，方向明確。`backend_smoke_test.py` 也全數通過。

### 7.4 保留集 v1 已作廢

新的線上模型訓練時看過 v1 的全部 613 張，所以 **v1 不能再用來評估線上模型**。
說明寫在 `data/roi_cache/holdout_split_v1_已作廢.md`，就放在切分檔旁邊，
免得有人先看到 json 就拿去用。

`models/holdout_scores.json` 裡的評估分兩類：`sweep_*` 與 `A/B_合併*` 沒看過 v1，
彼此之間永遠可比；`線上_*` 與 `final_*` 看過，只能同類互比。

下次資料擴充時建 v2，並且**線上模型要改成只用 train 部分訓練**，
否則每次都為了多那點資料把基準花掉，永遠累積不出可比較的歷史。

### 換模型的代價（當時的決策依據，保留備查）

要讓線上吃到那 101 張照片，該做的是**用全部資料（含保留集）以 B 的設定重訓**——
線上模型沒有理由浪費 27% 的資料。但這麼做之後：

> **新的線上模型就再也不能用保留集 v1 公平評估了**，因為它訓練時看過那些照片。
> 保留集就「花掉」了。

這是保留集固有的取捨。之後有兩條路：

1. **繼續用 v1 當基準**：線上模型只用 train 部分訓練，永遠犧牲 27% 的資料換取可比較性。
2. **花掉 v1，改用 v2**：線上模型用全部資料訓練，之後資料再擴充時建立 `holdout_split_v2`，
   從那一版開始重新累積比較基準。`build_holdout_split.py --version v2` 支援這件事，
   而且會拒絕覆蓋既有版本。

第 2 條比較實際，但要在訓練紀錄裡寫明「從哪一版換到哪一版」，否則跨版本的數字又會
變成不能比較。**這個決定該由專案負責人做，不該由跑訓練的人順手決定。**

### 本輪產出的模型放在哪

```
models/candidate_A_premerge/     A 的完整五部位（seed 42）
models/candidate_B_postmerge/    B 的完整五部位（seed 42）
models/sweep_{A,B}_eye_s{42,43,44}/   眼型 seed sweep
models/sweep_B_nose_s{43,44}/         鼻型雜訊對照
models/holdout_scores.json       全部評估結果，已入版控
```

這些目錄都**不是**線上服務讀的位置。線上讀的是 `models/basic_features_roi/`，
本輪結束時它與 GCS 上的 bundle 20260731 位元組相同（sha256 驗證過）。
