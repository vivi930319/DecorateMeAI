# 給資料庫端 — 後台管理的三項阻斷問題

**日期：** 2026-08-03
**提出方：** DECORATE ME 前端
**對象：** 會員資料庫後端維運者
**對應 issue：** #30、#31、#22
**性質：** 三項都在資料庫端，前端無法自行繞過

---

## 0. 為什麼這三項要一起講

它們都讓**後台管理員無法完成本來該做的事**，而且共同點是：前端該做的都做了，卡點在資料庫端。

商品清單那批問題另外寫在〈給資料庫端_商品清單API與點數打卡問題回報_2026-08-02.md〉，兩份不重疊。

嚴重度：

| 標記 | 意思 |
| --- | --- |
| 🔴 | 功能現在就是壞的，管理員做不了事 |
| 🟡 | 有替代路徑，但代價很高 |

---

## 1. 🔴 後台改權限全數失敗（issue #30）

### 現象

管理員在後台調整任何會員的角色或權限，一律失敗。

### 我們這邊已經排除的

- 前端有送 CSRF（Gateway 對 admin 寫入強制要求雙重提交 token）
- Gateway 的 `member-database` 白名單有 `api/members/[^/]+`
- 同一個 admin session 讀取 `GET /api/members` 是**成功的**——所以 session 本身有效

也就是說：**同一把 session，讀得到、寫不進去。**

### issue 標題自己指出了方向

> 待確認 admin 帳號是否為可讀取的 member row

這正是我們無法從外部確認的部分。請幫忙查：

1. admin 帳號在 `members` 表裡**有沒有對應的資料列**？
2. 寫入前的前置檢查是不是去 `members` 撈操作者、撈不到就擋？
3. 如果是，那 admin 帳號用的是另一張表（例如 `admins`）還是根本沒建列？

### 我們需要的答覆

一句「admin 帳號在 members 表有／沒有資料列」就能決定往哪修。如果沒有，要嘛補一列，要嘛把前置檢查改成不依賴 members。

---

## 2. 🔴 members 表有不合法 email，整批讀不出來（issue #31）

### 現象

後台會員名單載入失敗，上游回 **422**。

### 關鍵：一筆壞資料擋掉全部

422 來自回應驗證——只要 `members` 裡**有任何一列**的 email 不符合格式，整批回應就被擋下，後台一個會員都看不到。不是分頁問題，是全有或全無。

### 我們已經改善的部分（不是修好）

前端先前把 422 落到通用提示「請確認 admin session、CORS 與 cookie 設定」，會把查問題的人導向完全錯誤的方向——session 和 CORS 其實都是好的。

現在 422 會明講：

> 資料庫回 422：members 表裡有欄位格式不合法的資料（多半是 email），整批因此讀不出來。這不是登入或連線問題，請資料庫端清理該筆資料

**這只是讓症狀可讀，功能還是壞的。**

### 請你們做

1. 找出 `members` 裡 email 不合格式的資料列（空字串、缺 `@`、有空白、測試殘留值都算）
2. 清理或修正
3. **更重要的**：考慮讓 API 對單筆壞資料容錯——跳過該列並在回應裡標明，而不是整批 422。否則之後任何一筆髒資料都會再次讓整個後台名單消失。

第 3 點是根治；1、2 只能撐到下次。

---

## 3. 🟡 刪除會員是軟刪除，信箱永久卡住（issue #22）

### 現象

後台刪除會員後，該 email **無法再次註冊**——資料列還在，唯一索引仍然佔著。

### 為什麼這件事會咬到人

使用者用錯信箱註冊 → 請管理員刪掉 → 想用同一個信箱重新註冊 → 「這個信箱已經註冊過了」。從使用者的角度看，這個帳號被永久沒收了，而且沒有任何自救途徑。

Demo 現場如果發生，沒有任何辦法當場排除。

### 請你們做

改成硬刪除，或至少在軟刪除時**釋放 email 的唯一性**（例如改寫成 `deleted+<原值>+<時間戳>`）。

前端已經呼叫刪除端點並在成功後把該列從畫面移除，這一段不需要改。

---

## 4. 我們這邊的狀態

| 項目 | 前端 |
| --- | --- |
| #30 改權限 | 已送 CSRF 與 session，等你們確認 admin 的 member row |
| #31 422 | 已把錯誤訊息改成看得懂的，**功能仍壞** |
| #22 軟刪除 | 已呼叫刪除端點，等你們改語意 |

三項前端都沒有可繞過的路徑。

---

## 5. 順帶回報：issue #20 可以關掉

〈商品稽核分頁尚未併入正式前端〉已經過期。實際狀況：

- 後台導覽有「05 暫存商品審核」
- `Api.listStagingProducts` / `reviewStagingProduct` / `importStagingProduct` 三支都在
- 篩選、分頁、核准／退回／匯入都接上了

我們會在該 issue 留言說明後關閉。
