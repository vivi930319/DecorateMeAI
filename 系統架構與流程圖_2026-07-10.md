# Decorate Me 系統架構與串接流程圖

> 產出：2026-07-10
> 格式：Mermaid（GitHub / VS Code / mermaid.live 可直接渲染）
> 內容：① 整體技術架構　② 主流程（分析→建議→渲染→收藏）　③ 登入/註冊/OTP　④ 後台管理　⑤ 臉部分析 job　⑥ 商品推薦　⑦ 服務清單

---

## ① 整體技術架構

```mermaid
flowchart TB
  U["使用者瀏覽器"]

  subgraph FE["前端 · Firebase Hosting (decorate-me.web.app)"]
    APP["Vanilla JS<br/>config / data / api / router"]
    LS["localStorage / sessionStorage<br/>(收藏/購物車/點數/草稿 暫存)"]
  end

  subgraph OURS["我方後端 · Google Cloud Run (asia-east1)"]
    FB["face-basic<br/>BASIC 臉部分析<br/>FastAPI + InsightFace/MediaPipe"]
    FP["face-pro<br/>PRO 臉部分析"]
    RD["replicate-render<br/>AI 妝容渲染 FastAPI"]
  end

  subgraph TEAM["組員服務 · Cloudflare Tunnel (網址會變)"]
    MDB["會員+商品資料庫<br/>quickly-tend-sun-rod<br/>login/register/members/products/recommend/saved-looks/points"]
    OLL["Ollama 文字建議<br/>morning-deeper-kick<br/>(黃姵錚的 Mac)"]
  end

  subgraph EXT["外部/儲存"]
    REP["Replicate<br/>flux-kontext-pro"]
    GCS["GCS bucket<br/>decorate-me-renders (公開讀)"]
    FS["Firestore<br/>job_store (分析 job)"]
  end

  U --> APP
  APP <--> LS
  APP -->|"X-API-Key"| FB
  APP -->|"X-API-Key"| FP
  APP -->|"X-API-Key"| RD
  APP -->|"X-API-Key"| OLL
  APP -->|"session cookie"| MDB

  FB --> FS
  FP --> FS
  RD --> REP
  RD --> GCS
  APP -.顯示渲染圖.-> GCS
```

---

## ② 主流程：上傳 → 分析 → 建議 → 渲染 → 收藏

```mermaid
sequenceDiagram
  participant U as 使用者
  participant FE as 前端
  participant F as 臉部分析(Cloud Run)
  participant O as Ollama建議
  participant R as 渲染(Cloud Run)
  participant GCS as GCS
  participant DB as 會員資料庫

  U->>FE: 上傳照片
  FE->>FE: 壓縮 1024px/0.78 (Web Worker)
  FE->>F: POST /analyze (X-API-Key)
  F-->>FE: 臉型/眉/眼/鼻/嘴/膚色
  FE->>O: POST /suggest (X-API-Key)
  O-->>FE: 妝容建議 + 英文 renderPrompt
  FE->>R: POST /render (X-API-Key)
  R->>R: 去重快取檢查 (同圖同prompt)
  R-->>GCS: 上傳渲染圖
  GCS-->>R: 永久網址
  R-->>FE: afterImageUrl
  U->>FE: 按「收藏妝容對比圖」
  FE->>DB: POST saved-looks (session cookie)
  DB-->>FE: 201 已存
```

---

## ③ 登入 / 註冊 / OTP 流程

```mermaid
sequenceDiagram
  participant U as 使用者
  participant FE as 前端
  participant DB as 會員資料庫

  rect rgb(245,240,230)
  note over U,DB: 註冊
  U->>FE: 填 email/密碼/電話/年齡
  FE->>DB: POST /api/register (5 欄位)
  FE->>DB: POST /api/send-otp
  U->>FE: 輸入驗證碼
  FE->>DB: POST /api/verify-otp
  note over DB: ⚠ 目前後端未強制 OTP(可跳過)
  end

  rect rgb(235,242,238)
  note over U,DB: 登入
  U->>FE: email/密碼
  FE->>DB: POST /api/login (credentials:include)
  DB-->>FE: member 物件 + Set-Cookie session
  FE->>FE: 存 sessionStorage + 暫存帳密(自動重登用)
  note over FE: 跨站 cookie 重整易掉 → 401 自動重登
  end
```

---

## ④ 後台管理串接

```mermaid
flowchart LR
  A["admin 登入<br/>role=admin"] -->|session cookie| M["GET /api/members"]
  M --> T["會員清單表格"]
  T -->|每會員| SL["GET saved-looks (收藏數)"]
  T -->|每會員| PT["GET points (點數)"]
  T -->|改權限/VIP| PM["PATCH /api/members/{email}"]
  T -->|商品CRUD| PR["POST/PATCH /api/products"]
  M -.401 掉線.-> RL["自動重登 → 重試"]
```

---

## ⑤ 臉部分析 job 流程（非同步）

```mermaid
sequenceDiagram
  participant FE as 前端
  participant F as 臉部分析服務
  participant FS as Firestore(job_store)

  FE->>F: POST /v1/face/analyze/basic (圖片)
  F->>FS: 建立 job (排隊)
  F-->>FE: jobId
  loop 每秒輪詢
    FE->>F: GET job 狀態
    F->>FS: 讀 job
    F-->>FE: stage / progress %
  end
  FE->>F: GET job 結果
  F-->>FE: 分析結果 JSON
```

---

## ⑥ 商品推薦流程

```mermaid
flowchart TB
  A["臉部分析結果<br/>(faceShape/skinTone/lipLab)"] --> B["POST /recommend-products<br/>+ style 風格"]
  B -->|"⚠ 目前 404"| E["前端顯示<br/>推薦暫時無法載入"]
  B -->|正常| C["回傳商品清單"]
  C --> D["商品卡 (image_src 圖)"]
  A -.失敗退回.-> F["GET /api/products 全部商品"]
```

---

## ⑦ 服務與技術清單

| 元件 | 技術 | 位置 / 網址 | 認證 |
|---|---|---|---|
| 前端 | Vanilla JS | Firebase Hosting `decorate-me.web.app` | — |
| BASIC 分析 | FastAPI + InsightFace + MediaPipe | Cloud Run `face-basic-…run.app` | `X-API-Key` |
| PRO 分析 | FastAPI | Cloud Run `face-pro-…run.app` | `X-API-Key` |
| AI 渲染 | FastAPI + Replicate | Cloud Run `replicate-render-…run.app` | `X-API-Key` + 限流 + 去重 |
| 妝容建議 | Ollama | Cloudflare Tunnel `morning-deeper-kick…` | `X-API-Key` |
| 會員+商品庫 | 組員服務 | Cloudflare Tunnel `quickly-tend-sun-rod…` | session cookie |
| 渲染圖儲存 | GCS | bucket `decorate-me-renders` (公開讀) | — |
| 分析 job | Firestore | `job_store` | — |
| 外部 AI | Replicate | `flux-kontext-pro` | API token |

> ⚠ 標註：Cloudflare Tunnel 網址重啟就變（要同步改前端 `config.local.js`）；OTP 後端未強制、推薦端點 404、渲染圖公開——詳見《全系統審查_2026-07-10》與《資料庫端待辦_統一版》。
