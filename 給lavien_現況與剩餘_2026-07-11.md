# 資料庫端 — 現況與剩餘（實測版）

> 產出：2026-07-11　｜ 對象：lavien
> 依前端 curl 實測（tunnel `component-developmental-disclaimer-count`）。**你這波做超多，先說結論：大部分都好了，剩下幾個小確認。**

---

## ✅ 已完成（實測通過，不用再動）

| 功能 | 實測 |
|------|------|
| OTP 後端強制 | 未驗證 email `login` → **403**（讚，之前可跳過）|
| 點數系統 | members 直接帶 `points` / `lifetime`；`GET points` 回 `{balance, lifetime, transactions}`，有真實資料 |
| 每日簽到 | `POST check-in` → 回 `{awarded, checkedToday, streak}` |
| 任務 | `GET tasks` → `{tasks:[{taskId, reward, claimed}]}`；`claim` 端點存在 |
| 主題兌換 | `theme-shop/{id}/redeem` 端點存在 |
| 商品收藏 | `favorites/toggle` → 200、`GET favorites` → `{favorites:[{item_id,item_type}]}`（之前 500，修好了）|
| 購物車 | `GET cart` → `{cart:[]}` |
| 分析歷史 | `GET history` → `{results:[]}` |
| 商品推薦 | `/recommend-products` 用頂層 `lab`/`hex` 回 12 筆含 `delta_e`（前端已配合新格式）|
| **登入不掉線** | session cookie 加了 `Expires`（7 天）→ 重整不會掉，讚 |
| members 欄位 | 已帶 `points` / `lifetime` / `emailVerified` |

**→ 前端會逐一把成長系統（點數/簽到/任務/收藏/購物車/歷史）從本機改成接你這些端點。**

---

## 🔴 資安·重要（要補）：OTP 沒真正擋住假帳號

實測發現：用**假的、甚至根本不存在的 email** 也能註冊並進到 app，OTP 沒有實際防禦。
原因分兩邊：前端會改（驗證成功後改成走後端 `login`，不再本機直接登入）；資料庫這邊要補「地址是不是真帳號」的驗證：

1. **`send-otp` 有真的寄信嗎？** 需要真的用 SMTP 把驗證碼寄到那個信箱（收得到碼＝這是能收信的真帳號）。若現在只是回 200 沒真寄，假信箱就永遠驗得過。
2. **`verify-otp` 要嚴格比對**：比對 DB 存的碼 ＋ 時效（建議 5–10 分鐘過期）＋ 嘗試次數上限；通過才把 `email_verified=true`。
3. **未驗證帳號**：`login` 一律回 403（之前實測有擋到，請確認「註冊完但沒驗證」的帳號真的進不了），未驗證帳號定期清除。
4. **拒收拋棄式／一次性信箱網域**：註冊時擋掉 `mailinator.com`、`10minutemail.com`、`example.com`、`test.com` 這類網域，回 `400 DISPOSABLE_EMAIL`。
5. **（可選）MX 記錄檢查**：註冊時查 email 網域有沒有郵件伺服器（MX record），沒有＝假地址，直接拒。

→ 請回覆：`send-otp` 現在是不是真的寄信？`verify-otp` 現在會不會擋錯碼／過期碼？

---

## 🟡 還要確認 / 補的（不多）

### 1. 推薦碼（referral）
`GET /api/members/{email}/referral` 回 **405**。請告知推薦碼機制：
- 註冊帶推薦碼是走 `POST /api/register` 的 `referralCode` 欄位嗎？
- 綁定推薦人後有發點給推薦人嗎（建議 +20）？擋自我推薦/重複了嗎？
- 前端要不要一支「查我的推薦碼 / 我推薦了幾人」的端點？

### 2. 簽到「連續加碼」有做嗎？
`check-in` 回了 `streak`，但連續里程碑加碼請確認有實作：
- 連續第 **3 天 +5、7 天 +20、14 天 +40、30 天 +100**（跟每日 +10 疊加）。

### 3. `claim` / `redeem` 的正常回應格式
兩者實測回 400（可能因為 admin 已領過/點數狀態）。請告知**成功時**回什麼，好讓前端接：
- `POST tasks/{taskId}/claim` 成功 → 建議回 `{awarded, balance, lifetime}`
- `POST theme-shop/{themeId}/redeem` 成功 → 建議回 `{spent, balance, unlockedThemes}`；點數不足回 `400 INSUFFICIENT_POINTS`

### 4. 登入區分「未註冊」vs「密碼錯」
目前兩者是否回不同 `code`？前端已預留：
- 未註冊 → `USER_NOT_FOUND`（前端引導去註冊）
- 密碼錯 → `WRONG_PASSWORD`（前端提示重輸/改密碼）
（前端一收到這兩個 code 就自動精準引導；沒有的話退回現況。要不要做由你決定，資安上「不區分」也是合理的。）

### 5. 商品推薦 `style` 參數
`/recommend-products` 現在用色彩算 ΔE，請確認 `style`（港風/韓系…）有沒有影響結果——目前看起來只吃色彩。眼影/腮紅/眉彩這類「風格類」商品建議依 `style` 篩選。

---

## 🧹 順手清理：刪測試帳號
前端測試建的兩筆，麻煩從 DB 刪（含其 saved_looks/points 等關聯）：
- `probe_otp_check_1783526684@example.com`
- `probe_conn_test_20260708@example.com`

---

## 前端會用到的欄位格式（給你對照確認）

| 端點 | 前端預期回應 |
|------|-------------|
| `GET points` | `{ balance, lifetime, transactions:[{delta, reason, created_at, balance_after}] }` ✅ 已符合 |
| `POST check-in` | `{ awarded, checkedToday, streak }` ✅ 已符合 |
| `GET tasks` | `{ tasks:[{taskId, reward, claimed, done?}] }`（若有 `done`=條件是否達成 更好）|
| `GET favorites` | `{ favorites:[{item_id, item_type}] }` ✅ 已符合 |
| `GET cart` | `{ cart:[{item_id, qty}] }`（前端購物車每列是 id+數量）|
| `GET history` | `{ results:[{mode, result, created_at}] }` |

---

## 回覆前端
1. 推薦碼機制（第 1 項）怎麼用？
2. `claim`/`redeem` 成功回什麼（第 3 項）？
3. 登入要不要做「未註冊/密碼錯」區分（第 4 項）？
