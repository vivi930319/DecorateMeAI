# 給資料庫端：商品資料現況與需求

文件日期：2026-07-20
實測對象：`https://programmers-planners-convenient-had.trycloudflare.com/api/products`
實測筆數：1041 筆（全量抓取）
文件目的：說明推薦演算法端目前遇到的問題、**現在需要你們做什麼**、以及**現在請先不要做什麼**

---

## 0. 忙碌版結論

**現在要的（三件，照急迫度排序）**

1. 給我們 `PRODUCT_ADMIN_API_KEY` —— 現在 Admin 的新增／編輯／刪除全部 503
2. **確認 `id` 的重號問題**（`id=1` 對應到 7 個不同商品）—— 這是資料正確性問題，不只是我們不方便
3. 未來兩週**先不要大量增刪商品** —— 我們正在人工盤點 1040 筆商品的色系

**現在不要的（先別動，不是不做）**

1. 不要重算或修改現有的 `lab` / `hex_primary` 欄位
2. 不要重新編排商品 ID
3. 不要為了配合我們新增欄位
4. 不要修改商品名稱

理由都在下面。

---

## 1. 先說好的部分

資料品質比我們預期的好，這幾點先講清楚免得下面顯得都是抱怨：

- `lab` 覆蓋率 **99.9%**（1040/1041）
- `hex_primary` 覆蓋率 **100%**
- `brand`、`name`、`description`、`image_src` 全部 100%
- **唇彩的色值很準** —— 我們拿名稱含色彩字的商品去驗證色相，378 筆唇彩有 **78%** 對得上。像「櫻桃陷阱→`#b40000`」「白桃烏龍→`#dc8698`」都是正確的膏體色

唇彩和腮紅這兩類我們可以直接拿去算色差做推薦，不用你們再處理。

---

## 2. 現在要的

### 2.1 【最急】`PRODUCT_ADMIN_API_KEY`

我們的 Gateway (`ai_gateway.py:492`) 需要這把 key 才能代理 Admin 的寫入操作。目前是空的，所以：

```
POST   /admin-api/products        → 503 ADMIN_PROXY_NOT_CONFIGURED
PATCH  /admin-api/products/{id}   → 503
DELETE /admin-api/products/{id}   → 503
```

**Admin 後台的新增／編輯／刪除商品現在完全不能用。** 讀取不受影響。

請提供這把 key，或告訴我們正確的認證方式。

---

### 2.2 【重要】`id` 重號，`(type, id)` 才唯一

這是我們認為最需要你們確認的一件事。

**現況**：1041 筆商品裡，`id` 只有 **651 個不同的值**。有 120 個 id 各自重複出現，涉及 510 筆商品。

看起來每個品類是獨立資料表、各自從 1 開始自動編號，API 合併回傳時沒有加品類前綴。實例：

```
id=1 → 恆久完美無瑕持妝粉底 - LC6            (foundations)
id=1 → ettusais微醺暮色亮顏盤 01 薄櫻粉      (blushes)
id=1 → Sister Ann柔霧眼彩棒 05 西瓜氣泡      (eyeshadows)
id=1 → INTEGRATE超順手抗暈染眼線膠筆 nBR620  (eyeliner_mascara)
id=1 → IMMEME我愛小臉修容打亮棒002蒙布朗奶霜 (contouring)
id=1 → MKUP水汪汪大眼雙頭臥蠶筆-蜜桃粉       (highlighters)
id=1 → Za 經典旋轉眉筆-深棕BR611             (eyebrows)
```

`(type, id)` 組合則是完全唯一的（1041/1041）。

**影響**：任何拿 `id` 當商品識別的地方都會對錯商品 —— 包含收藏功能、推薦結果回傳、Admin 編輯刪除。我們這邊已經改用 `type:id` 當鍵值繞過，但這治標。

**想請你們確認**：

- 這是預期的設計，還是資料表合併時的疏漏？
- 如果是預期的，`/api/products/{id}` 這種端點打算怎麼定位商品？（目前實測回 **405**，看起來還沒實作）
- 有沒有可能改成全域唯一 ID？

> **但請先不要現在改**。改 ID 會讓我們正在進行的盤點作業全部失效，詳見第 3 節。**先確認、先規劃，動手時間我們協調。**

---

### 2.3 【重要】盤點期間請先凍結商品增刪

我們接下來會請組員**人工上網站盤點全部 1040 筆商品的色系**（原因見第 4 節），預計兩週左右。

盤點表是用 `type:id` 對應的。這段期間如果商品被大量增刪，或 ID 被重新編排，**組員的工作會直接作廢**。

**請協助**：

- 盤點期間避免大量增刪商品
- 如果一定要動，請告訴我們動了哪些（給個 `type:id` 清單就好）
- 少量新增沒關係，我們補盤即可

---

### 2.4 【想確認】`shade_name` 全空，爬蟲有抓到色號嗎？

`shade_name` 欄位存在，但 **1041 筆全部是空字串，覆蓋率 0%**。

可是商品名稱裡其實都有色號和色名：

```
Za靈動絲霧三角眉筆 GY951灰棕        ← 色號 GY951，色名「灰棕」
1028 速描師超細芯眉筆（BR22暖咖棕） ← 色號 BR22，色名「暖咖棕」
KATE凱婷 眉筆Z LB-1 淺金棕          ← 色號 LB-1，色名「淺金棕」
```

**想問**：爬蟲當初有抓到色號／色名嗎？只是沒寫進 `shade_name`，還是根本沒抓？

如果原始資料裡有，**麻煩回填這個欄位**。這對我們幫助很大 —— 眉筆這類商品的色名比圖片可靠得多，有了 `shade_name` 我們可以省下大量人工盤點。

如果沒有，那就算了，我們人工做。

---

### 2.5 【小事】有一筆測試資料混在正式資料裡

```
id=3131  type=lipsticks  name="Codex Proxy E2E 1784397858"  lab={}
```

這是唯一一筆沒有 `lab` 的資料，而且 `lab` 是空物件 `{}` 而不是空陣列 `[]`，型別跟其他筆不一致。應該是 E2E 測試留下來的。

我們這邊已經自動排除了，你們有空再清就好。順帶一提，正式環境跑 E2E 測試建議加個前綴或獨立標記，方便清理。

---

### 2.6 【小事】有一組重複商品

```
sale_page_id=11086490
  → contouring:3    IMMEME我愛小臉修容打亮棒003榛果奶糖
  → highlighters:7  IMMEME我愛小臉修容打亮棒003榛果奶糖
```

同一個商品同時被歸到「修容」和「打亮」兩個品類。這款是修容打亮兩用棒，所以可能是刻意的，也可能是爬蟲重複收錄。

`sale_page_id` 除了這一筆之外都是唯一的（1040/1041），**看起來 `sale_page_id` 比 `id` 更適合當全域唯一鍵**，供你們參考。

---

### 2.7 【長期】固定網址

`trycloudflare` 是臨時通道，**你們每次重開網址就會變**，我們這邊就會整個斷掉。這次就是這樣 —— 我們 `.env` 還指著 `yen-minute-from-ict.trycloudflare.com`，早就死了。

**短期**：換網址時請通知我們。
**長期**：如果之後還要持續對接，建議換成固定網址（Cloud Run、或付費版 ngrok）。畢業專題展示當天如果通道掉了會很難看。

---

## 3. 現在請先不要做的

不是不做，是**時機不對**。我們正要開始盤點，這段期間資料變動會讓工作作廢。

### ❌ 不要重算或修改 `lab` / `hex_primary`

我們知道有些色值是錯的（見第 4 節），但**請先不要動手修**。

原因：我們的人工盤點是以「現況」為基準做的。如果你們同時在改色值，我們會分不清一筆資料是原本就對、還是你們改過的，驗證會做不下去。

**修色值這件事等我們盤完再一起討論**，那時候我們手上會有 1040 筆人工核對過的色系資料，可以直接告訴你們哪些筆需要修、正確答案是什麼。那樣比你們現在盲改有效率得多。

### ❌ 不要重新編排商品 ID

理由同 2.3。ID 一動，盤點表全毀。

### ❌ 不要為了配合我們新增欄位

我們**之後**可能會希望有一個 `color_family` 欄位存人工核對的色系。但**現在不要加** —— 我們自己都還沒確定色系分類會有幾種、代號怎麼取。等盤完再談，免得你們白做一版。

### ❌ 不要修改商品名稱

商品名稱是我們盤點時的重要判斷依據（很多商品的色名就寫在名稱裡）。這段期間請不要做名稱正規化、去除品牌前綴之類的整理。

---

## 4. 附錄：為什麼我們要人工盤點色系

給你們理解背景，不是要求你們處理。

我們拿「名稱含明確色彩字的商品」去比對它的 `lab` 色相，看對不對得上：

| 品類 | 筆數 | 色值與名稱相符 | 判斷 |
|---|---|---|---|
| lipsticks | 378 | **78%** | 堪用 |
| blushes | 223 | **63%** | 普通 |
| eyeshadows | 60 | **12%** | 不可用 |
| eyebrows | 120 | 直接檢視即明顯錯誤 | 不可用 |

實際錯誤案例：

| 商品名稱 | 系統存的色值 | 問題 |
|---|---|---|
| Sister Ann 05 西瓜氣泡 | `#83726d` 灰褐 | 西瓜色不可能是灰的 |
| Sister Ann 03 夕陽紅寶 | `#7c625e` 灰褐 | 紅寶石色不見了 |
| Za GY951 灰棕眉筆 | `#d2cac0` L=82 近白 | 抓到筆桿顏色 |
| heme 胡桃棕眉筆 | `#464447` 中性深灰 | b\*=-1.9，棕色的 b\* 不可能是負的 |

**根本原因**：從商品圖自動抓色，遇到「眼影盤是一盤多色」「眉筆照片拍的是筆桿包裝」就一定會抓錯。這不是你們的實作問題，是這個方法本身的極限。

所以我們決定用人工補這塊，不會要求你們重做爬蟲。

> **一個誠實的但書**：我們對 foundations 和 highlighters 也測出很低的數字，但那是我們測法的問題 ——「粉底」「蜜粉」含「粉」字被誤判成粉紅色。這兩類的實際可靠度**尚未確認**，不要當成已知有問題。

---

## 5. 需要你們回覆的

麻煩就這五點給個回覆，其他都可以慢慢來：

1. `PRODUCT_ADMIN_API_KEY` 什麼時候可以給？
2. `id` 重號是預期設計還是 bug？
3. 未來兩週可以先不大量增刪商品嗎？
4. 爬蟲原始資料裡有色號／色名嗎？（有的話請回填 `shade_name`）
5. 換網址時可以通知我們嗎？

有問題直接找我們討論，不用等文件往返。
