# DECORATE ME · 六大系統圖

**基準日：** 2026-07-31
**用途：** 專題面試／口試答辯
**繪圖語法：** Mermaid（GitHub、VS Code、Typora 皆可直接渲染）

> 這裡的每一張圖都對應**實際存在的模組與端點**，不是示意圖。
> 服務名對應 Cloud Run 服務、路徑對應 `ai_gateway.UPSTREAMS` 的 `allowed_paths`、
> 函式名對應原始碼。面試被追問「這個箭頭實際是哪一行」時要答得出來。

---

## 一、系統架構圖

```mermaid
flowchart TB
    subgraph client["使用者端"]
        B["瀏覽器<br/>decorate-me.web.app"]
    end

    subgraph fb["Firebase Hosting"]
        FE["靜態前端 SPA<br/>index.html + js/router.js<br/>11 個頁面片段 pages/*.html"]
    end

    subgraph gcp["Google Cloud · asia-east1"]
        GW["<b>ai-gateway</b><br/>唯一對外入口<br/>allUsers 可呼叫"]

        subgraph private["私有服務（僅 ai-gateway@ 可呼叫）"]
            FB1["<b>face-basic</b><br/>Face_analyzer_BASIC.py<br/>正面五官分析"]
            FP["<b>face-pro</b><br/>Face_analyzer_PRO.py<br/>多角度／側臉"]
            RS["<b>render-service</b><br/>replicate_render_api.py<br/>妝後圖生成"]
        end

        FS[("Firestore<br/>job_store<br/>face_feedback<br/>face_corrections")]
        GCS[("GCS<br/>decorate-me-renders<br/>私有 bucket")]
        SM[["Secret Manager<br/>decorate-me-face-upstream-key"]]
    end

    subgraph ext["外部服務"]
        OL["Ollama 文字建議<br/>Quick Tunnel"]
        RP["Replicate API<br/>妝後圖模型"]
        MDB["會員資料庫<br/>組員維護"]
    end

    B -->|HTTPS| FE
    FE -->|"fetch + session cookie<br/>+ CSRF"| GW

    GW -->|"OIDC identity token<br/>+ x-api-key"| FB1
    GW -->|同上| FP
    GW -->|同上| RS
    GW -->|"session-only"| MDB
    GW -.->|"預設 fail closed<br/>ALLOW_EXTERNAL_TEXT_UPSTREAM"| OL

    FB1 --> FS
    FP --> FS
    RS --> RP
    RS --> GCS
    RS --> FS
    FB1 -.->|讀取| SM
    FP -.->|讀取| SM

    style GW fill:#b9975b,color:#fff
    style FB1 fill:#e8dcc0
    style FP fill:#e8dcc0
    style RS fill:#e8dcc0
```

### 關鍵設計決定

| 決定 | 理由 |
| --- | --- |
| **三個分析服務都不開 `allUsers`** | IAM 只綁 `ai-gateway@decorate-me.iam.gserviceaccount.com` 的 `roles/run.invoker`。外面打得到的只有 Gateway，繞過 Gateway 就繞過了 session／CSRF／actor 驗證 |
| **Gateway 用路徑白名單而非黑名單** | `is_path_allowed()` 用 `re.fullmatch`，沒列進 `allowed_paths` 的一律 404。上游新增端點時必須顯式放行 |
| **上游金鑰放 Secret Manager** | `FACE_API_KEY` 以 `secretKeyRef` 注入，不進映像也不進版控 |
| **face-basic 與 face-pro 共用同一個映像** | 靠 `SERVICE_NAME` 環境變數在 `cloud_start.py` 分派（`basic` / `pro` / `suggestion`），一次建置兩支同時更新 |

---

## 二、使用者流程圖

```mermaid
flowchart TD
    S([進入 decorate-me.web.app]) --> L{已登入？}
    L -->|否| LOGIN[註冊／登入<br/>會員資料庫發短效 token]
    L -->|是| DASH[首頁 dashboard]
    LOGIN --> DASH

    DASH --> UP[臉部分析 analysis<br/>上傳或拍照]
    UP --> Q1{影像安全檢查<br/>image_safety.sanitize_upload}
    Q1 -->|"格式錯／超過 8MB／<br/>像素炸彈"| ERR1[400 明確錯誤]
    Q1 -->|通過| POSE{姿態檢查<br/>InsightFace yaw/pitch}
    POSE -->|超出門檻| HINT[回傳拍攝提示<br/>請正對鏡頭]
    HINT --> UP
    POSE -->|通過| JOB[建立非同步 job<br/>POST /v1/face/jobs/basic]

    JOB --> POLL[前端輪詢<br/>GET /v1/face/jobs/id]
    POLL -->|processing| POLL
    POLL -->|completed| RESULT[五官結果 + 膚色四季型]

    RESULT --> FIX{使用者覺得判錯？}
    FIX -->|是| FEED[送出修正<br/>POST .../feedback<br/>存 face_feedback]
    FEED --> CACHE[face_corrections 記住<br/>下次同一張臉套用]
    FIX -->|否| STYLE

    RESULT --> STYLE[選擇風格 style<br/>日常／港風／韓系…]
    STYLE --> SUG[妝容建議 suggestion<br/>Ollama 生成五段文字]
    SUG --> REN[妝後圖 ai-render<br/>Replicate 生成]
    REN --> CMP[對比 compare<br/>妝前 vs 妝後]

    RESULT --> PROD[商品推薦 products<br/>依五官與四季型]
    PROD --> FAV[收藏 favorites]

    CMP --> SAVE[收藏妝容<br/>saved-looks]
    SAVE --> PROF[會員中心 profile<br/>統計卡可點擊跳轉]
    FAV --> PROF

    style ERR1 fill:#f8d7da
    style HINT fill:#fff3cd
    style RESULT fill:#d4edda
```

---

## 三、API 串接圖（非同步分析的完整時序）

```mermaid
sequenceDiagram
    autonumber
    participant U as 瀏覽器
    participant G as ai-gateway
    participant F as face-basic
    participant S as Firestore<br/>job_store
    participant C as face_corrections

    U->>G: POST /face-basic/v1/face/jobs/basic<br/>multipart: file<br/>Cookie: __session
    Note over G: 1. 驗 session（多槽 cookie）<br/>2. 驗 CSRF<br/>3. X-Expected-Actor 隔離<br/>4. is_path_allowed 白名單

    G->>F: 轉發 + OIDC identity token<br/>+ x-api-key（Secret Manager）
    Note over F: enforce_service_api_key<br/>缺金鑰則拒絕啟動（fail closed）

    F->>F: _read_clean_image<br/>→ sanitize_upload(bytes, mime)
    F->>S: create(job, status=queued)
    F-->>G: 202 {jobId, resultToken, expiresAt}
    G-->>U: 202（同上）

    Note over F: 背景執行緒開始分析

    F->>F: InsightFace 偵測 + 姿態
    F->>F: MediaPipe FaceMesh 468 landmark
    F->>F: face_roi.crop_roi × 5 部位
    F->>F: ONNX CNN 推論（5 個部位）
    F->>F: DINOv2 backbone × 3<br/>（face/eye/nose ROI）
    Note over F: 眼型取 DINOv2 4 類頭<br/>鼻型取「幾何⊕DINOv2」389 維融合頭

    F->>C: apply(result, image_hash)<br/>套用此臉先前的修正
    Note over C: 只存 SHA-256，不存影像<br/>result["_modelRaw"] 保留模型原始輸出
    F->>S: update(job, status=completed, result)

    loop 前端輪詢
        U->>G: GET /face-basic/v1/face/jobs/{id}/result<br/>X-Job-Token
        G->>F: 轉發
        F->>S: get(job)
        alt 尚未完成
            F-->>U: {error: job 尚未完成, status: processing}
        else 完成
            F-->>U: 200 {result, _modelRaw, 分類來源}
        end
    end

    opt 使用者修正
        U->>G: POST .../feedback {眼型: 圓眼}
        G->>F: 轉發
        Note over F: 驗證值必須出自現行分類表
        F->>S: face_feedback 寫入 _modelRaw 當 predicted
        F->>C: remember(hash, corrections)
    end
```

### 為什麼是非同步

同步 `/v1/face/analyze/basic` 仍然保留，但正式流程走 job：
完整分析含 InsightFace + MediaPipe + 5 次 ONNX + 3 次 DINOv2，
在 Cloud Run 的請求逾時下容易被砍。Job 立刻回 `jobId`，前端輪詢。

- Job TTL：`FACE_JOB_RETENTION_SECONDS = 3600`（1 小時）
- 併發上限：`FACE_JOB_MAX_COUNT = 200`
- 單次分析逾時：`FACE_JOB_TIMEOUT_SECONDS = 180`

---

## 四、使用案例圖

```mermaid
flowchart LR
    subgraph actors["參與者"]
        M(("會員"))
        V(("訪客"))
        A(("管理員"))
        SYS(("排程／系統"))
    end

    subgraph uc["DECORATE ME 系統"]
        U1["瀏覽首頁與風格"]
        U2["註冊／登入"]
        U3["上傳照片做臉部分析"]
        U4["修正五官判斷結果"]
        U5["取得妝容文字建議"]
        U6["生成妝後對比圖"]
        U7["收藏商品"]
        U8["收藏妝容"]
        U9["查看分析紀錄"]
        U10["每日打卡賺點數"]
        U11["點數商店兌換主題"]
        U12["管理商品資料"]
        U13["查看管理操作稽核"]
        U14["清理過期 job 與媒體"]
    end

    V --> U1
    V --> U2
    M --> U1
    M --> U3
    M --> U4
    M --> U5
    M --> U6
    M --> U7
    M --> U8
    M --> U9
    M --> U10
    M --> U11
    A --> U12
    A --> U13
    SYS --> U14

    U3 -.->|include| U2
    U5 -.->|include| U3
    U6 -.->|include| U3
    U4 -.->|extend| U3
    U8 -.->|include| U6

    style uc fill:#faf8f3
```

| 使用案例 | 前置條件 | 主要流程 | 例外 |
| --- | --- | --- | --- |
| U3 臉部分析 | 已登入、有正面照 | 上傳 → 安全檢查 → 姿態檢查 → 建 job → 輪詢 → 顯示五官 | 無臉／多臉／姿態超標／檔案超限 → 4xx 並給提示 |
| U4 修正判斷 | 已有 job 結果 | 選新類別 → 驗證屬現行分類表 → 寫 `face_feedback` | 撤回修正必須**刪除**紀錄，不能只是不寫 |
| U6 妝後圖 | 已有分析與風格 | 建 render job → Replicate → 下載進私有 GCS → 回短效網址 | 第三方失敗需回收孤兒物件 |

---

## 五、甘特圖（專題開發歷程）

```mermaid
gantt
    title DECORATE ME 專題全組分工與整合時程
    dateFormat YYYY-MM-DD
    axisFormat %Y/%m

    section 全組共同
    題目確認、需求訪談與功能盤點       :done, all1, 2025-10-01, 45d
    系統架構與各端 API 契約            :done, all2, 2025-11-15, 45d
    跨組串接、資安檢查與缺陷修正       :done, all3, 2026-06-01, 61d
    全系統 E2E、Demo 演練與文件統整     :done, all4, 2026-07-15, 49d

    section Web 前端與管理介面
    UI／UX、頁面流程與響應式版面       :done, fe1, 2025-11-01, 75d
    SPA 路由、登入與分析流程            :done, fe2, 2026-01-15, 90d
    商品、收藏、會員中心與管理後台      :done, fe3, 2026-04-01, 90d
    Gateway 遷移、資安與 E2E 修正       :done, fe4, 2026-06-15, 48d

    section AI 臉部分析與模型
    資料蒐集、人工標註與清理            :done, ai1, 2025-10-15, 105d
    幾何基線、ROI 與 identity split     :done, ai2, 2026-01-15, 75d
    CNN／DINOv2／5-fold 模型實驗         :done, ai3, 2026-03-01, 105d
    類別合併、ONNX、BASIC／PRO 部署      :done, ai4, 2026-06-01, 61d

    section Ollama 文字建議與生成
    妝容知識、Prompt 與輸出格式         :done, ol1, 2026-02-01, 60d
    Ollama API、串流與 Tunnel            :done, ol2, 2026-04-01, 75d
    Gateway 串接、fallback 與驗收        :done, ol3, 2026-06-15, 48d

    section 會員與資料庫
    會員 Schema、註冊登入與 OTP          :done, db1, 2025-12-01, 90d
    Session、角色、點數與會員功能        :done, db2, 2026-03-01, 105d
    收藏、歷史、刪除與管理 API           :done, db3, 2026-05-15, 79d
    Gateway Session 契約與整合修正       :done, db4, 2026-07-01, 33d

    section 商品資料與爬蟲
    商品欄位、分類與資料來源整理         :done, pc1, 2026-01-01, 90d
    商品爬蟲、清理、去重與 staging       :done, pc2, 2026-04-01, 90d
    商品 CRUD、審核匯入與 audit log      :done, pc3, 2026-06-15, 48d

    section 商品推薦演算法
    推薦需求、輸入特徵與評分規則         :done, rec1, 2026-02-15, 75d
    analysisPackage 與商品 API 串接       :done, rec2, 2026-05-01, 61d
    風格關鍵字、類別配額與結果校正       :done, rec3, 2026-07-01, 33d

    section AI 妝後圖與媒體
    生成模型選型、Prompt 與介面          :done, ren1, 2026-03-01, 75d
    Replicate Job、GCS 與結果呈現         :done, ren2, 2026-05-15, 61d
    Quota、Dedup、媒體刪除與隱私驗收     :done, ren3, 2026-07-01, 33d

    section Gateway、雲端與系統整合
    Docker、Cloud Run 與 Firebase 部署    :done, ops1, 2026-03-15, 90d
    Gateway、IAM、Secret 與同源代理       :done, ops2, 2026-05-15, 61d
    效能調校、監控、回復與 Demo 驗收      :done, ops3, 2026-07-01, 49d
```

> 此甘特圖呈現全組各端的平行工作與整合節點，不代表單一成員的個人開發歷程。模型逐代實驗的詳細時程另見模型開發歷程分冊。

---

## 六、活動圖（一次完整分析的內部流程）

```mermaid
flowchart TD
    ST([收到 multipart 上傳]) --> A1[分段讀取<br/>超過 MAX_UPLOAD_BYTES 立即停]
    A1 --> A2{magic bytes<br/>白名單}
    A2 -->|不符| E1[400 檔案格式不支援]
    A2 -->|符合| A3{解碼前檢查像素數<br/>擋解壓縮炸彈}
    A3 -->|超限| E2[400 影像過大]
    A3 -->|通過| A4[移除 EXIF／GPS<br/>回傳 bytes, mime]

    A4 --> B1[InsightFace 偵測人臉]
    B1 --> B2{偵測到臉？}
    B2 -->|否| B3[補 BORDER_REPLICATE 邊框重試<br/>臉貼齊畫面時 anchor 偵測器會漏]
    B3 --> B4{仍偵測不到？}
    B4 -->|是| E3[400 未偵測到人臉]
    B4 -->|否| B5
    B2 -->|是| B5[取面積最大的臉]

    B5 --> C1[計算 yaw / pitch]
    C1 --> C2{超出角度門檻？}
    C2 -->|是| C3[標記 lowConfidenceFields<br/>並給拍攝提示]
    C2 -->|否| C4
    C3 --> C4[MediaPipe FaceMesh<br/>468 landmark]

    C4 --> D1[face_roi.crop_roi<br/>臉型 128×128<br/>其餘 96×96]
    D1 --> D2[五個部位 ONNX CNN 推論]
    D1 --> D3[三個部位 DINOv2<br/>224×224 ROI → backbone]

    D3 --> D4{該部位是<br/>DINOv2 正式答案？}
    D4 -->|眼型| D5[4 類線性頭<br/>384 維]
    D4 -->|鼻型| D6[融合頭 389 維<br/>384 embedding + 5 幾何]
    D4 -->|臉型| D7[僅記錄 shadow<br/>正式答案用 CNN]

    D2 --> F1[彙整五官結果]
    D5 --> F1
    D6 --> F1
    D7 --> F1

    F1 --> F2[膚色 LAB → 四季型]
    F2 --> F3[臉部對稱性計算]
    F3 --> G1[face_corrections.apply<br/>套用此臉先前的修正]
    G1 --> G2[canonical_label 正規化<br/>舊類別名換成現行]
    G2 --> G3[_modelRaw 保留模型原始輸出]
    G3 --> G4[analysis_package.normalize<br/>中文標籤 → 英文代碼]
    G4 --> EN([寫入 job_store, status=completed])

    style E1 fill:#f8d7da
    style E2 fill:#f8d7da
    style E3 fill:#f8d7da
    style C3 fill:#fff3cd
    style G3 fill:#d1ecf1
```

### 活動圖裡三個非顯而易見的設計

**1. `BORDER_REPLICATE` 重試（B3）**
資料集是截圖裁出的大頭照，臉常貼齊畫面邊緣。RetinaFace 這類 anchor-based 偵測器
需要臉周圍留餘裕才框得住，直接餵原圖**實測有四成漏掉（545/1297）**。
補一圈複製邊框等於把臉「縮小」回偵測器習慣的比例，實測全數救回。

**2. `_modelRaw` 必須保留（G3）**
套用修正快取後，畫面顯示的是使用者的答案。若回饋面板把那個值當成 `predicted`
送回去，訓練資料會變成**模型在確認自己**——集合被自我循環的假資料填滿，
重訓分數憑空變好，而且從外面完全看不出來。

**3. `canonical_label` 在入口正規化（G2）**
`face_corrections` 存的是使用者當初送出的標籤，而分類表後來合併過。
不換的話，一個已經不存在的類別會被寫進結果，一路流到建議 prompt 與渲染 prompt——
那些對照表只認現行類別，查不到就整句略過，**不報錯**。
